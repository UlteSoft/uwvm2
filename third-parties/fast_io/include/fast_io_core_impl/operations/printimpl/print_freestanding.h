#pragma once

/*
 * Freestanding print execution pipeline (IO operation core).
 *
 * Upstream callers are either a public IO scenario facade or a lower-level
 * caller with an explicit output. The public
 * `operations::print_freestanding` entry validates the complete source record,
 * obtains `output_stream_ref`, and preserves the original source expressions
 * long enough for the pre-normalization compiler-constant query. Each value is
 * then transformed exactly once by `io_print_alias` and
 * `io_print_forward<output_char_type>`.
 *
 * The normalized record is expanded as semantic IO structure (`pack`, `cond`,
 * `io_null`, and `width`), given stable owned-or-borrowed storage, synchronized
 * through the stream mutex protocol, and offered first to a whole-record
 * `status_print_define` customization. The generic continuation otherwise
 * selects among reserve, dynamic-reserve, scatter, reserve-scatter,
 * context-print, buffered/direct, and staged strategies before reaching the
 * primitive write/obuffer operations.
 *
 * Namespace names describe normalization state rather than a simple height
 * hierarchy: `operations` contains the public generic operation,
 * `operations::defines` validates public expressions,
 * `operations::decay` operates on normalized observers and values, and
 * `operations::decay::defines` probes raw capabilities. Helpers named
 * `print_freestanding_*` are continuations of this one operation, not additional
 * public CPO entry points. `__builtin_constant_p` handling in this file is an IO
 * code-generation policy; it is deliberately evaluated before aliasing so
 * caller-expression evidence is not erased.
 */

#include "scatter_copy.h"

namespace fast_io
{

namespace manipulators
{
/// @brief Forward declaration of the explicit one-character semantic carrier.
/// @details The complete type is supplied by the pointer/scatter manipulator layer.
template <typename T>
struct chvw_t;

/// @brief Forward declaration of a borrowed character scatter with exact type-level extent.
/// @details The complete type owns no characters.
template <::std::integral char_type, ::std::size_t extent>
struct static_scatter_t;

/// @brief Forward declaration of a bounded C-string scatter used by compiler-constant planning.
/// @details The complete type carries a run-time length no greater than `extent`.
template <::std::integral char_type, ::std::size_t extent>
struct bounded_cstr_scatter_t;
} // namespace manipulators

namespace details
{
template <::std::integral char_type, ::fast_io::details::my_unsigned_integral T>
#if __has_cpp_attribute(__gnu__::__always_inline__)
[[__gnu__::__always_inline__]]
#elif __has_cpp_attribute(msvc::forceinline)
[[msvc::forceinline]]
#endif
inline constexpr char_type *print_fixed_decimal_integral_define(char_type *, T) noexcept;

} // namespace details

namespace operations::decay
{
template <bool line, ::std::integral char_type, typename output,
		  typename... Args>
inline consteval bool
print_semantic_optional_scatter_width_coalescing_plan_available() noexcept;
}

namespace details::decay
{


/** Preserves the readable extent carried by a bounded C-string after formatting has measured its run-time length. */
template <typename T>
struct print_bounded_cstr_scatter_traits
{
	inline static constexpr bool available{};
};

template <::std::integral char_type, ::std::size_t extent>
struct print_bounded_cstr_scatter_traits<
	::fast_io::manipulators::bounded_cstr_scatter_t<char_type, extent>>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t readable_extent{extent};
};

template <::std::integral char_type>
inline constexpr ::std::size_t print_contiguous_char_extent_add_or_unavailable(
	::std::size_t left, ::std::size_t right) noexcept;

/// @brief    Reusable scatter descriptor for a single trailing newline.
/// @details  The void specialization reports byte count for byte-scatter APIs; typed scatter descriptors report
///           character count for character-scatter APIs.
/// @tparam   char_type the character type of the newline literal
/// @tparam   T         the scatter value type, or void for byte scatter output
template <::std::integral char_type, typename T = char_type>
inline constexpr basic_io_scatter_t<T> line_scatter_common{__builtin_addressof(char_literal_v<u8'\n', char_type>),
														   ::std::same_as<T, void> ? sizeof(char_type) : 1};

/// @brief Exposes the exact extent of a fixed scatter in the output character domain.
/// @details The primary template deliberately rejects a scatter whose code-unit type differs from the destination.
///          The trait is available before the run scanner so an unbuffered endpoint can retain a fixed scatter's
///          original storage, while buffered and string-construction paths continue to use its exact reserve protocol.
template <::std::integral output_char_type, typename T>
struct print_static_scatter_traits
{
	inline static constexpr bool available{};
};

template <::std::integral char_type, ::std::size_t extent>
struct print_static_scatter_traits<
	char_type, ::fast_io::manipulators::static_scatter_t<char_type, extent>>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t size{extent};

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type>
	define(::fast_io::manipulators::static_scatter_t<
		   char_type, extent> const &value) noexcept
	{
		return {value.base, extent};
	}
};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_provider_recipe provider_type,
		  ::std::size_t offset, ::std::size_t extent>
	requires ::std::same_as<char_type, typename provider_type::char_type>
struct print_static_scatter_traits<
	char_type,
	::fast_io::manipulators::static_provider_node<
		provider_type, offset, extent>>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t size{extent};

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type>
	define(::fast_io::manipulators::static_provider_node<
		   provider_type, offset, extent> const &) noexcept
	{
		return {
			::fast_io::manipulators::static_provider_storage_t<
				provider_type>::storage.data() +
				offset,
			extent};
	}
};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_provider_recipe provider_type,
		  ::std::size_t offset, ::std::size_t extent>
	requires ::std::same_as<char_type, typename provider_type::char_type>
struct print_static_scatter_traits<
	char_type,
	::fast_io::manipulators::syntax_transport_details::static_provider_scatter_node<
		provider_type, offset, extent>>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t size{extent};

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type>
	define(::fast_io::manipulators::syntax_transport_details::static_provider_scatter_node<
		   provider_type, offset, extent> const &) noexcept
	{
		return {
			::fast_io::manipulators::static_provider_storage_t<
				provider_type>::storage.data() +
				offset,
			extent};
	}
};

/// @brief Owns one already-materialized compiler-constant proxy while exposing only its immutable descriptor spelling.
/// @details The wrapper exists solely in a proven per-leaf true arm.  Its payload descriptors are supplied by the
///          proxy's static-fragment protocol and remain valid independently of this object, while the wrapper itself
///          keeps any value state needed to select the correct static table entries alive through synchronous output.
template <::std::integral char_type, typename proxy_type>
struct print_compiler_constant_static_fragment_proxy
{
	proxy_type value;
};

template <typename T>
struct print_compiler_constant_static_fragment_proxy_traits
	: ::std::false_type
{};

template <::std::integral char_type, typename proxy_type>
struct print_compiler_constant_static_fragment_proxy_traits<
	print_compiler_constant_static_fragment_proxy<char_type, proxy_type>>
	: ::std::true_type
{};

template <::std::integral char_type, typename proxy_type>
	requires ::fast_io::compiler_constant_static_fragment_printable<
		char_type, proxy_type>
inline constexpr ::fast_io::reserve_scatters_size_result
	print_reserve_scatters_size(
		::fast_io::io_reserve_type_t<
			char_type,
			print_compiler_constant_static_fragment_proxy<char_type, proxy_type>>) noexcept
{
	return {
		print_compiler_constant_static_fragments_size(
			::fast_io::io_reserve_type<char_type, proxy_type>),
		0u};
}

template <::std::integral char_type, typename proxy_type>
	requires ::fast_io::compiler_constant_static_fragment_printable<
		char_type, proxy_type>
inline constexpr ::fast_io::basic_reserve_scatters_define_result<char_type>
print_reserve_scatters_define(
	::fast_io::io_reserve_type_t<
		char_type,
		print_compiler_constant_static_fragment_proxy<char_type, proxy_type>>,
	::fast_io::basic_io_scatter_t<char_type> *scatters,
	char_type *reserve,
	print_compiler_constant_static_fragment_proxy<
		char_type, proxy_type> const &value) noexcept
{
	return {
		print_compiler_constant_static_fragments_define(
			::fast_io::io_reserve_type<char_type, proxy_type>, scatters,
			value.value),
		reserve};
}

template <::std::integral char_type, typename proxy_type>
	requires ::fast_io::compiler_constant_static_fragment_printable<
		char_type, proxy_type>
inline constexpr ::std::true_type print_borrowed_reserve_scatters_source(
	::fast_io::io_reserve_type_t<
		char_type,
		print_compiler_constant_static_fragment_proxy<char_type, proxy_type>>) noexcept
{
	return {};
}

/// @brief Tests whether the first `count` normalized leaves contain a selected compiler-constant fragment proxy.
/// @details Ordinary `static_scatter_t` format literals do not activate this policy: a mixed run whose scalar query
///          was rejected must retain the historical contiguous materializer. Complete literal-only runs already take
///          the earlier immutable-fragment endpoint. Only a proxy created by a successful per-leaf/full true arm needs
///          the mature dispatcher to preserve its provider-owned character storage. The position fold denotes the same
///          closed prefix as recursive head removal, but forms the source pack once. This avoids quadratic
///          template-argument metadata for large records and remains a portable C++20 proof without compiler pack-indexing
///          support.
template <::std::size_t count, ::std::integral char_type, typename... Args>
	requires(count <= sizeof...(Args))
struct print_first_n_contains_static_fragment
	: ::std::bool_constant<[]() consteval {
		::std::size_t position{};
		return (false || ... ||
				(position++ < count &&
				 ::fast_io::details::decay::
					 print_compiler_constant_static_fragment_proxy_traits<
						 ::std::remove_cvref_t<Args>>::value));
	}()>
{};

/// @brief    Describes the first contiguous print run that can be represented by scatters and reserve buffers.
struct contiguous_scatter_result
{
	::std::size_t position{};
	::std::size_t neededscatters{};
	::std::size_t neededspace{};
	::std::size_t null{};
	bool lastisreserve{};
	bool hasscatters{};
	bool hasreserve{};
	bool hasdynamicreserve{};
};

/// @brief Proves that one exact scatter expression may be retained while later producers are invoked.
/// @details `scatter_printable` proves only the descriptor's shape. A producer may legally expose shared scratch
///          storage which its next invocation overwrites; collecting two such descriptors before writing would then
///          duplicate the last value. Multi-argument scatter plans therefore require the independent source-lifetime
///          marker. Single-argument dispatch intentionally does not use this predicate because it queries the producer
///          and consumes the descriptor immediately, before any other customization can invalidate it. The expression
///          type is intentionally not decayed: callers must name the category their materializer actually passes.
template <::std::integral char_type, typename expression_type>
inline constexpr bool retained_scatter_printable_v =
	::fast_io::scatter_printable_for<char_type, expression_type> &&
	::fast_io::borrowed_scatter_source<char_type, ::std::remove_cvref_t<expression_type>>;

/// @brief Proves that a reserve-scatters producer can join a retained multi-object descriptor plan.
/// @details Single-object reserve-scatters emission consumes descriptors immediately and needs no marker.  A grouped
///          plan invokes later producers first, so it additionally requires the source's explicit guarantee that no
///          descriptor names scratch those calls can overwrite.
template <::std::integral char_type, typename T>
inline constexpr bool retained_reserve_scatters_printable_v =
	(::fast_io::reserve_scatters_printable<char_type, T> ||
	 ::fast_io::dynamic_reserve_scatters_printable<char_type, T>) &&
	::fast_io::borrowed_reserve_scatters_source<char_type, T>;

#if (__cplusplus > 202302L && __cpp_pack_indexing >= 202311L) || FAST_IO_HAS_BUILTIN(__type_pack_element)

/// @brief Stores the accepted leading source descriptors without repeating a shrinking type pack.
/// @details Indexed member constants preserve the original lazy stop: a source after the first rejected source never
///          instantiates its formatter or size expression. The enclosing pack is shared by all scalar-index members.
template <typename classifier, typename... source_types>
struct print_leading_run_scan_elements
{
	using element_type = typename classifier::element_type;
	template <::std::size_t index>
	using source_type =
#if __cplusplus > 202302L && __cpp_pack_indexing >= 202311L
		source_types...[index];
#else
		__type_pack_element<index, source_types...>;
#endif

	template <::std::size_t index>
	inline static constexpr element_type element{[]() consteval {
		if constexpr (index != 0u)
		{
			if constexpr (element<index - 1u>.stop)
			{
				return element_type{};
			}
			else
			{
				return classifier::template classify<source_type<index>>();
			}
		}
		else
		{
			return classifier::template classify<source_type<index>>();
		}
	}()};

	struct table_type
	{
		element_type values[sizeof...(source_types)];
	};

	template <::std::size_t... index>
	inline static consteval table_type make_table(::std::index_sequence<index...>) noexcept
	{
		return {{element<index>...}};
	}

	inline static constexpr table_type table{
		make_table(::std::make_index_sequence<sizeof...(source_types)>{})};
};

struct contiguous_scatter_scan_element
{
	bool stop{true};
	bool reserve{};
	bool dynamic_reserve{};
	bool scatter{};
	bool null{};
	::std::size_t space{};
	::std::size_t scatters{};
};

template <bool retain_static_scatter, ::std::integral char_type>
struct contiguous_scatter_scan_classifier
{
	using element_type = contiguous_scatter_scan_element;
	template <typename Arg>
	inline static consteval element_type classify() noexcept
	{
		using value_type = ::std::remove_cvref_t<Arg>;
		if constexpr (::fast_io::details::decay::retained_scatter_printable_v<char_type, Arg &> ||
					  (retain_static_scatter && ::fast_io::details::decay::print_static_scatter_traits<char_type, value_type>::available))
		{
			return {false, false, false, true, false, 0u, 1u};
		}
		else if constexpr (::fast_io::reserve_printable<char_type, Arg>)
		{
			constexpr ::std::size_t size{print_reserve_size(::fast_io::io_reserve_type<char_type, Arg>)};
			static_assert(size != 0u);
			return {false, true, false, false, false, size, 1u};
		}
		else if constexpr (::fast_io::dynamic_reserve_printable<char_type, Arg>)
		{
			return {false, false, true, false, false, 0u, 1u};
		}
		else if constexpr (::fast_io::reserve_scatters_printable<char_type, Arg> &&
						   ::fast_io::details::decay::retained_reserve_scatters_printable_v<char_type, Arg>)
		{
			constexpr auto size{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, Arg>)};
			static_assert(size.scatters_size != 0u);
			return {false, true, false, true, false, size.reserve_size, size.scatters_size};
		}
		else if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
		{
			return {false, false, false, false, true, 0u, 0u};
		}
		else
		{
			return {};
		}
	}
};

#endif

/// @brief    Scans a parameter pack for the leading contiguous scatter/reserve-friendly print run.
/// @details  The result records how many arguments can be grouped, how many scatter descriptors and reserve
///           characters are needed, and whether the run contains null or reserve-like outputs.
/// @tparam   char_type the character type used by the print run
/// @tparam   Arg       the current argument type
/// @tparam   Args      the remaining argument types
/// @return   contiguous_scatter_result the compile-time description of the leading run
template <bool retain_static_scatter, ::std::integral char_type, typename Arg, typename... Args>
inline constexpr contiguous_scatter_result find_continuous_scatters_n_impl()
{
#if (__cplusplus > 202302L && __cpp_pack_indexing >= 202311L) || FAST_IO_HAS_BUILTIN(__type_pack_element)
	// Short runs share already-instantiated suffixes across related active records. Indexing only a large run
	// avoids replacing that useful cache with a separate descriptor table for every small condition selection.
	if constexpr (sizeof...(Args) >= 127u)
	{
		using classifier = ::fast_io::details::decay::contiguous_scatter_scan_classifier<retain_static_scatter, char_type>;
		if constexpr (classifier::template classify<Arg>().stop)
		{
			// The historical scanner stops immediately here; do not build scalar metadata for an unreachable tail.
			return {};
		}
		else
		{
			using elements_type = ::fast_io::details::decay::print_leading_run_scan_elements<
				::fast_io::details::decay::contiguous_scatter_scan_classifier<retain_static_scatter, char_type>, Arg, Args...>;
			constexpr auto &elements{elements_type::table.values};
			constexpr ::std::size_t count{sizeof...(Args) + 1u};
			::std::size_t accepted{};
			while (accepted != count && !elements[accepted].stop)
			{
				++accepted;
			}
			contiguous_scatter_result result{};
			while (accepted != 0u)
			{
				auto const current{elements[--accepted]};
				if (current.null)
				{
					::std::size_t const null_count{::fast_io::details::decay::print_strategy_saturating_add(result.null, 1u)};
					if (null_count == SIZE_MAX)
					{
						result = {};
						continue;
					}
					result.null = null_count;
				}
				else
				{
					::std::size_t const scatters{::fast_io::details::decay::print_strategy_extent_add_or_unavailable(
						result.neededscatters, current.scatters)};
					::std::size_t const space{current.reserve ? ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(result.neededspace, current.space) : result.neededspace};
					if (scatters == SIZE_MAX || space == SIZE_MAX)
					{
						result = {};
						continue;
					}
					result.neededscatters = scatters;
					result.neededspace = space;
					result.hasreserve |= current.reserve;
					result.hasdynamicreserve |= current.dynamic_reserve;
					result.hasscatters |= current.scatter;
					// The original rule concerns the complete source-pack tail, not a prefix cut short by a stop or overflow.
					if (accepted + 1u == count && !current.scatter && (current.reserve || current.dynamic_reserve))
					{
						result.lastisreserve = true;
					}
				}
				++result.position;
			}
			return result;
		}
	}
	else
#endif
	{

		contiguous_scatter_result ret{};
		using value_type = ::std::remove_cvref_t<Arg>;
		constexpr bool static_scatter{
			::fast_io::details::decay::print_static_scatter_traits<char_type, value_type>::available};
		if constexpr (
			::fast_io::details::decay::retained_scatter_printable_v<char_type, Arg &> ||
			(retain_static_scatter && static_scatter))
		{
			// A scatter-printable argument contributes one existing output range to the contiguous run.
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first so the current argument can extend the leading run.
				ret = find_continuous_scatters_n_impl<retain_static_scatter, char_type, Args...>();
			}
			constexpr ::std::size_t one{1u};
			::std::size_t const neededscatters{
				::fast_io::details::decay::print_strategy_extent_add_or_unavailable(ret.neededscatters, one)};
			if (neededscatters == SIZE_MAX)
			{
				// Descriptor aggregation is optional.  Splitting here lets the dispatcher emit the current object and retry
				// the tail instead of instantiating an unrepresentable retained descriptor array.
				return {};
			}
			++ret.position;
			ret.hasscatters = true;
			ret.neededscatters = neededscatters;
		}
		else if constexpr (::fast_io::reserve_printable<char_type, Arg>)
		{
			// A static reserve-printable argument contributes one materialized range and a known reserve size.
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first to preserve the aggregate run accounting.
				ret = find_continuous_scatters_n_impl<retain_static_scatter, char_type, Args...>();
			}
			constexpr ::std::size_t sz{print_reserve_size(::fast_io::io_reserve_type<char_type, Arg>)};
			static_assert(sz != 0);
			constexpr ::std::size_t one{1u};
			::std::size_t const neededscatters{
				::fast_io::details::decay::print_strategy_extent_add_or_unavailable(ret.neededscatters, one)};
			::std::size_t const neededspace{
				::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					ret.neededspace, sz)};
			if (neededscatters == SIZE_MAX || neededspace == SIZE_MAX)
			{
				// Every individual reserve producer remains valid; only this contiguous combination is declined.
				return {};
			}
			if constexpr (sizeof...(Args) == 0)
			{
				// A trailing reserve argument can be emitted directly without an extra scatter continuation.
				ret.lastisreserve = true;
			}
			++ret.position;
			ret.neededscatters = neededscatters;
			ret.neededspace = neededspace;
			ret.hasreserve = true;
		}
		else if constexpr (::fast_io::dynamic_reserve_printable<char_type, Arg>)
		{
			// A dynamic reserve-printable argument is part of the run, but its reserve size is measured later.
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first so this dynamic reserve extends the same leading run.
				ret = find_continuous_scatters_n_impl<retain_static_scatter, char_type, Args...>();
			}
			constexpr ::std::size_t one{1u};
			::std::size_t const neededscatters{
				::fast_io::details::decay::print_strategy_extent_add_or_unavailable(ret.neededscatters, one)};
			if (neededscatters == SIZE_MAX)
			{
				return {};
			}
			if constexpr (sizeof...(Args) == 0)
			{
				// A trailing dynamic reserve argument can be handled as the final materialized output.
				ret.lastisreserve = true;
			}
			++ret.position;
			ret.neededscatters = neededscatters;
			ret.hasdynamicreserve = true;
		}
		else if constexpr (
			::fast_io::reserve_scatters_printable<char_type, Arg> &&
			::fast_io::details::decay::retained_reserve_scatters_printable_v<char_type, Arg>)
		{
			// A reserve-scatters argument contributes its declared scatter count and reserve storage requirement.
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first to accumulate the complete leading run.
				ret = find_continuous_scatters_n_impl<retain_static_scatter, char_type, Args...>();
			}
			constexpr auto scatszres{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, Arg>)};
			static_assert(scatszres.scatters_size != 0);
			::std::size_t const neededspace{
				::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					ret.neededspace, scatszres.reserve_size)};
			::std::size_t const neededscatters{
				::fast_io::details::decay::print_strategy_extent_add_or_unavailable(ret.neededscatters,
																					scatszres.scatters_size)};
			if (neededspace == SIZE_MAX || neededscatters == SIZE_MAX)
			{
				// A retained reserve-scatter plan is only a batching optimization; an oversized aggregate is split safely.
				return {};
			}
			ret.hasscatters = true;
			ret.hasreserve = true;
			++ret.position;
			ret.neededspace = neededspace;
			ret.neededscatters = neededscatters;
		}
		else if constexpr (::std::same_as<::std::remove_cvref_t<Arg>, ::fast_io::io_null_t>)
		{
			// Null output is counted so pack positions remain correct while no emitted characters are reserved.
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first before adding this null position to the run.
				ret = find_continuous_scatters_n_impl<retain_static_scatter, char_type, Args...>();
			}
			::std::size_t const null_count{
				::fast_io::details::decay::print_strategy_saturating_add(ret.null, static_cast<::std::size_t>(1u))};
			if (null_count == SIZE_MAX)
			{
				return {};
			}
			++ret.position;
			ret.null = null_count;
		}
		else if constexpr (::fast_io::printable<char_type, Arg>)
		{
			// A generic printable argument stops the scatter/reserve run because it needs the normal emit path.
		}
		return ret;
	}
}

/// @brief Scans a run with the historical reserve preference for fixed scatters.
template <::std::integral char_type, typename Arg, typename... Args>
inline constexpr contiguous_scatter_result find_continuous_scatters_n()
{
	return ::fast_io::details::decay::find_continuous_scatters_n_impl<false, char_type, Arg, Args...>();
}

/// @brief Scans a native-scatter run while retaining fixed scatters in their original static storage.
template <::std::integral char_type, typename Arg, typename... Args>
inline constexpr contiguous_scatter_result find_continuous_native_scatters_n()
{
	return ::fast_io::details::decay::find_continuous_scatters_n_impl<true, char_type, Arg, Args...>();
}

/// @brief    Describes a contiguous reserve-only or scatter-only prefix inside a print run.
struct scatter_rsv_result
{
	::std::size_t position{};
	::std::size_t neededspace{};
	::std::size_t null{};
};

/// @brief Classifies one type for the reserve-only or scatter-only run scanner.
enum class scatter_rsv_scan_kind : unsigned char
{
	stop,
	reserve,
	scatter,
	null
};

/// @brief Holds the non-recursive per-type input consumed by the contiguous run scan.
struct scatter_rsv_scan_element
{
	scatter_rsv_scan_kind kind{};
	::std::size_t neededspace{};
};

/// @brief Classifies one argument without recursively instantiating the remainder of the parameter pack.
template <bool findscatter, ::std::integral char_type, typename Arg>
inline constexpr scatter_rsv_scan_element find_continuous_scatters_reserve_element()
{
	if constexpr (::fast_io::reserve_printable<char_type, Arg> && !findscatter)
	{
		return {
			scatter_rsv_scan_kind::reserve,
			print_reserve_size(::fast_io::io_reserve_type<char_type, Arg>)};
	}
	else if constexpr (
		::fast_io::details::decay::retained_scatter_printable_v<char_type, Arg &> && findscatter)
	{
		return {scatter_rsv_scan_kind::scatter, 0u};
	}
	else if constexpr (::std::same_as<::std::remove_cvref_t<Arg>, ::fast_io::io_null_t>)
	{
		return {scatter_rsv_scan_kind::null, 0u};
	}
	else
	{
		return {};
	}
}

/// @brief    Finds a contiguous reserve or scatter subrun used while building scatter descriptors.
/// @details  The findscatter flag selects whether the scan accepts scatter-printable arguments or reserve-printable
///           arguments, while nulls are counted and skipped in either mode.
/// @tparam   findscatter true to scan scatter-printable arguments, false to scan reserve-printable arguments
/// @tparam   char_type   the character type used by the print run
/// @tparam   Arg         the current argument type
/// @tparam   Args        the remaining argument types
/// @return   scatter_rsv_result the accepted run length, reserve space, and null count
template <bool findscatter, ::std::integral char_type, typename Arg, typename... Args>
inline constexpr scatter_rsv_result find_continuous_scatters_reserve_n()
{
	// A per-type fold keeps template-instantiation depth constant even for generated records with hundreds of fields.
	// The accepted prefix is then reduced from right to left so overflow resets and null accounting remain identical to
	// the historical recursive scan; only its compile-time representation changes.
	constexpr scatter_rsv_scan_element elements[]{
		::fast_io::details::decay::find_continuous_scatters_reserve_element<findscatter, char_type, Arg>(),
		::fast_io::details::decay::find_continuous_scatters_reserve_element<findscatter, char_type, Args>()...};
	constexpr ::std::size_t element_count{sizeof...(Args) + 1u};
	::std::size_t accepted{};
	for (; accepted != element_count; ++accepted)
	{
		if (elements[accepted].kind == scatter_rsv_scan_kind::stop)
		{
			break;
		}
	}

	scatter_rsv_result result{};
	while (accepted != 0u)
	{
		auto const element{elements[--accepted]};
		switch (element.kind)
		{
		case scatter_rsv_scan_kind::reserve:
		{
			::std::size_t const neededspace{
				::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					result.neededspace, element.neededspace)};
			if (neededspace == SIZE_MAX)
			{
				result = {};
				break;
			}
			++result.position;
			result.neededspace = neededspace;
			break;
		}
		case scatter_rsv_scan_kind::scatter:
			++result.position;
			break;
		case scatter_rsv_scan_kind::null:
		{
			::std::size_t const null_count{
				::fast_io::details::decay::print_strategy_saturating_add(result.null, 1u)};
			if (null_count == SIZE_MAX)
			{
				result = {};
				break;
			}
			++result.position;
			result.null = null_count;
			break;
		}
		case scatter_rsv_scan_kind::stop:
			break;
		}
	}
	return result;
}

/// @brief    Checks whether a stream's minimum output buffer size is strictly larger than N.
template <typename output, ::std::size_t N>
inline constexpr bool minimum_buffer_output_stream_require_size_constant_impl =
	(N < obuffer_minimum_size_define(::fast_io::io_reserve_type<typename output::output_char_type, output>));

/// @brief    Detects output streams that declare a minimum put area larger than N.
template <typename output, ::std::size_t N>
concept minimum_buffer_output_stream_require_size_impl =
	::fast_io::operations::decay::defines::has_obuffer_minimum_size_operations<output> &&
	minimum_buffer_output_stream_require_size_constant_impl<output, N>;

/// @brief    Default maximum stack-buffer budget used by freestanding print helpers.
inline constexpr ::std::size_t print_stack_buffer_default_max_bytes{
	::fast_io::details::print_stack_buffer_default_max_bytes};

using default_print_stack_policy = ::fast_io::details::default_print_stack_policy;

/// @brief    Stack-buffer policy alias for freestanding print materialization.
/// @tparam   requested_max_bytes requested maximum stack bytes for the policy
template <::std::size_t requested_max_bytes>
using print_stack_policy = ::fast_io::details::print_stack_policy<requested_max_bytes>;

/// @brief    Returns the maximum stack-buffer budget in bytes for a print stack policy.
/// @tparam   stack_policy the stack-buffer policy type
/// @return   ::std::size_t the maximum number of stack bytes available to print helpers
template <typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr ::std::size_t print_stack_buffer_max_bytes() noexcept
{
	return ::fast_io::details::print_stack_buffer_max_bytes<stack_policy>();
}

/// @brief    Converts a byte stack-buffer budget into an element count for a concrete element type.
/// @tparam   element_type the element stored in the stack buffer
/// @tparam   stack_policy the stack-buffer policy type
/// @return   ::std::size_t the maximum number of elements that fit in the policy budget
template <typename element_type, typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr ::std::size_t print_stack_buffer_max_element_count() noexcept
{
	constexpr ::std::size_t max_bytes{::fast_io::details::decay::print_stack_buffer_max_bytes<stack_policy>()};
	if constexpr (max_bytes < sizeof(element_type))
	{
		// The policy budget cannot hold even one element, so stack materialization is disabled.
		return 0u;
	}
	else
	{
		// The policy budget is expressed in bytes and is rounded down to a whole element count.
		return max_bytes / sizeof(element_type);
	}
}

/// @brief    Returns the maximum character count for stack buffers in the print path.
/// @tparam   char_type    the character type stored in the buffer
/// @tparam   stack_policy the stack-buffer policy type
/// @return   ::std::size_t the maximum number of characters available on the stack
template <::std::integral char_type, typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr ::std::size_t print_stack_buffer_max_size() noexcept
{
	return ::fast_io::details::decay::print_stack_buffer_max_element_count<char_type, stack_policy>();
}

/// @brief    Tests whether a requested fixed-size stack buffer is permitted by the print stack policy.
/// @tparam   requested_size the requested element count
/// @tparam   element_type   the element stored in the stack buffer
/// @tparam   stack_policy   the stack-buffer policy type
template <::std::size_t requested_size, typename element_type, typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr bool print_stack_buffer_size_within_limit =
	requested_size != 0 &&
	requested_size <= ::fast_io::details::decay::print_stack_buffer_max_element_count<element_type, stack_policy>();

/// @brief    Clamps a dynamic reserve producer's static stack hint to the active print stack budget.
/// @details  Dynamic producers may appear many times in one print run, so their local stack hints are bounded by the
///           print policy before a shared stack materialization path uses them.
/// @tparam   static_stack_size the producer-declared static stack size
/// @tparam   char_type         the character type stored in the stack buffer
/// @tparam   stack_policy      the stack-buffer policy type
/// @return   ::std::size_t     the stack size allowed for this producer
template <::std::size_t static_stack_size, ::std::integral char_type, typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr ::std::size_t dynamic_print_reserve_static_stack_budget() noexcept
{
	/*
	A dynamic producer's static stack size is only a local materialization hint.
	A print run can contain many dynamic producers, so directly merging all hints into one
	stack array can create a large frame and defeat the stack-safety purpose of this path.
	*/
	constexpr ::std::size_t run_cap{::fast_io::details::decay::print_stack_buffer_max_size<char_type, stack_policy>()};
	if constexpr (static_stack_size < run_cap)
	{
		// The producer's hint already fits inside the active stack-buffer policy.
		return static_stack_size;
	}
	else
	{
		// The producer's hint is capped so one dynamic output cannot dominate the stack frame.
		return run_cap;
	}
}

/// @brief    Computes the bounded static stack size for one dynamic reserve-printable argument.
/// @tparam   line         true when a trailing newline must fit in the same materialization buffer
/// @tparam   char_type    the character type stored in the stack buffer
/// @tparam   T            the dynamic reserve-printable argument type
/// @tparam   stack_policy the stack-buffer policy type
/// @return   ::std::size_t the bounded stack size, or zero when no static hint is available
template <bool line, ::std::integral char_type, typename T, typename stack_policy = ::fast_io::details::default_print_stack_policy>
inline constexpr ::std::size_t dynamic_print_reserve_static_stack_size()
{
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, nocvreft>)
	{
		// Dynamic reserve producers with a static hint can use bounded stack materialization.
		constexpr ::std::size_t static_stack_size{
			print_reserve_static_stack_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
		static_assert(!line || static_stack_size != SIZE_MAX);
		constexpr ::std::size_t payload_budget{
			::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<
				static_stack_size, char_type, stack_policy>()};
		if constexpr (line)
		{
			constexpr ::std::size_t run_cap{
				::fast_io::details::decay::print_stack_buffer_max_size<char_type, stack_policy>()};
			// The newline is part of the same automatic array, not an exemption from the byte policy.  When the payload
			// already consumes the cap, retaining the cap makes the measured `size <= stack_buffer_size` admission fail
			// and route the object to dynamic storage instead of constructing a cap-plus-one stack frame.
			if constexpr (run_cap == 0u || run_cap <= payload_budget)
			{
				return run_cap;
			}
			else
			{
				return payload_budget + 1u;
			}
		}
		else
		{
			return payload_budget;
		}
	}
	else
	{
		// Producers without a static hint use the non-static dynamic reserve path.
		return 0;
	}
}

/// @brief    Returns the storage size for one context-printable argument.
/// @details  A producer's advertised window is its usable output extent. `println` adds a distinct trailing slot; it
///           must not steal one character from that extent because a valid one-character producer would otherwise be
///           invoked with an empty range. Types without a declaration use the conservative historical window.
/// @tparam   line      true when storage also needs a distinct trailing-newline slot
/// @tparam   char_type the character type stored in the context buffer
/// @tparam   T         the context-printable argument type
/// @return   ::std::size_t the static context buffer size in characters
template <bool line, ::std::integral char_type, typename T>
inline constexpr ::std::size_t context_print_static_buffer_size() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::context_printable_with_static_buffer_size<char_type, value_type>)
	{
		// A declared static context buffer size is validated before it drives stack allocation.
		constexpr ::std::size_t n{
			print_context_static_buffer_size(::fast_io::io_reserve_type<char_type, value_type>)};
		static_assert(n != 0);
		static_assert(n != SIZE_MAX);
		static_assert(n < static_cast<::std::size_t>(PTRDIFF_MAX));
		// `n != SIZE_MAX` proves that adding the at-most-one line slot cannot wrap; the pointer-domain assertion
		// additionally proves that the producer window can be represented by every cursor difference below.
		return n + static_cast<::std::size_t>(line);
	}
	else
	{
		// Context producers without a declared size use the historical small default buffer.
		return 32u + static_cast<::std::size_t>(line);
	}
}

/// @brief    Cached context buffer size for a context-printable argument.
/// @tparam   line      true when a trailing newline is included in the same context buffer
/// @tparam   char_type the character type stored in the context buffer
/// @tparam   T         the context-printable argument type
template <bool line, ::std::integral char_type, typename T>
inline constexpr ::std::size_t context_print_static_buffer_size_v{
	::fast_io::details::decay::context_print_static_buffer_size<line, char_type, T>()};

/// @brief    Converts character scatter lengths into byte scatter lengths when char_type is wider than one byte.
/// @tparam   sz    sizeof(char_type), constrained to be non-zero
/// @param    first the first scatter descriptor to update
/// @param    last  one past the final scatter descriptor to update
template <::std::size_t sz>
	requires(sz != 0)
inline constexpr void scatter_rsv_update_times(::fast_io::io_scatter_t *first, ::fast_io::io_scatter_t *last) noexcept
{
	if constexpr (sz != 1)
	{
		// Wide-character reserve scatters must report byte counts to byte-oriented scatter APIs.
		for (; first != last; ++first)
		{
			first->len = ::fast_io::details::intrinsics::mul_or_overflow_die(first->len, sz);
		}
	}
}

/// @brief    Result of building byte-oriented reserve-scatters output.
/// @tparam   char_type the character type stored in the temporary reserve buffer
template <::std::integral char_type>
struct basic_reserve_scatters_define_byte_result
{
	io_scatter_t *scatters_pos_ptr;
	char_type *reserve_pos_ptr;
};

/// @brief Tests whether a reserve-scatters producer returned a cursor inside its declared storage slice.
/// @details A returned end cursor may equal `last`. At run time integer addresses avoid relational comparison or
///          subtraction on a potentially unrelated pointer; the stride check rejects a descriptor cursor that points
///          into the middle of an element. During constant evaluation, a conforming producer necessarily returns a
///          pointer into the supplied array, where ordinary pointer ordering is defined.
template <typename element_type>
inline constexpr bool print_reserve_scatters_cursor_in_closed_range(
	element_type *first, element_type *last, element_type *cursor) noexcept
{
	if (::std::is_constant_evaluated())
	{
		return first <= cursor && cursor <= last;
	}
	auto const first_address{reinterpret_cast<::std::uintptr_t>(first)};
	auto const last_address{reinterpret_cast<::std::uintptr_t>(last)};
	auto const cursor_address{reinterpret_cast<::std::uintptr_t>(cursor)};
	return first_address <= cursor_address && cursor_address <= last_address &&
		   (cursor_address - first_address) % sizeof(element_type) == 0u;
}

/// @brief    Builds reserve-scatters output for byte-oriented scatter APIs.
/// @details  A producer with the exact native-byte refinement writes genuine `io_scatter_t` storage directly. Other
///           producers use a real typed scratch array followed by checked, member-wise descriptor conversion. The two
///           paths remain separate because equal ABI layout is not C++ effective-type permission.
/// @tparam   char_type the character type stored in the temporary reserve buffer
/// @tparam   T         the reserve-scatters printable argument type
/// @param    pscatters the byte scatter descriptor storage
/// @param    buffer    the temporary reserve buffer
/// @param    t         the argument to materialize
/// @return   basic_reserve_scatters_define_byte_result<char_type> updated scatter and reserve-buffer cursors
template <::std::integral char_type, typename T>
inline ::fast_io::details::decay::basic_reserve_scatters_define_byte_result<char_type>
prrsvsct_byte_common_rsvsc_impl(io_scatter_t *pscatters, char_type *buffer, T &t)
{
	using value_type = ::std::remove_cvref_t<T>;
	using typed_scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
	constexpr auto capacity{
		print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
	static_assert(capacity.scatters_size != 0u);

	if constexpr (::fast_io::reserve_scatters_bytes_printable<char_type, T>)
	{
		auto const result{print_reserve_scatters_bytes_define(
			::fast_io::io_reserve_type<char_type, value_type>, pscatters, buffer, t)};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				pscatters, pscatters + capacity.scatters_size, result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				buffer, buffer + capacity.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
		{
			// Native byte production changes descriptor representation, not either capacity contract.
			::fast_io::fast_terminate();
		}
		return {result.scatters_pos_ptr, result.reserve_pos_ptr};
	}

	/*
	`basic_io_scatter_t<char_type>` and `io_scatter_t` intentionally have the same ABI layout, but layout compatibility
	does not grant C++ aliasing permission between two distinct class-template specializations. The former implementation
	reinterpreted the byte-descriptor array and let the customization write through the typed pointer. GCC 15 at -O2
	then correctly treated subsequent `io_scatter_t` reads as independent and, in a reserve-scatter -> C FILE workload,
	forwarded uninitialized descriptor words to fwrite_unlocked. Applying `may_alias` to a pointer typedef cannot change
	the effective type of the pointed-to array.

	The customization must therefore receive storage whose real element type matches its CPO signature. The conversion
	below reads each live typed descriptor by value and creates the corresponding byte descriptor by ordinary member
	assignment. This is also why the implementations stay separate instead of sharing storage through an ABI-layout
	claim. Checked multiplication proves that a wide-character length remains representable in the byte descriptor.
	*/
	auto materialize_and_convert = [&](typed_scatter_type *typed_scatters) {
		auto const result{print_reserve_scatters_define(
			::fast_io::io_reserve_type<char_type, value_type>, typed_scatters, buffer, t)};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				typed_scatters, typed_scatters + capacity.scatters_size, result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				buffer, buffer + capacity.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
		{
			// Do not let an invalid customization cursor participate in iteration, subtraction, or a later producer call.
			::fast_io::fast_terminate();
		}
		auto byte_curr{pscatters};
		for (auto typed_curr{typed_scatters}; typed_curr != result.scatters_pos_ptr; ++typed_curr, ++byte_curr)
		{
			*byte_curr = {
				typed_curr->base,
				::fast_io::details::intrinsics::mul_or_overflow_die(typed_curr->len, sizeof(char_type))};
		}
		return ::fast_io::details::decay::basic_reserve_scatters_define_byte_result<char_type>{
			byte_curr, result.reserve_pos_ptr};
	};

	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<
					  capacity.scatters_size, typed_scatter_type>)
	{
		// A bounded typed scratch array keeps the common static plan allocation-free.
		typed_scatter_type typed_scatters[capacity.scatters_size];
		return materialize_and_convert(typed_scatters);
	}
	else
	{
		// Large plans use typed dynamic storage; the byte destination remains independently owned by the caller.
		::fast_io::details::local_operator_new_array_ptr<typed_scatter_type> typed_scatters(capacity.scatters_size);
		return materialize_and_convert(typed_scatters.ptr);
	}
}

/// @brief    Builds byte-oriented reserve-scatters output and returns the final scatter cursor.
/// @tparam   char_type the character type stored in the temporary reserve buffer
/// @tparam   T         the reserve-scatters printable argument type
/// @param    pscatters the byte scatter descriptor storage
/// @param    buffer    the temporary reserve buffer
/// @param    t         the argument to materialize
/// @return   io_scatter_t* one past the final scatter descriptor
template <::std::integral char_type, typename T>
inline auto prrsvsct_byte_common_impl(io_scatter_t *pscatters, char_type *buffer, T &t)
{
	// The returned cursor describes entries that may borrow producer-owned storage. Bind the normalized owner held by
	// the calling print controller; an adapter-local copy would die before the controller performs its scatter write.
	return ::fast_io::details::decay::prrsvsct_byte_common_rsvsc_impl(pscatters, buffer, t).scatters_pos_ptr;
}

/// @brief    Returns the maximum scatter fallback coalescing threshold in characters.
/// @tparam   char_type the output character type
/// @return   ::std::size_t the stack-buffer character limit for scatter fallback coalescing
template <::std::integral char_type>
inline constexpr ::std::size_t print_coalesce_threshold_max_chars() noexcept
{
	return ::fast_io::details::decay::print_stack_buffer_max_size<char_type>();
}

/// @brief    Returns the largest character count that may describe one optional contiguous print buffer.
/// @details  A grouped buffer is traversed with character pointers and is allocated in bytes.  Both the cursor
///           difference and the byte allocation must therefore be representable.  The strict ptrdiff_t boundary
///           matches `print_strategy_extent_add_or_unavailable`: reaching that boundary is not an admissible grouped
///           extent, even though each source range may remain valid on its own.
/// @tparam   char_type the character type stored in the proposed contiguous buffer
/// @return   ::std::size_t the greatest safe optional contiguous character extent
template <::std::integral char_type>
inline constexpr ::std::size_t print_contiguous_char_extent_max_chars() noexcept
{
	constexpr ::std::size_t pointer_limit{static_cast<::std::size_t>(PTRDIFF_MAX)};
	constexpr ::std::size_t pointer_chars{pointer_limit == 0u ? 0u : pointer_limit - 1u};
	constexpr ::std::size_t byte_chars{SIZE_MAX / sizeof(char_type)};
	return pointer_chars < byte_chars ? pointer_chars : byte_chars;
}

/// @brief    Caps an optional whole-output policy to a representable contiguous character extent.
/// @details  Policy values decide whether a one-buffer optimization is attempted; they are not allocation requests.
///           Clamping an oversized customization preserves its intent without allowing SIZE_MAX, a ptrdiff_t-sized
///           value, or a character count whose byte multiplication wraps to approve an impossible allocation.
/// @tparam   char_type the output character type
/// @param    threshold the stream-supplied threshold in characters
/// @return   ::std::size_t a representable threshold, or zero when no grouped extent is possible
template <::std::integral char_type>
inline constexpr ::std::size_t print_contiguous_coalesce_threshold(::std::size_t threshold) noexcept
{
	constexpr ::std::size_t maximum{
		::fast_io::details::decay::print_contiguous_char_extent_max_chars<char_type>()};
	return threshold < maximum ? threshold : maximum;
}

/// @brief    Adds two proposed contiguous character extents, or rejects the optional grouping.
/// @details  The helper treats SIZE_MAX as an unavailable-plan sentinel and checks the byte as well as pointer-domain
///           limit before addition.  It is intentionally separate from fatal allocation arithmetic: callers can
///           still emit the original independent controls when only the aggregate buffer is unavailable.
/// @tparam   char_type the character type of the proposed buffer
/// @param    left      the accumulated extent
/// @param    right     the next extent contribution
/// @return   ::std::size_t the exact sum, or SIZE_MAX when no one-buffer representation exists
template <::std::integral char_type>
inline constexpr ::std::size_t print_contiguous_char_extent_add_or_unavailable(::std::size_t left,
																			   ::std::size_t right) noexcept
{
	constexpr ::std::size_t maximum{
		::fast_io::details::decay::print_contiguous_char_extent_max_chars<char_type>()};
	if (left == SIZE_MAX || right == SIZE_MAX || maximum < left || maximum < right || maximum - right < left)
	{
		return SIZE_MAX;
	}
	return left + right;
}

/// @brief    Reads the scatter fallback coalescing threshold for an output stream.
/// @details  Platform policies normally clamp their defaults to the active stack budget. A custom stream may return a
///           larger threshold; callers then allocate the measured output size dynamically instead of using the stack.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the threshold in characters, or zero when the stream does not opt in
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_scatter_fallback_full_output_threshold() noexcept
{
	if constexpr (::fast_io::scatter_fallback_full_output_threshold_stream<char_type, outputstmtype>)
	{
		// The customization is a required constant expression, so this branch produces a compile-time policy value.
		return ::fast_io::details::decay::print_contiguous_coalesce_threshold<char_type>(
			scatter_fallback_full_output_threshold(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>));
	}
	else
	{
		// Streams without the customization preserve the ordinary scatter-write fallback.
		return 0u;
	}
}

/// @brief    Computes the complete-output coalescing threshold for streams with direct scatter output.
/// @details  This threshold models one native scatter operation versus copying the complete payload followed by one
///           contiguous write. It is deliberately separate from the no-scatter fallback threshold.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the threshold in characters, or zero when disabled
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_scatter_direct_full_output_coalesce_threshold() noexcept
{
	if constexpr (::fast_io::scatter_direct_full_output_coalesce_threshold_stream<char_type, outputstmtype>)
	{
		constexpr ::std::size_t threshold{
			scatter_direct_full_output_coalesce_threshold(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>)};
		constexpr ::std::size_t max_threshold{
			::fast_io::details::decay::print_coalesce_threshold_max_chars<char_type>()};
		if constexpr (threshold < max_threshold)
		{
			return threshold;
		}
		else
		{
			return max_threshold;
		}
	}
	else
	{
		return 0u;
	}
}

/// @brief    Computes the full-output coalescing threshold for an output stream.
/// @details  Full semantic-output coalescing has its own stream customization. Scatter fallback and native-scatter
///           coalescing use different cost models and therefore do not supply a default for this threshold.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the threshold in characters, or zero when both threshold paths are disabled
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_full_output_coalesce_threshold() noexcept
{
	if constexpr (::fast_io::full_output_coalesce_threshold_stream<char_type, outputstmtype>)
	{
		// A stream-specific full-output threshold is capped by the same stack-buffer policy as scatter fallback.
		constexpr ::std::size_t threshold{
			full_output_coalesce_threshold(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>)};
		constexpr ::std::size_t max_threshold{
			::fast_io::details::decay::print_coalesce_threshold_max_chars<char_type>()};
		if constexpr (threshold < max_threshold)
		{
			// The stream threshold already fits within the stack-buffer policy.
			return threshold;
		}
		else
		{
			// The stack-buffer policy caps oversized stream thresholds.
			return max_threshold;
		}
	}
	else
	{
		// Semantic whole-output coalescing is disabled unless the stream opts into this independent policy.
		return 0u;
	}
}

/// @brief    Computes the dynamic full-output coalescing threshold for an output stream.
/// @details  Unlike the fixed full-output threshold, this value is not capped by the stack budget. It is nevertheless
///           capped by the contiguous-object and byte-allocation proof, so a policy cannot authorize an impossible
///           dynamic buffer. Zero disables dynamic materialization.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the maximum dynamically materialized output size in characters, or zero when disabled
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_full_output_dynamic_coalesce_threshold() noexcept
{
	if constexpr (::fast_io::full_output_dynamic_coalesce_threshold_stream<char_type, outputstmtype>)
	{
		return ::fast_io::details::decay::print_contiguous_coalesce_threshold<char_type>(
			full_output_dynamic_coalesce_threshold(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>));
	}
	else
	{
		return 0u;
	}
}

/// @brief    Selects the dynamic materialization limit for one semantic run shape.
/// @details  A lone width node uses the stream's repeated-fill crossover when available. In a mixed run, the width
///           may already be satisfied by its child or contribute only a few padding characters beside a large payload;
///           type structure cannot determine that run-time fact. Such runs therefore retain the general whole-record
///           limit instead of pessimistically applying the fill-only limit to unrelated output. Args are already-
///           forwarded semantic types.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval ::std::size_t print_full_output_dynamic_coalesce_threshold_for_run() noexcept
{
	constexpr bool contains_width{
		(::fast_io::details::decay::print_semantic_contains_width<::std::remove_cvref_t<Args>>::value || ...)};
	if constexpr (sizeof...(Args) == 1u && contains_width &&
				  ::fast_io::repeated_fill_output_policy_stream<char_type, outputstmtype>)
	{
		return ::fast_io::details::decay::print_contiguous_coalesce_threshold<char_type>(
			repeated_fill_output_policy_define(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>)
				.dynamic_coalesce_threshold);
	}
	else
	{
		return ::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold<char_type,
																					   outputstmtype>();
	}
}

/// @brief    Detects whether an output stream can write a contiguous byte range directly.
/// @tparam   outputstmtype the decayed output stream reference type
template <typename outputstmtype>
inline constexpr bool print_has_direct_write_bytes_operations =
	::fast_io::operations::decay::defines::has_write_all_bytes_overflow_define<outputstmtype> ||
	::fast_io::operations::decay::defines::has_write_some_bytes_overflow_define<outputstmtype> ||
	(sizeof(typename outputstmtype::output_char_type) == 1 &&
	 (::fast_io::operations::decay::defines::has_write_all_overflow_define<outputstmtype> ||
	  ::fast_io::operations::decay::defines::has_write_some_overflow_define<outputstmtype>));

/// @brief    Detects whether an output stream can write byte scatter descriptors directly.
/// @tparam   outputstmtype the decayed output stream reference type
template <typename outputstmtype>
inline constexpr bool print_has_direct_scatter_write_bytes_operations =
	::fast_io::operations::decay::defines::has_scatter_write_all_bytes_overflow_define<outputstmtype> ||
	::fast_io::operations::decay::defines::has_scatter_write_some_bytes_overflow_define<outputstmtype> ||
	(sizeof(typename outputstmtype::output_char_type) == 1 &&
	 (::fast_io::operations::decay::defines::has_scatter_write_all_overflow_define<outputstmtype> ||
	  ::fast_io::operations::decay::defines::has_scatter_write_some_overflow_define<outputstmtype>));

/// @brief    Detects whether an output stream can write a contiguous character range directly.
/// @tparam   outputstmtype the decayed output stream reference type
template <typename outputstmtype>
inline constexpr bool print_has_direct_write_operations =
	::fast_io::operations::decay::defines::has_write_all_overflow_define<outputstmtype> ||
	::fast_io::operations::decay::defines::has_write_some_overflow_define<outputstmtype> ||
	(sizeof(typename outputstmtype::output_char_type) == 1 &&
	 (::fast_io::operations::decay::defines::has_write_all_bytes_overflow_define<outputstmtype> ||
	  ::fast_io::operations::decay::defines::has_write_some_bytes_overflow_define<outputstmtype>));

/// @brief Proves that the generic complete-write fallback selects a scalar primitive before any scatter primitive.
/// @details The direct print helper is an inlining policy, not permission to change a stream's observable CPO
///          precedence.  Typed operations are considered first exactly as in `write_all_cold_impl`; only when none is
///          present does a one-byte stream enter the byte-operation ordering of `write_all_bytes_cold_impl`.
template <typename outputstmtype>
inline constexpr bool print_has_preferred_direct_write_operations{[]() consteval {
	if constexpr (
		::fast_io::operations::decay::defines::has_write_all_overflow_define<
			outputstmtype>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_scatter_write_all_overflow_define<outputstmtype>)
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_write_some_overflow_define<
			outputstmtype>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_scatter_write_some_overflow_define<outputstmtype>)
	{
		return false;
	}
	else if constexpr (sizeof(typename outputstmtype::output_char_type) == 1)
	{
		if constexpr (
			::fast_io::operations::decay::defines::
				has_write_all_bytes_overflow_define<outputstmtype>)
		{
			return true;
		}
		else if constexpr (
			::fast_io::operations::decay::defines::
				has_scatter_write_all_bytes_overflow_define<outputstmtype>)
		{
			return false;
		}
		else if constexpr (
			::fast_io::operations::decay::defines::
				has_write_some_bytes_overflow_define<outputstmtype>)
		{
			return true;
		}
		else
		{
			return false;
		}
	}
	else
	{
		return false;
	}
}()};

/// @brief Completes one contiguous print-level record through the stream's direct scalar primitive.
/// @details This is the normal-path counterpart of the generic write fallback.  Callers use it only after proving that
///          the normalized stream has no put area or mutex and exposes a direct scalar operation.  The dedicated helper
///          keeps small materialized records away from the generic cold adaptation graph while leaving the optimizer free
///          to choose an ordinary inline boundary.  Partial writes are retried here with exactly the same progress
///          semantics; error handling remains owned by the selected stream CPO, so genuinely exceptional paths retain
///          their existing cold policy.
template <typename outputstmtype>
inline constexpr void print_write_all_direct(
	outputstmtype &outstm,
	typename outputstmtype::output_char_type const *first,
	typename outputstmtype::output_char_type const *last)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<
				outputstmtype>);
	static_assert(
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>);
	static_assert(
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>);
	if constexpr (
		::fast_io::operations::decay::defines::has_write_all_overflow_define<
			outputstmtype>)
	{
		write_all_overflow_define(outstm, first, last);
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_write_some_overflow_define<
			outputstmtype>)
	{
		while ((first = write_some_overflow_define(outstm, first, last)) != last)
		{
		}
	}
	else if constexpr (
		sizeof(char_type) == 1 &&
		::fast_io::operations::decay::defines::
			has_write_all_bytes_overflow_define<outputstmtype>)
	{
		write_all_bytes_overflow_define(
			outstm, reinterpret_cast<::std::byte const *>(first),
			reinterpret_cast<::std::byte const *>(last));
	}
	else
	{
		static_assert(
			sizeof(char_type) == 1 &&
			::fast_io::operations::decay::defines::
				has_write_some_bytes_overflow_define<outputstmtype>);
		auto bytes_first{reinterpret_cast<::std::byte const *>(first)};
		auto const bytes_last{reinterpret_cast<::std::byte const *>(last)};
		while ((bytes_first = write_some_bytes_overflow_define(
					outstm, bytes_first, bytes_last)) != bytes_last)
		{
		}
	}
}

/// @brief Completes one already-materialized contiguous print record through the normal direct-output path.
/// @details Only an unlocked stream without a put area bypasses the generic cold overflow adapter here. Buffered
///          capacity misses and mutex-bearing outputs keep their established generic continuation, while an ordinary
///          direct print leaves only a genuine backend error in cold code. The owned stack or heap range remains valid
///          for the complete synchronous retry loop. Diagnostic front doors retain their independent cold dispatcher;
///          selecting this scalar completion does not change a caller's placement policy.
template <typename outputstmtype>
inline constexpr void print_write_all_materialized(
	outputstmtype &outstm,
	typename outputstmtype::output_char_type const *first,
	typename outputstmtype::output_char_type const *last)
{
	if constexpr (
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>)
	{
		::fast_io::details::decay::print_write_all_direct(
			outstm, first, last);
	}
	else
	{
		::fast_io::operations::decay::write_all_decay_dispatch(
			outstm, first, last);
	}
}

/// @brief Proves that the generic byte-complete fallback selects a scalar byte primitive before a scatter primitive.
/// @details This is the byte-descriptor counterpart of `print_has_preferred_direct_write_operations`.  The ordering
///          exactly mirrors `write_all_bytes_cold_impl`: native byte CPOs precede the one-byte typed fallback, and an
///          available scatter-all/some CPO blocks the corresponding scalar shortcut.
template <typename outputstmtype>
inline constexpr bool print_has_preferred_direct_write_bytes_operations{[]() consteval {
	if constexpr (
		::fast_io::operations::decay::defines::has_write_all_bytes_overflow_define<
			outputstmtype>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_scatter_write_all_bytes_overflow_define<outputstmtype>)
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_write_some_bytes_overflow_define<
			outputstmtype>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_scatter_write_some_bytes_overflow_define<outputstmtype>)
	{
		return false;
	}
	else if constexpr (sizeof(typename outputstmtype::output_char_type) == 1)
	{
		return ::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype>;
	}
	else
	{
		return false;
	}
}()};

/// @brief Completes one contiguous byte record through the preferred direct scalar primitive.
/// @details Print's byte-scatter planner uses this only for an unbuffered, unlocked stream after copying an admitted
///          complete payload.  The dedicated scalar completion avoids routing the successful small-record case through
///          the generic cold adapter while preserving its exact byte-versus-scatter CPO precedence.  Ordinary optimized
///          callers inline this small selector without requiring a global code-size policy.
template <typename outputstmtype>
inline constexpr void print_write_all_bytes_direct(
	outputstmtype &outstm, ::std::byte const *first, ::std::byte const *last)
{
	static_assert(
		::fast_io::details::decay::
			print_has_preferred_direct_write_bytes_operations<outputstmtype>);
	static_assert(
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>);
	static_assert(
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>);
	if constexpr (
		::fast_io::operations::decay::defines::has_write_all_bytes_overflow_define<
			outputstmtype>)
	{
		write_all_bytes_overflow_define(outstm, first, last);
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_write_some_bytes_overflow_define<
			outputstmtype>)
	{
		while ((first = write_some_bytes_overflow_define(outstm, first, last)) != last)
		{
		}
	}
	else
	{
		using char_type = typename outputstmtype::output_char_type;
		static_assert(sizeof(char_type) == 1);
		::fast_io::details::decay::print_write_all_direct(
			outstm, reinterpret_cast<char_type const *>(first),
			reinterpret_cast<char_type const *>(last));
	}
}

/// @brief    Detects whether an output stream can write character scatter descriptors directly.
/// @tparam   outputstmtype the decayed output stream reference type
template <typename outputstmtype>
inline constexpr bool print_has_direct_scatter_write_operations =
	::fast_io::operations::decay::defines::has_scatter_write_all_overflow_define<outputstmtype> ||
	::fast_io::operations::decay::defines::has_scatter_write_some_overflow_define<outputstmtype> ||
	(sizeof(typename outputstmtype::output_char_type) == 1 &&
	 (::fast_io::operations::decay::defines::has_scatter_write_all_bytes_overflow_define<outputstmtype> ||
	  ::fast_io::operations::decay::defines::has_scatter_write_some_bytes_overflow_define<outputstmtype>));

/// @brief Chooses the native descriptor representation advertised by the output graph.
/// @details A one-byte typed stream is `bytes_writable` because the generic byte writer can adapt byte ranges back to
///          character ranges. That fallback is an output capability, not permission to replace the producer's typed
///          customization with a byte customization: the two CPOs may have independent cost and state. Byte descriptor
///          construction is therefore selected only when the normalized stream graph itself exposes byte write or
///          byte scatter operations (including the seek-plus-pwrite fallback). Centralizing this stricter test keeps
///          buffered, unbuffered, and compiled plans consistent; notably, a C `FILE` observer does expose byte writes,
///          while a synthetic typed-only `char` sink deliberately remains on typed descriptors.
template <typename outputstmtype>
inline constexpr bool print_uses_byte_scatter_representation =
	::fast_io::operations::decay::defines::has_any_of_write_or_seek_pwrite_bytes_operations<
		outputstmtype>;

/// @brief Selects outputs which may consume provider-owned immutable fragment pointers directly.
/// @details A scatter CPO alone does not prove synchronous consumption or lack of buffering. The public marker carries
///          that semantic promise; structural exclusions defend against wrappers which accidentally forward it across
///          a put area or mutex, and the final capability check proves the matching native scatter primitive exists.
template <typename outputstmtype>
inline constexpr bool print_output_retains_static_scatter =
	::fast_io::synchronous_direct_scatter_output<
		typename outputstmtype::output_char_type, outputstmtype> &&
	!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
		outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
	((::fast_io::details::decay::print_uses_byte_scatter_representation<
		  outputstmtype> &&
	  ::fast_io::details::decay::
		  print_has_direct_scatter_write_bytes_operations<outputstmtype>) ||
	 (!::fast_io::details::decay::print_uses_byte_scatter_representation<
		  outputstmtype> &&
	  ::fast_io::details::decay::
		  print_has_direct_scatter_write_operations<outputstmtype>));

/// @brief Selects a scalar operation which may observe a provider-owned immutable pointer directly.
/// @details A scalar write capability alone does not promise that an adapter consumes the source before returning.
///          The scalar-specific synchronous-direct marker supplies that lifetime proof without claiming a scatter CPO;
///          put areas and mutex wrappers remain destination-owned and are excluded structurally. Keeping scalar and
///          scatter admission independent avoids pretending that scalar-only endpoints own a scatter CPO. POSIX opts
///          into both protocols; Win32/NT remain outside the scalar protocol because their observer type erases the
///          synchronous-versus-overlapped handle state needed to prove provider lifetime.
template <typename outputstmtype>
inline constexpr bool print_output_accepts_static_provider_scalar =
	::fast_io::synchronous_direct_scalar_output<
		typename outputstmtype::output_char_type, outputstmtype> &&
	!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
		outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
	::fast_io::details::decay::
		print_has_preferred_direct_write_operations<outputstmtype>;

/// @brief Continues a byte-scatter all-operation after one synchronous hot attempt made partial progress.
/// @details This helper is deliberately cold and out of line.  The provider descriptors live in the caller until the
///          complete operation returns, so a partially consumed first descriptor can be finished as a scalar range
///          before the untouched suffix re-enters the ordinary all-operation.  No retry machinery or descriptor
///          adjustment is retained in the successful static-fragment path.
template <typename outputstmtype>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#endif
inline constexpr void print_static_scatter_write_all_bytes_cold_continuation(
	outputstmtype &outstm, ::fast_io::io_scatter_t const *scatters,
	::std::size_t count, ::fast_io::io_scatter_status_t status)
{
	auto position{status.position};
	if (position > count) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	if (position == count)
	{
		if (status.position_in_scatter != 0u) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		return;
	}
	if (status.position_in_scatter != 0u)
	{
		auto const [base_pointer, length]{scatters[position]};
		if (status.position_in_scatter > length) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		auto const base{reinterpret_cast<::std::byte const *>(base_pointer)};
		::fast_io::details::write_all_bytes_impl(
			outstm, base + status.position_in_scatter, base + length);
		++position;
	}
	if (position != count)
	{
		::fast_io::details::scatter_write_all_bytes_cold_impl(
			outstm, scatters + position, count - position);
	}
}

/// @brief Continues a typed-scatter all-operation after one synchronous hot attempt made partial progress.
template <typename outputstmtype>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#endif
inline constexpr void print_static_scatter_write_all_cold_continuation(
	outputstmtype &outstm,
	::fast_io::basic_io_scatter_t<
		typename outputstmtype::output_char_type> const *scatters,
	::std::size_t count, ::fast_io::io_scatter_status_t status)
{
	auto position{status.position};
	if (position > count) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	if (position == count)
	{
		if (status.position_in_scatter != 0u) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		return;
	}
	if (status.position_in_scatter != 0u)
	{
		auto const [base, length]{scatters[position]};
		if (status.position_in_scatter > length) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		::fast_io::details::write_all_impl(
			outstm, base + status.position_in_scatter, base + length);
		++position;
	}
	if (position != count)
	{
		::fast_io::details::scatter_write_all_cold_impl(
			outstm, scatters + position, count - position);
	}
}

/// @brief Selects the narrow static-provider byte-scatter hot path without changing ordinary scatter precedence.
/// @details A byte scatter-all or byte scalar-all customization precedes scatter-some in the generic dispatcher and
///          therefore disables this shortcut.  A buffered, locked, asynchronous, or pointer-retaining output cannot
///          satisfy `print_output_retains_static_scatter` and receives exactly its former path.
template <typename outputstmtype>
inline constexpr bool print_static_scatter_hot_first_bytes_available =
	::fast_io::details::decay::
		print_output_retains_static_scatter<outputstmtype> &&
	::fast_io::details::decay::
		print_uses_byte_scatter_representation<outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_scatter_write_all_bytes_overflow_define<outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_write_all_bytes_overflow_define<outputstmtype> &&
	::fast_io::operations::decay::defines::
		has_scatter_write_some_bytes_overflow_define<outputstmtype>;

/// @brief Selects the matching typed static-provider hot path while preserving generic CPO precedence.
template <typename outputstmtype>
inline constexpr bool print_static_scatter_hot_first_available =
	::fast_io::details::decay::
		print_output_retains_static_scatter<outputstmtype> &&
	!::fast_io::details::decay::
		print_uses_byte_scatter_representation<outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_scatter_write_all_overflow_define<outputstmtype> &&
	!::fast_io::operations::decay::defines::
		has_write_all_overflow_define<outputstmtype> &&
	::fast_io::operations::decay::defines::
		has_scatter_write_some_overflow_define<outputstmtype>;

/// @brief Compiler code-generation policy for placing the first static scatter attempt in the caller.
/// @details Twenty-five alternating `/dev/null` process pairs on one pinned i9-14900HX P-core showed a median
///          improvement of 5.44% with GCC 15 and 4.31% with GCC 13.  Clang 23 was neutral at the median and 2.26%
///          slower by the paired mean, so Clang 17--23 deliberately retains the established out-of-line cold
///          completion.  Keep this policy separate from the stream protocol predicates above: output correctness and
///          pointer-lifetime admission must never depend on which compiler happens to instantiate the graph.
inline constexpr bool print_static_scatter_hot_first_compiler_policy =
#if defined(__GNUC__) && !defined(__clang__)
	true;
#else
	false;
#endif

/// @brief Issues the first synchronous byte-scatter attempt in the static-fragment caller's hot path.
template <typename outputstmtype>
inline constexpr void
print_static_scatter_write_all_bytes_direct(
	outputstmtype &outstm, ::fast_io::io_scatter_t const *scatters,
	::std::size_t count)
{
	static_assert(
		::fast_io::details::decay::
			print_static_scatter_hot_first_bytes_available<outputstmtype>);
	if (count == 0u)
	{
		return;
	}
	using char_type = typename outputstmtype::output_char_type;
	auto const admitted{
		::fast_io::details::scatter_write_maximum_count_clamp<
			char_type, outputstmtype>(count)};
	::fast_io::io_scatter_status_t status{};
	if constexpr (requires {
					  print_static_scatter_write_some_bytes_overflow_define(
						  outstm, scatters, admitted);
				  })
	{
		// A backend may expose a dedicated entry for this measured static-only
		// first attempt without changing its ordinary/runtime scatter CPO.
		status = print_static_scatter_write_some_bytes_overflow_define(
			outstm, scatters, admitted);
	}
	else
	{
		status = scatter_write_some_bytes_overflow_define(
			outstm, scatters, admitted);
	}
	if (status.position == count && status.position_in_scatter == 0u)
	{
		return;
	}
	::fast_io::details::decay::
		print_static_scatter_write_all_bytes_cold_continuation(
			outstm, scatters, count, status);
}

/// @brief Issues the first synchronous typed-scatter attempt in the static-fragment caller's hot path.
template <typename outputstmtype>
inline constexpr void
print_static_scatter_write_all_direct(
	outputstmtype &outstm,
	::fast_io::basic_io_scatter_t<
		typename outputstmtype::output_char_type> const *scatters,
	::std::size_t count)
{
	static_assert(
		::fast_io::details::decay::
			print_static_scatter_hot_first_available<outputstmtype>);
	if (count == 0u)
	{
		return;
	}
	using char_type = typename outputstmtype::output_char_type;
	auto const admitted{
		::fast_io::details::scatter_write_maximum_count_clamp<
			char_type, outputstmtype>(count)};
	auto const status{scatter_write_some_overflow_define(
		outstm, scatters, admitted)};
	if (status.position == count && status.position_in_scatter == 0u)
	{
		return;
	}
	::fast_io::details::decay::
		print_static_scatter_write_all_cold_continuation(
			outstm, scatters, count, status);
}

/// @brief    Returns the per-segment size threshold used by small-scatter repacking.
/// @details  Streams without the customization keep small-scatter repacking disabled.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the maximum segment size in characters, or zero when disabled
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_small_scatter_repack_size() noexcept
{
	if constexpr (::fast_io::small_scatter_coalesce_threshold_stream<char_type, outputstmtype>)
	{
		// Stream-provided repack thresholds opt into copying small scatter segments.
		return small_scatter_coalesce_threshold(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>);
	}
	else
	{
		// Streams without the customization keep native scatter descriptors untouched.
		return 0u;
	}
}

/// @brief    Returns the independently configured temporary chunk capacity for native scatter repacking.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the bounded chunk capacity in characters, or zero when repacking is disabled
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_scatter_repack_chunk_size() noexcept
{
	if constexpr (::fast_io::scatter_repack_chunk_size_stream<char_type, outputstmtype>)
	{
		constexpr ::std::size_t requested{
			scatter_repack_chunk_size(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>)};
		constexpr ::std::size_t maximum{
			::fast_io::details::decay::print_coalesce_threshold_max_chars<char_type>()};
		if constexpr (requested < maximum)
		{
			return requested;
		}
		else
		{
			return maximum;
		}
	}
	else
	{
		return 0u;
	}
}

/// @brief    Returns the minimum descriptor reduction required by a native scatter repack plan.
/// @details  Streams without a customization preserve the historical minimum of eight eliminated descriptors.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @return   ::std::size_t the minimum number of scatter descriptors that must be removed
template <::std::integral char_type, typename outputstmtype>
inline constexpr ::std::size_t print_scatter_repack_minimum_saved_scatter_count() noexcept
{
	if constexpr (::fast_io::scatter_repack_minimum_saved_scatter_count_stream<char_type, outputstmtype>)
	{
		return scatter_repack_minimum_saved_scatter_count(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>);
	}
	else
	{
		return 8u;
	}
}

/// @brief    Caps temporary scatter descriptor storage used by the repack emitter.
/// @tparam   scatter_type the scatter descriptor type being emitted
/// @return   ::std::size_t the stack descriptor count, capped at 64 descriptors
template <typename scatter_type>
inline constexpr ::std::size_t print_scatter_repack_stack_scatter_count() noexcept
{
	constexpr ::std::size_t stack_count{
		::fast_io::details::decay::print_stack_buffer_max_element_count<scatter_type>()};
	if constexpr (stack_count < 64u)
	{
		// Very small stack policies use their exact descriptor capacity.
		return stack_count;
	}
	else
	{
		// Larger stack policies are capped to keep the hot repack frame bounded.
		return 64u;
	}
}

/// @brief    Tries to repack small byte scatter segments before byte scatter output.
/// @details  The first pass plans descriptor reduction and buffer needs; the second pass emits either preserved
///           descriptors or copied coalesced chunks. Repacking is skipped unless enough descriptors are saved.
/// @tparam   outputstmtype the decayed output stream reference type
/// @param    outstm        the output stream reference
/// @param    scatters      the first byte scatter descriptor
/// @param    n             the number of descriptors
/// @return   bool true when this function emitted the output through the repacked descriptor list
template <typename outputstmtype>
inline constexpr bool print_scatter_write_all_bytes_try_repack_small(
	outputstmtype &outstm, ::fast_io::io_scatter_t const *scatters, ::std::size_t n)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t chunk_chars{
		::fast_io::details::decay::print_scatter_repack_chunk_size<char_type, outputstmtype>()};
	constexpr ::std::size_t requested_small_chars{
		::fast_io::details::decay::print_small_scatter_repack_size<char_type, outputstmtype>()};
	constexpr ::std::size_t small_chars{
		requested_small_chars < chunk_chars ? requested_small_chars : chunk_chars};
	if constexpr (chunk_chars == 0 || small_chars == 0 ||
				  !::fast_io::details::decay::print_has_direct_scatter_write_bytes_operations<outputstmtype>)
	{
		// Repacking requires both stream opt-in and a native byte-scatter output path.
		return false;
	}
	else
	{
		// The chunk helper is byte-budget capped.  Clamping the independent eligibility threshold to that capacity
		// proves both multiplications below and `chunk_bytes - current_size` in the planner cannot wrap.  A segment
		// larger than one chunk cannot be profitably copied into that chunk in any case.
		constexpr ::std::size_t chunk_bytes{chunk_chars * sizeof(char_type)};
		constexpr ::std::size_t small_bytes{small_chars * sizeof(char_type)};
		constexpr ::std::size_t minimum_saved_scatter_count{
			::fast_io::details::decay::print_scatter_repack_minimum_saved_scatter_count<char_type, outputstmtype>()};
		if constexpr (chunk_bytes == 0)
		{
			// A zero-capacity chunk cannot hold any repacked byte payload.
			return false;
		}
		else
		{
			::std::size_t output_count{};
			::std::size_t dynamic_buffer_size{};
			::std::size_t current_size{};
			::std::size_t current_count{};
			bool stack_chunk_used{};
			bool has_coalesced_chunk{};
			auto flush_plan = [&]() constexpr {
				if (current_count == 0)
				{
					// No pending small descriptors are available for the current planned chunk.
					return;
				}
				++output_count;
				if (1u < current_count)
				{
					// Multiple small descriptors in one chunk mean the final output descriptor count can shrink.
					has_coalesced_chunk = true;
					if (stack_chunk_used)
					{
						// The first copied chunk uses stack storage; later copied chunks need dynamic storage.
						dynamic_buffer_size =
							::fast_io::details::intrinsics::add_or_overflow_die(dynamic_buffer_size, current_size);
					}
					else
					{
						// Reserve the first copied chunk for the fixed stack buffer.
						stack_chunk_used = true;
					}
				}
				current_size = 0;
				current_count = 0;
			};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				// The planning pass preserves large segments and groups adjacent small segments into chunks.
				::std::size_t const len{iter->len};
				if (len == 0)
				{
					// Empty descriptors do not affect output or descriptor pressure.
					continue;
				}
				if (small_bytes < len)
				{
					// Large byte segments remain native scatter descriptors.
					flush_plan();
					++output_count;
					continue;
				}
				if (chunk_bytes - current_size < len)
				{
					// The current copied chunk is full enough; start a new planned chunk.
					flush_plan();
				}
				current_size += len;
				++current_count;
			}
			flush_plan();
			if (!has_coalesced_chunk || output_count > n || n - output_count < minimum_saved_scatter_count)
			{
				// Avoid extra copying unless the plan actually reduces enough scatter descriptors.
				return false;
			}

			::std::byte stack_buffer[chunk_bytes];
			::fast_io::details::local_operator_new_array_ptr<::std::byte> dynamic_buffer;
			if (dynamic_buffer_size != 0)
			{
				// Additional copied chunks beyond the first stack chunk use one dynamic byte buffer.
				dynamic_buffer.allocate_new(dynamic_buffer_size);
			}

			auto emit_repacked = [&](::fast_io::io_scatter_t *out_scatters) constexpr {
				// The emit pass repeats the plan while producing the final descriptor chain.
				auto out_curr{out_scatters};
				::std::byte *dynamic_curr{dynamic_buffer.ptr};
				::std::byte *chunk_begin{};
				::std::byte *chunk_curr{};
				::std::size_t chunk_size{};
				::std::size_t chunk_count{};
				bool chunk_is_stack{};
				bool stack_used{};
				::fast_io::io_scatter_t pending{};
				auto begin_chunk = [&]() constexpr {
					if (!stack_used)
					{
						// The first copied chunk uses the fixed stack buffer.
						chunk_begin = stack_buffer;
						chunk_curr = stack_buffer;
						chunk_is_stack = true;
						stack_used = true;
					}
					else
					{
						// Later copied chunks append into the pre-sized dynamic buffer.
						chunk_begin = dynamic_curr;
						chunk_curr = dynamic_curr;
						chunk_is_stack = false;
					}
				};
				auto flush_chunk = [&]() constexpr {
					if (chunk_count == 0)
					{
						// No pending chunk exists, so the output descriptor cursor is unchanged.
						return;
					}
					if (chunk_count == 1)
					{
						// A single small descriptor was not copied; preserve it as-is.
						*out_curr = pending;
						++out_curr;
					}
					else
					{
						// Multiple small descriptors were copied into one contiguous output chunk.
						*out_curr = {chunk_begin, chunk_size};
						++out_curr;
						if (!chunk_is_stack)
						{
							// Commit the dynamic-buffer cursor only after a dynamic chunk is emitted.
							dynamic_curr = chunk_curr;
						}
					}
					chunk_begin = {};
					chunk_curr = {};
					chunk_size = 0;
					chunk_count = 0;
					chunk_is_stack = false;
				};
				for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
				{
					// Rebuild the planned descriptor chain while copying only coalesced small chunks.
					::std::size_t const len{iter->len};
					if (len == 0)
					{
						// Empty descriptors are skipped in the emitted chain.
						continue;
					}
					if (small_bytes < len)
					{
						// Large byte segments flush pending small chunks and remain direct scatter entries.
						flush_chunk();
						*out_curr = *iter;
						++out_curr;
						continue;
					}
					if (chunk_bytes - chunk_size < len)
					{
						// The pending copied chunk cannot fit this segment.
						flush_chunk();
					}
					if (chunk_count == 0)
					{
						// The first small descriptor is kept pending until a second one proves copying is useful.
						pending = *iter;
						chunk_size = len;
						chunk_count = 1;
						continue;
					}
					if (chunk_count == 1)
					{
						// The second small descriptor starts an actual copied chunk.
						begin_chunk();
						auto const first{static_cast<::std::byte const *>(pending.base)};
						chunk_curr = ::fast_io::details::decay::small_scatter_copy_n(first, pending.len,
																					 chunk_curr);
					}
					auto const first{static_cast<::std::byte const *>(iter->base)};
					chunk_curr = ::fast_io::details::decay::small_scatter_copy_n(first, len, chunk_curr);
					chunk_size += len;
					++chunk_count;
				}
				flush_chunk();
				::std::size_t const repacked_count{static_cast<::std::size_t>(out_curr - out_scatters)};
				if constexpr (::fast_io::details::decay::print_has_direct_write_bytes_operations<outputstmtype>)
				{
					if (repacked_count == 1)
					{
						// A single repacked byte range can bypass scatter output entirely.
						auto first{static_cast<::std::byte const *>(out_scatters->base)};
						::fast_io::operations::decay::write_all_bytes_decay_dispatch(outstm, first,
																			first + out_scatters->len);
						return;
					}
				}
				::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(outstm, out_scatters, repacked_count);
			};

			constexpr ::std::size_t stack_scatter_count{
				::fast_io::details::decay::print_scatter_repack_stack_scatter_count<::fast_io::io_scatter_t>()};
			if constexpr (stack_scatter_count != 0)
			{
				if (output_count <= stack_scatter_count)
				{
					// The final descriptor chain fits in bounded stack storage.
					::fast_io::io_scatter_t out_scatters[stack_scatter_count];
					emit_repacked(out_scatters);
					return true;
				}
			}
			// Large repacked descriptor chains allocate descriptor storage before emission.
			::fast_io::details::local_operator_new_array_ptr<::fast_io::io_scatter_t> out_scatters(output_count);
			emit_repacked(out_scatters.ptr);
			return true;
		}
	}
}

/// @brief    Tries to repack small character scatter segments before character scatter output.
/// @details  This mirrors the byte-scatter repack path, but sizes chunks and descriptors in char_type units.
/// @tparam   outputstmtype the decayed output stream reference type
/// @param    outstm        the output stream reference
/// @param    scatters      the first character scatter descriptor
/// @param    n             the number of descriptors
/// @return   bool true when this function emitted the output through the repacked descriptor list
template <typename outputstmtype>
inline constexpr bool print_scatter_write_all_try_repack_small(
	outputstmtype &outstm,
	::fast_io::basic_io_scatter_t<typename outputstmtype::output_char_type> const *scatters, ::std::size_t n)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t chunk_chars{
		::fast_io::details::decay::print_scatter_repack_chunk_size<char_type, outputstmtype>()};
	constexpr ::std::size_t requested_small_chars{
		::fast_io::details::decay::print_small_scatter_repack_size<char_type, outputstmtype>()};
	constexpr ::std::size_t small_chars{
		requested_small_chars < chunk_chars ? requested_small_chars : chunk_chars};
	if constexpr (chunk_chars == 0 || small_chars == 0 ||
				  !::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>)
	{
		// Repacking requires both stream opt-in and a native character-scatter output path.
		return false;
	}
	else
	{
		// A candidate larger than the reusable chunk cannot participate in a legal copied group.  Clamping the
		// independent threshold establishes the invariant `current_size <= chunk_chars`, which makes every remaining-
		// capacity subtraction in both planning and emission passes well-defined.
		constexpr ::std::size_t minimum_saved_scatter_count{
			::fast_io::details::decay::print_scatter_repack_minimum_saved_scatter_count<char_type, outputstmtype>()};
		if constexpr (chunk_chars == 0)
		{
			// A zero-capacity chunk cannot hold any repacked character payload.
			return false;
		}
		else
		{
			using scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
			::std::size_t output_count{};
			::std::size_t dynamic_buffer_size{};
			::std::size_t current_size{};
			::std::size_t current_count{};
			bool stack_chunk_used{};
			bool has_coalesced_chunk{};
			auto flush_plan = [&]() constexpr {
				if (current_count == 0)
				{
					// No pending small descriptors are available for the current planned chunk.
					return;
				}
				++output_count;
				if (1u < current_count)
				{
					// Multiple small descriptors in one chunk mean the final output descriptor count can shrink.
					has_coalesced_chunk = true;
					if (stack_chunk_used)
					{
						// The first copied chunk uses stack storage; later copied chunks need dynamic storage.
						dynamic_buffer_size =
							::fast_io::details::intrinsics::add_or_overflow_die(dynamic_buffer_size, current_size);
					}
					else
					{
						// Reserve the first copied chunk for the fixed stack buffer.
						stack_chunk_used = true;
					}
				}
				current_size = 0;
				current_count = 0;
			};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				// The planning pass preserves large segments and groups adjacent small segments into chunks.
				::std::size_t const len{iter->len};
				if (len == 0)
				{
					// Empty descriptors do not affect output or descriptor pressure.
					continue;
				}
				if (small_chars < len)
				{
					// Large character segments remain native scatter descriptors.
					flush_plan();
					++output_count;
					continue;
				}
				if (chunk_chars - current_size < len)
				{
					// The current copied chunk is full enough; start a new planned chunk.
					flush_plan();
				}
				current_size += len;
				++current_count;
			}
			flush_plan();
			if (!has_coalesced_chunk || output_count > n || n - output_count < minimum_saved_scatter_count)
			{
				// Avoid extra copying unless the plan actually reduces enough scatter descriptors.
				return false;
			}

			char_type stack_buffer[chunk_chars];
			::fast_io::details::local_operator_new_array_ptr<char_type> dynamic_buffer;
			if (dynamic_buffer_size != 0)
			{
				// Additional copied chunks beyond the first stack chunk use one dynamic character buffer.
				dynamic_buffer.allocate_new(dynamic_buffer_size);
			}

			auto emit_repacked = [&](scatter_type *out_scatters) constexpr {
				// The emit pass repeats the plan while producing the final descriptor chain.
				auto out_curr{out_scatters};
				char_type *dynamic_curr{dynamic_buffer.ptr};
				char_type *chunk_begin{};
				char_type *chunk_curr{};
				::std::size_t chunk_size{};
				::std::size_t chunk_count{};
				bool chunk_is_stack{};
				bool stack_used{};
				scatter_type pending{};
				auto begin_chunk = [&]() constexpr {
					if (!stack_used)
					{
						// The first copied chunk uses the fixed stack buffer.
						chunk_begin = stack_buffer;
						chunk_curr = stack_buffer;
						chunk_is_stack = true;
						stack_used = true;
					}
					else
					{
						// Later copied chunks append into the pre-sized dynamic buffer.
						chunk_begin = dynamic_curr;
						chunk_curr = dynamic_curr;
						chunk_is_stack = false;
					}
				};
				auto flush_chunk = [&]() constexpr {
					if (chunk_count == 0)
					{
						// No pending chunk exists, so the output descriptor cursor is unchanged.
						return;
					}
					if (chunk_count == 1)
					{
						// A single small descriptor was not copied; preserve it as-is.
						*out_curr = pending;
						++out_curr;
					}
					else
					{
						// Multiple small descriptors were copied into one contiguous output chunk.
						*out_curr = {chunk_begin, chunk_size};
						++out_curr;
						if (!chunk_is_stack)
						{
							// Commit the dynamic-buffer cursor only after a dynamic chunk is emitted.
							dynamic_curr = chunk_curr;
						}
					}
					chunk_begin = {};
					chunk_curr = {};
					chunk_size = 0;
					chunk_count = 0;
					chunk_is_stack = false;
				};
				for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
				{
					// Rebuild the planned descriptor chain while copying only coalesced small chunks.
					::std::size_t const len{iter->len};
					if (len == 0)
					{
						// Empty descriptors are skipped in the emitted chain.
						continue;
					}
					if (small_chars < len)
					{
						// Large character segments flush pending small chunks and remain direct scatter entries.
						flush_chunk();
						*out_curr = *iter;
						++out_curr;
						continue;
					}
					if (chunk_chars - chunk_size < len)
					{
						// The pending copied chunk cannot fit this segment.
						flush_chunk();
					}
					if (chunk_count == 0)
					{
						// The first small descriptor is kept pending until a second one proves copying is useful.
						pending = *iter;
						chunk_size = len;
						chunk_count = 1;
						continue;
					}
					if (chunk_count == 1)
					{
						// The second small descriptor starts an actual copied chunk.
						begin_chunk();
						chunk_curr = ::fast_io::details::decay::small_scatter_copy_n(
							pending.base, pending.len, chunk_curr);
					}
					chunk_curr =
						::fast_io::details::decay::small_scatter_copy_n(iter->base, len, chunk_curr);
					chunk_size += len;
					++chunk_count;
				}
				flush_chunk();
				::std::size_t const repacked_count{static_cast<::std::size_t>(out_curr - out_scatters)};
				if constexpr (::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>)
				{
					if (repacked_count == 1)
					{
						// A single repacked character range can bypass scatter output entirely.
						auto [base, len] = *out_scatters;
						::fast_io::operations::decay::write_all_decay_dispatch(outstm, base, base + len);
						return;
					}
				}
				::fast_io::operations::decay::scatter_write_all_decay_dispatch(outstm, out_scatters, repacked_count);
			};

			constexpr ::std::size_t stack_scatter_count{
				::fast_io::details::decay::print_scatter_repack_stack_scatter_count<scatter_type>()};
			if constexpr (stack_scatter_count != 0)
			{
				if (output_count <= stack_scatter_count)
				{
					// The final descriptor chain fits in bounded stack storage.
					scatter_type out_scatters[stack_scatter_count];
					emit_repacked(out_scatters);
					return true;
				}
			}
			// Large repacked descriptor chains allocate descriptor storage before emission.
			::fast_io::details::local_operator_new_array_ptr<scatter_type> out_scatters(output_count);
			emit_repacked(out_scatters.ptr);
			return true;
		}
	}
}

/// @brief    Writes byte scatter descriptors, optionally coalescing small scatter runs first.
/// @details  Empty and single-scatter runs are handled directly. Multi-scatter runs are copied into a bounded stack
///           buffer according to either the direct-scatter or no-scatter fallback policy selected by stream
///           capability.
/// @tparam   outputstmtype the decayed output stream reference type
/// @param    outstm        the output stream reference
/// @param    scatters      the first byte scatter descriptor
/// @param    n             the number of scatter descriptors
template <typename outputstmtype>
inline constexpr void print_scatter_write_all_bytes_maybe_coalesce(outputstmtype &outstm,
																   ::fast_io::io_scatter_t const *scatters,
																   ::std::size_t n)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool preferred_direct_write{
		::fast_io::details::decay::
			print_has_preferred_direct_write_bytes_operations<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>};
	if (n == 0)
	{
		// An empty byte scatter run emits no output.
		return;
	}
	if (n == 1)
	{
		// A single byte scatter avoids scatter-write overhead and writes the range directly when non-empty.
		::std::size_t const len{scatters->len};
		if (len != 0)
		{
			// The single byte scatter contains payload bytes that can be written contiguously.
			auto first{static_cast<::std::byte const *>(scatters->base)};
			if constexpr (preferred_direct_write)
			{
				::fast_io::details::decay::print_write_all_bytes_direct(
					outstm, first, first + len);
			}
			else
			{
				::fast_io::operations::decay::write_all_bytes_decay_dispatch(
					outstm, first, first + len);
			}
		}
		return;
	}
	constexpr bool has_direct_scatter{
		::fast_io::details::decay::print_has_direct_scatter_write_bytes_operations<outputstmtype>};
	constexpr ::std::size_t threshold_chars{
		has_direct_scatter
			? ::fast_io::details::decay::print_scatter_direct_full_output_coalesce_threshold<char_type,
																							 outputstmtype>()
			: ::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>()};
	if constexpr (threshold_chars != 0 &&
				  ::fast_io::details::decay::print_has_direct_write_bytes_operations<outputstmtype>)
	{
		// The capability-specific policy permits this complete byte run to try one contiguous write.
		if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<threshold_chars, char_type>)
		{
			// A threshold inside the stack budget uses one fixed character-aligned stack allocation.
			char_type buffer[threshold_chars];
			auto curr{reinterpret_cast<::std::byte *>(buffer)};
			auto const first{curr};
			::std::size_t remaining{threshold_chars * sizeof(char_type)};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				::std::size_t const len{iter->len};
				if (remaining < len)
				{
					// An oversized run preserves native scatter output or the ordinary per-scatter write fallback.
					if (::fast_io::details::decay::print_scatter_write_all_bytes_try_repack_small(outstm, scatters,
																								  n))
					{
						return;
					}
					::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(outstm, scatters, n);
					return;
				}
				if (len != 0)
				{
					// Non-empty byte scatter payloads are appended to the stack coalescing buffer.
					curr = ::fast_io::details::decay::small_scatter_copy_n(
						static_cast<::std::byte const *>(iter->base), len, curr);
					remaining -= len;
				}
			}
			if (curr != first)
			{
				// At least one byte payload was coalesced, so emit the compact stack range.
				if constexpr (preferred_direct_write)
				{
					::fast_io::details::decay::print_write_all_bytes_direct(
						outstm, first, curr);
				}
				else
				{
					::fast_io::operations::decay::write_all_bytes_decay_dispatch(
						outstm, first, curr);
				}
			}
		}
		else
		{
			// A threshold above the stack budget first measures the exact byte count for one dynamic allocation.
			constexpr ::std::size_t threshold_bytes{
				threshold_chars > SIZE_MAX / sizeof(char_type) ? SIZE_MAX : threshold_chars * sizeof(char_type)};
			::std::size_t total_size{};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				::std::size_t const len{iter->len};
				if (threshold_bytes - total_size < len)
				{
					// A run above the dynamic threshold returns to native scatter or per-scatter output.
					if (::fast_io::details::decay::print_scatter_write_all_bytes_try_repack_small(outstm, scatters,
																								  n))
					{
						return;
					}
					::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(outstm, scatters, n);
					return;
				}
				total_size += len;
			}
			if (total_size != 0)
			{
				// Allocate exactly the measured bytes, copy every non-empty scatter, and write once.
				::fast_io::details::local_operator_new_array_ptr<::std::byte> buffer(total_size);
				::std::byte *curr{buffer.ptr};
				for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
				{
					if (iter->len != 0)
					{
						// Append this non-empty byte scatter to the dynamically allocated range.
						curr = ::fast_io::details::decay::small_scatter_copy_n(
							static_cast<::std::byte const *>(iter->base), iter->len, curr);
					}
				}
				if constexpr (preferred_direct_write)
				{
					::fast_io::details::decay::print_write_all_bytes_direct(
						outstm, buffer.ptr, curr);
				}
				else
				{
					::fast_io::operations::decay::write_all_bytes_decay_dispatch(
						outstm, buffer.ptr, curr);
				}
			}
		}
		return;
	}
	// Streams that cannot coalesce this path preserve native scatter output or its normal write fallback.
	if (::fast_io::details::decay::print_scatter_write_all_bytes_try_repack_small(outstm, scatters, n))
	{
		return;
	}
	::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(outstm, scatters, n);
}

/// @brief    Writes character scatter descriptors, optionally coalescing small scatter runs first.
/// @details  This mirrors the byte-scatter path but preserves character counts and uses character-range output APIs.
/// @tparam   outputstmtype the decayed output stream reference type
/// @param    outstm        the output stream reference
/// @param    scatters      the first character scatter descriptor
/// @param    n             the number of scatter descriptors
template <typename outputstmtype>
inline constexpr void print_scatter_write_all_maybe_coalesce(
	outputstmtype &outstm, ::fast_io::basic_io_scatter_t<typename outputstmtype::output_char_type> const *scatters,
	::std::size_t n)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool preferred_direct_write{
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>};
	if (n == 0)
	{
		// An empty character scatter run emits no output.
		return;
	}
	if (n == 1)
	{
		// A single character scatter avoids scatter-write overhead and writes the range directly when non-empty.
		auto [base, len] = *scatters;
		if (len != 0)
		{
			// The single character scatter contains payload characters that can be written contiguously.
			if constexpr (preferred_direct_write)
			{
				::fast_io::details::decay::print_write_all_direct(
					outstm, base, base + len);
			}
			else
			{
				::fast_io::operations::decay::write_all_decay_dispatch(
					outstm, base, base + len);
			}
		}
		return;
	}
	constexpr bool has_direct_scatter{
		::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>};
	constexpr ::std::size_t threshold_chars{
		has_direct_scatter
			? ::fast_io::details::decay::print_scatter_direct_full_output_coalesce_threshold<char_type,
																							 outputstmtype>()
			: ::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>()};
	if constexpr (threshold_chars != 0 &&
				  ::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>)
	{
		// The capability-specific policy permits this complete character run to try one contiguous write.
		if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<threshold_chars, char_type>)
		{
			// A threshold inside the stack budget uses one fixed character buffer.
			char_type buffer[threshold_chars];
			char_type *curr{buffer};
			::std::size_t remaining{threshold_chars};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				::std::size_t const len{iter->len};
				if (remaining < len)
				{
					// An oversized run preserves native scatter output or the ordinary per-scatter write fallback.
					if (::fast_io::details::decay::print_scatter_write_all_try_repack_small(outstm, scatters, n))
					{
						return;
					}
					::fast_io::operations::decay::scatter_write_all_decay_dispatch(outstm, scatters, n);
					return;
				}
				if (len != 0)
				{
					// Non-empty character scatters are appended to the stack coalescing buffer.
					curr = ::fast_io::details::decay::small_scatter_copy_n(iter->base, len, curr);
					remaining -= len;
				}
			}
			if (curr != buffer)
			{
				// At least one character payload was coalesced, so emit the compact stack range.
				if constexpr (preferred_direct_write)
				{
					::fast_io::details::decay::print_write_all_direct(
						outstm, buffer, curr);
				}
				else
				{
					::fast_io::operations::decay::write_all_decay_dispatch(
						outstm, buffer, curr);
				}
			}
		}
		else
		{
			// A threshold above the stack budget first measures the exact character count.
			::std::size_t total_size{};
			for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
			{
				::std::size_t const len{iter->len};
				if (threshold_chars - total_size < len)
				{
					// A run above the dynamic threshold returns to native scatter or per-scatter output.
					if (::fast_io::details::decay::print_scatter_write_all_try_repack_small(outstm, scatters, n))
					{
						return;
					}
					::fast_io::operations::decay::scatter_write_all_decay_dispatch(outstm, scatters, n);
					return;
				}
				total_size += len;
			}
			if (total_size != 0)
			{
				// Allocate exactly the measured characters, copy each non-empty scatter, and write once.
				::fast_io::details::local_operator_new_array_ptr<char_type> buffer(total_size);
				char_type *curr{buffer.ptr};
				for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
				{
					if (iter->len != 0)
					{
						// Append this non-empty character scatter to the dynamically allocated range.
						curr = ::fast_io::details::decay::small_scatter_copy_n(iter->base, iter->len, curr);
					}
				}
				if constexpr (preferred_direct_write)
				{
					::fast_io::details::decay::print_write_all_direct(
						outstm, buffer.ptr, curr);
				}
				else
				{
					::fast_io::operations::decay::write_all_decay_dispatch(
						outstm, buffer.ptr, curr);
				}
			}
		}
		return;
	}
	// Streams that cannot coalesce this path preserve native scatter output or its normal write fallback.
	if (::fast_io::details::decay::print_scatter_write_all_try_repack_small(outstm, scatters, n))
	{
		return;
	}
	::fast_io::operations::decay::scatter_write_all_decay_dispatch(outstm, scatters, n);
}

/// @brief Keeps the general no-scatter fallback out of the small run-time-plan caller's stack frame.
/// @details The general fallback contains independently selected stack and dynamic coalescing strategies. Inlining it
///          after the run-time plan's small-buffer attempt makes those mutually exclusive buffers share one caller
///          frame; on targets with stack-clash protection that can add page probes even when the small attempt succeeds.
///          This boundary is therefore part of the cost policy, not merely a code-size annotation.
template <typename outputstmtype>
#if FAST_IO_HAS_ATTRIBUTE(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#elif FAST_IO_HAS_ATTRIBUTE(msvc::noinline)
[[msvc::noinline]]
#endif
inline constexpr void print_runtime_scatter_plan_write_large(
	outputstmtype &outstm, ::fast_io::basic_io_scatter_t<typename outputstmtype::output_char_type> const *scatters,
	::std::size_t n)
{
	::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(outstm, scatters, n);
}

/// @brief Emits a materialized run-time scatter plan without heap-allocating its common small-output case.
/// @details This policy is intentionally narrower than print_scatter_write_all_maybe_coalesce. A no-scatter stream may
///          advertise a large fallback threshold because preserving one logical print call is more important than the
///          copy size for its general fallback. Using that threshold as a fixed stack extent is unsafe, while using it
///          as the size of an exact heap allocation makes even a short formatted record pay allocator latency. The
///          run-time plan has already paid to enumerate every descriptor, so it can first try a separately bounded
///          1-KiB scratch buffer. Native-scatter streams skip this path: copying before writev would impose a different
///          cost model and is already governed by the direct-scatter coalescing policy.
///
///          The loop maintains `remaining == local_capacity - copied`. It tests `len <= remaining` before copying, so
///          neither the subtraction nor the destination pointer can overflow. Failure delegates the original,
///          unmodified descriptor sequence to the general policy; therefore this optimization changes neither output
///          order nor the large-output fallback.
/// @tparam outputstmtype the decayed output stream reference type
/// @param  outstm        the output stream reference
/// @param  scatters      the first materialized character scatter descriptor
/// @param  n             the number of materialized descriptors
template <typename outputstmtype>
inline constexpr void print_runtime_scatter_plan_write(
	outputstmtype &outstm, ::fast_io::basic_io_scatter_t<typename outputstmtype::output_char_type> const *scatters,
	::std::size_t n)
{
	using char_type = typename outputstmtype::output_char_type;
	if (n == 0u)
	{
		return;
	}
	if (n == 1u)
	{
		// Preserve the ordinary scatter writer's single-descriptor rule: copying cannot reduce the number of calls.
		auto const [base, len] = *scatters;
		if (len != 0u)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(outstm, base, base + len);
		}
		return;
	}
	constexpr bool has_direct_scatter{
		::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>};
	constexpr bool has_direct_write{
		::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>};
	constexpr ::std::size_t fallback_threshold{
		::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>()};
	constexpr ::std::size_t requested_local_bytes{1024u};
	constexpr ::std::size_t requested_local_capacity{
		requested_local_bytes / sizeof(char_type)};
	constexpr ::std::size_t stack_capacity{
		::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
	constexpr ::std::size_t policy_capacity{
		fallback_threshold < requested_local_capacity ? fallback_threshold : requested_local_capacity};
	constexpr ::std::size_t local_capacity{policy_capacity < stack_capacity ? policy_capacity : stack_capacity};
	if constexpr (!has_direct_scatter && has_direct_write && local_capacity != 0u)
	{
		char_type buffer[local_capacity];
		char_type *curr{buffer};
		::std::size_t remaining{local_capacity};
		for (auto iter{scatters}, last{scatters + n}; iter != last; ++iter)
		{
			::std::size_t const len{iter->len};
			if (remaining < len) [[unlikely]]
			{
				::fast_io::details::decay::print_runtime_scatter_plan_write_large(outstm, scatters, n);
				return;
			}
			if (len != 0u)
			{
				curr = ::fast_io::details::decay::small_scatter_copy_n(iter->base, len, curr);
				remaining -= len;
			}
		}
		if (curr != buffer)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(outstm, buffer, curr);
		}
		return;
	}
	::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(outstm, scatters, n);
}

/// @brief    Dispatches scatter output to the byte or character scatter writer.
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   scatter_type  the scatter descriptor type
/// @param    outstm        the output stream reference
/// @param    scatters      the first scatter descriptor
/// @param    n             the number of scatter descriptors
template <typename outputstmtype, typename scatter_type>
inline constexpr void print_scatter_write_all_dispatch(outputstmtype &outstm, scatter_type const *scatters,
													   ::std::size_t n)
{
	if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
	{
		// Byte scatter descriptors are routed to the byte-oriented coalescing writer.
		::fast_io::details::decay::print_scatter_write_all_bytes_maybe_coalesce(outstm, scatters, n);
	}
	else
	{
		// Character scatter descriptors are routed to the character-oriented coalescing writer.
		::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(outstm, scatters, n);
	}
}

/// @brief Writes a descriptor run without copying its immutable payloads through a coalescing buffer.
/// @details Admission is owned by callers which proved both a retained static fragment in the exact prefix and a
///          synchronous native scatter destination.  Descriptor storage may remain automatic; only character payload
///          storage is preserved.  Byte-oriented endpoints receive byte lengths, typed endpoints character lengths.
template <typename outputstmtype, typename scatter_type>
inline constexpr void print_scatter_write_all_preserving_static_fragments(
	outputstmtype &outstm, scatter_type const *scatters, ::std::size_t n)
{
	// A single retained descriptor can preserve its source pointer while using the
	// scalar write CPO.  For a short multi-descriptor run there is no single source
	// pointer to preserve; use the destination's ordinary coalescing policy so
	// direct sinks still get one write (and retain writev for thresholds of zero or
	// oversized payloads).  This keeps the static-fragment identity guarantee for
	// the only observable single-source case without forcing every small literal
	// record through scatter_write_all.
	if (n == 1u)
	{
		if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
		{
			if constexpr (::fast_io::details::decay::print_has_preferred_direct_write_bytes_operations<outputstmtype>)
			{
				if (scatters[0].len != 0u)
				{
					auto const first{
						static_cast<::std::byte const *>(scatters[0].base)};
					::fast_io::details::decay::print_write_all_bytes_direct(
						outstm, first, first + scatters[0].len);
				}
				return;
			}
		}
		else if constexpr (::fast_io::details::decay::print_has_preferred_direct_write_operations<outputstmtype>)
		{
			if (scatters[0].len != 0u)
			{
				::fast_io::details::decay::print_write_all_direct(
					outstm, scatters[0].base, scatters[0].base + scatters[0].len);
			}
			return;
		}
	}
	if (n > 1u)
	{
		using char_type = typename outputstmtype::output_char_type;
		constexpr ::std::size_t threshold{
			::fast_io::details::decay::print_scatter_direct_full_output_coalesce_threshold<char_type, outputstmtype>()};
		if constexpr (threshold != 0u)
		{
			::fast_io::details::decay::print_scatter_write_all_dispatch(outstm, scatters, n);
			return;
		}
	}
	if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
	{
		::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(
			outstm, scatters, n);
	}
	else
	{
		::fast_io::operations::decay::scatter_write_all_decay_dispatch(
			outstm, scatters, n);
	}
}

/// @brief    Appends a newline scatter when requested and writes the full scatter run.
/// @tparam   needprintlf   true when the final scatter slot is reserved for a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   char_type     the output character type
/// @tparam   scatter_type  the scatter descriptor type
/// @param    outstm        the output stream reference
/// @param    scatters      the scatter descriptor storage
/// @param    n             the number of scatter descriptors, including the optional newline slot
template <bool needprintlf, typename outputstmtype, ::std::integral char_type, typename scatter_type>
inline constexpr void print_scatter_write_all_dispatch_with_line(outputstmtype &outstm, scatter_type *scatters,
																 ::std::size_t n)
{
	if constexpr (needprintlf)
	{
		// The caller reserved the final scatter slot for the trailing newline output.
		if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
		{
			// Byte scatter output stores the newline length in bytes.
			scatters[n - 1u] = ::fast_io::details::decay::line_scatter_common<char_type, void>;
		}
		else
		{
			// Character scatter output stores the newline length in characters.
			scatters[n - 1u] = ::fast_io::details::decay::line_scatter_common<char_type>;
		}
	}
	::fast_io::details::decay::print_scatter_write_all_dispatch(outstm, scatters, n);
}

/// @brief Detects the parameter wrapper used by the public print-forwarding protocol.
template <typename T>
struct print_runtime_scatter_plan_parameter : ::std::false_type
{};

template <typename value_type>
struct print_runtime_scatter_plan_parameter<::fast_io::parameter<value_type>> : ::std::true_type
{};

/// @brief Recovers the underlying value from public parameter wrappers before scatter-plan classification.
template <typename T>
inline constexpr decltype(auto) print_runtime_scatter_plan_unwrap(T &&t) noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::details::decay::print_runtime_scatter_plan_parameter<value_type>::value)
	{
		return ::fast_io::details::decay::print_runtime_scatter_plan_unwrap(
			::std::forward<T>(t).reference);
	}
	else
	{
		return ::std::forward<T>(t);
	}
}

/// @brief The exact expression produced after recursively unwrapping public parameter transports.
/// @details Keeping this alias separate from the decayed value type is essential for scatter CPO selection: the plan's
///          sizing and materialization passes both forward this expression, so an overload for another ref category is
///          not a valid capability proof.
template <typename T>
using print_runtime_scatter_plan_unwrapped_expression_t = decltype(::fast_io::details::decay::print_runtime_scatter_plan_unwrap(::std::declval<T>()));

template <typename T>
using print_runtime_scatter_plan_unwrapped_t =
	::std::remove_cvref_t<
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_expression_t<T>>;

/// @brief Tests whether one argument can participate in a run-time scatter plan.
/// @details A dynamic reserve-scatters argument is checked first by the sizing and materialization helpers because it
///          also satisfies dynamic_reserve_printable. The remaining alternatives already have established borrowed,
///          reserve, or fixed reserve-scatters representations.
template <::std::integral char_type, typename T>
inline constexpr bool print_runtime_scatter_plan_component_v =
	::fast_io::details::decay::retained_reserve_scatters_printable_v<
		char_type, ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T>> ||
	::fast_io::details::decay::retained_scatter_printable_v<
		char_type,
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_expression_t<T>> ||
	::fast_io::reserve_printable<char_type,
								 ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T>> ||
	::fast_io::dynamic_reserve_printable<
		char_type, ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T>> ||
	::std::same_as<::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T>, ::fast_io::io_null_t>;

/// @brief Tests whether a normalized run should use the run-time scatter-plan fast entry.
/// @details The plan requires an unbuffered destination and at least one component whose descriptor count is known
///          only at run time. A native character-scatter stream can emit the plan directly. A direct-write-only stream
///          is also eligible when it explicitly supplies a non-zero scatter-fallback coalescing policy; otherwise the
///          plan would merely replace one contiguous materialization with several writes. Buffered streams retain
///          their put-area-aware path because descriptor planning provides no additional syscall reduction there.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_runtime_scatter_plan_fast_entry_available_v =
	!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
	(::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype> ||
	 (::fast_io::details::decay::print_has_direct_write_operations<outputstmtype> &&
	  ::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>() != 0u)) &&
	(::fast_io::details::decay::print_runtime_scatter_plan_component_v<char_type, Args> && ...) &&
	((::fast_io::dynamic_reserve_scatters_printable<
		  char_type, ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<Args>> &&
	  ::fast_io::details::decay::retained_reserve_scatters_printable_v<
		  char_type, ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<Args>>) ||
	 ...);

/// @brief Computes descriptor and scratch upper capacities for one run-time scatter-plan component.
/// @details Reserve-scatters size functions return capacities, not exact consumption. The later define pass may leave
///          slots unused, but must return both cursors within these capacities. Size inspection precedes materialization,
///          so a component that traverses input here is responsible for requiring a multipass source.
template <::std::integral char_type, typename T>
inline constexpr ::fast_io::reserve_scatters_size_result print_runtime_scatter_plan_component_size(T &&t)
{
	decltype(auto) value{
		::fast_io::details::decay::print_runtime_scatter_plan_unwrap(::std::forward<T>(t))};
	using value_type = ::std::remove_cvref_t<decltype(value)>;
	if constexpr (::fast_io::dynamic_reserve_scatters_printable<char_type, value_type>)
	{
		return print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>,
										   ::std::forward<decltype(value)>(value));
	}
	else if constexpr (::fast_io::reserve_scatters_printable<char_type, value_type>)
	{
		return print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>);
	}
	else if constexpr (::fast_io::scatter_printable_for<
						   char_type, decltype(::std::forward<decltype(value)>(value))>)
	{
		return {1u, 0u};
	}
	else if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		return {1u, print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>)};
	}
	else if constexpr (::fast_io::dynamic_reserve_printable<char_type, value_type>)
	{
		return {1u,
				print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>,
								   ::std::forward<decltype(value)>(value))};
	}
	else
	{
		static_assert(::std::same_as<value_type, ::fast_io::io_null_t>);
		return {};
	}
}

/// @brief Computes the complete capacities for a normalized run-time scatter plan.
/// @details Each component capacity is retained beside the aggregate. Materialization must validate a producer's
///          returned cursors against the exact slice proved by that producer's size CPO; an aggregate-only bound cannot
///          identify which producer escaped and, more importantly, would allow the escaped cursor to become the next
///          producer's input before the final check. Recording the values here also avoids a second dynamic-size call.
template <bool line, ::std::integral char_type, typename... Args>
inline constexpr ::fast_io::reserve_scatters_size_result print_runtime_scatter_plan_size(
	::fast_io::reserve_scatters_size_result *component_capacities, Args &&...args)
{
	::fast_io::reserve_scatters_size_result result{};
	auto capacity_curr{component_capacities};
	auto add_component = [&result, &capacity_curr](auto &&arg) constexpr {
		auto const current{
			::fast_io::details::decay::print_runtime_scatter_plan_component_size<char_type>(
				::std::forward<decltype(arg)>(arg))};
		*capacity_curr++ = current;
		result.scatters_size = ::fast_io::details::intrinsics::add_or_overflow_die(result.scatters_size,
																				   current.scatters_size);
		result.reserve_size = ::fast_io::details::intrinsics::add_or_overflow_die(result.reserve_size,
																				  current.reserve_size);
	};
	(add_component(::std::forward<Args>(args)), ...);
	if constexpr (line)
	{
		result.scatters_size = ::fast_io::details::intrinsics::add_or_overflow_die(
			result.scatters_size, static_cast<::std::size_t>(1u));
	}
	return result;
}

/// @brief Appends one component to caller-owned character scatter and scratch cursors.
/// @details Each branch advances only the cursor for storage it actually consumes. Empty reserve/scatter results omit
///          their descriptor; unused capacity is permitted. A customization result is untrusted until both returned
///          cursors have been checked against this component's measured closed ranges. Validation precedes assignment,
///          subtraction, descriptor iteration, and the next producer call; this ordering is essential because ordinary
///          pointer subtraction cannot validate an unrelated pointer without already invoking undefined behavior.
///          Borrowed scatter components must keep their character storage alive until the synchronous final write.
template <::std::integral char_type, typename T>
inline constexpr void print_runtime_scatter_plan_materialize_one(
	::fast_io::basic_io_scatter_t<char_type> *&scatter_curr, char_type *&reserve_curr,
	::fast_io::reserve_scatters_size_result component_capacity, T &&t)
{
	decltype(auto) value{
		::fast_io::details::decay::print_runtime_scatter_plan_unwrap(::std::forward<T>(t))};
	using value_type = ::std::remove_cvref_t<decltype(value)>;
	auto scatter_first{scatter_curr};
	auto reserve_first{reserve_curr};
	auto scatter_last{scatter_first + component_capacity.scatters_size};
	auto reserve_last{reserve_first + component_capacity.reserve_size};
	if constexpr (::fast_io::dynamic_reserve_scatters_printable<char_type, value_type> ||
				  ::fast_io::reserve_scatters_printable<char_type, value_type>)
	{
		auto const result{print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>,
														scatter_first, reserve_first,
														::std::forward<decltype(value)>(value))};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				scatter_first, scatter_last, result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				reserve_first, reserve_last, result.reserve_pos_ptr)) [[unlikely]]
		{
			// A producer-local failure must stop before the forged cursor can seed a later component.
			::fast_io::fast_terminate();
		}
		scatter_curr = result.scatters_pos_ptr;
		reserve_curr = result.reserve_pos_ptr;
	}
	else if constexpr (::fast_io::scatter_printable_for<
						   char_type, decltype(::std::forward<decltype(value)>(value))>)
	{
		auto const scatter{
			print_scatter_define(::fast_io::io_reserve_type<char_type, value_type>,
								 ::std::forward<decltype(value)>(value))};
		if (scatter.len != 0u)
		{
			*scatter_curr++ = scatter;
		}
	}
	else if constexpr (::fast_io::reserve_printable<char_type, value_type> ||
					   ::fast_io::dynamic_reserve_printable<char_type, value_type>)
	{
		char_type *const result{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, reserve_first,
													 ::std::forward<decltype(value)>(value))};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				reserve_first, reserve_last, result)) [[unlikely]]
		{
			// Dynamic and static reserve CPOs share the same bounded cursor contract in this descriptor plan.
			::fast_io::fast_terminate();
		}
		reserve_curr = result;
		if (reserve_curr != reserve_first)
		{
			*scatter_curr++ = {reserve_first, static_cast<::std::size_t>(reserve_curr - reserve_first)};
		}
	}
	else
	{
		static_assert(::std::same_as<value_type, ::fast_io::io_null_t>);
	}
}

/// @brief Emits a normalized run through its run-time scatter plan.
/// @details Descriptor and scratch storage have independent cost budgets: at most four KiB of descriptors and one KiB
///          of reserve storage remain on the stack, while larger capacities use exact dynamic storage. Native-scatter
///          destinations retain their direct/coalesced scatter policy and hard descriptor-count batching. A
///          direct-write-only destination first uses the plan-specific compact coalescer and delegates large output to
///          its declared fallback policy.
///
///          Checked additions prove that aggregate capacities do not wrap. The explicit SIZE_MAX/PTRDIFF_MAX admission
///          test makes every subsequent cursor difference representable; the allocation wrappers separately validate
///          element-count-to-byte conversion. Returned cursors are checked as a contract tripwire, but producers remain
///          responsible for never writing beyond the supplied capacities.
template <bool line, typename outputstmtype, typename... Args>
	requires ::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
		typename outputstmtype::output_char_type, outputstmtype, Args...>
inline constexpr void print_runtime_scatter_plan_fast_entry_impl(
	outputstmtype &outstm, ::fast_io::reserve_scatters_size_result *component_capacities,
	Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	using scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
	static_assert(sizeof...(Args) != 0u);
	auto const capacity{
		::fast_io::details::decay::print_runtime_scatter_plan_size<line, char_type>(
			component_capacities, ::std::forward<Args>(args)...)};
	constexpr ::std::size_t maximum_pointer_distance{
		static_cast<::std::size_t>((::std::numeric_limits<::std::ptrdiff_t>::max)())};
	if (capacity.scatters_size == SIZE_MAX || capacity.reserve_size == SIZE_MAX ||
		maximum_pointer_distance < capacity.scatters_size || maximum_pointer_distance < capacity.reserve_size)
		[[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	if (capacity.scatters_size == 0u)
	{
		return;
	}
	// A descriptor is two machine words, so the historical 64-entry ceiling was only one KiB on 64-bit targets. That
	// forced otherwise modest borrowed ranges (for example, 128 elements plus separators = 255 descriptors) through
	// allocator traffic before a single writev-like call. A byte budget states the actual resource being controlled and
	// admits that common case without making descriptor count ABI-dependent. The global stack policy remains the hard
	// upper bound, proving that constrained 256-byte targets automatically retain a correspondingly smaller array.
	constexpr ::std::size_t requested_stack_scatter_bytes{4u * 1024u};
	constexpr ::std::size_t requested_stack_scatter_capacity{
		requested_stack_scatter_bytes / sizeof(scatter_type)};
	constexpr ::std::size_t stack_scatter_budget{
		::fast_io::details::decay::print_stack_buffer_max_element_count<scatter_type>()};
	constexpr ::std::size_t stack_scatter_capacity{
		stack_scatter_budget < requested_stack_scatter_capacity ? stack_scatter_budget
																: requested_stack_scatter_capacity};
	constexpr ::std::size_t requested_stack_reserve_bytes{1024u};
	constexpr ::std::size_t requested_stack_reserve_capacity{
		requested_stack_reserve_bytes / sizeof(char_type)};
	constexpr ::std::size_t stack_reserve_budget{
		::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
	constexpr ::std::size_t stack_reserve_capacity{
		stack_reserve_budget < requested_stack_reserve_capacity ? stack_reserve_budget
																: requested_stack_reserve_capacity};

	auto materialize_and_write = [&](scatter_type *scatters, char_type *reserve) constexpr {
		// The aggregate proof owns the allocation, while the retained component capacities prove every producer-local
		// closed range. The indexed expansion keeps those values aligned with their source arguments without repeating
		// an object-dependent size CPO.
		auto scatter_curr{scatters};
		auto reserve_curr{reserve};
		[&]<::std::size_t... index>(::std::index_sequence<index...>) constexpr {
			(::fast_io::details::decay::print_runtime_scatter_plan_materialize_one<char_type>(
				 scatter_curr, reserve_curr, component_capacities[index], ::std::forward<Args>(args)),
			 ...);
		}(::std::make_index_sequence<sizeof...(Args)>{});
		auto const scatter_end{scatters + capacity.scatters_size};
		auto const reserve_end{reserve + capacity.reserve_size};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				scatters, scatter_end, scatter_curr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				reserve, reserve_end, reserve_curr)) [[unlikely]]
		{
			// This is an internal induction tripwire; producer results were already rejected component by component.
			::fast_io::fast_terminate();
		}
		if constexpr (line)
		{
			if (scatter_curr == scatter_end) [[unlikely]]
			{
				::fast_io::fast_terminate();
			}
			*scatter_curr++ = ::fast_io::details::decay::line_scatter_common<char_type>;
		}
		::std::size_t const scatter_count{static_cast<::std::size_t>(scatter_curr - scatters)};
		::std::size_t const reserve_count{static_cast<::std::size_t>(reserve_curr - reserve)};
		if (capacity.scatters_size < scatter_count || capacity.reserve_size < reserve_count) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		::fast_io::details::decay::print_runtime_scatter_plan_write(outstm, scatters, scatter_count);
	};

	auto with_reserve_storage = [&](scatter_type *scatters) constexpr {
		if (capacity.reserve_size == 0u)
		{
			// Zero-capacity producers may compare this sentinel pointer but must neither dereference nor advance it.
			char_type dummy{};
			materialize_and_write(scatters, __builtin_addressof(dummy));
			return;
		}
		if constexpr (stack_reserve_capacity != 0u)
		{
			if (capacity.reserve_size <= stack_reserve_capacity)
			{
				char_type reserve[stack_reserve_capacity];
				materialize_and_write(scatters, reserve);
				return;
			}
		}
		::fast_io::details::local_operator_new_array_ptr<char_type> reserve(capacity.reserve_size);
		materialize_and_write(scatters, reserve.ptr);
	};

	if constexpr (stack_scatter_capacity != 0u)
	{
		if (capacity.scatters_size <= stack_scatter_capacity)
		{
			scatter_type scatters[stack_scatter_capacity];
			with_reserve_storage(scatters);
			return;
		}
	}
	::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(capacity.scatters_size);
	with_reserve_storage(scatters.ptr);
}

/// @brief Projects a fixed scatter into the existing borrowed-scatter protocol for a native run-time plan.
/// @details The ordinary run-time planner remains unchanged for every dynamic-only and scalar-write run. A native
///          scatter endpoint reaches this adapter only when its argument pack actually contains fixed storage; that
///          argument becomes a standard scatter descriptor while all other argument transports retain their exact
///          reference categories. The mature planner consequently computes zero scratch for the fixed fragment and
///          materializes its original pointer without a second destination-policy implementation.
template <::std::integral char_type, typename T>
inline constexpr decltype(auto)
print_runtime_scatter_plan_retain_static_arg(T &&t) noexcept
{
	using value_type =
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T>;
	using static_scatter_traits =
		::fast_io::details::decay::print_static_scatter_traits<char_type, value_type>;
	if constexpr (static_scatter_traits::available)
	{
		decltype(auto) value{
			::fast_io::details::decay::print_runtime_scatter_plan_unwrap(
				::std::forward<T>(t))};
		return static_scatter_traits::define(value);
	}
	else
	{
		return ::std::forward<T>(t);
	}
}

/// @brief Owns the per-component capacity proof and emits one run-time scatter plan.
/// @details The table must survive from object-dependent sizing through final materialization, so recomputing dynamic
///          sizes is not a sound substitute for storage: a size CPO may inspect mutable state or a multipass range.
///          Small argument packs use automatic storage, while large packs use one exact allocation. The local ceiling
///          is four KiB and is also clamped by the global byte-based stack policy; using only the global GNU/Linux
///          default here would permit a 64-KiB metadata frame before descriptor and reserve storage are considered.
///          This separate ceiling prevents a very large formatting pack from turning validation metadata itself into
///          a stack performance or exhaustion accident.
template <bool line, typename outputstmtype, typename... Args>
	requires ::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
		typename outputstmtype::output_char_type, outputstmtype, Args...>
inline constexpr void print_runtime_scatter_plan_fast_entry(outputstmtype &outstm, Args &&...args)
{
	using capacity_type = ::fast_io::reserve_scatters_size_result;
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t component_count{sizeof...(Args)};
	static_assert(component_count != 0u);
	constexpr bool retain_static_scatter{
		::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype> &&
		(false || ... ||
		 ::fast_io::details::decay::print_static_scatter_traits<
			 char_type,
			 ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<Args>>::available)};
	constexpr ::std::size_t requested_stack_capacity{(4u * 1024u) / sizeof(capacity_type)};
	constexpr ::std::size_t policy_stack_capacity{
		::fast_io::details::decay::print_stack_buffer_max_element_count<capacity_type>()};
	constexpr ::std::size_t stack_capacity{
		policy_stack_capacity < requested_stack_capacity ? policy_stack_capacity : requested_stack_capacity};
	if constexpr (component_count <= stack_capacity)
	{
		capacity_type component_capacities[component_count];
		if constexpr (retain_static_scatter)
		{
			::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_impl<line>(
				outstm, component_capacities,
				::fast_io::details::decay::print_runtime_scatter_plan_retain_static_arg<
					char_type>(::std::forward<Args>(args))...);
		}
		else
		{
			::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_impl<line>(
				outstm, component_capacities, ::std::forward<Args>(args)...);
		}
	}
	else
	{
		::fast_io::details::local_operator_new_array_ptr<capacity_type> component_capacities(component_count);
		if constexpr (retain_static_scatter)
		{
			::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_impl<line>(
				outstm, component_capacities.ptr,
				::fast_io::details::decay::print_runtime_scatter_plan_retain_static_arg<
					char_type>(::std::forward<Args>(args))...);
		}
		else
		{
			::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_impl<line>(
				outstm, component_capacities.ptr, ::std::forward<Args>(args)...);
		}
	}
}

/// @brief Proves the shape required by the bounded buffered reserve-scatters run.
/// @details This is deliberately the static protocol only. A dynamic reserve-scatters size CPO would have to be
///          invoked before materialization and could observe or consume the source; the buffered optimization below
///          instead derives every slice from a compile-time capacity and invokes each producer exactly once. Parameter
///          transports are unwrapped before classification so entry decay remains the sole ownership boundary.
template <::std::integral char_type, typename T>
inline constexpr bool print_retained_buffered_reserve_scatters_capability_v =
	::fast_io::reserve_scatters_printable<
		char_type,
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>>;

/// @brief Proves that descriptors from one buffered component survive all later component calls.
/// @details Capacity is not a provenance proof. The independent borrowed marker promises only that the materialized
///          descriptor payload remains live while the complete synchronous run is retained; it does not imply that a
///          producer is pure, repeatable, or safe to invoke during a separate sizing pass.
template <::std::integral char_type, typename T>
inline constexpr bool print_retained_buffered_reserve_scatters_lifetime_v =
	::fast_io::borrowed_reserve_scatters_source<
		char_type,
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>>;

/// @brief Proves that deferred buffered emission does not move a producer exception past earlier output.
/// @details The old fallback materializes and drains each leaf before invoking the next producer. Grouping a throwing
///          producer would change the externally visible partial-output boundary. Requiring the exact lvalue call used
///          by this implementation to be non-throwing preserves that boundary while still permitting stateful or
///          consuming producers: they are invoked once, from left to right, and are never replayed.
template <::std::integral char_type, typename T>
inline consteval bool print_retained_buffered_reserve_scatters_nothrow() noexcept
{
	if constexpr (!::fast_io::details::decay::print_retained_buffered_reserve_scatters_capability_v<
					  char_type, T>)
	{
		return false;
	}
	else
	{
		using value_type =
			::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>;
		// The capability concept above already proves that this exact named-lvalue CPO is well-formed and has the
		// required result type. Querying its two effects directly avoids a Clang 17 constant-evaluation defect for a
		// requires-expression returned from an immediately invoked variable-template lambda; no protocol constraint is
		// weakened because the expression category below is identical to the materializer's `unwrap(t)` call.
#if defined(__HERBCEPTIONS__)
		if constexpr (throws((print_reserve_scatters_define(
				::fast_io::io_reserve_type<char_type, value_type>,
				::std::declval<::fast_io::basic_io_scatter_t<char_type> *>(),
				::std::declval<char_type *>(),
				::fast_io::details::decay::print_runtime_scatter_plan_unwrap(::std::declval<T &>())))))
		{
			// Formal safety obligation: a retained producer may be reordered only when its deterministic error channel
			// is empty independently of the traditional C++ exception channel checked below.
			return false;
		}
#endif
		return noexcept(print_reserve_scatters_define(
			::fast_io::io_reserve_type<char_type, value_type>,
			::std::declval<::fast_io::basic_io_scatter_t<char_type> *>(),
			::std::declval<char_type *>(),
			::fast_io::details::decay::print_runtime_scatter_plan_unwrap(::std::declval<T &>())));
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_retained_buffered_reserve_scatters_nothrow_v =
	::fast_io::details::decay::print_retained_buffered_reserve_scatters_nothrow<char_type, T>();

/// @brief Proves that one retained component has an exact, non-throwing byte-descriptor materializer.
/// @details `noexcept` proves only that deferring this call cannot move an exception past earlier output. It does not
///          make `basic_io_scatter_t<char_type>` and `io_scatter_t` alias-compatible, nor does it authorize the generic
///          typed-to-byte adapter. A byte aggregate is therefore admitted only by the exact byte CPO that constructs
///          real `io_scatter_t` objects. Absence of that refinement deliberately returns false; the caller then builds
///          one typed aggregate and invokes the producer's original typed CPO without descriptor conversion.
template <::std::integral char_type, typename T>
inline consteval bool print_retained_buffered_reserve_scatters_exact_byte_nothrow() noexcept
{
	using value_type =
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>;
	using value_expression =
		::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_expression_t<T &>;
	if constexpr (::fast_io::reserve_scatters_bytes_printable<char_type, value_expression>)
	{
		// Model the named `decltype(auto) value` in the materializer, including cv-qualification, without changing the
		// phase-one owner into a reference. The capability proof makes the direct effect query substitution-safe and
		// avoids the same Clang 17 requires-expression constant-evaluation defect as the typed path above.
		using named_value_expression =
			::std::add_lvalue_reference_t<::std::remove_reference_t<value_expression>>;
#if defined(__HERBCEPTIONS__)
		if constexpr (throws((print_reserve_scatters_bytes_define(
				  ::fast_io::io_reserve_type<char_type, value_type>,
				  ::std::declval<::fast_io::io_scatter_t *>(), ::std::declval<char_type *>(),
				  ::std::declval<named_value_expression>()))))
		{
			// A deferred byte producer must also have an empty deterministic error channel; `noexcept` alone does not
			// establish that Herbceptions obligation.
			return false;
		}
#endif
		return noexcept(print_reserve_scatters_bytes_define(
			::fast_io::io_reserve_type<char_type, value_type>,
			::std::declval<::fast_io::io_scatter_t *>(), ::std::declval<char_type *>(),
			::std::declval<named_value_expression>()));
	}
	else
	{
		return false;
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_retained_buffered_reserve_scatters_exact_byte_nothrow_v =
	::fast_io::details::decay::print_retained_buffered_reserve_scatters_exact_byte_nothrow<char_type, T>();

/// @brief Proves exact byte-plan availability and exception safety for the selected leading component count.
/// @details Prefix length is a strategy result and can be shorter than the argument pack. Recursing on the explicit
///          count prevents a non-selected tail from disabling a safe byte plan. Requiring every selected leaf is also
///          what makes one homogeneous byte array sound: a mixed pack must not pay per-leaf typed scratch conversion or
///          silently substitute an adapter for an exact refinement.
template <::std::size_t count, ::std::integral char_type, typename T, typename... Args>
inline consteval bool print_retained_buffered_reserve_scatters_exact_byte_prefix_nothrow() noexcept
{
	static_assert(count != 0u);
	if constexpr (!::fast_io::details::decay::print_retained_buffered_reserve_scatters_exact_byte_nothrow_v<
					  char_type, T>)
	{
		return false;
	}
	else if constexpr (count == 1u)
	{
		return true;
	}
	else
	{
		static_assert(sizeof...(Args) != 0u);
		return []<typename next, typename... tail>(::std::type_identity<next>,
												   ::std::type_identity<tail>...) consteval {
			return ::fast_io::details::decay::
				print_retained_buffered_reserve_scatters_exact_byte_prefix_nothrow<
					count - 1u, char_type, next, tail...>();
		}(::std::type_identity<Args>{}...);
	}
}

/// @brief Compile-time capacities of the leading retained buffered reserve-scatters run.
/// @details `position` is kept distinct from `component_count` even though they are equal for the current strict
///          adjacency rule. That separation prevents a future null/filtering extension from silently weakening the
///          at-least-two-producer cost gate.
struct retained_buffered_reserve_scatters_run_result
{
	::std::size_t position{};
	::std::size_t component_count{};
	::std::size_t scatters_size{};
	::std::size_t reserve_size{};
};

/// @brief Finds the leading adjacent run eligible for one retained buffered drain.
/// @details Capability, lifetime, and exception admission are intentionally tested as independent facts. A failure at
///          any position terminates this prefix but leaves that leaf and its tail to the established dispatcher. All
///          sums use the optional-strategy overflow rule: an unrepresentable aggregate disables grouping rather than
///          invalidating any individually printable component.
template <::std::integral char_type, typename T, typename... Args>
inline constexpr retained_buffered_reserve_scatters_run_result
find_retained_buffered_reserve_scatters_run() noexcept
{
	constexpr bool has_capability{
		::fast_io::details::decay::print_retained_buffered_reserve_scatters_capability_v<char_type, T>};
	constexpr bool has_lifetime{
		::fast_io::details::decay::print_retained_buffered_reserve_scatters_lifetime_v<char_type, T>};
	constexpr bool preserves_exception_boundary{
		::fast_io::details::decay::print_retained_buffered_reserve_scatters_nothrow_v<char_type, T>};
	if constexpr (!has_capability || !has_lifetime || !preserves_exception_boundary)
	{
		return {};
	}
	else
	{
		retained_buffered_reserve_scatters_run_result result{};
		if constexpr (sizeof...(Args) != 0u)
		{
			result = ::fast_io::details::decay::find_retained_buffered_reserve_scatters_run<
				char_type, Args...>();
		}
		using value_type =
			::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>;
		constexpr auto component_capacity{
			print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
		::std::size_t const position{
			::fast_io::details::decay::print_strategy_extent_add_or_unavailable(result.position, 1u)};
		::std::size_t const component_count{
			::fast_io::details::decay::print_strategy_extent_add_or_unavailable(result.component_count, 1u)};
		::std::size_t const scatters_size{
			::fast_io::details::decay::print_strategy_extent_add_or_unavailable(
				result.scatters_size, component_capacity.scatters_size)};
		::std::size_t const reserve_size{
			::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				result.reserve_size, component_capacity.reserve_size)};
		if (position == SIZE_MAX || component_count == SIZE_MAX || scatters_size == SIZE_MAX ||
			reserve_size == SIZE_MAX)
		{
			return {};
		}
		return {position, component_count, scatters_size, reserve_size};
	}
}

/// @brief Selects the retained buffered run only when its complete local plan is inexpensive.
/// @details The source scanner proves representation and lifetime; this predicate owns destination and placement cost.
///          A real put area is required because the measured regression came from repeatedly entering the primitive
///          buffered scatter path. Two producers are the minimum run that can amortize that entry. Descriptor and
///          reserve arrays share one byte budget: the active central stack policy is the hard limit, while a 4-KiB
///          local ceiling prevents the 64-KiB GNU/Linux default from admitting a page-probed metadata frame. The one-
///          element reserve extent is a legal sentinel for zero-scratch producers and is included in the byte proof.
///
///          The descriptor representation is chosen for the whole selected prefix. Byte representation requires an
///          exact non-throwing byte CPO from every leaf; otherwise the already-proved non-throwing typed CPOs construct
///          a typed aggregate. This all-or-nothing rule removes the measured per-leaf scratch/conversion cost and, more
///          importantly, never treats layout equality as effective-type permission.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_retained_buffered_reserve_scatters_run_selected() noexcept
{
	if constexpr (!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
				  sizeof...(Args) < 2u)
	{
		return false;
	}
	else
	{
		constexpr auto result{
			::fast_io::details::decay::find_retained_buffered_reserve_scatters_run<char_type, Args...>()};
		if constexpr (result.component_count < 2u)
		{
			return false;
		}
		else
		{
			constexpr bool append_line{line && result.position == sizeof...(Args)};
			constexpr bool use_byte_descriptors{
				::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype> &&
				::fast_io::details::decay::
					print_retained_buffered_reserve_scatters_exact_byte_prefix_nothrow<
						result.position, char_type, Args...>()};
			using scatter_type = ::std::conditional_t<
				use_byte_descriptors, ::fast_io::io_scatter_t,
				::fast_io::basic_io_scatter_t<char_type>>;
			constexpr ::std::size_t descriptor_capacity{
				::fast_io::details::decay::print_strategy_extent_add_or_unavailable(
					result.scatters_size, static_cast<::std::size_t>(append_line))};
			if constexpr (descriptor_capacity == SIZE_MAX ||
						  descriptor_capacity > (::std::numeric_limits<::std::size_t>::max)() /
													sizeof(scatter_type))
			{
				return false;
			}
			else
			{
				constexpr ::std::size_t reserve_extent{result.reserve_size == 0u ? 1u : result.reserve_size};
				constexpr ::std::size_t descriptor_bytes{
					descriptor_capacity * sizeof(scatter_type)};
				constexpr ::std::size_t reserve_bytes{reserve_extent * sizeof(char_type)};
				constexpr ::std::size_t requested_plan_bytes{4u * 1024u};
				constexpr ::std::size_t central_stack_bytes{
					::fast_io::details::decay::print_stack_buffer_max_bytes<>()};
				constexpr ::std::size_t plan_budget{
					central_stack_bytes < requested_plan_bytes ? central_stack_bytes : requested_plan_bytes};
				return descriptor_bytes <= plan_budget && reserve_bytes <= plan_budget - descriptor_bytes;
			}
		}
	}
}

/// @brief Materializes the first `count` retained components once, in source order.
/// @details The shared materializer validates both returned cursors against this component's own compile-time slice
///          before either cursor can seed the next producer. Advancing by actual consumption reuses unused capacity
///          without allowing slices containing live output to overlap.
template <::std::size_t count, ::std::integral char_type, typename scatter_type, typename T,
		  typename... Args>
inline constexpr void print_retained_buffered_reserve_scatters_materialize(
	scatter_type *&scatter_curr, char_type *&reserve_curr, T &t, Args &...args)
{
	static_assert(count != 0u);
	if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
	{
		static_assert(
			::fast_io::details::decay::print_retained_buffered_reserve_scatters_exact_byte_nothrow_v<
				char_type, T>);
		// Selection proves that this exact CPO exists and is non-throwing. Invoke it directly into storage whose actual
		// element type is `io_scatter_t`; routing through the generic adapter would weaken that proof by retaining a
		// typed-scratch fallback which this homogeneous byte plan is specifically designed never to use.
		decltype(auto) value{
			::fast_io::details::decay::print_runtime_scatter_plan_unwrap(t)};
		using value_type = ::std::remove_cvref_t<decltype(value)>;
		constexpr auto component_capacity{
			print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
		auto const scatter_first{scatter_curr};
		auto const reserve_first{reserve_curr};
		auto const result{print_reserve_scatters_bytes_define(
			::fast_io::io_reserve_type<char_type, value_type>, scatter_first, reserve_first, value)};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				scatter_first, scatter_first + component_capacity.scatters_size,
				result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				reserve_first, reserve_first + component_capacity.reserve_size,
				result.reserve_pos_ptr)) [[unlikely]]
		{
			// Reject forged cursors before they can become the storage slice supplied to the next producer.
			::fast_io::fast_terminate();
		}
		scatter_curr = result.scatters_pos_ptr;
		reserve_curr = result.reserve_pos_ptr;
	}
	else
	{
		static_assert(::std::same_as<scatter_type, ::fast_io::basic_io_scatter_t<char_type>>);
		using value_type =
			::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<T &>;
		constexpr auto component_capacity{
			print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
		::fast_io::details::decay::print_runtime_scatter_plan_materialize_one<char_type>(
			scatter_curr, reserve_curr, component_capacity, t);
	}
	if constexpr (1u < count)
	{
		::fast_io::details::decay::print_retained_buffered_reserve_scatters_materialize<
			count - 1u, char_type>(scatter_curr, reserve_curr, args...);
	}
}

/// @brief Builds and drains one selected retained buffered reserve-scatters prefix.
/// @details Both arrays have exact compile-time extents admitted by the combined stack-byte proof above. Every producer
///          is called once and in argument order; the newline, when owned by this prefix, is appended only after all
///          component cursors have been validated. The final call submits the complete descriptor sequence to the
///          existing scatter machinery, which performs one put-area discovery/commit for a fitting buffered run and
///          retains its established overflow, native-scatter, and descriptor-limit behavior otherwise. A mixed exact-
///          byte/typed prefix deliberately uses typed descriptors for every leaf: exact byte is a refinement, not a
///          license to convert the remaining typed descriptors inside the hot aggregate build.
template <bool needprintlf, ::std::size_t position, ::std::integral char_type, typename outputstmtype,
		  typename T, typename... Args>
inline constexpr void print_retained_buffered_reserve_scatters_run(outputstmtype &outstm, T &t,
																   Args &...args)
{
	constexpr auto result{
		::fast_io::details::decay::find_retained_buffered_reserve_scatters_run<char_type, T, Args...>()};
	static_assert(result.component_count >= 2u && result.position == position);
	constexpr bool use_byte_descriptors{
		::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype> &&
		::fast_io::details::decay::
			print_retained_buffered_reserve_scatters_exact_byte_prefix_nothrow<
				position, char_type, T, Args...>()};
	using scatter_type = ::std::conditional_t<
		use_byte_descriptors, ::fast_io::io_scatter_t,
		::fast_io::basic_io_scatter_t<char_type>>;
	constexpr ::std::size_t descriptor_capacity{
		result.scatters_size + static_cast<::std::size_t>(needprintlf)};
	constexpr ::std::size_t reserve_extent{result.reserve_size == 0u ? 1u : result.reserve_size};
	scatter_type scatters[descriptor_capacity];
	char_type reserve[reserve_extent];
	auto scatter_curr{scatters};
	auto reserve_curr{reserve};
	::fast_io::details::decay::print_retained_buffered_reserve_scatters_materialize<position, char_type>(
		scatter_curr, reserve_curr, t, args...);
	if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
			scatters, scatters + result.scatters_size, scatter_curr) ||
		!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
			reserve, reserve + result.reserve_size, reserve_curr)) [[unlikely]]
	{
		// Component-local validation already proves this induction; retain one aggregate tripwire at the drain boundary.
		::fast_io::fast_terminate();
	}
	if constexpr (needprintlf)
	{
		if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
		{
			*scatter_curr++ = ::fast_io::details::decay::line_scatter_common<char_type, void>;
		}
		else
		{
			*scatter_curr++ = ::fast_io::details::decay::line_scatter_common<char_type>;
		}
	}
	::fast_io::details::decay::print_scatter_write_all_dispatch(
		outstm, scatters, static_cast<::std::size_t>(scatter_curr - scatters));
}

/// @brief    Writes one character scatter followed by a newline.
/// @details  Streams with byte-write support receive byte scatter descriptors; other streams receive character
///           scatter descriptors. Descriptor storage is placed on the stack when permitted by the stack policy.
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   char_type     the output character type
/// @param    outstm        the output stream reference
/// @param    scatter_res   the scatter payload to write before the newline
template <typename outputstmtype, ::std::integral char_type>
inline constexpr void print_single_scatter_with_line(outputstmtype &outstm,
													 ::fast_io::basic_io_scatter_t<char_type> scatter_res)
{
	if constexpr (::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>)
	{
		// Byte-capable streams receive byte scatter descriptors so wide characters have correct byte lengths.
		if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<2u, ::fast_io::io_scatter_t>)
		{
			// Small byte scatter descriptor storage is kept on the stack.
			::fast_io::io_scatter_t scatters[2];
			scatters[0] = {scatter_res.base,
						   ::fast_io::details::intrinsics::mul_or_overflow_die(
							   scatter_res.len, sizeof(char_type))};
			scatters[1] = {__builtin_addressof(char_literal_v<u8'\n', char_type>), sizeof(char_type)};
			::fast_io::details::decay::print_scatter_write_all_bytes_maybe_coalesce(outstm, scatters, 2u);
		}
		else
		{
			// Oversized byte scatter descriptor storage is allocated before writing the two-part output.
			::fast_io::details::local_operator_new_array_ptr<::fast_io::io_scatter_t> scatters(2u);
			scatters.ptr[0] = {scatter_res.base,
							   ::fast_io::details::intrinsics::mul_or_overflow_die(
								   scatter_res.len, sizeof(char_type))};
			scatters.ptr[1] = {__builtin_addressof(char_literal_v<u8'\n', char_type>), sizeof(char_type)};
			::fast_io::details::decay::print_scatter_write_all_bytes_maybe_coalesce(outstm, scatters.ptr, 2u);
		}
	}
	else
	{
		// Character-only streams receive character scatter descriptors directly.
		using scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
		if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<2u, scatter_type>)
		{
			// Small character scatter descriptor storage is kept on the stack.
			scatter_type scatters[2];
			scatters[0] = scatter_res;
			scatters[1] = {__builtin_addressof(char_literal_v<u8'\n', char_type>), 1u};
			::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(outstm, scatters, 2u);
		}
		else
		{
			// Oversized character scatter descriptor storage is allocated before writing the two-part output.
			::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(2u);
			scatters.ptr[0] = scatter_res;
			scatters.ptr[1] = {__builtin_addressof(char_literal_v<u8'\n', char_type>), 1u};
			::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(outstm, scatters.ptr, 2u);
		}
	}
}

/// @brief    Materializes reserve-scatters output into byte scatter descriptors and writes it.
/// @tparam   line          true when a trailing newline scatter is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   char_type     the output character type
/// @tparam   T             the reserve-scatters printable type
/// @param    outstm        the output stream reference
/// @param    scatters      the byte scatter descriptor storage
/// @param    t             the argument to materialize
template <bool line, typename outputstmtype, ::std::integral char_type, typename T>
inline constexpr void print_reserve_scatters_bytes_with_scatter(outputstmtype &outstm, ::fast_io::io_scatter_t *scatters,
																T &t)
{
	using value_type = ::std::remove_cvref_t<T>;
	constexpr auto sz{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
	constexpr ::std::size_t reserve_buffer_size{sz.reserve_size == 0 ? 1u : sz.reserve_size};
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<reserve_buffer_size, char_type>)
	{
		// Small reserve storage for byte scatter output is materialized on the stack.
		char_type buffer[reserve_buffer_size];
		::fast_io::io_scatter_t *ptr{::fast_io::details::decay::prrsvsct_byte_common_impl(scatters, buffer, t)};
		if constexpr (line)
		{
			// The line variant appends a byte-length newline scatter after the materialized scatters.
			*ptr = ::fast_io::details::decay::line_scatter_common<char_type, void>;
			++ptr;
		}
		::fast_io::details::decay::print_scatter_write_all_bytes_maybe_coalesce(
			outstm, scatters, static_cast<::std::size_t>(ptr - scatters));
	}
	else
	{
		// Large reserve storage for byte scatter output is allocated before materialization.
		::fast_io::details::local_operator_new_array_ptr<char_type> buffer(reserve_buffer_size);
		::fast_io::io_scatter_t *ptr{::fast_io::details::decay::prrsvsct_byte_common_impl(scatters, buffer.ptr, t)};
		if constexpr (line)
		{
			// The line variant appends a byte-length newline scatter after the materialized scatters.
			*ptr = ::fast_io::details::decay::line_scatter_common<char_type, void>;
			++ptr;
		}
		::fast_io::details::decay::print_scatter_write_all_bytes_maybe_coalesce(
			outstm, scatters, static_cast<::std::size_t>(ptr - scatters));
	}
}

/// @brief    Materializes reserve-scatters output into character scatter descriptors and writes it.
/// @tparam   line          true when a trailing newline scatter is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   char_type     the output character type
/// @tparam   T             the reserve-scatters printable type
/// @param    outstm        the output stream reference
/// @param    scatters      the character scatter descriptor storage
/// @param    t             the argument to materialize
template <bool line, typename outputstmtype, ::std::integral char_type, typename T>
inline constexpr void print_reserve_scatters_basic_with_scatter(
	outputstmtype &outstm, ::fast_io::basic_io_scatter_t<char_type> *scatters, T &t)
{
	using value_type = ::std::remove_cvref_t<T>;
	constexpr auto sz{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
	constexpr ::std::size_t reserve_buffer_size{sz.reserve_size == 0 ? 1u : sz.reserve_size};
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<reserve_buffer_size, char_type>)
	{
		// Small reserve storage for character scatter output is materialized on the stack.
		char_type buffer[reserve_buffer_size];
		auto const result{
			print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>, scatters, buffer, t)};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				scatters, scatters + sz.scatters_size, result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				buffer, buffer + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
		{
			// Validate both producer cursors before appending a line descriptor or deriving the scatter count.
			::fast_io::fast_terminate();
		}
		auto ptr{result.scatters_pos_ptr};
		if constexpr (line)
		{
			// The line variant appends a character-length newline scatter after the materialized scatters.
			*ptr = ::fast_io::details::decay::line_scatter_common<char_type>;
			++ptr;
		}
		::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(
			outstm, scatters, static_cast<::std::size_t>(ptr - scatters));
	}
	else
	{
		// Large reserve storage for character scatter output is allocated before materialization.
		::fast_io::details::local_operator_new_array_ptr<char_type> buffer(reserve_buffer_size);
		auto const result{print_reserve_scatters_define(
			::fast_io::io_reserve_type<char_type, value_type>, scatters, buffer.ptr, t)};
		if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				scatters, scatters + sz.scatters_size, result.scatters_pos_ptr) ||
			!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
				buffer.ptr, buffer.ptr + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
		{
			// The dynamic allocation changes ownership only; it does not weaken the component cursor contract.
			::fast_io::fast_terminate();
		}
		auto ptr{result.scatters_pos_ptr};
		if constexpr (line)
		{
			// The line variant appends a character-length newline scatter after the materialized scatters.
			*ptr = ::fast_io::details::decay::line_scatter_common<char_type>;
			++ptr;
		}
		::fast_io::details::decay::print_scatter_write_all_maybe_coalesce(
			outstm, scatters, static_cast<::std::size_t>(ptr - scatters));
	}
}

/// @brief    Emits one reserve-scatters printable argument through the best scatter descriptor representation.
/// @details  Runtime paths prefer byte scatter descriptors when the stream supports byte writes; constant-evaluated
///           paths and character-only streams use typed character scatter descriptors.
/// @tparam   line          true when a trailing newline scatter is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   char_type     the output character type
/// @tparam   T             the reserve-scatters printable type
/// @param    outstm        the output stream reference
/// @param    t             the argument to emit
template <bool line, typename outputstmtype, ::std::integral char_type, typename T>
inline constexpr void print_control_single_reserve_scatters(outputstmtype &outstm, T &t)
{
	using value_type = ::std::remove_cvref_t<T>;
	constexpr auto sz{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
	static_assert(sz.scatters_size != SIZE_MAX);
	constexpr ::std::size_t scattersnum{sz.scatters_size + static_cast<::std::size_t>(line)};
	FAST_IO_IF_NOT_CONSTEVAL
	{
		// Runtime byte-capable streams can use byte scatter descriptors for reserve-scatters output.
		if constexpr (::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>)
		{
			if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scattersnum,
																						  ::fast_io::io_scatter_t>)
			{
				// Small byte scatter descriptor storage is kept on the stack.
				::fast_io::io_scatter_t scatters[scattersnum];
				::fast_io::details::decay::print_reserve_scatters_bytes_with_scatter<line, outputstmtype, char_type>(
					outstm, scatters, t);
			}
			else
			{
				// Large byte scatter descriptor storage is allocated before materializing the output.
				::fast_io::details::local_operator_new_array_ptr<::fast_io::io_scatter_t> scatters(scattersnum);
				::fast_io::details::decay::print_reserve_scatters_bytes_with_scatter<line, outputstmtype, char_type>(
					outstm, scatters.ptr, t);
			}
			return;
		}
	}
	using scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scattersnum, scatter_type>)
	{
		// Character scatter descriptor storage is kept on the stack when the policy allows it.
		scatter_type scatters[scattersnum];
		::fast_io::details::decay::print_reserve_scatters_basic_with_scatter<line, outputstmtype, char_type>(
			outstm, scatters, t);
	}
	else
	{
		// Character scatter descriptor storage is allocated when the fixed run exceeds the stack policy.
		::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(scattersnum);
		::fast_io::details::decay::print_reserve_scatters_basic_with_scatter<line, outputstmtype, char_type>(
			outstm, scatters.ptr, t);
	}
}

/// @brief Validates one context producer result before any cursor is committed or dereferenced.
/// @details At run time addresses are compared as unsigned integers so even a malformed pointer unrelated to the
///          supplied array is rejected deterministically. Constant evaluation uses language pointer ordering, where a
///          conforming producer necessarily returns within the supplied array. A partial result that consumes no
///          character is a protocol failure: retrying the identical nonempty window has no observable route to
///          progress and historically formed an unbounded loop. Printing has no recoverable producer-error channel,
///          so contract failure uses `fast_terminate` consistently with invalid reserve-producer cursors.
template <::std::integral char_type>
inline constexpr void validate_context_print_result(char_type *begin, char_type *end, char_type *result,
													bool done) noexcept
{
	bool in_window{};
	if (::std::is_constant_evaluated())
	{
		in_window = begin <= result && result <= end;
	}
	else
	{
		auto const begin_address{reinterpret_cast<::std::uintptr_t>(begin)};
		auto const end_address{reinterpret_cast<::std::uintptr_t>(end)};
		auto const result_address{reinterpret_cast<::std::uintptr_t>(result)};
		in_window = begin_address <= result_address && result_address <= end_address &&
					((result_address - begin_address) % sizeof(char_type) == 0u);
	}
	if (!in_window || (!done && result == begin)) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
}

/// @brief    Drains a context-printable object through a caller-provided context buffer.
/// @details  Each iteration asks the context producer for the next filled window and writes it to the stream; the line
///           variant appends the trailing newline only to the final completed window.
/// @tparam   line          true when a trailing newline is appended after the final context window
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   context_type  the context state type
/// @tparam   T             the context-printable argument type
/// @tparam   char_type     the output character type
/// @param    outstm        the output stream reference
/// @param    st            the context object that produces output windows
/// @param    t             the argument being printed
/// @param    buffer        the first character in the context buffer
/// @param    buffered      one past the reusable buffered range passed to the producer
template <bool line, typename outputstmtype, typename context_type, typename T, ::std::integral char_type>
inline constexpr void print_context_define_to_window(outputstmtype &outstm, context_type &st, T &t, char_type *buffer,
													 char_type *buffered)
{
	for (;;)
	{
		auto [resit, done] = st.print_context_define(t, buffer, buffered);
		::fast_io::details::decay::validate_context_print_result(buffer, buffered, resit, done);
		if constexpr (line)
		{
			// The line variant may append the newline to the final context output window.
			if (done)
			{
				// The producer finished, so the newline is appended before this final window is written.
				*resit = ::fast_io::char_literal_v<u8'\n', char_type>;
				++resit;
			}
			::fast_io::operations::decay::write_all_decay_dispatch(outstm, buffer, resit);
			if (done)
			{
				// The final line-terminated context window has been written.
				return;
			}
		}
		else
		{
			// The non-line variant writes each produced context window without adding characters.
			::fast_io::operations::decay::write_all_decay_dispatch(outstm, buffer, resit);
			if (done)
			{
				// The final context window has been written.
				return;
			}
		}
	}
}

/// @brief Pairs a one-pass source with a destination that makes repeated direct emission cheap.
/// @details Capability alone is insufficient on both axes. `print_define` must be callable for the exact normalized
///          output, the source must opt into its one-pass representation, and the destination must state that direct
///          streaming is cheaper than a measure/materialize/write cycle. Explicit whole-output coalescing policies win
///          over this pair. Native run-time scatter admission is tested before every call site of this predicate, so a
///          sink capable of one batched scatter operation never degrades into per-element writes.
template <::std::integral char_type, typename output, typename T>
inline constexpr bool print_one_pass_direct_streaming_available_v =
	::fast_io::details::direct_printable_to<char_type, output, ::std::remove_cvref_t<T>> &&
	::fast_io::one_pass_printable_preferred<char_type, ::std::remove_cvref_t<T>> &&
	::fast_io::direct_streaming_preferred_stream<char_type, output> &&
	::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, output>() == 0u &&
	::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold<char_type, output>() == 0u;

/// @brief Owns the mutable state shared by every copy of a bounded one-pass staging observer.
template <::std::integral char_type, typename output_type>
struct print_single_pass_staging_state
{
	output_type *output{};
	char_type *begin{};
	char_type *current{};
	char_type *end{};
	bool external_write_active{};
};

/// @brief Lightweight output observer presented to a staged single-pass producer.
template <::std::integral char_type, typename output_type>
struct print_single_pass_staging_ref
{
	using output_char_type = char_type;
	print_single_pass_staging_state<char_type, output_type> *state{};
};

template <::std::integral char_type, typename output_type>
[[nodiscard]] inline constexpr char_type *obuffer_begin(
	print_single_pass_staging_ref<char_type, output_type> output) noexcept
{
	return output.state->begin;
}

template <::std::integral char_type, typename output_type>
[[nodiscard]] inline constexpr char_type *obuffer_curr(
	print_single_pass_staging_ref<char_type, output_type> output) noexcept
{
	return output.state->current;
}

template <::std::integral char_type, typename output_type>
[[nodiscard]] inline constexpr char_type *obuffer_end(
	print_single_pass_staging_ref<char_type, output_type> output) noexcept
{
	return output.state->end;
}

template <::std::integral char_type, typename output_type>
inline constexpr void obuffer_set_curr(
	print_single_pass_staging_ref<char_type, output_type> output,
	char_type *current) noexcept
{
	output.state->current = current;
}

template <::std::integral char_type, typename output_type>
[[nodiscard]] inline constexpr ::std::true_type
	print_deferred_obuffer_commit_safe(
		::fast_io::io_reserve_type_t<
			char_type, print_single_pass_staging_ref<char_type, output_type>>) noexcept
{
	// The adapter owns one stable caller-provided array. Cursor publication is a
	// plain state update until an explicit overflow/final flush reaches the sink.
	return {};
}

/// @brief Writes the staged prefix once and resets the local put area.
/// @details The in-progress flag prevents an exception from the destination write from causing the same unknown
///          partially-consumed range to be retried by the producer-exception cleanup path.
template <::std::integral char_type, typename output_type>
inline constexpr void print_single_pass_staging_flush(
	print_single_pass_staging_state<char_type, output_type> &state)
{
	if (state.current == state.begin)
	{
		return;
	}
	char_type *const last{state.current};
	state.external_write_active = true;
	::fast_io::operations::decay::write_all_decay_dispatch(
		*state.output, state.begin, last);
	state.external_write_active = false;
	state.current = state.begin;
}

template <::std::integral char_type, typename output_type>
inline constexpr void obuffer_overflow(
	print_single_pass_staging_ref<char_type, output_type> output,
	char_type value)
{
	auto &state{*output.state};
	::fast_io::details::decay::print_single_pass_staging_flush(state);
	*state.current++ = value;
}

/// @brief Publishes the current staging window before retrying one audited bounded leaf.
/// @details A failed conservative bound has not formatted the leaf. Flushing the completed prefix restores the full
///          window, after which core can retry the cheap bound without measuring or observing the source twice.
template <::std::integral char_type, typename output_type>
inline constexpr void print_single_pass_bounded_put_area_refresh(
	print_single_pass_staging_ref<char_type, output_type> output)
{
	::fast_io::details::decay::print_single_pass_staging_flush(
		*output.state);
}

/// @brief Completes a bulk write while retaining short suffixes for the final record write.
/// @details Existing staged bytes are filled to capacity before one flush. A large range arriving at an empty window
///          uses the underlying write directly; a short range remains staged so following punctuation can share the
///          same final operation.
template <::std::integral char_type, typename output_type>
inline constexpr void write_all_overflow_define(
	print_single_pass_staging_ref<char_type, output_type> output,
	char_type const *first, char_type const *last)
{
	auto &state{*output.state};
	while (first != last)
	{
		auto const capacity{static_cast<::std::size_t>(state.end - state.begin)};
		auto const available{static_cast<::std::size_t>(state.end - state.current)};
		auto const remaining{static_cast<::std::size_t>(last - first)};
		if (state.current == state.begin && capacity <= remaining)
		{
			state.external_write_active = true;
			::fast_io::operations::decay::write_all_decay_dispatch(
				*state.output, first, last);
			state.external_write_active = false;
			return;
		}
		auto const copied{remaining < available ? remaining : available};
		state.current = ::fast_io::details::non_overlapped_copy_n(
			first, copied, state.current);
		first += copied;
		if (state.current == state.end)
		{
			::fast_io::details::decay::print_single_pass_staging_flush(state);
		}
	}
}

/// @brief Formats one source traversal through a stream-authorized bounded stack window.
/// @details A record no larger than the destination's threshold reaches it in one write. Larger records flush full
///          windows and continue without replaying the source. If a producer throws before a destination write starts,
///          cleanup best-effort flushes the already-produced prefix and then preserves the original exception. The
///          ordinary inlining is retained after the complete GCC 11--16 and Clang 17--23 A/B: GCC 16 and every tested
///          Clang emit less text without the noinline boundary, while the run-time differences at the positive
///          endpoints are within noise. The destination policy, not a global placement attribute, owns this choice.
template <bool line, ::std::integral char_type, ::std::size_t capacity,
		  typename output_type, typename value_type>
	requires(capacity != 0u)
inline constexpr void print_single_pass_stage_and_write(
	output_type &output, value_type &value)
{
	char_type buffer[capacity];
	::fast_io::details::decay::print_single_pass_staging_state<
		char_type, output_type>
		state{__builtin_addressof(output), buffer, buffer,
			  buffer + capacity, false};
	::fast_io::details::decay::print_single_pass_staging_ref<
		char_type, output_type>
		staging{__builtin_addressof(state)};
#if (defined(_MSC_VER) && defined(_HAS_EXCEPTIONS) && _HAS_EXCEPTIONS != 0) || \
	(!defined(_MSC_VER) && defined(__cpp_exceptions))
	try
	{
#endif
		print_define(::fast_io::io_reserve_type<char_type, value_type>,
					 staging, value);
		if constexpr (line)
		{
			::fast_io::operations::decay::char_put_decay_dispatch(
				staging, ::fast_io::char_literal_v<u8'\n', char_type>);
		}
		::fast_io::details::decay::print_single_pass_staging_flush(state);
#if (defined(_MSC_VER) && defined(_HAS_EXCEPTIONS) && _HAS_EXCEPTIONS != 0) || \
	(!defined(_MSC_VER) && defined(__cpp_exceptions))
	}
	catch (...)
	{
		if (!state.external_write_active)
		{
			try
			{
				::fast_io::details::decay::print_single_pass_staging_flush(state);
			}
			catch (...)
			{
				// Preserve the producer exception; cleanup cannot safely replace it.
			}
		}
		throw;
	}
#endif
}

/// @brief Returns the currently writable character count without subtracting a lazy null cursor pair.
/// @details Audited outputs expose either an all-null lazy window or one ordered allocation. Their run-time address
///          distance is therefore both defined for the null state and numerically identical to array subtraction for
///          the live state. Constant evaluation retains language pointer subtraction after the non-null proof.
template <::std::integral char_type, typename output_type>
inline constexpr ::std::size_t print_obuffer_remaining_size(
	char_type *current, char_type *end) noexcept
{
	if constexpr (::fast_io::obuffer_address_distance_safe<char_type, output_type>)
	{
		if (::std::is_constant_evaluated())
		{
			return current == nullptr ? 0u : static_cast<::std::size_t>(end - current);
		}
		auto const current_address{reinterpret_cast<::std::uintptr_t>(current)};
		auto const end_address{reinterpret_cast<::std::uintptr_t>(end)};
		return static_cast<::std::size_t>((end_address - current_address) / sizeof(char_type));
	}
	else
	{
		if (current == nullptr || end == nullptr)
		{
			return 0u;
		}
		auto const difference{end - current};
		return difference < 0 ? 0u : static_cast<::std::size_t>(difference);
	}
}

/// @brief Identifies the normalized carrier for an ordinary signed 64-bit scalar.
template <typename value_type>
inline consteval bool print_signed_64_scalar_carrier() noexcept
{
	if constexpr (requires {
					  typename value_type::scalar_flags_type;
					  typename value_type::value_type;
				  })
	{
		return ::std::same_as<
			::std::remove_cvref_t<typename value_type::value_type>,
			::std::int64_t>;
	}
	else
	{
		return false;
	}
}

/// @brief Proves the exact grow-in-place protocol for one already-normalized source and output.
/// @details Source and destination evidence remain deliberately separate. The source promises a cached exact size,
///          non-throwing checked emission, and independence from destination relocation. The output promises both an
///          explicit growable put area and one deferred cursor publication. Non-throwing cursor access is required
///          because exact emission starts before the new logical cursor is published.
template <::std::integral char_type, typename output_type, typename value_type>
inline constexpr bool print_cached_precise_growable_obuffer_available_v =
	::fast_io::output_growth_independent_precise_reserve_printable<
		char_type, value_type> &&
	::fast_io::operations::decay::defines::has_obuffer_basic_operations<
		output_type> &&
	::fast_io::operations::decay::defines::has_obuffer_flush_reserve_define<
		output_type> &&
	::fast_io::deferred_obuffer_commit_safe<char_type, output_type> &&
	requires(output_type &output, char_type *cursor) {
#if defined(__HERBCEPTIONS__)
		// Let C, E, and S be the exact cursor-read, end-read, and cursor-publication expressions used below.
		// Deferred publication is admissible only when each operation proves `!throws(X) && noexcept(X)`.
		requires(!throws((obuffer_curr(output))));
		requires(!throws((obuffer_end(output))));
		requires(!throws((obuffer_set_curr(output, cursor))));
#endif
		{ obuffer_curr(output) } noexcept -> ::std::same_as<char_type *>;
		{ obuffer_end(output) } noexcept -> ::std::same_as<char_type *>;
		{ obuffer_set_curr(output, cursor) } noexcept -> ::std::same_as<void>;
	};

/// @brief Appends one cached exact source directly into a growable destination put area.
/// @details The source is measured before any destination mutation. A capacity miss then requests exactly the
///          additional complete record, after which fresh cursors are queried because growth may relocate storage.
///          Emission is non-throwing and its endpoint is checked before one final cursor publication. Thus an
///          allocation failure leaves the old destination unchanged, while no formatter exception can strand raw
///          bytes behind an unpublished cursor. The caller owns mutex and whole-run status dispatch.
template <bool line, ::std::integral char_type, typename output_type,
		  typename value_type>
	requires ::fast_io::details::decay::
		print_cached_precise_growable_obuffer_available_v<
			char_type, output_type, value_type>
	inline constexpr void print_cached_precise_growable_obuffer(
		output_type &output, value_type &value)
{
	using source_type = ::std::remove_cvref_t<value_type>;
	::std::size_t const payload_size{print_reserve_precise_size(
		::fast_io::io_reserve_type<char_type, source_type>, value)};
	::std::size_t const total_size{
		::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<
			char_type>(payload_size, static_cast<::std::size_t>(line))};
	if (total_size == SIZE_MAX) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	if (total_size == 0u)
	{
		// Preserve the exact formatter invocation even for an empty spelling, but do not require a zero-capacity output
		// to manufacture a non-null array. The protocol forbids a write when its exact extent is zero.
		char_type sentinel{};
		char_type *const first{__builtin_addressof(sentinel)};
		char_type *const actual_end{print_reserve_precise_define(
			::fast_io::io_reserve_type<char_type, source_type>, first,
			payload_size, value)};
		if (actual_end != first) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		return;
	}

	char_type *current{obuffer_curr(output)};
	char_type *put_end{obuffer_end(output)};
	::std::ptrdiff_t available{
		current == nullptr || put_end == nullptr ? -1 : put_end - current};
	if (available < 0 ||
		total_size > static_cast<::std::size_t>(available)) [[unlikely]]
	{
		// This operation requests additional capacity after the current logical cursor, rather than an absolute
		// destination size. The source-side relocation proof is what permits this call before emission.
		obuffer_flush_reserve_define(output, total_size);
		current = obuffer_curr(output);
		put_end = obuffer_end(output);
		available = current == nullptr || put_end == nullptr
						? -1
						: put_end - current;
		if (available < 0 ||
			total_size > static_cast<::std::size_t>(available)) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
	}

	char_type *expected_end{current + payload_size};
	char_type *const actual_end{print_reserve_precise_define(
		::fast_io::io_reserve_type<char_type, source_type>, current,
		payload_size, value)};
	if (actual_end != expected_end) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	// The line specialization publishes its newline in the same single cursor commit as the exact payload.
	if constexpr (line)
	{
		*expected_end++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	obuffer_set_curr(output, expected_end);
}

/// @brief    A destination whose put-area cursor can accept an exact reserve emission.
/// @details  Keep this separate from the broad obuffer capability: adapters with a partial or refill-on-demand
///           cursor protocol must retain the established materialize/write fallback.
template <typename output>
concept print_direct_obuffer_reserve_destination =
	::fast_io::direct_obuffer_copy_safe<typename output::output_char_type, output> &&
	requires(output &out, typename output::output_char_type *p) {
		{ obuffer_curr(out) } -> ::std::same_as<typename output::output_char_type *>;
		{ obuffer_end(out) } -> ::std::same_as<typename output::output_char_type *>;
		obuffer_set_curr(out, p);
	};

/// @brief Identifies an ordinary reserve writer which may initialize scratch inside its complete reserve bound.
/// @details Such a writer is memory-safe in an owning/refillable put area, but a fixed external view exposes the bytes
///          after its published cursor to the caller. Source implementations specialize this internal trait only for
///          audited library writers; it is not inferred from reserve printability itself.
template <::std::integral char_type, typename T>
struct print_fixed_external_overstore_traits
{
	inline static constexpr bool available{};
};

template <bool line, typename output, ::std::integral char_type,
		  typename... Args>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_fixed_static_reserve_run(output &, Args &...);

/// @brief    Emits one already-forwarded printable control argument to an output stream.
/// @details  The dispatcher selects the most specialized single-argument path available: scatter, static reserve,
///           dynamic reserve, reserve-scatters, context printing, or the ordinary print_define customization.
/// @tparam   line   true when a trailing newline is appended after this argument
/// @tparam   output the decayed output stream reference type
/// @tparam   T      the argument type
/// @param    outstm the output stream reference
/// @param    t      the argument to emit
template <bool line = false, typename output, typename T>
inline constexpr void print_control_single(output &outstm, T &t)
{
	using char_type = typename output::output_char_type;
	using value_type = ::std::remove_cvref_t<T>;
	using static_scatter_traits =
		::fast_io::details::decay::print_static_scatter_traits<char_type, value_type>;
	constexpr bool asan_activated{::fast_io::details::asan_state::current == ::fast_io::details::asan_state::activate};
	constexpr auto lfch{char_literal_v<u8'\n', char_type>};
	constexpr ::std::size_t single_pass_staging_capacity{
		::fast_io::details::decay::print_full_output_coalesce_threshold<
			char_type, output>()};
	if constexpr (
		!line &&
		::fast_io::details::decay::
			print_output_retains_static_scatter<output> &&
		static_scatter_traits::available)
	{
		// A non-line, unbuffered sink can consume the immutable source range itself.  Keeping the fixed-scatter pointer
		// intact avoids materializing the same bytes in a temporary before the primitive write.  Buffered destinations
		// deliberately stay on the reserve path below, where the type-level extent exposes short copies as immediate
		// stores; line output also stays there so payload and newline retain one materialized write boundary.
		static_assert(static_scatter_traits::size < PTRDIFF_MAX);
		if constexpr (static_scatter_traits::size != 0u)
		{
			auto const scatter{static_scatter_traits::define(t)};
			auto const base{scatter.base};
			::fast_io::operations::decay::write_all_decay_dispatch(
				outstm, base, base + static_scatter_traits::size);
		}
	}
	else if constexpr (
		::fast_io::scatter_printable_for<char_type, decltype((t))> &&
		(!static_scatter_traits::available ||
		 ::fast_io::details::decay::
			 print_output_retains_static_scatter<output>))
	{
		// The entry-owned control argument is a named lvalue here; only that exact CPO overload can justify this branch.
#if 0
		basic_io_scatter_t<char_type> scatter;
		if constexpr(::std::same_as<T,basic_io_scatter_t<char_type>>)
		{
			scatter=t;
		}
		else
		{
			scatter=print_scatter_define(::fast_io::io_reserve_type<char_type,value_type>,t);
		}
		if constexpr(line)
		{
			if constexpr(contiguous_output_stream<output>)
			{
				auto curr=obuffer_curr(out);
				auto end=obuffer_end(out);
				::std::ptrdiff_t sz(end-curr-1);
				::std::size_t const len{scatter.len};
				if(sz<static_cast<::std::ptrdiff_t>(len))
					fast_terminate();
				curr=non_overlapped_copy_n(scatter.base,scatter.len,curr);
				*curr=lfch;
				++curr;
				obuffer_set_curr(outstm,curr);
			}
			else if constexpr(::fast_io::operations::decay::defines::has_obuffer_basic_operations<output>)
			{
				auto curr=obuffer_curr(out);
				auto end=obuffer_end(out);
				::std::size_t const len{scatter.len};
				::std::ptrdiff_t sz(end-curr-1);
				if(static_cast<::std::ptrdiff_t>(len)<sz)[[likely]]
				{
					curr=details::non_overlapped_copy_n(scatter.base,len,curr);
					*curr=lfch;
					++curr;
					obuffer_set_curr(outstm,curr);
				}
				else
				{
					write(outstm,scatter.base,scatter.base+scatter.len);
					put(outstm,lfch);
				}
			}
			else 
			{
				write(outstm,scatter.base,scatter.base+scatter.len);
				write(outstm,__builtin_addressof(lfch),
				__builtin_addressof(lfch)+1);
			}
		}
		else
		{
			write(outstm,scatter.base,scatter.base+scatter.len);
		}
#endif
		basic_io_scatter_t<char_type> scatter_res{print_scatter_define(::fast_io::io_reserve_type<char_type, value_type>, t)};

		if constexpr (line)
		{
			// The line variant writes the scatter range and a trailing newline as one scatter-oriented output.
			::fast_io::details::decay::print_single_scatter_with_line(outstm, scatter_res);
		}
		else
		{
			// A fixed-capacity memory view can commit the scatter directly into its put area. Other destinations retain
			// the ordinary write CPO so growing buffers and refillable wrappers keep their established semantics.
			if constexpr (print_direct_obuffer_reserve_destination<output>)
			{
				if (scatter_res.len == 0u)
				{
					return;
				}
				auto curr{obuffer_curr(outstm)};
				auto end{obuffer_end(outstm)};
				if (curr == nullptr || end == nullptr || curr > end ||
					static_cast<::std::size_t>(end - curr) < scatter_res.len) [[unlikely]]
				{
					fast_terminate();
				}
				curr = ::fast_io::details::non_overlapped_copy_n(scatter_res.base, scatter_res.len, curr);
				obuffer_set_curr(outstm, curr);
			}
			else
			{
				::fast_io::operations::decay::write_all_decay_dispatch(outstm, scatter_res.base, scatter_res.base + scatter_res.len);
			}
		}
	}
	else if constexpr (reserve_printable<char_type, value_type>)
	{
		// Static reserve-printable arguments have a compile-time output size and can be materialized directly.
		constexpr ::std::size_t real_size{print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>)};
		constexpr ::std::size_t size{real_size + static_cast<::std::size_t>(line)};
		static_assert(real_size < PTRDIFF_MAX);
		constexpr bool prefer_static_reserve_staging{
#if defined(__aarch64__) || defined(_M_ARM64)
			::std::same_as<char_type, char> && real_size == 20u &&
			::fast_io::details::decay::print_signed_64_scalar_carrier<value_type>() &&
			requires(output &staging_output) {
				print_static_reserve_staging_output_define(staging_output);
			}
#else
			false
#endif
		};
		// Only a genuinely contiguous put-area may take this direct commit path.  Some
		// wrappers expose the obuffer cursor protocol while using a zero-sized or
		// refill-on-demand area; treating those as an always-fitting reserve would
		// turn a normal short write into fast_terminate (UD2).
		if constexpr (print_direct_obuffer_reserve_destination<output>)
		{
			// A reserve extent is a physical write bound, not necessarily the logical spelling. Mixed dispatch can reach
			// this leaf even when the complete run was not statically reserve-printable, so reuse the fixed-view helper:
			// it emits directly when the bound fits and otherwise stages the returned logical prefix before committing it.
			::fast_io::details::decay::print_fixed_static_reserve_run<
				line, output, char_type>(outstm, t);
		}
		else
		{
			if constexpr (prefer_static_reserve_staging)
			{
				// The formatter keeps its fixed-address fast path, then the owning buffer
				// absorbs the short result through its established write CPO.
				char_type buffer[size];
				char_type *iter{print_reserve_define(
					::fast_io::io_reserve_type<char_type, value_type>, buffer, t)};
				if constexpr (line)
				{
					*iter++ = lfch;
				}
				decltype(auto) staging_output{
					print_static_reserve_staging_output_define(outstm)};
				::fast_io::operations::decay::write_all_decay_dispatch(
					staging_output, buffer, iter);
			}
			else if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> &&
							   !asan_activated)
			{
				// Buffered streams first try to emit the static reserve output into the current put area.
				char_type *bcurr{obuffer_curr(outstm)};
				char_type *bend{obuffer_end(outstm)};
				// Equality is a valid exact fit: emission may commit the put cursor to `bend`.  The former strict
				// comparison forced a flush or temporary allocation despite having precisely enough live storage.
				bool fits{size <= ::fast_io::details::decay::print_obuffer_remaining_size<
									  char_type, output>(bcurr, bend)};
				if constexpr (minimum_buffer_output_stream_require_size_impl<output, size>)
				{
					// Streams with a sufficient declared minimum buffer can flush once and then emit in place.
					if (!fits) [[unlikely]]
					{
						// The current put area is short, so prepare the minimum-size buffer before emission.
						obuffer_minimum_size_flush_prepare_define(outstm);
						bcurr = obuffer_curr(outstm);
					}
					// The minimum-size CPO contract guarantees a live put area after a miss;
					// rechecking null here adds a branch to every buffered scalar emission.
					bcurr = print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, bcurr, t);
					if constexpr (line)
					{
						// The line variant appends the newline in the same output-buffer commit.
						*bcurr = lfch;
						++bcurr;
					}
					obuffer_set_curr(outstm, bcurr);
				}
				else
				{
					// Without a minimum-buffer guarantee, choose between direct put-area output and fallback storage.
					if (fits) [[likely]]
					{
						// The current put area has enough room, so emit directly and commit the new cursor.
						bcurr = print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, bcurr, t);
						if constexpr (line)
						{
							// The line variant appends the newline in the same output-buffer commit.
							*bcurr = lfch;
							++bcurr;
						}
						obuffer_set_curr(outstm, bcurr);
					}
					else [[unlikely]]
					{
						// The current put area is too small, so materialize the output outside the stream buffer.
						if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<size, char_type>)
						{
							// A small static reserve output is materialized on the stack and written once.
							char_type buffer[size];
							char_type *i{
								print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, buffer, t)};
							if constexpr (line)
							{
								// The line variant appends the newline before the stack buffer is written.
								*i = lfch;
								++i;
							}
							::fast_io::details::decay::print_write_all_materialized(outstm, buffer, i);
						}
						else
						{
							// A large static reserve output is materialized in heap storage and written once.
							::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
							char_type *i{
								print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, newptr.ptr, t)};
							if constexpr (line)
							{
								// The line variant appends the newline before the heap buffer is written.
								*i = lfch;
								++i;
							}
							::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, i);
						}
					}
				}
			}
			else
			{
				// Without a usable output buffer, choose stack or heap materialization for the static reserve output.
				if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<size, char_type>)
				{
					// Without a usable output buffer, small static reserve output is materialized on the stack.
					char_type buffer[size];
					char_type *i{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, buffer, t)};
					if constexpr (line)
					{
						// The line variant appends the newline before the stack buffer is written.
						*i = lfch;
						++i;
					}
					::fast_io::details::decay::print_write_all_materialized(outstm, buffer, i);
				}
				else
				{
					// Without a usable output buffer, large static reserve output is materialized in heap storage.
					::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
					char_type *i{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, newptr.ptr, t)};
					if constexpr (line)
					{
						// The line variant appends the newline before the heap buffer is written.
						*i = lfch;
						++i;
					}
					::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, i);
				}
			}
		}
	}
	else if constexpr (
		::fast_io::details::direct_printable_to<char_type, output, value_type> &&
		((::fast_io::put_area_printable_preferred<char_type, value_type> &&
		  ::fast_io::operations::decay::defines::has_obuffer_basic_operations<output>) ||
		 (::fast_io::buffered_printable_preferred<char_type, value_type> &&
		  (::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> ||
		   ::fast_io::buffered_printable_preferred_stream<char_type, output>))))
	{
		// The source and destination cost proofs are paired deliberately. A put-area-only producer cannot inherit a
		// generic append-stream preference, while a generally buffered-preferred producer may use either destination
		// proof. In both cases direct printing removes a redundant measure/materialize traversal without changing the
		// canonical strategy for unbuffered streams.
		print_define(::fast_io::io_reserve_type<char_type, value_type>, outstm, t);
		if constexpr (line)
		{
			::fast_io::operations::decay::char_put_decay_dispatch(outstm, lfch);
		}
	}
	else if constexpr (
		::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
			char_type, output, value_type &>)
	{
		// An unbuffered stream may select the object's run-time descriptor representation when it has either native
		// scatter output or an explicit full-payload coalescing fallback. The object remains dynamic-reserve printable for
		// string construction, buffered streams, and direct-write destinations that did not opt into that fallback cost model.
		::fast_io::details::decay::print_runtime_scatter_plan_fast_entry<line>(outstm, t);
	}
	else if constexpr (
		::fast_io::details::decay::print_one_pass_direct_streaming_available_v<
			char_type, output, value_type>)
	{
		// This branch is intentionally after run-time scatter selection. The paired positive cost proofs make direct
		// incremental emission valid for a cheap boundary without teaching ordinary write capability that system calls
		// are cheap. No size query or temporary payload is formed on this path.
		print_define(::fast_io::io_reserve_type<char_type, value_type>, outstm, t);
		if constexpr (line)
		{
			::fast_io::operations::decay::char_put_decay_dispatch(outstm, lfch);
		}
	}
	else if constexpr (
		reserve_scatters_printable<char_type, value_type> &&
		dynamic_reserve_printable<char_type, value_type> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> &&
		::fast_io::details::decay::print_has_direct_scatter_write_operations<output>)
	{
		// A static descriptor plan is already the final representation for an unbuffered native-scatter sink. Selecting
		// the contiguous dynamic-reserve fallback here would add a size pass and a complete payload copy before issuing
		// another high-level output dispatch. The backend may split or retry either dispatch. Buffered destinations
		// deliberately do not take this branch: their existing writable area makes contiguous reserve emission the
		// cheaper representation.
		::fast_io::details::decay::print_control_single_reserve_scatters<line, output, char_type>(outstm, t);
	}
	else if constexpr (dynamic_reserve_printable<char_type, value_type>)
	{
		// Dynamic reserve-printable arguments are measured at run time before choosing a materialization path.
		::std::size_t size{print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>, t)};
		constexpr ::std::size_t stack_buffer_size{
			::fast_io::details::decay::dynamic_print_reserve_static_stack_size<line, char_type, value_type>()};
		if constexpr (line)
		{
			// The line variant reserves one additional character and validates pointer-difference bounds.
			constexpr ::std::size_t mx{::std::numeric_limits<::std::ptrdiff_t>::max() - 1};
			if (size >= mx)
			{
				// The measured run cannot be represented safely with a trailing newline.
				fast_terminate();
			}
			++size;
		}
		else
		{
			// The non-line variant only needs to validate the measured dynamic output size.
			constexpr ::std::size_t mx{::std::numeric_limits<::std::ptrdiff_t>::max()};
			if (mx < size)
			{
				// The measured run cannot be represented safely by pointer differences.
				fast_terminate();
			}
		}
#if 0
		if constexpr(contiguous_output_stream<output>)
		{
			auto bcurr{obuffer_curr(outstm)};
			auto bend{obuffer_end(outstm)};
			auto it{print_reserve_define(::fast_io::io_reserve_type<char_type,value_type>,bcurr,t,size)};
			::std::size_t diff{static_cast<::std::size_t>(bend-bcurr)};
			if(diff<size)[[unlikely]]
				fast_terminate();
			if constexpr(line)
			{
				*it=lfch;
				++it;
			}
			obuffer_set_curr(outstm,it);
		}
		else
#endif
		{
			if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> &&
						  !asan_activated)
			{
				// Buffered streams first try to emit the dynamic reserve output into the current put area.
				auto curr{obuffer_curr(outstm)};
				auto ed{obuffer_end(outstm)};
				::std::ptrdiff_t const diff{
					curr == nullptr || ed == nullptr ? -1 : ed - curr};
				// `[curr, ed)` contains exactly `diff` writable elements; equality consumes the final slot and leaves
				// the cursor at `ed`, so it must stay on the direct path.
				bool fits{static_cast<::std::ptrdiff_t>(size) <= diff};
				if (fits)
				{
					// The current put area has enough room, so emit directly and commit the new cursor.
					auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, curr, t)};
					if constexpr (line)
					{
						// The line variant appends the newline in the same output-buffer commit.
						*it = lfch;
						++it;
					}
					obuffer_set_curr(outstm, it);
				}
				else
#if __has_cpp_attribute(unlikely)
					[[unlikely]]
#endif
				{
					// The current put area is too small, so dynamic output falls back to temporary storage.
					if constexpr (stack_buffer_size != 0)
					{
						// A static stack hint allows small dynamic output to avoid heap allocation.
						if (size <= stack_buffer_size)
						{
							// The measured dynamic output fits in the bounded stack buffer.
							char_type stack_buffer[stack_buffer_size];
							auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>,
														 stack_buffer, t)};
							if constexpr (line)
							{
								// The line variant appends the newline before the stack buffer is written.
								*it = lfch;
								++it;
							}
							::fast_io::details::decay::print_write_all_materialized(outstm, stack_buffer, it);
						}
						else
						{
							// The measured dynamic output exceeds the stack hint and is materialized on the heap.
							::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
							auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>,
														 newptr.ptr, t)};
							if constexpr (line)
							{
								// The line variant appends the newline before the heap buffer is written.
								*it = lfch;
								++it;
							}
							::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, it);
						}
					}
					else
					{
						// Without a usable static stack hint, dynamic output is materialized on the heap.
						::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
						auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, newptr.ptr,
													 t)};
						if constexpr (line)
						{
							// The line variant appends the newline before the heap buffer is written.
							*it = lfch;
							++it;
						}
						::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, it);
					}
				}
			}
			else
			{
				// Without a usable output buffer, dynamic output chooses between static stack hint and heap storage.
				if constexpr (stack_buffer_size != 0)
				{
					// Without a usable output buffer, a static stack hint may still avoid heap allocation.
					if (size <= stack_buffer_size)
					{
						// The measured dynamic output fits in the bounded stack buffer.
						char_type buffer[stack_buffer_size];
						auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, buffer, t)};
						if constexpr (line)
						{
							// The line variant appends the newline before the stack buffer is written.
							*it = lfch;
							++it;
						}
						::fast_io::details::decay::print_write_all_materialized(outstm, buffer, it);
					}
					else
					{
						// The measured dynamic output exceeds the stack hint and is materialized on the heap.
						::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
						auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, newptr.ptr, t)};
						if constexpr (line)
						{
							// The line variant appends the newline before the heap buffer is written.
							*it = lfch;
							++it;
						}
						::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, it);
					}
				}
				else
				{
					// Without a stack hint, dynamic output is materialized on the heap and written once.
					::fast_io::details::local_operator_new_array_ptr<char_type> newptr(size);
					auto it{print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, newptr.ptr, t)};
					if constexpr (line)
					{
						// The line variant appends the newline before the heap buffer is written.
						*it = lfch;
						++it;
					}
					::fast_io::details::decay::print_write_all_materialized(outstm, newptr.ptr, it);
				}
			}
		}
	}
	else if constexpr (reserve_scatters_printable<char_type, value_type>)
	{
		// Reserve-scatters printable arguments are emitted through the scatter descriptor materialization path.
		::fast_io::details::decay::print_control_single_reserve_scatters<line, output, char_type>(outstm, t);
	}
	else if constexpr (context_printable<char_type, value_type>)
	{
		// One owned state spans every producer window. The shared owner keeps small states allocation-free while moving
		// large or over-aligned states out of the hot frame without weakening construction or destruction semantics.
		using state_type = ::fast_io::details::print_context_state_t<char_type, value_type>;
		::fast_io::details::with_print_context_state<state_type>([&](state_type &st) constexpr {
			constexpr ::std::size_t reserved_size{
				::fast_io::details::decay::context_print_static_buffer_size_v<line, char_type, value_type>};
			constexpr ::std::size_t producer_window_size{
				reserved_size - static_cast<::std::size_t>(line)};
			constexpr ::std::ptrdiff_t producer_window_difference{
				static_cast<::std::ptrdiff_t>(producer_window_size)};
			if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<output>)
			{
				// Buffered streams let the context producer fill the current put area directly when possible.
				for (;;)
				{
					auto bcurr{obuffer_curr(outstm)};
					auto bed{obuffer_end(outstm)};
					if (bcurr == nullptr || bed == nullptr || bed <= bcurr)
#if __has_cpp_attribute(unlikely)
						[[unlikely]]
#endif
					{
						// An exhausted put area must be refreshed or replaced by an explicit context window.
						if constexpr (minimum_buffer_output_stream_require_size_impl<output, producer_window_size>)
						{
							// Only the producer window must be contiguous here. The newline uses char_put after completion and
							// therefore must not raise the stream's minimum contiguous-buffer requirement.
							obuffer_minimum_size_flush_prepare_define(outstm);
							bcurr = obuffer_curr(outstm);
							bed = obuffer_end(outstm);
						}
						else
						{
							if constexpr (
								::fast_io::operations::decay::defines::has_output_or_io_stream_buffer_flush_define<output>)
							{
								// A flushable put area gets one opportunity to expose a fresh producer window.  Merely having
								// obuffer cursors does not imply this CPO; keeping the probe here prevents a valid fixed-memory
								// sink from failing during template instantiation in a run-time-only exhausted-buffer branch. The
								// named observer must use policy dispatch so an inline put cursor is never flushed through a copy.
								::fast_io::operations::decay::output_stream_buffer_flush_decay_dispatch(outstm);
								bcurr = obuffer_curr(outstm);
								bed = obuffer_end(outstm);
							}
							if constexpr (
								!::fast_io::operations::decay::defines::has_output_or_io_stream_buffer_flush_define<output>)
							{
								// A terminal/fixed put area cannot manufacture capacity after exhaustion.  Continue the context
								// operation through an explicitly owned window and the stream's ordinary write protocol.
								if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<
												  reserved_size, char_type>)
								{
									char_type buffer[reserved_size];
									::fast_io::details::decay::print_context_define_to_window<line>(
										outstm, st, t, buffer, buffer + producer_window_size);
								}
								else
								{
									::fast_io::details::local_operator_new_array_ptr<char_type> newptr(reserved_size);
									::fast_io::details::decay::print_context_define_to_window<line>(
										outstm, st, t, newptr.ptr, newptr.ptr + producer_window_size);
								}
								return;
							}
							else if (bcurr == nullptr || bed == nullptr ||
									 bed - bcurr < producer_window_difference)
#if __has_cpp_attribute(unlikely)
								[[unlikely]]
#endif
							{
								// Even after flushing, the put area is too small for one context producer window.
								if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<
												  reserved_size, char_type>)
								{
									// A too-small put area falls back to a stack context buffer when it fits the policy.
									char_type buffer[reserved_size];
									char_type *buffered{buffer + producer_window_size};
									::fast_io::details::decay::print_context_define_to_window<line>(
										outstm, st, t, buffer, buffered);
								}
								else
								{
									// A too-small put area falls back to heap context storage for large windows.
									::fast_io::details::local_operator_new_array_ptr<char_type> newptr(reserved_size);
									char_type *buffer{newptr.ptr};
									char_type *buffered{buffer + producer_window_size};
									::fast_io::details::decay::print_context_define_to_window<line>(
										outstm, st, t, buffer, buffered);
								}
								return;
							}
						}
					}
					else
					{
						// A non-empty put area is handed directly to the context producer.
						auto [resit, done] = st.print_context_define(t, bcurr, bed);
						::fast_io::details::decay::validate_context_print_result(bcurr, bed, resit, done);
						obuffer_set_curr(outstm, resit);
						if (done)
#if __has_cpp_attribute(likely)
							[[likely]]
#endif
						{
							// The context producer finished this argument, so only optional line termination remains.
							if constexpr (line)
							{
								// The line variant writes the trailing newline after the final context window.
								::fast_io::operations::decay::char_put_decay_dispatch(outstm, lfch);
							}
							return;
						}
					}
				}
			}
			else
			{
				// Unbuffered context output uses an explicit context window sized by the static context requirement.
				if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<reserved_size, char_type>)
				{
					// Unbuffered streams use a stack context buffer when the fixed window fits the policy.
					char_type buffer[reserved_size];
					char_type *buffered{buffer + producer_window_size};
					::fast_io::details::decay::print_context_define_to_window<line>(outstm, st, t, buffer, buffered);
				}
				else
				{
					// Unbuffered streams allocate context storage when the fixed window exceeds the stack policy.
					::fast_io::details::local_operator_new_array_ptr<char_type> newptr(reserved_size);
					char_type *buffer{newptr.ptr};
					char_type *buffered{buffer + producer_window_size};
					::fast_io::details::decay::print_context_define_to_window<line>(outstm, st, t, buffer, buffered);
				}
				return;
			}
		});
		return;
	}
	else if constexpr (
		::fast_io::single_pass_staging_printable<char_type, value_type> &&
		::fast_io::details::direct_printable_to<char_type, output, value_type> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> &&
		single_pass_staging_capacity != 0u &&
		::fast_io::details::decay::print_stack_buffer_size_within_limit<
			single_pass_staging_capacity, char_type>)
	{
		// The source guarantees one traversal and the destination independently authorizes this bounded stack budget.
		// Keeping the test at the final direct-print boundary preserves every scatter, reserve, and context strategy above.
		::fast_io::details::decay::print_single_pass_stage_and_write<
			line, char_type, single_pass_staging_capacity>(outstm, t);
	}
	else if constexpr (::fast_io::details::direct_printable_to<char_type, output, value_type>)
	{
		// The direct customization was proved against this exact post-unlock output type, not a synthetic dummy sink.
		print_define(::fast_io::io_reserve_type<char_type, value_type>, outstm, t);
		if constexpr (line)
		{
			// The line variant appends the newline after the customization has emitted its output.
			::fast_io::operations::decay::char_put_decay_dispatch(outstm, lfch);
		}
	}
	else if constexpr (::std::same_as<::std::remove_cvref_t<value_type>, ::fast_io::io_null_t>)
	{
		// Null output contributes no payload, but the line variant still emits its trailing newline.
		if constexpr (line)
		{
			::fast_io::operations::decay::char_put_decay_dispatch(outstm, lfch);
		}
	}
	else
	{
		// Unrecognized argument types fail with the same concrete-output predicate used by front-end admission.
		constexpr bool direct_to_output{
			::fast_io::details::direct_printable_to<char_type, output, value_type>};
		static_assert(direct_to_output, "type is not printable to this output stream");
	}
}

/// @brief Default policy for keeping one ordinary dynamic-reserve control behind a compiler boundary.
/// @details The primary template is deliberately false. A destination implementation may specialize this internal
///          trait only for an exact adapter/compiler combination whose inliner has been measured to duplicate the
///          complete cursor state machine. Source admission remains separate below, so an output cost workaround cannot
///          change formatter precedence merely because an argument also exposes a dynamic-reserve fallback.
template <typename output>
struct print_control_single_out_of_line_traits
{
	inline static constexpr bool value{};
};

/// @brief Admits only the plain dynamic-reserve representation to the destination's out-of-line cost policy.
/// @details Every stronger representation is rejected explicitly. The helper still calls `print_control_single` with
///          the original named decay object and exact output reference, preserving CPO lookup, ABI parameter passing,
///          cursor publication, exceptions, and the final-line rule; only the compiler's interprocedural placement may
///          differ.
template <typename output, typename T>
inline constexpr bool print_control_single_out_of_line_eligible = []() constexpr {
	using output_type = ::std::remove_cvref_t<output>;
	using char_type = typename output_type::output_char_type;
	using value_type = ::std::remove_cvref_t<T>;
	return ::fast_io::details::decay::print_control_single_out_of_line_traits<
			   output_type>::value &&
		   ::fast_io::dynamic_reserve_printable<char_type, value_type> &&
		   !::fast_io::reserve_printable<char_type, value_type> &&
		   !::fast_io::precise_reserve_printable<char_type, value_type> &&
		   !::fast_io::dynamic_reserve_with_possible_static_stack_size<
			   char_type, value_type> &&
		   !::fast_io::single_pass_bounded_materialization_source<
			   char_type, value_type> &&
		   !::fast_io::single_pass_staging_printable<char_type, value_type> &&
		   !::fast_io::scatter_printable_for<char_type, value_type &> &&
		   !::fast_io::reserve_scatters_printable<char_type, value_type> &&
		   !::fast_io::dynamic_reserve_scatters_printable<char_type, value_type> &&
		   !::fast_io::context_printable<char_type, value_type> &&
		   !::fast_io::details::direct_printable_to<char_type, output_type, value_type>;
}();

/// @brief Preserves ordinary inlining while containing GCC's audited std::string provenance diagnostic.
/// @details GCC 13--16 can retain the enclosing string object's 32-byte bound after inlining a valid dynamic writer
///          into its private put area. The destination/source predicate above is the only caller of this wrapper, so the
///          lexical suppression cannot affect other outputs or stronger formatter representations. Independent
///          `-Warray-bounds` diagnostics and sanitizer instrumentation remain active for a writer which exceeds its own
///          published bound. `always_inline` makes this a diagnostic boundary only, with no ABI or run-time call.
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wstringop-overflow"
#endif
template <bool line, typename output, typename T>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_control_single_diagnostic_boundary(output &outstm, T &t)
{
	::fast_io::details::decay::print_control_single<line>(outstm, t);
}
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
#pragma GCC diagnostic pop
#endif

/// @brief Emits four admitted controls behind one compiler boundary.
/// @details Four controls amortize the call/return cost while bounding duplicated cursor code. The exact source
///          references are retained, and only the last member receives `line`, so chunking is observationally identical
///          to the ordinary left-to-right recursion. `noinline` changes placement only: the mature put-area dispatcher
///          remains the body and no append materialization, extra size observation, ownership, or output CPO is added.
template <bool line, typename output, typename T0, typename T1, typename T2, typename T3>
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
[[gnu::noinline]]
#endif
inline constexpr void print_control_chunk_out_of_line(
	output &outstm, T0 &t0, T1 &t1, T2 &t2, T3 &t3)
{
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t0);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t1);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t2);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<line>(outstm, t3);
}

/// @brief Emits a final five-control chunk without returning the last control to the enclosing inliner.
/// @details A non-multiple large run terminates in one complete no-inline state machine. This preserves the cursor
///          provenance boundary through the final logical control instead of exposing a one-control tail to GCC.
template <bool line, typename output, typename T0, typename T1, typename T2, typename T3, typename T4>
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
[[gnu::noinline]]
#endif
inline constexpr void print_control_final_chunk_out_of_line(
	output &outstm, T0 &t0, T1 &t1, T2 &t2, T3 &t3, T4 &t4)
{
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t0);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t1);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t2);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t3);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<line>(outstm, t4);
}

/// @brief Emits a final six-control chunk under the same exact-output placement proof.
template <bool line, typename output, typename T0, typename T1, typename T2, typename T3, typename T4, typename T5>
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
[[gnu::noinline]]
#endif
inline constexpr void print_control_final_chunk_out_of_line(
	output &outstm, T0 &t0, T1 &t1, T2 &t2, T3 &t3, T4 &t4, T5 &t5)
{
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t0);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t1);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t2);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t3);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t4);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<line>(outstm, t5);
}

/// @brief Emits a final seven-control chunk under the same exact-output placement proof.
template <bool line, typename output, typename T0, typename T1, typename T2, typename T3, typename T4, typename T5,
		  typename T6>
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13
[[gnu::noinline]]
#endif
inline constexpr void print_control_final_chunk_out_of_line(
	output &outstm, T0 &t0, T1 &t1, T2 &t2, T3 &t3, T4 &t4, T5 &t5, T6 &t6)
{
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t0);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t1);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t2);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t3);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t4);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<false>(outstm, t5);
	::fast_io::details::decay::print_control_single_diagnostic_boundary<line>(outstm, t6);
}

/// @brief Partitions an admitted large run into shared four-control compiler chunks.
/// @details The caller proves at least 32 controls. Recursion emits complete chunks and selects one exact five-, six-,
///          or seven-control terminal helper when the cardinality is not divisible by four. Repeated type patterns
///          consequently reuse the same bounded helper instead of generating one full state machine per leaf; smaller
///          runs retain full inlining because their measured code budget is bounded.
template <bool line, typename output, typename T0, typename T1, typename T2,
		  typename T3, typename... Args>
inline constexpr void print_controls_out_of_line_chunks(
	output &outstm, T0 &t0, T1 &t1, T2 &t2, T3 &t3, Args &...args)
{
	if constexpr (sizeof...(Args) == 0u)
	{
		::fast_io::details::decay::print_control_chunk_out_of_line<line>(outstm, t0, t1, t2, t3);
	}
	else if constexpr (sizeof...(Args) <= 3u)
	{
		// Overload resolution selects the exact five-, six-, or seven-control terminal chunk.
		::fast_io::details::decay::print_control_final_chunk_out_of_line<line>(outstm, t0, t1, t2, t3, args...);
	}
	else
	{
		::fast_io::details::decay::print_control_chunk_out_of_line<false>(outstm, t0, t1, t2, t3);
		::fast_io::details::decay::print_controls_out_of_line_chunks<line>(
			outstm, args...);
	}
}

/// @brief    Describes a contiguous run that can share a context-capture buffer.
/// @details  The scan tracks context producers, dynamic reserve producers with bounded stack hints, and static
///           reserve bursts so the caller can size one reusable capture buffer for the run.
struct context_capture_run_result
{
	::std::size_t position{};
	::std::size_t context_buffer_size{};
	::std::size_t dynamic_buffer_size{};
	::std::size_t max_static_reserve_burst_size{};
	::std::size_t leading_static_reserve_burst_size{};
	bool has_context{};
	bool has_dynamic{};
};

#if (__cplusplus > 202302L && __cpp_pack_indexing >= 202311L) || FAST_IO_HAS_BUILTIN(__type_pack_element)

struct context_capture_scan_element
{
	bool stop{true};
	bool reserve{};
	bool dynamic_reserve{};
	bool context{};
	::std::size_t size{};
};

template <::std::integral char_type>
struct context_capture_scan_classifier
{
	using element_type = context_capture_scan_element;
	template <typename Arg>
	inline static consteval element_type classify() noexcept
	{
		using value_type = ::std::remove_cvref_t<Arg>;
		if constexpr (::fast_io::reserve_printable<char_type, value_type>)
		{
			constexpr ::std::size_t size{print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>)};
			static_assert(size != 0u);
			return {false, true, false, false, size};
		}
		else if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, value_type>)
		{
			constexpr ::std::size_t hint{print_reserve_static_stack_size(::fast_io::io_reserve_type<char_type, value_type>)};
			return {false, false, true, false,
					::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<hint, char_type>()};
		}
		else if constexpr (::fast_io::context_printable_with_static_buffer_size<char_type, value_type>)
		{
			return {false, false, false, true,
					::fast_io::details::decay::context_print_static_buffer_size_v<false, char_type, value_type>};
		}
		else if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
		{
			return {false, false, false, false, 0u};
		}
		else
		{
			return {};
		}
	}
};

#endif

/// @brief    Finds the leading run that benefits from context-capture buffering.
/// @details  Static reserve outputs are grouped into burst sizes, while context and bounded dynamic reserve outputs
///           contribute reusable window sizes. Null outputs keep position accounting without increasing storage.
/// @tparam   char_type the character type used by the print run
/// @tparam   Arg       the current argument type
/// @tparam   Args      the remaining argument types
/// @return   context_capture_run_result the capture-buffer requirements for the leading run
template <::std::integral char_type, typename Arg, typename... Args>
inline constexpr context_capture_run_result find_context_capture_run_n()
{
#if (__cplusplus > 202302L && __cpp_pack_indexing >= 202311L) || FAST_IO_HAS_BUILTIN(__type_pack_element)
	// Short runs share already-instantiated suffixes across related active records. Indexing only a large run
	// avoids replacing that useful cache with a separate descriptor table for every small condition selection.
	if constexpr (sizeof...(Args) >= 127u)
	{
		using classifier = ::fast_io::details::decay::context_capture_scan_classifier<char_type>;
		if constexpr (classifier::template classify<Arg>().stop)
		{
			// The historical scanner stops immediately here; do not build scalar metadata for an unreachable tail.
			return {};
		}
		else
		{
			using elements_type = ::fast_io::details::decay::print_leading_run_scan_elements<
				::fast_io::details::decay::context_capture_scan_classifier<char_type>, Arg, Args...>;
			constexpr auto &elements{elements_type::table.values};
			constexpr ::std::size_t count{sizeof...(Args) + 1u};
			::std::size_t accepted{};
			while (accepted != count && !elements[accepted].stop)
			{
				++accepted;
			}
			context_capture_run_result result{};
			while (accepted != 0u)
			{
				auto const current{elements[--accepted]};
				if (current.reserve)
				{
					::std::size_t const burst{::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
						result.leading_static_reserve_burst_size, current.size)};
					if (burst == SIZE_MAX)
					{
						result = {};
						continue;
					}
					result.leading_static_reserve_burst_size = burst;
					if (result.max_static_reserve_burst_size < burst)
					{
						result.max_static_reserve_burst_size = burst;
					}
				}
				else if (current.dynamic_reserve)
				{
					result.leading_static_reserve_burst_size = 0u;
					result.has_dynamic = true;
					if (result.dynamic_buffer_size < current.size)
					{
						result.dynamic_buffer_size = current.size;
					}
				}
				else if (current.context)
				{
					result.leading_static_reserve_burst_size = 0u;
					result.has_context = true;
					if (result.context_buffer_size < current.size)
					{
						result.context_buffer_size = current.size;
					}
				}
				++result.position;
			}
			return result;
		}
	}
	else
#endif
	{

		using nocvreft = ::std::remove_cvref_t<Arg>;
		if constexpr (::fast_io::reserve_printable<char_type, nocvreft>)
		{
			// Static reserve output can be accumulated into the leading contiguous reserve burst.
			context_capture_run_result ret{};
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first so this reserve output extends the front of the run.
				ret = ::fast_io::details::decay::find_context_capture_run_n<char_type, Args...>();
			}
			constexpr ::std::size_t sz{print_reserve_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
			static_assert(sz != 0);
			::std::size_t const leading_burst{
				::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					ret.leading_static_reserve_burst_size, sz)};
			if (leading_burst == SIZE_MAX)
			{
				// Context capture is an optional coalescing plan.  Declining this prefix preserves each producer's valid
				// individual protocol and prevents an impossible aggregate array from becoming an NTTP.
				return {};
			}
			++ret.position;
			ret.leading_static_reserve_burst_size = leading_burst;
			if (ret.max_static_reserve_burst_size < ret.leading_static_reserve_burst_size)
			{
				// The current static reserve burst is the largest contiguous reserve output seen so far.
				ret.max_static_reserve_burst_size = ret.leading_static_reserve_burst_size;
			}
			return ret;
		}
		else if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, nocvreft>)
		{
			// Bounded dynamic reserve output participates through its reusable maximum stack window.
			context_capture_run_result ret{};
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first before this dynamic producer resets the static burst.
				ret = ::fast_io::details::decay::find_context_capture_run_n<char_type, Args...>();
			}
			constexpr ::std::size_t static_stack_size{
				print_reserve_static_stack_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
			constexpr ::std::size_t dynamic_buffer_size{
				::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<static_stack_size, char_type>()};
			++ret.position;
			ret.leading_static_reserve_burst_size = 0;
			ret.has_dynamic = true;
			if (ret.dynamic_buffer_size < dynamic_buffer_size)
			{
				// The capture buffer must fit the largest bounded dynamic reserve window in the run.
				ret.dynamic_buffer_size = dynamic_buffer_size;
			}
			return ret;
		}
		else if constexpr (::fast_io::context_printable_with_static_buffer_size<char_type, nocvreft>)
		{
			// Context-printable output anchors the capture run and requires its declared context window.
			context_capture_run_result ret{};
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first before this context producer resets the static burst.
				ret = ::fast_io::details::decay::find_context_capture_run_n<char_type, Args...>();
			}
			constexpr ::std::size_t context_buffer_size{
				::fast_io::details::decay::context_print_static_buffer_size_v<false, char_type, nocvreft>};
			++ret.position;
			ret.leading_static_reserve_burst_size = 0;
			ret.has_context = true;
			if (ret.context_buffer_size < context_buffer_size)
			{
				// The capture buffer must fit the largest static context window in the run.
				ret.context_buffer_size = context_buffer_size;
			}
			return ret;
		}
		else if constexpr (::std::same_as<nocvreft, ::fast_io::io_null_t>)
		{
			// Null output keeps the argument position in the run while requiring no capture storage.
			context_capture_run_result ret{};
			if constexpr (sizeof...(Args) != 0)
			{
				// Remaining arguments are scanned first so the null can extend the positional prefix.
				ret = ::fast_io::details::decay::find_context_capture_run_n<char_type, Args...>();
			}
			++ret.position;
			return ret;
		}
		else
		{
			// Any other output protocol terminates the context-capture prefix.
			return {};
		}
	}
}

/// @brief    Writes pending context-capture buffer contents and resets the cursor.
/// @tparam   output    the decayed output stream reference type
/// @tparam   char_type the character type stored in the capture buffer
/// @param    outstm    the output stream reference
/// @param    buffer    the first character in the capture buffer
/// @param    curr      the current output cursor, reset to buffer after flushing
template <typename output, ::std::integral char_type>
inline constexpr void context_capture_flush(output &outstm, char_type *buffer, char_type *&curr)
{
	if (curr != buffer)
	{
		// Pending captured output is emitted as one contiguous range before the buffer is reused.
		::fast_io::operations::decay::write_all_decay_dispatch(outstm, buffer, curr);
		curr = buffer;
	}
}

/// @brief    Emits a captured context/static-reserve run through a reusable buffer.
/// @details  Static reserve and bounded dynamic reserve values are appended to the capture buffer when they fit;
///           larger values fall back to their ordinary single-control output path. Context producers stream windows
///           through the same buffer and the final recursive step appends the optional newline.
/// @tparam   needprintlf true when the final captured output should append a newline
/// @tparam   n           the number of arguments remaining in the captured run
/// @tparam   output      the decayed output stream reference type
/// @tparam   char_type   the character type stored in the capture buffer
/// @tparam   T           the current argument type
/// @tparam   Args        the remaining argument types
/// @param    outstm      the output stream reference
/// @param    buffer      the first character in the capture buffer
/// @param    curr        the current output cursor
/// @param    end         one past the capture buffer
/// @param    t           the current argument to emit
/// @param    args        the remaining arguments to emit
template <bool needprintlf, ::std::size_t n, typename output, ::std::integral char_type, typename T,
		  typename... Args>
inline constexpr void print_context_capture_run(output &outstm, char_type *buffer, char_type *curr, char_type *end,
												T &t, Args &...args)
{
	if constexpr (n != 0)
	{
		// A non-empty captured run consumes the current argument and then continues recursively.
		using value_type = ::std::remove_cvref_t<T>;
		if constexpr (::fast_io::reserve_printable<char_type, value_type>)
		{
			// Static reserve output is appended to the capture buffer when the fixed size fits.
			constexpr ::std::size_t sz{print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>)};
			static_assert(sz < PTRDIFF_MAX);
			if (static_cast<::std::size_t>(end - curr) < sz)
			{
				// The current buffer tail is too small, so existing captured output is flushed first.
				::fast_io::details::decay::context_capture_flush(outstm, buffer, curr);
			}
			if (sz <= static_cast<::std::size_t>(end - buffer))
			{
				// The static reserve output fits in the reusable capture buffer.
				curr = print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, curr, t);
			}
			else
			{
				// The static reserve output is larger than the capture buffer and must use its normal path.
				::fast_io::details::decay::print_control_single<false>(outstm, t);
			}
		}
		else if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, value_type>)
		{
			// Bounded dynamic reserve output is measured before deciding whether it fits the capture buffer.
			::std::size_t const sz{print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>, t)};
			if (static_cast<::std::size_t>(end - curr) < sz)
			{
				// The current buffer tail is too small, so existing captured output is flushed first.
				::fast_io::details::decay::context_capture_flush(outstm, buffer, curr);
			}
			if (sz <= static_cast<::std::size_t>(end - buffer))
			{
				// The measured dynamic reserve output fits in the reusable capture buffer.
				curr = print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, curr, t);
			}
			else
			{
				// The measured dynamic reserve output is too large and must use its normal path.
				::fast_io::details::decay::print_control_single<false>(outstm, t);
			}
		}
		else if constexpr (::fast_io::context_printable_with_static_buffer_size<char_type, value_type>)
		{
			// Context-printable output streams repeated producer windows through the capture buffer.
			using state_type = ::fast_io::details::print_context_state_t<char_type, value_type>;
			::fast_io::details::with_print_context_state<state_type>([&](state_type &st) constexpr {
				for (;;)
				{
					if (curr == end)
					{
						// A full capture buffer is emitted before asking the context producer for another window.
						::fast_io::details::decay::context_capture_flush(outstm, buffer, curr);
					}
					char_type *const producer_begin{curr};
					auto [resit, done] = st.print_context_define(t, producer_begin, end);
					::fast_io::details::decay::validate_context_print_result(producer_begin, end, resit, done);
					curr = resit;
					if (done)
					{
						// The context producer has completed the current argument.
						break;
					}
				}
			});
		}
		else if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
		{
			// Null output intentionally contributes no captured characters.
		}

		if constexpr (n == 1)
		{
			// The final captured argument is responsible for the optional newline and final flush.
			if constexpr (needprintlf)
			{
				// The line variant appends a trailing newline into the capture buffer before flushing.
				if (curr == end)
				{
					// A full buffer is flushed to make room for the newline character.
					::fast_io::details::decay::context_capture_flush(outstm, buffer, curr);
				}
				*curr = ::fast_io::char_literal_v<u8'\n', char_type>;
				++curr;
			}
			::fast_io::details::decay::context_capture_flush(outstm, buffer, curr);
		}
		else
		{
			// More captured arguments remain, so continue the run with the updated buffer cursor.
			::fast_io::details::decay::print_context_capture_run<needprintlf, n - 1>(outstm, buffer, curr, end,
																					 args...);
		}
	}
}

#if 0
template<bool ln,::std::integral char_type,::std::size_t n,typename Arg,typename ...Args>
inline constexpr char_type* printrsvcontiguousimpl(char_type* iter,Arg arg,Args... args)
{
	if constexpr(sizeof...(Args)!=0)
	{
		ret = find_continuous_scatters_n<char_type,Args...>();
	}
	if constexpr(::fast_io::scatter_printable_for<char_type, Arg &>)
	{
		return {ret.position+1,ret.neededspace,ret.hasdynamicreserve};
	}
	else if constexpr(::fast_io::reserve_printable<char_type,Arg>)
	{
		constexpr
			::std::size_t sz{print_reserve_size(::fast_io::io_reserve_type<char_type,Arg>)};
		return {ret.position+1,
			::fast_io::details::add_overflow(ret.neededspace,sz),
			ret.hasdynamicreserve};
	}
	else if constexpr(::fast_io::dynamic_reserve_printable<char_type,Arg>)
	{
		return {ret.position+1,ret.neededspace,true};
	}
}
#endif

/// @brief    Emits a control sequence while preserving the caller's final line policy.
/// @details  The final argument receives the line flag when requested; earlier arguments are emitted without a
///           newline so the whole sequence behaves like one logical print run.
/// @tparam   ln     true when the final argument should append a newline
/// @tparam   output the decayed output stream reference type
/// @tparam   T      the current argument type
/// @tparam   Args   the remaining argument types
/// @param    outstm the output stream reference
/// @param    t      the current argument to emit
/// @param    args   the remaining arguments to emit
template <bool ln, typename output, typename T, typename... Args>
// GCC 11--16 and Clang 17--23 inline this type-only recursion unaided in the
// control/scatter matrix.  Keeping ordinary placement avoids imposing the
// whole downstream graph on callers which stop after the first control.
inline constexpr void print_controls_line(output &outstm, T &t, Args &...args)
{
	if constexpr (sizeof...(Args) == 0)
	{
		// The final control argument receives the caller's line policy.
		print_control_single<ln>(outstm, t);
	}
	else
	{
#if (defined(__OPTIMIZE__) || defined(__OPTIMIZE_SIZE__)) && 0
		// Disabled experimental multi-control path would emit the run through a specialized recursion.
		print_controls_line_multi_impl<ln, 0>(outstm, t, args...);
#else
		if constexpr (ln)
		{
			// Line mode emits the head without a newline and recurses so only the tail receives the newline.
			print_control_single<false>(outstm, t);
			print_controls_line<ln>(outstm, args...);
		}
		else
		{
			// Non-line mode emits every control argument without adding line termination.
			print_control_single<false>(outstm, t);
			(print_control_single<false>(outstm, args), ...);
		}
#endif
	}
}

/// @brief    Materializes the requested reserve-printable prefix into an existing contiguous buffer.
/// @details  Prefixes through 96 positions retain the historical recursion; object-level A/B across the supported GNU
///           and Clang range proves that this keeps ordinary generated code unchanged. The first known frontend-depth
///           failure appears near 109 positions, so larger prefixes use a zipped index expansion which selects the
///           first n positions without recursively instantiating the remainder of the pack. Null arguments consume a
///           position without output and reserve-printable arguments advance the cursor through print_reserve_define.
///           Both paths preserve the original left-to-right producer order.
/// @tparam   n         the number of argument positions to consume
/// @tparam   char_type the buffer character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @param    ptr       the current output cursor
/// @param    t         the current argument
/// @param    args      the remaining arguments
/// @return   char_type* one past the materialized reserve output
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// Clang 17--22 needs this cursor recursion exposed: removing the attribute
// regresses the measured three-integer obuffer loop by 1.2--5.8%. Clang 23 is
// an explicit reversal (forcing is 5.6% slower), so the upper bound is closed;
// GCC 11--16 is neutral or improves without forcing. Later Clang releases
// inherit the Clang 23 ordinary-inlining policy until a new measurement says
// otherwise.
#if defined(__clang__) && 17 <= __clang_major__ && __clang_major__ < 23
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr char_type *print_n_reserve(char_type *ptr, T &t, Args &...args)
{
	static_assert(n <= sizeof...(Args) + 1u);
	if constexpr (n <= 96u)
	{
		if constexpr (n == 0u)
		{
			return ptr;
		}
		else if constexpr (::std::same_as<::std::remove_cvref_t<T>, ::fast_io::io_null_t>)
		{
			if constexpr (sizeof...(Args) == 0u || n < 2u)
			{
				return ptr;
			}
			else
			{
				return print_n_reserve<n - 1u>(ptr, args...);
			}
		}
		else
		{
			ptr = print_reserve_define(
				::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>, ptr, t);
			if constexpr (sizeof...(Args) == 0u || n < 2u)
			{
				return ptr;
			}
			else
			{
				return print_n_reserve<n - 1u>(ptr, args...);
			}
		}
	}
	else
	{
		return [&]<::std::size_t... index, typename... Values>(
				   ::std::index_sequence<index...>, Values &...values) constexpr {
			auto emit_one = [&]<::std::size_t position, typename Value>(Value &value) constexpr {
				if constexpr (position < n)
				{
					using value_type = ::std::remove_cvref_t<Value>;
					if constexpr (!::std::same_as<value_type, ::fast_io::io_null_t>)
					{
						ptr = print_reserve_define(
							::fast_io::io_reserve_type<char_type, value_type>, ptr, value);
					}
				}
			};
			(emit_one.template operator()<index>(values), ...);
			return ptr;
		}(::std::make_index_sequence<sizeof...(Args) + 1u>{}, t, args...);
	}
}

/// @brief Emits a fully static reserve run directly into a proven fixed put area.
/// @details The ordinary buffered dispatcher intentionally keeps reserve recursion outline-friendly on GCC because it
/// serves many owner/refill destinations. A fixed external view has a stronger destination proof: all extents are
/// compile-time, no flush or growth CPO can run, and one capacity check covers the complete ordered run. Keeping this
/// leaf separate prevents the inlining decision from leaking into buffered owners or dynamic reserve producers.
template <::std::integral char_type, typename value_type>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr char_type *
print_fixed_decimal_integer_emit(char_type *ptr, value_type value) noexcept
{
	using value_decay_type = ::std::remove_cvref_t<value_type>;
	using unsigned_type = ::std::make_unsigned_t<value_decay_type>;
	unsigned_type unsigned_value{static_cast<unsigned_type>(value)};
	if constexpr (::std::is_signed_v<value_decay_type>)
	{
		if (value < 0)
		{
			*ptr++ = ::fast_io::char_literal_v<u8'-', char_type>;
			unsigned_type zero{};
			unsigned_value = static_cast<unsigned_type>(zero - unsigned_value);
		}
	}
	return ::fast_io::details::print_fixed_decimal_integral_define<char_type>(ptr, unsigned_value);
}

template <typename T>
struct print_fixed_hot_scalar_traits
{
	inline static constexpr bool available{};
};

/// @brief Classifies the exact owned default-decimal scalar admitted by fresh-result concat.
/// @details The primary template is fail-closed because the older buffered hot-leaf classifier intentionally accepts
///          output-equivalent reference and policy records.  Fresh-result construction has a narrower measured cost
///          domain and therefore receives an independent specialization from the scalar implementation.
template <typename T>
struct print_concat_fresh_fixed_decimal_scalar_traits
{
	inline static constexpr bool available{};
};

/// @brief Propagates the fixed-external overstore audit through semantic transport nodes.
/// @details A semantic width can keep a reserve leaf hidden after condition selection, while a pack or condition can
///          introduce another forwarding boundary before the leaf is emitted.  Model those exact named-member
///          forwarding rules and stop only at the source-specific physical-store trait above.  This remains an internal
///          opt-in: arbitrary reserve writers are not classified from their reserve size alone.
template <::std::integral char_type, typename T>
struct print_fixed_external_semantic_overstore;

template <::std::integral char_type, typename node_expression, ::std::size_t... index>
inline consteval bool print_fixed_external_semantic_pack_overstore(
	::std::index_sequence<index...>) noexcept
{
	return (false || ... ||
			::fast_io::details::decay::print_fixed_external_semantic_overstore<
				char_type,
				::fast_io::details::decay::print_semantic_stable_forwarded_arg_t<
					char_type,
					decltype(::fast_io::containers::get<index>(
						::std::declval<node_expression>().storage))>>::value);
}

template <::std::integral char_type, typename T>
inline consteval bool print_fixed_external_semantic_node_overstore() noexcept
{
	using node_expression =
		::fast_io::details::decay::print_semantic_node_expression_t<T>;
	using node_type = ::std::remove_cvref_t<node_expression>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		return ::fast_io::details::decay::print_fixed_external_semantic_pack_overstore<
			char_type, node_expression>(::std::make_index_sequence<node_type::size>{});
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		using first_expression = decltype((::std::declval<node_expression>().t1));
		using second_expression = decltype((::std::declval<node_expression>().t2));
		return ::fast_io::details::decay::print_fixed_external_semantic_overstore<
				   char_type,
				   ::fast_io::details::decay::print_semantic_stable_input_forwarded_arg_t<
					   char_type, first_expression>>::value ||
			   ::fast_io::details::decay::print_fixed_external_semantic_overstore<
				   char_type,
				   ::fast_io::details::decay::print_semantic_stable_input_forwarded_arg_t<
					   char_type, second_expression>>::value;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		using child_expression =
			decltype((::std::declval<node_expression>().reference));
		return ::fast_io::details::decay::print_fixed_external_semantic_overstore<
			char_type,
			::fast_io::details::decay::print_semantic_stable_input_forwarded_arg_t<
				char_type, child_expression>>::value;
	}
	else
	{
		return false;
	}
}

template <::std::integral char_type, typename T>
struct print_fixed_external_semantic_overstore_dispatch
	: ::std::bool_constant<
		  ::fast_io::details::decay::print_fixed_external_overstore_traits<
			  char_type, ::std::remove_cvref_t<T>>::available>
{};

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::print_semantic_node<T>
struct print_fixed_external_semantic_overstore_dispatch<char_type, T>
	: ::std::bool_constant<
		  ::fast_io::details::decay::print_fixed_external_semantic_node_overstore<
			  char_type, T>()>
{};

template <::std::integral char_type, typename T>
struct print_fixed_external_semantic_overstore
	: ::fast_io::details::decay::print_fixed_external_semantic_overstore_dispatch<
		  char_type, T>
{};

template <::std::integral char_type, typename value_type>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr char_type *
print_fixed_static_reserve_emit_one(char_type *ptr, value_type &value)
{
	using decay_type = ::std::remove_cvref_t<value_type>;
	if constexpr (::std::same_as<decay_type, ::fast_io::io_null_t>)
	{
		return ptr;
	}
	else if constexpr (print_fixed_hot_scalar_traits<decay_type>::available)
	{
		return print_fixed_decimal_integer_emit<char_type>(ptr, value.reference);
	}
	else if constexpr (sizeof(char_type) == 1u && ::fast_io::details::is_ascii<char_type> &&
					   ::std::integral<decay_type> && !::std::same_as<decay_type, bool>)
	{
		// Raw fundamental values can occur in lower-level calls which bypass scalar normalization.
		return print_fixed_decimal_integer_emit<char_type>(ptr, value);
	}
	else
	{
		return print_reserve_define(::fast_io::io_reserve_type<char_type, decay_type>, ptr, value);
	}
}

template <::std::integral char_type, typename... Args>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr char_type *
print_fixed_static_reserve_emit(char_type *ptr, Args &...args)
{
	((ptr = print_fixed_static_reserve_emit_one<char_type>(ptr, args)), ...);
	return ptr;
}

/// @brief Selects the decimal scalar emitter for a proven reusable put area.
/// @details The ordinary reserve CPO deliberately keeps its compiler-controlled
/// inlining policy because it is also used by direct, decorated, and refillable
/// destinations.  A basic buffered reference has a separate proof that one
/// bounded non-throwing run may be formatted before publishing its cursor.  Use
/// that proof as the destination-side cost gate so the formatter is force-inlined
/// only for this measured hot shape (not for every reserve caller).
template <typename output, ::std::integral char_type, typename value_type>
inline consteval bool print_buffered_hot_decimal_scalar_available() noexcept
{
	return ::fast_io::operations::decay::defines::has_obuffer_basic_operations<output> &&
		   ::fast_io::single_pass_bounded_obuffer_materialization_safe<char_type, output> &&
		   (print_fixed_hot_scalar_traits<::std::remove_cvref_t<value_type>>::available ||
			(sizeof(char_type) == 1u && ::fast_io::details::is_ascii<char_type> &&
			 ::std::integral<::std::remove_cvref_t<value_type>> &&
			 !::std::same_as<::std::remove_cvref_t<value_type>, bool>));
}

template <typename output, ::std::integral char_type, typename value_type>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr char_type *
print_buffered_hot_decimal_scalar_emit(char_type *ptr, value_type &value) noexcept
{
	return print_fixed_static_reserve_emit_one<char_type>(ptr, value);
}

template <typename output, ::std::integral char_type, typename... value_types>
inline consteval bool print_buffered_hot_decimal_scalar_run_available() noexcept
{
	return sizeof...(value_types) == 1u &&
		   (print_buffered_hot_decimal_scalar_available<output, char_type, value_types>() && ...);
}

/// @brief Classifies a library-owned integral scalar manipulator at the public source boundary.
/// @details The primary template is deliberately fail-closed because reserve printability alone cannot prove that
///          alias/status normalization is library-owned or that reading the source has no external side effect.  The
///          scalar implementation specializes this trait only for an owned fundamental integral payload and a valid
///          integer flag record.  Admission still independently proves exact normalization closure, a non-throwing
///          reserve writer, and a fixed destination below.
template <typename source_type>
struct print_fixed_public_integral_manip_source_traits
{
	inline static constexpr bool available{};
};

/// @brief Emits one decimal scalar directly through a proven buffered put area.
/// @details This is a destination-specialized entry, intentionally separate from
/// `print_control_single`: forcing the generic dispatcher to inline the large
/// integer formatter makes that dispatcher itself miss the caller's inlining
/// budget.  The isolated entry keeps the ordinary CPO graph unchanged while a
/// basic buffered reference gets the old scalar loop's code shape.
template <bool line, typename output, typename T>
	requires(print_buffered_hot_decimal_scalar_available<
			 output, typename output::output_char_type, T>())
FAST_IO_GNU_ALWAYS_INLINE
#if defined(__GNUC__) && !defined(__clang__) && __has_cpp_attribute(__gnu__::__flatten__)
	[[gnu::flatten]]
#endif
	inline constexpr void print_control_single_buffered_hot(output &outstm, T &value) noexcept
{
	using char_type = typename output::output_char_type;
	using value_type = ::std::remove_cvref_t<T>;
	constexpr ::std::size_t payload_size{
		print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>)};
	constexpr ::std::size_t total_size{payload_size + static_cast<::std::size_t>(line)};
	static_assert(payload_size < PTRDIFF_MAX && total_size < PTRDIFF_MAX);
	constexpr auto lfch{::fast_io::char_literal_v<u8'\n', char_type>};

	char_type *curr{obuffer_curr(outstm)};
	char_type *const end{obuffer_end(outstm)};
	bool fits{total_size <= print_obuffer_remaining_size<char_type, output>(curr, end)};
	if constexpr (minimum_buffer_output_stream_require_size_impl<output, total_size>)
	{
		if (!fits) [[unlikely]]
		{
			obuffer_minimum_size_flush_prepare_define(outstm);
			curr = obuffer_curr(outstm);
		}
	}
	else if (!fits) [[unlikely]]
	{
		char_type buffer[total_size == 0u ? 1u : total_size];
		char_type *iter{print_buffered_hot_decimal_scalar_emit<output, char_type>(buffer, value)};
		if (iter < buffer || buffer + payload_size < iter) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		if constexpr (line)
		{
			*iter++ = lfch;
		}
		::fast_io::operations::decay::write_all_decay_dispatch(outstm, buffer, iter);
		return;
	}

	curr = print_buffered_hot_decimal_scalar_emit<output, char_type>(curr, value);
	if constexpr (line)
	{
		*curr++ = lfch;
	}
	obuffer_set_curr(outstm, curr);
}

template <::std::integral char_type, typename T>
inline consteval ::std::size_t print_fixed_static_reserve_extent() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
	{
		return 0u;
	}
	else
	{
		return print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>);
	}
}

template <typename output, ::std::integral char_type, typename... Args>
inline consteval bool print_fixed_static_reserve_run_available() noexcept
{
	return print_direct_obuffer_reserve_destination<output> &&
		   (true && ... &&
			(::std::same_as<::std::remove_cvref_t<Args>, ::fast_io::io_null_t> ||
			 ::fast_io::reserve_printable<char_type, ::std::remove_cvref_t<Args>>));
}

/// @brief Restricts the public fixed-view shortcut to source expressions whose normalization is completely local.
/// @details The ordinary dispatcher must inspect arbitrary alias/status/semantic CPOs.  This predicate is deliberately
/// narrower: it admits only built-in integral scalars (excluding character and boolean values) and const character
/// arrays (string literals).  Their alias/forward operations are the library-owned, non-observing normalization path;
/// no user customization can be skipped by the public shortcut.  The normalized result is still checked against the
/// reserve protocol so a future carrier change automatically falls back to the generic dispatcher.
template <::std::integral char_type, typename source_type>
inline consteval bool print_fixed_public_source_available() noexcept
{
	using source_reference = ::std::remove_reference_t<source_type>;
	using source_value = ::std::remove_cvref_t<source_type>;
	if constexpr (::std::is_volatile_v<source_reference>)
	{
		return false;
	}
	constexpr bool is_character =
		::std::same_as<source_value, char> || ::std::same_as<source_value, signed char> ||
		::std::same_as<source_value, unsigned char> || ::std::same_as<source_value, wchar_t> ||
		::std::same_as<source_value, char8_t> || ::std::same_as<source_value, char16_t> ||
		::std::same_as<source_value, char32_t>;
	if constexpr (::std::is_array_v<source_reference>)
	{
		using element_type = ::std::remove_extent_t<source_reference>;
		if constexpr (::std::is_const_v<element_type> &&
					  ::std::same_as<::std::remove_cv_t<element_type>, char_type> &&
					  (::std::extent_v<source_reference> != 0u))
		{
			using normalized_type = ::std::remove_cvref_t<decltype(::fast_io::io_print_forward<char_type>(
				::fast_io::io_print_alias(::std::declval<source_reference &>())))>;
			return ::fast_io::reserve_printable<char_type, normalized_type>;
		}
		else
		{
			return false;
		}
	}
	else if constexpr (::std::integral<source_value> && !::std::same_as<source_value, bool> && !is_character)
	{
		using normalized_type = ::std::remove_cvref_t<decltype(::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(::std::declval<source_reference &>())))>;
		return ::fast_io::reserve_printable<char_type, normalized_type>;
	}
	else
	{
		return false;
	}
}

template <typename output, ::std::integral char_type, typename... Args>
inline consteval bool print_fixed_public_run_available() noexcept
{
	if constexpr (!print_direct_obuffer_reserve_destination<output> || sizeof...(Args) == 0u)
	{
		return false;
	}
	else
	{
		return (print_fixed_public_source_available<char_type, Args>() && ...);
	}
}

template <bool line, typename output, ::std::integral char_type, typename... normalized_types>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_fixed_public_run_normalized(output &outstm, normalized_types &&...normalized_values)
{
	// Forwarding references preserve the temporary carriers through this synchronous call while their named
	// expressions are ordinary lvalues for the reserve emitter. Keeping this helper force-inlined avoids a per-record
	// lambda/function call on Clang's fixed-view path.
	::fast_io::details::decay::print_fixed_static_reserve_run<line, output, char_type>(
		outstm, normalized_values...);
}

/// @brief Emits a public fixed-view record after performing exactly the normal built-in source normalization.
/// @details This is called only after `print_fixed_public_run_available` has proved every source is a scalar/literal
/// shape.  It therefore bypasses the compiler-constant facade without bypassing alias semantics: each source is still
/// passed through `io_print_alias` and `io_print_forward` exactly once before the static reserve run is committed.
template <bool line, typename output, ::std::integral char_type, typename... Args>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_fixed_public_run(output &outstm, Args &...args)
{
	// The normalized scalar carrier is a prvalue (the default integer status-forward CPO returns a scalar manipulator),
	// while the static reserve emitter deliberately consumes named lvalues. The forwarding helper extends each
	// temporary through the complete synchronous emission without allocating a tuple or introducing another boundary.
	print_fixed_public_run_normalized<line, output, char_type>(
		outstm, ::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(args))...);
}

template <::std::integral char_type, typename... Args>
inline consteval ::std::size_t print_fixed_static_reserve_total() noexcept
{
	::std::size_t total{};
	((total = total > SIZE_MAX - print_fixed_static_reserve_extent<char_type, Args>()
				  ? SIZE_MAX
				  : total + print_fixed_static_reserve_extent<char_type, Args>()),
	 ...);
	return total;
}

template <bool line, typename output, ::std::integral char_type, typename... Args>
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_fixed_static_reserve_run(output &outstm, Args &...args)
{
	constexpr ::std::size_t reserve_total{print_fixed_static_reserve_total<char_type, Args...>()};
	constexpr ::std::size_t output_size{
		reserve_total == SIZE_MAX || (line && reserve_total == SIZE_MAX - 1u)
			? SIZE_MAX
			: reserve_total + static_cast<::std::size_t>(line)};
	static_assert(output_size != SIZE_MAX);
	auto curr{obuffer_curr(outstm)};
	auto const end{obuffer_end(outstm)};
	if ((curr == nullptr) != (end == nullptr) ||
		(curr != nullptr && curr > end)) [[unlikely]]
	{
		fast_terminate();
	}
	::std::size_t const available{
		curr == nullptr ? 0u : static_cast<::std::size_t>(end - curr)};
	constexpr bool requires_external_staging{
		(false || ... ||
		 print_fixed_external_overstore_traits<
			 char_type, ::std::remove_cvref_t<Args>>::available)};
	if constexpr (!requires_external_staging)
	{
		if (output_size <= available) [[likely]]
		{
			curr = print_fixed_static_reserve_emit<char_type>(curr, args...);
			if constexpr (line)
			{
				*curr++ = ::fast_io::char_literal_v<u8'\n', char_type>;
			}
			obuffer_set_curr(outstm, curr);
			return;
		}
	}

	// A reserve bound is a physical-store bound, not necessarily the logical result size. The historical fixed-view
	// path materialized on a bound miss and then wrote only [buffer,actual_end), which permits a true exact fit. Keep
	// that behavior here, and also use it for an audited writer whose fast implementation may initialize scratch after
	// its returned cursor. The destination cursor is published once, after the complete actual extent has been checked.
	if constexpr (output_size == 0u)
	{
		return;
	}
	else if constexpr (
		::fast_io::details::decay::print_stack_buffer_size_within_limit<
			output_size, char_type>)
	{
		char_type buffer[output_size];
		char_type *actual_end{
			print_fixed_static_reserve_emit<char_type>(buffer, args...)};
		if constexpr (line)
		{
			*actual_end++ = ::fast_io::char_literal_v<u8'\n', char_type>;
		}
		if (actual_end < buffer || buffer + output_size < actual_end) [[unlikely]]
		{
			fast_terminate();
		}
		::std::size_t const actual_size{
			static_cast<::std::size_t>(actual_end - buffer)};
		if (available < actual_size) [[unlikely]]
		{
			fast_terminate();
		}
		if (actual_size != 0u)
		{
			curr = ::fast_io::details::non_overlapped_copy_n(
				buffer, actual_size, curr);
		}
		obuffer_set_curr(outstm, curr);
	}
	else
	{
		::fast_io::details::local_operator_new_array_ptr<char_type> buffer{
			output_size};
		char_type *actual_end{
			print_fixed_static_reserve_emit<char_type>(buffer.ptr, args...)};
		if constexpr (line)
		{
			*actual_end++ = ::fast_io::char_literal_v<u8'\n', char_type>;
		}
		if (actual_end < buffer.ptr || buffer.ptr + output_size < actual_end) [[unlikely]]
		{
			fast_terminate();
		}
		::std::size_t const actual_size{
			static_cast<::std::size_t>(actual_end - buffer.ptr)};
		if (available < actual_size) [[unlikely]]
		{
			fast_terminate();
		}
		if (actual_size != 0u)
		{
			curr = ::fast_io::details::non_overlapped_copy_n(
				buffer.ptr, actual_size, curr);
		}
		obuffer_set_curr(outstm, curr);
	}
}

/// @brief Projects a retained scatter leaf into its native character descriptor.
/// @details Fixed scatters are core pointer/extent values, so a native-scatter endpoint can retain their original
///          storage without first copying through the reserve protocol. Every other leaf follows the established
///          scatter CPO unchanged. The helper is internal and is reached for fixed scatters only after the
///          destination-aware scanner has selected native scatter output.
template <::std::integral char_type, typename T>
// The complete GCC 11--16 / Clang 17--23 matrix emits the same descriptor
// projection without a forced attribute; ordinary inline avoids duplicating
// it when a retained scatter is rejected by a later destination policy.
inline constexpr ::fast_io::basic_io_scatter_t<char_type>
print_native_scatter_define(T &t)
{
	using value_type = ::std::remove_cvref_t<T>;
	using static_scatter_traits =
		::fast_io::details::decay::print_static_scatter_traits<char_type, value_type>;
	if constexpr (static_scatter_traits::available)
	{
		return static_scatter_traits::define(t);
	}
	else if constexpr (::std::same_as<value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		return t;
	}
	else
	{
		return print_scatter_define(::fast_io::io_reserve_type<char_type, value_type>, t);
	}
}

/// @brief    Builds N scatter descriptors from scatter-printable arguments.
/// @details  Null arguments are skipped, byte scatter descriptors convert character counts to byte counts, and typed
///           scatter descriptors preserve character counts for character-oriented output. The controller performs the
///           actual scatter write only after this recursive builder returns. Arguments are therefore references to the
///           controller-owned normalized objects: a borrowed descriptor may point into its producer, and copying that
///           producer into a recursive frame would destroy the referenced storage before the write.
/// @tparam   n           the number of argument positions to consume
/// @tparam   char_type   the print character type
/// @tparam   scattertype void for byte scatters, or char_type for character scatters
/// @tparam   T           the current argument type
/// @tparam   Args        the remaining argument types
/// @param    pscatters   the current scatter descriptor cursor
/// @param    t           the current argument
/// @param    args        the remaining arguments
template <::std::size_t n, ::std::integral char_type, typename scattertype, typename T, typename... Args>
// Every tested frontend naturally folds the descriptor count and null cases.
// Do not force the recursive builder into callers whose selected stream later
// coalesces or copies the same run.
inline constexpr void print_n_scatters(basic_io_scatter_t<scattertype> *pscatters,
#if __has_cpp_attribute(maybe_unused)
									   [[maybe_unused]]
#endif
									   T &t,
#if __has_cpp_attribute(maybe_unused)
									   [[maybe_unused]]
#endif
									   Args &...args)
{
	if constexpr (n != 0)
	{
		// A non-empty scatter run consumes the current argument position.
		if constexpr (::std::same_as<::std::remove_cvref_t<T>, ::fast_io::io_null_t>)
		{
			// Null output contributes no scatter descriptor but may be followed by more output positions.
			if constexpr (1 < n)
			{
				// Continue building descriptors for the remaining argument positions.
				return print_n_scatters<n - 1, char_type>(pscatters, args...);
			}
		}
		// Static scatter leaves already own stable descriptors and need no reserve-buffer materialization.
		else if constexpr (
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, ::std::remove_cvref_t<T>>::available)
		{
			// A fixed scatter already names stable storage; retain that pointer instead of copying its reserve spelling.
			auto const sct{
				::fast_io::details::decay::print_native_scatter_define<char_type>(t)};
			if constexpr (::std::same_as<scattertype, void>)
			{
				*pscatters = io_scatter_t{
					sct.base,
					::fast_io::details::intrinsics::mul_or_overflow_die(sct.len, sizeof(char_type))};
			}
			else
			{
				*pscatters = sct;
			}
		}
		else if constexpr (::std::same_as<::std::remove_cvref_t<T>, basic_io_scatter_t<scattertype>>)
		{
			// Existing scatter descriptors can be copied or byte-length-adjusted without calling customization.
			if constexpr (::std::same_as<scattertype, void>)
			{
				// Byte scatter output stores the payload extent in bytes.
				*pscatters = io_scatter_t{
					t.base,
					::fast_io::details::intrinsics::mul_or_overflow_die(t.len, sizeof(char_type))};
			}
			else
			{
				// Character scatter output preserves the existing typed descriptor.
				*pscatters = t;
			}
		}
		else
		{
			// Scatter-printable output is queried for its single contiguous output range.
			basic_io_scatter_t<char_type> sct{
				::fast_io::details::decay::print_native_scatter_define<char_type>(t)};
			if constexpr (::std::same_as<scattertype, void>)
			{
				// Byte scatter output stores the produced scatter extent in bytes.
				*pscatters = io_scatter_t{
					sct.base,
					::fast_io::details::intrinsics::mul_or_overflow_die(sct.len, sizeof(char_type))};
			}
			else
			{
				// Character scatter output stores the produced scatter extent in characters.
				*pscatters = sct;
			}
		}
		if constexpr (1 < n)
		{
			// More consumed positions remain, so advance the descriptor cursor and continue.
			++pscatters;
			return print_n_scatters<n - 1, char_type>(pscatters, args...);
		}
	}
}

/// @brief    Computes the total character count for N scatter-printable/null arguments.
/// @tparam   n         the number of argument positions to consume
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @param    t         the current argument
/// @param    args      the remaining arguments
/// @return   ::std::size_t the combined scatter character count, or SIZE_MAX when one contiguous extent is unavailable
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// Exact-size A/B binaries are identical without forcing on GCC 11--16 and
// Clang 17--23, including retained and null descriptors.
inline constexpr ::std::size_t print_n_scatter_total_size(
#if __has_cpp_attribute(maybe_unused)
	[[maybe_unused]]
#endif
	T &t,
#if __has_cpp_attribute(maybe_unused)
	[[maybe_unused]]
#endif
	Args &...args)
{
	if constexpr (n == 0)
	{
		// An empty scatter prefix contributes no characters.
		return 0;
	}
	else
	{
		// A non-empty scatter prefix accumulates the current descriptor length and the tail length.
		using nocvreft = ::std::remove_cvref_t<T>;
		::std::size_t current{};
		if constexpr (!::std::same_as<nocvreft, ::fast_io::io_null_t>)
		{
			// Non-null scatter-printable arguments expose their character length through print_scatter_define.
			current = ::fast_io::details::decay::print_native_scatter_define<char_type>(t).len;
		}
		if constexpr (n == 1)
		{
			// The current argument is the final consumed scatter position.
			return current;
		}
		else
		{
			// This total is used only to decide optional materialization. Individual retained scatters remain valid when
			// their aggregate cannot inhabit one character-pointer domain, so reject the optimization instead of dying.
			return ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				current, ::fast_io::details::decay::print_n_scatter_total_size<n - 1, char_type>(args...));
		}
	}
}

/// @brief Tests print's retained byte threshold without multiplying a character length by its width.
/// @details `ceil(minimum_bytes / sizeof(char_type))` is the first character count whose object representation spans
///          the required byte extent. Division and remainder preserve the proof when multiplying an adversarial
///          descriptor length by `sizeof(char_type)` would overflow size_t.
template <::std::integral char_type>
inline constexpr bool print_scatter_materialize_payload_meets_read_prfch_threshold(
	::std::size_t length) noexcept
{
	constexpr ::std::size_t character_width{sizeof(char_type)};
	constexpr ::std::size_t minimum_bytes{
		::fast_io::print_scatter_materialize_read_prfch_minimum_payload_bytes};
	constexpr ::std::size_t minimum_characters{
		minimum_bytes / character_width +
		static_cast<::std::size_t>(minimum_bytes % character_width != 0u)};
	return minimum_characters <= length;
}

/// @brief Carries the exact scatter-prefix size and the two bounded run-time prefetch statistics.
/// @details `nonempty_count` saturates at the policy threshold and therefore cannot overflow. The payload predicate is
///          meaningful only at run time; constant evaluation leaves the statistics at their neutral defaults and uses
///          the same historical materializer selected before prefetch support existed.
struct print_scatter_materialize_read_prfch_size_result
{
	::std::size_t total_size{};
	::std::size_t nonempty_count{};
	bool every_nonempty_payload_is_large{true};
};

/// @brief Measures a proved scatter prefix and classifies prefetch eligibility in that same required traversal.
/// @details This is instantiated only after the site concept proves the platform, static trip count, and every
///          normalized source's provenance. No payload base is inspected here. The complete prefix is rejected when
///          any nonempty range is below four KiB, retaining the all-large rule which excluded the measured 7--31
///          percent small/hot regressions. The exact-size addition has the historical optional-plan semantics: SIZE_MAX
///          returns control to the native scatter path rather than turning a valid independent descriptor chain into a
///          fatal allocation request.
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// The all-large prefetch probe remains byte-identical under ordinary inlining
// throughout the tested compiler matrix, so this measuring recursion does not
// own a global code-size policy.
inline constexpr ::fast_io::details::decay::print_scatter_materialize_read_prfch_size_result
print_n_scatter_total_size_with_read_prfch(T &t,
#if __has_cpp_attribute(maybe_unused)
										   [[maybe_unused]]
#endif
										   Args &...args)
{
	static_assert(n != 0u);
	using nocvreft = ::std::remove_cvref_t<T>;
	::std::size_t current{};
	if constexpr (!::std::same_as<nocvreft, ::fast_io::io_null_t>)
	{
		current = print_scatter_define(::fast_io::io_reserve_type<char_type, nocvreft>, t).len;
	}

	::fast_io::details::decay::print_scatter_materialize_read_prfch_size_result result{};
	if constexpr (1u < n)
	{
		result = ::fast_io::details::decay::print_n_scatter_total_size_with_read_prfch<n - 1u, char_type>(
			args...);
	}
	result.total_size =
		::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			current, result.total_size);
	// The payload-size heuristics are runtime-only metadata. Use the project two-way constant-evaluation gate so this
	// remains valid in C++20 while selecting native `if !consteval` on implementations that provide it.
	FAST_IO_IF_NOT_CONSTEVAL
	{
		if (current != 0u)
		{
			if (result.nonempty_count <
				::fast_io::print_scatter_materialize_read_prfch_minimum_descriptor_count)
			{
				++result.nonempty_count;
			}
			result.every_nonempty_payload_is_large =
				result.every_nonempty_payload_is_large &&
				::fast_io::details::decay::print_scatter_materialize_payload_meets_read_prfch_threshold<
					char_type>(current);
		}
	}
	return result;
}

/**
 * Copies one bounded C-string without erasing the source array's readable extent.
 *
 * The frontend has already established `count <= extent` while measuring the
 * bounded C-string. Keeping that invariant in this type-directed materializer
 * lets a small extent discard the generic large-copy arm at compile time. This
 * is both the exact source-safety proof and the cure for GCC 16's optimization-
 * budget-sensitive `-Warray-bounds` diagnostic; it adds no run-time check and
 * leaves the ordinary scatter ABI and generic copy algorithm unchanged.
 */
template <::std::size_t extent, ::std::integral char_type>
inline constexpr char_type *print_bounded_cstr_scatter_materialize(
	char_type const *first, ::std::size_t count, char_type *result) noexcept
{
	FAST_IO_ASSUME(count <= extent);
	if constexpr (extent <= 16u)
	{
		for (::std::size_t index{}; index != count; ++index)
		{
			result[index] = first[index];
		}
		return result + count;
	}
	else
	{
		return ::fast_io::details::decay::small_scatter_copy_n(
			first, count, result);
	}
}

/// @brief    Copies N scatter-printable/null arguments into a contiguous buffer.
/// @tparam   n         the number of argument positions to consume
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @param    ptr       the current output cursor
/// @param    t         the current argument
/// @param    args      the remaining arguments
/// @return   char_type* one past the copied scatter output
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// GCC 11--16 and Clang 17--23 produce the same large-scatter materializer
// without forcing; leave placement to the selected copy strategy.
inline constexpr char_type *print_n_scatter_materialize(char_type *ptr,
#if __has_cpp_attribute(maybe_unused)
														[[maybe_unused]]
#endif
														T &t,
#if __has_cpp_attribute(maybe_unused)
														[[maybe_unused]]
#endif
														Args &...args)
{
	if constexpr (n == 0)
	{
		// An empty scatter prefix leaves the materialization cursor unchanged.
		return ptr;
	}
	else
	{
		// A non-empty scatter prefix copies the current range when it is not null output.
		using nocvreft = ::std::remove_cvref_t<T>;
		if constexpr (!::std::same_as<nocvreft, ::fast_io::io_null_t>)
		{
			if constexpr (::fast_io::details::decay::
							  print_bounded_cstr_scatter_traits<nocvreft>::available)
			{
				// Do not project this producer to the generic scatter ABI before
				// its readable array extent has discharged the copy bound.
				ptr = ::fast_io::details::decay::
					print_bounded_cstr_scatter_materialize<
						::fast_io::details::decay::
							print_bounded_cstr_scatter_traits<
								nocvreft>::readable_extent>(t.base, t.len, ptr);
			}
			else
			{
				// Scatter-printable arguments expose a contiguous range that can
				// be copied into the output buffer.
				auto scatter{
					::fast_io::details::decay::print_native_scatter_define<
						char_type>(t)};
				ptr = ::fast_io::details::decay::small_scatter_copy_n(
					scatter.base, scatter.len, ptr);
			}
		}
		if constexpr (n == 1)
		{
			// The current argument is the final consumed scatter position.
			return ptr;
		}
		else
		{
			// More scatter positions remain, so continue from the advanced cursor.
			return ::fast_io::details::decay::print_n_scatter_materialize<n - 1, char_type>(ptr, args...);
		}
	}
}

/// @brief Identifies core reserve nodes whose emission reads no output state or user customization.
template <typename T>
struct print_buffered_passive_reserve_leaf : ::std::false_type
{};

template <::std::integral value_type>
struct print_buffered_passive_reserve_leaf<::fast_io::manipulators::chvw_t<value_type>> : ::std::true_type
{};

template <::std::integral char_type, ::std::size_t extent>
struct print_buffered_passive_reserve_leaf<
	::fast_io::manipulators::static_scatter_t<char_type, extent>> : ::std::true_type
{};

template <::fast_io::manipulators::static_provider_recipe provider_type,
		  ::std::size_t offset, ::std::size_t extent>
struct print_buffered_passive_reserve_leaf<
	::fast_io::manipulators::syntax_transport_details::static_provider_scatter_node<
		provider_type, offset, extent>> : ::std::true_type
{};

/// @brief Proves that one normalized leaf may join a preflighted mixed put-area run.
/// @details Static reserve leaves contribute a type-level capacity and must emit without throwing because the final
///          cursor is published only after the complete run. Dynamic text is admitted only after ordinary alias/status
///          forwarding has reduced it to `basic_io_scatter_t`; reading that value object adds no producer observation
///          and its three source-side semantic proofs are supplied directly by the core descriptor type. Null output
///          is the neutral member of the run.
template <::std::integral char_type, typename T>
inline consteval bool print_buffered_mixed_put_area_leaf() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t> ||
				  ::std::same_as<value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::details::decay::print_buffered_passive_reserve_leaf<value_type>::value &&
		::fast_io::reserve_printable<char_type, value_type>)
	{
#if defined(__HERBCEPTIONS__)
		return requires(char_type *iter, T &value) {
			// E is the named-lvalue reserve definition executed after the complete run passes preflight.
			// The delayed cursor commit is valid iff E has neither deterministic nor unwind failure.
			requires(!throws((print_reserve_define(
				::fast_io::io_reserve_type<char_type, value_type>, iter, value))));
			{
				print_reserve_define(
					::fast_io::io_reserve_type<char_type, value_type>, iter, value)
			} noexcept -> ::std::same_as<char_type *>;
		};
#else
		return noexcept(print_reserve_define(
			::fast_io::io_reserve_type<char_type, value_type>,
			::std::declval<char_type *>(), ::std::declval<T &>()));
#endif
	}
	else
	{
		return false;
	}
}

/// @brief Checks the leaf protocol for the first N positions of a normalized argument run.
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
inline consteval bool print_buffered_mixed_put_area_run_available() noexcept
{
	if constexpr (n == 0u)
	{
		return true;
	}
	else if constexpr (!::fast_io::details::decay::print_buffered_mixed_put_area_leaf<char_type, T>())
	{
		return false;
	}
	else if constexpr (n == 1u)
	{
		return true;
	}
	else
	{
		static_assert(sizeof...(Args) != 0u);
		return ::fast_io::details::decay::print_buffered_mixed_put_area_run_available<
			n - 1u, char_type, Args...>();
	}
}

/// @brief Tests whether N mixed reserve/scatter leaves fit in one already-measured put area.
/// @details Capacity is consumed by subtraction instead of summed, so an adversarial scatter length cannot overflow
///          an aggregate.  No output bytes or cursor publications occur on a miss.
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// GCC 11--16 and Clang 17--23 emit byte-identical buffered mixed-run code with
// ordinary inlining; forcing this recursive capacity check only broadens the
// caller's inlining obligation without changing the selected fast path.
inline constexpr bool print_buffered_mixed_put_area_fits(
	::std::size_t &remaining, T &t, Args &...args) noexcept
{
	static_assert(n != 0u);
	using value_type = ::std::remove_cvref_t<T>;
	::std::size_t current_size{};
	if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		current_size = print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>);
	}
	else if constexpr (::std::same_as<value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		current_size = t.len;
	}
	if (remaining < current_size)
	{
		return false;
	}
	remaining -= current_size;
	if constexpr (n == 1u)
	{
		return true;
	}
	else
	{
		return ::fast_io::details::decay::print_buffered_mixed_put_area_fits<
			n - 1u, char_type>(remaining, args...);
	}
}

/// @brief Copies one scatter in the compact complete-run path without hiding a propagated tiny extent.
/// @details Unknown ranges remain builtin-copy-shaped operations. An inlined constant extent instead reuses the shared
///          short-scatter policy so its typed stores and adjacent format literals remain eligible for backend merging.
template <::std::integral char_type>
// The complete tested GNU/Clang matrix folds constant tiny lengths without a
// mandatory-inline attribute and leaves unknown lengths in the same copy CPO.
inline constexpr char_type *print_buffered_mixed_put_area_compact_copy_n(
	char_type const *source, ::std::size_t count, char_type *destination)
{
#if FAST_IO_HAS_BUILTIN(__builtin_constant_p)
	if (__builtin_constant_p(count))
	{
		return ::fast_io::details::decay::small_scatter_copy_n(source, count, destination);
	}
#endif
	return ::fast_io::details::non_overlapped_copy_n(source, count, destination);
}

/**
 * @brief Accounts for one leaf of a large mixed put-area record.
 * @details Keeping the leaf operation separate from the pack fold avoids recursively
 *          instantiating every remaining argument suffix.  The ordinary inline is
 *          sufficient for GCC 11--16 and Clang 17--23; only the enclosing Clang 23
 *          record boundary needs forced inlining.
 */
template <::std::integral char_type, typename Value>
inline constexpr bool print_buffered_mixed_put_area_fit_one_large(
	::std::size_t &remaining, Value &value) noexcept
{
	using value_type = ::std::remove_cvref_t<Value>;
	::std::size_t current_size{};
	if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		current_size = print_reserve_size(
			::fast_io::io_reserve_type<char_type, value_type>);
	}
	else if constexpr (::std::same_as<
						   value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		current_size = value.len;
	}
	if (remaining < current_size)
	{
		return false;
	}
	remaining -= current_size;
	return true;
}

/**
 * @brief Emits one preflighted leaf of a large mixed put-area record.
 * @details This non-recursive leaf lets the enclosing fold expose every copy to the
 *          optimizer without creating one externally visible helper per pack suffix.
 */
template <::std::integral char_type, bool compact_copy, typename Value>
inline constexpr char_type *print_buffered_mixed_put_area_emit_one_large(
	char_type *cursor, Value &value)
{
	using value_type = ::std::remove_cvref_t<Value>;
	if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		cursor = print_reserve_define(
			::fast_io::io_reserve_type<char_type, value_type>, cursor, value);
	}
	else if constexpr (::std::same_as<
						   value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		if (value.len != 0u)
		{
			if constexpr (compact_copy)
			{
				cursor = ::fast_io::details::decay::
					print_buffered_mixed_put_area_compact_copy_n(
						value.base, value.len, cursor);
			}
			else
			{
				cursor = ::fast_io::details::decay::put_area_scatter_copy_n(
					value.base, value.len, cursor);
			}
		}
	}
	return cursor;
}

/// @brief Emits N preflighted mixed leaves into a put area whose cursor publication may be deferred.
/// @details Ordinary-sized packs retain this code-size-conscious recursive fallback. Forcing every
///          recursion level raised compile cost and duplicated small call sites; the direct-fold path
///          below is therefore confined to the measured large-record threshold.
template <::std::size_t n, ::std::integral char_type, bool compact_copy = false,
		  typename T, typename... Args>
inline constexpr char_type *print_buffered_mixed_put_area_emit(
	char_type *cursor, T &t, Args &...args)
{
	static_assert(n != 0u);
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		cursor = print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, cursor, t);
	}
	else if constexpr (::std::same_as<value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		if (t.len != 0u)
		{
			if constexpr (compact_copy)
			{
				cursor = ::fast_io::details::decay::print_buffered_mixed_put_area_compact_copy_n(
					t.base, t.len, cursor);
			}
			else
			{
				cursor = ::fast_io::details::decay::put_area_scatter_copy_n(
					t.base, t.len, cursor);
			}
		}
	}
	if constexpr (n == 1u)
	{
		return cursor;
	}
	else
	{
		return ::fast_io::details::decay::print_buffered_mixed_put_area_emit<
			n - 1u, char_type, compact_copy>(cursor, args...);
	}
}

/// @brief Proves the non-throwing cursor operations used by deferred put-area publication.
template <typename outputstmtype, typename char_type>
concept print_buffered_mixed_nothrow_put_area = ::std::integral<char_type> && requires(
																	  outputstmtype &optstm, char_type *cursor) {
#if defined(__HERBCEPTIONS__)
	// These are the exact named output expressions used by both mixed emitters. A deterministic error cannot be
	// represented after raw bytes have been written but before the final cursor is published.
	requires(!throws((obuffer_curr(optstm))));
	requires(!throws((obuffer_end(optstm))));
	requires(!throws((obuffer_set_curr(optstm, cursor))));
#endif
	{ obuffer_curr(optstm) } noexcept -> ::std::same_as<char_type *>;
	{ obuffer_end(optstm) } noexcept -> ::std::same_as<char_type *>;
	{ obuffer_set_curr(optstm, cursor) } noexcept -> ::std::same_as<void>;
};

/**
 * @brief Preflights and emits an entire large mixed record with two direct folds.
 * @details Clang's recursive inliner starts retaining one helper per argument suffix
 *          beyond 96 components.  Folding the complete pack here removes that
 *          quadratic call graph while leaving the normal small-record path unchanged.
 */
template <bool compact_copy, bool line, ::std::size_t n, ::std::integral char_type,
		  typename outputstmtype, typename... Args>
	requires(n > 96u && n == sizeof...(Args))
#if defined(__clang__) && !defined(_MSC_VER) && __clang_major__ >= 23
// Clang 23 at -O3 otherwise keeps two multi-kilobyte specializations out of line.
// Forcing this single record boundary removes both calls and lets the two folds share
// optimization context.  GCC 11--16 emits byte-identical code without the attribute;
// Clang 17--22 has no stable size/runtime win, and clang-cl/MSVC is intentionally
// excluded because no force-inline benefit has been demonstrated there.
FAST_IO_GNU_ALWAYS_INLINE inline constexpr bool print_buffered_mixed_put_area_try_large(
#else
inline constexpr bool print_buffered_mixed_put_area_try_large(
#endif
	outputstmtype &optstm, Args &...args)
{
	static_assert(::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				  outputstmtype, char_type>);
	char_type *const initial{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	if (initial == nullptr || end == nullptr)
	{
		return false;
	}
	::std::ptrdiff_t const difference{end - initial};
	if (difference < static_cast<::std::ptrdiff_t>(line))
	{
		return false;
	}
	::std::size_t remaining{
		static_cast<::std::size_t>(difference) - static_cast<::std::size_t>(line)};
	if (!(::fast_io::details::decay::print_buffered_mixed_put_area_fit_one_large<
			  char_type>(remaining, args) &&
		  ...))
	{
		return false;
	}
	char_type *cursor{initial};
	((cursor = ::fast_io::details::decay::print_buffered_mixed_put_area_emit_one_large<
		  char_type, compact_copy>(cursor, args)),
	 ...);
	if constexpr (line)
	{
		*cursor = ::fast_io::char_literal_v<u8'\n', char_type>;
		++cursor;
	}
	obuffer_set_curr(optstm, cursor);
	return true;
}

/// @brief Attempts one-capacity-check emission for a mixed static-reserve/retained-scatter prefix.
template <bool compact_copy, bool line, ::std::size_t n, ::std::integral char_type,
		  typename outputstmtype,
		  typename T, typename... Args>
// All supported test compilers already inline the successful put-area path and
// keep the miss path unchanged, so this policy helper remains ordinary inline.
inline constexpr bool print_buffered_mixed_put_area_try_impl(
	outputstmtype &optstm, T &t, Args &...args)
{
	static_assert(n != 0u);
	static_assert(::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				  outputstmtype, char_type>);
	char_type *const initial{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	// A zero-capacity adapter may use null sentinels; even null-minus-null is not a valid pointer-domain proof.
	if (initial == nullptr || end == nullptr)
	{
		return false;
	}
	::std::ptrdiff_t const difference{end - initial};
	if (difference < static_cast<::std::ptrdiff_t>(line))
	{
		return false;
	}
	::std::size_t remaining{
		static_cast<::std::size_t>(difference) - static_cast<::std::size_t>(line)};
	if (!::fast_io::details::decay::print_buffered_mixed_put_area_fits<n, char_type>(
			remaining, t, args...))
	{
		return false;
	}
	char_type *cursor{
		::fast_io::details::decay::print_buffered_mixed_put_area_emit<
			n, char_type, compact_copy>(initial, t, args...)};
	if constexpr (line)
	{
		*cursor = ::fast_io::char_literal_v<u8'\n', char_type>;
		++cursor;
	}
	obuffer_set_curr(optstm, cursor);
	return true;
}

/// @brief Attempts one-capacity-check emission for an output carrying the general deferred-commit proof.
template <bool line, ::std::size_t n, ::std::integral char_type, typename outputstmtype,
		  typename T, typename... Args>
inline constexpr bool print_buffered_mixed_put_area_try(
	outputstmtype &optstm, T &t, Args &...args)
{
	static_assert(::fast_io::deferred_obuffer_commit_safe<char_type, outputstmtype>);
	return ::fast_io::details::decay::print_buffered_mixed_put_area_try_impl<
		false, line, n, char_type>(optstm, t, args...);
}

/// @brief Preserves the established generic strategy when a fixed buffer view gains the deferred-commit CPO.
/// @details `basic_obuffer_view_ref` is admitted by the dedicated complete-run entry, whose compact copy shape is part
///          of its cost proof.  Its historical generic dispatcher instead selects the retained-scatter strategy.  This
///          exact private exception prevents the new semantic CPO from changing that cost choice without constraining
///          any external output type which independently provides deferred-commit and terminal-overflow customizations.
template <typename outputstmtype>
inline constexpr bool print_buffered_mixed_generic_endpoint = true;

/// @brief Copies one run-time-proved large scatter behind a deliberate code-generation boundary.
/// @details The hinted strategy admits only payloads of at least four KiB, so one ordinary call is negligible beside
///          the required memory transfer. Keeping the copy out of line is nevertheless material for large variadic
///          print sites: when GCC inlines a dynamic-size memcpy after each hint, its size/alias versions can clone the
///          following lookahead repeatedly. A 32-source assembly probe produced hundreds of duplicate prefetch sites.
///          This boundary leaves one hint and one call per source while the compile-time-disabled and run-time-false
///          paths continue to use the untouched tiny-scatter-aware materializer. It is non-constexpr by construction;
///          the selector rejects constant evaluation before this function can be called.
template <::std::integral char_type>
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#elif __has_cpp_attribute(msvc::noinline)
[[msvc::noinline]]
#endif
inline char_type *print_scatter_materialize_large_copy_n(
	char_type const *first, ::std::size_t count, char_type *result) noexcept
{
	return ::fast_io::details::non_overlapped_copy_n(first, count, result);
}

/// @brief Consumes a proved scatter prefix while retaining one nonempty source for one-step lookahead.
/// @details Each scatter CPO is invoked exactly once in this copy traversal. Null and empty sources are skipped before
///          their base can become a hint target. On observing the next nonempty range, the helper first hints that
///          range and then copies the retained current range, preserving physical copy order. The source-side borrowed
///          marker already proves that the sizing observation and this observation expose the same length and bytes;
///          the read-prefetch marker independently proves that every exposed live range is ordinary cacheable memory.
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// GCC 11--16 and Clang 17--23 generate the same hinted traversal under
// ordinary inlining, including the all-large run-time gate.
inline constexpr void print_n_scatter_materialize_with_read_prfch_step(
	char_type *&ptr, ::fast_io::basic_io_scatter_t<char_type> &current, bool &has_current, T &t,
#if __has_cpp_attribute(maybe_unused)
	[[maybe_unused]]
#endif
	Args &...args)
{
	static_assert(n != 0u);
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (!::std::same_as<nocvreft, ::fast_io::io_null_t>)
	{
		auto const next{
			print_scatter_define(::fast_io::io_reserve_type<char_type, nocvreft>, t)};
		if (next.len != 0u)
		{
			if (has_current)
			{
				FAST_IO_IF_NOT_CONSTEVAL
				{
					// `next.len != 0` proves that `next.base` names the live range promised by the source marker.
					::fast_io::prfch<
						::fast_io::prfch_mode::read,
						::fast_io::print_scatter_materialize_read_prfch_level,
						::fast_io::print_scatter_materialize_read_prfch_retention>(next.base);
				}
				// The run-time gate proved this repeatable nonempty descriptor is at least four KiB. Bypassing the shared
				// tiny-scatter switch is therefore equivalent and prevents its 0--16 dispatch from being cloned at every
				// source position in a large variadic print specialization.
				ptr = ::fast_io::details::decay::print_scatter_materialize_large_copy_n(
					current.base, current.len, ptr);
			}
			current = next;
			has_current = true;
		}
	}
	if constexpr (1u < n)
	{
		::fast_io::details::decay::print_n_scatter_materialize_with_read_prfch_step<n - 1u,
																					char_type>(ptr, current, has_current, args...);
	}
}

/// @brief Copies N proved scatter/null arguments with the measured next-nonempty-source read hint.
/// @details The one-descriptor delay is only metadata; output bytes remain in source order. The final retained range is
///          copied after the traversal because it has no next source to hint. Callers must not enter this helper until
///          the existing size traversal has proved the run-time count and all-large payload threshold.
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
inline constexpr char_type *print_n_scatter_materialize_with_read_prfch(
	char_type *ptr, T &t, Args &...args)
{
	static_assert(n != 0u);
	::fast_io::basic_io_scatter_t<char_type> current{};
	bool has_current{};
	::fast_io::details::decay::print_n_scatter_materialize_with_read_prfch_step<n, char_type>(
		ptr, current, has_current, t, args...);
	if (has_current)
	{
		// The final pending descriptor satisfies the same all-large gate as every earlier copied descriptor.
		ptr = ::fast_io::details::decay::print_scatter_materialize_large_copy_n(
			current.base, current.len, ptr);
	}
	return ptr;
}

/// @brief Selects the hinted or historical scatter materializer without adding work to a rejected run.
/// @details The compile-time-false instantiation contains neither eligibility branches nor lookahead code. For an
///          admitted strategy, a false run-time gate calls `print_n_scatter_materialize` directly; this is the original
///          materializer and consequently performs no next-source search. Constant evaluation also takes that path, so
///          compile-time print semantics never depend on a processor hint.
template <bool read_prfch, ::std::size_t n, ::std::integral char_type, typename T, typename... Args>
inline constexpr char_type *print_n_scatter_materialize_selected(
	char_type *ptr, bool runtime_read_prfch, T &t, Args &...args)
{
	if constexpr (read_prfch)
	{
		FAST_IO_IF_NOT_CONSTEVAL
		{
			if (runtime_read_prfch)
			{
				return ::fast_io::details::decay::print_n_scatter_materialize_with_read_prfch<n, char_type>(
					ptr, t, args...);
			}
		}
	}
	return ::fast_io::details::decay::print_n_scatter_materialize<n, char_type>(ptr, t, args...);
}

/**
 * Maximum descriptor count admitted to the recursive pre-descriptor
 * materializer.
 *
 * On a 96-field alternating text/literal record (192 leaves), Clang 23 spent
 * 63.8 s / 2.76 GiB and emitted a 96,481-byte helper. Delegating runs above 64
 * descriptors to the ordinary descriptor path reduced that probe to 29.2 s /
 * 1.37 GiB and 741 ns, while the existing iterative final compactor still
 * selected one write for its 1,633-byte payload. Large borrowed payloads remain
 * eligible for writev there. This is an IO-algorithm instantiation boundary,
 * not a destination CPO and not a reason to discard immutable pointers.
 */
inline constexpr ::std::size_t
	print_scatter_pre_descriptor_materialization_max_descriptor_count{64u};

/// @brief    Tries to emit a scatter-only prefix as one contiguous output range.
/// @tparam   needprintlf   true when the final emitted output should append a newline
/// @tparam   position      the number of argument positions included in the scatter prefix
/// @tparam   descriptor_capacity the number of source descriptors, excluding nulls and a synthetic newline
/// @tparam   char_type     the print character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
/// @return   bool true when the prefix was emitted without building scatter descriptors
template <bool needprintlf, ::std::size_t position, ::std::size_t descriptor_capacity,
		  ::std::integral char_type, bool preserve_static_fragments = false,
		  typename outputstmtype, typename T, typename... Args>
inline constexpr bool print_controls_scatters_try_materialize(outputstmtype &optstm, T &t, Args &...args)
{
	if constexpr (
		descriptor_capacity >
		::fast_io::details::decay::
			print_scatter_pre_descriptor_materialization_max_descriptor_count)
	{
		// Keep the recursive materializer in the discarded `else` arm. An early
		// return by itself still requires the compiler to semantically instantiate
		// later dependent statements. Clang 23 measurements at 224 fields showed
		// that discarding the body lowers peak compile RSS by seven percent while
		// producing a byte-identical executable.
		return false;
	}
	else
	{
		if constexpr (preserve_static_fragments)
		{
			// The payload pointer itself is the optimization target.  Measuring and
			// copying the run here would discard the proof before descriptors exist.
			return false;
		}
		constexpr bool read_prfch{
			::fast_io::print_scatter_materialize_read_prfch_strategy<
				::fast_io::details::native_prfch_platform, position, descriptor_capacity, T, Args...>};
		constexpr bool has_direct_scatter{[]() constexpr {
			if constexpr (::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>)
			{
				return ::fast_io::details::decay::print_has_direct_scatter_write_bytes_operations<outputstmtype>;
			}
			else
			{
				return ::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>;
			}
		}()};
		constexpr ::std::size_t threshold_chars{
			has_direct_scatter
				? ::fast_io::details::decay::print_scatter_direct_full_output_coalesce_threshold<char_type,
																								 outputstmtype>()
				: ::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>()};
		if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
					  (threshold_chars != 0 &&
					   ::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>))
		{
			// A coalescing-capable stream measures the complete scatter prefix before choosing storage. The newline is part
			// of the same proposed buffer; an unavailable total must return to the ordinary scatter write, not terminate.
			::std::size_t source_size{};
			bool runtime_read_prfch{};
			if constexpr (read_prfch)
			{
				auto const measurement{
					::fast_io::details::decay::print_n_scatter_total_size_with_read_prfch<position,
																						  char_type>(t, args...)};
				source_size = measurement.total_size;
				FAST_IO_IF_NOT_CONSTEVAL
				{
					runtime_read_prfch =
						measurement.every_nonempty_payload_is_large &&
						measurement.nonempty_count ==
							::fast_io::print_scatter_materialize_read_prfch_minimum_descriptor_count;
				}
			}
			else
			{
				// A rejected platform, capacity, or source proof retains the exact historical sizing instantiation.
				source_size = ::fast_io::details::decay::print_n_scatter_total_size<position, char_type>(
					t, args...);
			}
			::std::size_t const total_size{
				::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					source_size, static_cast<::std::size_t>(needprintlf))};
			if (total_size == SIZE_MAX)
			{
				// Retained scatter provenance explicitly permits this second observation by the normal descriptor path.
				return false;
			}
			if (total_size == 0)
			{
				// A zero-sized scatter prefix completes without touching the output stream.
				return true;
			}
			auto materialize = [&](char_type *destination) constexpr {
				return ::fast_io::details::decay::print_n_scatter_materialize_selected<read_prfch,
																					   position, char_type>(destination, runtime_read_prfch, t, args...);
			};
			if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
			{
				// Buffered streams prefer direct materialization into the current put area.
				char_type *const curr{obuffer_curr(optstm)};
				char_type *const end{obuffer_end(optstm)};
				if (curr != nullptr && end != nullptr &&
					static_cast<::std::size_t>(end - curr) >= total_size)
				{
					// The put area can hold the full scatter prefix, so no temporary stack buffer is needed.
					char_type *ptr{materialize(curr)};
					if constexpr (needprintlf)
					{
						// The line variant appends the trailing newline inside the same output-buffer commit.
						*ptr = ::fast_io::char_literal_v<u8'\n', char_type>;
						++ptr;
					}
					obuffer_set_curr(optstm, ptr);
					return true;
				}
			}
			if constexpr (threshold_chars != 0 &&
						  ::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>)
			{
				// Unbuffered streams can coalesce into stack or dynamically allocated contiguous storage.
				if (total_size <= threshold_chars)
				{
					// The full scatter prefix fits the stream's whole-output coalescing policy.
					if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<threshold_chars,
																								  char_type>)
					{
						// A policy threshold inside the stack budget uses one fixed stack buffer.
						char_type buffer[threshold_chars];
						char_type *ptr{materialize(buffer)};
						if constexpr (needprintlf)
						{
							// The line variant appends its newline inside the same stack allocation.
							*ptr = ::fast_io::char_literal_v<u8'\n', char_type>;
							++ptr;
						}
						::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptr);
					}
					else
					{
						// A policy threshold above the stack budget allocates only the measured output size.
						::fast_io::details::local_operator_new_array_ptr<char_type> buffer(total_size);
						char_type *ptr{materialize(buffer.ptr)};
						if constexpr (needprintlf)
						{
							// The line variant appends its newline inside the same dynamic allocation.
							*ptr = ::fast_io::char_literal_v<u8'\n', char_type>;
							++ptr;
						}
						::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer.ptr, ptr);
					}
					return true;
				}
			}
		}
		// The scatter prefix could not be coalesced by this helper; callers should use the normal scatter path.
		return false;
	}
}

/// @brief    Computes the combined run-time reserve size for dynamic reserve arguments in a prefix.
/// @tparam   n         the number of argument positions to inspect
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @param    t         the current argument
/// @param    args      the remaining arguments
/// @return   ::std::size_t the sum of dynamic reserve sizes, or SIZE_MAX when one contiguous extent is unavailable
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// GCC 11--16 and Clang 17--23 fold the non-dynamic positions and checked
// additions identically under ordinary inlining.
inline constexpr ::std::size_t ndynamic_print_reserve_size(T &t, Args &...args)
{
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (n == 0)
	{
		// An empty prefix has no dynamic reserve size contribution.
		return 0;
	}
	else if constexpr (n == 1)
	{
		// The final inspected position either contributes its measured dynamic size or zero.
		if constexpr (::fast_io::dynamic_reserve_printable<char_type, nocvreft>)
		{
			// Dynamic reserve output contributes its run-time measured size only while it remains a legal one-buffer extent.
			return ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				0u, print_reserve_size(::fast_io::io_reserve_type<char_type, nocvreft>, t));
		}
		else
		{
			// Non-dynamic output contributes no run-time reserve size.
			return 0;
		}
	}
	else
	{
		// Multiple positions are accumulated as an optional contiguous extent, never as fatal allocation arithmetic.
		if constexpr (::fast_io::dynamic_reserve_printable<char_type, nocvreft>)
		{
			// Evaluate in source order. The dynamic-reserve protocol explicitly requires a stable multi-pass source, which
			// proves that the later sequential fallback may observe the same object again without needing an ad-hoc cache.
			::std::size_t const current{print_reserve_size(::fast_io::io_reserve_type<char_type, nocvreft>, t)};
			::std::size_t const tail{
				::fast_io::details::decay::ndynamic_print_reserve_size<n - 1, char_type>(args...)};
			return ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(current, tail);
		}
		else
		{
			// Non-dynamic output is skipped while the remaining prefix is measured.
			return ::fast_io::details::decay::ndynamic_print_reserve_size<n - 1, char_type>(args...);
		}
	}
}

/// @brief    Computes the policy-capped static stack hints for dynamic reserve arguments in a prefix.
/// @tparam   n         the number of argument positions to inspect
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @return   ::std::size_t min(active stack capacity, sum of declared hints)
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
// The stack-budget fold is a constant expression and all thirteen tested
// frontends eliminate it without a mandatory-inline contract.
inline constexpr ::std::size_t ndynamic_print_reserve_static_stack_size()
{
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (n == 0)
	{
		// An empty prefix needs no dynamic reserve stack budget.
		return 0;
	}
	else if constexpr (n == 1)
	{
		// The final inspected position contributes at most the complete run's active stack budget.
		if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, nocvreft>)
		{
			constexpr ::std::size_t producer_hint{
				print_reserve_static_stack_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
			return ::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<producer_hint, char_type>();
		}
		else
		{
			// Producers without a static hint contribute no stack budget.
			return 0;
		}
	}
	else
	{
		// Multiple positions are accumulated directly in the bounded policy domain.
		if constexpr (::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, nocvreft>)
		{
			constexpr ::std::size_t run_cap{
				::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
			constexpr ::std::size_t producer_hint{
				print_reserve_static_stack_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
			constexpr ::std::size_t producer_budget{
				::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<producer_hint, char_type>()};
			constexpr ::std::size_t tail_budget{
				::fast_io::details::decay::ndynamic_print_reserve_static_stack_size<n - 1, char_type, Args...>()};
			// `min(cap, a + b)` is associative for nonnegative sizes.  Folding in this capped domain therefore gives
			// exactly the downstream admission value without ever evaluating the potentially overflowing raw sum.
			return ::fast_io::details::decay::print_strategy_add_capped(producer_budget, tail_budget, run_cap);
		}
		else
		{
			// Output without a static dynamic-reserve hint is skipped while the remaining prefix is inspected.
			return ::fast_io::details::decay::ndynamic_print_reserve_static_stack_size<n - 1, char_type, Args...>();
		}
	}
}

/// @brief    Tests whether every dynamic reserve argument in a prefix has a static stack hint.
/// @tparam   n         the number of argument positions to inspect
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @return   bool true when stack-buffer materialization can be bounded statically
template <::std::size_t n, ::std::integral char_type, typename T, typename... Args>
inline constexpr bool ndynamic_print_reserve_has_static_stack_size()
{
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (n == 0)
	{
		// An empty prefix trivially satisfies the static-stack-hint requirement.
		return true;
	}
	else if constexpr (::fast_io::dynamic_reserve_printable<char_type, nocvreft> &&
					   !::fast_io::dynamic_reserve_with_possible_static_stack_size<char_type, nocvreft>)
	{
		// A dynamic reserve producer without a static hint forces heap materialization.
		return false;
	}
	else if constexpr (n == 1)
	{
		// The final inspected position did not violate the static-stack-hint requirement.
		return true;
	}
	else
	{
		// Continue checking the remaining prefix positions.
		return ::fast_io::details::decay::ndynamic_print_reserve_has_static_stack_size<n - 1, char_type, Args...>();
	}
}

/// @brief    Builds mixed reserve/scatter descriptors for a prefix into caller-provided storage.
/// @tparam   needprintlf true when the final descriptor should include a trailing newline
/// @tparam   n           the number of argument positions to consume
/// @tparam   char_type   the print character type
/// @tparam   scattertype void for byte scatters, or char_type for character scatters
/// @tparam   T           the current argument type
/// @tparam   Args        the remaining argument types
/// @param    pscatters   the current scatter descriptor cursor
/// @param    ptr         the current reserve-buffer cursor
/// @param    t           the current argument
/// @param    args        the remaining arguments
/// @return   one past the final scatter descriptor written
template <bool needprintlf, ::std::size_t n, ::std::integral char_type, bool retain_static_scatter,
		  typename scattertype, typename T,
		  typename... Args>
inline constexpr auto print_n_scatters_reserve(basic_io_scatter_t<scattertype> *pscatters, char_type *ptr, T &t,
											   Args &...args);

/// @brief    Tests whether the next argument in a pack is reserve-like.
/// @tparam   char_type the print character type
/// @tparam   T         the next argument type
/// @tparam   Args      ignored remaining argument types
/// @return   bool true when the next argument can continue a reserve coalescing run
template <bool retain_static_scatter, ::std::integral char_type, typename T, typename... Args>
inline constexpr bool print_next_is_reserve() noexcept
{
	using nocvreft = ::std::remove_cvref_t<T>;
	if constexpr (
		(!retain_static_scatter ||
		 !::fast_io::details::decay::print_static_scatter_traits<char_type, nocvreft>::available) &&
		(::fast_io::reserve_printable<char_type, nocvreft> ||
		 ::fast_io::dynamic_reserve_printable<char_type, nocvreft>))
	{
		// Static and dynamic reserve-printable arguments can extend the current reserve scatter.
		return true;
	}
	else
	{
		// Non-reserve output terminates the current reserve scatter.
		return false;
	}
}

/// @brief    Empty-pack overload for reserve-lookahead checks.
/// @tparam   char_type the print character type
/// @return   bool false because no next argument exists
template <bool retain_static_scatter, ::std::integral char_type>
inline constexpr bool print_next_is_reserve() noexcept
{
	return false;
}

/// @brief    Continues coalescing adjacent reserve outputs into the current scatter descriptor.
/// @details  The continuation keeps one reserve-buffer base pointer while reserve-like arguments remain adjacent.
///           When the run ends, it commits the coalesced range as either a byte or character scatter descriptor. The
///           mutually recursive builder returns its descriptor cursor before the owning controller writes; reference
///           parameters keep every self-borrowed scatter attached to that controller's live normalized arguments.
/// @tparam   needprintlf true when the final consumed output should append a newline
/// @tparam   n           the number of argument positions remaining
/// @tparam   char_type   the print character type
/// @tparam   scattertype void for byte scatters, or char_type for character scatters
/// @tparam   T           the current argument type
/// @tparam   Args        the remaining argument types
/// @param    pscatters   the current scatter descriptor cursor
/// @param    base        the first character in the coalesced reserve range
/// @param    ptr         the current reserve-buffer cursor
/// @param    t           the current argument
/// @param    args        the remaining arguments
/// @return   one past the final scatter descriptor written
template <bool needprintlf, ::std::size_t n, ::std::integral char_type, bool retain_static_scatter = false,
		  typename scattertype, typename T,
		  typename... Args>
// The complete compiler matrix produces the same cursor pipeline without
// forcing, so recursive descriptor construction remains ordinary inline.
inline constexpr auto print_n_scatters_reserve_cont(basic_io_scatter_t<scattertype> *pscatters, char_type *base,
													char_type *ptr, T &t, Args &...args)
{
	if constexpr (n != 0)
	{
		// A non-empty continuation consumes the current position in the active reserve run.
		using nocvreft = ::std::remove_cvref_t<T>;
		if constexpr (
			(!retain_static_scatter ||
			 !::fast_io::details::decay::print_static_scatter_traits<char_type, nocvreft>::available) &&
			(reserve_printable<char_type, nocvreft> || dynamic_reserve_printable<char_type, nocvreft>))
		{
			// Reserve-like output extends the active contiguous reserve buffer range.
			ptr = print_reserve_define(::fast_io::io_reserve_type<char_type, nocvreft>, ptr, t);
			if constexpr (::fast_io::details::decay::print_next_is_reserve<
							  retain_static_scatter, char_type, Args...>())
			{
				// The next output is also reserve-like, so keep coalescing into the same scatter descriptor.
				return ::fast_io::details::decay::print_n_scatters_reserve_cont<
					needprintlf, n - 1, char_type, retain_static_scatter>(
					pscatters, base, ptr, args...);
			}
			else
			{
				// The reserve run ends here, so commit the buffered range as one scatter descriptor.
				if constexpr (n == 1 && needprintlf)
				{
					// The line variant appends its newline inside the final coalesced reserve range.
					*ptr = char_literal_v<u8'\n', char_type>;
					++ptr;
				}
				::std::size_t const sz{static_cast<::std::size_t>(ptr - base)};
				if constexpr (::std::same_as<scattertype, void>)
				{
					// Byte scatter output records the coalesced reserve range length in bytes.
					*pscatters = io_scatter_t{
						base,
						::fast_io::details::intrinsics::mul_or_overflow_die(sz, sizeof(char_type))};
				}
				else
				{
					// Character scatter output records the coalesced reserve range length in characters.
					*pscatters = basic_io_scatter_t<char_type>{base, sz};
				}
				++pscatters;
				if constexpr (1 < n)
				{
					// More output remains after the reserve run, so resume the mixed materializer.
					return ::fast_io::details::decay::print_n_scatters_reserve<
						needprintlf, n - 1, char_type, retain_static_scatter>(
						pscatters, ptr, args...);
				}
			}
		}
		else
		{
			// A non-reserve argument terminates the current reserve run before it is processed.
			::std::size_t const sz{static_cast<::std::size_t>(ptr - base)};
			if constexpr (::std::same_as<scattertype, void>)
			{
				// Byte scatter output commits the completed reserve run in bytes.
				*pscatters = io_scatter_t{
					base,
					::fast_io::details::intrinsics::mul_or_overflow_die(sz, sizeof(char_type))};
			}
			else
			{
				// Character scatter output commits the completed reserve run in characters.
				*pscatters = basic_io_scatter_t<char_type>{base, sz};
			}
			++pscatters;
			if constexpr (1 < n)
			{
				// The terminating argument still belongs to the overall run and is processed next.
				return ::fast_io::details::decay::print_n_scatters_reserve<
					needprintlf, n - 1, char_type, retain_static_scatter>(
					pscatters, ptr, t, args...);
			}
		}
	}
	// No more output positions remain, so return the current scatter cursor.
	return pscatters;
}

/// @brief    Materializes a mixed scatter/reserve prefix into scatter descriptors and reserve storage.
/// @details  Reserve-like outputs are coalesced into contiguous reserve scatters, existing scatter outputs are
///           forwarded as descriptors, reserve-scatters outputs append their own descriptors, and nulls are skipped.
///           This function and its continuation only build the caller's descriptor array; they do not consume it.
///           Binding the caller-owned normalized objects is consequently a lifetime requirement, not merely a copy
///           optimization, because a legal borrowed producer may return a descriptor into itself.
/// @tparam   needprintlf true when the final consumed output should append a newline
/// @tparam   n           the number of argument positions to consume
/// @tparam   char_type   the print character type
/// @tparam   scattertype void for byte scatters, or char_type for character scatters
/// @tparam   T           the current argument type
/// @tparam   Args        the remaining argument types
/// @param    pscatters   the current scatter descriptor cursor
/// @param    ptr         the current reserve-buffer cursor
/// @param    t           the current argument
/// @param    args        the remaining arguments
/// @return   one past the final scatter descriptor written
template <bool needprintlf, ::std::size_t n, ::std::integral char_type, bool retain_static_scatter = false,
		  typename scattertype, typename T,
		  typename... Args>
// GCC 11--16 and Clang 17--23 naturally inline the selected descriptor writes;
// forcing the complete recursion has no code-generation benefit.
inline constexpr auto print_n_scatters_reserve(basic_io_scatter_t<scattertype> *pscatters, char_type *ptr, T &t,
											   Args &...args)
{
	if constexpr (n != 0)
	{
		// A non-empty mixed run consumes the current argument position.
		using nocvreft = ::std::remove_cvref_t<T>;
		using static_scatter_traits =
			::fast_io::details::decay::print_static_scatter_traits<char_type, nocvreft>;
		if constexpr (retain_static_scatter && static_scatter_traits::available)
		{
			// Native-scatter endpoints retain a fixed scatter's original storage and exact type-level extent.
			auto const sct{
				::fast_io::details::decay::print_native_scatter_define<char_type>(t)};
			if constexpr (::std::same_as<scattertype, void>)
			{
				*pscatters = io_scatter_t{
					sct.base,
					::fast_io::details::intrinsics::mul_or_overflow_die(sct.len, sizeof(char_type))};
			}
			else
			{
				*pscatters = sct;
			}
			++pscatters;
			if constexpr (n == 1 && needprintlf)
			{
				*pscatters = ::fast_io::details::decay::line_scatter_common<char_type, scattertype>;
				++pscatters;
			}
			if constexpr (1 < n)
			{
				return ::fast_io::details::decay::print_n_scatters_reserve<
					needprintlf, n - 1, char_type, retain_static_scatter>(
					pscatters, ptr, args...);
			}
		}
		else if constexpr (::fast_io::reserve_printable<char_type, nocvreft> ||
						   ::fast_io::dynamic_reserve_printable<char_type, nocvreft>)
		{
			// Reserve-like output is materialized into reserve storage before becoming a scatter descriptor.
			auto ptred{print_reserve_define(::fast_io::io_reserve_type<char_type, nocvreft>, ptr, t)};
			if constexpr (sizeof...(Args) != 0 &&
						  ::fast_io::details::decay::print_next_is_reserve<
							  retain_static_scatter, char_type, Args...>())
			{
				// Adjacent reserve-like output is coalesced into the same scatter descriptor.
				return ::fast_io::details::decay::print_n_scatters_reserve_cont<
					needprintlf, n - 1, char_type, retain_static_scatter>(
					pscatters, ptr, ptred, args...);
			}
			else
			{
				// This reserve output stands alone and is committed as one scatter descriptor.
				if constexpr (n == 1 && needprintlf)
				{
					// The line variant appends its newline to the final reserve range before committing it.
					*ptred = char_literal_v<u8'\n', char_type>;
					++ptred;
				}
				::std::size_t const sz{static_cast<::std::size_t>(ptred - ptr)};
				if constexpr (::std::same_as<scattertype, void>)
				{
					// Byte scatter output records the reserve range length in bytes.
					*pscatters = io_scatter_t{
						ptr,
						::fast_io::details::intrinsics::mul_or_overflow_die(sz, sizeof(char_type))};
				}
				else
				{
					// Character scatter output records the reserve range length in characters.
					*pscatters = basic_io_scatter_t<char_type>{ptr, sz};
				}
				++pscatters;
				if constexpr (1 < n)
				{
					// Remaining positions continue after the committed reserve range.
					return ::fast_io::details::decay::print_n_scatters_reserve<
						needprintlf, n - 1, char_type, retain_static_scatter>(
						pscatters, ptred, args...);
				}
			}
		}
		else if constexpr (::fast_io::scatter_printable_for<char_type, decltype((t))>)
		{
			// Scatter-printable output contributes a descriptor without consuming reserve storage.
			if constexpr (::std::same_as<nocvreft, basic_io_scatter_t<scattertype>>)
			{
				// Existing scatter descriptor arguments are copied directly when their representation matches.
				if constexpr (::std::same_as<scattertype, void>)
				{
					// Byte scatter output converts an existing character length to bytes.
					*pscatters = io_scatter_t{
						t.base,
						::fast_io::details::intrinsics::mul_or_overflow_die(t.len, sizeof(char_type))};
				}
				else
				{
					// Character scatter output preserves the existing typed descriptor.
					*pscatters = t;
				}
			}
			else
			{
				// General scatter-printable output is queried for its exposed output range.
				basic_io_scatter_t<char_type> sct{print_scatter_define(
					::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>, t)};
				if constexpr (::std::same_as<scattertype, void>)
				{
					// Byte scatter output stores the exposed range length in bytes.
					*pscatters = io_scatter_t{
						sct.base,
						::fast_io::details::intrinsics::mul_or_overflow_die(sct.len, sizeof(char_type))};
				}
				else
				{
					// Character scatter output stores the exposed range length in characters.
					*pscatters = sct;
				}
			}
			++pscatters;
			if constexpr (n == 1 && needprintlf)
			{
				// The line variant appends a separate newline scatter after the final scatter output.
				*pscatters = ::fast_io::details::decay::line_scatter_common<char_type, scattertype>;
				++pscatters;
			}
			if constexpr (1 < n)
			{
				// Remaining positions continue with the same reserve-buffer cursor.
				return ::fast_io::details::decay::print_n_scatters_reserve<
					needprintlf, n - 1, char_type, retain_static_scatter>(pscatters, ptr, args...);
			}
		}
		else if constexpr (::fast_io::reserve_scatters_printable<char_type, nocvreft>)
		{
			// Reserve-scatters output supplies one or more descriptors and advances reserve storage itself.
			if constexpr (::std::same_as<scattertype, void>)
			{
				// Byte scatter output uses the byte-oriented reserve-scatters adapter.
				auto pit{::fast_io::details::decay::prrsvsct_byte_common_rsvsc_impl(pscatters, ptr, t)};
				if constexpr (1 < n)
				{
					// Remaining positions continue from the adapter's updated cursors.
					return ::fast_io::details::decay::print_n_scatters_reserve<
						needprintlf, n - 1, char_type, retain_static_scatter>(
						pit.scatters_pos_ptr, pit.reserve_pos_ptr, args...);
				}
				else if constexpr (n == 1 && needprintlf)
				{
					// The line variant appends a newline scatter after the final byte reserve-scatters output.
					*pit.scatters_pos_ptr = ::fast_io::details::decay::line_scatter_common<char_type, scattertype>;
					++pit.scatters_pos_ptr;
				}
				return pit.scatters_pos_ptr;
			}
			else
			{
				// Character scatter output uses the typed reserve-scatters customization directly.
				constexpr auto component_capacity{
					print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, nocvreft>)};
				auto pit{
					print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, nocvreft>, pscatters, ptr, t)};
				if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
						pscatters, pscatters + component_capacity.scatters_size, pit.scatters_pos_ptr) ||
					!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
						ptr, ptr + component_capacity.reserve_size, pit.reserve_pos_ptr)) [[unlikely]]
				{
					/*
					A typed descriptor plan shares the caller's aggregate arrays with every later producer. Checking only
					the aggregate end after recursion is too late: an escaped cursor would already have become the next
					producer's base, and subtracting that unrelated pointer would itself be undefined. The component-local
					closed ranges are the exact proof supplied by this CPO's constexpr capacity declaration.
					*/
					::fast_io::fast_terminate();
				}
				if constexpr (1 < n)
				{
					// Remaining positions continue from the customization's updated cursors.
					return ::fast_io::details::decay::print_n_scatters_reserve<
						needprintlf, n - 1, char_type, retain_static_scatter>(
						pit.scatters_pos_ptr, pit.reserve_pos_ptr, args...);
				}
				else if constexpr (n == 1 && needprintlf)
				{
					// The line variant appends a newline scatter after the final typed reserve-scatters output. The
					// returned cursor is intentionally mutable because this descriptor belongs to the caller's array,
					// not to the producer's capacity result.
					*pit.scatters_pos_ptr = ::fast_io::details::decay::line_scatter_common<char_type, scattertype>;
					++pit.scatters_pos_ptr;
				}
				return pit.scatters_pos_ptr;
			}
		}
		else if constexpr (::std::same_as<nocvreft, ::fast_io::io_null_t>)
		{
			// Null output consumes a position while emitting no scatter descriptor.
			if constexpr (n == 1 && needprintlf)
			{
				// A final null still carries the caller's requested trailing newline.
				*pscatters = ::fast_io::details::decay::line_scatter_common<char_type, scattertype>;
				++pscatters;
			}
			if constexpr (1 < n)
			{
				// Continue with the next argument while preserving the current cursors.
				return ::fast_io::details::decay::print_n_scatters_reserve<
					needprintlf, n - 1, char_type, retain_static_scatter>(pscatters, ptr, args...);
			}
		}
	}
	// No more positions remain, so return the current scatter cursor.
	return pscatters;
}

/// @brief Reports whether a measured compiler/target pair benefits from the put-area vector copy leaf.
/// @details This is solely a code-generation gate; semantic eligibility is proved at the call site.
/// @tparam descriptor_count the compile-time number of descriptors in the retained scatter run
template <::std::size_t descriptor_count>
inline consteval bool
print_direct_obuffer_scatter_vector_copy_codegen_supported() noexcept
{
	/*
	Semantic proof.  Let C_i be the cursor before descriptor i.  The caller
	establishes stable source storage and [C_i, C_i + len_i) as a valid,
	non-overlapping destination interval.  put_area_scatter_copy_n copies exactly
	len_i elements and returns C_i + len_i, so induction preserves both the byte
	sequence and the cursor of non_overlapped_copy_n for every descriptor.  The
	stream cursor is published only after the complete ordered run.  This
	predicate therefore selects between equivalent leaves and cannot admit a new
	protocol.

	Cost proof.  Eight descriptors are the first measured point at which the
	target-aware head/tail vector leaf amortizes its size ladder on x86-64 GCC 16,
	x86-64 Clang 22, and Apple AArch64 Clang 23.  The two-descriptor Clang case was
	neutral while instantiating additional text.  Positive compiler intervals are
	future-open; unmeasured targets and earlier frontends preserve the existing
	memcpy-shaped leaf.
	*/
#if defined(__x86_64__) &&                                           \
	((defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 16) || \
	 (defined(__clang__) && __clang_major__ >= 22))
	return descriptor_count >= 8u;
#elif defined(__APPLE__) && (defined(__aarch64__) || defined(__arm64__)) && \
	defined(__clang__) && __clang_major__ >= 23
	return descriptor_count >= 8u;
#else
	return false;
#endif
}

/// @brief    Writes a scatter-only prefix using stack or heap descriptor storage.
/// @tparam   needprintlf   true when the final descriptor should be a trailing newline
/// @tparam   position      the number of argument positions included in the scatter prefix
/// @tparam   scatterscount the number of scatter descriptors to write
/// @tparam   char_type     the print character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool needprintlf, ::std::size_t position, ::std::size_t scatterscount, ::std::integral char_type,
		  bool preserve_static_fragments = false, typename outputstmtype, typename T, typename... Args>
inline constexpr void print_controls_scatters(outputstmtype &optstm, T &t, Args &...args)
{
	using scatter_type = ::std::conditional_t<
		::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>,
		::fast_io::io_scatter_t, ::fast_io::basic_io_scatter_t<char_type>>;
	static_assert(static_cast<::std::size_t>(needprintlf) <= scatterscount);
	constexpr ::std::size_t source_scatter_capacity{
		scatterscount - static_cast<::std::size_t>(needprintlf)};
	// A fixed-capacity one-byte memory view has no meaningful write CPO work to perform. Build the small descriptor
	// array once and copy its ranges directly into the put area; this is the same sequence-preserving operation as the
	// historical buffered path, without a size-measurement/materialization probe on every print call.
	if constexpr (print_direct_obuffer_reserve_destination<outputstmtype> && sizeof(char_type) == 1u &&
				  !preserve_static_fragments)
	{
		scatter_type scatters[scatterscount];
		::fast_io::details::decay::print_n_scatters<position, char_type>(scatters, t, args...);
		if constexpr (needprintlf)
		{
			scatters[scatterscount - 1u] = ::fast_io::details::decay::line_scatter_common<char_type>;
		}
		auto curr{obuffer_curr(optstm)};
		auto const end{obuffer_end(optstm)};
		for (::std::size_t i{}; i != scatterscount; ++i)
		{
			::std::size_t const len{scatters[i].len};
			if (len == 0u)
			{
				continue;
			}
			if (curr == nullptr || end == nullptr || curr > end ||
				static_cast<::std::size_t>(end - curr) < len) [[unlikely]]
			{
				fast_terminate();
			}
			if constexpr (::fast_io::details::decay::
							  print_direct_obuffer_scatter_vector_copy_codegen_supported<
								  scatterscount>())
			{
				// The descriptor contract proves the source extent; the immediately preceding check proves the destination extent.
				curr = ::fast_io::details::decay::put_area_scatter_copy_n(
					scatters[i].base, len, curr);
			}
			else
			{
				curr = ::fast_io::details::non_overlapped_copy_n(
					scatters[i].base, len, curr);
			}
		}
		obuffer_set_curr(optstm, curr);
		return;
	}
	if (::fast_io::details::decay::print_controls_scatters_try_materialize<
			needprintlf, position, source_scatter_capacity, char_type,
			preserve_static_fragments>(optstm, t, args...))
	{
		return;
	}
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scatterscount, scatter_type>)
	{
		// Small scatter descriptor runs are built on the stack and written once.
		scatter_type scatters[scatterscount];
		::fast_io::details::decay::print_n_scatters<position, char_type>(scatters, t, args...);
		if constexpr (needprintlf)
		{
			if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
			{
				scatters[scatterscount - 1u] =
					::fast_io::details::decay::line_scatter_common<char_type, void>;
			}
			else
			{
				scatters[scatterscount - 1u] =
					::fast_io::details::decay::line_scatter_common<char_type>;
			}
		}
		if constexpr (preserve_static_fragments)
		{
			::fast_io::details::decay::
				print_scatter_write_all_preserving_static_fragments(
					optstm, scatters, scatterscount);
		}
		else
		{
			::fast_io::details::decay::print_scatter_write_all_dispatch(
				optstm, scatters, scatterscount);
		}
	}
	else
	{
		// Large scatter descriptor runs allocate descriptor storage before one scatter write.
		::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(scatterscount);
		::fast_io::details::decay::print_n_scatters<position, char_type>(scatters.ptr, t, args...);
		if constexpr (needprintlf)
		{
			if constexpr (::std::same_as<scatter_type, ::fast_io::io_scatter_t>)
			{
				scatters.ptr[scatterscount - 1u] =
					::fast_io::details::decay::line_scatter_common<char_type, void>;
			}
			else
			{
				scatters.ptr[scatterscount - 1u] =
					::fast_io::details::decay::line_scatter_common<char_type>;
			}
		}
		if constexpr (preserve_static_fragments)
		{
			::fast_io::details::decay::
				print_scatter_write_all_preserving_static_fragments(
					optstm, scatters.ptr, scatterscount);
		}
		else
		{
			::fast_io::details::decay::print_scatter_write_all_dispatch(
				optstm, scatters.ptr, scatterscount);
		}
	}
}

/// @brief    Materializes a mixed reserve/scatter prefix with caller-provided scatter descriptor storage.
/// @tparam   needprintlf   true when the final consumed output should append a newline
/// @tparam   position      the number of argument positions included in the prefix
/// @tparam   mxsize        the static reserve-buffer character requirement
/// @tparam   char_type     the print character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   scatter_type  the scatter descriptor representation
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    scatters      the caller-provided scatter descriptor storage
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool needprintlf, ::std::size_t position, ::std::size_t mxsize, ::std::integral char_type,
		  bool retain_static_scatter = false, bool preserve_static_fragments = false,
		  typename outputstmtype, typename scatter_type, typename T,
		  typename... Args>
inline constexpr void print_controls_scatters_reserve_with_scatter(
	outputstmtype &optstm, scatter_type *scatters, T &t, Args &...args)
{
	constexpr ::std::size_t buffer_size{mxsize == 0 ? 1u : mxsize};
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<buffer_size, char_type>)
	{
		// Small reserve storage is materialized on the stack before scatter output is written.
		char_type buffer[buffer_size];
		auto ptr{::fast_io::details::decay::print_n_scatters_reserve<
			needprintlf, position, char_type, retain_static_scatter>(
			scatters, buffer, t, args...)};
		if constexpr (preserve_static_fragments)
		{
			::fast_io::details::decay::
				print_scatter_write_all_preserving_static_fragments(
					optstm, scatters,
					static_cast<::std::size_t>(ptr - scatters));
		}
		else
		{
			::fast_io::details::decay::print_scatter_write_all_dispatch(
				optstm, scatters,
				static_cast<::std::size_t>(ptr - scatters));
		}
	}
	else
	{
		// Large reserve storage is allocated before scatter descriptors are materialized and written.
		::fast_io::details::local_operator_new_array_ptr<char_type> buffer(buffer_size);
		auto ptr{::fast_io::details::decay::print_n_scatters_reserve<
			needprintlf, position, char_type, retain_static_scatter>(
			scatters, buffer.ptr, t, args...)};
		if constexpr (preserve_static_fragments)
		{
			::fast_io::details::decay::
				print_scatter_write_all_preserving_static_fragments(
					optstm, scatters,
					static_cast<::std::size_t>(ptr - scatters));
		}
		else
		{
			::fast_io::details::decay::print_scatter_write_all_dispatch(
				optstm, scatters,
				static_cast<::std::size_t>(ptr - scatters));
		}
	}
}

/// @brief    Writes a mixed static reserve/scatter prefix using stack or heap descriptor storage.
/// @tparam   needprintlf   true when the final consumed output should append a newline
/// @tparam   position      the number of argument positions included in the prefix
/// @tparam   scatterscount the number of scatter descriptors available
/// @tparam   mxsize        the static reserve-buffer character requirement
/// @tparam   char_type     the print character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool needprintlf, ::std::size_t position, ::std::size_t scatterscount, ::std::size_t mxsize,
		  ::std::integral char_type, bool retain_static_scatter = false,
		  bool preserve_static_fragments = false, typename outputstmtype, typename T,
		  typename... Args>
inline constexpr void print_controls_scatters_reserve(outputstmtype &optstm, T &t, Args &...args)
{
	using scatter_type = ::std::conditional_t<
		::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>,
		::fast_io::io_scatter_t, ::fast_io::basic_io_scatter_t<char_type>>;
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scatterscount, scatter_type>)
	{
		// Small scatter descriptor storage is kept on the stack for the mixed reserve/scatter run.
		scatter_type scatters[scatterscount];
		::fast_io::details::decay::print_controls_scatters_reserve_with_scatter<
			needprintlf, position, mxsize, char_type, retain_static_scatter,
			preserve_static_fragments>(optstm, scatters, t, args...);
	}
	else
	{
		// Large scatter descriptor storage is allocated before the mixed run is materialized.
		::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(scatterscount);
		::fast_io::details::decay::print_controls_scatters_reserve_with_scatter<
			needprintlf, position, mxsize, char_type, retain_static_scatter,
			preserve_static_fragments>(
			optstm, scatters.ptr, t, args...);
	}
}

/// @brief    Materializes a mixed dynamic reserve/scatter prefix with caller-provided descriptor storage.
/// @details  When every dynamic producer has a static stack hint and the measured total fits the bounded stack buffer,
///           reserve storage stays on the stack; otherwise the measured total drives one heap allocation.
/// @tparam   needprintlf           true when the final consumed output should append a newline
/// @tparam   position              the number of argument positions included in the prefix
/// @tparam   stack_buffer_size     the bounded stack reserve-buffer size
/// @tparam   has_static_stack_size true when all dynamic producers have static stack hints
/// @tparam   char_type             the print character type
/// @tparam   outputstmtype         the decayed output stream reference type
/// @tparam   scatter_type          the scatter descriptor representation
/// @tparam   T                     the current argument type
/// @tparam   Args                  the remaining argument types
/// @param    optstm                the output stream reference
/// @param    scatters              the caller-provided scatter descriptor storage
/// @param    totalsz               the measured reserve-buffer character requirement
/// @param    t                     the current argument
/// @param    args                  the remaining arguments
template <bool needprintlf, ::std::size_t position, ::std::size_t stack_buffer_size, bool has_static_stack_size,
		  ::std::integral char_type, bool retain_static_scatter = false,
		  bool preserve_static_fragments = false, typename outputstmtype,
		  typename scatter_type, typename T, typename... Args>
// The caller owns the descriptor array while this helper owns the character
// buffer; their addresses form one synchronous materialization transaction.
// Clang 17--22 otherwise retained an out-of-line helper for heterogeneous N=8+
// runs.  Forced inlining reduced text on every measured Clang release and made
// both Clang 22 N=8 controls win all eleven paired samples, so later Clang
// versions inherit that future-open code-generation policy.  GNU frontends are
// deliberately excluded: GCC 14--16 already inline naturally, while GCC 13
// enlarged the N=8 line specialization by 9,522 text bytes.  Semantic
// eligibility and the outer public-call boundary are unchanged.
#if defined(__clang__) && __clang_major__ >= 17
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void print_controls_dynamic_scatters_reserve_with_scatter(outputstmtype &optstm, scatter_type *scatters,
																			   ::std::size_t totalsz, T &t, Args &...args)
{
	if constexpr (has_static_stack_size &&
				  ::fast_io::details::decay::print_stack_buffer_size_within_limit<stack_buffer_size, char_type>)
	{
		// A statically bounded dynamic run may use stack reserve storage when the measured total fits.
		if (totalsz <= stack_buffer_size)
		{
			// The measured dynamic reserve output fits in the bounded stack buffer and writes once.
			char_type buffer[stack_buffer_size];
			auto ptr{::fast_io::details::decay::print_n_scatters_reserve<
				needprintlf, position, char_type, retain_static_scatter>(
				scatters, buffer, t, args...)};
			if constexpr (preserve_static_fragments)
			{
				::fast_io::details::decay::
					print_scatter_write_all_preserving_static_fragments(
						optstm, scatters,
						static_cast<::std::size_t>(ptr - scatters));
			}
			else
			{
				::fast_io::details::decay::print_scatter_write_all_dispatch(
					optstm, scatters,
					static_cast<::std::size_t>(ptr - scatters));
			}
			return;
		}
	}
	// The measured reserve output needs heap storage before descriptors can be written.
	::fast_io::details::local_operator_new_array_ptr<char_type> buffer(totalsz == 0 ? 1u : totalsz);
	auto ptr{::fast_io::details::decay::print_n_scatters_reserve<
		needprintlf, position, char_type, retain_static_scatter>(
		scatters, buffer.ptr, t, args...)};
	if constexpr (preserve_static_fragments)
	{
		::fast_io::details::decay::
			print_scatter_write_all_preserving_static_fragments(
				optstm, scatters,
				static_cast<::std::size_t>(ptr - scatters));
	}
	else
	{
		::fast_io::details::decay::print_scatter_write_all_dispatch(
			optstm, scatters, static_cast<::std::size_t>(ptr - scatters));
	}
}

/// @brief    Writes a mixed dynamic reserve/scatter prefix using stack or heap descriptor storage.
/// @tparam   needprintlf           true when the final consumed output should append a newline
/// @tparam   position              the number of argument positions included in the prefix
/// @tparam   scatterscount         the number of scatter descriptors available
/// @tparam   stack_buffer_size     the bounded stack reserve-buffer size
/// @tparam   has_static_stack_size true when all dynamic producers have static stack hints
/// @tparam   char_type             the print character type
/// @tparam   outputstmtype         the decayed output stream reference type
/// @tparam   T                     the current argument type
/// @tparam   Args                  the remaining argument types
/// @param    optstm                the output stream reference
/// @param    totalsz               the measured reserve-buffer character requirement
/// @param    t                     the current argument
/// @param    args                  the remaining arguments
template <bool needprintlf, ::std::size_t position, ::std::size_t scatterscount, ::std::size_t stack_buffer_size,
		  bool has_static_stack_size, ::std::integral char_type, bool retain_static_scatter = false,
		  bool preserve_static_fragments = false, typename outputstmtype, typename T, typename... Args>
// Ordinary inlining preserves the compile-time descriptor-storage decision in
// GCC 11--16 and Clang 17--23, with byte-identical benchmark binaries.
inline constexpr void print_controls_dynamic_scatters_reserve(
	outputstmtype &optstm, ::std::size_t totalsz, T &t, Args &...args)
{
	using scatter_type = ::std::conditional_t<
		::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>,
		::fast_io::io_scatter_t, ::fast_io::basic_io_scatter_t<char_type>>;
	if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scatterscount, scatter_type>)
	{
		// Small scatter descriptor storage is kept on the stack for the dynamic mixed run.
		scatter_type scatters[scatterscount];
		::fast_io::details::decay::print_controls_dynamic_scatters_reserve_with_scatter<
			needprintlf, position, stack_buffer_size, has_static_stack_size,
			char_type, retain_static_scatter, preserve_static_fragments>(
			optstm, scatters, totalsz, t, args...);
	}
	else
	{
		// Large scatter descriptor storage is allocated before the dynamic mixed run is materialized.
		::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(scatterscount);
		::fast_io::details::decay::print_controls_dynamic_scatters_reserve_with_scatter<
			needprintlf, position, stack_buffer_size, has_static_stack_size,
			char_type, retain_static_scatter, preserve_static_fragments>(
			optstm, scatters.ptr, totalsz, t, args...);
	}
}

/// @brief    Primary non-buffered control dispatcher for a freestanding print run.
/// @tparam   line          true when the final emitted output should append a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   skippings     number of already-consumed head arguments to skip
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool line, typename outputstmtype, ::std::size_t skippings, typename T, typename... Args>
inline constexpr void print_controls_impl(outputstmtype &optstm, T &t, Args &...args);

#if __cplusplus > 202302L && __cpp_pack_indexing >= 202311L
/// @brief Re-enters the original control scanner at the first unconsumed argument.
/// @details Only named lvalues cross this type-only edge. Pack indexing removes skipped specializations without
///          copying a source, forwarding it again, or introducing any intermediate output-strategy decision.
template <bool line, ::std::size_t skippings, typename outputstmtype, typename... Values>
inline constexpr void print_controls_skip(outputstmtype &optstm, Values &...values)
{
	static_assert(skippings < sizeof...(Values));
	[&]<::std::size_t... index>(::std::index_sequence<index...>) constexpr {
		::fast_io::details::decay::print_controls_impl<line, outputstmtype, 0u>(
			optstm, values...[skippings + index]...);
	}(::std::make_index_sequence<sizeof...(Values) - skippings>{});
}
#endif

/// @brief Tests whether this exact run contains immutable payload which the destination can consume in place.
/// @details A selected compiler-constant proxy and a type-sized `static_scatter_t` carry the same relevant lifetime
///          proof: their payload is independent of the descriptor/materialization frame.  Preserving that fact in a
///          mixed unknown run keeps the known fragment in immutable storage while only reserve siblings enter scratch.
template <typename outputstmtype, typename... Args>
inline constexpr bool print_output_run_retains_static_scatter =
	::fast_io::details::decay::print_output_retains_static_scatter<outputstmtype> &&
	(false || ... ||
	 (::fast_io::details::decay::
		  print_compiler_constant_static_fragment_proxy_traits<
			  ::std::remove_cvref_t<Args>>::value ||
	  ::fast_io::details::decay::print_static_scatter_traits<
		  typename outputstmtype::output_char_type,
		  ::std::remove_cvref_t<Args>>::available));

template <typename outputstmtype, typename T, typename... Args>
inline consteval contiguous_scatter_result find_output_continuous_scatters_n() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::details::decay::
			print_output_run_retains_static_scatter<outputstmtype, T, Args...>)
	{
		return ::fast_io::details::decay::find_continuous_native_scatters_n<char_type, T, Args...>();
	}
	else
	{
		return ::fast_io::details::decay::find_continuous_scatters_n<char_type, T, Args...>();
	}
}

/// @brief    Emits the remaining ordinary controls without attempting another grouped materialization.
/// @details  This is the conservative continuation for a run whose optional aggregate size is unavailable. Dynamic
///           reserve sizing is a documented stable multi-pass protocol, so the earlier failed grouping measurement
///           does not require caching to preserve the subsequent per-control emission. Keeping this path separate
///           also guarantees that a trailing newline belongs to exactly the final control.
/// @tparam   line          true when the final emitted control appends a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the current ordinary control type
/// @tparam   Args          the remaining ordinary control types
/// @param    optstm        the output stream reference
/// @param    t             the current control
/// @param    args          the remaining controls
template <bool line, typename outputstmtype, typename T, typename... Args>
inline constexpr void print_controls_sequential(outputstmtype &optstm, T &t, Args &...args)
{
	if constexpr (sizeof...(Args) == 0u)
	{
		// The final control owns the caller's optional newline.
		::fast_io::details::decay::print_control_single<line>(optstm, t);
	}
	else
	{
		// Earlier controls deliberately have no line terminator.
		::fast_io::details::decay::print_control_single<false>(optstm, t);
		::fast_io::details::decay::print_controls_sequential<line>(optstm, args...);
	}
}

/// @brief    Empty-pack overload for the dynamic reserve/scatter fast-entry test.
/// @tparam   char_type the print character type
/// @return   bool false because no argument prefix exists
template <::std::integral char_type>
inline constexpr bool print_controls_dynamic_scatters_reserve_fast_entry_available() noexcept
{
	return false;
}

/// @brief    Tests whether a print run can enter the dynamic reserve/scatter fast path.
/// @tparam   char_type the print character type
/// @tparam   T         the current argument type
/// @tparam   Args      the remaining argument types
/// @return   bool true when the leading run contains both scatter output and dynamic reserve output
template <::std::integral char_type, typename T, typename... Args>
inline constexpr bool print_controls_dynamic_scatters_reserve_fast_entry_available() noexcept
{
	constexpr contiguous_scatter_result res{
		::fast_io::details::decay::find_continuous_scatters_n<char_type, T, Args...>()};
	return res.position != 0 && res.hasscatters && res.hasdynamicreserve;
}

/// @brief Tests whether a normalized run contains a direct leaf that explicitly prefers a real writable put area.
/// @details The legacy mixed scatter/dynamic fast entry classifies only source representations. If it runs before the
///          output-aware dispatcher, an adjacent static scatter makes a marked range perform a complete size traversal,
///          materialize into temporary storage, and copy into a destination that could have accepted one traversal
///          directly. Admission therefore pairs three independent proofs: exact obuffer cursors on the destination, an
///          affirmative source cost marker, and a `print_define` callable for that exact output type. Append-only and
///          native scatter/write sinks cannot satisfy the first proof. Unmarked or destination-specific leaves cannot
///          satisfy the remaining proofs. This is a cost predicate only; the existing checked buffered dispatcher owns
///          short-put-area overflow, ordering, and final-newline semantics.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_run_has_put_area_preferred_direct_leaf_v =
	::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
	(false || ... ||
	 (::fast_io::put_area_printable_preferred<char_type, ::std::remove_cvref_t<Args>> &&
	  ::fast_io::details::direct_printable_to<
		  char_type, outputstmtype, ::std::remove_cvref_t<Args>>));

/// @brief    Emits the leading dynamic reserve/scatter run through the specialized fast path.
/// @tparam   line          true when the final emitted output should append a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool line, typename outputstmtype, typename T, typename... Args>
// The static analysis result is folded at every tested call site without a
// mandatory-inline attribute; keep this wrapper free to remain out of line in
// exceptionally large generated records.
inline constexpr void print_controls_dynamic_scatters_reserve_fast_entry(
	outputstmtype &optstm, T &t, Args &...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr contiguous_scatter_result res{
		::fast_io::details::decay::find_continuous_scatters_n<char_type, T, Args...>()};
	static_assert(res.position != 0);
	static_assert(res.hasscatters && res.hasdynamicreserve);
	if constexpr (line)
	{
		// A line-terminated fast path must have a finite scatter count for the optional newline descriptor.
		static_assert(res.neededscatters != SIZE_MAX);
	}
	static_assert(SIZE_MAX != sizeof...(Args));
	constexpr ::std::size_t n{sizeof...(Args) + static_cast<::std::size_t>(1)};
	constexpr bool needprintlf{n == res.position && line};
	constexpr ::std::size_t mxsize{
		static_cast<::std::size_t>(res.neededspace + static_cast<::std::size_t>(needprintlf))};
	constexpr ::std::size_t scatterscount{res.neededscatters +
										  static_cast<::std::size_t>(line && res.position == n)};
	constexpr bool has_static_stack_size{
		::fast_io::details::decay::ndynamic_print_reserve_has_static_stack_size<res.position, char_type, T,
																				Args...>()};
	constexpr ::std::size_t producer_static_stack_size{
		::fast_io::details::decay::ndynamic_print_reserve_static_stack_size<res.position, char_type, T, Args...>()};
	constexpr ::std::size_t dynamic_stack_budget{
		::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<producer_static_stack_size, char_type>()};
	constexpr ::std::size_t stack_buffer_size{
		::fast_io::details::intrinsics::add_or_overflow_die(mxsize, dynamic_stack_budget)};
	::std::size_t const dynsz{
		::fast_io::details::decay::ndynamic_print_reserve_size<res.position, char_type>(t, args...)};
	::std::size_t const totalsz{
		::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(mxsize, dynsz)};
	if (totalsz == SIZE_MAX)
	{
		// The mixed run is still printable; only its shared reserve buffer is unavailable.
		return ::fast_io::details::decay::print_controls_sequential<line>(optstm, t, args...);
	}
	::fast_io::details::decay::print_controls_dynamic_scatters_reserve<
		needprintlf, res.position, scatterscount, stack_buffer_size, has_static_stack_size, char_type>(optstm, totalsz,
																									   t, args...);
	if constexpr (res.position != n)
	{
		// Remaining arguments after the fast prefix continue through the general control dispatcher.
		print_controls_impl<line, outputstmtype, res.position - 1>(optstm, args...);
	}
}

/// @brief    Emits a freestanding control run without relying on the stream put area.
/// @details  The dispatcher coalesces context-capable runs, scatter-only prefixes, reserve-only prefixes, and mixed
///           reserve/scatter prefixes before recursively emitting any remaining arguments.
/// @tparam   line          true when the final emitted output should append a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   skippings     number of already-consumed head arguments to skip
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool line, typename outputstmtype, ::std::size_t skippings = 0, typename T, typename... Args>
/*
GCC 16 classifies the type-changing continuation after an unretained scatter as
an unlikely size-growth edge.  In a heterogeneous run that edge is instead the
only ordered continuation and may be reached repeatedly; leaving each suffix
out of line retains overlapping template clones and one call per boundary.

The attribute changes only this internal layout decision.  For every
instantiation, the function still emits the same maximal prefix and invokes the
unique suffix specialization exactly once iff that prefix does not consume the
record.  It neither changes the already-decayed `T&` objects nor adds a
forwarding/reference layer.  Native P1 output is byte-identical and P2 has the
same text size; large heterogeneous raw-output runs remove the redundant call
chain.  GCC 11--15 did not reproduce the improvement, so they deliberately keep
their ordinary inliner.  Size-optimized, AVX-disabled, non-x86, and non-GCC
builds likewise retain the original policy.
*/
#if defined(__GNUC__) && !defined(__clang__) && !defined(__INTEL_COMPILER) &&       \
	!defined(__CUDACC__) && defined(__OPTIMIZE__) && !defined(__OPTIMIZE_SIZE__) && \
	__GNUC__ >= 16 && defined(__AVX__) &&                                           \
	(defined(__x86_64__) || defined(_M_AMD64) || defined(_M_X64)) &&                \
	!(defined(__arm64ec__) || defined(_M_ARM64EC))
[[__gnu__::__always_inline__]]
#endif
inline constexpr void print_controls_impl(outputstmtype &optstm, T &t, Args &...args)
{
	if constexpr (skippings != 0)
	{
		// Consumed positions do not participate in another strategy decision. Discard their scanners as well as
		// their emitters; otherwise every skipped suffix recreates metadata that the selected run never uses.
#if __cplusplus > 202302L && __cpp_pack_indexing >= 202311L
		::fast_io::details::decay::print_controls_skip<line, skippings>(optstm, t, args...);
#else
		print_controls_impl<line, outputstmtype, skippings - 1>(optstm, args...);
#endif
	}
	else if constexpr (sizeof...(Args) == 0)
	{
		print_control_single<line>(optstm, t);
	}
	else
	{
		using char_type = typename outputstmtype::output_char_type;
		using scatter_type [[maybe_unused]] = ::std::conditional_t<
			::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>,
			io_scatter_t, basic_io_scatter_t<char_type>>;
		constexpr contiguous_scatter_result res{
			::fast_io::details::decay::find_output_continuous_scatters_n<outputstmtype, T, Args...>()};
		constexpr bool retain_static_scatter{
			::fast_io::details::decay::print_output_retains_static_scatter<outputstmtype>};
		constexpr bool preserve_static_fragments{
			retain_static_scatter &&
			::fast_io::details::decay::print_first_n_contains_static_fragment<
				res.position, char_type, T, Args...>::value};
		constexpr context_capture_run_result context_capture_res{
			::fast_io::details::decay::find_context_capture_run_n<char_type, T, Args...>()};
		if constexpr (context_capture_res.has_context)
		{
			// Runs containing context-printable output use a reusable capture buffer to reduce small writes.
			static_assert(SIZE_MAX != sizeof...(Args));
			constexpr ::std::size_t n{sizeof...(Args) + static_cast<::std::size_t>(1)};
			constexpr bool needprintlf{n == context_capture_res.position && line};
			/*
			Context and dynamic producer sizes are reusable streaming windows.
			Only consecutive static reserve producers contribute a burst size, so
			the final capture buffer uses max semantics instead of summing producer
			windows across the run.
			*/
			constexpr ::std::size_t context_dynamic_size{
				context_capture_res.context_buffer_size < context_capture_res.dynamic_buffer_size
					? context_capture_res.dynamic_buffer_size
					: context_capture_res.context_buffer_size};
			constexpr ::std::size_t reserve_context_dynamic_size{
				context_dynamic_size < context_capture_res.max_static_reserve_burst_size
					? context_capture_res.max_static_reserve_burst_size
					: context_dynamic_size};
			constexpr ::std::size_t buffer_size{reserve_context_dynamic_size < 32u ? 32u
																				   : reserve_context_dynamic_size};
			if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<buffer_size, char_type>)
			{
				// Small context-capture buffers stay on the stack while the captured run is emitted.
				char_type buffer[buffer_size];
				::fast_io::details::decay::print_context_capture_run<needprintlf, context_capture_res.position>(
					optstm, buffer, buffer, buffer + buffer_size, t, args...);
			}
			else
			{
				// Large context-capture buffers are allocated before the captured run is emitted.
				::fast_io::details::local_operator_new_array_ptr<char_type> newptr(buffer_size);
				char_type *buffer{newptr.ptr};
				::fast_io::details::decay::print_context_capture_run<needprintlf, context_capture_res.position>(
					optstm, buffer, buffer, buffer + buffer_size, t, args...);
			}
			if constexpr (context_capture_res.position != n)
			{
				// Arguments after the context-captured prefix continue through the general dispatcher.
				print_controls_impl<line, outputstmtype, context_capture_res.position - 1>(optstm, args...);
			}
		}
		else if constexpr (res.position == 0)
		{
			// The current argument cannot join a contiguous optimized prefix, so emit it alone.
			print_control_single<false>(optstm, t);
			// Spell the known stream type and zero skip count explicitly. GCC 11 cannot apply the default non-type argument
			// before deducing the following type pack here, although newer frontends produce the same specialization.
			print_controls_impl<line, outputstmtype, 0u>(optstm, args...);
		}
		else
		{
			// A contiguous scatter/reserve-friendly prefix was found and can be emitted as one optimized run.
			if constexpr (line)
			{
				// A line-terminated optimized prefix must have a finite scatter count when a newline descriptor is needed.
				static_assert(res.neededscatters != SIZE_MAX);
			}
			static_assert(SIZE_MAX != sizeof...(Args));
			constexpr ::std::size_t n{sizeof...(Args) + static_cast<::std::size_t>(1)};
			constexpr bool needprintlf{n == res.position && line};
			if constexpr (res.hasscatters && !res.hasreserve && !res.hasdynamicreserve)
			{
				// Scatter-only prefixes are emitted directly as scatter descriptors.
				constexpr ::std::size_t scatterscount{res.neededscatters + static_cast<::std::size_t>(needprintlf)};
				::fast_io::details::decay::print_controls_scatters<
					needprintlf, res.position, scatterscount, char_type,
					preserve_static_fragments>(
					optstm, t, args...);
				if constexpr (res.position != n)
				{
					// Arguments after the scatter-only prefix continue through the general dispatcher.
					print_controls_impl<line, outputstmtype, res.position - 1>(optstm, args...);
				}
			}
			else
			{
				// Prefixes with reserve output need temporary character storage before they can be written.
				constexpr ::std::size_t mxsize{
					static_cast<::std::size_t>(res.neededspace + static_cast<::std::size_t>(needprintlf))};
				if constexpr (!res.hasscatters)
				{
					// Reserve-only prefixes can be materialized as one contiguous output range.
					static_assert(!needprintlf || res.neededspace != SIZE_MAX);
					if constexpr (res.hasdynamicreserve)
					{
						// Dynamic reserve-only prefixes are measured before choosing stack or heap storage.
						constexpr bool has_static_stack_size{
							::fast_io::details::decay::ndynamic_print_reserve_has_static_stack_size<res.position,
																									char_type, T,
																									Args...>()};
						constexpr ::std::size_t producer_static_stack_size{
							::fast_io::details::decay::ndynamic_print_reserve_static_stack_size<res.position, char_type,
																								T, Args...>()};
						constexpr ::std::size_t dynamic_stack_budget{
							::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<producer_static_stack_size,
																								 char_type>()};
						constexpr ::std::size_t stack_buffer_size{
							::fast_io::details::intrinsics::add_or_overflow_die(mxsize, dynamic_stack_budget)};
						::std::size_t const dynsz{
							::fast_io::details::decay::ndynamic_print_reserve_size<res.position, char_type>(t, args...)};
						::std::size_t const totalsz{
							::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(mxsize,
																												  dynsz)};
						if (totalsz == SIZE_MAX)
						{
							// One shared dynamic reserve range is optional; preserve output with per-control emission instead.
							return ::fast_io::details::decay::print_controls_sequential<line>(optstm, t, args...);
						}
						if constexpr (has_static_stack_size &&
									  ::fast_io::details::decay::print_stack_buffer_size_within_limit<stack_buffer_size,
																									  char_type>)
						{
							// A statically bounded dynamic reserve prefix may stay on the stack.
							if (totalsz <= stack_buffer_size)
							{
								// The measured dynamic reserve prefix fits in the bounded stack buffer.
								char_type buffer[stack_buffer_size];
								char_type *ptred{
									::fast_io::details::decay::print_n_reserve<res.position, char_type>(buffer, t,
																										args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the contiguous stack buffer.
									*ptred = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptred;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptred);
							}
							else
							{
								// The measured dynamic reserve prefix exceeds the stack budget and uses heap storage.
								::fast_io::details::local_operator_new_array_ptr<char_type> newptr(totalsz);
								char_type *buffer{newptr.ptr};
								char_type *ptred{
									::fast_io::details::decay::print_n_reserve<res.position, char_type>(buffer, t,
																										args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the contiguous heap buffer.
									*ptred = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptred;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptred);
							}
						}
						else
						{
							// Dynamic reserve prefixes without bounded stack eligibility use heap storage.
							::fast_io::details::local_operator_new_array_ptr<char_type> newptr(totalsz);
							char_type *buffer{newptr.ptr};
							char_type *ptred{
								::fast_io::details::decay::print_n_reserve<res.position, char_type>(buffer, t, args...)};
							if constexpr (needprintlf)
							{
								// The line variant appends the newline to the contiguous heap buffer.
								*ptred = ::fast_io::char_literal_v<u8'\n', char_type>;
								++ptred;
							}
							::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptred);
						}
					}
					else if constexpr (res.hasreserve)
					{
						// Static reserve-only prefixes have a compile-time storage requirement.
						if constexpr (res.neededspace == 0)
						{
							// A zero-sized reserve prefix can only emit the optional trailing newline.
							if constexpr (needprintlf)
							{
								// The line variant emits the trailing newline as a single character output.
								::fast_io::operations::decay::char_put_decay_dispatch(optstm,
																					  ::fast_io::char_literal_v<u8'\n', char_type>);
							}
						}
						else
						{
							// Non-empty static reserve prefixes are materialized before a single contiguous write.
							if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<mxsize,
																										  char_type>)
							{
								// Small static reserve prefixes are materialized on the stack.
								char_type buffer[mxsize];
								char_type *ptred{
									::fast_io::details::decay::print_n_reserve<res.position, char_type>(buffer, t, args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the stack buffer.
									*ptred = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptred;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptred);
							}
							else
							{
								// Large static reserve prefixes are materialized in heap storage.
								::fast_io::details::local_operator_new_array_ptr<char_type> newptr(mxsize);
								char_type *buffer{newptr.ptr};
								char_type *ptred{
									::fast_io::details::decay::print_n_reserve<res.position, char_type>(buffer, t, args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the heap buffer.
									*ptred = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptred;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptred);
							}
						}
					}
				}
				else if constexpr (res.hasreserve && !res.hasdynamicreserve)
				{
					// Mixed scatter/static-reserve prefixes are emitted through prebuilt scatter descriptors.
					constexpr ::std::size_t scatterscount{res.neededscatters +
														  static_cast<::std::size_t>(line && res.position == n)};
					::fast_io::details::decay::print_controls_scatters_reserve<
						needprintlf, res.position, scatterscount, mxsize, char_type,
						retain_static_scatter, preserve_static_fragments>(
						optstm, t, args...);
				}
				else if constexpr (res.hasdynamicreserve)
				{
					// Mixed scatter/dynamic-reserve prefixes are measured before descriptor materialization.
					constexpr ::std::size_t scatterscount{res.neededscatters +
														  static_cast<::std::size_t>(line && res.position == n)};
					constexpr bool has_static_stack_size{
						::fast_io::details::decay::ndynamic_print_reserve_has_static_stack_size<res.position, char_type, T,
																								Args...>()};
					constexpr ::std::size_t producer_static_stack_size{
						::fast_io::details::decay::ndynamic_print_reserve_static_stack_size<res.position, char_type, T,
																							Args...>()};
					constexpr ::std::size_t dynamic_stack_budget{
						::fast_io::details::decay::dynamic_print_reserve_static_stack_budget<producer_static_stack_size,
																							 char_type>()};
					constexpr ::std::size_t stack_buffer_size{
						::fast_io::details::intrinsics::add_or_overflow_die(mxsize, dynamic_stack_budget)};
					::std::size_t const dynsz{
						::fast_io::details::decay::ndynamic_print_reserve_size<res.position, char_type>(t, args...)};
					::std::size_t const totalsz{
						::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(mxsize, dynsz)};
					if (totalsz == SIZE_MAX)
					{
						// A measured aggregate may be unavailable even when every producer is individually printable.
						return ::fast_io::details::decay::print_controls_sequential<line>(optstm, t, args...);
					}
					::fast_io::details::decay::print_controls_dynamic_scatters_reserve<
						needprintlf, res.position, scatterscount, stack_buffer_size, has_static_stack_size, char_type,
						retain_static_scatter, preserve_static_fragments>(optstm, totalsz, t, args...);
				}
				if constexpr (res.position != n)
				{
					// Arguments after the optimized prefix continue through the general dispatcher.
					print_controls_impl<line, outputstmtype, res.position - 1>(optstm, args...);
				}
			}
		}
	}
}

/// @brief    Emits a freestanding control run while taking advantage of an output stream put area.
/// @details  Scatter prefixes bypass the put area through scatter writes; reserve prefixes first try the current
///           output buffer and only fall back to temporary storage when the put area is too small.
/// @tparam   line          true when the final emitted output should append a newline
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   skippings     number of already-consumed head arguments to skip
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
/// @param    optstm        the output stream reference
/// @param    t             the current argument
/// @param    args          the remaining arguments
template <bool line, typename outputstmtype, ::std::size_t skippings = 0, typename T, typename... Args>
inline constexpr void print_controls_buffer_impl(outputstmtype &optstm, T &t, Args &...args)
{
	if constexpr (skippings != 0)
	{
		// Already-emitted prefix positions are skipped before the buffered dispatcher resumes.
		::fast_io::details::decay::print_controls_buffer_impl<line, outputstmtype, skippings - 1>(optstm, args...);
	}
	else if constexpr (sizeof...(Args) == 0)
	{
		// A single remaining argument uses the one-control emitter with the caller's line policy.
		using char_type = typename outputstmtype::output_char_type;
		if constexpr (print_fixed_static_reserve_run_available<outputstmtype, char_type, T>())
		{
			// Semantic condition selection can reduce a record to one ordinary reserve leaf. Re-apply the fixed-view
			// boundary here so an audited formatter which initializes scratch inside its reserve bound is staged before
			// the returned logical prefix is committed to externally visible storage.
			print_fixed_static_reserve_run<line, outputstmtype, char_type>(optstm, t);
		}
		else if constexpr (print_buffered_hot_decimal_scalar_available<
							   outputstmtype, char_type, T>())
		{
			print_control_single_buffered_hot<line>(optstm, t);
		}
		else if constexpr (
			::fast_io::details::decay::print_control_single_out_of_line_eligible<
				outputstmtype, T>)
		{
			::fast_io::details::decay::print_control_single_diagnostic_boundary<line>(
				optstm, t);
		}
		else
		{
			print_control_single<line>(optstm, t);
		}
	}
	else
	{
		// Multiple arguments may expose scatter or reserve prefixes that can be grouped before recursion.
		using char_type = typename outputstmtype::output_char_type;
		static_assert(SIZE_MAX != sizeof...(Args));
		constexpr ::std::size_t n{sizeof...(Args) + static_cast<::std::size_t>(1)};
		if constexpr (print_fixed_static_reserve_run_available<outputstmtype, char_type, T, Args...>())
		{
			print_fixed_static_reserve_run<line, outputstmtype, char_type>(optstm, t, args...);
			return;
		}
		else if constexpr (
			(n == 32u || n >= 52u) &&
			::fast_io::details::decay::print_control_single_out_of_line_eligible<
				outputstmtype, T> &&
			(::fast_io::details::decay::print_control_single_out_of_line_eligible<
				 outputstmtype, Args> &&
			 ...))
		{
			// GCC's isolated 32-control cliff benefits from chunks. Counts 33--51 retain inlining because their measured
			// aggregate cost still benefits from local cursor propagation. From 52 onward the shared chunks bound compile
			// resources, text size, and nonempty latency; this deliberately forgoes cross-leaf optimization of the rare
			// all-empty pack rather than pre-observing size CPOs and changing their required evaluation order.
			::fast_io::details::decay::print_controls_out_of_line_chunks<line>(optstm, t, args...);
			return;
		}
		constexpr bool fixed_external_overstore_component_run{
			print_direct_obuffer_reserve_destination<outputstmtype> &&
			(::fast_io::details::decay::print_fixed_external_overstore_traits<
				 char_type, ::std::remove_cvref_t<T>>::available ||
			 (false || ... ||
			  ::fast_io::details::decay::print_fixed_external_overstore_traits<
				  char_type, ::std::remove_cvref_t<Args>>::available))};
		constexpr auto mixed_result{
			::fast_io::details::decay::find_continuous_scatters_n<char_type, T, Args...>()};
		// A fixed view keeps its established retained-scatter dispatch here. Dedicated complete-run callers may still
		// select the compact entry after proving the whole pack, while every external endpoint retains its prior choice.
		constexpr bool generic_mixed_put_area_endpoint{
			::fast_io::details::decay::print_buffered_mixed_generic_endpoint<
				::std::remove_cvref_t<outputstmtype>>};
		constexpr bool mixed_put_area_run{
			mixed_result.position > 1u && mixed_result.hasscatters && mixed_result.hasreserve &&
			!mixed_result.hasdynamicreserve && generic_mixed_put_area_endpoint &&
			::fast_io::deferred_obuffer_commit_safe<char_type, outputstmtype> &&
			::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				outputstmtype, char_type> &&
			::fast_io::details::decay::print_buffered_mixed_put_area_run_available<
				mixed_result.position, char_type, T, Args...>()};
		if constexpr (mixed_put_area_run)
		{
			constexpr bool needprintlf{line && mixed_result.position == n};
			if (::fast_io::details::decay::print_buffered_mixed_put_area_try<
					needprintlf, mixed_result.position, char_type>(optstm, t, args...)) [[likely]]
			{
				if constexpr (mixed_result.position != n)
				{
					::fast_io::details::decay::print_controls_buffer_impl<
						line, outputstmtype, mixed_result.position - 1u>(optstm, args...);
				}
				return;
			}
		}
		if constexpr (
			::fast_io::details::decay::print_retained_buffered_reserve_scatters_run_selected<
				line, char_type, outputstmtype, T, Args...>())
		{
			// Static retained reserve-scatters producers are grouped before either legacy scanner can split the run into
			// one primitive buffered scatter operation per leaf.
			constexpr auto retained_result{
				::fast_io::details::decay::find_retained_buffered_reserve_scatters_run<
					char_type, T, Args...>()};
			constexpr bool needprintlf{line && retained_result.position == n};
			::fast_io::details::decay::print_retained_buffered_reserve_scatters_run<
				needprintlf, retained_result.position, char_type>(optstm, t, args...);
			if constexpr (retained_result.position != n)
			{
				// The retained prefix emitted no newline when a tail remains; skip exactly its remaining consumed positions
				// and let the established buffered recursion preserve the caller's final line policy.
				::fast_io::details::decay::print_controls_buffer_impl<
					line, outputstmtype, retained_result.position - 1u>(optstm, args...);
			}
		}
		else
		{
			constexpr auto scatters_result{
				::fast_io::details::decay::find_continuous_scatters_reserve_n<true, char_type, T, Args...>()};
			using scatter_type [[maybe_unused]] = ::std::conditional_t<
				::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>,
				io_scatter_t, basic_io_scatter_t<char_type>>;
			if constexpr (scatters_result.position != 0)
			{
				// A leading scatter-friendly prefix is emitted through scatter descriptors.
				if constexpr (line)
				{
					// A line-terminated scatter prefix must have a finite position for the optional newline descriptor.
					static_assert(scatters_result.position != SIZE_MAX);
				}

				constexpr bool needprintlf{n == scatters_result.position && line};
				constexpr ::std::size_t scatterscount{scatters_result.position - scatters_result.null +
													  static_cast<::std::size_t>(needprintlf)};
				::fast_io::details::decay::print_controls_scatters<needprintlf, scatters_result.position,
																   scatterscount, char_type>(optstm, t, args...);
				if constexpr (scatters_result.position != n)
				{
					// Arguments after the scatter prefix continue through the buffered dispatcher.
					::fast_io::details::decay::print_controls_buffer_impl<line, outputstmtype,
																		  scatters_result.position - 1>(optstm, args...);
				}
			}
			else
			{
				// Without a scatter prefix, try to group a reserve-only prefix into the output buffer.
				constexpr auto rsvresult{
					::fast_io::details::decay::find_continuous_scatters_reserve_n<false, char_type, T, Args...>()};
				if constexpr (1 < rsvresult.position)
				{
					// Reserve prefixes with more than one position can amortize buffer checks and writes.
					constexpr bool needprintlf{n == rsvresult.position && line};
					constexpr ::std::size_t buffersize{rsvresult.neededspace + static_cast<::std::size_t>(needprintlf)};
					char_type *bcurr{obuffer_curr(optstm)};
					char_type *bend{obuffer_end(optstm)};
					// A reserve prefix may end exactly at `bend`; no sentinel character is stored in the put area.
					bool fits{!fixed_external_overstore_component_run &&
							  buffersize <= ::fast_io::details::decay::print_obuffer_remaining_size<
												char_type, outputstmtype>(bcurr, bend)};
					if constexpr (!fixed_external_overstore_component_run &&
								  minimum_buffer_output_stream_require_size_impl<outputstmtype, buffersize>)
					{
						// Streams with a sufficient minimum put area can prepare the buffer once before writing in place.
						if (!fits) [[unlikely]]
						{
							// The current put area is short, so prepare a minimum-size buffer for the reserve prefix.
							obuffer_minimum_size_flush_prepare_define(optstm);
							bcurr = obuffer_curr(optstm);
						}
						// A successful minimum-buffer prepare is the capacity proof; avoid a
						// redundant null guard in the hot grouped-reserve path.
						bcurr = ::fast_io::details::decay::print_n_reserve<rsvresult.position, char_type>(
							bcurr, t, args...);
						if constexpr (needprintlf)
						{
							// The line variant appends the newline in the same output-buffer commit.
							*bcurr = ::fast_io::char_literal_v<u8'\n', char_type>;
							++bcurr;
						}
						obuffer_set_curr(optstm, bcurr);
					}
					else
					{
						// Streams without a minimum-size guarantee use the current put area only when it already fits.
						if (fits) [[likely]]
						{
							// The current put area can hold the entire reserve prefix.
							bcurr = ::fast_io::details::decay::print_n_reserve<rsvresult.position, char_type>(
								bcurr, t, args...);
							if constexpr (needprintlf)
							{
								// The line variant appends the newline in the same output-buffer commit.
								*bcurr = ::fast_io::char_literal_v<u8'\n', char_type>;
								++bcurr;
							}
							obuffer_set_curr(optstm, bcurr);
						}
						else [[unlikely]]
						{
							// The current put area is too small, so materialize the reserve prefix out of line.
							if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<
											  buffersize, char_type>)
							{
								// Small reserve prefixes fall back to stack materialization and one grouped write dispatch.
								char_type buffer[buffersize];
								char_type *ptr{::fast_io::details::decay::print_n_reserve<rsvresult.position,
																						  char_type>(buffer, t,
																									 args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the stack buffer before writing.
									*ptr = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptr;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptr);
							}
							else
							{
								// Large reserve prefixes fall back to heap materialization and one grouped write dispatch.
								::fast_io::details::local_operator_new_array_ptr<char_type> newptr(buffersize);
								char_type *buffer{newptr.ptr};
								char_type *ptr{::fast_io::details::decay::print_n_reserve<rsvresult.position,
																						  char_type>(buffer, t,
																									 args...)};
								if constexpr (needprintlf)
								{
									// The line variant appends the newline to the heap buffer before writing.
									*ptr = ::fast_io::char_literal_v<u8'\n', char_type>;
									++ptr;
								}
								::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, ptr);
							}
						}
					}
					if constexpr (rsvresult.position != n)
					{
						// Arguments after the reserve prefix continue through the buffered dispatcher.
						::fast_io::details::decay::print_controls_buffer_impl<
							line, outputstmtype, rsvresult.position - 1>(optstm, args...);
					}
				}
				else
				{
					// No useful prefix was found, so emit the current argument and continue with the tail.
					if constexpr (
						::fast_io::details::decay::print_control_single_out_of_line_eligible<
							outputstmtype, T>)
					{
						::fast_io::details::decay::print_control_single_diagnostic_boundary<
							line && sizeof...(args) == 0>(optstm, t);
					}
					else
					{
						::fast_io::details::decay::print_control_single<
							line && sizeof...(args) == 0>(optstm, t);
					}
					if constexpr (sizeof...(args) != 0)
					{
						// The tail still owns the caller's final line policy.
						::fast_io::details::decay::print_controls_buffer_impl<line>(optstm, args...);
					}
				}
			}
		}
	}
}

/// @brief    Returns the semantic node stored inside a parameter object, or the value itself.
/// @tparam   T the semantic candidate type
/// @param    t the semantic candidate object
/// @return   decltype(auto) the unwrapped semantic node reference or the original object
template <typename T>
inline constexpr decltype(auto) print_semantic_node_ref(T &&t) noexcept
{
	if constexpr (::fast_io::details::decay::print_semantic_parameter_object_v<::std::remove_cvref_t<T>>)
	{
		// Parameter objects forward semantic operations to their wrapped reference member.
		return ::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t).reference);
	}
	else
	{
		// Plain semantic nodes and leaves are already the reference used by the printer.
		return ::std::forward<T>(t);
	}
}

/// @brief    Detects semantic arguments that expand into print packs.
/// @tparam   T the argument type to inspect
template <typename T>
inline constexpr bool print_semantic_pack_argument_v =
	::fast_io::details::print_pack<T> ||
	(::fast_io::details::decay::print_semantic_parameter_object_v<::std::remove_cvref_t<T>> &&
	 ::fast_io::details::print_pack<
		 decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<T>()))>);

/// @brief Detects a semantic condition at one normalized argument position.
/// @details The one-layer semantic-node predicate is part of execution semantics: recursive access through
///          `print_semantic_node_ref` identifies the stored node only after that admission check succeeds.
template <typename T>
inline constexpr bool print_semantic_top_level_condition_v =
	::fast_io::details::decay::print_semantic_node<T> &&
	::fast_io::details::decay::print_semantic_condition_v<
		::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<T>()))>>;

/// @brief Identifies a normalized argument which execution must route through semantic expansion.
/// @details Ordinary semantic-node admission intentionally unwraps only one public parameter layer. Pack execution is
///          the documented exception: `print_semantic_pack_argument_v` accepts recursively wrapped packs and the pack
///          expander consumes that exact shape. Keeping the union in one predicate prevents a supported pack from
///          falling into `print_freestanding_decay_no_pack`. It does not broaden condition or width recognition.
template <typename T>
inline constexpr bool print_semantic_execution_node_v =
	::fast_io::details::decay::print_semantic_node<T> ||
	::fast_io::details::decay::print_semantic_pack_argument_v<T>;

/// @brief Proves that a complete normalized argument list needs no semantic pack/node dispatcher.
/// @details This is the substitution-time contract of `print_freestanding_decay_no_pack`; keeping it as a named fold
///          makes every caller prove the same execution invariant before the function body can participate.
template <typename... Args>
inline constexpr bool print_semantic_flat_record_v =
	(!::fast_io::details::decay::print_semantic_execution_node_v<Args> && ...);

/// @brief    Alias result type produced by io_print_alias for one semantic input argument.
/// @tparam   char_type the print character type
/// @tparam   T         the input argument type
template <::std::integral char_type, typename T>
using print_semantic_input_alias_t = decltype(::fast_io::io_print_alias(::std::declval<T>()));

/// @brief    Forwarded semantic input type after aliasing and character-aware print forwarding.
/// @tparam   char_type the print character type
/// @tparam   T         the input argument type
template <::std::integral char_type, typename T>
using print_semantic_input_forward_t =
	decltype(::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(::std::declval<T>())));

/// @brief    Detects inputs that become semantic nodes after aliasing and print forwarding.
/// @tparam   char_type the print character type
/// @tparam   T         the input argument type
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_input_argument_v =
	::fast_io::details::decay::print_semantic_node_no_parameter_v<
		::std::remove_cvref_t<::fast_io::details::decay::print_semantic_input_alias_t<char_type, T>>> ||
	::fast_io::details::decay::print_semantic_execution_node_v<
		::fast_io::details::decay::print_semantic_input_forward_t<char_type, T>>;

/// @brief Proves one public source is already a closed library integral-scalar record.
/// @details Let S be the cvref-stripped public source and N its ordinary `io_print_alias` followed by
///          `io_print_forward` result.  Volatile access is rejected, the source trait proves that S owns a fundamental
///          integral value with no user-associated namespace, and `same_as<S,N>` proves normalization neither replaces
///          the operation type nor introduces a status-bearing proxy.  The final requirements prove the same static
///          reserve extent and non-throwing writer consumed by the fixed-view emitter.  Thus the shortcut skips only
///          dispatcher scaffolding; it does not skip or duplicate source normalization.
template <::std::integral char_type, typename source_type>
inline consteval bool
print_fixed_public_integral_manip_source_available() noexcept
{
	using source_reference = ::std::remove_reference_t<source_type>;
	using source_value = ::std::remove_cvref_t<source_type>;
	if constexpr (
		::std::is_volatile_v<source_reference> ||
		!print_fixed_public_integral_manip_source_traits<source_value>::available)
	{
		return false;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_input_argument_v<
			char_type, source_reference &>)
	{
		return false;
	}
	else if constexpr (requires(source_reference &source) {
#if defined(__HERBCEPTIONS__)
							   // The shortcut elides the generic normalization graph. Each expression it elides must have
							   // neither deterministic failure nor a traditional unwind edge for this exact named public lvalue.
							   requires(!throws((::fast_io::io_print_alias(source))));
							   requires(!throws((::fast_io::io_print_forward<char_type>(
								   ::fast_io::io_print_alias(source)))));
#endif
							   {
							   ::fast_io::io_print_alias(source)
						   } noexcept;
						   {
							   ::fast_io::io_print_forward<char_type>(
								   ::fast_io::io_print_alias(source))
						   } noexcept -> ::std::same_as<source_value>;
					   })
	{
		return ::fast_io::reserve_printable<char_type, source_value> &&
			requires(char_type * ptr, source_value & value)
		{
#if defined(__HERBCEPTIONS__)
				requires(!throws((print_reserve_define(
					::fast_io::io_reserve_type<char_type, source_value>, ptr, value))));
#endif
				{
				print_reserve_define(
					::fast_io::io_reserve_type<char_type, source_value>,
					ptr, value)
			} noexcept -> ::std::same_as<char_type *>;
		};
	}
	else
	{
		return false;
	}
}

/// @brief Admits a complete integral-manipulator run to the fixed external-buffer emitter.
/// @details The destination marker proves stable externally owned storage with no hidden growth, flush, lock, or status
///          operation.  Every source independently satisfies the closed-normalization proof above.  A single static
///          capacity check may therefore replace the general CPO graph, after which reserve writers execute exactly once
///          in left-to-right order and the destination publishes exactly the same final cursor.  Line mode contributes
///          its one trailing newline inside that same checked extent.
template <bool line, typename output, ::std::integral char_type,
		  typename... Args>
inline consteval bool
print_fixed_public_integral_manip_run_available() noexcept
{
	return sizeof...(Args) != 0u && sizeof...(Args) <= 32u &&
		   print_direct_obuffer_reserve_destination<output> &&
		   !::fast_io::operations::decay::defines::
			   has_output_or_io_stream_mutex_ref_define<output> &&
		   !::fast_io::operations::decay::defines::has_status_print_define<
			   line, output, ::std::remove_cvref_t<Args>...> &&
		   (print_fixed_public_integral_manip_source_available<
				char_type, Args>() &&
			...);
}

/// @brief Derives semantic input forwarding's exception specification from the CPO branch it invokes.
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_input_forward_nothrow = []() constexpr {
	if constexpr (::fast_io::details::decay::print_semantic_node_no_parameter_v<
					  ::std::remove_cvref_t<
						  decltype(::fast_io::io_print_alias(::std::declval<T>()))>>)
	{
		using alias_result = decltype(::fast_io::io_print_alias(::std::declval<T>()));
		return noexcept(::fast_io::io_print_alias(::std::declval<T>())) &&
			   ::fast_io::details::io_print_forward_transport_nothrow<alias_result>;
	}
	else
	{
		return noexcept(::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(::std::declval<T>())));
	}
}();

#if defined(__HERBCEPTIONS__)
/// @brief Classifies semantic source normalization's deterministic error effect from its exact selected expression.
/// @details A parameterless semantic alias deliberately bypasses status forwarding and enters only the ordinary
///          value/reference transport. Every other alias enters character-aware forwarding. Querying the complete
///          expression in each arm includes both alias evaluation and any required result materialization without
///          conflating that ABI effect with the independent traditional-exception `noexcept` contract above.
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_input_forward_herbceptions_throws = []() constexpr {
	if constexpr (::fast_io::details::decay::print_semantic_node_no_parameter_v<
						  ::std::remove_cvref_t<
							  decltype(::fast_io::io_print_alias(::std::declval<T>()))>>)
	{
		return throws((::fast_io::details::io_print_forward_transport(
			::fast_io::io_print_alias(::std::declval<T>()))));
	}
	else
	{
		return throws((::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(::std::declval<T>()))));
	}
}();
#endif

/// @brief    Applies the semantic input forwarding protocol to one argument.
/// @tparam   char_type the print character type
/// @tparam   T         the input argument type
/// @param    t         the input argument
/// @return   decltype(auto) the semantic node or forwarded print argument
/// @note     Both aliasing and status forwarding are extension CPOs.  The conditional exception specification is
///           derived from the selected branch, so a throwing customization propagates rather than terminating.
template <::std::integral char_type, typename T>
inline constexpr decltype(auto) print_semantic_input_forward(T &&t)
	FAST_IO_HERBCEPTIONS_THROWS_OR_NOEXCEPT(
		(::fast_io::details::decay::print_semantic_input_forward_herbceptions_throws<char_type, T>),
		::fast_io::details::decay::print_semantic_input_forward_nothrow<char_type, T>)
{
	// The exact alias-and-forward transaction determines one conditional deterministic effect. A false condition remains
	// on the language-defined plain ABI; ordinary compilers retain the historical conditional-noexcept signature.
	if constexpr (::fast_io::details::decay::print_semantic_node_no_parameter_v<
						  ::std::remove_cvref_t<
							  decltype(::fast_io::io_print_alias(::std::forward<T>(t)))>>)
	{
		// Alias results that are already parameterless semantic nodes must not enter status forwarding again. They do,
		// however, need the ordinary value-or-reference transport at this normalization boundary: copying a large
		// trivially-copyable pack here materializes its complete tuple on every print call. The transport keeps small ABI
		// values by value, carries a large lvalue as `parameter<exact-reference>`, and owns an rvalue. The resulting object
		// is thereafter a named lvalue throughout strategy selection. Semantic node recognition already unwraps
		// `parameter`, so this changes no pack/condition/width meaning.
		return ::fast_io::details::io_print_forward_transport(
			::fast_io::io_print_alias(::std::forward<T>(t)));
	}
	else
	{
		// Ordinary alias results use the character-aware print forwarding path.
		return ::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(::std::forward<T>(t)));
	}
}

/// @brief Models the named expression produced after semantic input forwarding.
/// @details `print_semantic_input_forward` may return either an owned value or an exact reference. Every consumer binds
///          that result to a named local before sizing or emission, so the subsequent expression is an lvalue which
///          preserves the result's cv-qualification. This alias is the proof type for those exact calls; raw
///          `io_print_forward(io_print_alias(...))` is not equivalent when the alias result is already a semantic node.
template <::std::integral char_type, typename T>
using print_semantic_stable_input_forward_t = ::std::add_lvalue_reference_t<
	::std::remove_reference_t<decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
		::std::declval<T>()))>>;

/// @brief    Invokes a continuation with every element from a semantic pack.
/// @tparam   T            the pack-like semantic node type
/// @tparam   continuation the continuation receiving expanded elements
/// @tparam   I            the pack indices to expand
/// @param    t            the pack-like semantic node
/// @param    cont         the continuation to invoke
/// @return   decltype(auto) the continuation result
template <typename T, typename continuation, ::std::size_t... I>
inline constexpr decltype(auto) print_semantic_pack_apply_impl(T &&t, continuation &&cont,
															   ::std::index_sequence<I...>)
{
	auto &&pack_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	// `pack_ref` denotes the object owned by the normalization frame (or an exact borrowed parameter). Its members are
	// named subobjects, hence lvalues, even when a value-producing CPO created the enclosing object. Expanding through
	// an xvalue here would disagree with the structural admission traits and would accidentally require movable stored
	// leaves after ownership had already been established.
	return cont(::fast_io::containers::get<I>(pack_ref.storage)...);
}

/// @brief    Expands a semantic pack using its declared pack size.
/// @tparam   T            the pack-like semantic node type
/// @tparam   continuation the continuation receiving expanded elements
/// @param    t            the pack-like semantic node
/// @param    cont         the continuation to invoke
/// @return   decltype(auto) the continuation result
template <typename T, typename continuation>
inline constexpr decltype(auto) print_semantic_pack_apply(T &&t, continuation &&cont)
{
	using pack_type =
		::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t)))>;
	return ::fast_io::details::decay::print_semantic_pack_apply_impl(
		::std::forward<T>(t), ::std::forward<continuation>(cont), ::std::make_index_sequence<pack_type::size>{});
}

/// @brief Identifies the normalization phase at which an empty-record proof observes one semantic expression.
/// @details An already-forwarded argument and a selected condition arm may be inspected by condition selection. A raw
///          pack member has not yet crossed `io_print_alias` and `io_print_forward`; except for fast_io's exact null
///          identity and recursively nested packs, its apparent structure is therefore not execution evidence.
enum class print_semantic_empty_proof_stage : unsigned char
{
	already_forwarded,
	pack_member,
	condition_branch
};

template <print_semantic_empty_proof_stage stage, typename T>
inline constexpr bool print_semantic_argument_provably_empty_impl(T &&t);

/// @brief Proves that every raw stored member of one semantic pack normalizes to no active leaf.
/// @details Pack expansion itself is library-owned, but each non-pack member subsequently crosses provider alias/status
///          forwarding before condition selection. Members therefore enter the raw-pack-member proof stage rather than
///          inheriting the enclosing argument's stage. This boundary is the reason a provider cannot make an apparently
///          empty stored condition disappear before its forwarding replacement is observed.
template <typename T, ::std::size_t... I>
inline constexpr bool print_semantic_pack_provably_empty_impl(
	T &&t, ::std::index_sequence<I...>)
{
	auto &&pack_ref{
		::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	return (true && ... &&
			::fast_io::details::decay::print_semantic_argument_provably_empty_impl<
				print_semantic_empty_proof_stage::pack_member>(
				::fast_io::containers::get<I>(pack_ref.storage)));
}

/// @brief Conservatively proves that one semantic expression contributes no active print leaf at its execution phase.
/// @details The phase is part of the proof. Already-forwarded arguments undergo pack expansion followed by condition
///          selection, and a selected condition arm follows the branch selector's null/pack/condition order. A raw pack
///          member is different: runtime first applies `io_print_forward(io_print_alias(member))`. The provider may
///          replace an original condition or null-like object with a nonempty value, so inspecting that original node
///          would move provider effects across the mutex and could suppress output. Raw-member recursion is consequently
///          limited to nested packs, which runtime expands before forwarding, and exact `io_null_t`, whose normalization
///          identity is owned by fast_io. A wrapped null, multiply wrapped condition, width, or ordinary leaf remains
///          inconclusive. False is conservative and merely retains the normal lock-and-normalize execution path.
template <print_semantic_empty_proof_stage stage, typename T>
inline constexpr bool print_semantic_argument_provably_empty_impl(T &&t)
{
	if constexpr (::std::same_as<::std::remove_cvref_t<T>, ::fast_io::io_null_t>)
	{
		// Exact io_null_t is fast_io's library-owned identity leaf. Parameter-wrapped nulls deliberately do not match.
		return true;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_pack_argument_v<T>)
	{
		using node_type = ::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::forward<T>(t)))>;
		return ::fast_io::details::decay::print_semantic_pack_provably_empty_impl(
			::std::forward<T>(t), ::std::make_index_sequence<node_type::size>{});
	}
	else if constexpr (
		stage != print_semantic_empty_proof_stage::pack_member &&
		::fast_io::details::decay::print_semantic_top_level_condition_v<T>)
	{
		auto &&node_ref{
			::fast_io::details::decay::print_semantic_node_ref(
				::std::forward<T>(t))};
		if (node_ref.pred)
		{
			return ::fast_io::details::decay::print_semantic_argument_provably_empty_impl<
				print_semantic_empty_proof_stage::condition_branch>(
				node_ref.t1);
		}
		return ::fast_io::details::decay::print_semantic_argument_provably_empty_impl<
			print_semantic_empty_proof_stage::condition_branch>(
			node_ref.t2);
	}
	else
	{
		return false;
	}
}

/// @brief Applies the empty-record proof to one argument already owned by the normalized print-level boundary.
template <typename T>
inline constexpr bool print_semantic_argument_provably_empty(T &&t)
{
	return ::fast_io::details::decay::print_semantic_argument_provably_empty_impl<
		print_semantic_empty_proof_stage::already_forwarded>(::std::forward<T>(t));
}

/// @brief Proves that the current values in a semantic source run normalize to zero active leaves.
/// @details Every argument must be structurally empty. This fold is used only when the type graph contains a pack or
///          top-level condition, so a standalone `io_null` retains its historical explicit-argument path.
template <typename... Args>
inline constexpr bool print_semantic_run_provably_empty(Args &&...args)
{
	return (true && ... &&
			::fast_io::details::decay::print_semantic_argument_provably_empty(
				::std::forward<Args>(args)));
}

/// @brief    Prefix continuation that prepends a stored value reference to later semantic arguments.
/// @tparam   continuation the downstream continuation type
/// @tparam   T            the stored value type
template <typename continuation, typename T>
struct print_semantic_value_prefix_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::std::remove_reference_t<T> *valueptr;

	/// @brief    Invokes the downstream continuation with the stored value before the tail arguments.
	/// @tparam   TailArgs the tail argument types
	/// @param    tail_args the tail arguments
	/// @return   decltype(auto) the downstream continuation result
	template <typename... TailArgs>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr decltype(auto) operator()(TailArgs &&...tail_args) const
	{
		return (*contptr)(::std::forward<T>(*valueptr), ::std::forward<TailArgs>(tail_args)...);
	}
};

/// @brief    Prefix continuation that prepends a stored lvalue to later semantic arguments.
/// @tparam   continuation the downstream continuation type
/// @tparam   T            the stored lvalue type
template <typename continuation, typename T>
struct print_semantic_lvalue_prefix_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::std::remove_reference_t<T> *valueptr;

	/// @brief    Invokes the downstream continuation with the stored lvalue before the tail arguments.
	/// @tparam   TailArgs the tail argument types
	/// @param    tail_args the tail arguments
	/// @return   decltype(auto) the downstream continuation result
	template <typename... TailArgs>
	inline constexpr decltype(auto) operator()(TailArgs &&...tail_args) const
	{
		return (*contptr)(*valueptr, ::std::forward<TailArgs>(tail_args)...);
	}
};

/// @brief    Builds a tuple of pointers used to preserve expanded semantic pack element lifetimes.
/// @details  The helper centralizes the clang missing-braces suppression needed by fast_io's tuple aggregate form.
/// @tparam   Args the pointer argument types stored in the tuple
/// @param    args the pointers to store
/// @return   ::fast_io::containers::tuple<Args...> containing the supplied pointers
template <typename... Args>
[[nodiscard]] inline constexpr ::fast_io::containers::tuple<Args...>
print_semantic_pack_pointer_tuple(Args... args) noexcept
{
#if defined(__clang__)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wmissing-braces"
#endif
	return ::fast_io::containers::tuple<Args...>{args...};
#if defined(__clang__)
#pragma clang diagnostic pop
#endif
}

/// @brief    Completes semantic pack expansion when no input arguments remain.
/// @tparam   already_forwarded true when non-pack inputs were already print-forwarded
/// @tparam   char_type         the print character type
/// @tparam   continuation      the continuation receiving the expanded run
/// @param    cont              the continuation to invoke
/// @return   decltype(auto) the continuation result
template <bool already_forwarded, ::std::integral char_type, typename continuation>
inline constexpr decltype(auto) print_semantic_pack_expand(continuation &&cont)
{
	return cont();
}

/// @brief    Expands semantic pack arguments before invoking a continuation.
/// @tparam   already_forwarded true when non-pack inputs were already print-forwarded
/// @tparam   char_type         the print character type
/// @tparam   continuation      the continuation receiving the expanded run
/// @tparam   T                 the current argument type
/// @tparam   Args              the remaining argument types
/// @param    cont              the continuation to invoke
/// @param    t                 the current argument
/// @param    args              the remaining arguments
/// @return   decltype(auto) the continuation result
template <bool already_forwarded, ::std::integral char_type, typename continuation, typename T, typename... Args>
inline constexpr decltype(auto) print_semantic_pack_expand(continuation &&cont, T &&t, Args &&...args);

/// @brief    Reattaches already-expanded pack elements in front of the remaining tail.
/// @details  The continuation chain is strictly synchronous. `contptr` and every pointer in `expandedptrs` refer to
///           parameters or local continuations in enclosing calls whose lifetimes cover the nested invocation; none is
///           retained after operator() returns.
/// @tparam   already_forwarded true when non-pack inputs were already print-forwarded
/// @tparam   char_type         the print character type
/// @tparam   continuation      the final continuation type
/// @tparam   ExpandedPackArgs  the expanded pack element types
template <bool already_forwarded, ::std::integral char_type, typename continuation, typename... ExpandedPackArgs>
struct print_semantic_pack_expand_tail_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::fast_io::containers::tuple<::std::remove_reference_t<ExpandedPackArgs> *...> expandedptrs;

	/// @brief    Invokes the final continuation with expanded pack elements followed by tail arguments.
	/// @tparam   I        the expanded pack indices
	/// @tparam   TailArgs the tail argument types
	/// @param    <unnamed> the index sequence selecting expanded pack element pointers
	/// @param    tail_args the tail arguments
	/// @return   decltype(auto) the final continuation result
	template <::std::size_t... I, typename... TailArgs>
	inline constexpr decltype(auto) operator_impl(::std::index_sequence<I...>, TailArgs &&...tail_args) const
	{
		return (*contptr)(::std::forward<ExpandedPackArgs>(*::fast_io::containers::get<I>(expandedptrs))...,
						  ::std::forward<TailArgs>(tail_args)...);
	}

	/// @brief    Builds the index sequence used to reattach expanded pack elements.
	/// @tparam   TailArgs the tail argument types
	/// @param    tail_args the tail arguments
	/// @return   decltype(auto) the final continuation result
	template <typename... TailArgs>
	inline constexpr decltype(auto) operator()(TailArgs &&...tail_args) const
	{
		return operator_impl(::std::make_index_sequence<sizeof...(ExpandedPackArgs)>{},
							 ::std::forward<TailArgs>(tail_args)...);
	}
};

/// @brief    Continues pack expansion after one pack has been expanded in the middle of a run.
/// @tparam   already_forwarded true when non-pack inputs were already print-forwarded
/// @tparam   char_type         the print character type
/// @tparam   continuation      the final continuation type
/// @tparam   Args              the original tail argument types
template <bool already_forwarded, ::std::integral char_type, typename continuation, typename... Args>
struct print_semantic_pack_expand_middle_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::fast_io::containers::tuple<::std::remove_reference_t<Args> *...> const *argptrs;

	/// @brief    Restarts pack expansion for the saved tail arguments.
	/// @tparam   tail_continuation the continuation carrying the already-expanded pack prefix
	/// @tparam   I                 the saved tail indices
	/// @param    tail_cont         the continuation receiving the expanded tail
	/// @param    <unnamed>         the index sequence selecting saved tail pointers
	/// @return   decltype(auto) the final continuation result
	template <typename tail_continuation, ::std::size_t... I>
	inline constexpr decltype(auto) operator_impl(tail_continuation &&tail_cont, ::std::index_sequence<I...>) const
	{
		return ::fast_io::details::decay::print_semantic_pack_expand<already_forwarded, char_type>(
			::std::forward<tail_continuation>(tail_cont),
			::std::forward<Args>(*::fast_io::containers::get<I>(*argptrs))...);
	}

	/// @brief    Captures the expanded pack elements and resumes expansion for the saved tail.
	/// @tparam   ExpandedPackArgs the expanded pack element types
	/// @param    expanded_pack_args the expanded pack elements
	/// @return   decltype(auto) the final continuation result
	template <typename... ExpandedPackArgs>
	inline constexpr decltype(auto) operator()(ExpandedPackArgs &&...expanded_pack_args) const
	{
		return operator_impl(
			::fast_io::details::decay::print_semantic_pack_expand_tail_continuation<
				already_forwarded, char_type, continuation, ExpandedPackArgs...>{
				contptr,
				::fast_io::details::decay::print_semantic_pack_pointer_tuple(
					__builtin_addressof(expanded_pack_args)...)},
			::std::make_index_sequence<sizeof...(Args)>{});
	}
};

/// @brief    Entry continuation used when the current argument expands into a semantic pack.
/// @tparam   already_forwarded true when non-pack inputs were already print-forwarded
/// @tparam   char_type         the print character type
/// @tparam   continuation      the final continuation type
/// @tparam   Args              the saved tail argument types
template <bool already_forwarded, ::std::integral char_type, typename continuation, typename... Args>
struct print_semantic_pack_expand_initial_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::fast_io::containers::tuple<::std::remove_reference_t<Args> *...> argptrs;

	/// @brief    Receives the current pack's elements and resumes expansion with the saved tail.
	/// @tparam   PackArgs the expanded current-pack element types
	/// @param    pack_args the expanded current-pack elements
	/// @return   decltype(auto) the final continuation result
	template <typename... PackArgs>
	inline constexpr decltype(auto) operator()(PackArgs &&...pack_args) const
	{
		return ::fast_io::details::decay::print_semantic_pack_expand<false, char_type>(
			::fast_io::details::decay::print_semantic_pack_expand_middle_continuation<
				already_forwarded, char_type, continuation, Args...>{contptr, __builtin_addressof(argptrs)},
			::std::forward<PackArgs>(pack_args)...);
	}
};

/// @brief    Expands semantic packs and forwards ordinary semantic inputs before invoking a continuation.
/// @details  Pack arguments are replaced by their stored elements. Ordinary arguments either keep their already
///           forwarded form or are aliased and print-forwarded into a stable local value.
/// @tparam   already_forwarded true when ordinary inputs have already passed semantic forwarding
/// @tparam   char_type         the print character type
/// @tparam   continuation      the continuation receiving the expanded run
/// @tparam   T                 the current argument type
/// @tparam   Args              the remaining argument types
/// @param    cont              the continuation to invoke
/// @param    t                 the current argument
/// @param    args              the remaining arguments
/// @return   decltype(auto) the continuation result
template <bool already_forwarded, ::std::integral char_type, typename continuation, typename T, typename... Args>
inline constexpr decltype(auto) print_semantic_pack_expand(continuation &&cont, T &&t, Args &&...args)
{
	if constexpr (::fast_io::details::print_pack<T> ||
				  (::fast_io::details::decay::print_semantic_parameter_object_v<::std::remove_cvref_t<T>> &&
				   ::fast_io::details::print_pack<
					   decltype(::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t)))>))
	{
		// Pack arguments are expanded in place and then rejoined with the saved tail arguments.
		return ::fast_io::details::decay::print_semantic_pack_apply(
			::std::forward<T>(t),
			::fast_io::details::decay::print_semantic_pack_expand_initial_continuation<
				already_forwarded, char_type, continuation, Args...>{__builtin_addressof(cont),
																	 ::fast_io::details::decay::print_semantic_pack_pointer_tuple(
																		 __builtin_addressof(args)...)});
	}
	else if constexpr (already_forwarded)
	{
		// Already-forwarded ordinary arguments are captured by reference and prepended to the expanded tail.
		return ::fast_io::details::decay::print_semantic_pack_expand<already_forwarded, char_type>(
			::fast_io::details::decay::print_semantic_value_prefix_continuation<continuation, T>{
				__builtin_addressof(cont), __builtin_addressof(t)},
			::std::forward<Args>(args)...);
	}
	else
	{
		// A value result is owned by this scope, while an lvalue result remains a reference to its noncopyable proxy.
		// The synchronous continuation chain cannot outlive either storage source.
		decltype(auto) forwarded{
			::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(::std::forward<T>(t)))};
		return ::fast_io::details::decay::print_semantic_pack_expand<already_forwarded, char_type>(
			::fast_io::details::decay::print_semantic_lvalue_prefix_continuation<
				continuation, decltype(forwarded)>{__builtin_addressof(cont), __builtin_addressof(forwarded)},
			::std::forward<Args>(args)...);
	}
}

template <::std::integral char_type, typename continuation>
inline constexpr decltype(auto) print_semantic_select_conditions(continuation &&cont);

template <::std::integral char_type, typename continuation, typename T, typename... Args>
inline constexpr decltype(auto) print_semantic_select_conditions(continuation &&cont, T &&t, Args &&...args);

/// @brief    Reattaches the unprocessed argument tail after expanding an active condition-pack branch.
/// @details  Pack expansion and recursive condition selection complete before this object returns. Consequently every
///           saved pointer refers to an enclosing parameter whose lifetime covers the invocation. The selected branch
///           is forwarded exactly once, while the inactive branch contributes no leaf, capacity, or output-policy cost.
/// @tparam   char_type    the print character type
/// @tparam   continuation the final normalized-run continuation
/// @tparam   TailArgs     the already-forwarded tail argument types
template <::std::integral char_type, typename continuation, typename... TailArgs>
struct print_semantic_selected_pack_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::fast_io::containers::tuple<::std::remove_reference_t<TailArgs> *...> tailptrs;

	/// @brief Recursively selects conditions in the expanded branch and then in the saved tail.
	template <::std::size_t... I, typename... PackArgs>
	inline constexpr decltype(auto) operator_impl(::std::index_sequence<I...>, PackArgs &&...pack_args) const
	{
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			*contptr, ::std::forward<PackArgs>(pack_args)...,
			::std::forward<TailArgs>(*::fast_io::containers::get<I>(tailptrs))...);
	}

	/// @brief Reattaches the saved tail using a compile-time pointer index sequence.
	template <typename... PackArgs>
	inline constexpr decltype(auto) operator()(PackArgs &&...pack_args) const
	{
		return operator_impl(::std::make_index_sequence<sizeof...(TailArgs)>{},
							 ::std::forward<PackArgs>(pack_args)...);
	}
};

/// @brief    Normalizes one selected condition branch before strategy selection.
/// @details  Null branches disappear, pack branches are recursively expanded and ordinary branches are forwarded
///           exactly once before being reinserted into the normalized run.
template <::std::integral char_type, typename continuation, typename Branch, typename... Args>
inline constexpr decltype(auto) print_semantic_select_condition_branch(continuation &&cont, Branch &&branch,
																	   Args &&...args)
{
	if constexpr (::fast_io::details::decay::print_semantic_top_level_condition_v<Branch>)
	{
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			::std::forward<continuation>(cont), ::std::forward<Branch>(branch),
			::std::forward<Args>(args)...);
	}
	else if constexpr (::std::same_as<::std::remove_cvref_t<Branch>, ::fast_io::io_null_t>)
	{
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			::std::forward<continuation>(cont), ::std::forward<Args>(args)...);
	}
	else if constexpr (::fast_io::details::decay::print_semantic_pack_argument_v<Branch>)
	{
		return ::fast_io::details::decay::print_semantic_pack_expand<false, char_type>(
			::fast_io::details::decay::print_semantic_selected_pack_continuation<
				char_type, continuation, Args...>{
				__builtin_addressof(cont),
				::fast_io::details::decay::print_semantic_pack_pointer_tuple(__builtin_addressof(args)...)},
			::std::forward<Branch>(branch));
	}
	else
	{
		// Preserve a customization-provided lvalue proxy. A value result is still materialized as the local object whose
		// lifetime covers the complete recursive condition-selection call.
		decltype(auto) forwarded{
			::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<Branch>(branch))};
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			::fast_io::details::decay::print_semantic_lvalue_prefix_continuation<
				continuation, decltype(forwarded)>{__builtin_addressof(cont), __builtin_addressof(forwarded)},
			::std::forward<Args>(args)...);
	}
}

/// @brief    Completes condition selection for a normalized semantic run.
template <::std::integral char_type, typename continuation>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr decltype(auto) print_semantic_select_conditions(continuation &&cont)
{
	return ::std::forward<continuation>(cont)();
}

/// @brief    Selects active conditions before leaf counting, sizing and output-strategy dispatch.
/// @details  This removes inactive branch cost from the strategy model and prevents condition-pack boundaries from
///           fragmenting an otherwise coalescible run.
template <::std::integral char_type, typename continuation, typename T, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr decltype(auto) print_semantic_select_conditions(continuation &&cont, T &&t, Args &&...args)
{
	if constexpr (::fast_io::details::decay::print_semantic_top_level_condition_v<T>)
	{
		auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
		if (node_ref.pred)
		{
			return ::fast_io::details::decay::print_semantic_select_condition_branch<char_type>(
				::std::forward<continuation>(cont), node_ref.t1, ::std::forward<Args>(args)...);
		}
		return ::fast_io::details::decay::print_semantic_select_condition_branch<char_type>(
			::std::forward<continuation>(cont), node_ref.t2, ::std::forward<Args>(args)...);
	}
	else if constexpr (::std::same_as<::std::remove_cvref_t<T>, ::fast_io::io_null_t>)
	{
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			::std::forward<continuation>(cont), ::std::forward<Args>(args)...);
	}
	else
	{
		return ::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			::fast_io::details::decay::print_semantic_value_prefix_continuation<continuation, T>{
				__builtin_addressof(cont), __builtin_addressof(t)},
			::std::forward<Args>(args)...);
	}
}

/// @brief Classifies the closed fast_io leaf set admitted by the optional-scatter source plan.
/// @details The primary template fails closed. Exact specializations below contain no user-associated type argument;
///          therefore their ADL set cannot introduce a user `status_print_define` after a condition disappears. This
///          is a semantic ownership proof, not an inference from structural reserve/scatter printability.
template <typename T>
struct print_semantic_optional_scatter_closed_leaf : ::std::false_type
{};

template <::std::integral char_type>
struct print_semantic_optional_scatter_closed_leaf<
	::fast_io::basic_io_scatter_t<char_type>> : ::std::true_type
{};

template <::std::integral char_type>
struct print_semantic_optional_scatter_closed_leaf<
	::fast_io::basic_prfch_cacheable_io_scatter_t<char_type>>
	: ::std::true_type
{
	// This fast_io-owned descriptor differs from basic_io_scatter_t only by an explicit cacheable-read provenance
	// assertion. Its existing scatter, borrowed-source, output-independence, direct-equivalence, and copy-stability
	// proofs establish the identical retained character range required by the closed semantic plan. No template
	// argument can add an associated user namespace because the sole argument is the integral output character type.
};

template <::std::integral char_type, ::std::size_t extent>
struct print_semantic_optional_scatter_closed_leaf<
	::fast_io::manipulators::static_scatter_t<char_type, extent>> : ::std::true_type
{};

template <::std::integral char_type>
struct print_semantic_optional_scatter_closed_leaf<
	::fast_io::manipulators::chvw_t<char_type>> : ::std::true_type
{};

template <::std::integral char_type, typename T>
inline consteval bool print_semantic_optional_scatter_argument() noexcept;

/// @brief Proves one condition arm recursively selects the exact null identity or one retained closed scatter leaf.
/// @details A nested condition is another value-level selector, not an additional active-record leaf. Recursing through
///          it therefore preserves the original predicate order while proving the same single terminal leaf set without
///          forming its type product. A non-null terminal must expose its descriptor through the same named-lvalue
///          expression used by condition selection. Fixed scatters carry the established stable-storage contract. An
///          exact same-character `chvw_t` stores its only code unit inside the enclosing condition tree, whose semantic
///          frame outlives synchronous plan emission. Every other descriptor requires the explicit borrowed marker
///          which keeps its payload alive through later component materialization.
template <::std::integral char_type, typename T>
inline consteval bool print_semantic_optional_scatter_branch() noexcept
{
	using branch_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<branch_type, ::fast_io::io_null_t>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::details::decay::
			print_semantic_top_level_condition_v<T>)
	{
		return ::fast_io::details::decay::
			print_semantic_optional_scatter_argument<char_type, T>();
	}
	else
	{
		return ::fast_io::details::decay::
				   print_semantic_optional_scatter_closed_leaf<
					   branch_type>::value &&
			   (::std::same_as<
					branch_type,
					::fast_io::manipulators::chvw_t<char_type>> ||
				::fast_io::details::decay::print_static_scatter_traits<
					char_type, branch_type>::available ||
				::fast_io::details::decay::retained_scatter_printable_v<
					char_type, T>);
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_optional_scatter_branch_v =
	::fast_io::details::decay::
		print_semantic_optional_scatter_branch<char_type, T>();

/// @brief Detects a closed scatter selection after semantic input normalization.
/// @details The condition factory may physically reorder its arms to reduce padding and invert the predicate. Each arm
///          is therefore proved independently and may be either the exact null identity or one closed, stable scatter
///          leaf. At least one arm must be non-null. A two-scatter choice still contributes exactly the single selected
///          leaf to the active record, so it has the same descriptor and contiguous representations as an optional arm.
template <::std::integral char_type, typename T>
inline consteval bool print_semantic_optional_scatter_argument() noexcept
{
	if constexpr (!::fast_io::details::decay::print_semantic_top_level_condition_v<T>)
	{
		return false;
	}
	else
	{
		using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<T>()));
		using node_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<node_result>>;
		using first_expression = decltype((::std::declval<node_expression>().t1));
		using second_expression = decltype((::std::declval<node_expression>().t2));
		constexpr bool first_null{
			::std::same_as<::std::remove_cvref_t<first_expression>,
						   ::fast_io::io_null_t>};
		constexpr bool second_null{
			::std::same_as<::std::remove_cvref_t<second_expression>,
						   ::fast_io::io_null_t>};
		return !(first_null && second_null) &&
			   ::fast_io::details::decay::
				   print_semantic_optional_scatter_branch_v<
					   char_type, first_expression> &&
			   ::fast_io::details::decay::
				   print_semantic_optional_scatter_branch_v<
					   char_type, second_expression>;
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_optional_scatter_argument_v{
	::fast_io::details::decay::
		print_semantic_optional_scatter_argument<char_type, T>()};

/// @brief Proves one mandatory source leaf is closed and compatible with the existing run-time scatter plan.
/// @details Public parameter transports are recursively unwrapped by the plan itself. Classifying that same effective
///          type keeps ownership and CPO category identical between this proof and materialization.
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_optional_scatter_plain_argument_v =
	!::fast_io::details::decay::print_semantic_execution_node_v<T> &&
	(::fast_io::details::decay::
		 print_semantic_optional_scatter_closed_leaf<
			 ::fast_io::details::decay::
				 print_runtime_scatter_plan_unwrapped_t<T>>::value ||
	 ::fast_io::semantic_optional_scatter_status_transparent_leaf<
		 char_type,
		 ::fast_io::details::decay::
			 print_runtime_scatter_plan_unwrapped_t<T>>) &&
	::fast_io::details::decay::print_runtime_scatter_plan_component_v<
		char_type, T>;

/// @brief Borrows one normalized closed scatter condition as a dynamic descriptor component.
/// @details The enclosing semantic frame owns `source` until synchronous scatter emission completes. No branch is
///          copied or forwarded again, which is valid only because admission restricted both arms to trivial,
///          fast_io-owned scatter/null identities whose alias and print-forward operations are representation identities.
template <::std::integral char_type, typename T>
struct print_semantic_optional_scatter_component
{
	T *source;
};

/// @brief Obtains the descriptor selected by one admitted closed scatter condition.
/// @details The returned empty descriptor represents a selected exact null arm only inside the private compacting plan;
///          it is never exposed to public printable concepts or to status dispatch. The materializer omits it before
///          the destination's scatter CPO is called. Two non-null arms produce the one descriptor selected by `pred`.
template <::std::integral char_type, typename T>
[[nodiscard]] inline constexpr ::fast_io::basic_io_scatter_t<char_type>
print_semantic_optional_scatter_select(T &source)
{
	auto &&node{
		::fast_io::details::decay::print_semantic_node_ref(source)};
	auto select_branch = []<typename Branch>(Branch &branch) constexpr
		-> ::fast_io::basic_io_scatter_t<char_type> {
		using branch_type = ::std::remove_cvref_t<Branch>;
		if constexpr (
			::std::same_as<branch_type,
						   ::fast_io::io_null_t>)
		{
			return {};
		}
		else if constexpr (
			::fast_io::details::decay::
				print_semantic_top_level_condition_v<Branch &>)
		{
			// A nested selector contributes exactly its recursively selected terminal leaf. No intermediate condition
			// object escapes this call, and the enclosing source tree remains alive through synchronous materialization.
			return ::fast_io::details::decay::
				print_semantic_optional_scatter_select<char_type>(branch);
		}
		else if constexpr (
			::std::same_as<branch_type,
						   ::fast_io::manipulators::chvw_t<char_type>>)
		{
			// The enclosing condition remains a named source object until the synchronous writer returns. Borrowing its
			// one character therefore preserves the exact chvw spelling without allocating reserve storage or extending a
			// pointer beyond the member's lifetime.
			return {__builtin_addressof(branch.reference), 1u};
		}
		else if constexpr (
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, branch_type>::available)
		{
			// A fixed scatter is a descriptor already; unlike a general scatter CPO it needs no borrowed-source marker
			// because this call merely copies the descriptor from the condition-owned branch and never reinvokes a producer.
			return ::fast_io::details::decay::print_static_scatter_traits<
				char_type, branch_type>::define(branch);
		}
		else
		{
			return print_scatter_define(
				::fast_io::io_reserve_type<
					char_type, branch_type>,
				branch);
		}
	};
	if (node.pred)
	{
		return select_branch(node.t1);
	}
	return select_branch(node.t2);
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr ::std::size_t print_reserve_size(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_scatter_component<char_type, T>>,
		::fast_io::details::decay::
			print_semantic_optional_scatter_component<char_type, T>
				value)
{
	return ::fast_io::details::decay::
		print_semantic_optional_scatter_select<char_type>(*value.source)
			.len;
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr char_type *print_reserve_define(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_scatter_component<char_type, T>>,
		char_type *destination,
		::fast_io::details::decay::
			print_semantic_optional_scatter_component<char_type, T>
				value)
{
	auto const scatter{
		::fast_io::details::decay::
			print_semantic_optional_scatter_select<char_type>(*value.source)};
	if (scatter.len == 0u)
	{
		return destination;
	}
	return ::fast_io::details::decay::small_scatter_copy_n(
		scatter.base, scatter.len, destination);
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr ::fast_io::reserve_scatters_size_result
		print_reserve_scatters_size(
			::fast_io::io_reserve_type_t<
				char_type,
				::fast_io::details::decay::
					print_semantic_optional_scatter_component<char_type, T>>,
			::fast_io::details::decay::
				print_semantic_optional_scatter_component<char_type, T>) noexcept
{
	// Capacity is deliberately predicate-independent. Materialization evaluates the condition once and may consume
	// zero of this one descriptor, avoiding a separate value-dependent sizing pass.
	return {1u, 0u};
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr ::fast_io::basic_reserve_scatters_define_result<char_type>
	print_reserve_scatters_define(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_scatter_component<char_type, T>>,
		::fast_io::basic_io_scatter_t<char_type> *scatters,
		char_type *reserve,
		::fast_io::details::decay::
			print_semantic_optional_scatter_component<char_type, T>
				value)
{
	auto const scatter{
		::fast_io::details::decay::
			print_semantic_optional_scatter_select<char_type>(*value.source)};
	if (scatter.len != 0u)
	{
		*scatters++ = scatter;
	}
	return {scatters, reserve};
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr ::std::true_type print_borrowed_reserve_scatters_source(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_scatter_component<char_type, T>>) noexcept
{
	// Admission required the selected non-null descriptor to remain valid across every later component producer.
	return {};
}

template <::std::integral char_type, typename T>
[[nodiscard]] inline constexpr decltype(auto)
print_semantic_optional_scatter_adapt(T &value) noexcept
{
	if constexpr (
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>)
	{
		return ::fast_io::details::decay::
			print_semantic_optional_scatter_component<char_type, T>{
				__builtin_addressof(value)};
	}
	else
	{
		return (value);
	}
}

/// @brief Represents one closed scatter selection as a dynamic contiguous control for strategy-neutral compaction.
/// @details The view owns no payload. Its source condition remains alive for the complete synchronous dispatcher call,
///          and the selected non-null descriptor is copied into storage already owned by the ordinary control scanner.
///          Unlike the descriptor component above, this type intentionally exposes no scatter protocol: the existing
///          scanner remains responsible for choosing contiguous write versus native scatter from the complete adapted
///          segment, exactly as it does after ordinary condition selection.
template <::std::integral char_type, typename T>
struct print_semantic_optional_contiguous_component
{
	T *source;
};

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr ::std::size_t print_reserve_size(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_contiguous_component<char_type, T>>,
		::fast_io::details::decay::
			print_semantic_optional_contiguous_component<char_type, T>
				value)
{
	return ::fast_io::details::decay::
		print_semantic_optional_scatter_select<char_type>(
			   *value.source)
			.len;
}

template <::std::integral char_type, typename T>
	requires ::fast_io::details::decay::
		print_semantic_optional_scatter_argument_v<char_type, T &>
	inline constexpr char_type *print_reserve_define(
		::fast_io::io_reserve_type_t<
			char_type,
			::fast_io::details::decay::
				print_semantic_optional_contiguous_component<char_type, T>>,
		char_type *destination,
		::fast_io::details::decay::
			print_semantic_optional_contiguous_component<char_type, T>
				value)
{
	auto const scatter{
		::fast_io::details::decay::
			print_semantic_optional_scatter_select<char_type>(
				*value.source)};
	if (scatter.len == 0u)
	{
		return destination;
	}
	return ::fast_io::details::decay::small_scatter_copy_n(
		scatter.base, scatter.len, destination);
}

/// @brief Returns a finite local-buffer hint when both condition arms have a type-level maximum extent.
/// @details Fixed scatters and the same-character one-code-unit view are bounded; a general borrowed scatter is not.
///          `SIZE_MAX` is an internal unavailable sentinel and never becomes a public stack-size customization.
template <::std::integral char_type, typename Branch>
inline consteval ::std::size_t
print_semantic_optional_contiguous_branch_max_size() noexcept
{
	using branch_type = ::std::remove_cvref_t<Branch>;
	if constexpr (::std::same_as<branch_type, ::fast_io::io_null_t>)
	{
		return 0u;
	}
	else if constexpr (
		::fast_io::details::decay::
			print_semantic_top_level_condition_v<Branch &>)
	{
		using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<Branch &>()));
		using node_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<node_result>>;
		using first_expression = decltype((::std::declval<node_expression>().t1));
		using second_expression = decltype((::std::declval<node_expression>().t2));
		constexpr ::std::size_t first{
			::fast_io::details::decay::
				print_semantic_optional_contiguous_branch_max_size<
					char_type, first_expression>()};
		constexpr ::std::size_t second{
			::fast_io::details::decay::
				print_semantic_optional_contiguous_branch_max_size<
					char_type, second_expression>()};
		if constexpr (first == SIZE_MAX || second == SIZE_MAX)
		{
			return SIZE_MAX;
		}
		else
		{
			return first < second ? second : first;
		}
	}
	else if constexpr (
		::std::same_as<
			branch_type,
			::fast_io::manipulators::chvw_t<char_type>>)
	{
		return 1u;
	}
	else if constexpr (
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, branch_type>::available)
	{
		return ::fast_io::details::decay::print_static_scatter_traits<
			char_type, branch_type>::size;
	}
	else
	{
		return SIZE_MAX;
	}
}

template <::std::integral char_type, typename T>
inline consteval ::std::size_t
print_semantic_optional_contiguous_max_size() noexcept
{
	static_assert(
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>);
	using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
		::std::declval<T &>()));
	using node_expression = ::std::add_lvalue_reference_t<
		::std::remove_reference_t<node_result>>;
	using first_expression = decltype((::std::declval<node_expression>().t1));
	using second_expression = decltype((::std::declval<node_expression>().t2));
	constexpr ::std::size_t first{
		::fast_io::details::decay::
			print_semantic_optional_contiguous_branch_max_size<
				char_type, first_expression>()};
	constexpr ::std::size_t second{
		::fast_io::details::decay::
			print_semantic_optional_contiguous_branch_max_size<
				char_type, second_expression>()};
	if constexpr (first == SIZE_MAX || second == SIZE_MAX)
	{
		return SIZE_MAX;
	}
	else
	{
		return first < second ? second : first;
	}
}

template <::std::integral char_type, typename T>
	requires(
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<
				char_type, T &> &&
		::fast_io::details::decay::
				print_semantic_optional_contiguous_max_size<
					char_type, T>() != SIZE_MAX)
inline constexpr ::std::size_t print_reserve_static_stack_size(
	::fast_io::io_reserve_type_t<
		char_type,
		::fast_io::details::decay::
			print_semantic_optional_contiguous_component<
				char_type, T>>) noexcept
{
	return ::fast_io::details::decay::
		print_semantic_optional_contiguous_max_size<char_type, T>();
}

template <::std::integral char_type, typename T>
[[nodiscard]] inline constexpr decltype(auto)
print_semantic_optional_contiguous_adapt(T &value) noexcept
{
	if constexpr (
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>)
	{
		return ::fast_io::details::decay::
			print_semantic_optional_contiguous_component<char_type, T>{
				__builtin_addressof(value)};
	}
	else
	{
		return (value);
	}
}

/// @brief Stores the one fixed descriptor selected by a closed condition for whole-record materialization.
/// @details This value is private to the width/coalescing linear plan. Admission proves that every terminal arm is a
///          fast_io-owned fixed scatter, one-character view, or null identity, so selecting the descriptor is a pure
///          field read and the borrowed payload outlives the synchronous print operation. Caching it performs the same
///          one predicate selection as the ordinary active-record builder; precise sizing and final materialization do
///          not re-read the condition or introduce a second formatter invocation.
template <::std::integral char_type>
struct print_semantic_optional_coalescing_component
{
	::fast_io::basic_io_scatter_t<char_type> scatter;
};

template <::std::integral char_type>
inline constexpr ::std::size_t print_reserve_size(
	::fast_io::io_reserve_type_t<
		char_type,
		::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>>,
	::fast_io::details::decay::
		print_semantic_optional_coalescing_component<char_type>
			value) noexcept
{
	return value.scatter.len;
}

template <::std::integral char_type>
inline constexpr char_type *print_reserve_define(
	::fast_io::io_reserve_type_t<
		char_type,
		::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>>,
	char_type *destination,
	::fast_io::details::decay::
		print_semantic_optional_coalescing_component<char_type>
			value) noexcept
{
	if (value.scatter.len == 0u)
	{
		return destination;
	}
	return ::fast_io::details::decay::small_scatter_copy_n(
		value.scatter.base, value.scatter.len, destination);
}

template <::std::integral char_type>
inline constexpr ::std::size_t print_reserve_precise_size(
	::fast_io::io_reserve_type_t<
		char_type,
		::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>>,
	::fast_io::details::decay::
		print_semantic_optional_coalescing_component<char_type>
			value) noexcept
{
	return value.scatter.len;
}

template <::std::integral char_type>
inline constexpr char_type *print_reserve_precise_define(
	::fast_io::io_reserve_type_t<
		char_type,
		::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>>,
	char_type *destination, ::std::size_t precise_size,
	::fast_io::details::decay::
		print_semantic_optional_coalescing_component<char_type>
			value) noexcept
{
	// The cached descriptor is the complete selected arm, so its recorded extent is the exact size established above.
	if (precise_size == 0u)
	{
		return destination;
	}
	return ::fast_io::details::decay::small_scatter_copy_n(
		value.scatter.base, precise_size, destination);
}

/// @brief Replaces one admitted closed condition by its cached selected descriptor and leaves every other source intact.
template <::std::integral char_type, typename T>
[[nodiscard]] inline constexpr decltype(auto)
print_semantic_optional_coalescing_adapt(T &value)
{
	if constexpr (
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>)
	{
		return ::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>{
				::fast_io::details::decay::
					print_semantic_optional_scatter_select<char_type>(value)};
	}
	else
	{
		return (value);
	}
}

template <::std::integral char_type, typename T>
using print_semantic_optional_coalescing_adapted_t =
	::std::conditional_t<
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>,
		::fast_io::details::decay::
			print_semantic_optional_coalescing_component<char_type>,
		T>;

/// @brief Classifies whether one selected closed-condition arm is a scatter source in the ordinary control scanner.
/// @details Fixed immutable scatters retain scatter identity only for a destination carrying the synchronous direct-
///          scatter proof; otherwise their established path is contiguous reserve materialization. The one-character
///          view and null identity are reserve/no-output controls. The remaining admitted closed arm is a borrowed
///          run-time scatter and therefore keeps scatter identity independently of static-fragment retention.
template <typename outputstmtype, ::std::integral char_type,
		  typename Branch>
inline constexpr bool
print_semantic_optional_selected_branch_requires_scatter(
	Branch &branch) noexcept
{
	using branch_type = ::std::remove_cvref_t<Branch>;
	if constexpr (
		::std::same_as<branch_type, ::fast_io::io_null_t> ||
		::std::same_as<
			branch_type,
			::fast_io::manipulators::chvw_t<char_type>>)
	{
		return false;
	}
	else if constexpr (
		::fast_io::details::decay::
			print_semantic_top_level_condition_v<Branch &>)
	{
		auto &&node{
			::fast_io::details::decay::print_semantic_node_ref(branch)};
		if (node.pred)
		{
			return ::fast_io::details::decay::
				print_semantic_optional_selected_branch_requires_scatter<
					outputstmtype, char_type>(node.t1);
		}
		return ::fast_io::details::decay::
			print_semantic_optional_selected_branch_requires_scatter<
				outputstmtype, char_type>(node.t2);
	}
	else if constexpr (
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, branch_type>::available)
	{
		return ::fast_io::details::decay::
			print_output_retains_static_scatter<outputstmtype>;
	}
	else
	{
		static_assert(
			::fast_io::details::decay::retained_scatter_printable_v<
				char_type, Branch &>);
		return true;
	}
}

template <typename outputstmtype, ::std::integral char_type, typename T>
inline constexpr bool
print_semantic_optional_argument_requires_scatter(T &value) noexcept
{
	if constexpr (
		!::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>)
	{
		return false;
	}
	else
	{
		auto &&node{
			::fast_io::details::decay::print_semantic_node_ref(value)};
		if (node.pred)
		{
			return ::fast_io::details::decay::
				print_semantic_optional_selected_branch_requires_scatter<
					outputstmtype, char_type>(node.t1);
		}
		return ::fast_io::details::decay::
			print_semantic_optional_selected_branch_requires_scatter<
				outputstmtype, char_type>(node.t2);
	}
}

/// @brief Proves that a normalized semantic source run may bypass active-record type enumeration.
/// @details Four closed scatter selections are the first real-project scaling point; smaller records retain their established
///          branch-specialized code. Three mandatory leaves keep every active record outside empty, singleton, and
///          exact-two policies, so compacting conditions cannot change a special output boundary. The destination and
///          closed-leaf proofs jointly exclude every active-record status owner. Finally, this path is enabled only
///          when the frontend has no optimizer-constant query, or on GNU/Clang releases for which the existing
///          active-record compiler-constant policy is structurally query-free. A type-owned static provider has already
///          normalized to its retained static fragment on a no-builtin frontend, so that independent feature remains
///          intact; no accepted `__builtin_constant_p` branch is removed or converted into a run-time test.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
inline consteval bool print_semantic_optional_scatter_plan_available() noexcept
{
#if !FAST_IO_HAS_BUILTIN(__builtin_constant_p) ||                   \
	(defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	constexpr ::std::size_t selection_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::
				 print_semantic_optional_scatter_argument_v<
					 char_type, Args &>) +
		 ... + 0u)};
	constexpr ::std::size_t mandatory_count{
		sizeof...(Args) - selection_count};
	if constexpr (selection_count < 4u || mandatory_count < 3u)
	{
		// These cardinalities are necessary for the strategy. Ending the proof here prevents rejected short records from
		// forming destination and per-source protocol graphs which cannot affect the false result.
		return false;
	}
	else
	{
		// Every admitted run contains an adapted optional component, so this destination predicate plus the per-source fold
		// below is exactly the existing run-time plan's availability proof. A buffered output retains its put-area strategy;
		// a direct-write-only output must supply the same nonzero coalescing policy used by the plan's ordinary fallback.
		constexpr bool destination_plan_available{
			!::fast_io::operations::decay::defines::
				has_obuffer_basic_operations<outputstmtype> &&
			(::fast_io::details::decay::
				 print_has_direct_scatter_write_operations<outputstmtype> ||
			 (::fast_io::details::decay::
				  print_has_direct_write_operations<outputstmtype> &&
			  ::fast_io::details::decay::
					  print_scatter_fallback_full_output_threshold<
						  char_type, outputstmtype>() != 0u))};
		if constexpr (
			!::fast_io::semantic_optional_scatter_plan_stream<
				char_type, outputstmtype> ||
			!destination_plan_available)
		{
			return false;
		}
		else
		{
			return ((::fast_io::details::decay::
						 print_semantic_optional_scatter_argument_v<
							 char_type, Args &> ||
					 ::fast_io::details::decay::
						 print_semantic_optional_scatter_plain_argument_v<
							 char_type, Args &>) &&
					...);
		}
	}
#else
	(void)line;
	(void)sizeof(char_type);
	(void)sizeof(outputstmtype);
	(void)sizeof...(Args);
	return false;
#endif
}

/// @brief Emits an admitted closed-scatter source graph through the active record's ordinary control strategy.
/// @details A selected arm is first classified by the same destination-dependent scatter rule as the generic scanner.
///          If any selected arm is a scatter source, the existing descriptor plan preserves its native-scatter or
///          declared-coalescing writer. Otherwise each condition becomes a borrowed dynamic contiguous view whose size
///          is zero for the inactive arm, and the generic scanner keeps the mandatory record's existing contiguous-
///          versus-scatter decision. Thus predicate values may change the strategy exactly where ordinary active types
///          change it, without constructing those types. Lambda parameters give temporary adapters a named lifetime
///          spanning the complete synchronous scan.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
	requires(
		::fast_io::details::decay::
			print_semantic_optional_scatter_plan_available<
				line, char_type, outputstmtype, Args...>())
inline constexpr void print_semantic_optional_scatter_plan_emit(
	outputstmtype &optstm, Args &...args)
{
	bool const selected_condition_scatter{
		(false || ... ||
		 ::fast_io::details::decay::
			 print_semantic_optional_argument_requires_scatter<
				 outputstmtype, char_type>(args))};
	if (selected_condition_scatter)
	{
		// Ordinary condition selection exposes at least one active scatter-category arm, so the active generic control
		// record selects the existing descriptor plan. Retain that same plan and its destination-specific writer.
		::fast_io::details::decay::print_runtime_scatter_plan_fast_entry<
			line>(
			optstm,
			::fast_io::details::decay::
				print_semantic_optional_scatter_adapt<
					char_type>(args)...);
	}
	else
	{
		// With no selected condition scatter, dynamic contiguous views preserve omission while allowing mandatory plain
		// leaves to make exactly the same reserve-versus-scatter decision as the ordinary generic control scanner.
		[]<typename... AdaptedArgs>(
			outputstmtype &output,
			AdaptedArgs &&...adapted_args) constexpr {
			::fast_io::details::decay::print_controls_impl<
				line, outputstmtype, 0u>(output, adapted_args...);
		}(
			optstm,
			::fast_io::details::decay::
				print_semantic_optional_contiguous_adapt<
					char_type>(args)...);
	}
}

/// @brief    Checks whether one decayed print argument is accepted by the freestanding dispatcher.
/// @details  The dispatcher receives every normalized argument by value and invokes its CPO through the named local.
///           Scatter admission therefore models `remove_cvref_t<T>&`; the public forwarding-expression query would
///           reject a callable lvalue-only producer and falsely admit an rvalue-only producer that the body cannot use.
/// @tparam   char_type the output character type
/// @tparam   T         the decayed argument type
template <::std::integral char_type, typename T>
struct print_freestanding_decay_param_okay_single
	: ::std::bool_constant<
		  ::fast_io::printable<char_type, T> || ::fast_io::reserve_printable<char_type, T> ||
		  ::fast_io::dynamic_reserve_printable<char_type, T> ||
		  ::fast_io::scatter_printable_for<char_type, ::std::remove_cvref_t<T> &> ||
		  ::fast_io::reserve_scatters_printable<char_type, T> || ::fast_io::context_printable<char_type, T> ||
		  ::std::same_as<::std::remove_cvref_t<T>, ::fast_io::io_null_t> ||
		  print_semantic_params_okay<char_type, ::std::remove_cvref_t<T>>::value>
{};

/// @brief Tests the direct-print protocol at the destination reached after mutex unwrapping.
/// @details The runtime dispatcher unwraps a mutex stream before it emits individual arguments. Mirroring that
///          recursion here is necessary: probing only the locked wrapper would reject a valid customization for the
///          unlocked observer, while probing the historical dummy stream can admit an overload that is not callable
///          at the actual destination. The same-type guard prevents a malformed mutex customization from creating an
///          infinite concept recursion.
template <::std::integral char_type, typename output, typename T>
inline consteval bool print_directly_to_effective_output() noexcept
{
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<normalized_output>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<normalized_output>)
		{
			using unlocked_result = decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
				::std::declval<normalized_output &>()));
			// Runtime binds the CPO result with `decltype(auto)` and then passes that named expression onward. Removing only
			// the reference therefore models the next dispatcher template argument without inventing mutability.
			using unlocked_output = ::std::remove_reference_t<unlocked_result>;
			return ::fast_io::details::decay::print_directly_to_effective_output<
				char_type, unlocked_output, T>();
		}
		else
		{
			// A partial mutex customization is not permission to bypass synchronization and probe the locked wrapper.
			return false;
		}
	}
	return ::fast_io::details::direct_printable_to<char_type, normalized_output, T>;
}

template <typename output, ::std::integral char_type, typename T>
struct print_freestanding_output_param_okay_single;

/// @brief Proves that the normalized destination can consume an arbitrary contiguous character run.
/// @details Formatter protocols such as reserve, scatter, context, staged, and semantic padding produce characters in
///          library-owned storage before handing them to primitive output. Merely exposing `output_char_type` cannot
///          complete that transfer: the primitive write dispatcher otherwise reaches an empty terminal branch and a
///          syntactically accepted operation silently loses output. Typed completion is modeled by `writable`. A direct
///          byte completion is independently sufficient for a wider character type because `write_all_impl` converts
///          the complete `[char_type*, char_type*)` range to its exact byte extent before dispatch. This is not the
///          inverse one-byte adaptation encoded inside either public concept, so the print strategy must compose
///          `writable || bytes_writable` explicitly rather than weakening their unit-specific definitions.
template <typename output>
inline constexpr bool print_freestanding_primitive_output_okay = []() constexpr {
	// A mutex CPO may return a const observer reference. Runtime retains that cv-qualification in its named
	// `decltype(auto)` binding, so primitive capability discovery must not silently replace it with a mutable type.
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (requires { typename normalized_output::output_char_type; })
	{
		return ::std::integral<typename normalized_output::output_char_type> &&
			   (::fast_io::operations::decay::defines::writable<normalized_output> ||
				::fast_io::operations::decay::defines::bytes_writable<normalized_output>);
	}
	else
	{
		return false;
	}
}();

/// @brief Proves the one-character terminal route required by line ownership.
/// @details A specialized character-put CPO is sufficient for a newline even when bulk writes are intentionally absent;
///          otherwise the ordinary writable route supplies the one-character fallback. A put area alone is not a
///          completion proof because it may be full when the operation begins.
template <typename output>
inline constexpr bool print_freestanding_line_output_okay = []() constexpr {
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (requires { typename normalized_output::output_char_type; })
	{
		return ::fast_io::details::decay::print_freestanding_primitive_output_okay<normalized_output> ||
			   ::fast_io::operations::decay::defines::has_output_stream_char_put_overflow_define<normalized_output>;
	}
	else
	{
		return false;
	}
}();

/// @brief Converts a CPO result type to the exact expression category observed by the next semantic continuation.
/// @details Every forwarding result is first bound to a named `decltype(auto)` local. A value or rvalue-reference result
///          is consequently passed onward as a mutable lvalue, while an lvalue result retains its original cv-qualification.
template <typename T>
using print_semantic_named_result_expression_t =
	::std::add_lvalue_reference_t<::std::remove_reference_t<T>>;

template <typename output, ::std::integral char_type, typename expression>
inline consteval bool print_semantic_output_expression_okay() noexcept;

/// @brief Proves the raw-forwarding step applied to one member of an expanded semantic pack.
/// @details A member which is already a pack is recursively expanded before forwarding. Every other member uses the
///          pack expander's exact `io_print_forward(io_print_alias(member))` expression and is then observed as a named
///          local. This distinction is required because condition and width branches use `print_semantic_input_forward`.
template <typename output, ::std::integral char_type, typename expression>
inline consteval bool print_semantic_pack_member_output_okay() noexcept
{
	if constexpr (
		::fast_io::details::decay::print_semantic_pack_argument_v<expression>)
	{
		return ::fast_io::details::decay::
			print_semantic_output_expression_okay<output, char_type, expression>();
	}
	else
	{
		using forwarded_result = decltype(::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(::std::declval<expression>())));
		using forwarded_argument = ::std::remove_reference_t<forwarded_result>;
		return ::fast_io::details::decay::
			print_freestanding_output_param_okay_single<
				output, char_type, forwarded_argument>::value;
	}
}

template <typename output, ::std::integral char_type, typename node_expression,
		  ::std::size_t... I>
inline consteval bool print_semantic_pack_output_expression_okay_impl(
	::std::index_sequence<I...>) noexcept
{
	return (::fast_io::details::decay::
				print_semantic_pack_member_output_okay<
					output, char_type,
					decltype(::fast_io::containers::get<I>(
						::std::declval<node_expression>().storage))>() &&
			...);
}

/// @brief Proves one condition alternative using the branch normalization selected by runtime.
/// @details Nested conditions, exact nulls, and raw packs are handled before ordinary branch forwarding. The ordinary
///          case deliberately calls `print_semantic_input_forward`: unlike raw pack-member forwarding, an alias result
///          which is already a semantic node must not pass through status forwarding a second time.
template <typename output, ::std::integral char_type, typename expression>
inline consteval bool print_semantic_condition_branch_output_okay() noexcept
{
	if constexpr (
		::fast_io::details::decay::print_semantic_top_level_condition_v<expression> ||
		::fast_io::details::decay::print_semantic_pack_argument_v<expression>)
	{
		return ::fast_io::details::decay::
			print_semantic_output_expression_okay<output, char_type, expression>();
	}
	else if constexpr (
		::std::same_as<::std::remove_cvref_t<expression>, ::fast_io::io_null_t>)
	{
		return true;
	}
	else
	{
		using forwarded_result = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::declval<expression>()));
		using forwarded_argument = ::std::remove_reference_t<forwarded_result>;
		return ::fast_io::details::decay::
			print_freestanding_output_param_okay_single<
				output, char_type, forwarded_argument>::value;
	}
}

/// @brief Recursively validates one exact semantic-node expression against a concrete output stream.
/// @details The proof starts from the same named node reference as execution. Consequently an outer const pack or
///          condition makes its value members const lvalues, while tuple reference members retain their referent type.
///          Pack and condition traversal remain linear here: both condition arms are checked independently without
///          constructing the Cartesian product of complete active records.
template <typename output, ::std::integral char_type, typename expression>
inline consteval bool print_semantic_output_expression_okay() noexcept
{
	using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
		::std::declval<expression>()));
	using node_expression = ::std::add_lvalue_reference_t<
		::std::remove_reference_t<node_result>>;
	using node_type = ::std::remove_cvref_t<node_result>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		return ::fast_io::details::decay::
			print_semantic_pack_output_expression_okay_impl<
				output, char_type, node_expression>(
				::std::make_index_sequence<node_type::size>{});
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		using first_expression = decltype((::std::declval<node_expression>().t1));
		using second_expression = decltype((::std::declval<node_expression>().t2));
		return ::fast_io::details::decay::
				   print_semantic_condition_branch_output_okay<
					   output, char_type, first_expression>() &&
			   ::fast_io::details::decay::
				   print_semantic_condition_branch_output_okay<
					   output, char_type, second_expression>();
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		using width_traits =
			::fast_io::details::decay::print_semantic_width_traits<node_type>;
		constexpr bool fill_character_matches{[]() consteval {
			if constexpr (width_traits::has_fill_char)
			{
				return ::std::same_as<
					char_type, typename width_traits::fill_char_type>;
			}
			else
			{
				return true;
			}
		}()};
		using child_expression = decltype((::std::declval<node_expression>().reference));
		using forwarded_result = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::declval<child_expression>()));
		using forwarded_argument = ::std::remove_reference_t<forwarded_result>;
		return fill_character_matches &&
			   ::fast_io::details::decay::
				   print_freestanding_primitive_output_okay<output> &&
			   ::fast_io::details::decay::
				   print_freestanding_output_param_okay_single<
					   output, char_type, forwarded_argument>::value;
	}
	else
	{
		return false;
	}
}

/// @brief Compatibility wrapper for the exact-expression semantic output proof.
/// @details `T` is the dispatcher template argument; runtime observes it through the named lvalue expression `T&`.
template <typename output, ::std::integral char_type, typename T>
struct print_semantic_output_params_okay
	: ::std::bool_constant<
		  ::fast_io::details::decay::print_semantic_output_expression_okay<
			  output, char_type, T &>()>
{};

/// @brief Validates one normalized argument against the protocol branch that can actually emit it.
/// @details Reserve, scatter, context, and staged protocols describe only how a source materializes characters; each
///          requires the independent primitive-output proof above. Semantic packs and conditions recursively retain
///          that distinction, while width additionally requires primitive output for possible padding. Only a direct
///          stream-specific CPO may be complete without a primitive sink. The normalized dispatcher owns a by-value
///          copy and calls scatter CPOs through that named object, so the scatter alternative uses the exact mutable-
///          lvalue expression.
template <typename output, ::std::integral char_type, typename T>
inline consteval bool print_freestanding_output_param_okay_single_impl() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_execution_node_v<T>)
	{
		return ::fast_io::details::decay::
			print_semantic_output_expression_okay<output, char_type, T &>();
	}
	else if constexpr (
		::fast_io::reserve_printable<char_type, T> ||
		::fast_io::dynamic_reserve_printable<char_type, T> ||
		::fast_io::scatter_printable_for<char_type, T &> ||
		::fast_io::reserve_scatters_printable<char_type, T> ||
		::fast_io::context_printable<char_type, T>)
	{
		return ::fast_io::details::decay::print_freestanding_primitive_output_okay<output>;
	}
	else
	{
		return ::fast_io::details::decay::print_directly_to_effective_output<char_type, output, T>();
	}
}

template <typename output, ::std::integral char_type, typename T>
struct print_freestanding_output_param_okay_single
	: ::std::bool_constant<
		  ::fast_io::details::decay::print_freestanding_output_param_okay_single_impl<
			  output, char_type, T>()>
{};

/// @brief Carries one type-only active print-record prefix or unprocessed semantic suffix.
/// @details The list has no run-time representation. It exists solely to prove every record shape which pack expansion
///          and condition selection can pass to the ordinary complete-record dispatcher.
template <typename... Args>
struct print_semantic_active_record_type_list
{};

/// @brief Identifies the normalization operation which runtime applies to one pending semantic expression.
/// @details The stages are intentionally distinct. In particular, a pack produced by forwarding is not sent back into
///          the earlier global pack-expansion pass; it remains a semantic node and is emitted at its own record boundary.
enum class print_semantic_active_record_stage : unsigned char
{
	already_forwarded,
	pack_member,
	condition_branch,
	post_forward
};

template <print_semantic_active_record_stage stage_value, typename expression_type>
struct print_semantic_active_record_input
{
	inline static constexpr print_semantic_active_record_stage stage{stage_value};
	using expression = expression_type;
};

template <typename expression, typename tail_list, typename index_sequence>
struct print_semantic_active_record_pack_prepend_impl;

/// @brief Prepends the exact member expressions exposed by one named semantic pack node.
/// @details `get` is formed on the cv-qualified named node expression, not on the pack's declared element types. This
///          preserves const value members while correctly retaining the referent type of tuple reference members.
template <typename expression, typename... TailArgs, ::std::size_t... I>
struct print_semantic_active_record_pack_prepend_impl<
	expression, print_semantic_active_record_type_list<TailArgs...>,
	::std::index_sequence<I...>>
{
	using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
		::std::declval<expression>()));
	using node_expression = ::std::add_lvalue_reference_t<
		::std::remove_reference_t<node_result>>;
	using type = print_semantic_active_record_type_list<
		print_semantic_active_record_input<
			print_semantic_active_record_stage::pack_member,
			decltype(::fast_io::containers::get<I>(
				::std::declval<node_expression>().storage))>...,
		TailArgs...>;
};

template <typename expression, typename tail_list>
struct print_semantic_active_record_pack_prepend
	: print_semantic_active_record_pack_prepend_impl<
		  expression, tail_list,
		  ::std::make_index_sequence<
			  ::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(
				  ::std::declval<expression>()))>::size>>
{};

template <bool line, typename output, typename active_list, typename remaining_list>
struct print_semantic_active_record_runs_okay_impl;

template <bool line, typename output, typename active_list>
inline consteval bool print_semantic_active_flat_fallback_okay() noexcept;

/// @brief Validates one fully selected active record against the operation execution actually used for that shape.
/// @details A plain active sequence re-enters `print_freestanding_decay_no_pack`, so an exact whole-record status CPO
///          may own otherwise unprintable leaves, including the zero-leaf sequence. A remaining width node is emitted
///          by semantic node logic instead; whole-record status is not an execution branch there, so the ordinary
///          per-node and primitive-output proof remains mandatory. This distinction prevents admission from promising
///          a customization which the selected implementation would never call.
template <bool line, typename output, typename... ActiveArgs>
struct print_semantic_active_record_runs_okay_impl<
	line, output, print_semantic_active_record_type_list<ActiveArgs...>,
	print_semantic_active_record_type_list<>>
{
	using normalized_output = ::std::remove_reference_t<output>;
	using char_type = typename normalized_output::output_char_type;
	inline static constexpr bool has_remaining_semantic_node{
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_execution_node_v<ActiveArgs>)};
	inline static constexpr bool ordinary_fallback{
		(!line || ::fast_io::details::decay::
					  print_freestanding_line_output_okay<normalized_output>) &&
		(::fast_io::details::decay::print_freestanding_output_param_okay_single<
			 normalized_output, char_type,
			 ::std::remove_reference_t<ActiveArgs>>::value &&
		 ...)};
	inline static constexpr bool value{
		ordinary_fallback ||
		(!has_remaining_semantic_node &&
		 ::fast_io::operations::decay::defines::has_status_print_define<
			 line, normalized_output,
			 ::std::remove_reference_t<ActiveArgs>...>) ||
		(has_remaining_semantic_node &&
		 ::fast_io::details::decay::
			 print_semantic_active_flat_fallback_okay<
				 line, normalized_output,
				 print_semantic_active_record_type_list<ActiveArgs...>>())};
};

/// @brief Enumerates the active type sequences produced by semantic pack and condition normalization.
/// @details The stage tag mirrors the four runtime transitions: initial already-forwarded inputs, raw pack members,
///          raw selected condition arms, and named post-forwarding results. Exact null removal and condition selection
///          occur only in the post-forward stage, while raw packs expand before forwarding. Both condition alternatives
///          must satisfy the continuation because the predicate is a run-time value. The evaluator is instantiated only
///          after the ordinary linear capability proof has failed, so a normal writable stream avoids the record product.
template <bool line, typename output, typename... ActiveArgs,
		  print_semantic_active_record_stage stage, typename expression,
		  typename... TailArgs>
struct print_semantic_active_record_runs_okay_impl<
	line, output, print_semantic_active_record_type_list<ActiveArgs...>,
	print_semantic_active_record_type_list<
		print_semantic_active_record_input<stage, expression>, TailArgs...>>
{
	using normalized_output = ::std::remove_reference_t<output>;
	using char_type = typename normalized_output::output_char_type;

	inline static constexpr bool value{[]() consteval {
		using tail_list =
			print_semantic_active_record_type_list<TailArgs...>;
		if constexpr (
			stage == print_semantic_active_record_stage::already_forwarded)
		{
			if constexpr (
				::fast_io::details::decay::
					print_semantic_pack_argument_v<expression>)
			{
				using expanded_tail = typename ::fast_io::details::decay::
					print_semantic_active_record_pack_prepend<
						expression, tail_list>::type;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						expanded_tail>::value;
			}
			else
			{
				using next_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::post_forward,
						expression>,
					TailArgs...>;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						next_list>::value;
			}
		}
		else if constexpr (
			stage == print_semantic_active_record_stage::pack_member)
		{
			if constexpr (
				::fast_io::details::decay::
					print_semantic_pack_argument_v<expression>)
			{
				using expanded_tail = typename ::fast_io::details::decay::
					print_semantic_active_record_pack_prepend<
						expression, tail_list>::type;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						expanded_tail>::value;
			}
			else
			{
				using forwarded_result = decltype(::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(
						::std::declval<expression>())));
				using forwarded_expression =
					::fast_io::details::decay::
						print_semantic_named_result_expression_t<
							forwarded_result>;
				using next_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::post_forward,
						forwarded_expression>,
					TailArgs...>;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						next_list>::value;
			}
		}
		else if constexpr (
			stage == print_semantic_active_record_stage::condition_branch)
		{
			if constexpr (
				::fast_io::details::decay::
					print_semantic_top_level_condition_v<expression>)
			{
				using next_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::post_forward,
						expression>,
					TailArgs...>;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						next_list>::value;
			}
			else if constexpr (
				::std::same_as<::std::remove_cvref_t<expression>,
							   ::fast_io::io_null_t>)
			{
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						tail_list>::value;
			}
			else if constexpr (
				::fast_io::details::decay::
					print_semantic_pack_argument_v<expression>)
			{
				using expanded_tail = typename ::fast_io::details::decay::
					print_semantic_active_record_pack_prepend<
						expression, tail_list>::type;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						expanded_tail>::value;
			}
			else
			{
				using forwarded_result = decltype(::fast_io::details::decay::
													  print_semantic_input_forward<char_type>(
														  ::std::declval<expression>()));
				using forwarded_expression =
					::fast_io::details::decay::
						print_semantic_named_result_expression_t<
							forwarded_result>;
				using next_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::post_forward,
						forwarded_expression>,
					TailArgs...>;
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						next_list>::value;
			}
		}
		else
		{
			static_assert(
				stage == print_semantic_active_record_stage::post_forward);
			if constexpr (
				::std::same_as<::std::remove_cvref_t<expression>,
							   ::fast_io::io_null_t>)
			{
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<ActiveArgs...>,
						tail_list>::value;
			}
			else if constexpr (
				::fast_io::details::decay::
					print_semantic_top_level_condition_v<expression>)
			{
				using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
					::std::declval<expression>()));
				using node_expression = ::std::add_lvalue_reference_t<
					::std::remove_reference_t<node_result>>;
				using first_expression = decltype((::std::declval<node_expression>().t1));
				using second_expression = decltype((::std::declval<node_expression>().t2));
				using first_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::condition_branch,
						first_expression>,
					TailArgs...>;
				using second_list = print_semantic_active_record_type_list<
					print_semantic_active_record_input<
						print_semantic_active_record_stage::condition_branch,
						second_expression>,
					TailArgs...>;
				return ::fast_io::details::decay::
						   print_semantic_active_record_runs_okay_impl<
							   line, output,
							   print_semantic_active_record_type_list<ActiveArgs...>,
							   first_list>::value &&
					   ::fast_io::details::decay::
						   print_semantic_active_record_runs_okay_impl<
							   line, output,
							   print_semantic_active_record_type_list<ActiveArgs...>,
							   second_list>::value;
			}
			else
			{
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay_impl<
						line, output,
						print_semantic_active_record_type_list<
							ActiveArgs..., expression>,
						tail_list>::value;
			}
		}
	}()};
};

/// @brief Proves one plain prefix against the complete-record dispatcher used by semantic fallback.
/// @details The arguments are exact named expressions. Removing only references reproduces the `Args` deduced by
///          `print_freestanding_decay_no_pack(Args&...)`; an exact status CPO may own the prefix when ordinary output
///          protocols do not. An empty non-line prefix is always a valid no-op, while an empty line prefix still needs
///          either its exact status operation or the one-character output proof.
template <bool line, typename output, typename... expressions>
inline consteval bool print_semantic_active_plain_record_okay(
	print_semantic_active_record_type_list<expressions...>) noexcept
{
	using normalized_output = ::std::remove_reference_t<output>;
	using char_type = typename normalized_output::output_char_type;
	constexpr bool ordinary_fallback{
		(!line || ::fast_io::details::decay::
					  print_freestanding_line_output_okay<normalized_output>) &&
		(::fast_io::details::decay::print_freestanding_output_param_okay_single<
			 normalized_output, char_type,
			 ::std::remove_reference_t<expressions>>::value &&
		 ...)};
	return ordinary_fallback ||
		   ::fast_io::operations::decay::defines::has_status_print_define<
			   line, normalized_output,
			   ::std::remove_reference_t<expressions>...>;
}

/// @brief Proves the independent record emitted for one semantic node encountered by flat fallback.
/// @details A retained pack or condition is not merged with adjacent plain prefixes. Its raw members re-enter
///          `print_semantic_emit<..., false>` and therefore begin at the pack-member forwarding stage. This permits an
///          exact child-record status customization without incorrectly treating that customization as owner of the
///          surrounding prefix or suffix. Width remains governed by its dedicated linear output proof.
template <bool line, typename output, typename expression>
inline consteval bool print_semantic_active_node_record_okay() noexcept
{
	using normalized_output = ::std::remove_reference_t<output>;
	using char_type = typename normalized_output::output_char_type;
	using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
		::std::declval<expression>()));
	using node_expression = ::std::add_lvalue_reference_t<
		::std::remove_reference_t<node_result>>;
	using node_type = ::std::remove_cvref_t<node_result>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		using expanded = typename ::fast_io::details::decay::
			print_semantic_active_record_pack_prepend<
				expression,
				print_semantic_active_record_type_list<>>::type;
		return ::fast_io::details::decay::
			print_semantic_active_record_runs_okay_impl<
				line, normalized_output,
				print_semantic_active_record_type_list<>, expanded>::value;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		using first_expression = decltype((::std::declval<node_expression>().t1));
		using second_expression = decltype((::std::declval<node_expression>().t2));
		using first_list = print_semantic_active_record_type_list<
			print_semantic_active_record_input<
				print_semantic_active_record_stage::pack_member,
				first_expression>>;
		using second_list = print_semantic_active_record_type_list<
			print_semantic_active_record_input<
				print_semantic_active_record_stage::pack_member,
				second_expression>>;
		return ::fast_io::details::decay::
				   print_semantic_active_record_runs_okay_impl<
					   line, normalized_output,
					   print_semantic_active_record_type_list<>,
					   first_list>::value &&
			   ::fast_io::details::decay::
				   print_semantic_active_record_runs_okay_impl<
					   line, normalized_output,
					   print_semantic_active_record_type_list<>,
					   second_list>::value;
	}
	else if constexpr (
		::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		return ::fast_io::details::decay::
			print_semantic_output_expression_okay<
				normalized_output, char_type, expression>();
	}
	else
	{
		return false;
	}
}

template <bool line, typename output, typename prefix_list,
		  typename remaining_list>
struct print_semantic_active_flat_fallback_okay_impl;

template <bool line, typename output, typename... prefix_expressions>
struct print_semantic_active_flat_fallback_okay_impl<
	line, output,
	print_semantic_active_record_type_list<prefix_expressions...>,
	print_semantic_active_record_type_list<>>
	: ::std::bool_constant<
		  ::fast_io::details::decay::print_semantic_active_plain_record_okay<
			  line, output>(print_semantic_active_record_type_list<
							prefix_expressions...>{})>
{};

template <bool line, typename output, typename... prefix_expressions,
		  typename expression, typename... tail_expressions>
struct print_semantic_active_flat_fallback_okay_impl<
	line, output,
	print_semantic_active_record_type_list<prefix_expressions...>,
	print_semantic_active_record_type_list<expression, tail_expressions...>>
	: ::std::bool_constant<[]() consteval {
		if constexpr (
			::fast_io::details::decay::print_semantic_execution_node_v<expression>)
		{
			return ::fast_io::details::decay::
					   print_semantic_active_plain_record_okay<false, output>(
						   print_semantic_active_record_type_list<
							   prefix_expressions...>{}) &&
				   ::fast_io::details::decay::
					   print_semantic_active_node_record_okay<
						   false, output, expression>() &&
				   ::fast_io::details::decay::
					   print_semantic_active_flat_fallback_okay_impl<
						   line, output,
						   print_semantic_active_record_type_list<>,
						   print_semantic_active_record_type_list<
							   tail_expressions...>>::value;
		}
		else
		{
			return ::fast_io::details::decay::
				print_semantic_active_flat_fallback_okay_impl<
					line, output,
					print_semantic_active_record_type_list<
						prefix_expressions..., expression>,
					print_semantic_active_record_type_list<
						tail_expressions...>>::value;
		}
	}()>
{};

template <bool line, typename output, typename active_list>
inline consteval bool print_semantic_active_flat_fallback_okay() noexcept
{
	return ::fast_io::details::decay::
		print_semantic_active_flat_fallback_okay_impl<
			line, output, print_semantic_active_record_type_list<>,
			active_list>::value;
}

template <bool line, typename output, typename... Args>
inline constexpr bool print_semantic_active_record_runs_okay{
	::fast_io::details::decay::print_semantic_active_record_runs_okay_impl<
		line, output, print_semantic_active_record_type_list<>,
		print_semantic_active_record_type_list<
			print_semantic_active_record_input<
				print_semantic_active_record_stage::already_forwarded,
				Args &>...>>::value};

/// @brief Proves that one normalized user leaf is an explicitly marked ordinary control-dispatch barrier.
/// @details A direct-only marker or a context marker supplies the ADL/status and observational proof. Every negative
///          structural test is a consumer obligation: the ordinary control scanner must reach the same direct or
///          context branch before any alternative which could retain, size, stage, or materialize the leaf with an
///          adjacent segment. Rejecting compiler-constant materialization also proves that replacing the complete
///          active-record value query with query-free segment emission cannot remove a successful
///          `__builtin_constant_p` path.
template <typename output, ::std::integral char_type, typename T>
inline constexpr bool print_semantic_optional_scatter_barrier_argument_v =
	[]() consteval {
		if constexpr (::fast_io::details::decay::print_semantic_execution_node_v<T>)
		{
			// A semantic node is never a user control barrier. Discard its formatter graph before querying protocols
			// which cannot change this result; an ordinary Boolean initializer would still instantiate those operands.
			return false;
		}
		else
		{
			using source_type = ::std::remove_cvref_t<T>;
			using provider_type =
				::fast_io::details::decay::
					print_runtime_scatter_plan_unwrapped_t<T &>;
			constexpr bool direct_provider{
				::fast_io::semantic_optional_scatter_barrier_leaf<char_type, provider_type>};
			constexpr bool context_provider{
				::fast_io::semantic_optional_scatter_context_barrier_leaf<char_type, provider_type>};
			if constexpr (!direct_provider && !context_provider)
			{
				// Either provider promise is a necessary premise of the unchanged disjunction. Most literals and scalar
				// leaves carry neither, so do not construct their direct/context/constant protocol graphs just to reject them.
				return false;
			}
			else
			{
				// parameter<T> is fast_io's transparent public transport, not a new formatter. Only provider discovery uses the
				// referent. Every operation check below remains attached to the actual normalized source, and the enclosing plan
				// independently rejects an exact whole-record status CPO for that wrapper and all of its associated namespaces.
				constexpr bool common_negative_protocols{
					!::fast_io::scatter_printable_for<char_type, source_type &> &&
					!::fast_io::reserve_printable<char_type, source_type> &&
					!::fast_io::dynamic_reserve_printable<char_type, source_type> &&
					!::fast_io::reserve_scatters_printable<char_type, source_type> &&
					!::fast_io::staged_printable<char_type, source_type> &&
					!::fast_io::single_pass_staging_printable<char_type, source_type> &&
					!::fast_io::compiler_constant_printable<char_type, source_type>};
				if constexpr (!common_negative_protocols)
				{
					return false;
				}
				else if constexpr (direct_provider &&
								   ::fast_io::details::direct_printable_to<
									   char_type, output, source_type> &&
								   !::fast_io::context_printable<char_type, source_type>)
				{
					return true;
				}
				else if constexpr (context_provider && ::fast_io::context_printable<char_type, source_type>)
				{
					constexpr bool selected_preferred_direct{
						::fast_io::details::direct_printable_to<
							char_type, output, source_type> &&
						((::fast_io::put_area_printable_preferred<
							  char_type, source_type> &&
						  ::fast_io::operations::decay::defines::
							  has_obuffer_basic_operations<output>) ||
						 (::fast_io::buffered_printable_preferred<
							  char_type, source_type> &&
						  (::fast_io::operations::decay::defines::
							   has_obuffer_basic_operations<output> ||
						   ::fast_io::buffered_printable_preferred_stream<
							   char_type, output>)))};
					return !selected_preferred_direct &&
						   !::fast_io::details::decay::
							   print_one_pass_direct_streaming_available_v<
								   char_type, output, source_type>;
				}
				else
				{
					return false;
				}
			}
		}
	}();

/// @brief Proves that one top-level width is already a mandatory flat-fallback boundary for this destination.
/// @details A retained width suppresses whole-record status and optimizer-constant dispatch in the ordinary semantic
///          continuation. With no writable put area and both whole-output coalescing policies disabled, precise and
///          bounded aggregation have no destination. Requiring the exact forwarded width to lack a static bound also
///          makes the remaining static attempt fail without output, so ordinary execution must flush the preceding
///          flat prefix, invoke this same width node once, and resume at the following source. The output-expression
///          proof checks the named child, fill domain, and primitive destination independently. Segmenting only this
///          mechanically forced boundary therefore preserves characters, primitive calls, exceptions, width strategy,
///          source lifetime, and every independent compiler-constant path owned inside the width child.
template <typename output, ::std::integral char_type, typename T>
inline constexpr bool
	print_semantic_optional_scatter_width_barrier_argument_v =
		[]() consteval {
			if constexpr (
				!::fast_io::details::decay::print_semantic_execution_node_v<T>)
			{
				return false;
			}
			else
			{
				using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
					::std::declval<T &>()));
				using node_type = ::std::remove_cvref_t<node_result>;
				if constexpr (
					!::fast_io::details::decay::print_semantic_width_v<node_type>)
				{
					return false;
				}
				else
				{
					using forwarded_type =
						::fast_io::details::decay::
							print_semantic_forwarded_arg_t<char_type, T &>;
					return ::fast_io::details::decay::
							   print_semantic_output_expression_okay<
								   output, char_type, T &>() &&
						   !::fast_io::operations::decay::defines::
							   has_obuffer_basic_operations<output> &&
						   ::fast_io::details::decay::
								   print_full_output_coalesce_threshold<
									   char_type, output>() == 0u &&
						   ::fast_io::details::decay::
								   print_full_output_dynamic_coalesce_threshold<
									   char_type, output>() == 0u &&
						   !::fast_io::details::decay::
							   print_semantic_static_bounded_size<
								   char_type, forwarded_type>::available;
				}
			}
		}();

/// @brief Proves the narrower width shape whose complete-record coalescing can be replayed without active-type products.
/// @details The width node itself must lack a type-static bound, while its child must be an immutable fixed scatter.
///          Consequently every active record has the same strategy shape: static whole-record materialization is
///          unavailable, precise and bounded sizes are both max(runtime_width,fixed_child_size), and formatting the
///          child has no user callback or multi-pass source observation. The unbuffered requirement keeps a failed
///          whole-record attempt from changing any cursor state before the ordinary width-delimited fallback begins.
template <typename output, ::std::integral char_type, typename T>
inline constexpr bool
	print_semantic_optional_scatter_coalescing_width_argument_v =
		[]() consteval {
			if constexpr (
				!::fast_io::details::decay::print_semantic_execution_node_v<T>)
			{
				return false;
			}
			else
			{
				using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
					::std::declval<T &>()));
				using node_expression = ::std::add_lvalue_reference_t<
					::std::remove_reference_t<node_result>>;
				using node_type = ::std::remove_cvref_t<node_result>;
				if constexpr (
					!::fast_io::details::decay::print_semantic_width_v<node_type>)
				{
					return false;
				}
				else
				{
					using child_expression = decltype((::std::declval<node_expression>().reference));
					using child_type = ::std::remove_cvref_t<
						::fast_io::details::decay::
							print_semantic_stable_input_forward_t<
								char_type, child_expression>>;
					using forwarded_type =
						::fast_io::details::decay::
							print_semantic_forwarded_arg_t<char_type, T &>;
					return ::fast_io::details::decay::
							   print_semantic_output_expression_okay<
								   output, char_type, T &>() &&
						   !::fast_io::operations::decay::defines::
							   has_obuffer_basic_operations<output> &&
						   (::std::same_as<
								child_type,
								::fast_io::manipulators::chvw_t<char_type>> ||
							::fast_io::details::decay::
								print_static_scatter_traits<
									char_type, child_type>::available) &&
						   !::fast_io::details::decay::
							   print_semantic_static_bounded_size<
								   char_type, forwarded_type>::available;
				}
			}
		}();

/// @brief Accepts only a closed condition whose every terminal descriptor has a finite immutable extent.
template <::std::integral char_type, typename T>
inline consteval bool
print_semantic_optional_fixed_scatter_argument() noexcept
{
	if constexpr (
		!::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &>)
	{
		return false;
	}
	else
	{
		return ::fast_io::details::decay::
				   print_semantic_optional_contiguous_max_size<
					   char_type, T>() != SIZE_MAX;
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_optional_fixed_scatter_argument_v{
	::fast_io::details::decay::
		print_semantic_optional_fixed_scatter_argument<char_type, T>()};

/// @brief Classifies either an explicitly proved user control barrier or a mechanically forced width boundary.
template <typename output, ::std::integral char_type, typename T>
inline constexpr bool
	print_semantic_optional_scatter_boundary_argument_v =
		::fast_io::details::decay::
			print_semantic_optional_scatter_barrier_argument_v<
				output, char_type, T> ||
		::fast_io::details::decay::
			print_semantic_optional_scatter_width_barrier_argument_v<
				output, char_type, T>;

/// @brief Carries the proof result for one closed-scatter segment between proven dispatch boundaries.
/// @details A segment with four or more optional leaves must satisfy the existing linear descriptor-plan proof. A
///          smaller segment deliberately uses the exact active-record evaluator: its Cartesian bound is at most eight,
///          while retaining the established C++20 capability proof for both arms. Empty and plain-only segments are
///          likewise covered by that evaluator. `linear` records whether this mixed source graph actually removes an
///          otherwise unbounded product rather than selecting a new strategy for an already-small record.
template <bool line, ::std::integral char_type, typename output,
		  typename segment_list>
struct print_semantic_optional_scatter_barrier_segment_proof;

template <bool line, ::std::integral char_type, typename output,
		  typename... SegmentArgs>
struct print_semantic_optional_scatter_barrier_segment_proof<
	line, char_type, output,
	print_semantic_active_record_type_list<SegmentArgs...>>
{
	inline static constexpr ::std::size_t selection_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::
				 print_semantic_optional_scatter_argument_v<
					 char_type, SegmentArgs &>) +
		 ... + 0u)};
	inline static constexpr bool linear{selection_count >= 4u};
	inline static constexpr bool value{[]() consteval {
		if constexpr (linear)
		{
			return ::fast_io::details::decay::
				print_semantic_optional_scatter_plan_available<
					line, char_type, output, SegmentArgs...>();
		}
		else
		{
			// At most three optional leaves instantiate at most eight active shapes. Keeping the old evaluator here
			// makes each small suffix retain the same named-lvalue forwarding and primitive-output requirements.
			return ::fast_io::details::decay::
				print_semantic_active_record_runs_okay<
					line, output, SegmentArgs...>;
		}
	}()};
};

#if (__cplusplus > 202302L && __cpp_pack_indexing >= 202311L) || FAST_IO_HAS_BUILTIN(__type_pack_element)

/// @brief Partitions a normalized source graph once, then applies the unchanged proof to each exact segment.
/// @details Let B(i) be the existing boundary predicate and S(i) the existing segment-source predicate. The scan
///          rejects exactly when !B(i) && !S(i), and records the maximal half-open intervals between B positions.
///          Each non-final interval is proved with line=false; the final interval, including an empty suffix, owns
///          the original line flag. These are precisely the transitions of the former prefix/suffix type-list walk.
///          Only the representation of the proof changes: O(N) scalar metadata replaces O(N^2) repeated type-list
///          entries. No runtime emitter, cardinality threshold, active-record status lookup, or CPO policy changes.
template <bool line, ::std::integral char_type, typename output, typename... Args>
struct print_semantic_optional_scatter_barrier_partition
{
	template <::std::size_t index>
	using source_type =
#if __cplusplus > 202302L && __cpp_pack_indexing >= 202311L
		Args...[index];
#else
		// Some Clang releases advertise the feature macro in C++20 as an extension. Keep the source language valid
		// under -Werror=c++26-extensions by requiring both the language version and the feature macro above.
		__type_pack_element<index, Args...>;
#endif

	inline static constexpr auto partition{[]() consteval {
		struct result_type
		{
			::std::size_t begins[sizeof...(Args) + 1u]{};
			::std::size_t ends[sizeof...(Args) + 1u]{};
			::std::size_t count{1u};
			bool valid{true};
		};
		result_type result{};
		constexpr bool boundaries[]{
			::fast_io::details::decay::print_semantic_optional_scatter_boundary_argument_v<
				output, char_type, Args>...,
			false};
		constexpr bool sources[]{
			(::fast_io::details::decay::print_semantic_optional_scatter_argument_v<char_type, Args &> ||
			 ::fast_io::details::decay::print_semantic_optional_scatter_plain_argument_v<char_type, Args &>)...,
			false};
		for (::std::size_t index{}; index != sizeof...(Args); ++index)
		{
			if (boundaries[index])
			{
				result.ends[result.count - 1u] = index;
				result.begins[result.count++] = index + 1u;
			}
			else if (!sources[index])
			{
				result.valid = false;
				return result;
			}
		}
		result.ends[result.count - 1u] = sizeof...(Args);
		return result;
	}()};

	template <::std::size_t segment, ::std::size_t... offset>
	inline static consteval bool prove_segment(::std::index_sequence<offset...>) noexcept
	{
		// Index the enclosing pack directly: wrapping each lookup in tuple_element would repeat a complete pack in
		// every substituted helper specialization. No source object is formed and original cv-qualification is retained.
		constexpr bool segment_line{line && segment + 1u == partition.count};
		return ::fast_io::details::decay::print_semantic_optional_scatter_barrier_segment_proof<
			segment_line, char_type, output,
			print_semantic_active_record_type_list<source_type<partition.begins[segment] + offset>...>>::value;
	}

	template <::std::size_t... segment>
	inline static consteval bool prove_segments(::std::index_sequence<segment...>) noexcept
	{
		return (prove_segment<segment>(::std::make_index_sequence<
									   partition.ends[segment] - partition.begins[segment]>{}) &&
				...);
	}

	inline static constexpr bool value{[]() consteval {
		if constexpr (!partition.valid)
		{
			return false;
		}
		else
		{
			return prove_segments(::std::make_index_sequence<partition.count>{});
		}
	}()};
};

/// @brief An empty source graph has exactly one final segment and no indexable source type.
/// @details Reuse the original terminal proof directly. In particular, the empty-line case must not be replaced by
///          unconditional success, and no dependent pack-index alias is declared for a pack with no valid index.
template <bool line, ::std::integral char_type, typename output>
struct print_semantic_optional_scatter_barrier_partition<line, char_type, output>
	: print_semantic_optional_scatter_barrier_segment_proof<
		  line, char_type, output, print_semantic_active_record_type_list<>>
{};

#else

// Without language or compiler pack indexing, an overload-table lookup still considers O(N) candidates per source.
// GCC 11 measurements rejected that emulation (higher time and memory than the original walk). Retain the original
// proof representation on such frontends; this feature gate changes compiler work only, never runtime admission.
/// @brief Scans one normalized source graph and proves every segment and barrier without forming their global product.
template <bool line, ::std::integral char_type, typename output,
		  typename segment_list, typename remaining_list>
struct print_semantic_optional_scatter_barrier_scan;

template <bool line, ::std::integral char_type, typename output,
		  typename... SegmentArgs>
struct print_semantic_optional_scatter_barrier_scan<
	line, char_type, output,
	print_semantic_active_record_type_list<SegmentArgs...>,
	print_semantic_active_record_type_list<>>
{
	using segment_proof =
		::fast_io::details::decay::
			print_semantic_optional_scatter_barrier_segment_proof<
				line, char_type, output,
				print_semantic_active_record_type_list<SegmentArgs...>>;
	inline static constexpr bool value{segment_proof::value};
};

template <bool line, ::std::integral char_type, typename output,
		  typename... SegmentArgs, typename T, typename... Args>
struct print_semantic_optional_scatter_barrier_scan<
	line, char_type, output,
	print_semantic_active_record_type_list<SegmentArgs...>,
	print_semantic_active_record_type_list<T, Args...>>
{
	inline static constexpr bool current_is_barrier{
		::fast_io::details::decay::
			print_semantic_optional_scatter_boundary_argument_v<
				output, char_type, T>};
	inline static constexpr bool current_is_segment_source{
		::fast_io::details::decay::
			print_semantic_optional_scatter_argument_v<char_type, T &> ||
		::fast_io::details::decay::
			print_semantic_optional_scatter_plain_argument_v<
				char_type, T &>};

	using result = decltype([]() consteval {
		if constexpr (current_is_barrier)
		{
			using prefix_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_barrier_segment_proof<
						false, char_type, output,
						print_semantic_active_record_type_list<
							SegmentArgs...>>;
			using tail_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_barrier_scan<
						line, char_type, output,
						print_semantic_active_record_type_list<>,
						print_semantic_active_record_type_list<Args...>>;
			return ::std::type_identity<::std::bool_constant<
				prefix_proof::value && tail_proof::value>>{};
		}
		else if constexpr (current_is_segment_source)
		{
			using tail_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_barrier_scan<
						line, char_type, output,
						print_semantic_active_record_type_list<
							SegmentArgs..., T>,
						print_semantic_active_record_type_list<Args...>>;
			return ::std::type_identity<::std::bool_constant<
				tail_proof::value>>{};
		}
		else
		{
			return ::std::type_identity<::std::false_type>{};
		}
	}());

	inline static constexpr bool value{typename result::type{}};
};

template <bool line, ::std::integral char_type, typename output, typename... Args>
struct print_semantic_optional_scatter_barrier_partition
	: print_semantic_optional_scatter_barrier_scan<
		line, char_type, output, print_semantic_active_record_type_list<>,
		print_semantic_active_record_type_list<Args...>>
{};

#endif

/// @brief Proves that a normalized source graph may be split only at independently proved dispatch boundaries.
/// @details The global source record must not own an exact status CPO. User barriers and the destination supply paired
///          ADL proofs; width boundaries instead satisfy the complete mechanical flat-fallback proof above. Four total
///          conditions are the first global Cartesian scaling point even when barriers divide them into smaller local
///          segments: every segment of four or more uses the linear plan, while each smaller segment retains its bounded
///          active-shape verification. The compiler gate is identical to the optional-scatter plan: no accepted
///          optimizer-constant record is converted into a run-time query. A user barrier is structurally ineligible for
///          constant materialization, while a retained width already suppresses that complete-record query in ordinary
///          dispatch.
template <bool line, ::std::integral char_type, typename output,
		  typename... Args>
inline consteval bool
print_semantic_optional_scatter_barrier_plan_available() noexcept
{
#if !FAST_IO_HAS_BUILTIN(__builtin_constant_p) ||                   \
	(defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	constexpr ::std::size_t barrier_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::
				 print_semantic_optional_scatter_boundary_argument_v<
					 output, char_type, Args>) +
		 ... + 0u)};
	constexpr ::std::size_t selection_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::
				 print_semantic_optional_scatter_argument_v<
					 char_type, Args &>) +
		 ... + 0u)};
	if constexpr (barrier_count == 0u || selection_count < 4u)
	{
		// A barrier and four optional selections are independent necessary conditions. A rejected source graph must not
		// instantiate the recursive segment proof or destination/status protocols merely to rediscover this false result.
		return false;
	}
	else
	{
		using scan =
			::fast_io::details::decay::
				print_semantic_optional_scatter_barrier_partition<
					line, char_type, output, Args...>;
		if constexpr (!scan::value)
		{
			return false;
		}
		else
		{
			return ::fast_io::semantic_optional_scatter_barrier_plan_stream<
					   char_type, output> &&
				   !::fast_io::operations::decay::defines::
					   has_status_print_define<line, output, Args...>;
		}
	}
#else
	(void)line;
	(void)sizeof(char_type);
	(void)sizeof(output);
	(void)sizeof...(Args);
	return false;
#endif
}

template <bool value_result, bool has_linear_result>
struct print_semantic_optional_scatter_width_scan_result
{
	inline static constexpr bool value{value_result};
	inline static constexpr bool has_linear_segment{has_linear_result};
};

/// @brief Scans segments separated only by the strict fixed-child width shape used by whole-record coalescing.
/// @details Every segment reuses the existing closed-scatter proof. The scan is linear in source arity and never forms
///          an active-record type product; `has_linear_segment` ensures this machinery is selected only when it removes
///          at least one four-condition Cartesian expansion. Validity and profitability share one result type, so each
///          recursive source suffix is analyzed once while retaining both independent proof bits.
template <bool line, ::std::integral char_type, typename output,
		  typename segment_list, typename remaining_list>
struct print_semantic_optional_scatter_width_coalescing_scan;

template <bool line, ::std::integral char_type, typename output,
		  typename... SegmentArgs>
struct print_semantic_optional_scatter_width_coalescing_scan<
	line, char_type, output,
	print_semantic_active_record_type_list<SegmentArgs...>,
	print_semantic_active_record_type_list<>>
{
	using segment_proof =
		::fast_io::details::decay::
			print_semantic_optional_scatter_barrier_segment_proof<
				line, char_type, output,
				print_semantic_active_record_type_list<SegmentArgs...>>;
	using result = print_semantic_optional_scatter_width_scan_result<
		segment_proof::value, segment_proof::linear>;
	inline static constexpr bool value{result::value};
	inline static constexpr bool has_linear_segment{
		result::has_linear_segment};
};

template <bool line, ::std::integral char_type, typename output,
		  typename... SegmentArgs, typename T, typename... Args>
struct print_semantic_optional_scatter_width_coalescing_scan<
	line, char_type, output,
	print_semantic_active_record_type_list<SegmentArgs...>,
	print_semantic_active_record_type_list<T, Args...>>
{
	inline static constexpr bool current_is_width{
		::fast_io::details::decay::
			print_semantic_optional_scatter_coalescing_width_argument_v<
				output, char_type, T>};
	inline static constexpr bool current_is_segment_source{
		::fast_io::details::decay::
			print_semantic_optional_fixed_scatter_argument_v<
				char_type, T> ||
		::fast_io::details::decay::
			print_semantic_optional_scatter_plain_argument_v<
				char_type, T &>};

	using result = typename decltype([]() consteval {
		if constexpr (current_is_width)
		{
			using prefix_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_barrier_segment_proof<
						false, char_type, output,
						print_semantic_active_record_type_list<
							SegmentArgs...>>;
			using tail_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_width_coalescing_scan<
						line, char_type, output,
						print_semantic_active_record_type_list<>,
						print_semantic_active_record_type_list<Args...>>;
			return ::std::type_identity<
				print_semantic_optional_scatter_width_scan_result<
					prefix_proof::value && tail_proof::value,
					prefix_proof::linear ||
						tail_proof::has_linear_segment>>{};
		}
		else if constexpr (current_is_segment_source)
		{
			using tail_proof =
				::fast_io::details::decay::
					print_semantic_optional_scatter_width_coalescing_scan<
						line, char_type, output,
						print_semantic_active_record_type_list<
							SegmentArgs..., T>,
						print_semantic_active_record_type_list<Args...>>;
			return ::std::type_identity<
				print_semantic_optional_scatter_width_scan_result<
					tail_proof::value,
					tail_proof::has_linear_segment>>{};
		}
		else
		{
			return ::std::type_identity<
				print_semantic_optional_scatter_width_scan_result<
					false, false>>{};
		}
	}())::type;

	inline static constexpr bool value{result::value};
	inline static constexpr bool has_linear_segment{
		result::has_linear_segment};
};

/// @brief Validates one complete, already-forwarded print run against the branch selected for its destination.
/// @details Status printing owns the entire argument pack, so validating every argument first rejects legitimate
///          stream-specific records that intentionally have no standalone formatter. Conversely, an outer status CPO
///          on a mutex wrapper is not callable by the dispatcher: synchronization is selected first and capability
///          discovery resumes on the unlocked reference. This recursive proof mirrors that exact ordering. A partial
///          mutex marker is rejected rather than treated as permission to bypass locking.
/// @tparam line   true when the operation owns a trailing newline
/// @tparam output the decayed output reference selected by `output_stream_ref`
/// @tparam Args   the by-value argument types received by the decayed dispatcher
template <bool line, typename output, typename... Args>
inline consteval bool print_freestanding_output_run_okay() noexcept
{
	// The public entry supplies a mutable owned observer, while mutex recursion may supply a cv-qualified borrowed
	// observer. Removing only references models the exact `outputstmtype` deduced by the runtime `T&` dispatcher.
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (!requires { typename normalized_output::output_char_type; })
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<normalized_output>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<normalized_output>)
		{
			using unlocked_result = decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
				::std::declval<normalized_output &>()));
			using unlocked_output = ::std::remove_reference_t<unlocked_result>;
			return ::fast_io::details::decay::print_freestanding_output_run_okay<
				line, unlocked_output, ::std::remove_reference_t<Args>...>();
		}
		else
		{
			return false;
		}
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_status_print_define<
			line, normalized_output, ::std::remove_reference_t<Args>...>)
	{
		// The status CPO has the same normalized destination, line ownership, and argument pack as the call site.
		return true;
	}
	else
	{
		using char_type = typename normalized_output::output_char_type;
		constexpr bool ordinary_fallback{
			(!line || ::fast_io::details::decay::
						  print_freestanding_line_output_okay<normalized_output>) &&
			(::fast_io::details::decay::print_freestanding_output_param_okay_single<
				 normalized_output, char_type,
				 ::std::remove_reference_t<Args>>::value &&
			 ...)};
		if constexpr (ordinary_fallback)
		{
			return true;
		}
		else if constexpr (
			(false || ... ||
			 ::fast_io::details::decay::print_semantic_execution_node_v<Args>))
		{
			if constexpr (
				::fast_io::details::decay::
					print_semantic_optional_scatter_plan_available<
						line, char_type, normalized_output,
						::std::remove_reference_t<Args>...>())
			{
				// The closed-scatter provider proof is sufficient for every active record: each condition contributes exactly
				// the selected null identity or retained fast_io-owned scatter leaf, every mandatory
				// leaf independently satisfies the ordinary component protocol, and the effective destination proves that
				// no record assembled from this closed set can acquire a whole-record status customization. Consequently
				// enumerating the Cartesian product of condition arms cannot reject an operation already proved here; it can
				// only repeat the same leaf and destination obligations once per active type sequence.
				return true;
			}
			else if constexpr (
				::fast_io::operations::decay::
					print_semantic_optional_scatter_width_coalescing_plan_available<
						line, char_type, normalized_output,
						::std::remove_reference_t<Args>...>())
			{
				// Every active record has at least nine mandatory leaves, one retained unbounded width, and only fixed
				// closed-condition descriptors. The later proof replays the identical whole-record strategy before it
				// permits width-delimited fallback, so enumerating active types supplies no additional operation evidence.
				return true;
			}
			else if constexpr (
				::fast_io::details::decay::
					print_semantic_optional_scatter_barrier_plan_available<
						line, char_type, normalized_output,
						::std::remove_reference_t<Args>...>())
			{
				// Each marked direct-only leaf is the same hard boundary already reached by the generic control scanner.
				// The paired destination/leaf proofs exclude every associated whole-record status owner, each large segment
				// reuses the closed scatter-selection proof, and every smaller segment retains a bounded active-record check.
				// The global Cartesian product therefore adds no capability evidence beyond those independent segments.
				return true;
			}
			else
			{
#if defined(FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC)
				// Diagnostic-only fail-fast gate for real-project shape discovery. Admission reaches this point immediately
				// before the active-record Cartesian product. Keeping the probe here, in addition to the execution-side
				// probe, makes an unclassified source record report its complete call-site types without first exhausting
				// front-end memory. The block is absent from normal translation units and cannot affect concepts or codegen.
				constexpr ::std::size_t diagnostic_top_level_condition_count{
					(static_cast<::std::size_t>(
						 ::fast_io::details::decay::
							 print_semantic_top_level_condition_v<Args>) +
					 ... + 0u)};
				static_assert(
					diagnostic_top_level_condition_count <
						static_cast<::std::size_t>(
							FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC),
					"unclassified large semantic condition record during admission");
#endif
				// The source graph itself is not the complete operation pack. Expand
				// every type-only pack/condition shape and prove the exact record which
				// the semantic continuation can hand to ordinary status dispatch.
				return ::fast_io::details::decay::
					print_semantic_active_record_runs_okay<
						line, normalized_output,
						::std::remove_reference_t<Args>...>;
			}
		}
		else
		{
			return false;
		}
	}
}

/// @brief Detects the exact whole-record status owner reached after every mandatory mutex unwrap.
/// @details The run-time dispatcher never calls a status CPO attached only to a locking wrapper. This proof therefore
///          follows each complete mutex protocol to the effective output and tests the original normalized argument
///          pack there. It is used before collapsing a structurally empty semantic graph: an exact provider operation
///          for that source graph must retain precedence over the derived zero-argument record.
template <bool line, typename output, typename... Args>
inline consteval bool print_freestanding_effective_status_owner() noexcept
{
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (!requires { typename normalized_output::output_char_type; })
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<normalized_output>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::
				has_complete_output_stream_mutex_protocol<normalized_output>)
		{
			using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
				::std::declval<normalized_output &>()))>;
			return ::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					line, unlocked_output, Args...>();
		}
		else
		{
			return false;
		}
	}
	else
	{
		return ::fast_io::operations::decay::defines::
			has_status_print_define<line, normalized_output, Args...>;
	}
}

/// @brief Determines whether an effective destination explicitly observes an empty non-line print record.
/// @details The default is false. A provider opts in with the already existing exact zero-argument
///          `status_print_define<false>(output)` operation. Mutex wrappers do not become observable merely because
///          locking has side effects: capability discovery follows the complete protocol to the effective unlocked
///          output without evaluating it, and a run-time lock is acquired only when that output owns the completion
///          operation. A partial mutex protocol cannot prove an observable operation and remains false here; ordinary
///          print admission diagnoses it independently.
template <typename output>
inline consteval bool print_freestanding_empty_run_observable() noexcept
{
	using normalized_output = ::std::remove_reference_t<output>;
	if constexpr (!requires { typename normalized_output::output_char_type; })
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<normalized_output>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<normalized_output>)
		{
			using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
				::std::declval<normalized_output &>()))>;
			return ::fast_io::details::decay::
				print_freestanding_empty_run_observable<unlocked_output>();
		}
		else
		{
			return false;
		}
	}
	else
	{
		return ::fast_io::operations::decay::defines::
			has_status_print_define<false, normalized_output>;
	}
}

} // namespace details::decay

namespace operations::decay::defines
{

/// @brief Recognizes a normalized destination whose effective output explicitly observes an empty print record.
/// @details The concept is false by default. A provider opts in by defining the exact zero-argument completion
///          operation `status_print_define<false>(effective_output) -> void`; a separate Boolean marker cannot claim
///          observability without supplying the operation which consumes the record. The proof recursively follows a
///          complete mutex protocol to the effective unlocked output, matching ordinary print capability discovery.
///
///          "Empty record" means either zero source arguments or zero active arguments after fast_io-owned pack
///          expansion and condition selection. It does not mean one active argument whose representation has zero
///          characters: an empty string/static fragment and a standalone `io_null_t` retain their explicit-argument
///          operation path. A structurally selected `io_null_t` below a pack/condition may participate in a proven
///          zero-active record. Line mode is never governed by this concept because its owned newline is observable.
template <typename output>
concept empty_print_observable =
	::fast_io::details::decay::print_freestanding_empty_run_observable<output>();

} // namespace operations::decay::defines

namespace operations
{

namespace decay
{

/// @brief Completes one empty non-line print record at the normalized print-level boundary.
/// @details A destination without `empty_print_observable` is ignored before locking or primitive output. If the
///          effective unlocked destination supplies the zero-argument status operation, every complete outer mutex is
///          acquired in normal outside-in order and the operation is invoked exactly once. This helper must be used by
///          every print-level ownership or optimization entry which can receive an empty source pack; a public facade
///          must not suppress output-reference normalization on its behalf. Byte count alone is not evidence for this
///          entry: any still-active leaf, including a zero-character literal or standalone null object, remains an
///          ordinary explicit record unless semantic structure has selected it away.
template <typename outputstmtype>
inline constexpr void print_freestanding_empty_run(outputstmtype &optstm)
{
	if constexpr (
		::fast_io::operations::decay::defines::
			empty_print_observable<outputstmtype>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::
				has_output_or_io_stream_mutex_ref_define<outputstmtype>)
		{
			static_assert(
				::fast_io::operations::decay::defines::
					has_complete_output_stream_mutex_protocol<outputstmtype>);
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked_output =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::print_freestanding_empty_run(
				unlocked_output);
		}
		else
		{
			status_print_define<false>(optstm);
		}
	}
}

/// @brief    Freestanding print dispatcher for runs that do not require semantic pack expansion.
/// @details  The dispatcher handles status-print customizations, empty line output, mutex-wrapped streams, buffered
///           streams, and the generic control emitter. Its arguments borrow the single normalized owner established by
///           `print_freestanding_decay`; mutex recursion and strategy helpers must not reopen a copy boundary.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the print run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   decltype(auto) the selected output path's return value, when any
template <bool line, typename outputstmtype, typename... Args>
	requires(::fast_io::details::decay::print_semantic_flat_record_v<Args...>)
inline constexpr decltype(auto) print_freestanding_decay_no_pack(outputstmtype &optstm, Args &...args)
{
	if constexpr (!line && sizeof...(Args) == 0u)
	{
		// Empty-record observability belongs to the print level, before any
		// destination strategy can turn a no-op into a lock or primitive call.
		return ::fast_io::operations::decay::print_freestanding_empty_run(optstm);
	}
	else if constexpr (::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<outputstmtype>)
		{
			// Synchronization is an outer-stream invariant, not an optional fallback. Testing a status CPO before this
			// branch lets a wrapper that happens to advertise both protocols bypass its mutex entirely. Lock once and
			// repeat capability selection on the unlocked reference so status, buffering, and primitive output all
			// observe the same protected resource.
			::fast_io::operations::decay::stream_ref_decay_lock_guard lg{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked_output =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			return ::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
				unlocked_output, args...);
		}
		else
		{
			static_assert(
				::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<outputstmtype>,
				"fast_io: an output mutex CPO requires a storable lock/unlock proxy and a type-progressing, "
				"character-preserving unlocked output reference");
		}
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_status_print_define<line, outputstmtype, Args...>)
	{
		// Status-print streams handle the complete run through their stream-specific customization.
		return status_print_define<line>(optstm, args...);
	}
	else if constexpr (sizeof...(Args) == 0)
	{
		// The earlier print-level branch owns a non-line empty record. Therefore
		// reaching this branch proves line mode and emits its one required newline.
		static_assert(line);
		using char_type = typename outputstmtype::output_char_type;
		return ::fast_io::operations::decay::char_put_decay_dispatch(
			optstm, char_literal_v<u8'\n', char_type>);
	}
	else if constexpr (
		::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
			typename outputstmtype::output_char_type, outputstmtype, Args &...>)
	{
		// Normalized unbuffered runs containing a run-time scatter component bypass the compile-time descriptor
		// scanner. This keeps its constexpr capacities valid while allowing this separate run-time plan to compose.
		return ::fast_io::details::decay::print_runtime_scatter_plan_fast_entry<line>(optstm, args...);
	}
	else if constexpr (
		::fast_io::details::decay::print_buffered_hot_decimal_scalar_run_available<
			outputstmtype, typename outputstmtype::output_char_type, Args...>())
	{
		// Keep the measured basic-buffer scalar shape out of the large recursive
		// buffered dispatcher.  The dedicated leaf has the same capacity/flush
		// proof, but its force-inline boundary lets the caller's scalar loop retain
		// the formatter body instead of paying one call per record.
		return ::fast_io::details::decay::print_control_single_buffered_hot<line>(optstm, args...);
	}
	else if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
	{
		// Buffered streams use the put-area-aware control dispatcher.
		return ::fast_io::details::decay::print_controls_buffer_impl<line>(optstm, args...);
	}
	else
	{
		// Streams without a put area use the generic control dispatcher. The explicit stream type and initial zero skip
		// count avoid GCC 11's failure to apply the intervening default non-type argument before deducing `T, Args...`.
		return ::fast_io::details::decay::print_controls_impl<line, outputstmtype, 0u>(
			optstm, args...);
	}
}

/// @brief Emits one already-normalized plain run through a shared bounded staging window.
/// @details This is the run counterpart of `print_single_pass_stage_and_write`.  It is used only when the destination
///          authorizes a finite whole-output stack budget and at least one leaf explicitly promises endpoint-independent
///          one-pass emission.  The ordinary no-pack dispatcher writes every sibling into the same put area, so static
///          punctuation and a recursive producer share one final write when the record fits.  Overflow never replays a
///          source: each full prefix is committed and traversal resumes in the reset window.  Therefore after producing
///          `n` characters the emitted prefix plus `[begin,current)` is exactly the first `n` characters of the ordinary
///          ordered run, which proves byte and side-effect order by induction over dispatcher operations.
template <bool line, ::std::integral char_type, ::std::size_t capacity,
		  typename output_type, typename... Args>
	requires(capacity != 0u)
inline constexpr void print_single_pass_stage_run_and_write(
	output_type &output, Args &...args)
{
	char_type buffer[capacity];
	::fast_io::details::decay::print_single_pass_staging_state<
		char_type, output_type>
		state{__builtin_addressof(output), buffer, buffer,
			  buffer + capacity, false};
	::fast_io::details::decay::print_single_pass_staging_ref<
		char_type, output_type>
		staging{__builtin_addressof(state)};
#if (defined(_MSC_VER) && defined(_HAS_EXCEPTIONS) && _HAS_EXCEPTIONS != 0) || \
	(!defined(_MSC_VER) && defined(__cpp_exceptions))
	try
	{
#endif
		::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
			staging, args...);
		::fast_io::details::decay::print_single_pass_staging_flush(state);
#if (defined(_MSC_VER) && defined(_HAS_EXCEPTIONS) && _HAS_EXCEPTIONS != 0) || \
	(!defined(_MSC_VER) && defined(__cpp_exceptions))
	}
	catch (...)
	{
		if (!state.external_write_active)
		{
			try
			{
				::fast_io::details::decay::print_single_pass_staging_flush(state);
			}
			catch (...)
			{
				// Preserve the producer exception; cleanup cannot safely replace it.
			}
		}
		throw;
	}
#endif
}

/// @brief    Forward declaration for semantic-aware freestanding emission.
/// @tparam   line              true when a trailing newline is appended
/// @tparam   already_forwarded true when inputs have already passed semantic forwarding
/// @tparam   char_type         the output character type
/// @tparam   outputstmtype     the decayed output stream reference type
/// @tparam   Args              the argument types in the print run
/// @param    optstm            the output stream reference
/// @param    args              the arguments to emit
template <bool line, bool already_forwarded, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr void print_semantic_emit(outputstmtype &optstm, Args &&...args);

/// @brief Writes a large contiguous fill in reusable blocks outside the caller's hot stack frame.
/// @details The 4-KiB block amortizes write-call overhead for large padding, but embedding it in every width emitter
///          forces a stack-page probe even for a field containing only a few characters. The noinline boundary is the
///          evidence-backed separation between the small-field latency policy and the large-field throughput policy.
///          Each loop iteration removes exactly `buffer_size` characters while `n > buffer_size`; consequently the
///          final write is non-empty and no larger than the initialized block.
template <::std::integral char_type, typename outputstmtype>
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#elif __has_cpp_attribute(msvc::noinline)
[[msvc::noinline]]
#endif
inline constexpr void print_semantic_write_fill_large_contiguous(outputstmtype &optstm, ::std::size_t n, char_type ch)
{
	constexpr ::std::size_t desired_buffer_size{
		(4096u / sizeof(char_type)) == 0u ? 1u : (4096u / sizeof(char_type))};
	constexpr ::std::size_t stack_buffer_limit{
		::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
	if constexpr (stack_buffer_limit == 0u)
	{
		// A zero stack budget is an explicit prohibition, not a request for the default 4-KiB frame. Keep the same
		// bounded-block algorithm in dynamic storage so the policy affects storage placement without multiplying write
		// calls. The allocation is isolated in this already-cold helper and is independent of payload size.
		::fast_io::details::local_operator_new_array_ptr<char_type> buffer(desired_buffer_size);
		::fast_io::details::my_fill_n(buffer.ptr, desired_buffer_size, ch);
		while (desired_buffer_size < n)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(
				optstm, buffer.ptr, buffer.ptr + desired_buffer_size);
			n -= desired_buffer_size;
		}
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer.ptr, buffer.ptr + n);
	}
	else
	{
		constexpr ::std::size_t buffer_size{
			stack_buffer_limit < desired_buffer_size ? stack_buffer_limit : desired_buffer_size};
		char_type buffer[buffer_size];
		::fast_io::details::my_fill_n(buffer, buffer_size, ch);
		while (buffer_size < n)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, buffer + buffer_size);
			n -= buffer_size;
		}
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, buffer + n);
	}
}

/// @brief    Writes a repeated fill character using the stream's contiguous/scatter cost policy.
/// @tparam   char_type     the output character type
/// @tparam   outputstmtype the decayed output stream reference type
/// @param    optstm        the output stream reference
/// @param    n             the number of fill characters to emit
/// @param    ch            the fill character
template <::std::integral char_type, typename outputstmtype>
inline constexpr void print_semantic_write_fill(outputstmtype &optstm, ::std::size_t n, char_type ch)
{
	if (n == 0)
	{
		// Empty padding emits no output.
		return;
	}
	constexpr ::std::size_t requested_inline_bytes{1024u};
	constexpr ::std::size_t requested_inline_capacity{
		requested_inline_bytes / sizeof(char_type)};
	constexpr ::std::size_t inline_stack_limit{
		::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
	constexpr ::std::size_t inline_capacity{
		inline_stack_limit < requested_inline_capacity ? inline_stack_limit : requested_inline_capacity};
	if constexpr (inline_capacity != 0u)
	{
		if (n <= inline_capacity)
		{
			// Small padding uses only the bounded latency buffer. Besides avoiding unnecessary stores, keeping this
			// branch before the scatter policy prevents a 256 -> 257 transition from instantiating a 4-KiB hot frame.
			char_type buffer[inline_capacity];
			::fast_io::details::my_fill_n(buffer, n, ch);
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, buffer + n);
			return;
		}
	}
	if constexpr (::fast_io::repeated_fill_output_policy_stream<char_type, outputstmtype> &&
				  ::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>)
	{
		constexpr auto policy{repeated_fill_output_policy_define(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<outputstmtype>>)};
		// The policy fields answer independent cost questions. dynamic_coalesce_threshold is consumed by the outer
		// whole-output materializer; scatter_block_size controls how much fill work is shared by repeated descriptors;
		// preferred_scatter_count is only a soft batching target and is clamped below by both stack and syscall limits.
		constexpr ::std::size_t stack_block_limit{
			::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
		constexpr ::std::size_t block_size{
			policy.scatter_block_size < stack_block_limit ? policy.scatter_block_size : stack_block_limit};
		using scatter_type = ::fast_io::basic_io_scatter_t<char_type>;
		constexpr ::std::size_t hard_scatter_limit{
			::fast_io::details::scatter_write_maximum_count_or_unlimited<char_type, outputstmtype>()};
		constexpr ::std::size_t stack_scatter_limit{
			::fast_io::details::decay::print_stack_buffer_max_element_count<scatter_type>()};
		constexpr ::std::size_t policy_scatter_limit{
			policy.preferred_scatter_count < hard_scatter_limit ? policy.preferred_scatter_count : hard_scatter_limit};
		constexpr ::std::size_t scatter_count{
			policy_scatter_limit < stack_scatter_limit ? policy_scatter_limit : stack_scatter_limit};
		if constexpr (block_size != 0u && scatter_count != 0u)
		{
			char_type buffer[block_size];
			::fast_io::details::my_fill_n(buffer, block_size, ch);
			if (n <= block_size)
			{
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, buffer + n);
				return;
			}
			scatter_type scatters[scatter_count];
			for (auto &scatter : scatters)
			{
				// Every descriptor borrows the same immutable filled block; no payload-sized temporary is constructed.
				scatter = {buffer, block_size};
			}
			constexpr ::std::size_t batch_size{block_size * scatter_count};
			while (batch_size < n)
			{
				// One complete batch removes exactly batch_size characters, so the loop strictly progresses and leaves
				// at most one legal descriptor batch for the final calculation.
				::fast_io::details::decay::print_scatter_write_all_dispatch(optstm, scatters, scatter_count);
				n -= batch_size;
			}
			::std::size_t const full_scatter_count{n / block_size};
			::std::size_t const tail_size{n % block_size};
			::std::size_t const final_scatter_count{full_scatter_count + static_cast<::std::size_t>(tail_size != 0u)};
			if (tail_size != 0u)
			{
				scatters[full_scatter_count].len = tail_size;
			}
			::fast_io::details::decay::print_scatter_write_all_dispatch(optstm, scatters, final_scatter_count);
			return;
		}
	}
	// Only the throughput path reaches the separately framed 4-KiB contiguous writer.
	::fast_io::operations::decay::print_semantic_write_fill_large_contiguous(optstm, n, ch);
}

/// @brief    Forward declaration for measuring a semantic argument after input forwarding.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to measure
/// @return   ::std::size_t the precise emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_precise_size_arg(T &&t);

/// @brief    Computes the precise emitted size for a semantic leaf.
/// @details  Leaves use their most direct sizing protocol; reserve-scatters leaves materialize descriptors only to
///           sum the generated scatter lengths.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic leaf type
/// @param    t         the leaf to measure
/// @return   ::std::size_t the precise emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_precise_size_leaf(T &&t)
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
	{
		// Null leaves emit no characters.
		return 0;
	}
	else if constexpr (::fast_io::static_precise_reserve_printable<char_type, value_type>)
	{
		// Static precise reserve leaves expose their character count at compile time.
		return print_reserve_static_precise_size(::fast_io::io_reserve_type<char_type, value_type>);
	}
	else if constexpr (::fast_io::precise_reserve_printable<char_type, value_type>)
	{
		// Run-time precise reserve leaves report their exact character count before emission.
		return print_reserve_precise_size(::fast_io::io_reserve_type<char_type, value_type>, ::std::forward<T>(t));
	}
	else if constexpr (::fast_io::scatter_printable_for<
						   char_type, decltype(::std::forward<T>(t))>)
	{
		// Measurement forwards the leaf, so its exact forwarded expression must own the selected scatter overload.
		return print_scatter_define(::fast_io::io_reserve_type<char_type, value_type>, ::std::forward<T>(t)).len;
	}
	else if constexpr (::fast_io::reserve_scatters_printable<char_type, value_type>)
	{
		// Reserve-scatters leaves must build their scatters before the total length can be summed.
		constexpr auto sz{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
		static_assert(sz.scatters_size != 0);
		constexpr ::std::size_t reserve_buffer_size{sz.reserve_size == 0 ? 1u : sz.reserve_size};
		constexpr bool stack_buffer_ok{
			::fast_io::details::decay::print_stack_buffer_size_within_limit<reserve_buffer_size, char_type> &&
			::fast_io::details::decay::print_stack_buffer_size_within_limit<sz.scatters_size,
																			::fast_io::basic_io_scatter_t<char_type>>};
		if constexpr (stack_buffer_ok)
		{
			// Small reserve-scatters sizing uses stack descriptor and reserve storage.
			::fast_io::basic_io_scatter_t<char_type> scatters[sz.scatters_size];
			char_type buffer[reserve_buffer_size];
			auto result{print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>, scatters,
													  buffer, ::std::forward<T>(t))};
			if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					scatters, scatters + sz.scatters_size, result.scatters_pos_ptr) ||
				!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					buffer, buffer + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
			{
				// Validation must precede descriptor iteration: comparing an unrelated producer cursor in the loop
				// condition would already have left the array domain and therefore could not serve as validation.
				::fast_io::fast_terminate();
			}
			::std::size_t len{};
			for (auto iter{scatters}; iter != result.scatters_pos_ptr; ++iter)
			{
				// Each produced scatter contributes its character length to the precise total.
				len = ::fast_io::details::intrinsics::add_or_overflow_die(len, iter->len);
			}
			return len;
		}
		else
		{
			// Large reserve-scatters sizing allocates descriptor and reserve storage before summing lengths.
			::fast_io::details::local_operator_new_array_ptr<::fast_io::basic_io_scatter_t<char_type>> scatters(
				sz.scatters_size);
			::fast_io::details::local_operator_new_array_ptr<char_type> buffer(reserve_buffer_size);
			auto result{print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>,
													  scatters.ptr, buffer.ptr, ::std::forward<T>(t))};
			if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					scatters.ptr, scatters.ptr + sz.scatters_size, result.scatters_pos_ptr) ||
				!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					buffer.ptr, buffer.ptr + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
			{
				// Heap storage changes only the allocation policy; the customization has the identical closed-range
				// contract and may not seed iteration with an untrusted cursor.
				::fast_io::fast_terminate();
			}
			::std::size_t len{};
			for (auto iter{scatters.ptr}; iter != result.scatters_pos_ptr; ++iter)
			{
				// Each produced scatter contributes its character length to the precise total.
				len = ::fast_io::details::intrinsics::add_or_overflow_die(len, iter->len);
			}
			return len;
		}
	}
}

/// @brief    Normalizes a width placement value into its integral dispatch code.
/// @tparam   placement_type the placement enumeration or integral-like type
/// @param    placement      the placement value
/// @return   ::std::size_t  the normalized placement code
template <typename placement_type>
inline constexpr ::std::size_t print_semantic_width_placement_code(placement_type placement) noexcept
{
	return static_cast<::std::size_t>(placement);
}

/// @brief    Returns the fill character for a semantic width node.
/// @tparam   char_type            the output character type
/// @tparam   width_traits         the compile-time width trait bundle
/// @tparam   width_reference_type the width node reference type
/// @param    wr                   the width node reference
/// @return   char_type            the selected fill character
template <::std::integral char_type, typename width_traits, typename width_reference_type>
inline constexpr char_type print_semantic_width_fill_char(width_reference_type &&wr) noexcept
{
	if constexpr (width_traits::has_fill_char)
	{
		// Width nodes with an explicit fill character cast it to the active output character type.
		return static_cast<char_type>(wr.ch);
	}
	else
	{
		// Width nodes without an explicit fill character use a space.
		return ::fast_io::char_literal_v<u8' ', char_type>;
	}
}

/// @brief    Returns the width placement selected by static traits or by the node at run time.
/// @tparam   width_traits         the compile-time width trait bundle
/// @tparam   width_reference_type the width node reference type
/// @param    wr                   the width node reference
/// @return   auto                 the selected placement value
template <typename width_traits, typename width_reference_type>
inline constexpr auto print_semantic_width_placement(width_reference_type &&wr) noexcept
{
	if constexpr (width_traits::runtime_placement)
	{
		// Runtime-placement width nodes carry the placement value in the node itself.
		return wr.placement;
	}
	else
	{
		// Static-placement width nodes use the placement encoded in their traits.
		return width_traits::static_placement;
	}
}

/// @brief    Forward declaration for measuring a semantic argument's internal padding shift.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to inspect
/// @return   ::std::size_t the internal shift position, or zero when unavailable
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_internal_shift_arg(T &&t);

/// @brief    Computes the internal padding insertion point for a semantic node or leaf.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic node or leaf type
/// @param    t         the semantic node or leaf to inspect
/// @return   ::std::size_t the internal shift position, or zero when unavailable
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_internal_shift(T &&t)
{
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	if constexpr (::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		// Conditions measure the internal shift of the branch that will actually be emitted.
		if (node_ref.pred)
		{
			// The true branch is selected for internal padding placement.
			return ::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(node_ref.t1);
		}
		// The false branch is selected for internal padding placement.
		return ::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(node_ref.t2);
	}
	else if constexpr (::fast_io::details::print_pack<node_type> ||
					   ::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		// Packs and nested width nodes do not expose one stable leaf-level internal shift.
		return 0;
	}
	else
	{
		// Leaf nodes may provide a print_define_internal_shift customization.
		using value_type = ::std::remove_cvref_t<decltype(node_ref)>;
		if constexpr (::fast_io::printable_internal_shift<char_type, value_type>)
		{
			// The leaf customization reports the number of characters before internal padding.
			return print_define_internal_shift(::fast_io::io_reserve_type<char_type, value_type>, node_ref);
		}
		else
		{
			// Leaves without an internal-shift customization fall back to ordinary right placement.
			return 0;
		}
	}
}

/// @brief    Applies semantic input forwarding before measuring internal padding shift.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to inspect
/// @return   ::std::size_t the internal shift position, or zero when unavailable
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_internal_shift_arg(T &&t)
{
	decltype(auto) forwarded{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<T>(t))};
	// A value result is owned by this frame. From this point onward the actual customization expression is the named
	// object, not a reconstructed xvalue; this is the same category used by semantic admission.
	return ::fast_io::operations::decay::print_semantic_internal_shift<char_type>(forwarded);
}

/// @brief    Continuation that accumulates precise sizes for expanded semantic pack elements.
/// @tparam   char_type the output character type
template <::std::integral char_type>
struct print_semantic_precise_size_pack_continuation
{
	::std::size_t *totalptr;

	/// @brief    Adds every expanded pack element size to the running precise total.
	/// @tparam   PackArgs the expanded pack element types
	/// @param    pack_args the expanded pack elements
	template <typename... PackArgs>
	inline constexpr void operator()(PackArgs &&...pack_args) const
	{
		auto add_one = [this](auto &&pack_arg) constexpr {
			if (*totalptr != SIZE_MAX)
			{
				// A pack aggregate is an optional contiguous-strategy proof. Once unavailable, later sizing CPOs need
				// not be invoked merely to rediscover that the same one-buffer plan cannot be formed.
				*totalptr = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					*totalptr,
					::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(
						::std::forward<decltype(pack_arg)>(pack_arg)));
			}
		};
		(add_one(::std::forward<PackArgs>(pack_args)), ...);
	}
};

/// @brief    Computes the precise emitted size for a semantic node or leaf.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic node or leaf type
/// @param    t         the semantic node or leaf to measure
/// @return   ::std::size_t the precise emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_precise_size(T &&t)
{
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		// Packs sum the precise size of every stored element.
		::std::size_t total{};
		::fast_io::details::decay::print_semantic_pack_apply(
			node_ref,
			::fast_io::operations::decay::print_semantic_precise_size_pack_continuation<char_type>{
				__builtin_addressof(total)});
		return total;
	}
	else if constexpr (::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		// Conditions measure only the branch that will be emitted.
		if (node_ref.pred)
		{
			// The predicate selected the true branch for size calculation.
			return ::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(node_ref.t1);
		}
		else
		{
			// The predicate selected the false branch for size calculation.
			return ::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(node_ref.t2);
		}
	}
	else if constexpr (::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		// Width nodes use a static child bound to avoid measuring a child that padding always covers.
		using width_traits = ::fast_io::details::decay::print_semantic_width_traits<node_type>;
		// Parenthesized decltype observes the member-access expression. `node_ref` is a named object and therefore an
		// lvalue even when this function received an rvalue node; the forwarding trait must model the same lvalue passed
		// to the size CPO below, rather than the member's declared type as an invented prvalue.
		using width_child_type =
			::fast_io::details::decay::print_semantic_stable_input_forward_t<
				char_type, decltype((node_ref.reference))>;
		::std::size_t const width{node_ref.width};
		auto const placement{
			::fast_io::operations::decay::print_semantic_width_placement<width_traits>(node_ref)};
		::std::size_t const placement_code{
			::fast_io::operations::decay::print_semantic_width_placement_code(placement)};
		if constexpr (::fast_io::details::decay::print_semantic_static_bounded_size<
						  char_type, width_child_type>::available)
		{
			// A compile-time child bound can prove that active padding determines the complete field length.
			constexpr ::std::size_t child_bound{
				::fast_io::details::decay::print_semantic_static_bounded_size<char_type, width_child_type>::size};
			if (placement_code != 0u && child_bound <= width)
			{
				// Every possible child fits inside the requested field, so no run-time child measurement is needed.
				// The unchecked emitter uses the same proof to select bounded one-pass child emission safely.
				return width;
			}
		}
		// The field width does not dominate the child bound, so the actual child length remains semantically relevant.
		::std::size_t const child_len{
			::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(node_ref.reference)};
		if (width <= child_len || placement_code == 0u)
		{
			// Disabled placement or an already-wide child emits only the child size.
			return child_len;
		}
		// Active placement pads the child up to the requested field width.
		return width;
	}
	else
	{
		// Leaf nodes use their leaf-specific precise-size protocol.
		return ::fast_io::operations::decay::print_semantic_precise_size_leaf<char_type>(
			node_ref);
	}
}

/// @brief    Applies semantic input forwarding before precise-size measurement.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to measure
/// @return   ::std::size_t the precise emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_precise_size_arg(T &&t)
{
	decltype(auto) forwarded{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<T>(t))};
	// The forwarding result is now owned (or exactly borrowed) and is deliberately consumed as a named lvalue. This
	// prevents a second category transition from invalidating lvalue-qualified sizing CPOs.
	return ::fast_io::operations::decay::print_semantic_precise_size<char_type>(forwarded);
}

/// @brief    Forward declaration for unchecked semantic emission into an already-sized buffer.
/// @tparam   char_type the destination buffer character type
/// @tparam   bounded   true when the destination was sized from an upper bound and reserve emitters may use it
/// @tparam   T         the semantic node or leaf type
/// @param    iter      the current output cursor
/// @param    t         the semantic node or leaf to emit
/// @return   char_type* one past the emitted output
template <::std::integral char_type, bool bounded = false, typename T>
inline constexpr char_type *print_semantic_emit_unchecked(char_type *iter, T &&t);

/// @brief    Forwards one semantic print argument and emits it into an already-sized buffer.
/// @details  This wrapper preserves the semantic forwarding rules used by the checked emitters before entering the
///           unchecked node dispatcher.
/// @tparam   char_type  the character type of the destination buffer
/// @tparam   bounded    true when the caller guarantees upper-bound capacity instead of only precise-size capacity
/// @tparam   T          the argument type accepted by the semantic print layer
/// @param    iter       a pointer to the next output position in a buffer large enough for the argument
/// @param    t          the argument to print
/// @return   char_type* a pointer one past the emitted argument
template <::std::integral char_type, bool bounded = false, typename T>
inline constexpr char_type *print_semantic_emit_unchecked_arg(char_type *iter, T &&t)
{
	decltype(auto) forwarded{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<T>(t))};
	// Emit from the stable local expression that the concept graph measured. Re-forwarding an owned result as an
	// xvalue would impose an unproved second transport and make stored move-only leaves spuriously ill-formed.
	return ::fast_io::operations::decay::print_semantic_emit_unchecked<char_type, bounded>(iter, forwarded);
}

/// @brief    Applies unchecked semantic emission to every element supplied by a pack expansion.
/// @tparam   char_type the character type of the destination buffer
/// @tparam   bounded   true when every expanded element may consume its reserve upper-bound capacity
template <::std::integral char_type, bool bounded = false>
struct print_semantic_emit_unchecked_pack_continuation
{
	char_type **iterptr;

	/// @brief    Emits every expanded pack element and updates the shared output cursor.
	/// @tparam   PackArgs the expanded pack element types
	/// @param    pack_args the expanded pack elements
	template <typename... PackArgs>
	inline constexpr void operator()(PackArgs &&...pack_args) const
	{
		// Preserve the caller's capacity policy across every element in the expanded pack.
		((*iterptr = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
			  *iterptr, ::std::forward<PackArgs>(pack_args))),
		 ...);
	}
};

/// @brief    Emits a semantic leaf into an already-sized buffer.
/// @details  Exact-sized destinations use precise protocols where required. Upper-bound-sized destinations prefer
///           reserve protocols so a leaf that supports both bounded and precise sizing is emitted only once.
/// @tparam   char_type  the character type of the destination buffer
/// @tparam   bounded    true when the destination has capacity for the leaf's reserve upper bound
/// @tparam   T          the semantic leaf type
/// @param    iter       a pointer to the next output position
/// @param    t          the leaf to emit
/// @return   char_type* a pointer one past the emitted leaf
template <::std::integral char_type, bool bounded = false, typename T>
inline constexpr char_type *print_semantic_emit_unchecked_leaf(char_type *iter, T &&t)
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t>)
	{
		// Null leaves intentionally emit no output and leave the cursor unchanged.
		return iter;
	}
	else if constexpr (bounded && (::fast_io::reserve_printable<char_type, value_type> ||
								   ::fast_io::dynamic_reserve_printable<char_type, value_type>))
	{
		// The caller allocated the reserve upper bound, so the one-pass reserve define is both safe and cheaper than
		// recomputing an exact length before emission.
		return print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, iter,
									::std::forward<T>(t));
	}
	else if constexpr (::fast_io::static_precise_reserve_printable<char_type, value_type>)
	{
		// Static precise reserve leaves know their full extent and can write directly to the current cursor.
		return print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, iter, ::std::forward<T>(t));
	}
	else if constexpr (::fast_io::precise_reserve_printable<char_type, value_type>)
	{
		// Precise reserve leaves compute their exact size before choosing the define protocol.
		::std::size_t const precise_size{
			print_reserve_precise_size(::fast_io::io_reserve_type<char_type, value_type>, t)};
		if constexpr (requires {
						  {
							  print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter,
														   precise_size, ::std::forward<T>(t))
						  } -> ::std::same_as<char_type *>;
					  })
		{
			// Pointer-returning precise reserve leaves advance the cursor through their define operation.
			return print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter,
												precise_size, ::std::forward<T>(t));
		}
		else
		{
			// Void-returning precise reserve leaves rely on the measured size to advance the cursor.
			print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter,
										 precise_size, ::std::forward<T>(t));
			return iter + precise_size;
		}
	}
	else if constexpr (::fast_io::dynamic_reserve_printable<char_type, value_type>)
	{
		// Dynamic reserve leaves have already been admitted by a bounded materialization path.
		return print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, iter, ::std::forward<T>(t));
	}
	else if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		// Static reserve leaves have already been admitted by a bounded materialization path.
		return print_reserve_define(::fast_io::io_reserve_type<char_type, value_type>, iter, ::std::forward<T>(t));
	}
	else if constexpr (::fast_io::scatter_printable_for<
						   char_type, decltype(::std::forward<T>(t))>)
	{
		// Emission preserves the leaf's forwarded category; checking another category cannot justify the call below.
		auto scatter{print_scatter_define(::fast_io::io_reserve_type<char_type, value_type>, ::std::forward<T>(t))};
		return ::fast_io::details::non_overlapped_copy_n(scatter.base, scatter.len, iter);
	}
	else if constexpr (::fast_io::reserve_scatters_printable<char_type, value_type>)
	{
		// Reserve-scatters leaves materialize temporary descriptors before copying each segment.
		constexpr auto sz{print_reserve_scatters_size(::fast_io::io_reserve_type<char_type, value_type>)};
		static_assert(sz.scatters_size != 0);
		constexpr ::std::size_t reserve_buffer_size{sz.reserve_size == 0 ? 1u : sz.reserve_size};
		constexpr bool stack_buffer_ok{
			::fast_io::details::decay::print_stack_buffer_size_within_limit<reserve_buffer_size, char_type> &&
			::fast_io::details::decay::print_stack_buffer_size_within_limit<sz.scatters_size,
																			::fast_io::basic_io_scatter_t<char_type>>};
		if constexpr (stack_buffer_ok)
		{
			// Small reserve-scatters leaves materialize temporary scatter and reserve storage on the stack.
			::fast_io::basic_io_scatter_t<char_type> scatters[sz.scatters_size];
			char_type buffer[reserve_buffer_size];
			auto result{print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>, scatters,
													  buffer, ::std::forward<T>(t))};
			if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					scatters, scatters + sz.scatters_size, result.scatters_pos_ptr) ||
				!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					buffer, buffer + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
			{
				// Both producer cursors are untrusted until checked; descriptor iteration and copying follow only after
				// the closed-range proof succeeds.
				::fast_io::fast_terminate();
			}
			for (auto scatter_iter{scatters}; scatter_iter != result.scatters_pos_ptr; ++scatter_iter)
			{
				// Each produced scatter segment is copied into the unchecked contiguous output buffer.
				iter = ::fast_io::details::non_overlapped_copy_n(scatter_iter->base, scatter_iter->len, iter);
			}
			return iter;
		}
		else
		{
			// Large reserve-scatters leaves allocate temporary scatter and reserve storage before copying output.
			::fast_io::details::local_operator_new_array_ptr<::fast_io::basic_io_scatter_t<char_type>> scatters(
				sz.scatters_size);
			::fast_io::details::local_operator_new_array_ptr<char_type> buffer(reserve_buffer_size);
			auto result{print_reserve_scatters_define(::fast_io::io_reserve_type<char_type, value_type>,
													  scatters.ptr, buffer.ptr, ::std::forward<T>(t))};
			if (!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					scatters.ptr, scatters.ptr + sz.scatters_size, result.scatters_pos_ptr) ||
				!::fast_io::details::decay::print_reserve_scatters_cursor_in_closed_range(
					buffer.ptr, buffer.ptr + sz.reserve_size, result.reserve_pos_ptr)) [[unlikely]]
			{
				// The dynamic allocation path carries the same producer contract and must reject forged cursors before
				// any pointer-controlled copy loop observes them.
				::fast_io::fast_terminate();
			}
			for (auto scatter_iter{scatters.ptr}; scatter_iter != result.scatters_pos_ptr; ++scatter_iter)
			{
				// Each produced scatter segment is copied into the unchecked contiguous output buffer.
				iter = ::fast_io::details::non_overlapped_copy_n(scatter_iter->base, scatter_iter->len, iter);
			}
			return iter;
		}
	}
}

/// @brief    Emits any semantic print node into an already-sized buffer.
/// @details  This unchecked dispatcher is used after precise sizing or bounded coalescing has guaranteed enough
///           contiguous output space. The bounded policy is propagated through packs, conditions, and width nodes so
///           leaves select an emission protocol compatible with the capacity proof held by the caller.
/// @tparam   char_type  the character type of the destination buffer
/// @tparam   bounded    true when the buffer is sized by semantic upper bounds rather than exact emitted lengths
/// @tparam   T          the semantic node or leaf type
/// @param    iter       a pointer to the next output position
/// @param    t          the node to emit
/// @return   char_type* a pointer one past the emitted node
template <::std::integral char_type, bool bounded, typename T>
inline constexpr char_type *print_semantic_emit_unchecked(char_type *iter, T &&t)
{
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		// Packs emit each element in storage order into the same unchecked output run.
		::fast_io::details::decay::print_semantic_pack_apply(
			node_ref,
			::fast_io::operations::decay::print_semantic_emit_unchecked_pack_continuation<char_type, bounded>{
				__builtin_addressof(iter)});
		return iter;
	}
	else if constexpr (::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		// Branch selection is cheaper than comparing emitted values at run time and preserves the caller's capacity proof.
		if (node_ref.pred)
		{
			return ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(iter,
																									   node_ref.t1);
		}
		return ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(iter,
																								   node_ref.t2);
	}
	else if constexpr (::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		// Width nodes materialize their child before padding so dynamic-reserve children use their actual length.
		using width_traits = ::fast_io::details::decay::print_semantic_width_traits<node_type>;
		using width_child_type =
			::fast_io::details::decay::print_semantic_stable_input_forward_t<
				char_type, decltype((node_ref.reference))>;
		::std::size_t const width{node_ref.width};
		char_type const fillch{
			::fast_io::operations::decay::print_semantic_width_fill_char<char_type, width_traits>(node_ref)};
		auto const placement{
			::fast_io::operations::decay::print_semantic_width_placement<width_traits>(node_ref)};
		::std::size_t const placement_code{
			::fast_io::operations::decay::print_semantic_width_placement_code(placement)};
		char_type *const first{iter};
		char_type *last;
		if constexpr (!bounded && ::fast_io::details::decay::print_semantic_static_bounded_size<
									  char_type, width_child_type>::available)
		{
			// An exact-sized outer run may still use bounded child emission when active padding proves that the field
			// has at least the child's compile-time maximum capacity.
			constexpr ::std::size_t child_bound{
				::fast_io::details::decay::print_semantic_static_bounded_size<char_type, width_child_type>::size};
			if (placement_code != 0u && child_bound <= width)
			{
				// The requested field contains the complete reserve frame, allowing one-pass bounded child emission.
				last = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, true>(
					iter, node_ref.reference);
			}
			else
			{
				// Without a dominating field width, the exact outer allocation cannot safely admit reserve over-allocation.
				last = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, false>(
					iter, node_ref.reference);
			}
		}
		else
		{
			// Already-bounded runs propagate their proof directly; children without a static bound retain the caller's
			// exact policy.
			last = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
				iter, node_ref.reference);
		}
		::std::size_t const len{static_cast<::std::size_t>(last - first)};
		if (width <= len || placement_code == 0u)
		{
			// Width that is already satisfied, or disabled placement, leaves the child bytes unchanged.
			return last;
		}
		::std::size_t const padding{width - len};
		if (placement_code == 1u)
		{
			// Left placement appends all padding after the already-emitted child.
			return ::fast_io::details::my_fill_n(last, padding, fillch);
		}
		if (placement_code == 2u)
		{
			// Center placement shifts the child right, then fills both sides in place.
			::std::size_t const left_padding{padding >> 1u};
			::std::size_t const right_padding{padding - left_padding};
			char_type *const child_pos{first + left_padding};
			::fast_io::details::my_copy_right_shift(first, last, left_padding);
			::fast_io::details::my_fill_n(first, left_padding, fillch);
			::fast_io::details::my_fill_n(child_pos + len, right_padding, fillch);
			return first + width;
		}
		if (placement_code == 4u)
		{
			// Internal placement inserts padding after the sign or prefix reported by the child.
			::std::size_t const internal_len{
				::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(node_ref.reference)};
			if (internal_len != 0 && internal_len <= len)
			{
				// A valid internal shift allows padding to be inserted inside the already-emitted child bytes.
				char_type *const shift_pos{first + internal_len};
				::fast_io::details::my_copy_right_shift(shift_pos, last, padding);
				::fast_io::details::my_fill_n(shift_pos, padding, fillch);
				return first + width;
			}
		}
		// Right placement, or internal placement without a shift point, moves the child behind leading padding.
		::fast_io::details::my_copy_right_shift(first, last, padding);
		::fast_io::details::my_fill_n(first, padding, fillch);
		return first + width;
	}
	else
	{
		// Leaf nodes use the fastest printable protocol supported by their decayed type.
		return ::fast_io::operations::decay::print_semantic_emit_unchecked_leaf<char_type, bounded>(
			iter, node_ref);
	}
}

/// @brief    Computes the total static precise size for a semantic print run.
/// @details  The value is available only when every argument has a compile-time precise semantic size.
/// @tparam   line      true when a trailing newline is part of the run
/// @tparam   char_type the character type of the output run
/// @tparam   Args      the argument types in the run
/// @return   ::std::size_t the static total size, or SIZE_MAX when sizing or one contiguous extent is unavailable
template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t print_semantic_static_precise_total_size() noexcept
{
	if constexpr ((::fast_io::details::decay::print_semantic_static_precise_size<char_type, Args>::available && ...))
	{
		// A fully static run may feed fixed-size coalescing only while its aggregate remains one representable extent.
		::std::size_t total{};
		((total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			  total, ::fast_io::details::decay::print_semantic_static_precise_size<char_type, Args>::size)),
		 ...);
		if constexpr (line)
		{
			// The newline participates in the same contiguous-object proof as every formatted argument.
			total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				total, static_cast<::std::size_t>(1u));
		}
		// SIZE_MAX means only that fixed-size coalescing is unavailable; ordinary per-argument printing remains valid.
		return total;
	}
	else
	{
		// A non-static argument prevents fixed-size coalescing at compile time.
		return SIZE_MAX;
	}
}

/// @brief    Computes the total static upper bound for a semantic print run.
/// @details  The value is available when every argument has a compile-time maximum semantic size.
/// @tparam   line      true when a trailing newline is part of the run
/// @tparam   char_type the character type of the output run
/// @tparam   Args      the argument types in the run
/// @return   ::std::size_t the static total upper bound, or SIZE_MAX when bounding or one contiguous extent is unavailable
template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t print_semantic_static_bounded_total_size() noexcept
{
	if constexpr ((::fast_io::details::decay::print_semantic_static_bounded_size<char_type, Args>::available && ...))
	{
		::std::size_t total{};
		((total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			  total, ::fast_io::details::decay::print_semantic_static_bounded_size<char_type, Args>::size)),
		 ...);
		if constexpr (line)
		{
			total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				total, static_cast<::std::size_t>(1u));
		}
		return total;
	}
	else
	{
		return SIZE_MAX;
	}
}

/// @brief    Computes the run-time precise size for a semantic print run.
/// @details  This is used by checked coalescing paths before allocating or reserving contiguous output space. Exact
///           leaf lengths remain valid even when their aggregate cannot inhabit one C++ pointer domain; in that case
///           SIZE_MAX reports that the optional one-buffer strategy is unavailable.
/// @tparam   line      true when a trailing newline is part of the run
/// @tparam   char_type the character type of the output run
/// @tparam   Args      the argument types in the run
/// @param    args      the arguments whose semantic sizes are measured
/// @return   ::std::size_t the exact total, or SIZE_MAX when one contiguous extent is unavailable
template <bool line, ::std::integral char_type, typename... Args>
inline constexpr ::std::size_t print_semantic_precise_total_size(Args &&...args)
{
	::std::size_t total{};
	auto add_one = [&total](auto &&arg) constexpr {
		if (total != SIZE_MAX)
		{
			total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				total,
				::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(
					::std::forward<decltype(arg)>(arg)));
		}
	};
	(add_one(args), ...);
	if constexpr (line)
	{
		// Newline belongs to the same proposed contiguous range and therefore participates in the pointer-domain proof.
		total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			total, static_cast<::std::size_t>(1u));
	}
	return total;
}

/// @brief    Describes the staged-printable members of one semantic argument run.
/// @details  The recursive specializations count staged arguments and reject groups whose prepared-state types,
///           minimum/maximum counts, or fallback-placement policies differ. Ordinary arguments remain in the run
///           without consuming prepared-state slots when the semantic contiguous-buffer protocol can emit them.
/// @tparam   char_type the destination character type
/// @tparam   Args      the decayed semantic argument types
template <::std::integral char_type, typename... Args>
struct print_semantic_staged_group;

/// @brief Proves that the complete prepared-state array remains a bounded optimization frame.
/// @details Staging is optional, so an oversized state group must fall back instead of injecting an arbitrary frame.
///          The 4-KiB ceiling is independent of the output buffer budget; alignment padding is included explicitly and
///          division is used before multiplication so the proof cannot overflow `size_t`.
template <typename state_type, ::std::size_t count>
inline consteval bool print_semantic_staged_state_array_stack_safe() noexcept
{
	constexpr ::std::size_t configured_bytes{
		::fast_io::details::decay::print_stack_buffer_max_bytes()};
	constexpr ::std::size_t preferred_bytes{4096u};
	constexpr ::std::size_t budget{
		configured_bytes < preferred_bytes ? configured_bytes : preferred_bytes};
	if constexpr (count == 0u || budget == 0u || alignof(state_type) > budget)
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t usable{budget - (alignof(state_type) - 1u)};
		return count <= usable / sizeof(state_type);
	}
}

/// @brief Selects one type from a parameter pack without recursively rebuilding staged-group metadata.
/// @details Only one such chain is instantiated, to obtain the first staged member after a fold has proved one exists.
///          The former group implementation instantiated a distinct metadata tail for every argument in the run.
template <::std::size_t index, typename T, typename... Args>
struct print_semantic_staged_pack_element
	: ::fast_io::operations::decay::print_semantic_staged_pack_element<index - 1u, Args...>
{};

template <typename T, typename... Args>
struct print_semantic_staged_pack_element<0u, T, Args...>
{
	using type = T;
};

/// @brief Finds the first staged member in a nonempty pack already proved to contain one.
/// @details A compact boolean table makes this a single constant-evaluation loop rather than one metadata class per
///          argument tail. The returned index is consumed by one pack-element selection only.
template <::std::integral char_type, typename... Args>
inline consteval ::std::size_t print_semantic_first_staged_index() noexcept
{
	constexpr bool staged[]{::fast_io::staged_printable<char_type, Args>...};
	for (::std::size_t index{}; index != sizeof...(Args); ++index)
	{
		if (staged[index])
		{
			return index;
		}
	}
	return sizeof...(Args);
}

/// @brief Compares one member with the representative staged protocol.
/// @details Ordinary members are neutral. A staged member must expose the same state, scheduling envelope, and
///          fallback placement as the representative, exactly matching the former adjacent-tail comparisons.
template <::std::integral char_type, typename representative_type, typename T>
inline consteval bool print_semantic_staged_member_compatible() noexcept
{
	if constexpr (::fast_io::staged_printable<char_type, T>)
	{
		return ::std::same_as<
				   ::fast_io::details::staged_printable_state_t<char_type, representative_type>,
				   ::fast_io::details::staged_printable_state_t<char_type, T>> &&
			   print_staged_width(::fast_io::io_reserve_type<char_type, representative_type>) ==
				   print_staged_width(::fast_io::io_reserve_type<char_type, T>) &&
			   print_staged_max_count(::fast_io::io_reserve_type<char_type, representative_type>) ==
				   print_staged_max_count(::fast_io::io_reserve_type<char_type, T>) &&
			   print_staged_fallback_inline(::fast_io::io_reserve_type<char_type, representative_type>) ==
				   print_staged_fallback_inline(::fast_io::io_reserve_type<char_type, T>);
	}
	else
	{
		return true;
	}
}

/// @brief Implements staged-group metadata after one fold determines whether any staged member exists.
template <::std::integral char_type, bool has_staged_member, typename... Args>
struct print_semantic_staged_group_impl;

/// @brief Preserves the exact metadata of an empty or entirely ordinary run.
template <::std::integral char_type, typename... Args>
struct print_semantic_staged_group_impl<char_type, false, Args...>
{
	using prepared_type = void;
	inline static constexpr ::std::size_t count{};
	inline static constexpr ::std::size_t width{};
	inline static constexpr ::std::size_t max_count{SIZE_MAX};
	inline static constexpr bool fallback_inline{};
	inline static constexpr bool compatible{true};
	inline static constexpr bool emittable{
		(true && ... &&
		 ::fast_io::details::decay::print_semantic_bounded_size_ok<char_type, Args>::value)};
	inline static constexpr bool storage_safe{true};
	inline static constexpr bool available{};
};

/// @brief Reduces compatible staged-group metadata with pack folds.
/// @details The representative is the leftmost staged member, matching the metadata selected by the former recursive
///          head specialization. Count, compatibility, and ordinary reserve proofs are independent reductions, so
///          they do not require successively shorter group specializations.
template <::std::integral char_type, typename... Args>
struct print_semantic_staged_group_impl<char_type, true, Args...>
{
	inline static constexpr ::std::size_t representative_index{
		::fast_io::operations::decay::print_semantic_first_staged_index<char_type, Args...>()};
	static_assert(representative_index < sizeof...(Args));
	using representative_type = typename ::fast_io::operations::decay::print_semantic_staged_pack_element<
		representative_index, Args...>::type;
	using prepared_type = ::fast_io::details::staged_printable_state_t<char_type, representative_type>;
	inline static constexpr ::std::size_t count{
		(static_cast<::std::size_t>(::fast_io::staged_printable<char_type, Args>) + ...)};
	inline static constexpr ::std::size_t width{
		print_staged_width(::fast_io::io_reserve_type<char_type, representative_type>)};
	inline static constexpr ::std::size_t max_count{
		print_staged_max_count(::fast_io::io_reserve_type<char_type, representative_type>)};
	inline static constexpr bool fallback_inline{print_staged_fallback_inline(
		::fast_io::io_reserve_type<char_type, representative_type>)};
	inline static constexpr bool compatible{
		(true && ... &&
		 ::fast_io::operations::decay::print_semantic_staged_member_compatible<
			 char_type, representative_type, Args>())};
	// Staged emission writes into a buffer sized by the ordinary bounded protocol. Requiring that fallback here is the
	// capacity proof for both an ineligible value and a staged implementation that emits its documented maximum.
	inline static constexpr bool emittable{
		(true && ... &&
		 ::fast_io::details::decay::print_semantic_bounded_size_ok<char_type, Args>::value)};
	inline static constexpr bool storage_safe{
		::fast_io::operations::decay::print_semantic_staged_state_array_stack_safe<prepared_type, count>()};
	inline static constexpr bool available{
		compatible && emittable && storage_safe && count >= width && count <= max_count};
};

/// @brief Describes a staged group without recursively materializing every argument tail.
template <::std::integral char_type, typename... Args>
struct print_semantic_staged_group
	: ::fast_io::operations::decay::print_semantic_staged_group_impl<
		  char_type, (false || ... || ::fast_io::staged_printable<char_type, Args>), Args...>
{};

/// @brief    Tests whether one argument permits staged emission for its current value.
/// @details  Ordinary arguments are neutral members of the all-eligible reduction. Staged arguments delegate the
///           value-dependent decision to their print_staged_eligible customization.
/// @tparam   char_type the destination character type
/// @tparam   T         the argument type
/// @param    t         the argument to test
/// @return   bool true when this argument does not prevent staging the run
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_staged_eligible(T const &t) noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::staged_printable<char_type, value_type>)
	{
		// A staged argument may reject exceptional or otherwise unsupported run-time values.
		return print_staged_eligible(::fast_io::io_reserve_type<char_type, value_type>, t);
	}
	else
	{
		// An ordinary argument has no prepared state and therefore cannot invalidate the staged subset.
		return true;
	}
}

/// @brief Stores every argument position's compact prepared-state index.
/// @details Entry N is the number of staged members before argument N. One prefix loop computes the complete map;
///          prepare and emit then use constant array lookups instead of independently instantiating N recursive
///          position walks, which made an interleaved staged run quadratic in its total component count.
template <::std::size_t extent>
struct print_semantic_staged_position_map
{
	::std::size_t values[extent];
};

template <::std::integral char_type, typename... Args>
	requires(sizeof...(Args) != 0u)
inline consteval auto print_semantic_make_staged_position_map() noexcept
{
	::fast_io::operations::decay::print_semantic_staged_position_map<sizeof...(Args)> result{};
	constexpr bool staged[]{::fast_io::staged_printable<char_type, Args>...};
	::std::size_t prepared_index{};
	for (::std::size_t argument_index{}; argument_index != sizeof...(Args); ++argument_index)
	{
		result.values[argument_index] = prepared_index;
		prepared_index += static_cast<::std::size_t>(staged[argument_index]);
	}
	return result;
}

template <::std::integral char_type, typename... Args>
	requires(sizeof...(Args) != 0u)
inline constexpr auto print_semantic_staged_positions{
	::fast_io::operations::decay::print_semantic_make_staged_position_map<char_type, Args...>()};

/// @brief    Prepares one staged argument into its compact state slot.
/// @details  Ordinary arguments deliberately perform no work because they are emitted by the regular semantic path.
/// @tparam   char_type    the destination character type
/// @tparam   position     the prepared-state slot assigned to this argument
/// @tparam   prepared_type the common prepared-state type of the group
/// @tparam   T            the argument type
/// @param    prepared     the group prepared-state array
/// @param    t            the argument to prepare
template <::std::integral char_type, ::std::size_t position, typename prepared_type, typename T>
// GCC 11--16 and Clang 17--23 naturally inline this lane operation; forcing it
// changes neither caller code nor measured throughput.
inline constexpr void print_semantic_staged_prepare_one(
	prepared_type *prepared, T const &t) noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::staged_printable<char_type, value_type>)
	{
		// Store only staged results so unrelated arguments do not create holes in the prepared-state array.
		prepared[position] = print_staged_prepare(::fast_io::io_reserve_type<char_type, value_type>, t);
	}
}

/// @brief    Prepares every staged-printable member of an argument run before any member is emitted.
/// @details  Separating the prepare fold from the emit fold exposes independent conversion work to the instruction
///           scheduler while preserving the original argument order during emission.
/// @tparam   char_type     the destination character type
/// @tparam   prepared_type the common prepared-state type of the group
/// @tparam   positions     the original argument-pack positions
/// @tparam   Args          the argument types
/// @param    prepared      the group prepared-state array
/// @param    <unnamed>     the index sequence selecting every argument
/// @param    args          the arguments to prepare
template <::std::integral char_type, typename prepared_type, ::std::size_t... positions, typename... Args>
inline constexpr void print_semantic_staged_prepare(
	prepared_type *prepared, ::std::index_sequence<positions...>, Args const &...args) noexcept
{
	(::fast_io::operations::decay::print_semantic_staged_prepare_one<
		 char_type,
		 ::fast_io::operations::decay::print_semantic_staged_positions<
			 char_type, ::std::remove_cvref_t<Args>...>
			 .values[positions]>(prepared, args),
	 ...);
}

/// @brief    Emits one member of a prepared semantic run.
/// @details  Staged members consume their prepared state; ordinary members delegate to the existing unchecked
///           semantic emitter with the caller's capacity policy unchanged.
/// @tparam   char_type     the destination character type
/// @tparam   bounded       true when the destination has the semantic upper-bound capacity
/// @tparam   position      the prepared-state slot assigned to this argument
/// @tparam   prepared_type the common prepared-state type of the group
/// @tparam   T             the argument type
/// @param    iter          the current output cursor
/// @param    prepared      the group prepared-state array
/// @param    t             the argument to emit
/// @return   char_type* one past this argument's output
template <::std::integral char_type, bool bounded, ::std::size_t position,
		  typename prepared_type, typename T>
inline constexpr char_type *print_semantic_staged_emit_one(
	char_type *iter, prepared_type const *prepared, T &&t)
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::staged_printable<char_type, value_type>)
	{
		// Admission probes the staged protocol through `value_type const&`. Preserve that exact expression here: a
		// mutable lvalue overload is a distinct CPO and may be deleted, throwing, or semantically unrelated even though
		// the const overload proved the staged contract.
		return print_staged_define(
			::fast_io::io_reserve_type<char_type, value_type>, iter,
			static_cast<value_type const &>(t), prepared[position]);
	}
	else
	{
		// Preserve the established semantic behavior for separators and every other ordinary argument.
		return ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
			iter, ::std::forward<T>(t));
	}
}

/// @brief    Emits a fully prepared argument run in its original order.
/// @details  The compact prepared-state position is recomputed for each original argument-pack position, allowing
///           staged and ordinary arguments to remain interleaved without changing observable output order.
/// @tparam   char_type     the destination character type
/// @tparam   bounded       true when the destination has the semantic upper-bound capacity
/// @tparam   prepared_type the common prepared-state type of the group
/// @tparam   positions     the original argument-pack positions
/// @tparam   Args          the argument types
/// @param    iter          the current output cursor
/// @param    prepared      the group prepared-state array
/// @param    <unnamed>     the index sequence selecting every argument
/// @param    args          the arguments to emit
/// @return   char_type* one past the complete run
template <::std::integral char_type, bool bounded, typename prepared_type,
		  ::std::size_t... positions, typename... Args>
inline constexpr char_type *print_semantic_staged_emit(
	char_type *iter, prepared_type const *prepared, ::std::index_sequence<positions...>, Args &&...args)
{
	((iter = ::fast_io::operations::decay::print_semantic_staged_emit_one<
		  char_type, bounded,
		  ::fast_io::operations::decay::print_semantic_staged_positions<
			  char_type, ::std::remove_cvref_t<Args>...>
			  .values[positions]>(
		  iter, prepared, ::std::forward<Args>(args))),
	 ...);
	return iter;
}

/// @brief    Emits an ineligible staged group through a cold per-argument semantic path.
/// @details  This is the conservative placement selected by staged customizations that have not proved an in-caller
///           fallback profitable. Keeping it out of line bounds the regular staged body. The optional attributes
///           affect placement only; a compiler that does not provide them executes the identical fallback body.
/// @tparam   char_type the destination character type
/// @tparam   bounded   true when the destination has the semantic upper-bound capacity
/// @tparam   Args      the argument types
/// @param    iter      the current output cursor
/// @param    args      the arguments to emit
/// @return   char_type* one past the complete run
template <::std::integral char_type, bool bounded, typename... Args>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#endif
inline char_type *print_semantic_staged_fallback(char_type *iter, Args &&...args)
{
	((iter = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
		  iter, ::std::forward<Args>(args))),
	 ...);
	return iter;
}

/// @brief    Emits an ineligible staged group through an in-caller per-argument semantic path.
/// @details  A staged type may select this form only through its compile-time fallback-placement CPO. The emitted
///           operations are exactly the same fold as the cold form. GCC 11--16 and Clang 17--23 already inline this
///           selected hot arm; a mandatory attribute showed no throughput or caller-code benefit.
/// @tparam   char_type the destination character type
/// @tparam   bounded   true when the destination has the semantic upper-bound capacity
/// @tparam   Args      the argument types
/// @param    iter      the current output cursor
/// @param    args      the arguments to emit
/// @return   char_type* one past the complete run
template <::std::integral char_type, bool bounded, typename... Args>
inline constexpr char_type *
print_semantic_staged_fallback_inline(char_type *iter, Args &&...args)
{
	((iter = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
		  iter, ::std::forward<Args>(args))),
	 ...);
	return iter;
}

/// @brief    Emits a semantic print run into an already-sized contiguous buffer.
/// @details  The bounded policy distinguishes buffers allocated from reserve upper bounds from buffers allocated at
///           exact emitted size, preventing precise-capable leaves from forcing unnecessary second measurements.
/// @tparam   line      true when a trailing newline is appended
/// @tparam   char_type the character type of the destination buffer
/// @tparam   bounded   true when the buffer has capacity for every argument's semantic reserve upper bound
/// @tparam   Args      the argument types in the run
/// @param    iter      a pointer to the next output position
/// @param    args      the arguments to emit in order
/// @return   char_type* a pointer one past the emitted run
template <bool line, ::std::integral char_type, bool bounded = false, typename... Args>
// The full dynamic-floating benchmark matrix found no measurable advantage from
// forcing this whole semantic run into its caller; ordinary inlining avoids an
// unconditional code-size policy on heterogeneous packs.
inline constexpr char_type *print_semantic_emit_unchecked_run(
	char_type *iter, Args &&...args)
{
	using staged_group =
		::fast_io::operations::decay::print_semantic_staged_group<char_type, ::std::remove_cvref_t<Args>...>;
	if constexpr (staged_group::available && bounded)
	{
		// A compatible group in an upper-bound-sized buffer can safely use the two-phase staged protocol.
		using prepared_type = typename staged_group::prepared_type;
		bool eligible{true};
		// Evaluate every lane without short-circuit branches so the compiler can combine or vectorize the checks.
		((eligible = eligible &
					 ::fast_io::operations::decay::print_semantic_staged_eligible<char_type>(args)),
		 ...);
		if (eligible) [[likely]]
		{
			// Prepare every independent staged value first, then serialize the original argument run into the buffer.
			prepared_type prepared[staged_group::count];
			(::fast_io::operations::decay::print_semantic_staged_prepare<char_type>(
				prepared, ::std::make_index_sequence<sizeof...(Args)>{}, args...));
			iter = ::fast_io::operations::decay::print_semantic_staged_emit<char_type, bounded>(
				iter, prepared, ::std::make_index_sequence<sizeof...(Args)>{}, ::std::forward<Args>(args)...);
		}
		else
		{
			// Any ineligible value sends the whole run to the original formatter so no partial output is observable.
			if constexpr (staged_group::fallback_inline)
			{
				// The staged type has audited an in-caller fallback for this complete compatible group.
				iter = ::fast_io::operations::decay::print_semantic_staged_fallback_inline<char_type, bounded>(
					iter, ::std::forward<Args>(args)...);
			}
			else
			{
				// The conservative policy keeps uncommon scalar fallback materialization outside the staged body.
				iter = ::fast_io::operations::decay::print_semantic_staged_fallback<char_type, bounded>(
					iter, ::std::forward<Args>(args)...);
			}
		}
	}
	else
	{
		// Small, incompatible, or exact-sized runs retain the established per-argument semantic emission path.
		((iter = ::fast_io::operations::decay::print_semantic_emit_unchecked_arg<char_type, bounded>(
			  iter, ::std::forward<Args>(args))),
		 ...);
	}
	if constexpr (line)
	{
		// The line variant appends a newline after the last emitted argument.
		*iter = ::fast_io::char_literal_v<u8'\n', char_type>;
		++iter;
	}
	return iter;
}

/// @brief    Forward declaration for computing a bounded semantic argument size after input forwarding.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to measure
/// @return   ::std::size_t an upper bound for the emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_bounded_size_arg(T &&t);

/// @brief    Computes an emitted-size upper bound for a semantic leaf.
/// @details  Static reserve bounds take precedence over precise sizing so a bounded allocation and its subsequent
///           emission use the same protocol. Leaves without a static reserve bound fall back to exact or dynamic
///           object-specific sizing.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic leaf type
/// @param    t         the leaf to measure
/// @return   ::std::size_t an upper bound for the emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_bounded_size_leaf(T &&t)
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		// Prefer the compile-time reserve maximum even when the leaf also exposes a run-time precise size.
		return print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>);
	}
	else if constexpr (::fast_io::details::decay::print_semantic_precise_leaf_size_ok_v<
						   char_type, decltype(::std::forward<T>(t))>)
	{
		// Existing precise leaves already provide an exact bound. Preserve the forwarded expression because a scatter
		// replay proof for an lvalue-only producer is not interchangeable with the public rvalue compatibility query.
		return ::fast_io::operations::decay::print_semantic_precise_size_leaf<char_type>(::std::forward<T>(t));
	}
	else if constexpr (::fast_io::dynamic_reserve_printable<char_type, value_type>)
	{
		// Dynamic reserve leaves report the maximum buffer size needed for this object.
		return print_reserve_size(::fast_io::io_reserve_type<char_type, value_type>, ::std::forward<T>(t));
	}
}

/// @brief    Continuation that accumulates bounded sizes for expanded semantic pack elements.
/// @tparam   char_type the output character type
template <::std::integral char_type>
struct print_semantic_bounded_size_pack_continuation
{
	::std::size_t *totalptr;

	/// @brief    Adds every expanded pack element upper bound to the running total.
	/// @tparam   PackArgs the expanded pack element types
	/// @param    pack_args the expanded pack elements
	template <typename... PackArgs>
	inline constexpr void operator()(PackArgs &&...pack_args) const
	{
		auto add_one = [this](auto &&pack_arg) constexpr {
			if (*totalptr != SIZE_MAX)
			{
				// Conservative per-leaf bounds may be individually useful even when their sum cannot be allocated.
				// Propagate unavailability instead of converting a failed grouping optimization into termination.
				*totalptr = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
					*totalptr,
					::fast_io::operations::decay::print_semantic_bounded_size_arg<char_type>(
						::std::forward<decltype(pack_arg)>(pack_arg)));
			}
		};
		(add_one(::std::forward<PackArgs>(pack_args)), ...);
	}
};

/// @brief    Computes an emitted-size upper bound for a semantic node or leaf.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic node or leaf type
/// @param    t         the semantic node or leaf to measure
/// @return   ::std::size_t an upper bound for the emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_bounded_size(T &&t)
{
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		// Packs sum the upper bound of every stored element.
		::std::size_t total{};
		::fast_io::details::decay::print_semantic_pack_apply(
			node_ref,
			::fast_io::operations::decay::print_semantic_bounded_size_pack_continuation<char_type>{
				__builtin_addressof(total)});
		return total;
	}
	else if constexpr (::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		// Conditions need only the branch selected for this object.
		if (node_ref.pred)
		{
			return ::fast_io::operations::decay::print_semantic_bounded_size_arg<char_type>(node_ref.t1);
		}
		return ::fast_io::operations::decay::print_semantic_bounded_size_arg<char_type>(node_ref.t2);
	}
	else if constexpr (::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		// Width nodes use the larger of the child upper bound and the active field width.
		using width_traits = ::fast_io::details::decay::print_semantic_width_traits<node_type>;
		::std::size_t const child_bound{
			::fast_io::operations::decay::print_semantic_bounded_size_arg<char_type>(node_ref.reference)};
		::std::size_t const width{node_ref.width};
		auto const placement{
			::fast_io::operations::decay::print_semantic_width_placement<width_traits>(node_ref)};
		::std::size_t const placement_code{
			::fast_io::operations::decay::print_semantic_width_placement_code(placement)};
		if (width <= child_bound || placement_code == 0u)
		{
			return child_bound;
		}
		return width;
	}
	else
	{
		// Leaf nodes use either an exact size or a dynamic reserve bound.
		return ::fast_io::operations::decay::print_semantic_bounded_size_leaf<char_type>(
			node_ref);
	}
}

/// @brief    Applies semantic input forwarding before bounded-size measurement.
/// @tparam   char_type the output character type
/// @tparam   T         the semantic input argument type
/// @param    t         the argument to measure
/// @return   ::std::size_t an upper bound for the emitted character count
template <::std::integral char_type, typename T>
inline constexpr ::std::size_t print_semantic_bounded_size_arg(T &&t)
{
	decltype(auto) forwarded{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<T>(t))};
	// Bounded sizing observes the same named object later used by unchecked emission. This equality of expressions is
	// the evidence that a bound obtained from one ref-qualified overload applies to the subsequent define call.
	return ::fast_io::operations::decay::print_semantic_bounded_size<char_type>(forwarded);
}

/// @brief    Computes the run-time bounded size for a semantic print run.
/// @tparam   line      true when a trailing newline is part of the run
/// @tparam   char_type the character type of the output run
/// @tparam   Args      the argument types in the run
/// @param    args      the arguments whose semantic bounds are measured
/// @return   ::std::size_t the total upper bound, or SIZE_MAX when a grouped contiguous extent is unavailable
template <bool line, ::std::integral char_type, typename... Args>
inline constexpr ::std::size_t print_semantic_bounded_total_size(Args &&...args)
{
	::std::size_t total{};
	auto add_one = [&total](auto &&arg) constexpr {
		if (total != SIZE_MAX)
		{
			total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
				total,
				::fast_io::operations::decay::print_semantic_bounded_size_arg<char_type>(
					::std::forward<decltype(arg)>(arg)));
		}
	};
	(add_one(args), ...);
	if constexpr (line)
	{
		// The newline must fit the same optional grouped range.
		total = ::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			total, static_cast<::std::size_t>(1u));
	}
	return total;
}

/// @brief Names the semantic object observed after recursively removing a public parameter transport.
/// @details The filtered print run owns stable named arguments.  A width node can therefore be inspected through the
///          same lvalue expression that bounded emission later observes, without forwarding or copying its child.
template <typename T>
using print_semantic_single_pass_bounded_node_ref_t = decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<T &>()));

template <typename T>
using print_semantic_single_pass_bounded_node_t = ::std::remove_cvref_t<
	::fast_io::operations::decay::print_semantic_single_pass_bounded_node_ref_t<T>>;

/// @brief Proves that internal-width inspection ends in one non-throwing plain-leaf customization.
/// @details The bounded put-area strategy has already restricted admission to an explicitly audited source.  Internal
///          placement additionally observes the child after its reserve define, so model that second forwarding chain
///          independently: the same named lvalue must reach a plain leaf whose shift CPO is explicitly `noexcept`.
///          Conditions, packs, nested widths, and leaves without the CPO remain on the established measured path.
template <::std::integral char_type, typename expression_type>
inline consteval bool print_semantic_single_pass_bounded_internal_shift_nothrow() noexcept
{
	if constexpr (!requires {
#if defined(__HERBCEPTIONS__)
						  requires(!throws((
							  ::fast_io::details::decay::print_semantic_input_forward<char_type>(
								  ::std::declval<expression_type>()))));
#endif
						  {
						  ::fast_io::details::decay::print_semantic_input_forward<char_type>(
							  ::std::declval<expression_type>())
					  } noexcept;
				  })
	{
		return false;
	}
	else
	{
		using forwarded_result = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::declval<expression_type>()));
		using stable_forwarded_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<forwarded_result>>;
		using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<stable_forwarded_expression>()));
		using stable_node_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<node_result>>;
		using node_type = ::std::remove_cvref_t<node_result>;
		if constexpr (
			::fast_io::details::print_pack<node_type> ||
			::fast_io::details::decay::print_semantic_condition_v<node_type> ||
			::fast_io::details::decay::print_semantic_width_v<node_type>)
		{
			return false;
		}
		else
		{
			return requires(stable_node_expression node) {
#if defined(__HERBCEPTIONS__)
				// Internal placement observes this exact named leaf after emission. A deterministic failure would
				// strand already-written output even when the extension currently reports `noexcept(E)` as true.
				requires(!throws((print_define_internal_shift(
					::fast_io::io_reserve_type<char_type, node_type>, node))));
#endif
				{
					print_define_internal_shift(
						::fast_io::io_reserve_type<char_type, node_type>, node)
				} noexcept -> ::std::same_as<::std::size_t>;
			};
		}
	}
}

/// @brief Proves the actual bounded semantic emission chain cannot throw after raw put-area writes begin.
/// @details `print_semantic_emit_unchecked_arg` first owns/borrows the input-forwarding result as a named lvalue. Width
///          nodes then recurse through their child; only the final leaf invokes a reserve define. Model those exact
///          categories instead of requiring a nonexistent top-level width reserve CPO.
template <::std::integral char_type, typename expression_type>
inline consteval bool print_semantic_single_pass_bounded_define_nothrow() noexcept
{
	if constexpr (!requires {
#if defined(__HERBCEPTIONS__)
						  requires(!throws((
							  ::fast_io::details::decay::print_semantic_input_forward<char_type>(
								  ::std::declval<expression_type>()))));
#endif
						  {
						  ::fast_io::details::decay::print_semantic_input_forward<char_type>(
							  ::std::declval<expression_type>())
					  } noexcept;
				  })
	{
		return false;
	}
	else
	{
		using forwarded_result = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::declval<expression_type>()));
		using stable_forwarded_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<forwarded_result>>;
		using node_result = decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<stable_forwarded_expression>()));
		using stable_node_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<node_result>>;
		using node_type = ::std::remove_cvref_t<node_result>;
		if constexpr (
			::fast_io::details::decay::print_semantic_width_v<node_type>)
		{
			using width_traits =
				::fast_io::details::decay::print_semantic_width_traits<node_type>;
			using child_expression = decltype((::std::declval<stable_node_expression>().reference));
			if constexpr (width_traits::runtime_placement)
			{
				// Run-time placement may select internal padding. Admit it only when both the bounded child define and
				// the independently forwarded plain-leaf shift query are non-throwing.
				return ::fast_io::operations::decay::
						   print_semantic_single_pass_bounded_define_nothrow<
							   char_type, child_expression>() &&
					   ::fast_io::operations::decay::
						   print_semantic_single_pass_bounded_internal_shift_nothrow<
							   char_type, child_expression>();
			}
			else if constexpr (
				::fast_io::operations::decay::print_semantic_width_placement_code(
					width_traits::static_placement) == 4u)
			{
				// Static internal placement receives the same narrow proof. Arbitrary semantic/custom leaves cannot
				// enter merely because some unrelated source in the run carries the bounded-materialization marker.
				return ::fast_io::operations::decay::
						   print_semantic_single_pass_bounded_define_nothrow<
							   char_type, child_expression>() &&
					   ::fast_io::operations::decay::
						   print_semantic_single_pass_bounded_internal_shift_nothrow<
							   char_type, child_expression>();
			}
			else
			{
				return ::fast_io::operations::decay::
					print_semantic_single_pass_bounded_define_nothrow<
						char_type, child_expression>();
			}
		}
		else if constexpr (
			!::fast_io::details::print_pack<node_type> &&
			!::fast_io::details::decay::print_semantic_condition_v<node_type> &&
			(::fast_io::reserve_printable<char_type, node_type> ||
			 ::fast_io::dynamic_reserve_printable<char_type, node_type>))
		{
			return requires(char_type *iter, stable_node_expression node) {
#if defined(__HERBCEPTIONS__)
				// Let E be the named leaf define executed by the unchecked emitter. Strategy admission requires
				// `!throws(E) && noexcept(E)`; the following compound requirement supplies the second conjunct.
				requires(!throws((print_reserve_define(
					::fast_io::io_reserve_type<char_type, node_type>, iter, node))));
#endif
				{
					print_reserve_define(
						::fast_io::io_reserve_type<char_type, node_type>,
						iter, node)
				} noexcept -> ::std::same_as<char_type *>;
			};
		}
		else
		{
			return false;
		}
	}
}

/// @brief Recognizes a library-audited source whose cheap non-fatal bound avoids repeated exact formatting.
/// @details The opt-in is destination-neutral because multiple consumers need the same proof: the source-specific size
///          query is noexcept, repeatable, does not materialize the representation, and returns SIZE_MAX rather than
///          terminating when the caller's finite contiguous range cannot contain its reserve maximum. Its actual
///          semantic bounded-emission chain must also end in a non-throwing pointer-returning reserve define, so a
///          deferred cursor is never stranded by an exception after raw output has begun. Exact `true_type` matching
///          makes the source's explicit one-pass opt-in visible rather than inferring it from an ordinary
///          reserve-size customization.
template <typename char_type, typename T>
concept print_semantic_single_pass_bounded_source = ::std::integral<char_type> &&
													::fast_io::operations::decay::print_semantic_single_pass_bounded_define_nothrow<
														char_type, T &>() &&
													::fast_io::single_pass_bounded_materialization_source<
														char_type,
														::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>> &&
													requires {
														{
															print_single_pass_bounded_direct_put_area_safe(
																::fast_io::io_reserve_type<
																	char_type,
																	::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>>)
														} -> ::std::same_as<::std::true_type>;
													};

/// @brief Recognizes a core passive reserve companion whose bounded define is the complete non-throwing operation.
/// @details Reuse the mixed-put-area leaf whitelist instead of treating arbitrary `reserve_printable` customizations as
///          pure.  The exact node expression keeps cv/ref admission and execution identical.
template <typename char_type, typename T>
concept print_semantic_single_pass_bounded_passive_companion =
	::std::integral<char_type> &&
	::fast_io::details::decay::print_buffered_passive_reserve_leaf<
		::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>>::value &&
	::fast_io::reserve_printable<
		char_type,
		::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>> &&
	requires(T &value, char_type *iter) {
#if defined(__HERBCEPTIONS__)
		requires(!throws((print_reserve_define(
			::fast_io::io_reserve_type<
				char_type,
				::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>>,
			iter,
			::fast_io::details::decay::print_semantic_node_ref(value)))));
#endif
		{
			print_reserve_define(
				::fast_io::io_reserve_type<
					char_type,
					::fast_io::operations::decay::print_semantic_single_pass_bounded_node_t<T>>,
				iter,
				::fast_io::details::decay::print_semantic_node_ref(value))
		} noexcept -> ::std::same_as<char_type *>;
	};

/// @brief Admits one component to the speculative put-area plan without an object-dependent fallback query.
/// @details Marked sources provide their audited cheap bound. Every companion must be a whitelisted passive core leaf;
///          this excludes arbitrary reserve/precise/scatter producers whose observation or define semantics could
///          otherwise change when the available put area selects bounded emission before the established precise path.
template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_single_pass_bounded_component_v =
	::fast_io::operations::decay::print_semantic_single_pass_bounded_source<char_type, T> ||
	::fast_io::operations::decay::print_semantic_single_pass_bounded_passive_companion<
		char_type, T>;

/// @brief Extends passive admission only for semantic runs beyond the established compact-record limit.
/// @details A larger run may contain a finite semantic-width chain around a passive leaf, or a leaf whose associated
///          namespace explicitly proves the same type-static, non-throwing reserve semantics. This capability describes
///          a type; only IO core may consume it when selecting a destination strategy.
template <typename char_type, typename T>
inline consteval bool
print_semantic_extended_bounded_passive_companion_impl() noexcept
{
	if constexpr (!::std::integral<char_type>)
	{
		return false;
	}
	else
	{
		using node_expression =
			::fast_io::operations::decay::print_semantic_single_pass_bounded_node_ref_t<T>;
		using node_type = ::std::remove_cvref_t<node_expression>;
		if constexpr (::fast_io::details::decay::print_semantic_width_v<node_type>)
		{
			using stable_node_expression =
				::std::add_lvalue_reference_t<::std::remove_reference_t<node_expression>>;
			using child_expression = decltype((::std::declval<stable_node_expression>().reference));
			// A width emitter writes a child of length L and max(W-L,0) fill characters, so its length is
			// max(W,L). From the child's proof L <= B, monotonicity of max gives max(W,L) <= max(W,B),
			// exactly the semantic bound queried by the extended capacity probe below.
			return ::fast_io::operations::decay::
					   print_semantic_single_pass_bounded_define_nothrow<char_type, T &>() &&
				   ::fast_io::operations::decay::
					   print_semantic_extended_bounded_passive_companion_impl<
						   char_type, child_expression>();
		}
		else
		{
			constexpr bool explicitly_passive = requires {
				{
					print_extended_bounded_passive_companion_safe(
						::fast_io::io_reserve_type<char_type, node_type>)
				} -> ::std::same_as<::std::true_type>;
			};
			return ::fast_io::operations::decay::
					   print_semantic_single_pass_bounded_passive_companion<char_type, T> ||
				   (explicitly_passive &&
					::fast_io::reserve_printable<char_type, node_type> &&
					requires(node_expression node, char_type *iter) {
#if defined(__HERBCEPTIONS__)
						requires(!throws((print_reserve_define(
							::fast_io::io_reserve_type<char_type, node_type>, iter, node))));
#endif
						{
							print_reserve_define(
								::fast_io::io_reserve_type<char_type, node_type>, iter, node)
						} noexcept -> ::std::same_as<char_type *>;
					});
		}
	}
}

template <typename char_type, typename T>
concept print_semantic_extended_bounded_passive_companion =
	::fast_io::operations::decay::
		print_semantic_extended_bounded_passive_companion_impl<char_type, T>();

template <::std::integral char_type, typename T>
inline constexpr bool print_semantic_extended_bounded_component_v =
	::fast_io::operations::decay::print_semantic_single_pass_bounded_source<char_type, T> ||
	::fast_io::operations::decay::print_semantic_extended_bounded_passive_companion<
		char_type, T>;

/// @brief Recognizes a passive component whose object-dependent bound is cheap and non-fatal.
/// @details The extended companion proof includes semantic width: if its child length L is bounded by B, the complete
///          field length max(W,L) is bounded by max(W,B). The preflight may therefore read W without formatting the
///          child. An object-dependent conversion source is excluded so this large-record protocol stays disjoint from
///          the dynamic-float path, while every admitted define chain remains non-throwing after a successful preflight.
template <typename char_type, typename T>
concept print_semantic_large_passive_bounded_component =
	::std::integral<char_type> &&
	!::fast_io::operations::decay::print_semantic_single_pass_bounded_source<char_type, T> &&
	::fast_io::operations::decay::print_semantic_extended_bounded_passive_companion<
		char_type, T>;

/// @brief Recognizes the first measured all-passive component-count band with severe frontend scaling costs.
/// @details Twenty complex integer fields lower to 41 components, the first measured point with excessive compile
///          time and memory consumption. Twenty-four through thirty-two fields lower to 49--65 components, where
///          affected audited frontends hit their default template-depth limit. A linear non-formatting preflight
///          prevents this closed band from instantiating the generic whole-run planner; the upper endpoint limits both
///          the preflight fold and unchecked emitter expansion to the measured 32-field requirement.
template <::std::integral char_type, typename... Args>
inline consteval bool print_semantic_large_passive_bounded_run() noexcept
{
	if constexpr (sizeof...(Args) < 41u || 65u < sizeof...(Args))
	{
		return false;
	}
	else
	{
		return (::fast_io::operations::decay::print_semantic_large_passive_bounded_component<
					char_type, Args> &&
				...);
	}
}

/// @brief Recognizes the output-independent structure of one audited bounded run.
/// @details The source proof and passive-companion whitelist describe formatting semantics, not destination storage.
///          Keeping that proof separate lets a writable put area and a compact stream-authorized stack frame reuse the
///          same one-pass conversion without letting either destination capability broaden source admission.
template <::std::integral char_type, typename... Args>
inline consteval bool print_semantic_single_pass_bounded_run() noexcept
{
	if constexpr (sizeof...(Args) == 0u || 8u < sizeof...(Args))
	{
		return false;
	}
	else
	{
		return (::fast_io::operations::decay::print_semantic_single_pass_bounded_source<
					char_type, Args> ||
				...) &&
			   (::fast_io::operations::decay::print_semantic_single_pass_bounded_component_v<
					char_type, Args> &&
				...);
	}
}

/// @brief Recognizes only larger audited runs within the extended put-area budget.
/// @details The size checks precede every capability name so a compact run or a run beyond the cap cannot instantiate
///          any extended companion, capacity, or emitter machinery. This keeps both excluded regions on their original
///          type graph and generated code. The upper endpoint is audited separately for compile RSS and text growth.
template <::std::integral char_type, typename... Args>
inline consteval bool print_semantic_extended_bounded_run() noexcept
{
	if constexpr (sizeof...(Args) <= 8u || 64u < sizeof...(Args))
	{
		return false;
	}
	else
	{
		return (::fast_io::operations::decay::print_semantic_single_pass_bounded_source<
					char_type, Args> ||
				...) &&
			   (::fast_io::operations::decay::print_semantic_extended_bounded_component_v<
					char_type, Args> &&
				...);
	}
}

/// @brief Selects the narrow direct-put-area optimization for a small audited semantic run.
/// @details At least one marked source supplies the profitability reason.  The eight-component ceiling bounds template
///          growth while covering compact formatted records; larger and structurally richer runs retain the established
///          precise/bounded strategy order unchanged.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_single_pass_bounded_put_area_run() noexcept
{
	if constexpr (
		!::fast_io::operations::decay::print_semantic_single_pass_bounded_run<
			char_type, Args...>() ||
		(::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> &&
		 (false || ... ||
		  ::fast_io::details::decay::print_fixed_external_semantic_overstore<
			  char_type, Args>::value)) ||
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
		!::fast_io::deferred_obuffer_commit_safe<char_type, outputstmtype> ||
		!::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
			outputstmtype, char_type>)
	{
		return false;
	}
	else
	{
		return true;
	}
}

/// @brief Selects extended bounded materialization only for an existing safe writable put area.
/// @details This selector is disjoint from the original compact selector and does not authorize stack materialization.
///          A failed capacity probe observes only source bounds and then returns to the unchanged semantic dispatcher.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_extended_bounded_put_area_run() noexcept
{
	if constexpr (!::fast_io::operations::decay::print_semantic_extended_bounded_run<
					  char_type, Args...>() ||
				  (::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> &&
				   (false || ... ||
					::fast_io::details::decay::print_fixed_external_semantic_overstore<
						char_type, Args>::value)))
	{
		return false;
	}
	else
	{
		return ::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
			   ::fast_io::single_pass_bounded_obuffer_materialization_safe<
				   char_type, outputstmtype> &&
			   ::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				   outputstmtype, char_type>;
	}
}

/// @brief Selects a large passive run only for a put area with deferred single-commit semantics.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_large_passive_bounded_put_area_run() noexcept
{
	if constexpr (!::fast_io::operations::decay::print_semantic_large_passive_bounded_run<
					  char_type, Args...>() ||
				  (::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> &&
				   (false || ... ||
					::fast_io::details::decay::print_fixed_external_semantic_overstore<
						char_type, Args>::value)))
	{
		return false;
	}
	else
	{
		return ::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
			   ::fast_io::single_pass_bounded_obuffer_materialization_safe<
				   char_type, outputstmtype> &&
			   ::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				   outputstmtype, char_type>;
	}
}

/// @brief Selects one bounded stack attempt only when the destination already advertises that byte budget.
/// @details Runtime widths make the record bound object-dependent, so the stream's fixed whole-output threshold is the
///          array extent. The preflight below admits emission only when every max(W,B) fits that extent. Requiring the
///          threshold itself to satisfy core's stack policy prevents a generated record from creating an unbounded
///          frame merely to avoid the generic planner. Put-area outputs use the cursor selector above and are excluded.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_large_passive_bounded_stack_run() noexcept
{
	if constexpr (!::fast_io::operations::decay::print_semantic_large_passive_bounded_run<
					  char_type, Args...>() ||
				  ::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t threshold{
			::fast_io::details::decay::print_full_output_coalesce_threshold<
				char_type, outputstmtype>()};
		return threshold != 0u &&
			   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
				   threshold, char_type>;
	}
}

/// @brief Adds one component's cheap capacity requirement to an available put-area prefix.
template <::std::integral char_type, typename T>
	requires ::fast_io::operations::decay::print_semantic_single_pass_bounded_component_v<
		char_type, T>
inline constexpr void print_semantic_single_pass_bounded_add_component(
	::std::size_t &total, ::std::size_t maximum_size, T &value)
{
	if (total == SIZE_MAX || maximum_size < total)
	{
		total = SIZE_MAX;
		return;
	}
	auto &&node_ref{
		::fast_io::details::decay::print_semantic_node_ref(value)};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	auto const remaining{maximum_size - total};
	::std::size_t component_size;
	if constexpr (
		::fast_io::operations::decay::print_semantic_single_pass_bounded_source<
			char_type, T>)
	{
		component_size =
			::fast_io::single_pass_bounded_materialization_size_invoke<char_type>(
				node_ref, remaining);
	}
	else
	{
		component_size = print_reserve_size(
			::fast_io::io_reserve_type<char_type, node_type>);
	}
	if (component_size == SIZE_MAX || remaining < component_size)
	{
		total = SIZE_MAX;
		return;
	}
	total += component_size;
}

/// @brief Adds one component admitted only by the extended put-area protocol.
/// @details Keeping this operation distinct from the compact helper is a code-generation boundary: runs of at most
///          eight components and runs beyond the extended cap never instantiate its semantic-width machinery.
template <::std::integral char_type, typename T>
	requires ::fast_io::operations::decay::print_semantic_extended_bounded_component_v<
		char_type, T>
inline constexpr void print_semantic_extended_bounded_add_component(
	::std::size_t &total, ::std::size_t maximum_size, T &value)
{
	if (total == SIZE_MAX || maximum_size < total)
	{
		total = SIZE_MAX;
		return;
	}
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(value)};
	auto const remaining{maximum_size - total};
	::std::size_t component_size;
	if constexpr (
		::fast_io::operations::decay::print_semantic_single_pass_bounded_source<
			char_type, T>)
	{
		component_size =
			::fast_io::single_pass_bounded_materialization_size_invoke<char_type>(
				node_ref, remaining);
	}
	else
	{
		// Extended passive leaves may be wrapped in semantic widths. The recursive admission proof establishes
		// that this operation returns max(width, child_bound) at each level without formatting the child.
		component_size =
			::fast_io::operations::decay::print_semantic_bounded_size<char_type>(node_ref);
	}
	if (component_size == SIZE_MAX || remaining < component_size)
	{
		total = SIZE_MAX;
		return;
	}
	total += component_size;
}

/// @brief Publishes one already-admitted audited bounded run from a caller-provided put-area cursor.
/// @details GCC otherwise inlines the large dynamic-precision conversion separately into the early probe and the
///          historical bounded fallback. One non-clonable GNU call boundary lets both branches share that formatter;
///          other compilers retain their ordinary optimizer-selected boundary because their floating backend is already
///          outlined.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
			char_type, outputstmtype, Args...>())
#if defined(__GNUC__) && !defined(__clang__)
[[gnu::noinline, gnu::noclone]]
#endif
inline constexpr void print_semantic_emit_single_pass_bounded_put_area(
	outputstmtype &optstm, char_type *curr, Args &&...args)
{
	char_type *const actual_end{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<
			line, char_type, true>(curr, ::std::forward<Args>(args)...)};
	obuffer_set_curr(optstm, actual_end);
}

/// @brief Emits one audited bounded run directly into an existing put area when its cheap upper bound fits.
/// @details A failed attempt has observed the output only through its explicit stable/noexcept put-area opt-in, queried
///          only non-fatal source markers and compile-time reserve extents, and has not written output.  The caller may
///          therefore resume its exact historical precise path.  A successful attempt propagates `bounded == true`
///          through width, so the dynamic-precision float is defined exactly once and any padding move operates on that
///          already-materialized representation in the final destination.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
			char_type, outputstmtype, Args...>())
inline constexpr bool
print_semantic_try_single_pass_bounded_put_area(outputstmtype &optstm, Args &...args)
{
	char_type *const curr{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	// A stable zero-capacity adapter may use null sentinels; even nullptr-nullptr is not a valid pointer subtraction.
	if (curr == nullptr || end == nullptr)
	{
		return false;
	}
	::std::ptrdiff_t const difference{end - curr};
	if (difference < 0)
	{
		return false;
	}
	::std::size_t const available{static_cast<::std::size_t>(difference)};
	::std::size_t total{line ? 1u : 0u};
	(::fast_io::operations::decay::print_semantic_single_pass_bounded_add_component<
		 char_type>(total, available, args),
	 ...);
	if (total == SIZE_MAX)
	{
		return false;
	}
	::fast_io::operations::decay::print_semantic_emit_single_pass_bounded_put_area<
		line, char_type>(optstm, curr, args...);
	return true;
}

/// @brief Publishes one extended bounded run after its complete capacity proof succeeds.
/// @details The source and companion concepts prove that unchecked bounded emission is non-throwing. The destination
///          cursor remains unpublished until every component has been written, so no partially visible run exists.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_extended_bounded_put_area_run<
			char_type, outputstmtype, Args...>())
inline constexpr void print_semantic_emit_extended_bounded_put_area(
	outputstmtype &optstm, char_type *current, Args &...args)
{
	char_type *const actual_end{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<
			line, char_type, true>(current, args...)};
	obuffer_set_curr(optstm, actual_end);
}

/// @brief Attempts the extended run without touching the destination on a capacity miss.
/// @details Each source query is a read-only, non-fatal bound. Passive companions contribute only type-static reserve
///          bounds and semantic max(width, child_bound), so returning false has neither formatted a value nor published
///          a cursor and the caller may enter the historical exact-size path unchanged.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_extended_bounded_put_area_run<
			char_type, outputstmtype, Args...>())
inline constexpr bool
print_semantic_try_extended_bounded_put_area(outputstmtype &optstm, Args &...args)
{
	char_type *const current{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	if (current == nullptr || end == nullptr)
	{
		return false;
	}
	::std::ptrdiff_t const difference{end - current};
	if (difference < 0)
	{
		return false;
	}
	::std::size_t const available{static_cast<::std::size_t>(difference)};
	::std::size_t total{line ? 1u : 0u};
	(::fast_io::operations::decay::print_semantic_extended_bounded_add_component<
		 char_type>(total, available, args),
	 ...);
	if (total == SIZE_MAX)
	{
		return false;
	}
	::fast_io::operations::decay::print_semantic_emit_extended_bounded_put_area<
		line, char_type>(optstm, current, args...);
	return true;
}

/// @brief Commits one large passive run when its complete object-dependent bound fits the current put area.
/// @details Each component contributes either its static reserve maximum or max(runtime_width,child_bound). If their
///          saturated sum fits, actual_size <= sum(bounds)+line proves every unchecked define remains inside the put
///          area. A miss performs no formatting, write, or cursor publication and may refresh or use checked output.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_large_passive_bounded_put_area_run<
			char_type, outputstmtype, Args...>())
inline constexpr bool print_semantic_try_large_passive_bounded_put_area(
	outputstmtype &optstm, Args &...args)
{
	char_type *const current{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	if (current == nullptr || end == nullptr)
	{
		return false;
	}
	::std::ptrdiff_t const difference{end - current};
	if (difference < 0)
	{
		return false;
	}
	::std::size_t const available{static_cast<::std::size_t>(difference)};
	::std::size_t total{line ? 1u : 0u};
	(::fast_io::operations::decay::print_semantic_extended_bounded_add_component<
		 char_type>(total, available, args),
	 ...);
	if (total == SIZE_MAX)
	{
		return false;
	}
	char_type *const actual_end{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<
			line, char_type, true>(current, args...)};
	obuffer_set_curr(optstm, actual_end);
	return true;
}

/// @brief Attempts one large passive run in the destination's fixed whole-output stack budget.
/// @details The preflight computes every object-dependent width bound against the fixed capacity before touching the
///          buffer. Success proves the unchecked ordered concatenation fits and permits one `write_all`; failure leaves
///          the output untouched for checked component emission. Only [buffer,actual_end) is ever observed.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_large_passive_bounded_stack_run<
			line, char_type, outputstmtype, Args...>())
inline constexpr bool print_semantic_try_large_passive_bounded_stack(
	outputstmtype &optstm, Args &...args)
{
	constexpr ::std::size_t capacity{
		::fast_io::details::decay::print_full_output_coalesce_threshold<
			char_type, outputstmtype>()};
	static_assert(capacity != 0u);
	::std::size_t total{line ? 1u : 0u};
	(::fast_io::operations::decay::print_semantic_extended_bounded_add_component<
		 char_type>(total, capacity, args),
	 ...);
	if (total == SIZE_MAX)
	{
		return false;
	}
	char_type buffer[capacity];
	char_type *const actual_end{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<
			line, char_type, true>(buffer, args...)};
	::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, actual_end);
	return true;
}

/// @brief Emits a capacity-rejected large passive run through checked component operations.
/// @details Every admitted component has a non-throwing reserve chain; a semantic width is sent through the ordinary
///          single-component semantic emitter, while a plain leaf uses the no-pack dispatcher. Ordered component output
///          followed by one optional newline preserves the character sequence without recreating a whole-run planner.
///          A fold avoids one continuation type per prefix.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(::fast_io::operations::decay::print_semantic_large_passive_bounded_run<
			 char_type, Args...>())
inline constexpr void print_semantic_emit_large_passive_checked(
	outputstmtype &optstm, Args &...args)
{
	auto values{::fast_io::containers::forward_as_tuple(args...)};
	[&]<::std::size_t... index>(::std::index_sequence<index...>) constexpr {
		auto emit_component = [&]<::std::size_t position, typename T>(T &value) constexpr {
			// The ordinary flat continuation gives line ownership to its final component. Preserve that primitive
			// grouping here: a final literal may append the newline in the same checked write, which also keeps the
			// same partial-prefix and exception boundary on a capacity miss. Index expansion supplies this fact without
			// recursively creating one continuation type for every component prefix.
			constexpr bool component_line{line && position + 1u == sizeof...(Args)};
			if constexpr (
				::fast_io::details::decay::print_semantic_execution_node_v<T>)
			{
				::fast_io::operations::decay::print_semantic_emit<
					component_line, true, char_type>(optstm, value);
			}
			else
			{
				::fast_io::operations::decay::print_freestanding_decay_no_pack<
					component_line>(optstm, value);
			}
		};
		(emit_component.template operator()<index>(
			 ::fast_io::containers::get<index>(values)),
		 ...);
	}(::std::make_index_sequence<sizeof...(Args)>{});
}

/// @brief Maximum compact one-pass frame admitted before exact semantic sizing.
/// @details This is intentionally smaller than a stream's general coalescing threshold.  It covers bounded scalar
///          fields (including binary32 fixed formatting and its width/prefix companions) while keeping the speculative
///          frame below the inline-stack budget on every character width. Larger precision requests retain the mature
///          exact/dynamic path instead of making every caller pay for threshold-sized storage.
template <::std::integral char_type>
inline constexpr ::std::size_t print_semantic_single_pass_bounded_stack_capacity() noexcept
{
	constexpr ::std::size_t requested_bytes{512u};
	return requested_bytes / sizeof(char_type);
}

/// @brief Selects compact stack materialization for an audited run on a stream that explicitly coalesces output.
template <::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_single_pass_bounded_stack_run() noexcept
{
	constexpr ::std::size_t capacity{
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_capacity<char_type>()};
	constexpr ::std::size_t threshold{
		::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>()};
	return capacity != 0u && capacity <= threshold &&
		   ::fast_io::operations::decay::print_semantic_single_pass_bounded_run<char_type, Args...>();
}

/// @brief Maximum stack extent that may remain inline in a semantic strategy dispatcher.
/// @details This is a byte-based code-generation limit, not an output-admission limit. Semantic condition selection
///          creates several mutually exclusive template instantiations. If every instantiation inlines a large local
///          buffer, compilers conservatively reserve all lifetimes in the common caller and emit stack-page probes for
///          storage that the selected branch never uses. One KiB keeps compact fields inline; larger buffers cross an
///          explicit call boundary while retaining the stream's original coalescing threshold.
template <::std::integral char_type>
inline constexpr ::std::size_t print_semantic_inline_stack_max_size() noexcept
{
	constexpr ::std::size_t requested_bytes{1024u};
	return requested_bytes / sizeof(char_type);
}

/// @brief Materializes one semantic run in a branch-local large stack frame and writes the produced prefix.
/// @details The caller proves that `buffer_size` is either an exact size or an upper bound for the selected run. The
///          bounded emitter may return before the end of that capacity; only the returned prefix is observable. This
///          function is deliberately not inlined: its frame belongs only to the chosen condition branch, preventing
///          mutually exclusive threshold-sized arrays from being summed in the outer dispatcher. The motivating
///          GCC/x86-64 record composition placed four alternative 64-KiB buffers in one approximately 264-KiB frame;
///          the call boundary reduced that frame without changing the stream's advertised threshold.
template <bool line, ::std::integral char_type, bool bounded, ::std::size_t buffer_size,
		  typename outputstmtype, typename... Args>
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#elif __has_cpp_attribute(msvc::noinline)
[[msvc::noinline]]
#endif
inline constexpr void print_semantic_materialize_large_stack_and_write(outputstmtype &optstm, Args &&...args)
{
	static_assert(buffer_size != 0u);
	char_type buffer[buffer_size];
	char_type *const iter{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, bounded>(
			buffer, ::std::forward<Args>(args)...)};
	if (iter != buffer)
	{
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, iter);
	}
}

/// @brief Materializes a semantic run using either an inline compact frame or an isolated large frame.
/// @details The two implementations have identical capacity and cursor postconditions. They are separate solely to
///          constrain generated stack lifetimes: an inline compact frame avoids call overhead, whereas a large frame
///          must not contaminate every alternative produced by nested condition selection.
template <bool line, ::std::integral char_type, bool bounded, ::std::size_t buffer_size,
		  typename outputstmtype, typename... Args>
inline constexpr void print_semantic_materialize_stack_and_write(outputstmtype &optstm, Args &&...args)
{
	static_assert(buffer_size != 0u);
	constexpr ::std::size_t inline_limit{
		::fast_io::operations::decay::print_semantic_inline_stack_max_size<char_type>()};
	if constexpr (inline_limit != 0u && buffer_size <= inline_limit)
	{
		char_type buffer[buffer_size];
		char_type *const iter{
			::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, bounded>(
				buffer, ::std::forward<Args>(args)...)};
		if (iter != buffer)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, iter);
		}
	}
	else
	{
		::fast_io::operations::decay::print_semantic_materialize_large_stack_and_write<
			line, char_type, bounded, buffer_size>(optstm, ::std::forward<Args>(args)...);
	}
}

/// @brief Stages a bounded semantic run before committing it to caller-visible fixed storage.
/// @details An audited reserve formatter may initialize scratch anywhere inside its advertised bound and return a
///          shorter logical spelling.  That is safe in library-owned staging storage but not in an external view whose
///          bytes after the published cursor remain observable.  The run-time bound selects a compact common stack
///          frame or exact dynamic storage; only the returned prefix reaches the destination.  A bound larger than the
///          remaining view is still permitted when the actual spelling fits, preserving fixed-view exact-fit behavior.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
inline constexpr void print_semantic_materialize_fixed_external_bounded_and_write(
	outputstmtype &optstm, ::std::size_t required, Args &&...args)
{
	if (required == 0u)
	{
		return;
	}
	constexpr ::std::size_t stack_capacity{
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_capacity<
			char_type>()};
	if (required <= stack_capacity)
	{
		char_type buffer[stack_capacity];
		char_type *const actual_end{
			::fast_io::operations::decay::print_semantic_emit_unchecked_run<
				line, char_type, true>(buffer, ::std::forward<Args>(args)...)};
		if (actual_end < buffer || buffer + required < actual_end) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		if (actual_end != buffer)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(
				optstm, buffer, actual_end);
		}
	}
	else
	{
		::fast_io::details::buffer_alloc_arr_ptr<char_type, false> buffer(required);
		char_type *const first{buffer.get()};
		char_type *const actual_end{
			::fast_io::operations::decay::print_semantic_emit_unchecked_run<
				line, char_type, true>(first, ::std::forward<Args>(args)...)};
		if (actual_end < first || first + required < actual_end) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		if (actual_end != first)
		{
			::fast_io::operations::decay::write_all_decay_dispatch(
				optstm, first, actual_end);
		}
	}
}

/// @brief Attempts one compact audited materialization before invoking an expensive exact-size protocol.
/// @details Only non-fatal source-specific bounds are queried. Failure has not touched the stream and rejoins the
///          established precise path. Success formats each source once into a capacity-proven local range and performs
///          one contiguous write, so a small dynamic floating field cannot instantiate or execute the large precise
///          decimal metadata machinery merely to learn a length that the cheap bound already covers.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_run<
			char_type, outputstmtype, Args...>())
/// @brief Owns the successful direct-stream conversion frame after the cheap capacity probe.
/// @details GCC otherwise expands the complete decimal conversion into every direct-stream caller.  Keeping only this
///          already-selected arm non-clonable reduced the measured GCC 15 wrapper from 7.8 KiB to 129 bytes without
///          changing the capacity query or adding work to a rejected run.  Clang keeps its existing inlining decision.
#if defined(__GNUC__) && !defined(__clang__)
[[gnu::noinline, gnu::noclone]]
#endif
inline constexpr void
print_semantic_emit_single_pass_bounded_stack(outputstmtype &optstm,
											  Args &...args)
{
	constexpr ::std::size_t capacity{
		::fast_io::operations::decay::
			print_semantic_single_pass_bounded_stack_capacity<char_type>()};
	char_type buffer[capacity];
	char_type *const iter{
		::fast_io::operations::decay::print_semantic_emit_unchecked_run<
			line, char_type, true>(buffer, args...)};
	if (iter != buffer)
	{
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, iter);
	}
}

template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_run<
			char_type, outputstmtype, Args...>())
inline constexpr bool
print_semantic_try_single_pass_bounded_stack(outputstmtype &optstm, Args &...args)
{
	constexpr ::std::size_t capacity{
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_capacity<char_type>()};
	::std::size_t total{line ? 1u : 0u};
	(::fast_io::operations::decay::print_semantic_single_pass_bounded_add_component<
		 char_type>(total, capacity, args),
	 ...);
	if (total == SIZE_MAX)
	{
		return false;
	}
	::fast_io::operations::decay::print_semantic_emit_single_pass_bounded_stack<
		line, char_type>(optstm, args...);
	return true;
}

/// @brief    Attempts one-pass coalescing from a compile-time semantic upper bound.
/// @details  This path intentionally precedes precise coalescing. When the complete run has a static reserve bound,
///           it can emit directly with reserve protocols and avoid a separate exact-size traversal. The path is used
///           only when an existing output area or the stream's coalescing threshold can hold the proven maximum.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the semantic run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   bool          true when this path emitted the run or proved it empty
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_semantic_try_static_bounded_coalesce(outputstmtype &optstm, Args &&...args)
{
	// Forwarded semantic types determine whether every element contributes a compile-time reserve maximum.
	constexpr ::std::size_t static_bound{
		::fast_io::operations::decay::print_semantic_static_bounded_total_size<
			line, char_type,
			::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
	if constexpr (static_bound == SIZE_MAX)
	{
		// At least one element lacks a static upper bound, so precise or run-time bounded strategies must decide.
		return false;
	}
	else if constexpr (static_bound == 0u)
	{
		// An empty non-line run is complete without touching the output stream.
		return true;
	}
	else
	{
		// A non-zero compile-time bound can be consumed either by the stream's put area or by a fixed stack frame.
		if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
		{
			// Buffered streams avoid a temporary frame when their current put area contains the entire reserve bound.
			char_type *const curr{obuffer_curr(optstm)};
			char_type *const end{obuffer_end(optstm)};
			if (curr != nullptr && end != nullptr &&
				static_cast<::std::size_t>(end - curr) >= static_bound)
			{
				// The static capacity proof permits bounded leaf emission directly into the stream buffer.
				char_type *const iter{
					::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, true>(
						curr, ::std::forward<Args>(args)...)};
				obuffer_set_curr(optstm, iter);
				return true;
			}
		}
		constexpr ::std::size_t threshold_chars{
			::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>()};
		if constexpr (threshold_chars != 0u && static_bound <= threshold_chars)
		{
			// The proven bound selects one branch-local frame; unused reserve capacity is never written.
			::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
				line, char_type, true, static_bound>(optstm, ::std::forward<Args>(args)...);
			// A zero-byte result is still successfully handled because the non-zero bound is only a maximum.
			return true;
		}
		// Neither available output storage nor the coalescing policy can hold the static reserve frame.
		return false;
	}
}

/// @brief    Attempts to coalesce a bounded semantic print run into one contiguous output operation.
/// @details  This path admits dynamic-reserve leaves by allocating for their upper bound and writing only the prefix
///           ending at the returned cursor. Unused reserve capacity is therefore never observable. Existing put-area,
///           fixed stack, and independently admitted dynamic storage remain distinct cost policies.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   bool          true when the whole run was emitted by this coalescing path
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_semantic_try_bounded_coalesce(outputstmtype &optstm, Args &&...args)
{
	if constexpr ((::fast_io::details::decay::print_semantic_bounded_size_ok<
					   char_type,
					   ::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>>::value &&
				   ...))
	{
		// A bounded run can be materialized once when its upper bound fits the available coalescing space.
		::std::size_t const required{
			::fast_io::operations::decay::print_semantic_bounded_total_size<line, char_type>(args...)};
		if (required == SIZE_MAX) [[unlikely]]
		{
			// Every leaf remains valid; only the optional aggregate buffer is unrepresentable. The caller will emit
			// the semantic graph through its ordinary sequential strategy.
			return false;
		}
		if (required == 0)
		{
			// The selected semantic branch emits no bytes, so the bounded path completes without an output operation.
			return true;
		}
		if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
		{
			// Buffered streams can use the existing put area when it can hold the upper bound.
			char_type *const curr{obuffer_curr(optstm)};
			char_type *const end{obuffer_end(optstm)};
			if (curr != nullptr && end != nullptr &&
				static_cast<::std::size_t>(end - curr) >= required)
			{
				// The put area was checked against the upper bound, so reserve-based bounded emission is safe in place.
				if constexpr (
					::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
						char_type, outputstmtype, Args...>())
				{
					// Audited expensive sources share the same bounded conversion body with the earlier cheap-bound probe.
					::fast_io::operations::decay::print_semantic_emit_single_pass_bounded_put_area<
						line, char_type>(optstm, curr, ::std::forward<Args>(args)...);
				}
				else
				{
					char_type *const iter{
						::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, true>(
							curr, ::std::forward<Args>(args)...)};
					obuffer_set_curr(optstm, iter);
				}
				return true;
			}
		}
		constexpr ::std::size_t threshold_chars{
			::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>()};
		if constexpr (threshold_chars != 0)
		{
			constexpr ::std::size_t static_bound{
				::fast_io::operations::decay::print_semantic_static_bounded_total_size<
					line, char_type,
					::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
			if constexpr (static_bound != SIZE_MAX && static_bound <= threshold_chars)
			{
				// Statically bounded runs use the proven maximum instead of the stream threshold as frame size.
				if (required <= static_bound)
				{
					// Materialize with the same static-bound policy used to size this branch-local frame.
					constexpr ::std::size_t buffer_size{static_bound == 0 ? 1u : static_bound};
					::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
						line, char_type, true, buffer_size>(optstm, ::std::forward<Args>(args)...);
					return true;
				}
			}
			else
			{
				// Runs without a static upper bound preserve the historical threshold-sized stack buffer.
				if (required <= threshold_chars)
				{
					// The threshold-sized branch frame contains the run-time upper bound.
					::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
						line, char_type, true, threshold_chars>(optstm, ::std::forward<Args>(args)...);
					return true;
				}
			}
		}
		constexpr ::std::size_t materialize_limit{
			::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
				char_type, outputstmtype,
				::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
		if constexpr (materialize_limit != 0u)
		{
			if (required <= materialize_limit)
			{
				// Dynamic coalescing has its own stream policy; it never inherits a stack-capacity constant.
				::fast_io::details::buffer_alloc_arr_ptr<char_type, false> buffer(required);
				char_type *const first{buffer.get()};
				char_type *const last{
					::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, true>(
						first, ::std::forward<Args>(args)...)};
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
				return true;
			}
		}
	}
	// At least one requirement for bounded coalescing failed, so the caller must use the normal emit path.
	return false;
}

/// @brief    Attempts to coalesce a precise semantic print run into one contiguous output operation.
/// @details  Storage selection has three independent stages. An existing put area is free of temporary allocation; a
///           stream-declared full-output threshold admits bounded stack materialization; and a separate dynamic
///           threshold may admit exact heap materialization. The heap stage never inherits the stack threshold, so a
///           large allocation policy cannot silently enlarge every caller's frame.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   bool          true when the whole run was emitted by this coalescing path
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_semantic_try_precise_coalesce(outputstmtype &optstm, Args &&...args)
{
	if constexpr ((::fast_io::details::decay::print_semantic_precise_size_ok<
					   char_type,
					   ::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>>::value &&
				   ...))
	{
		// A fully precise run can be measured before choosing a coalesced output strategy.
		::std::size_t const required{
			::fast_io::operations::decay::print_semantic_precise_total_size<line, char_type>(
				args...)};
		if (required == SIZE_MAX) [[unlikely]]
		{
			// Exact components can still be written separately even though no single temporary range can contain them.
			return false;
		}
		if (required == 0)
		{
			// An empty precise run completes without touching the output stream.
			return true;
		}
		if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
		{
			// Buffered streams can coalesce directly into their current put area when enough space remains.
			char_type *const curr{obuffer_curr(optstm)};
			char_type *const end{obuffer_end(optstm)};
			if (curr != nullptr && end != nullptr &&
				static_cast<::std::size_t>(end - curr) >= required)
			{
				// The existing output buffer has enough room, so emit in place and advance the stream cursor once.
				char_type *const iter{
					::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type>(
						curr, ::std::forward<Args>(args)...)};
				obuffer_set_curr(optstm, iter);
				return true;
			}
		}
		constexpr ::std::size_t threshold_chars{
			::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>()};
		if constexpr (threshold_chars != 0)
		{
			// A non-zero full-output threshold permits bounded stack-buffer coalescing.
			constexpr ::std::size_t static_total{
				::fast_io::operations::decay::print_semantic_static_precise_total_size<line, char_type,
																					   ::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
			if constexpr (static_total != SIZE_MAX && static_total <= threshold_chars)
			{
				// A compile-time precise run uses an exactly-sized branch frame and one grouped write dispatch.
				constexpr ::std::size_t buffer_size{static_total == 0 ? 1u : static_total};
				::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
					line, char_type, false, buffer_size>(optstm, ::std::forward<Args>(args)...);
				return true;
			}
			else
			{
				constexpr ::std::size_t static_bound{
					::fast_io::operations::decay::print_semantic_static_bounded_total_size<
						line, char_type,
						::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
				if constexpr (static_bound != SIZE_MAX && static_bound <= threshold_chars)
				{
					// Unequal static alternatives are not precise as a type, but their maximum still bounds the frame.
					if (required <= static_bound)
					{
						constexpr ::std::size_t buffer_size{static_bound == 0 ? 1u : static_bound};
						::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
							line, char_type, false, buffer_size>(optstm, ::std::forward<Args>(args)...);
						return true;
					}
				}
				else if (required <= threshold_chars)
				{
					// Non-static precise runs use the isolated threshold-sized branch frame.
					::fast_io::operations::decay::print_semantic_materialize_stack_and_write<
						line, char_type, false, threshold_chars>(optstm, ::std::forward<Args>(args)...);
					return true;
				}
			}
		}
		constexpr ::std::size_t materialize_limit{
			::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
				char_type, outputstmtype,
				::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
		if constexpr (materialize_limit != 0u)
		{
			if (required <= materialize_limit)
			{
				// Exact dynamic storage avoids coupling heap materialization to the stream's fixed stack threshold.
				::fast_io::details::buffer_alloc_arr_ptr<char_type, false> buffer(required);
				char_type *const first{buffer.get()};
				char_type *const last{
					::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type>(
						first, ::std::forward<Args>(args)...)};
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
				return true;
			}
		}
	}
	// At least one requirement for precise coalescing failed, so the caller must use the normal emit path.
	return false;
}

/// @brief    Tests whether precise coalescing matches the composition's cheapest strategy.
/// @details  Width-bearing runs with no contiguous target avoid a duplicate outer measurement. Long statically
///           bounded packs continue to bounded materialization and use the returned cursor as their actual length.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the semantic run
/// @return   bool          true when the caller should attempt precise coalescing
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_precise_coalesce_strategy_selected() noexcept
{
	// Compile-time structure determines whether invoking an exact-size traversal can improve this output path.
	constexpr ::std::size_t coalesce_threshold{
		::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>()};
	constexpr ::std::size_t dynamic_coalesce_threshold{
		::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
			char_type, outputstmtype,
			::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
	constexpr bool contains_width{
		(::fast_io::details::decay::print_semantic_contains_width<
			 ::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>>::value ||
		 ...)};
	constexpr ::std::size_t semantic_leaf_count{
		::fast_io::details::decay::print_semantic_leaf_count_sum<
			::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
	constexpr ::std::size_t run_static_bound{
		::fast_io::operations::decay::print_semantic_static_bounded_total_size<
			line, char_type,
			::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>...>()};
	using staged_group = ::fast_io::operations::decay::print_semantic_staged_group<
		char_type,
		::std::remove_cvref_t<
			::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, Args>>...>;
	if constexpr (staged_group::available)
	{
		// Exact-size emission intentionally passes `bounded == false` and therefore disables the staged formatter.
		// A compatible staged group already has an ordinary upper-bound proof, so select the bounded strategy instead.
		return false;
	}
	else if constexpr (!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
					   coalesce_threshold == 0u && dynamic_coalesce_threshold == 0u && contains_width)
	{
		// Width performs its own exact measurement, so an unavailable outer coalescing target must not measure first.
		return false;
	}
	else if constexpr (
		contains_width &&
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
		(::fast_io::details::decay::print_semantic_bounded_size_ok<
			 char_type,
			 ::fast_io::details::decay::print_semantic_forwarded_arg_t<
				 char_type, Args>>::value &&
		 ...))
	{
		// A writable put area turns an inexpensive width bound into the capacity proof for one-pass child emission.
		// Exact sizing would traverse a reserve-bounded scalar only to repeat its conversion during materialization.
		return false;
	}
	else if constexpr (
		semantic_leaf_count >=
			::fast_io::details::decay::print_semantic_precise_materialization_leaf_threshold &&
		run_static_bound != SIZE_MAX)
	{
		// A long bounded run generates each leaf once and discards unused reserve capacity through its returned cursor.
		return false;
	}
	else
	{
		// Compact or unbounded compositions retain exact-size coalescing and its allocation/write-count advantages.
		return true;
	}
}

/// @brief Proves the whole-record-first linear plan for fixed conditions separated by run-time widths.
/// @details Provider obligations are the existing closed-scatter and barrier-stream proofs. The consumer additionally
///          establishes all strategy facts which active-record enumeration would otherwise rediscover:
///
///          1. every condition arm is a null identity or immutable finite descriptor, so its cached contiguous adapter
///             has exactly the selected arm's characters, precise size, bounded size, lifetime, and no callback;
///          2. every admitted width has an immutable fixed child but no type-static whole-field bound, so every active
///             record rejects static coalescing and retains both the precise and bounded run-time attempts;
///          3. at least nine mandatory arguments make both compact single-pass probes false for every selection, while
///             a retained width makes the run semantic and prevents whole-record compiler-constant materialization;
///          4. the adapted record is precise-size capable, is not a staged group, and has more than one argument, so
///             fixed and dynamic whole-output policies are identical to those of every active record; and
///          5. if all unchanged whole-record attempts fail without output, each scanned width is exactly the boundary
///             reached by ordinary flat fallback and every intervening condition segment has its independent proof.
///
///          These obligations preserve characters, primitive operation boundaries, exception-visible prefixes,
///          allocation policy, line ownership, and every child-owned optimizer-constant path without instantiating the
///          Cartesian product of condition arms.
template <bool line, ::std::integral char_type, typename output,
		  typename... Args>
inline consteval bool
print_semantic_optional_scatter_width_coalescing_plan_available() noexcept
{
#if !FAST_IO_HAS_BUILTIN(__builtin_constant_p) ||                   \
	(defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	constexpr ::std::size_t selection_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::
				 print_semantic_optional_fixed_scatter_argument_v<
					 char_type, Args>) +
		 ... + 0u)};
	constexpr ::std::size_t mandatory_count{
		sizeof...(Args) - selection_count};
	if constexpr (selection_count < 4u || mandatory_count < 9u)
	{
		// The width strategy cannot be profitable below either cardinality. Keep rejected records outside the recursive
		// segment scan and adapted coalescing proof graph.
		return false;
	}
	else
	{
		constexpr ::std::size_t width_count{
			(static_cast<::std::size_t>(
				 ::fast_io::details::decay::
					 print_semantic_optional_scatter_coalescing_width_argument_v<
						 output, char_type, Args>) +
			 ... + 0u)};
		if constexpr (width_count == 0u)
		{
			return false;
		}
		else
		{
			using scan =
				::fast_io::details::decay::
					print_semantic_optional_scatter_width_coalescing_scan<
						line, char_type, output,
						::fast_io::details::decay::
							print_semantic_active_record_type_list<>,
						::fast_io::details::decay::
							print_semantic_active_record_type_list<Args...>>;
			if constexpr (!scan::has_linear_segment || !scan::value)
			{
				return false;
			}
			else
			{
				constexpr bool precise_adapted_record{
					(::fast_io::details::decay::print_semantic_precise_size_ok<
						 char_type,
						 ::fast_io::details::decay::print_semantic_forwarded_arg_t<
							 char_type,
							 ::fast_io::details::decay::
								 print_semantic_optional_coalescing_adapted_t<
									 char_type, Args> &>>::value &&
					 ...)};
				using adapted_staged_group =
					::fast_io::operations::decay::print_semantic_staged_group<
						char_type,
						::std::remove_cvref_t<
							::fast_io::details::decay::print_semantic_forwarded_arg_t<
								char_type,
								::fast_io::details::decay::
									print_semantic_optional_coalescing_adapted_t<
										char_type, Args> &>>...>;
				constexpr bool has_whole_output_target{
					::fast_io::details::decay::
							print_full_output_coalesce_threshold<
								char_type, output>() != 0u ||
					::fast_io::details::decay::
							print_full_output_dynamic_coalesce_threshold<
								char_type, output>() != 0u};
				return precise_adapted_record &&
					   !adapted_staged_group::available &&
					   has_whole_output_target &&
					   ::fast_io::semantic_optional_scatter_barrier_plan_stream<
						   char_type, output> &&
					   !::fast_io::operations::decay::defines::
						   has_status_print_define<line, output, Args...>;
			}
		}
	}
#else
	(void)line;
	(void)sizeof(char_type);
	(void)sizeof(output);
	(void)sizeof...(Args);
	return false;
#endif
}

/// @brief    Emits a precisely measured width child without repeating a leaf-level size calculation.
/// @details  A direct precise-reserve leaf receives the length already computed by the width dispatcher. Composite
///           children retain ordinary semantic emission because their aggregate length cannot describe each leaf.
/// @tparam   char_type the character type of the destination buffer
/// @tparam   T         the width child input type
/// @param    iter      a pointer to the next output position
/// @param    len       the previously measured complete child length
/// @param    t         the width child to emit
/// @return   char_type* a pointer one past the emitted child
template <::std::integral char_type, typename T>
inline constexpr char_type *print_semantic_emit_prepared_width_child(char_type *iter, ::std::size_t len, T &&t)
{
	decltype(auto) forwarded{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(::std::forward<T>(t))};
	using value_type = ::std::remove_cvref_t<decltype(forwarded)>;
	if constexpr (!::fast_io::details::decay::print_semantic_execution_node_v<value_type> &&
				  ::fast_io::precise_reserve_printable<char_type, value_type>)
	{
		// A measured leaf can pass its exact extent directly to the precise define customization.
		if constexpr (requires {
						  {
							  print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter, len,
														   forwarded)
						  } -> ::std::same_as<char_type *>;
					  })
		{
			// Pointer-returning precise define operations report the actual next output position. The exact expression in
			// the requires-clause is repeated here so overload selection cannot drift between admission and execution.
			return print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter, len,
												forwarded);
		}
		else
		{
			// Void-returning precise define operations advance by the previously measured exact length.
			print_reserve_precise_define(::fast_io::io_reserve_type<char_type, value_type>, iter, len,
										 forwarded);
			return iter + len;
		}
	}
	else
	{
		// Composite and non-precise children use the regular semantic dispatcher.
		return ::fast_io::operations::decay::print_semantic_emit_unchecked<char_type, false>(
			iter, forwarded);
	}
}

/// @brief    Emits a width-formatted semantic child using the most direct available output path.
/// @details  The function first tries the stream put area, then a small stack buffer, and finally a streaming fallback
///           that writes padding and child output in sequence.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the child reference type
/// @param    optstm        the output stream reference
/// @param    reference     the child node or leaf to format
/// @param    width         the requested minimum field width
/// @param    fillch        the padding character
/// @param    placement_code the normalized width placement code
/// @param    len           the precise child length
template <bool line, ::std::integral char_type, typename outputstmtype, typename T>
inline constexpr void print_semantic_emit_width_direct(outputstmtype &optstm, T &&reference, ::std::size_t width,
													   char_type fillch, ::std::size_t placement_code,
													   ::std::size_t len)
{
	if constexpr (::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
	{
		// Buffered streams first try to build the entire width-formatted field in the existing put area.
		::std::size_t required{(width <= len || placement_code == 0u) ? len : width};
		if constexpr (line)
		{
			// The line variant needs one additional output slot for the trailing newline.
			required = ::fast_io::details::intrinsics::add_or_overflow_die(required, static_cast<::std::size_t>(1u));
		}
		char_type *const curr{obuffer_curr(optstm)};
		char_type *const end{obuffer_end(optstm)};
		if (curr != nullptr && end != nullptr &&
			static_cast<::std::size_t>(end - curr) >= required)
		{
			// The stream put area can hold the complete field, so emit directly and commit once.
			char_type *iter{curr};
			if (width <= len || placement_code == 0u)
			{
				// Direct-buffer width is already satisfied, so only the child output is emitted.
				iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(iter, len,
																										 reference);
			}
			else
			{
				// Direct-buffer padding is required before the formatted field can be committed.
				::std::size_t const padding{width - len};
				if (placement_code == 1u)
				{
					// Direct-buffer left placement appends all padding after the child output.
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
					iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
				}
				else if (placement_code == 2u)
				{
					// Direct-buffer center placement emits left padding, child output, then right padding.
					::std::size_t const left_padding{padding >> 1u};
					::std::size_t const right_padding{padding - left_padding};
					iter = ::fast_io::details::my_fill_n(iter, left_padding, fillch);
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
					iter = ::fast_io::details::my_fill_n(iter, right_padding, fillch);
				}
				else if (placement_code == 4u)
				{
					// Direct-buffer internal placement inserts padding after the child's sign or prefix.
					::std::size_t const internal_len{
						::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(reference)};
					if (internal_len == 0)
					{
						// Without an internal shift point, internal placement falls back to right placement.
						iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
						iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
							iter, len, reference);
					}
					else
					{
						// A valid internal shift point lets the already-emitted suffix move right to make room.
						char_type *const first{iter};
						char_type *const last{
							::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
								iter, len, reference)};
						if (len < internal_len)
						{
							// A run-time shift outside the emitted child cannot be an insertion point. Preserve width's
							// minimum-field contract by applying the documented right-placement fallback, matching the
							// unchecked semantic path instead of silently dropping all requested padding.
							::fast_io::details::my_copy_right_shift(first, last, padding);
							::fast_io::details::my_fill_n(first, padding, fillch);
							iter = first + width;
						}
						else
						{
							// The suffix is shifted right and the internal padding is filled in place.
							char_type *const shift_pos{first + internal_len};
							::fast_io::details::my_copy_right_shift(shift_pos, last, padding);
							::fast_io::details::my_fill_n(shift_pos, padding, fillch);
							iter = first + width;
						}
					}
				}
				else
				{
					// Direct-buffer right placement emits all padding before the child output.
					iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
				}
			}
			if constexpr (line)
			{
				// The line variant terminates the completed field with a newline.
				*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
			}
			obuffer_set_curr(optstm, iter);
			return;
		}
	}
	::std::size_t required{(width <= len || placement_code == 0u) ? len : width};
	if constexpr (line)
	{
		// The fallback sizing path also accounts for the optional trailing newline.
		required = ::fast_io::details::intrinsics::add_or_overflow_die(required, static_cast<::std::size_t>(1u));
	}
	constexpr ::std::size_t small_width_buffer_size{
		(::fast_io::details::decay::print_stack_buffer_max_size<char_type>() < static_cast<::std::size_t>(256u))
			? ::fast_io::details::decay::print_stack_buffer_max_size<char_type>()
			: static_cast<::std::size_t>(256u)};
	if constexpr (small_width_buffer_size != 0u)
	{
		// A non-zero small-width buffer enables a stack-buffer fallback for compact formatted fields.
		if (required <= small_width_buffer_size)
		{
			// Small width-formatted fields use a bounded stack buffer and one stream write.
			char_type buffer[small_width_buffer_size];
			char_type *iter{buffer};
			if (width <= len || placement_code == 0u)
			{
				// Stack-buffer width is already satisfied, so only the child output is emitted.
				iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(iter, len,
																										 reference);
			}
			else
			{
				// Stack-buffer padding is required before the formatted field can be written once.
				::std::size_t const padding{width - len};
				if (placement_code == 1u)
				{
					// Stack-buffer left placement appends all padding after the child output.
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
					iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
				}
				else if (placement_code == 2u)
				{
					// Stack-buffer center placement emits left padding, child output, then right padding.
					::std::size_t const left_padding{padding >> 1u};
					::std::size_t const right_padding{padding - left_padding};
					iter = ::fast_io::details::my_fill_n(iter, left_padding, fillch);
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
					iter = ::fast_io::details::my_fill_n(iter, right_padding, fillch);
				}
				else if (placement_code == 4u)
				{
					// Stack-buffer internal placement inserts padding after the child's sign or prefix.
					::std::size_t const internal_len{
						::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(reference)};
					if (internal_len == 0)
					{
						// Without an internal shift point, internal placement falls back to right placement.
						iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
						iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
							iter, len, reference);
					}
					else
					{
						// A valid internal shift point lets the already-emitted suffix move right to make room.
						char_type *const first{iter};
						char_type *const last{
							::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
								iter, len, reference)};
						if (len < internal_len)
						{
							// An out-of-range insertion point degrades to right placement. The required-size check above
							// proves that the overlapping shift and leading fill remain inside this stack buffer.
							::fast_io::details::my_copy_right_shift(first, last, padding);
							::fast_io::details::my_fill_n(first, padding, fillch);
							iter = first + width;
						}
						else
						{
							// The suffix is shifted right and the internal padding is filled in place.
							char_type *const shift_pos{first + internal_len};
							::fast_io::details::my_copy_right_shift(shift_pos, last, padding);
							::fast_io::details::my_fill_n(shift_pos, padding, fillch);
							iter = first + width;
						}
					}
				}
				else
				{
					// Stack-buffer right placement emits all padding before the child output.
					iter = ::fast_io::details::my_fill_n(iter, padding, fillch);
					iter = ::fast_io::operations::decay::print_semantic_emit_prepared_width_child<char_type>(
						iter, len, reference);
				}
			}
			if constexpr (line)
			{
				// The line variant terminates the stack-buffered field with a newline.
				*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
			}
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, iter);
			return;
		}
	}
	using forwarded_reference_type = ::std::remove_cvref_t<
		::fast_io::details::decay::print_semantic_stable_input_forward_t<
			char_type, decltype((reference))>>;
	constexpr bool passive_scatter_child{
		::std::same_as<forwarded_reference_type, ::fast_io::basic_io_scatter_t<char_type>>};
	auto emit_forwarded_child = [&](auto &output) constexpr {
		decltype(auto) forwarded{
			::fast_io::details::decay::print_semantic_input_forward<char_type>(reference)};
		// Width sizing and every contiguous emitter observe this same named post-forwarding category. Marking it already
		// forwarded prevents an alias result which is itself semantic from entering status forwarding a second time.
		::fast_io::operations::decay::print_semantic_emit<false, true, char_type>(
			output, forwarded);
	};
	auto emit_stream_child = [&]() constexpr {
		if constexpr (passive_scatter_child)
		{
			if (len == 0u)
			{
				// A basic scatter is only a pointer/length value and has no define callback to observe. Once its measured
				// length is zero, calling the generic stream path can only manufacture an empty write system call. This
				// optimization is intentionally not generalized to arbitrary zero-length printable customizations, whose
				// define operation may carry user-visible behavior beyond producing characters.
				return;
			}
		}
		emit_forwarded_child(optstm);
	};
	if (width <= len || placement_code == 0u)
	{
		// Streaming fallback width is already satisfied, so emit the child directly to the stream.
		emit_stream_child();
	}
	else
	{
		// Streaming fallback padding is written directly around the child output.
		::std::size_t const padding{width - len};
		if (placement_code == 1u)
		{
			// Streaming fallback left placement writes the child first and then the trailing padding.
			emit_stream_child();
			::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
		}
		else if (placement_code == 2u)
		{
			// Streaming fallback center placement writes leading padding, child output, and trailing padding.
			::std::size_t const left_padding{padding >> 1u};
			::std::size_t const right_padding{padding - left_padding};
			::fast_io::operations::decay::print_semantic_write_fill(optstm, left_padding, fillch);
			emit_stream_child();
			::fast_io::operations::decay::print_semantic_write_fill(optstm, right_padding, fillch);
		}
		else if (placement_code == 4u)
		{
			// Streaming fallback internal placement may need a temporary child buffer for byte insertion.
			::std::size_t const internal_len{
				::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(reference)};
			if (internal_len == 0)
			{
				// Without an internal shift point, internal placement falls back to right placement.
				::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
				emit_stream_child();
			}
			else
			{
				// The child is buffered so padding can be inserted between its internal prefix and suffix.
				::fast_io::basic_dynamic_output_buffer<char_type> buffer;
				decltype(auto) buffer_ref = ::fast_io::operations::output_stream_ref(buffer);
				emit_forwarded_child(buffer_ref);
				char_type const *const first{buffer.begin_ptr};
				char_type const *const last{buffer.curr_ptr};
				if (len < internal_len)
				{
					// An invalid insertion point has no safe prefix/suffix split. Apply the same right-placement
					// fallback as every contiguous strategy so output semantics do not depend on buffer capacity.
					::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
					::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
				}
				else
				{
					// The stream receives the internal prefix, padding, and remaining child suffix in order.
					::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, first + internal_len);
					::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
					::fast_io::operations::decay::write_all_decay_dispatch(optstm, first + internal_len, last);
				}
			}
		}
		else
		{
			// Streaming fallback right placement writes leading padding before the child output.
			::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
			emit_stream_child();
		}
	}
	if constexpr (line)
	{
		// The line variant appends the newline after the streaming fallback field.
		::fast_io::operations::decay::char_put_decay_dispatch(optstm, ::fast_io::char_literal_v<u8'\n', char_type>);
	}
}

/// @brief    Emits every element of a semantic pack through the checked stream emitter.
/// @tparam   line          true when the last emitted element should carry the caller's line policy
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_semantic_emit_node_pack_continuation
{
	// The public normalization frame establishes a lifetime-valid observer; it may preserve a caller-owned lvalue or
	// materialize a value. A reference member lets continuation copies preserve identity without imposing copyability or
	// triviality on the observer itself.
	outputstmtype &optstm;

	/// @brief    Emits all expanded pack elements through the checked semantic emitter.
	/// @tparam   PackArgs the expanded pack element types
	/// @param    pack_args the expanded pack elements
	template <typename... PackArgs>
	inline constexpr void operator()(PackArgs &&...pack_args) const
	{
		::fast_io::operations::decay::print_semantic_emit<line, false, char_type>(
			optstm, ::std::forward<PackArgs>(pack_args)...);
	}
};

/// @brief    Emits a semantic width node to an output stream.
/// @details  Precise-size children use the direct width emitter; non-precise children are first materialized into a
///           dynamic buffer so width placement can be applied accurately.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the width semantic node type
/// @param    optstm        the output stream reference
/// @param    t             the width node to emit
template <bool line, ::std::integral char_type, typename outputstmtype, typename T>
inline constexpr void print_semantic_emit_width(outputstmtype &optstm, T &&t)
{
	auto &&width_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using width_type = ::std::remove_cvref_t<decltype(width_ref)>;
	using width_traits = ::fast_io::details::decay::print_semantic_width_traits<width_type>;
	using width_child_type =
		::fast_io::details::decay::print_semantic_stable_input_forward_t<
			char_type, decltype((width_ref.reference))>;
	using width_child_node_type = ::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<width_child_type>()))>;
	constexpr bool child_selects_composite_branch{
		::fast_io::details::print_pack<width_child_node_type> ||
		::fast_io::details::decay::print_semantic_condition_v<width_child_node_type>};
	::std::size_t const width{width_ref.width};
	char_type const fillch{
		::fast_io::operations::decay::print_semantic_width_fill_char<char_type, width_traits>(width_ref)};
	auto const placement{::fast_io::operations::decay::print_semantic_width_placement<width_traits>(width_ref)};
	::std::size_t const placement_code{
		::fast_io::operations::decay::print_semantic_width_placement_code(placement)};
	if constexpr (::fast_io::details::decay::print_semantic_static_precise_size<char_type,
																				width_child_type>::available)
	{
		// Static precise children can pass a compile-time length directly into the width emitter.
		constexpr ::std::size_t len{
			::fast_io::details::decay::print_semantic_static_precise_size<char_type, width_child_type>::size};
		::fast_io::operations::decay::print_semantic_emit_width_direct<line, char_type>(
			optstm, width_ref.reference, width, fillch, placement_code, len);
		return;
	}
	else if constexpr (::fast_io::details::decay::print_semantic_precise_size_ok<char_type, width_child_type>::value)
	{
		constexpr ::std::size_t inline_materialization_capacity{
			::fast_io::basic_dynamic_output_buffer<char_type>::buffer_size};
		if constexpr (!child_selects_composite_branch)
		{
			// A non-composite precise child is cheap to measure and can use the direct width path without first owning a
			// temporary.
			::std::size_t const len{
				::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(width_ref.reference)};
			::fast_io::operations::decay::print_semantic_emit_width_direct<line, char_type>(
				optstm, width_ref.reference, width, fillch, placement_code, len);
			return;
		}
		else if (inline_materialization_capacity < width)
		{
			// A wide composite field retains the two-pass direct policy. Materializing it would either grow an inline
			// buffer repeatedly or allocate before the exact child length is known. Compact conditions and packs fall
			// through instead: their precise-size protocol would traverse the selected branch and then direct emission
			// would traverse that same branch again, whereas the inline buffer below materializes it exactly once.
			::std::size_t const len{
				::fast_io::operations::decay::print_semantic_precise_size_arg<char_type>(width_ref.reference)};
			::fast_io::operations::decay::print_semantic_emit_width_direct<line, char_type>(
				optstm, width_ref.reference, width, fillch, placement_code, len);
			return;
		}
	}
	// Non-precise children, and compact composite precise children, are buffered so the child is traversed once and
	// its actual length is available before placement is applied.
	::fast_io::basic_dynamic_output_buffer<char_type> buffer;
	decltype(auto) buffer_ref = ::fast_io::operations::output_stream_ref(buffer);
	decltype(auto) forwarded_child{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(
			width_ref.reference)};
	::fast_io::operations::decay::print_semantic_emit<false, true, char_type>(
		buffer_ref, forwarded_child);
	char_type const *const first{buffer.begin_ptr};
	char_type const *const last{buffer.curr_ptr};
	::std::size_t const len{static_cast<::std::size_t>(last - first)};
	if (width <= len || placement_code == 0u)
	{
		// Buffered fallback width is already satisfied, so the child bytes are forwarded unchanged.
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
	}
	else
	{
		// Buffered fallback padding is written around the materialized child bytes.
		::std::size_t const padding{width - len};
		::std::size_t const buffer_capacity{static_cast<::std::size_t>(buffer.end_ptr - buffer.begin_ptr)};
		if (width <= buffer_capacity)
		{
			// Capacity is checked after child materialization because a dynamic child may have moved the inline buffer.
			// The child occupies [first, first + len), and width <= capacity proves that every overlapping move and fill
			// below remains within the live allocation. Compact composite width therefore needs one child traversal
			// followed by one grouped write dispatch; the backend remains free to split or retry that dispatch.
			char_type *const mutable_first{buffer.begin_ptr};
			char_type *const mutable_last{mutable_first + len};
			char_type *field_last;
			if (placement_code == 1u)
			{
				// Left placement appends padding in the unused tail of the materialization buffer.
				field_last = ::fast_io::details::my_fill_n(mutable_last, padding, fillch);
			}
			else if (placement_code == 2u)
			{
				// Center placement uses overlap-safe movement before filling the two disjoint padding ranges.
				::std::size_t const left_padding{padding >> 1u};
				::std::size_t const right_padding{padding - left_padding};
				char_type *const child_pos{mutable_first + left_padding};
				::fast_io::details::my_copy_right_shift(
					mutable_first, mutable_last, left_padding);
				::fast_io::details::my_fill_n(mutable_first, left_padding, fillch);
				::fast_io::details::my_fill_n(child_pos + len, right_padding, fillch);
				field_last = mutable_first + width;
			}
			else if (placement_code == 4u)
			{
				// Internal placement inserts padding only when the child reports a valid prefix boundary.
				::std::size_t const internal_len{
					::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(width_ref.reference)};
				if (internal_len == 0u)
				{
					// A missing internal boundary has the documented right-placement fallback.
					::fast_io::details::my_copy_right_shift(
						mutable_first, mutable_last, padding);
					::fast_io::details::my_fill_n(mutable_first, padding, fillch);
					field_last = mutable_first + width;
				}
				else if (len < internal_len)
				{
					// The child emitted fewer characters than its claimed insertion point.  The metadata cannot safely
					// partition the byte range, but width still promises a minimum field extent; use the same lossless
					// right-placement fallback as the direct and unchecked strategies.
					::fast_io::details::my_copy_right_shift(
						mutable_first, mutable_last, padding);
					::fast_io::details::my_fill_n(mutable_first, padding, fillch);
					field_last = mutable_first + width;
				}
				else
				{
					char_type *const shift_pos{mutable_first + internal_len};
					::fast_io::details::my_copy_right_shift(
						shift_pos, mutable_last, padding);
					::fast_io::details::my_fill_n(shift_pos, padding, fillch);
					field_last = mutable_first + width;
				}
			}
			else
			{
				// Right placement moves the child behind the leading fill using overlap-safe movement.
				::fast_io::details::my_copy_right_shift(
					mutable_first, mutable_last, padding);
				::fast_io::details::my_fill_n(mutable_first, padding, fillch);
				field_last = mutable_first + width;
			}
			if constexpr (line)
			{
				if (field_last != buffer.end_ptr)
				{
					// Spare capacity keeps the newline in the same final write.
					*field_last++ = ::fast_io::char_literal_v<u8'\n', char_type>;
					::fast_io::operations::decay::write_all_decay_dispatch(optstm, mutable_first, field_last);
					return;
				}
			}
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, mutable_first, field_last);
			if constexpr (line)
			{
				// A completely full buffer writes its newline separately instead of growing solely for one character.
				::fast_io::operations::decay::char_put_decay_dispatch(
					optstm, ::fast_io::char_literal_v<u8'\n', char_type>);
			}
			return;
		}
		if (placement_code == 1u)
		{
			// Buffered fallback left placement writes the child bytes followed by trailing padding.
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
			::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
		}
		else if (placement_code == 2u)
		{
			// Buffered fallback center placement writes leading padding, child bytes, and trailing padding.
			::std::size_t const left_padding{padding >> 1u};
			::std::size_t const right_padding{padding - left_padding};
			::fast_io::operations::decay::print_semantic_write_fill(optstm, left_padding, fillch);
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
			::fast_io::operations::decay::print_semantic_write_fill(optstm, right_padding, fillch);
		}
		else if (placement_code == 4u)
		{
			// Buffered fallback internal placement uses the child's reported prefix length for insertion.
			::std::size_t const internal_len{
				::fast_io::operations::decay::print_semantic_internal_shift_arg<char_type>(width_ref.reference)};
			if (internal_len == 0)
			{
				// Without an internal shift point, internal placement falls back to right placement.
				::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
			}
			else if (len < internal_len)
			{
				// An invalid internal span cannot authorize pointer splitting.  Leading fill followed by the complete
				// child is the right-placement fallback and keeps this streaming branch observably identical to every
				// contiguous width strategy.
				::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
			}
			else
			{
				// The stream receives the buffered internal prefix, padding, and suffix in order.
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, first + internal_len);
				::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
				::fast_io::operations::decay::write_all_decay_dispatch(optstm, first + internal_len, last);
			}
		}
		else
		{
			// Buffered fallback right placement writes leading padding before the child bytes.
			::fast_io::operations::decay::print_semantic_write_fill(optstm, padding, fillch);
			::fast_io::operations::decay::write_all_decay_dispatch(optstm, first, last);
		}
	}
	if constexpr (line)
	{
		// The line variant appends a newline after the buffered fallback field.
		::fast_io::operations::decay::char_put_decay_dispatch(optstm, ::fast_io::char_literal_v<u8'\n', char_type>);
	}
}

/// @brief    Emits one semantic node through the checked stream path.
/// @details  The checked node dispatcher handles packs, conditions, and width nodes before returning control to the
///           flattened semantic emitter.
/// @tparam   line          true when this node is responsible for the caller's trailing newline
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   T             the semantic node type
/// @param    optstm        the output stream reference
/// @param    t             the semantic node to emit
template <bool line, ::std::integral char_type, typename outputstmtype, typename T>
inline constexpr void print_semantic_emit_node(outputstmtype &optstm, T &&t)
{
	auto &&node_ref{::fast_io::details::decay::print_semantic_node_ref(::std::forward<T>(t))};
	using node_type = ::std::remove_cvref_t<decltype(node_ref)>;
	if constexpr (::fast_io::details::print_pack<node_type>)
	{
		// Pack nodes are expanded and each element is emitted through the checked stream path.
		::fast_io::details::decay::print_semantic_pack_apply(
			node_ref,
			::fast_io::operations::decay::print_semantic_emit_node_pack_continuation<line, char_type, outputstmtype>{
				optstm});
	}
	else if constexpr (::fast_io::details::decay::print_semantic_condition_v<node_type>)
	{
		// Checked condition nodes dispatch exactly the branch selected by the predicate.
		if (node_ref.pred)
		{
			// Condition nodes emit the true alternative selected by the predicate.
			::fast_io::operations::decay::print_semantic_emit<line, false, char_type>(optstm, node_ref.t1);
		}
		else
		{
			// Condition nodes emit the false alternative selected by the predicate.
			::fast_io::operations::decay::print_semantic_emit<line, false, char_type>(optstm, node_ref.t2);
		}
	}
	else if constexpr (::fast_io::details::decay::print_semantic_width_v<node_type>)
	{
		// Width nodes delegate to the width-specific emitter so padding placement remains centralized.
		::fast_io::operations::decay::print_semantic_emit_width<line, char_type>(optstm,
																				 ::std::forward<T>(t));
	}
}

/// @brief    Invokes the accumulated flattened semantic continuation with the final line policy.
/// @tparam   line          true when the flattened run appends a trailing newline
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   continuation  the accumulated prefix continuation type
/// @details  The destination is only a recursion anchor at this terminal step. Borrowing it preserves the exact
///           normalized observer established by the enclosing print operation; copying an unused observer would make
///           a filtered-to-empty semantic record reject a valid immovable output before its zero-record continuation
///           can apply status, mutex, and line semantics.
template <bool line, ::std::integral char_type, typename outputstmtype, typename continuation>
#if __has_cpp_attribute(__gnu__::__flatten__)
[[__gnu__::__flatten__]]
#endif
inline constexpr void print_semantic_emit_flat_impl(outputstmtype &, continuation &&cont)
{
	cont.template operator()<line>();
}

/// @brief    Accumulates non-node arguments while flattening a semantic print run.
/// @tparam   continuation the downstream flattened continuation type
/// @tparam   T            the argument type captured as part of the prefix
template <typename continuation, typename T>
struct print_semantic_emit_flat_prefix_continuation
{
	::std::remove_reference_t<continuation> *contptr;
	::std::remove_reference_t<T> *valueptr;

	/// @brief    Prepends the captured plain argument to the flattened prefix.
	/// @tparam   prefix_line true when the prefix should append a newline
	/// @tparam   Prefix      the already-accumulated prefix argument types
	/// @param    prefix      the already-accumulated prefix arguments
	template <bool prefix_line, typename... Prefix>
	inline constexpr void operator()(Prefix &&...prefix) const
	{
		contptr->template operator()<prefix_line>(::std::forward<T>(*valueptr),
												  ::std::forward<Prefix>(prefix)...);
	}
};

/// @brief    Flattens a semantic print run until the next semantic node is reached.
/// @details  Plain arguments are accumulated into the prefix continuation; once a node is found, the prefix is emitted,
///           the node is handled, and flattening resumes for the remaining arguments.
/// @tparam   line          true when the final flattened run appends a trailing newline
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   continuation  the accumulated prefix continuation type
/// @tparam   T             the current argument type
/// @tparam   Args          the remaining argument types
template <bool line, ::std::integral char_type, typename outputstmtype, typename continuation, typename T,
		  typename... Args>
#if __has_cpp_attribute(__gnu__::__flatten__)
[[__gnu__::__flatten__]]
#endif
inline constexpr void print_semantic_emit_flat_impl(outputstmtype &optstm, continuation &&cont, T &&t, Args &&...args)
{
	if constexpr (
		::fast_io::details::decay::print_semantic_execution_node_v<T>)
	{
		// A semantic node terminates the current flat prefix before node-specific output is emitted.
		cont.template operator()<false>();
		::fast_io::operations::decay::print_semantic_emit_node<false, char_type>(optstm, ::std::forward<T>(t));
		::fast_io::operations::decay::print_semantic_emit<line, true, char_type>(optstm,
																				 ::std::forward<Args>(args)...);
	}
	else
	{
		// A plain argument is captured into the prefix so later coalescing can treat it as one flat run.
		::fast_io::operations::decay::print_semantic_emit_flat_impl<line, char_type>(
			optstm,
			::fast_io::operations::decay::print_semantic_emit_flat_prefix_continuation<continuation, T>{
				__builtin_addressof(cont), __builtin_addressof(t)},
			::std::forward<Args>(args)...);
	}
}

/// @brief    Emits the flattened prefix through the ordinary freestanding printing machinery.
/// @tparam   outputstmtype the decayed output stream reference type
template <typename outputstmtype>
struct print_semantic_emit_freestanding_continuation
{
	outputstmtype &optstm;

	/// @brief    Emits the accumulated flattened prefix through the best freestanding path.
	/// @tparam   prefix_line true when this prefix should append a newline
	/// @tparam   Prefix      the flattened prefix argument types
	/// @param    prefix      the flattened prefix arguments
	template <bool prefix_line, typename... Prefix>
	inline constexpr void operator()(Prefix &&...prefix) const
	{
		using char_type = typename outputstmtype::output_char_type;
		if constexpr (
			!::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					prefix_line, outputstmtype,
					::std::remove_reference_t<Prefix>...>() &&
			!::fast_io::details::decay::print_output_run_retains_static_scatter<outputstmtype, Prefix...> &&
			::fast_io::details::decay::print_controls_dynamic_scatters_reserve_fast_entry_available<
				char_type, ::std::remove_cvref_t<Prefix>...>())
		{
			// The fast dynamic reserve-scatters entry handles the prefix when every argument supports it and the
			// effective destination does not own the exact complete-record status operation.
			::fast_io::details::decay::print_controls_dynamic_scatters_reserve_fast_entry<prefix_line>(
				optstm, ::std::forward<Prefix>(prefix)...);
		}
		else
		{
			// The generic no-pack path emits prefixes that do not match the reserve-scatters fast entry.
			::fast_io::operations::decay::print_freestanding_decay_no_pack<prefix_line>(
				optstm, ::std::forward<Prefix>(prefix)...);
		}
	}
};

/// @brief Places an exceptionally large already-flat prefix behind a frontend recursion boundary.
/// @details Clang 17 through 23 can exhaust their frontend stack while instantiating more than 128 nested prefix
///          continuations; whether a particular record crosses the limit also depends on the destination type. The
///          active plain arguments are passed to the unchanged terminal continuation in one call; noinline prevents
///          the enclosing flatten attribute from reconstructing the rejected type recursion. The open lower-bounded
///          guard carries the verified Clang 23 result to later releases until a compiler demonstrates a reversal.
template <bool line, typename outputstmtype, typename... Args>
#if defined(__clang__) && __clang_major__ >= 17 && \
	FAST_IO_HAS_ATTRIBUTE(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#endif
inline constexpr void print_semantic_emit_large_plain_prefix(
	outputstmtype &optstm, Args &&...args)
{
	::fast_io::operations::decay::print_semantic_emit_freestanding_continuation<outputstmtype>{optstm}
		.template operator()<line>(::std::forward<Args>(args)...);
}

/// @brief Avoids GNU's linear continuation type depth after a plain coalescing fallback.
/// @details The caller's filtered arguments have already completed input/status forwarding, pack expansion, null
///          removal, and condition selection. A pack, condition, or width that remains is a semantic node and retains
///          the node-aware flattening path. When no node remains, the complete active sequence is already one plain
///          prefix, so invoking the same terminal continuation directly preserves strategy order and line ownership.
///          GCC 11 through 16 otherwise exhaust the default template depth on generated records above 128 fields,
///          while retaining the established continuation representation for ordinary packs preserves their generated
///          code exactly. The open GNU guard deliberately carries the GCC 16 result to later GCC releases until a
///          compiler demonstrates a reversal.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr void print_semantic_emit_flat_fallback(outputstmtype &optstm, Args &&...args)
{
#if defined(__GNUC__) && !defined(__clang__)
	if constexpr (
		!(::fast_io::details::decay::print_semantic_execution_node_v<Args> || ...) &&
		sizeof...(Args) > 128u)
	{
		::fast_io::operations::decay::print_semantic_emit_freestanding_continuation<outputstmtype>{optstm}
			.template operator()<line>(::std::forward<Args>(args)...);
	}
	else
	{
		::fast_io::operations::decay::print_semantic_emit_flat_impl<line, char_type>(
			optstm,
			::fast_io::operations::decay::print_semantic_emit_freestanding_continuation<outputstmtype>{optstm},
			::std::forward<Args>(args)...);
	}
#elif defined(__clang__) && __clang_major__ >= 17
	if constexpr (
		!(::fast_io::details::decay::print_semantic_execution_node_v<Args> || ...) &&
		sizeof...(Args) > 128u)
	{
		::fast_io::operations::decay::print_semantic_emit_large_plain_prefix<line>(
			optstm, ::std::forward<Args>(args)...);
	}
	else
	{
		::fast_io::operations::decay::print_semantic_emit_flat_impl<line, char_type>(
			optstm,
			::fast_io::operations::decay::print_semantic_emit_freestanding_continuation<outputstmtype>{optstm},
			::std::forward<Args>(args)...);
	}
#else
	::fast_io::operations::decay::print_semantic_emit_flat_impl<line, char_type>(
		optstm,
		::fast_io::operations::decay::print_semantic_emit_freestanding_continuation<outputstmtype>{optstm},
		::std::forward<Args>(args)...);
#endif
}

/// @brief Detects a normalized top-level width node, including a public parameter wrapper.
/// @details Classification is performed on the normalized node reference rather than the source type because aliasing
///          and print forwarding may replace a user object with a semantic manipulator.
template <typename T>
inline constexpr bool print_semantic_top_level_width_v =
	::fast_io::details::decay::print_semantic_node<T> &&
	::fast_io::details::decay::print_semantic_width_v<::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<T>()))>>;

/// @brief Tests whether a top-level width's forwarded child owns a run-time scatter representation.
/// @details The child must be classified after the same character-aware forwarding used by output. Inspecting the
///          pre-forwarded source can select a protocol whose customization tag does not match the object passed to it.
template <::std::integral char_type, typename T>
inline consteval bool print_semantic_top_level_width_has_runtime_scatter() noexcept
{
	if constexpr (::fast_io::operations::decay::print_semantic_top_level_width_v<T>)
	{
		using node_reference = decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<T>()));
		// The emitter first binds the normalized node to a named `auto&&`; that name is an lvalue even when its type is
		// an rvalue reference. Parenthesized decltype on the same named-node model therefore gives exactly the category
		// later passed to status forwarding. Using either unparenthesized member decltype (the declared member type) or
		// `declval<node_reference>()` (an xvalue for a temporary node) can select a different ref-qualified CPO.
		using child_reference = decltype((::std::declval<node_reference &>().reference));
		using child_type =
			::fast_io::details::decay::print_semantic_stable_input_forward_t<
				char_type, child_reference>;
		return ::fast_io::dynamic_reserve_scatters_printable<char_type, child_type> &&
			   ::fast_io::details::decay::retained_reserve_scatters_printable_v<char_type, child_type>;
	}
	else
	{
		return false;
	}
}

/// @brief Tests whether a top-level width owns a forwarded semantic child rather than a plain leaf.
/// @details A condition or pack child must already be traversed by the width emitter to obtain its exact field length.
///          Repeating that traversal in the outer coalescer supplies no additional capacity proof and retains every
///          alternative branch's materialization frame.
template <::std::integral char_type, typename T>
inline consteval bool print_semantic_top_level_width_has_semantic_child() noexcept
{
	if constexpr (::fast_io::operations::decay::print_semantic_top_level_width_v<T>)
	{
		using node_reference = decltype(::fast_io::details::decay::print_semantic_node_ref(::std::declval<T>()));
		using child_reference = decltype((::std::declval<node_reference &>().reference));
		using child_type =
			::fast_io::details::decay::print_semantic_stable_input_forward_t<
				char_type, child_reference>;
		return ::fast_io::details::decay::print_semantic_execution_node_v<child_type>;
	}
	else
	{
		return false;
	}
}

/// @brief Admits direct scalar emission for exactly two passive typed-scatter leaves.
/// @details A `basic_io_scatter_t` leaf is already the final descriptor: acquiring it is a non-throwing field read and
///          cannot invoke a producer whose effects would otherwise need to precede every write.  The destination gates
///          prove that the established descriptor fallback also performs ordered typed scalar writes. Native typed or
///          byte scatter, byte representation, put areas, mutex ownership, status dispatch, and every whole-output
///          coalescing policy are excluded because each can change either the primitive boundary or its lifetime.
///          `direct_streaming_preferred_stream` is the independent cost opt-in; scalar syntax alone does not establish
///          that bypassing the general plan is profitable. The exact-two limit is measured and intentionally does not
///          generalize to larger packs, whose code growth and instruction-cache cost require separate evidence.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr bool print_semantic_exact_two_passive_scatter_direct_write_v =
	sizeof...(Args) == 2u &&
	(::std::same_as<::std::remove_cvref_t<Args>, ::fast_io::basic_io_scatter_t<char_type>> && ...) &&
	::fast_io::direct_streaming_preferred_stream<char_type, outputstmtype> &&
	::fast_io::operations::decay::defines::has_write_all_overflow_define<outputstmtype> &&
	!::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype> &&
	!::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype> &&
	!::fast_io::details::decay::print_has_direct_scatter_write_bytes_operations<outputstmtype> &&
	!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
	!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
	!::fast_io::operations::decay::defines::has_status_print_define<
		line, outputstmtype, ::std::remove_cvref_t<Args>...> &&
	!::fast_io::semantic_plain_leaf_coalesce_preferred_stream<char_type, outputstmtype> &&
	::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>() == 0u &&
	::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
		char_type, outputstmtype, ::std::remove_cvref_t<Args>...>() == 0u &&
	::fast_io::details::decay::print_scatter_fallback_full_output_threshold<char_type, outputstmtype>() == 0u;

// The compiler-constant strategy is defined after the stable dispatcher whose implementation it reuses as the exact
// false continuation.  Semantic pack/condition filtering occurs earlier in this file, so these declarations let its
// continuation offer the same strategy only after the active leaf sequence is known.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool print_compiler_constant_materialization_available() noexcept;

template <::std::integral char_type, typename... Args>
[[nodiscard]] inline constexpr bool
print_compiler_constant_materialization_eligible_run(Args const &...args) noexcept;

template <::std::integral char_type, typename... Args>
[[nodiscard]] inline constexpr bool
print_compiler_constant_materialization_gate(Args const &...args) noexcept;

template <bool line, typename outputstmtype, typename... Args>
[[nodiscard]] inline constexpr bool print_freestanding_decay_compiler_constant_materialized(
	outputstmtype &optstm, Args... args);

/// @brief Tries compiler-constant materialization on an already-selected active record.
/// @details The definition follows the compiler-constant protocol implementation.  This early declaration lets the
///          semantic terminal keep the value query in its caller while leaving the large run-time emitter out of line.
template <bool line, typename outputstmtype, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr bool
	print_semantic_try_compiler_constant_active_record(
		outputstmtype &optstm, Args &...args);

/// @brief Emits the rejected bounded run one already-filtered component at a time.
/// @details This continuation is reached only after the compact one-pass capacity probe returned `SIZE_MAX`.  Calling
///          the general flat emitter here makes GCC instantiate and flatten every large-precision coalescing
///          alternative even though the common bounded arm has already returned; the resulting dead continuation used
///          to own a roughly 48-KiB probed frame.  At this point coalescing has already declined the complete run, so
///          preserving the established fallback semantics requires only ordered component emission.  Plain leaves use
///          the ordinary no-pack dispatcher, while semantic nodes keep their dedicated width/condition/pack dispatcher.
///          Only the final component receives the caller's line policy.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename T, typename... Args>
inline constexpr void print_semantic_emit_rejected_bounded_run(
	outputstmtype &optstm, T &&value, Args &&...args)
{
	constexpr bool component_line{line && sizeof...(Args) == 0u};
	if constexpr (
		::fast_io::details::decay::print_semantic_execution_node_v<T>)
	{
		::fast_io::operations::decay::print_semantic_emit_node<
			component_line, char_type>(optstm, ::std::forward<T>(value));
	}
	else
	{
		::fast_io::operations::decay::print_freestanding_decay_no_pack<
			component_line>(optstm, ::std::forward<T>(value));
	}
	if constexpr (sizeof...(Args) != 0u)
	{
		::fast_io::operations::decay::print_semantic_emit_rejected_bounded_run<
			line, char_type>(optstm, ::std::forward<Args>(args)...);
	}
}

/// @brief Completes the established precise semantic strategy after a cheap one-pass bound rejects the run.
/// @details The caller has not written output.  GCC's enclosing semantic dispatcher is deliberately flattened; without
///          this boundary it clones precise-size, bounded-size, and flat-emission alternatives into that hot caller,
///          even though ordinary fixed floating precisions have already returned through the compact bounded arm.  The
///          GNU-only non-clonable boundary keeps the uncommon large-precision continuation out of the caller's stack
///          frame.  Other compilers retain their existing inlining decision.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
#if defined(__GNUC__) && !defined(__clang__)
[[gnu::noinline, gnu::noclone]]
#endif
inline constexpr void print_semantic_precise_coalesce_fallback(
	outputstmtype &optstm, Args &&...args)
{
	if (!::fast_io::operations::decay::print_semantic_try_static_bounded_coalesce<
			line, char_type>(optstm, ::std::forward<Args>(args)...) &&
		!::fast_io::operations::decay::print_semantic_try_precise_coalesce<
			line, char_type>(optstm, ::std::forward<Args>(args)...) &&
		!::fast_io::operations::decay::print_semantic_try_bounded_coalesce<
			line, char_type>(optstm, ::std::forward<Args>(args)...))
	{
		::fast_io::operations::decay::print_semantic_emit_rejected_bounded_run<
			line, char_type>(optstm, ::std::forward<Args>(args)...);
	}
}

/// @brief Proves that one sibling is locally bounded or explicitly safe for shared one-pass staging.
/// @details A fixed scatter or static-reserve leaf is admitted only when its complete type-level extent fits the same
///          destination window.  This deliberately rejects `basic_io_scatter_t`, dynamic reserve, context, and retained
///          reserve-scatter siblings: their run-time payload can be arbitrarily large, and replacing a native writev
///          plan with bounded copying would regress the large borrowed-fragment path.  A leaf which reaches
///          `print_define` must instead supply the stronger single-pass staging CPO; mere direct printability is
///          insufficient because a custom formatter may inspect or mutate its concrete endpoint.
template <::std::integral char_type, ::std::size_t capacity, typename T>
inline consteval bool print_plain_leaf_single_pass_staging_safe() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	using static_scatter_traits =
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, value_type>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t> ||
				  ::fast_io::single_pass_staging_printable<char_type, value_type>)
	{
		return true;
	}
	else if constexpr (static_scatter_traits::available)
	{
		return static_scatter_traits::size <= capacity;
	}
	else if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		return print_reserve_size(
				   ::fast_io::io_reserve_type<char_type, value_type>) <= capacity;
	}
	else
	{
		return false;
	}
}

/// @brief Returns the fixed window consumption of one shared-staging sibling.
/// @details The explicitly marked recursive producer is unbounded and therefore contributes zero to this separate
///          sibling budget: overflow correctness is supplied by its one-pass protocol.  Fixed leaves contribute their
///          complete extent.  `SIZE_MAX` is a fail-closed sentinel for a category not admitted by the preceding proof.
template <::std::integral char_type, typename T>
inline consteval ::std::size_t
print_plain_leaf_single_pass_staging_fixed_extent() noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	using static_scatter_traits =
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, value_type>;
	if constexpr (::std::same_as<value_type, ::fast_io::io_null_t> ||
				  ::fast_io::single_pass_staging_printable<char_type, value_type>)
	{
		return 0u;
	}
	else if constexpr (static_scatter_traits::available)
	{
		return static_scatter_traits::size;
	}
	else if constexpr (::fast_io::reserve_printable<char_type, value_type>)
	{
		return print_reserve_size(
			::fast_io::io_reserve_type<char_type, value_type>);
	}
	else
	{
		return SIZE_MAX;
	}
}

/// @brief Selects shared staging for a mixed plain run containing an audited one-pass direct producer.
/// @details The destination owns both the capacity and stack-placement decision.  Requiring more than one leaf avoids
///          perturbing the established singleton path.  At least one explicit source marker is necessary so ordinary
///          reserve/scatter records retain their mature descriptor and contiguous strategies.  Every other leaf must
///          be representable by a destination-independent protocol, and the exact staged observer must accept the
///          completed run including line ownership.  Mutex and status customizations have already been selected by the
///          outer dispatcher; excluding them here prevents a synthetic put area from acquiring endpoint semantics.
///          Let `F` be the saturating sum of every fixed sibling extent plus the optional line character.  Requiring
///          `F < capacity` leaves at least one character for an explicitly marked producer, so selecting shared staging
///          can reduce a write boundary; `F >= capacity` cannot provide that guarantee and keeps the established
///          scatter/direct plan.  Saturation makes this proof independent of `size_t` wraparound.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
inline consteval bool print_plain_single_pass_staging_run() noexcept
{
	constexpr ::std::size_t capacity{
		::fast_io::details::decay::print_full_output_coalesce_threshold<
			char_type, outputstmtype>()};
	using staging_output = ::fast_io::details::decay::print_single_pass_staging_ref<
		char_type, outputstmtype>;
	::std::size_t fixed_sibling_extent{line ? 1u : 0u};
	((fixed_sibling_extent =
		  fixed_sibling_extent == SIZE_MAX || fixed_sibling_extent > capacity ||
				  ::fast_io::operations::decay::
						  print_plain_leaf_single_pass_staging_fixed_extent<
							  char_type, Args>() > capacity - fixed_sibling_extent
			  ? SIZE_MAX
			  : fixed_sibling_extent +
					::fast_io::operations::decay::
						print_plain_leaf_single_pass_staging_fixed_extent<
							char_type, Args>()),
	 ...);
	return sizeof...(Args) > 1u && capacity != 0u &&
		   fixed_sibling_extent < capacity &&
		   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
			   capacity, char_type> &&
		   !::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			   outputstmtype> &&
		   !::fast_io::operations::decay::defines::
			   has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		   !::fast_io::operations::decay::defines::has_status_print_define<
			   line, outputstmtype, ::std::remove_cvref_t<Args>...> &&
		   (false || ... || ::fast_io::single_pass_staging_printable<char_type, ::std::remove_cvref_t<Args>>) &&
		   (::fast_io::operations::decay::
				print_plain_leaf_single_pass_staging_safe<
					char_type, capacity, Args>() &&
			...) &&
		   !::fast_io::operations::decay::defines::has_status_print_define<
			   line, staging_output, ::std::remove_cvref_t<Args>...> &&
		   ::fast_io::details::decay::print_freestanding_output_run_okay<
			   line, staging_output, ::std::remove_cvref_t<Args>...>();
}

/// @brief    Entry continuation for flattened semantic emission.
/// @details  It first applies the exact-two direct-write policy when its destination proof is complete, then tries
///           one-pass static-bound coalescing, exact-size coalescing, and general run-time bounded coalescing before
///           falling back to semantic flattening.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_semantic_emit_flat_runtime_continuation
{
	using output_char_type = char_type;
	outputstmtype &optstm;

	/// @brief    Emits a filtered semantic run, preferring coalescing before flattening.
	/// @tparam   FilteredArgs the filtered argument types
	/// @param    filtered_args the filtered arguments
	template <typename... FilteredArgs>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr void operator()(FilteredArgs &&...filtered_args) const
	{
		constexpr bool has_remaining_semantic_node{
			(false || ... ||
			 ::fast_io::details::decay::print_semantic_execution_node_v<FilteredArgs>)};
		constexpr bool has_effective_whole_record_status{
			!has_remaining_semantic_node &&
			::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					line, outputstmtype,
					::std::remove_reference_t<FilteredArgs>...>()};
		if constexpr (has_effective_whole_record_status)
		{
			// Status printing owns the complete active record. Route through the ordinary dispatcher before any scatter,
			// staging, coalescing, or put-area strategy is instantiated; a run-time return alone would still instantiate
			// those discarded-in-semantics branches and could reject a status-only leaf.
			::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
				optstm, filtered_args...);
		}
		else
		{
			constexpr bool fixed_external_overstore_run{
				::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> &&
				(false || ... ||
				 ::fast_io::details::decay::print_fixed_external_semantic_overstore<
					 char_type,
					 ::fast_io::details::decay::print_semantic_forwarded_arg_t<
						 char_type, FilteredArgs>>::value)};
			constexpr bool fixed_external_overstore_bounded_run{
				fixed_external_overstore_run &&
				(true && ... &&
				 ::fast_io::details::decay::print_semantic_bounded_size_ok<
					 char_type,
					 ::fast_io::details::decay::print_semantic_forwarded_arg_t<
						 char_type, FilteredArgs>>::value)};
			if constexpr (fixed_external_overstore_bounded_run)
			{
				// Condition selection has already removed inactive top-level alternatives. A selected plain leaf, retained width,
				// or nested pack can still contain an audited reserve writer which uses scratch beyond its logical cursor.
				// Materialize that complete active semantic record before any direct put-area strategy is considered. Safe
				// semantic integer/string runs never instantiate this branch, and an unrepresentable aggregate simply retains
				// the checked component fallback below.
				::std::size_t required{line ? 1u : 0u};
				auto add_component = [&required](auto &component) constexpr {
					if (required != SIZE_MAX)
					{
						required = ::fast_io::details::decay::
							print_contiguous_char_extent_add_or_unavailable<char_type>(
								required,
								::fast_io::operations::decay::print_semantic_bounded_size<
									char_type>(component));
					}
				};
				// The filter continuation already owns stable forwarded lvalues. Measuring those exact expressions avoids a
				// second public forwarding pass through a parameter transport, which can conservatively report an unavailable
				// aggregate even though the unwrapped active node has a finite bound.
				(add_component(filtered_args), ...);
				if (required != SIZE_MAX) [[likely]]
				{
					::fast_io::operations::decay::
						print_semantic_materialize_fixed_external_bounded_and_write<
							line, char_type>(
							optstm, required,
							::std::forward<FilteredArgs>(filtered_args)...);
					return;
				}
			}
			if constexpr (
				::fast_io::operations::decay::print_semantic_exact_two_passive_scatter_direct_write_v<
					line, char_type, outputstmtype, FilteredArgs...>)
			{
				// A descriptor type alone does not prove a pointer domain for `{nullptr,0}`.  Retain the general dispatcher
				// whenever either payload is empty: besides avoiding null arithmetic, that preserves whether an empty scalar
				// customization is observed.  For two nonempty leaves the gate above proves exact ordered-call equivalence.
				if ((true && ... && (filtered_args.len != 0u)))
				{
					(::fast_io::operations::decay::write_all_decay_dispatch(
						 optstm, filtered_args.base, filtered_args.base + filtered_args.len),
					 ...);
					if constexpr (line)
					{
						// The ordinary descriptor fallback appends this same static range.  Reusing its base preserves both the
						// scalar write boundary and pointer identity; `char_put` would be a different output customization.
						auto const [base, len]{::fast_io::details::decay::line_scatter_common<char_type>};
						::fast_io::operations::decay::write_all_decay_dispatch(optstm, base, base + len);
					}
					return;
				}
			}
			constexpr bool has_semantic_node{has_remaining_semantic_node};
			constexpr bool has_runtime_scatter_component{
				(false || ... ||
				 (::fast_io::dynamic_reserve_scatters_printable<
					  char_type,
					  ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<FilteredArgs>> &&
				  ::fast_io::details::decay::retained_reserve_scatters_printable_v<
					  char_type,
					  ::fast_io::details::decay::print_runtime_scatter_plan_unwrapped_t<FilteredArgs>>))};
			using staged_group = ::fast_io::operations::decay::print_semantic_staged_group<
				char_type, ::std::remove_cvref_t<FilteredArgs>...>;
			constexpr bool retain_plain_leaves_for_coalescing{
				sizeof...(FilteredArgs) != 0u &&
				(::fast_io::semantic_plain_leaf_coalesce_preferred_stream<char_type, outputstmtype> ||
				 staged_group::available)};
			constexpr bool shared_single_pass_staging_run{
				!has_semantic_node && !has_runtime_scatter_component &&
				::fast_io::operations::decay::
					print_plain_single_pass_staging_run<
						line, char_type, outputstmtype, FilteredArgs...>()};
			constexpr bool singleton_put_area_preferred_plain_leaf{
				sizeof...(FilteredArgs) == 1u && !has_semantic_node &&
				!retain_plain_leaves_for_coalescing &&
				::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
				(::fast_io::put_area_printable_preferred<
					 char_type, ::std::remove_cvref_t<FilteredArgs>> &&
				 ...)};
			constexpr bool singleton_direct_streaming_preferred_plain_leaf{
				sizeof...(FilteredArgs) == 1u && !has_semantic_node &&
				!retain_plain_leaves_for_coalescing &&
				(::fast_io::details::decay::print_one_pass_direct_streaming_available_v<
					 char_type, outputstmtype, FilteredArgs> &&
				 ...)};
			if constexpr (singleton_put_area_preferred_plain_leaf)
			{
				// Pack expansion or condition selection can leave one ordinary range leaf. Semantic ancestry is not a cost
				// property: after inactive branches have disappeared, this is the same object/put-area pair as a plain print.
				// Re-entering semantic precise coalescing would traverse a run-time-scatter range twice and silently defeat its
				// explicit one-pass put-area marker. A stream's more specific plain-leaf coalescing marker still takes precedence.
				// The normalized wrapper forwards the source marker, and line ownership remains at this outer call, so the direct
				// no-pack path is category-correct and semantically identical for plain, pack-selected, and condition-selected
				// singleton leaves.
				::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
					optstm, ::std::forward<FilteredArgs>(filtered_args)...);
			}
			else if constexpr (shared_single_pass_staging_run)
			{
				// The active run is already flat and every source has passed the staged-observer proof.  One adapter instance
				// must own the whole sequence; staging each direct leaf separately would retain one syscall per literal/range
				// boundary and defeat the destination's whole-output threshold.
				constexpr ::std::size_t capacity{
					::fast_io::details::decay::print_full_output_coalesce_threshold<
						char_type, outputstmtype>()};
				::fast_io::operations::decay::print_single_pass_stage_run_and_write<
					line, char_type, capacity>(optstm, filtered_args...);
			}
			else if constexpr (!has_semantic_node && !has_runtime_scatter_component &&
							   !retain_plain_leaves_for_coalescing)
			{
				// Pack expansion and condition selection have produced only ordinary forwarded leaves. Re-entering the
				// semantic sizing framework cannot discover any placement operation, but it would duplicate the mature
				// scatter/reserve run scanner. The direct no-pack path preserves order and line ownership while restoring its
				// compile-time descriptor aggregation; inactive branches have already been removed from this sequence. A
				// run-time-scatter leaf is deliberately excluded: the strategy block below can either build its descriptor plan
				// or coalesce it with adjacent prefixes for a stream that does not advertise scatter-plan fallback. Sending such
				// a mixed run to the ordinary scanner split one logical record into several writes on direct-write-only sinks.
				// An empty run deliberately remains here even for a marked stream: `println(pack())` is only a newline and has
				// no multi-leaf replay cost for a coalescer to remove.
				::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
					optstm, ::std::forward<FilteredArgs>(filtered_args)...);
			}
			else
			{
				constexpr bool single_top_level_width{
					sizeof...(FilteredArgs) == 1u &&
					(::fast_io::operations::decay::print_semantic_top_level_width_v<FilteredArgs> && ...)};
				constexpr bool width_has_runtime_scatter{
					sizeof...(FilteredArgs) == 1u &&
					(::fast_io::operations::decay::print_semantic_top_level_width_has_runtime_scatter<
						 char_type, FilteredArgs>() &&
					 ...)};
				constexpr bool width_has_semantic_child{
					sizeof...(FilteredArgs) == 1u &&
					(::fast_io::operations::decay::print_semantic_top_level_width_has_semantic_child<
						 char_type, FilteredArgs>() &&
					 ...)};
				constexpr bool width_owns_child_traversal{
					width_has_runtime_scatter || width_has_semantic_child};
				constexpr bool has_declared_whole_output_coalescing{
					::fast_io::details::decay::print_full_output_coalesce_threshold<
						char_type, outputstmtype>() != 0u ||
					::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
						char_type, outputstmtype,
						::fast_io::details::decay::print_semantic_forwarded_arg_t<
							char_type, FilteredArgs>...>() != 0u};
				constexpr bool use_direct_width_strategy{
					single_top_level_width && width_owns_child_traversal &&
					!has_declared_whole_output_coalescing &&
					(::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
					 ::fast_io::buffered_printable_preferred_stream<char_type, outputstmtype> ||
					 ::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype> ||
					 ::fast_io::details::decay::print_has_direct_write_operations<outputstmtype>)};
				if constexpr (
					::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
						char_type, outputstmtype, FilteredArgs...>)
				{
					// Pack expansion, null filtering, and condition selection have already normalized this run. A run-time
					// scatter plan therefore sees only the active semantic leaves and can avoid an otherwise redundant heap copy.
					::fast_io::details::decay::print_runtime_scatter_plan_fast_entry<line>(
						optstm, ::std::forward<FilteredArgs>(filtered_args)...);
				}
				else if constexpr (singleton_direct_streaming_preferred_plain_leaf)
				{
					// Pack expansion or condition selection can expose one ordinary range leaf. Native scatter admission was
					// tested first; for a non-scatter destination, the paired positive cost markers now restore the leaf's
					// one-pass representation without letting semantic ancestry force a precise temporary. Restricting the
					// bypass to one active leaf avoids splitting an otherwise coalescible record into extra destination calls.
					::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(
						optstm, ::std::forward<FilteredArgs>(filtered_args)...);
				}
				else if constexpr (use_direct_width_strategy)
				{
					// A single width node already owns the exact child-measurement and final emission strategy. Bypassing outer
					// precise/bounded materialization avoids duplicate traversals and large mutually exclusive stack frames. The
					// child-ownership predicate is required even for a stream with a writable put area. A plain statically
					// bounded field can be emitted by the outer bounded coalescer in one traversal and one cursor commit; routing
					// every such field through the direct-width dispatcher duplicates classification and loses that compact code
					// shape.  A run-time-scatter or semantic child is different: the width node already owns its descriptor or
					// selected-branch traversal, so outer materialization would repeat work.  Keeping capability admission and
					// traversal ownership as independent proofs prevents an available obuffer from overriding the cheaper plain-
					// leaf strategy. Every destination capability is subordinate to an explicit whole-output coalescing policy:
					// for a wide composite field, that policy can turn child plus padding into one coalesced output dispatch, while
					// direct width must
					// stream several fill blocks if an advertised put area happens to be short at run time. The outer coalescer
					// still tries that put area first, then consumes the declared stack/dynamic fallback, so no useful capacity is
					// lost by honoring the policy here. Measured condition(pack) fields with seven to nine leaves were more than
					// twice as slow before the child-ownership boundary was made explicit.
					(::fast_io::operations::decay::print_semantic_emit_node<line, char_type>(
						 optstm, ::std::forward<FilteredArgs>(filtered_args)),
					 ...);
				}
				// Strategy selection is ordered by cost: a compile-time bound avoids measurement, a precise size avoids excess
				// allocation, and a run-time bound handles the remaining coalescible compositions.
				else if constexpr (
					::fast_io::operations::decay::print_semantic_precise_coalesce_strategy_selected<
						line, char_type, outputstmtype, FilteredArgs...>())
				{
					if constexpr (
						::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
							char_type, outputstmtype, FilteredArgs...>())
					{
						// Expensive exact-size sources with an audited cheap bound get the existing put area first.  A failed
						// capacity probe has not emitted or observed an unmarked object and therefore rejoins the old order below.
						if (::fast_io::operations::decay::print_semantic_try_single_pass_bounded_put_area<
								line, char_type>(optstm, filtered_args...))
						{
							return;
						}
					}
					if constexpr (
						::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_run<
							char_type, outputstmtype, FilteredArgs...>())
					{
						// A stream-authorized compact frame is the non-buffered counterpart of the put-area probe above. It
						// must precede exact sizing: dynamic fixed floating fields otherwise build precise decimal metadata only
						// to discover that their already-audited reserve bound fits in a very small contiguous output.
						if (::fast_io::operations::decay::print_semantic_try_single_pass_bounded_stack<
								line, char_type>(optstm, filtered_args...))
						{
							return;
						}
					}
					// Selected compact and unbounded compositions preserve the established three-stage coalescing shape.
					// For an audited bounded source this point is reached only after its cheap finite-capacity probe rejected
					// the run; keeping the continuation separate prevents the large-precision alternatives from owning the
					// common caller's stack frame on GCC.
					if constexpr (
						::fast_io::operations::decay::
							print_semantic_single_pass_bounded_stack_run<
								char_type, outputstmtype, FilteredArgs...>())
					{
						::fast_io::operations::decay::
							print_semantic_precise_coalesce_fallback<line, char_type>(
								optstm,
								::std::forward<FilteredArgs>(filtered_args)...);
					}
					else if (!::fast_io::operations::decay::
								 print_semantic_try_static_bounded_coalesce<
									 line, char_type>(
									 optstm,
									 ::std::forward<FilteredArgs>(filtered_args)...) &&
							 !::fast_io::operations::decay::
								 print_semantic_try_precise_coalesce<line, char_type>(
									 optstm,
									 ::std::forward<FilteredArgs>(filtered_args)...) &&
							 !::fast_io::operations::decay::
								 print_semantic_try_bounded_coalesce<line, char_type>(
									 optstm,
									 ::std::forward<FilteredArgs>(filtered_args)...))
					{
						::fast_io::operations::decay::print_semantic_emit_flat_fallback<line, char_type>(
							optstm, ::std::forward<FilteredArgs>(filtered_args)...);
					}
				}
				else
				{
					constexpr bool has_contiguous_target{
						::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
						(::fast_io::details::decay::print_full_output_coalesce_threshold<char_type, outputstmtype>() != 0u) ||
						(::fast_io::details::decay::print_full_output_dynamic_coalesce_threshold_for_run<
							 char_type, outputstmtype,
							 ::fast_io::details::decay::print_semantic_forwarded_arg_t<char_type, FilteredArgs>...>() != 0u)};
					if constexpr (has_contiguous_target)
					{
						// Long bounded packs with contiguous storage skip precise sizing but retain both bounded stages.
						if (!::fast_io::operations::decay::print_semantic_try_static_bounded_coalesce<line, char_type>(
								optstm, ::std::forward<FilteredArgs>(filtered_args)...) &&
							!::fast_io::operations::decay::print_semantic_try_bounded_coalesce<line, char_type>(
								optstm, ::std::forward<FilteredArgs>(filtered_args)...))
						{
							// Failed bounded coalescing keeps semantics by flattening nodes around ordinary freestanding output.
							::fast_io::operations::decay::print_semantic_emit_flat_fallback<line, char_type>(
								optstm, ::std::forward<FilteredArgs>(filtered_args)...);
						}
					}
					else
					{
						// Without contiguous storage, both run-time sizing passes would be unable to change the output strategy.
						if (!::fast_io::operations::decay::print_semantic_try_static_bounded_coalesce<line, char_type>(
								optstm, ::std::forward<FilteredArgs>(filtered_args)...))
						{
							// A failed static attempt falls directly to checked semantic emission without measuring an unusable bound.
							::fast_io::operations::decay::print_semantic_emit_flat_fallback<line, char_type>(
								optstm, ::std::forward<FilteredArgs>(filtered_args)...);
						}
					}
				}
			}
		}
	}
};

/// @brief Emits one locally selected boundary-delimited segment through the ordinary control scanner.
/// @details The complete active record contains either a direct-only user barrier or a retained width, so its complete-
///          record compiler-constant materialization is structurally unavailable and its eventual flat fallback reaches
///          `print_controls_impl` for each plain prefix. Calling that same status-free inner scanner for a locally
///          selected segment avoids introducing a subgroup status lookup which the original complete-record dispatcher
///          never performs. An empty non-line segment is skipped; an empty final line segment emits only the newline
///          still owned by the complete record.
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_semantic_optional_scatter_barrier_segment_continuation
{
	outputstmtype &optstm;

	template <typename... FilteredArgs>
	inline constexpr void operator()(FilteredArgs &&...filtered_args) const
	{
		if constexpr (sizeof...(FilteredArgs) == 0u)
		{
			if constexpr (line)
			{
				::fast_io::operations::decay::char_put_decay_dispatch(
					optstm,
					::fast_io::char_literal_v<u8'\n', char_type>);
			}
		}
		else
		{
			// These named expressions are the same lvalues accumulated by the full generic control dispatcher. Do not
			// forward them into a new ownership or status boundary.
			::fast_io::details::decay::print_controls_impl<
				line, outputstmtype, 0u>(optstm, filtered_args...);
		}
	}
};

/// @brief Finds the next proven dispatch boundary in a tuple of pointers to normalized source arguments.
/// @details `fast_io::containers::tuple_element` uses C++26 pack indexing when available and its recursive C++20
///          fallback otherwise. The algorithm and admitted source graph are identical in both language modes.
template <::std::size_t index, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple>
inline consteval ::std::size_t
print_semantic_optional_scatter_find_barrier() noexcept
{
	using tuple_type = ::std::remove_cvref_t<source_tuple>;
	constexpr ::std::size_t count{::std::tuple_size_v<tuple_type>};
	if constexpr (index == count)
	{
		return count;
	}
	else
	{
		using pointer_type = ::std::tuple_element_t<index, tuple_type>;
		using source_type = ::std::remove_pointer_t<pointer_type>;
		if constexpr (
			::fast_io::details::decay::
				print_semantic_optional_scatter_boundary_argument_v<
					outputstmtype, char_type, source_type>)
		{
			return index;
		}
		else
		{
			return ::fast_io::operations::decay::
				print_semantic_optional_scatter_find_barrier<
					index + 1u, outputstmtype, char_type,
					source_tuple>();
		}
	}
}

/// @brief Emits one half-open source segment between two proven dispatch boundaries.
/// @details A four-condition segment takes the existing linear compacting plan. A smaller segment performs only its
///          bounded local condition selection and then enters the ordinary status-free control scanner. In particular,
///          this helper never evaluates the active-record compiler-constant gate: the enclosing barrier proof already
///          established that the original complete active record could not satisfy that gate.
template <bool line, ::std::size_t begin, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple,
		  ::std::size_t... offset>
inline constexpr void
print_semantic_optional_scatter_emit_barrier_segment_impl(
	outputstmtype &optstm, source_tuple &sources,
	::std::index_sequence<offset...>)
{
	using tuple_type = ::std::remove_cvref_t<source_tuple>;
	if constexpr (sizeof...(offset) == 0u)
	{
		if constexpr (line)
		{
			::fast_io::operations::decay::char_put_decay_dispatch(
				optstm,
				::fast_io::char_literal_v<u8'\n', char_type>);
		}
	}
	else if constexpr (
		::fast_io::details::decay::
			print_semantic_optional_scatter_plan_available<
				line, char_type, outputstmtype,
				::std::remove_pointer_t<::std::tuple_element_t<
					begin + offset, tuple_type>>...>())
	{
		::fast_io::details::decay::
			print_semantic_optional_scatter_plan_emit<line, char_type>(
				optstm,
				*::fast_io::containers::get<begin + offset>(sources)...);
	}
	else
	{
		::fast_io::details::decay::print_semantic_select_conditions<
			char_type>(
			::fast_io::operations::decay::
				print_semantic_optional_scatter_barrier_segment_continuation<
					line, char_type, outputstmtype>{optstm},
			*::fast_io::containers::get<begin + offset>(sources)...);
	}
}

template <bool line, ::std::size_t begin, ::std::size_t end,
		  typename outputstmtype, ::std::integral char_type,
		  typename source_tuple>
	requires(begin <= end)
inline constexpr void print_semantic_optional_scatter_emit_barrier_segment(
	outputstmtype &optstm, source_tuple &sources)
{
	::fast_io::operations::decay::
		print_semantic_optional_scatter_emit_barrier_segment_impl<
			line, begin, outputstmtype, char_type>(
			optstm, sources,
			::std::make_index_sequence<end - begin>{});
}

/// @brief Finds the next strict fixed-child width after whole-record coalescing has declined without output.
template <::std::size_t index, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple>
inline consteval ::std::size_t
print_semantic_optional_scatter_find_coalescing_width() noexcept
{
	using tuple_type = ::std::remove_cvref_t<source_tuple>;
	constexpr ::std::size_t count{::std::tuple_size_v<tuple_type>};
	if constexpr (index == count)
	{
		return count;
	}
	else
	{
		using pointer_type = ::std::tuple_element_t<index, tuple_type>;
		using source_type = ::std::remove_pointer_t<pointer_type>;
		if constexpr (
			::fast_io::details::decay::
				print_semantic_optional_scatter_coalescing_width_argument_v<
					outputstmtype, char_type, source_type>)
		{
			return index;
		}
		else
		{
			return ::fast_io::operations::decay::
				print_semantic_optional_scatter_find_coalescing_width<
					index + 1u, outputstmtype, char_type,
					source_tuple>();
		}
	}
}

/// @brief Re-enters semantic emission on the original suffix after one width boundary has been emitted.
template <bool line, ::std::size_t begin, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple,
		  ::std::size_t... offset>
inline constexpr void
print_semantic_optional_scatter_emit_coalescing_width_runtime_tail_impl(
	outputstmtype &optstm, source_tuple &sources,
	::std::index_sequence<offset...>)
{
	::fast_io::operations::decay::print_semantic_emit<
		line, true, char_type>(
		optstm,
		*::fast_io::containers::get<begin + offset>(sources)...);
}

/// @brief Replays the ordinary flat fallback only after every complete-record coalescing attempt returned false.
/// @details The first prefix uses the already-proved closed-scatter segment emitter and the width is invoked once
///          without line ownership. The ordinary flat emitter then re-enters the complete semantic dispatcher on its
///          untouched suffix; doing the same here preserves any suffix-wide coalescing, compiler-constant query, later
///          width strategy, and final newline ownership. No zero-length adapter crosses the width boundary.
template <bool line, ::std::size_t begin, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple>
inline constexpr void
print_semantic_optional_scatter_emit_coalescing_width_fallback_tail(
	outputstmtype &optstm, source_tuple &sources)
{
	using tuple_type = ::std::remove_cvref_t<source_tuple>;
	constexpr ::std::size_t count{::std::tuple_size_v<tuple_type>};
	constexpr ::std::size_t width{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_find_coalescing_width<
				begin, outputstmtype, char_type, source_tuple>()};
	if constexpr (width == count)
	{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_barrier_segment<
				line, begin, count, outputstmtype, char_type>(
				optstm, sources);
	}
	else
	{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_barrier_segment<
				false, begin, width, outputstmtype, char_type>(
				optstm, sources);
		::fast_io::operations::decay::print_semantic_emit_node<
			false, char_type>(
			optstm,
			*::fast_io::containers::get<width>(sources));
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_coalescing_width_runtime_tail_impl<
				line, width + 1u, outputstmtype, char_type>(
				optstm, sources,
				::std::make_index_sequence<count - width - 1u>{});
	}
}

/// @brief Emits all source segments and their proven dispatch boundaries in original order.
/// @details A user barrier is invoked once through `print_control_single`, the same terminal reached after the generic
///          scanner drains its preceding compatible prefix. A mechanically forced width boundary is invoked once
///          through the ordinary semantic-node emitter with line disabled; the tail retains line ownership exactly as
///          in flat fallback, including the empty-tail newline case. If a final optional segment selects no leaf, its
///          retained line ownership is discharged by the segment's one-character empty case.
template <bool line, ::std::size_t begin, typename outputstmtype,
		  ::std::integral char_type, typename source_tuple>
inline constexpr void print_semantic_optional_scatter_emit_barrier_tail(
	outputstmtype &optstm, source_tuple &sources)
{
	using tuple_type = ::std::remove_cvref_t<source_tuple>;
	constexpr ::std::size_t count{::std::tuple_size_v<tuple_type>};
	constexpr ::std::size_t barrier{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_find_barrier<
				begin, outputstmtype, char_type,
				source_tuple>()};
	if constexpr (barrier == count)
	{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_barrier_segment<
				line, begin, count, outputstmtype, char_type>(
				optstm, sources);
	}
	else
	{
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_barrier_segment<
				false, begin, barrier, outputstmtype, char_type>(
				optstm, sources);
		using barrier_pointer_type =
			::std::tuple_element_t<barrier, tuple_type>;
		using barrier_source_type =
			::std::remove_pointer_t<barrier_pointer_type>;
		if constexpr (
			::fast_io::details::decay::
				print_semantic_optional_scatter_width_barrier_argument_v<
					outputstmtype, char_type, barrier_source_type>)
		{
			// Ordinary flat fallback always emits a width node without line ownership, then resumes semantic emission on
			// the tail. Repeating both calls preserves even the final-width newline as a distinct tail operation.
			::fast_io::operations::decay::print_semantic_emit_node<
				false, char_type>(
				optstm,
				*::fast_io::containers::get<barrier>(sources));
			::fast_io::operations::decay::
				print_semantic_optional_scatter_emit_barrier_tail<
					line, barrier + 1u, outputstmtype, char_type>(
					optstm, sources);
		}
		else
		{
			constexpr bool barrier_owns_line{
				line && barrier + 1u == count};
			::fast_io::details::decay::print_control_single<
				barrier_owns_line>(
				optstm,
				*::fast_io::containers::get<barrier>(sources));
			if constexpr (barrier + 1u != count)
			{
				::fast_io::operations::decay::
					print_semantic_optional_scatter_emit_barrier_tail<
						line, barrier + 1u, outputstmtype, char_type>(
						optstm, sources);
			}
		}
	}
}

template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
	requires(
		::fast_io::details::decay::
			print_semantic_optional_scatter_barrier_plan_available<
				line, char_type, outputstmtype, Args...>())
inline constexpr void print_semantic_optional_scatter_barrier_plan_emit(
	outputstmtype &optstm, Args &...args)
{
	auto sources{
		::fast_io::details::decay::print_semantic_pack_pointer_tuple(
			__builtin_addressof(args)...)};
	::fast_io::operations::decay::
		print_semantic_optional_scatter_emit_barrier_tail<
			line, 0u, outputstmtype, char_type>(optstm, sources);
}

/// @brief Executes the admitted width plan with the ordinary whole-record strategy taking precedence over segmentation.
/// @details Closed conditions are selected once into borrowed fixed descriptors. The three calls are deliberately in
///          the same order as `print_semantic_emit_flat_runtime_continuation`; the plan proof makes both compact bounded
///          probes and every alternative strategy branch structurally false for all active records. A successful call
///          therefore commits the identical single contiguous operation. Only three side-effect-free failures permit
///          the proven width-delimited flat fallback, which receives original sources rather than zero-size adapters.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
	requires(
		::fast_io::operations::decay::
			print_semantic_optional_scatter_width_coalescing_plan_available<
				line, char_type, outputstmtype, Args...>())
inline constexpr void
print_semantic_optional_scatter_width_coalescing_plan_emit(
	outputstmtype &optstm, Args &...args)
{
	bool const emitted{
		[]<typename... AdaptedArgs>(
			outputstmtype &output,
			AdaptedArgs &&...adapted_args) constexpr {
			return ::fast_io::operations::decay::
					   print_semantic_try_static_bounded_coalesce<
						   line, char_type>(output, adapted_args...) ||
				   ::fast_io::operations::decay::
					   print_semantic_try_precise_coalesce<
						   line, char_type>(output, adapted_args...) ||
				   ::fast_io::operations::decay::
					   print_semantic_try_bounded_coalesce<
						   line, char_type>(output, adapted_args...);
		}(
			optstm,
			::fast_io::details::decay::
				print_semantic_optional_coalescing_adapt<
					char_type>(args)...)};
	if (!emitted)
	{
		auto sources{
			::fast_io::details::decay::print_semantic_pack_pointer_tuple(
				__builtin_addressof(args)...)};
		::fast_io::operations::decay::
			print_semantic_optional_scatter_emit_coalescing_width_fallback_tail<
				line, 0u, outputstmtype, char_type>(optstm, sources);
	}
}

/// @brief    Applies the value-level constant gate at the exact active-record boundary.
/// @details  Pack expansion and condition selection have already fixed the operation arity and leaf types.  The small
///           wrapper must remain in the expression caller so an accepted `__builtin_constant_p` query disappears with
///           its materializer, while a rejected query enters the unchanged large run-time continuation below.  Keeping
///           those bodies separate is the code-size proof: forced placement covers only the query and one cold edge,
///           never the mutually exclusive semantic sizing and output strategies.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   char_type     the character type of the output stream
/// @tparam   outputstmtype the decayed output stream reference type
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_semantic_emit_flat_continuation
{
	using output_char_type = char_type;
	outputstmtype &optstm;

	template <typename... FilteredArgs>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr void operator()(FilteredArgs &&...filtered_args) const
	{
		constexpr bool has_remaining_semantic_node{
			(false || ... ||
			 ::fast_io::details::decay::print_semantic_execution_node_v<FilteredArgs>)};
		constexpr bool has_effective_whole_record_status{
			!has_remaining_semantic_node &&
			::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					line, outputstmtype,
					::std::remove_reference_t<FilteredArgs>...>()};
		if constexpr (!has_remaining_semantic_node &&
					  !has_effective_whole_record_status)
		{
			if (::fast_io::operations::decay::
					print_semantic_try_compiler_constant_active_record<line>(
						optstm, filtered_args...))
			{
				return;
			}
		}
		::fast_io::operations::decay::
			print_semantic_emit_flat_runtime_continuation<
				line, char_type, outputstmtype>{optstm}(
				::std::forward<FilteredArgs>(filtered_args)...);
	}
};

/// @brief    Filters null semantic outputs before the flattened emitter is invoked.
/// @tparam   emit_flat_type the flattened emitter continuation type
template <typename emit_flat_type>
struct print_semantic_filter_flat_continuation
{
	::std::remove_reference_t<emit_flat_type> *emitflatptr;

	/// @brief    Removes null outputs from a flattened semantic run before emission.
	/// @tparam   FlatArgs the flattened argument types
	/// @param    flat_args the flattened arguments
	template <typename... FlatArgs>
	inline constexpr void operator()(FlatArgs &&...flat_args) const
	{
		::fast_io::details::decay::print_semantic_select_conditions<
			typename ::std::remove_reference_t<emit_flat_type>::output_char_type>(
			*emitflatptr, ::std::forward<FlatArgs>(flat_args)...);
	}
};

#include "print_semantic_status_probe.h"
#include "print_semantic_linear.h"

/// @brief Shares the ordinary control plan only after all earlier active-record strategies are excluded.
/// @details Closed literal conditions cannot introduce staged or runtime-scatter producers. At least three mandatory
///          leaves exclude empty/singleton/exact-two whole-record policies. Exact status ownership remains separately
///          proved for every active type sequence; no provider declaration is inferred from a formatting protocol.
template <bool line, ::std::integral char_type, typename outputstmtype, typename... Args>
inline consteval bool print_semantic_linear_control_plan_available() noexcept
{
#if __cplusplus > 202302L && ((defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || (defined(__clang__) && __clang_major__ >= 13))
	constexpr ::std::size_t selections{(static_cast<::std::size_t>(
											::fast_io::details::decay::print_semantic_top_level_condition_v<Args &>) +
										... + 0u)};
	if constexpr (selections < 4u || sizeof...(Args) - selections < 3u ||
				  ::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
				  ::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
				  ::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> ||
				  !::fast_io::semantic_optional_scatter_plan_stream<char_type, outputstmtype> ||
				  ::fast_io::semantic_plain_leaf_coalesce_preferred_stream<char_type, outputstmtype> ||
				  (false || ... || ::fast_io::staged_printable<char_type, ::std::remove_cvref_t<Args>>) ||
				  (false || ... || ::fast_io::single_pass_staging_printable<char_type, ::std::remove_cvref_t<Args>>))
	{
		return false;
	}
	else
	{
		constexpr bool native_scatter{[]() consteval {
			if constexpr (::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype>)
			{
				return ::fast_io::details::decay::print_has_direct_scatter_write_bytes_operations<outputstmtype>;
			}
			else
			{
				return ::fast_io::details::decay::print_has_direct_scatter_write_operations<outputstmtype>;
			}
		}()};
		if constexpr (!native_scatter ||
					  !::fast_io::operations::decay::print_semantic_linear::plan_available<char_type, outputstmtype, Args...>())
		{
			return false;
		}
		else
		{
			// Audited GNU/Clang active-record compiler-constant dispatch is structurally query-free. Sources which could
			// select a different whole-record policy were discarded above, leaving the unchanged no-pack control order.
			if constexpr (::fast_io::semantic_status_free_record<line, char_type, outputstmtype, Args...>)
			{
				return true;
			}
			else
			{
				return !::fast_io::operations::decay::print_semantic_any_exact_status<line, char_type, outputstmtype, Args...>();
			}
		}
	}
#else
	return false;
#endif
}

/// @brief    Emits a semantic-aware freestanding print run.
/// @details  The entry expands semantic packs and nulls when needed, then routes the resulting flat run through
///           coalescing and semantic node emission. Ordinary `inline` is deliberate. A GCC 11--16 and Clang 17--23
///           N20/N32 matrix showed that forcing GNU flatten left Clang's generated text unchanged, except for a
///           256-byte reduction without flatten in the mixed32 benchmark, while it enlarged most GCC texts and raised
///           GCC compile time or memory. P-core measurements of obuffer, direct, and buffered mixed32 output found no
///           throughput regression without the attribute. Normal `-O3` therefore provides sufficient local inlining
///           without imposing every mutually exclusive semantic branch on its caller.
/// @tparam   line              true when a trailing newline is appended
/// @tparam   already_forwarded true when inputs have already passed semantic input forwarding
/// @tparam   char_type         the character type of the output stream
/// @tparam   outputstmtype     the decayed output stream reference type
/// @tparam   Args              the argument types in the print run
/// @param    optstm            the output stream reference
/// @param    args              the arguments to emit
template <bool line, bool already_forwarded, ::std::integral char_type, typename outputstmtype, typename... Args>
inline constexpr void print_semantic_emit(outputstmtype &optstm, Args &&...args)
{
	print_semantic_emit_flat_continuation<line, char_type, outputstmtype> emit_flat{optstm};
	constexpr bool has_pack_or_null{
		((::fast_io::details::decay::print_semantic_pack_argument_v<Args> ||
		  ::std::same_as<::std::remove_cvref_t<Args>, ::fast_io::io_null_t>) ||
		 ...)};
	constexpr bool has_top_level_condition{
		(false || ... || ::fast_io::details::decay::print_semantic_top_level_condition_v<Args>)};
	constexpr bool use_optional_scatter_source_plan = []() consteval {
		if constexpr (
			already_forwarded && sizeof...(Args) >= 7u &&
			!has_pack_or_null && has_top_level_condition)
		{
			// Execution owns named lvalues, while public admission stores the same normalized types with their original
			// cv-qualification. Removing references only makes both consumers reuse one sufficient-proof specialization;
			// the helper reconstructs the exact named-lvalue expression for every source protocol internally.
			return ::fast_io::details::decay::
				print_semantic_optional_scatter_plan_available<
					line, char_type, outputstmtype,
					::std::remove_reference_t<Args>...>();
		}
		else
		{
			return false;
		}
	}();
	constexpr bool use_optional_scatter_width_coalescing_plan = []() consteval {
		if constexpr (
			already_forwarded && sizeof...(Args) >= 13u &&
			!has_pack_or_null && has_top_level_condition &&
			!use_optional_scatter_source_plan)
		{
			return ::fast_io::operations::decay::
				print_semantic_optional_scatter_width_coalescing_plan_available<
					line, char_type, outputstmtype,
					::std::remove_reference_t<Args>...>();
		}
		else
		{
			return false;
		}
	}();
	constexpr bool use_optional_scatter_barrier_plan = []() consteval {
		if constexpr (
			already_forwarded && sizeof...(Args) >= 8u &&
			!has_pack_or_null && has_top_level_condition &&
			!use_optional_scatter_source_plan)
		{
			return ::fast_io::details::decay::
				print_semantic_optional_scatter_barrier_plan_available<
					line, char_type, outputstmtype,
					::std::remove_reference_t<Args>...>();
		}
		else
		{
			return false;
		}
	}();
	constexpr bool use_linear_control_plan = []() consteval {
		if constexpr (already_forwarded && !has_pack_or_null && has_top_level_condition &&
					  !use_optional_scatter_source_plan && !use_optional_scatter_width_coalescing_plan && !use_optional_scatter_barrier_plan)
		{
			return ::fast_io::operations::decay::print_semantic_linear_control_plan_available<
				line, char_type, outputstmtype, ::std::remove_reference_t<Args>...>();
		}
		else
		{
			return false;
		}
	}();

#if defined(FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC)
	// Diagnostic-only fail-fast gate for real-project shape discovery. It stops before active-record type enumeration and
	// is absent from normal translation units, so it cannot alter public admission, execution, or optimizer-constant paths.
	constexpr ::std::size_t diagnostic_top_level_condition_count{
		(static_cast<::std::size_t>(
			 ::fast_io::details::decay::print_semantic_top_level_condition_v<Args>) +
		 ... + 0u)};
	static_assert(
		diagnostic_top_level_condition_count <
				static_cast<::std::size_t>(
					FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC) ||
			use_optional_scatter_source_plan ||
			use_optional_scatter_width_coalescing_plan ||
			use_optional_scatter_barrier_plan || use_linear_control_plan,
		"unclassified large semantic condition record");
#endif
	constexpr bool bypass_condition_selection = []() consteval {
		if constexpr (sizeof...(Args) >= 41u)
		{
			return true;
		}
		else if constexpr (sizeof...(Args) > 24u)
		{
			// Only the measured dynamic-precision band needs source capability discovery. Keeping this fold inside a
			// discarded size branch is a compile-time isolation proof: a run of at most 24 components never names, and
			// therefore never instantiates, the bounded-source protocol merely to reject this bypass.
			return (false || ... ||
					::fast_io::operations::decay::print_semantic_single_pass_bounded_source<
						char_type, Args>);
		}
		else
		{
			return false;
		}
	}();
	if constexpr (use_optional_scatter_source_plan)
	{
		// This source graph has jointly proved status transparency, ordinary-control equivalence, and a closed leaf set.
		// Emit it before continuation construction so no active-record type product is instantiated. Mutex unwrapping and
		// source normalization already occurred in the outer dispatcher; run-time arm classification retains the selected
		// active record's contiguous/scatter CPO, primitive boundary, and optional newline ownership.
		::fast_io::details::decay::
			print_semantic_optional_scatter_plan_emit<line, char_type>(
				optstm, args...);
	}
	else if constexpr (use_optional_scatter_width_coalescing_plan)
	{
		// The selected fixed descriptors first replay the active record's complete static, precise, and bounded
		// coalescing order. Width segmentation is reached only after those attempts fail without touching the observer;
		// it then uses original sources, preserving the ordinary flat fallback's component and newline boundaries.
		::fast_io::operations::decay::
			print_semantic_optional_scatter_width_coalescing_plan_emit<
				line, char_type>(optstm, args...);
	}
	else if constexpr (use_optional_scatter_barrier_plan)
	{
		// The complete record cannot enter compiler-constant materialization because every admitted direct-only barrier
		// fails that source protocol. Emit each large optional segment through the existing linear plan, each bounded
		// segment through local condition selection, and each barrier through the exact control operation already selected
		// by ordinary flat fallback. No active-record type product spans a barrier.
		::fast_io::operations::decay::
			print_semantic_optional_scatter_barrier_plan_emit<
				line, char_type>(optstm, args...);
	}
	else if constexpr (use_linear_control_plan)
	{
		::fast_io::operations::decay::print_semantic_linear::emit<line>(optstm, args...);
	}

#if defined(FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC)
	else if constexpr (
		diagnostic_top_level_condition_count >=
		static_cast<::std::size_t>(FAST_IO_SEMANTIC_CONDITION_DIAGNOSTIC))
	{
		// The assertion above already rejects this diagnostic-only translation unit. A failed assertion alone does
		// not discard subsequent templates: explicitly stop recovery from instantiating the very Cartesian product
		// being diagnosed. Accepted plans and every build without the diagnostic macro retain their original path.
	}
#endif
	else if constexpr (
		already_forwarded && !has_pack_or_null && !has_top_level_condition &&
		bypass_condition_selection)
	{
		// Every argument is already normalized and no branch can disappear, so condition selection is the identity map:
		// it preserves the same objects in the same order and invokes `emit_flat` exactly once. Calling that continuation
		// directly removes only condition selection's one-prefix-type-per-component nesting; later output strategies retain
		// their own ordinary pack expansions. The 41-component threshold applies to every normalized flat type sequence,
		// not merely the passive run optimized below: identity of condition selection follows from the absence of a
		// top-level condition, pack, or null, independently of leaf protocols. An audited bounded source retains the lower
		// threshold required by dynamic-precision floating records.
		emit_flat(::std::forward<Args>(args)...);
	}
	else if constexpr (already_forwarded && !has_pack_or_null)
	{
		// An already-normalized flat run needs only condition selection before the cost model sees its active leaves.
		::fast_io::details::decay::print_semantic_select_conditions<char_type>(
			emit_flat, ::std::forward<Args>(args)...);
	}
	else
	{
		// The expansion helper also performs character-aware alias/status forwarding for every argument when
		// `already_forwarded` is false. This remains necessary even when the run contains no pack or null: width and
		// condition emitters pass their named child members through this entry with `already_forwarded == false`.
		// Bypassing expansion solely because the type graph was flat used to send the original child to direct-print
		// fallback, while admission and sizing had classified its forwarded representation. The continuation is
		// synchronous, so a value returned by status forwarding remains alive through condition selection and emission.
		::fast_io::details::decay::print_semantic_pack_expand<already_forwarded, char_type>(
			::fast_io::operations::decay::print_semantic_filter_flat_continuation<decltype(emit_flat)>{
				__builtin_addressof(emit_flat)},
			::std::forward<Args>(args)...);
	}
}

/// @brief    Stable-reference dispatcher for one owned, already-decayed print run.
/// @details  The owning entry below materializes each normalized argument once. This helper retains those exact objects
///           across mutex recursion, status selection, and semantic emission; copying a named pack here would duplicate
///           the complete width/condition/pack graph and reject a valid node that owns a move-only leaf. Ordinary inline
///           placement is intentional: GNU flatten cloned mutually exclusive integer, floating, and semantic graphs,
///           moved successful direct output into cold overflow partitions on GCC, and increased measured `.text` by
///           31--36 percent at `-O3`. The compiler already propagates the small type-only dispatch decisions here.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the print run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   decltype(auto) the return value of the selected output customization, when any
template <bool line, typename outputstmtype, typename... Args>
inline constexpr decltype(auto) print_freestanding_decay_impl(outputstmtype &optstm, Args &...args)
{
	if constexpr (!line && sizeof...(Args) == 0u)
	{
		// This is the lowest stable-reference record boundary. Resolve the
		// provider's empty-record contract before mutex or output strategy selection.
		return ::fast_io::operations::decay::print_freestanding_empty_run(optstm);
	}
	else if constexpr (::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<outputstmtype>)
		{
			constexpr bool structural_graph_can_select_empty{
				(false || ... ||
				 (::fast_io::details::decay::print_semantic_pack_argument_v<Args> ||
				  ::fast_io::details::decay::print_semantic_top_level_condition_v<Args>))};
			if constexpr (
				!line && structural_graph_can_select_empty &&
				!::fast_io::details::decay::
					print_freestanding_effective_status_owner<
						line, outputstmtype, Args...>())
			{
				// Pack expansion and condition selection can turn a nonzero source arity into a zero-leaf record. Prove
				// that fact from fast_io-owned structural values before acquiring the mutex. An exact status owner for
				// the original graph was excluded above, so resolving the derived empty record cannot bypass a provider
				// operation. An inconclusive leaf retains the established lock-before-normalization ordering.
				if (::fast_io::details::decay::print_semantic_run_provably_empty(args...))
				{
					return ::fast_io::operations::decay::print_freestanding_empty_run(optstm);
				}
			}
			// Apply synchronization before every output-specific shortcut. In particular an outer status CPO must not
			// pre-empt the wrapper's lock; the corresponding customization belongs on the unlocked observer, whose
			// capabilities are selected recursively while this guard remains alive.
			::fast_io::operations::decay::stream_ref_decay_lock_guard lg{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			// Preserve a stable-reference CPO result exactly; a prvalue result instead creates the sole unlocked owner.
			// Both categories then remain lvalues for the rest of the synchronized recursive operation.
			decltype(auto) unlocked_output =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(
				unlocked_output, args...);
		}
		else
		{
			static_assert(
				::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<outputstmtype>,
				"fast_io: an output mutex CPO requires a storable lock/unlock proxy and a type-progressing, "
				"character-preserving unlocked output reference");
		}
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_status_print_define<line, outputstmtype, Args...>)
	{
		// Status-print streams delegate the whole freestanding run to their status customization.
		return status_print_define<line>(optstm, args...);
	}
	else if constexpr (sizeof...(Args) == 0)
	{
		// Non-line emptiness was resolved before mutex/status selection. The only
		// remaining zero-source record is line mode and owns exactly one newline.
		static_assert(line);
		using char_type = typename outputstmtype::output_char_type;
		return ::fast_io::operations::decay::char_put_decay_dispatch(
			optstm, char_literal_v<u8'\n', char_type>);
	}
	else if constexpr (
		(::fast_io::details::decay::print_semantic_execution_node_v<Args> || ...))
	{
		// Semantic nodes require flattening and condition selection before any contiguous strategy can be proved.
		using char_type = typename outputstmtype::output_char_type;
		return ::fast_io::operations::decay::print_semantic_emit<line, true, char_type>(optstm, args...);
	}
	else if constexpr (
		::fast_io::operations::decay::print_semantic_staged_group<
			typename outputstmtype::output_char_type, ::std::remove_cvref_t<Args>...>::available)
	{
		using char_type = typename outputstmtype::output_char_type;
		using staged_group = ::fast_io::operations::decay::print_semantic_staged_group<
			char_type, ::std::remove_cvref_t<Args>...>;
		/*
		A finite staged maximum is evidence that the customization audited one small
		register envelope rather than the general semantic graph. These arguments are
		already normalized plain leaves, so their compile-time reserve sum is the
		complete capacity proof: condition selection and pack filtering cannot discover
		another branch or value. Bypassing those generic continuations removes the
		wrapper call that otherwise dominates cheap integer conversion.

		A fitting put area is the measured destination for this shortcut. A direct-write-
		only destination or a put area without enough remaining capacity retains the
		established semantic entry, whose stream-specific coalescing policy controls stack
		materialization and refill. Unbounded staged formatters likewise remain on that
		entry until their complete-width matrix proves a bounded placement.
		*/
		if constexpr (staged_group::max_count != SIZE_MAX &&
					  ::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
		{
			constexpr ::std::size_t static_bound{
				::fast_io::operations::decay::print_semantic_static_bounded_total_size<
					line, char_type, ::std::remove_cvref_t<Args>...>()};
			if constexpr (static_bound != 0u && static_bound != SIZE_MAX)
			{
				// A staged member may coexist with a dynamically bounded ordinary leaf such as string_view. Group
				// eligibility proves that the general semantic entry can allocate a bounded run, but it does not make
				// every leaf's upper bound type-static. Keep this direct cursor comparison only when the complete static
				// extent exists; otherwise the unchanged semantic continuation below performs its run-time sizing before
				// staging.
				char_type *const curr{obuffer_curr(optstm)};
				char_type *const end{obuffer_end(optstm)};
				if (curr != nullptr && end != nullptr &&
					static_cast<::std::size_t>(end - curr) >= static_bound) [[likely]]
				{
					char_type *const iter{
						::fast_io::operations::decay::print_semantic_emit_unchecked_run<line, char_type, true>(
							curr, args...)};
					obuffer_set_curr(optstm, iter);
					return;
				}
			}
		}
		return ::fast_io::operations::decay::print_semantic_emit<line, true, char_type>(optstm, args...);
	}
	else
	{
		// Non-semantic non-empty runs choose between the fast reserve/scatter entry and the generic path.
		using char_type = typename outputstmtype::output_char_type;
		if constexpr (::fast_io::details::decay::print_fixed_static_reserve_run_available<
						  outputstmtype, char_type, Args...>())
		{
			::fast_io::details::decay::print_fixed_static_reserve_run<line, outputstmtype, char_type>(optstm, args...);
			return;
		}
		else if constexpr (
			::fast_io::details::decay::print_runtime_scatter_plan_fast_entry_available_v<
				char_type, outputstmtype, Args &...>)
		{
			// Run-time descriptor producers must be selected before the legacy mixed dynamic-reserve entry, whose
			// compile-time descriptor count intentionally models each dynamic producer as one contiguous range.
			return ::fast_io::details::decay::print_runtime_scatter_plan_fast_entry<line>(optstm, args...);
		}
		// A singleton cached-size source can grow the destination once without perturbing mixed-run planning.
		else if constexpr (
			sizeof...(Args) == 1u &&
			(::fast_io::details::decay::
				 print_cached_precise_growable_obuffer_available_v<
					 char_type, outputstmtype, Args> &&
			 ...))
		{
			// Exact in-place append is deliberately a whole-run singleton strategy. Applying it inside the leaf dispatcher
			// would reserve independently for every member of a mixed record and defeat its established combined sizing
			// and scatter choices. Native run-time scatter remains above it; mutex and status normalization remain above
			// this complete plain-run dispatcher.
			FAST_IO_IF_NOT_CONSTEVAL
			{
				(::fast_io::details::decay::
					 print_cached_precise_growable_obuffer<line, char_type>(
						 optstm, args),
				 ...);
				return;
			}
			else
			{
				// Runtime string adapters expose writable capacity through implementation-defined CPOs which are
				// intentionally unavailable during portable constant evaluation. Preserve the existing materialization
				// dispatcher there.
				return ::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(optstm, args...);
			}
		}
		else if constexpr (
			::fast_io::operations::decay::print_plain_single_pass_staging_run<
				line, char_type, outputstmtype, Args...>())
		{
			// A format frontend is only one producer of this shape: ordinary IO packs can likewise contain punctuation
			// beside a recursive one-pass leaf.  Core owns the device decision and lets the complete normalized run share
			// the stream-authorized window; neither grammar nor range code chooses a syscall policy.
			constexpr ::std::size_t capacity{
				::fast_io::details::decay::print_full_output_coalesce_threshold<
					char_type, outputstmtype>()};
			return ::fast_io::operations::decay::
				print_single_pass_stage_run_and_write<line, char_type, capacity>(
					optstm, args...);
		}
		else if constexpr (
			sizeof...(Args) == 1u &&
			(::fast_io::details::decay::print_one_pass_direct_streaming_available_v<
				 char_type, outputstmtype, Args> &&
			 ...))
		{
			// A singleton keeps the cost proof local: the stream explicitly accepts the producer's incremental calls,
			// while no unrelated record leaves are split into additional writes. `print_control_single` repeats the same
			// exact source/destination gate after the no-pack dispatcher reaches the leaf. Native scatter remained first.
			return ::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(optstm, args...);
		}
		else if constexpr (
			!::fast_io::details::decay::print_run_has_put_area_preferred_direct_leaf_v<
				char_type, outputstmtype, Args...> &&
			!::fast_io::details::decay::print_output_run_retains_static_scatter<outputstmtype, Args...> &&
			::fast_io::details::decay::print_controls_dynamic_scatters_reserve_fast_entry_available<
				char_type, ::std::remove_cvref_t<Args>...>())
		{
			// Non-semantic arguments use the dynamic reserve-scatters fast entry unless a true put-area/source pair has
			// proved that the output-aware no-pack dispatcher can consume a direct leaf without premeasurement.
			return ::fast_io::details::decay::print_controls_dynamic_scatters_reserve_fast_entry<line>(optstm,
																									   args...);
		}
		else
		{
			// Non-semantic arguments without the fast entry use the general no-pack freestanding path.
			return ::fast_io::operations::decay::print_freestanding_decay_no_pack<line>(optstm, args...);
		}
	}
}

template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_compiler_constant_materialization_reserve_size() noexcept
{
	::std::size_t total{static_cast<::std::size_t>(line)};
	((total = ::fast_io::details::decay::
		  print_contiguous_char_extent_add_or_unavailable<char_type>(
			  total,
			  print_reserve_size(::fast_io::io_reserve_type<
								 char_type,
								 ::fast_io::details::compiler_constant_materialized_t<
									 char_type, Args>>))),
	 ...);
	return total;
}

/// @brief Bounds the aggregate representation of a materialized proxy run.
/// @details Output reserve size alone cannot constrain a third-party proxy's
///          object representation.  The true arm owns every proxy by value, so
///          reject a run whose aggregate proxy state would exceed the same
///          deliberately small budget used for its output buffer.
template <::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_compiler_constant_materialization_proxy_bytes() noexcept
{
	::std::size_t total{};
	constexpr ::std::size_t maximum{
		::fast_io::details::compiler_constant_materialization_max_bytes};
	((total = total == SIZE_MAX ||
					  sizeof(::fast_io::details::compiler_constant_materialized_t<
							 char_type, Args>) > maximum - total
				  ? SIZE_MAX
				  : total + sizeof(::fast_io::details::compiler_constant_materialized_t<
								   char_type, Args>)),
	 ...);
	return total;
}

/// @brief Tests whether a normalized plain run can be rebound to compiler-constant reserve proxies.
/// @details Mutex/status ownership and semantic selection stay in the established dispatcher.  Requiring at least one
///          changed proxy avoids rerouting an all-fixed run whose existing strategy is already optimal.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool print_compiler_constant_materialization_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> ||
		::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, Args...> ||
		(::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			 outputstmtype> &&
		 (!::fast_io::compiler_constant_obuffer_materialization_safe<
			  char_type, outputstmtype> ||
		  !::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
			  outputstmtype, char_type>)) ||
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_execution_node_v<Args>) ||
		!(::fast_io::compiler_constant_materialization_graph_proven_source_shape<
			  char_type, Args> &&
		  ...) ||
		!(::fast_io::compiler_constant_printable<char_type, Args> && ...))
	{
		return false;
	}
	else
	{
		constexpr bool changes_representation{(false || ... ||
											   !::std::same_as<
												   ::std::remove_cvref_t<Args>,
												   ::fast_io::details::compiler_constant_materialized_t<char_type, Args>>)};
		constexpr ::std::size_t reserve_size{
			::fast_io::operations::decay::
				print_compiler_constant_materialization_reserve_size<
					line, char_type, Args...>()};
		constexpr ::std::size_t proxy_bytes{
			::fast_io::operations::decay::
				print_compiler_constant_materialization_proxy_bytes<
					char_type, Args...>()};
		return changes_representation && reserve_size != SIZE_MAX &&
			   reserve_size <=
				   ::fast_io::details::compiler_constant_materialization_max_bytes /
					   sizeof(char_type) &&
			   proxy_bytes != SIZE_MAX &&
			   proxy_bytes <=
				   ::fast_io::details::compiler_constant_materialization_max_bytes &&
			   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
				   reserve_size, char_type>;
	}
}

template <::std::integral char_type, typename... Args>
// The materialization predicates and forwarding chain are naturally folded by
// GCC 11--16 and Clang 17--23; mandatory inlining only enlarges the obligation
// around a branch which may reject the strategy.
[[nodiscard]] inline constexpr bool
print_compiler_constant_materialization_eligible_run(Args const &...args) noexcept
{
	return (true && ... && print_compiler_constant_materialization_eligible(::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<Args>>, args));
}

/// @brief Applies the optimizer query without emitting an unproved run-time call.
/// @details Audited marker-bearing queries are themselves required to inline
///          and fold unknown fields to false.  An unmarked extension is tested
///          as a direct builtin expression; this is more conservative, but an
///          opaque or noinline query then disappears completely when its result
///          cannot be proved at compile time.
template <::std::integral char_type, typename... Args>
[[nodiscard]] inline constexpr bool
print_compiler_constant_materialization_gate(Args const &...args) noexcept
{
#if FAST_IO_HAS_BUILTIN(__builtin_constant_p)
	if constexpr ((::fast_io::compiler_constant_query_inline_safe<
					   char_type, ::std::remove_cvref_t<Args>> &&
				   ...))
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_materialization_eligible_run<char_type>(
				args...);
	}
	else
	{
		return __builtin_constant_p(
				   ::fast_io::operations::decay::
					   print_compiler_constant_materialization_eligible_run<char_type>(
						   args...)) &&
			   ::fast_io::operations::decay::
				   print_compiler_constant_materialization_eligible_run<char_type>(
					   args...);
	}
#else
	// Native MSVC has no `__builtin_constant_p` equivalent.  Consume every
	// parameter explicitly so the deliberately closed gate also satisfies
	// `/W4 /WX`; `sizeof...(args)` observes only the pack cardinality and does
	// not constitute a use of the individual function parameters.
	((void)args, ...);
	return false;
#endif
}

template <bool line, ::std::integral char_type, typename T, typename... Args>
inline constexpr char_type *
print_compiler_constant_materialization_define_chain(
	char_type *iter, T &value, Args &...args)
{
	iter = print_reserve_define(
		::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>,
		iter, value);
	if constexpr (sizeof...(Args) == 0u)
	{
		if constexpr (line)
		{
			*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
		}
		return iter;
	}
	else
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_materialization_define_chain<line, char_type>(
				iter, args...);
	}
}

/// @brief Owns and coalesces the materialized proxies in one exact strategy frame.
template <bool line, typename outputstmtype, typename... Args>
[[nodiscard]] inline constexpr bool
print_freestanding_decay_compiler_constant_materialized(
	outputstmtype &optstm, Args... args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t reserve_size{[]() consteval {
		::std::size_t total{static_cast<::std::size_t>(line)};
		((total = ::fast_io::details::decay::
			  print_contiguous_char_extent_add_or_unavailable<char_type>(
				  total,
				  print_reserve_size(::fast_io::io_reserve_type<
									 char_type, ::std::remove_cvref_t<Args>>))),
		 ...);
		return total;
	}()};
	static_assert(
		::fast_io::details::decay::print_stack_buffer_size_within_limit<
			reserve_size, char_type>);
	static_assert(
		reserve_size <=
		::fast_io::details::compiler_constant_materialization_max_bytes /
			sizeof(char_type));
	if constexpr (
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>)
	{
		static_assert(
			::fast_io::compiler_constant_obuffer_materialization_safe<
				char_type, outputstmtype>);
		static_assert(
			::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				outputstmtype, char_type>);
		char_type *const curr{obuffer_curr(optstm)};
		char_type *const put_end{obuffer_end(optstm)};
		if (curr == nullptr || put_end == nullptr)
		{
			return false;
		}
		::std::ptrdiff_t const difference{put_end - curr};
		if (difference < 0)
		{
			return false;
		}
		if (reserve_size <= static_cast<::std::size_t>(difference)) [[likely]]
		{
			char_type *const actual_end{
				::fast_io::operations::decay::
					print_compiler_constant_materialization_define_chain<line, char_type>(
						curr, args...)};
			obuffer_set_curr(optstm, actual_end);
			return true;
		}
		// Keep the established formatter as the capacity-miss continuation. Materializing
		// into a local array here would add a cold stack frame to an otherwise direct
		// buffered call and would replace the mature run-time formatter with the proxy.
		return false;
	}
	else
	{
		constexpr ::std::size_t storage_size{reserve_size == 0u ? 1u : reserve_size};
		// The define chain initializes every code unit in the exact record and
		// returns its terminal pointer. Only [buffer, end) reaches the writer, so
		// the unused one-element zero-length storage and any unwritten tail are
		// deliberately unobservable and need no value initialization.
		char_type buffer[storage_size];
		char_type *const end{
			::fast_io::operations::decay::
				print_compiler_constant_materialization_define_chain<line, char_type>(
					buffer, args...)};
		::fast_io::operations::decay::write_all_decay_dispatch(optstm, buffer, end);
		return true;
	}
}

/// @brief Always-inline value-level gate shared by ordinary and format-lowered print calls.
/// @details For an unknown value every builtin predicate folds to false in this wrapper and the only surviving edge is
///          the exact historical dispatcher call.  The replacement objects and their arrays exist solely in the true
///          arm, so they cannot enlarge or otherwise perturb the hot run-time frame.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_freestanding_decay_compiler_constant_dispatch(
	outputstmtype &optstm, Args &...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::print_compiler_constant_materialization_available<
			line, outputstmtype, Args...>())
	{
		if (::fast_io::operations::decay::
				print_compiler_constant_materialization_gate<char_type>(args...))
		{
			if (::fast_io::operations::decay::
					print_freestanding_decay_compiler_constant_materialized<line>(
						optstm,
						print_compiler_constant_materialize_gate_proven(
							::fast_io::io_reserve_type<char_type,
													   ::std::remove_cvref_t<Args>>,
							args)...))
			{
				return;
			}
		}
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, args...);
}

/// @brief Owns values supplied directly to the explicit decayed-print boundary.
/// @details By-value output transport is intentional at this compatibility entry: it owns a directly supplied observer
///          value for the call and exposes its decayed type to the optimizer. The borrowed-output entry below is
///          selected when an enclosing operation has already established a lifetime-valid observer; it retains only
///          the equivalent by-value argument boundary. Both entries immediately continue through stable references,
///          so mutex unwrapping and cold placement do not reconstruct either observer or normalized argument graph.
///          This ownership shim remains ordinary inline; flattening it would recursively impose every mutually
///          exclusive dispatcher strategy on callers instead of leaving the optimizer an ordinary placement boundary.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr decltype(auto) print_freestanding_decay(outputstmtype optstm, Args... args)
{
	return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, args...);
}

/// @brief Recognizes one width around a plain type-bounded leaf for direct put-area materialization.
/// @details The child bound is a type constant, so checking `max(width, child_bound)` does not inspect or format the
///          child.  This is intentionally disjoint from the source-authored one-pass protocol below: dynamic or
///          composite sources retain that stronger CPO and its complete-run planner.
template <::std::integral char_type, typename outputstmtype, typename T>
inline consteval bool print_semantic_single_width_put_area_run() noexcept
{
	if constexpr (
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> ||
		(::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype> &&
		 ::fast_io::details::decay::print_fixed_external_semantic_overstore<
			 char_type, T>::value) ||
		!::fast_io::single_pass_bounded_obuffer_materialization_safe<
			char_type, outputstmtype> ||
		!::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
			outputstmtype, char_type> ||
		!::fast_io::details::decay::print_semantic_execution_node_v<T>)
	{
		return false;
	}
	else
	{
		using node_expression = decltype(::fast_io::details::decay::print_semantic_node_ref(
			::std::declval<T &>()));
		using node_type = ::std::remove_cvref_t<node_expression>;
		if constexpr (!::fast_io::details::decay::print_semantic_width_v<node_type>)
		{
			return false;
		}
		else
		{
			using child_expression = decltype((::std::declval<node_expression>().reference));
			using child_type =
				::fast_io::details::decay::print_semantic_stable_input_forward_t<
					char_type, child_expression>;
			return !::fast_io::details::decay::print_semantic_execution_node_v<child_type> &&
				   ::fast_io::details::decay::print_semantic_static_bounded_size<
					   char_type, child_type>::available;
		}
	}
}

/// @brief Emits one plain type-bounded width without entering complete semantic run planning.
/// @details A successful capacity proof formats the child once into unpublished put-area storage and commits once.
///          A miss has observed only destination cursors and the width scalar, so the established checked dispatcher
///          remains the exact continuation and can refresh, allocate, or stream according to destination policy.
template <bool line, typename outputstmtype, typename width_source_type>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		::fast_io::operations::decay::print_semantic_single_width_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			width_source_type>() &&
		!::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			width_source_type>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, width_source_type>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, width_source_type width_source)
{
	using char_type = typename outputstmtype::output_char_type;
	auto &&node_ref{
		::fast_io::details::decay::print_semantic_node_ref(width_source)};
	using child_expression = decltype((node_ref.reference));
	using child_type =
		::fast_io::details::decay::print_semantic_stable_input_forward_t<
			char_type, child_expression>;
	constexpr ::std::size_t child_bound{
		::fast_io::details::decay::print_semantic_static_bounded_size<
			char_type, child_type>::size};
	::std::size_t required{node_ref.width < child_bound ? child_bound : node_ref.width};
	if constexpr (line)
	{
		required = ::fast_io::details::intrinsics::add_or_overflow_die(
			required, static_cast<::std::size_t>(1u));
	}
	char_type *const current{obuffer_curr(optstm)};
	char_type *const end{obuffer_end(optstm)};
	if (current != nullptr && end != nullptr && current <= end &&
		static_cast<::std::size_t>(end - current) >= required) [[likely]]
	{
		char_type *iter{
			::fast_io::operations::decay::print_semantic_emit_unchecked_arg<
				char_type, true>(current, width_source)};
		if constexpr (line)
		{
			*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
		}
		obuffer_set_curr(optstm, iter);
		return;
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, width_source);
}

/// @brief Gives a destination-neutral bounded semantic run one shared put-area attempt.
/// @details The source concepts prove a non-fatal capacity bound and a non-throwing
///          reserve definition. Core can consequently classify the complete run,
///          write it behind an unpublished cursor, and commit that cursor exactly
///          once. Format lowering contributes only typed semantic leaves; neither
///          this policy nor its capacity check names a format-layer endpoint.
template <bool line, typename outputstmtype, typename... bounded_source_types>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			bounded_source_types...>() &&
		// A direct fixed view with a compile-time reserve proof is a strictly stronger
		// execution policy: it needs neither the conservative capacity probe nor its
		// generic continuation.  Making that implication explicit keeps the overload
		// set disjoint when a leaf, such as exact_decimal, advertises both protocols.
		!::fast_io::details::decay::print_fixed_static_reserve_run_available<
			outputstmtype, typename outputstmtype::output_char_type,
			bounded_source_types...>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, bounded_source_types...>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, bounded_source_types... bounded_sources)
{
	using char_type = typename outputstmtype::output_char_type;
	if (::fast_io::operations::decay::print_semantic_try_single_pass_bounded_put_area<
			line, char_type>(optstm, bounded_sources...)) [[likely]]
	{
		return;
	}
	if constexpr (requires {
					  print_single_pass_bounded_put_area_refresh(optstm);
				  })
	{
		char_type *const begin{obuffer_begin(optstm)};
		char_type *const current{obuffer_curr(optstm)};
		char_type *const end{obuffer_end(optstm)};
		if (begin != nullptr && current != begin && end != nullptr && begin <= end)
		{
			::std::size_t const full_capacity{
				static_cast<::std::size_t>(end - begin)};
			::std::size_t total{line ? 1u : 0u};
			(::fast_io::operations::decay::
				 print_semantic_single_pass_bounded_add_component<char_type>(
					 total, full_capacity, bounded_sources),
			 ...);
			if (total != SIZE_MAX)
			{
				// Only publish the completed prefix after proving that a fresh
				// window admits the rejected source. A source whose conservative
				// bound is inherently too large retains the exact continuation and
				// may still fit the current tail by its actual size.
				print_single_pass_bounded_put_area_refresh(optstm);
				if (::fast_io::operations::decay::
						print_semantic_try_single_pass_bounded_put_area<
							line, char_type>(optstm, bounded_sources...)) [[likely]]
				{
					return;
				}
			}
		}
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, bounded_sources...);
}

/// @brief Handles the first failing large passive/type-bounded band without constructing the generic semantic planner.
/// @details A current-window hit writes and commits once. On a buffered stream with an explicit refresh operation, a
///          tail miss first publishes the pending prefix, restores the full window, and retries the complete run. Only
///          when the proven bound cannot fit a fresh window (or the destination has no refresh operation) does the
///          checked component fold run. The miss path is not cold: a steady buffered stream reaches it periodically.
template <bool line, typename outputstmtype, typename... passive_bounded_types>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		::fast_io::operations::decay::print_semantic_large_passive_bounded_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			passive_bounded_types...>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, passive_bounded_types...>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, passive_bounded_types... passive_bounded_values)
{
	using char_type = typename outputstmtype::output_char_type;
	if (::fast_io::operations::decay::print_semantic_try_large_passive_bounded_put_area<
			line, char_type>(optstm, passive_bounded_values...)) [[likely]]
	{
		return;
	}
	if constexpr (requires { print_single_pass_bounded_put_area_refresh(optstm); })
	{
		char_type *const begin{obuffer_begin(optstm)};
		char_type *const current{obuffer_curr(optstm)};
		char_type *const end{obuffer_end(optstm)};
		if (begin != nullptr && current != begin && end != nullptr && begin <= end)
		{
			::std::size_t const full_capacity{static_cast<::std::size_t>(end - begin)};
			::std::size_t total{line ? 1u : 0u};
			(::fast_io::operations::decay::print_semantic_extended_bounded_add_component<
				 char_type>(total, full_capacity, passive_bounded_values),
			 ...);
			if (total != SIZE_MAX)
			{
				print_single_pass_bounded_put_area_refresh(optstm);
				if (::fast_io::operations::decay::print_semantic_try_large_passive_bounded_put_area<
						line, char_type>(optstm, passive_bounded_values...)) [[likely]]
				{
					return;
				}
			}
		}
	}
	::fast_io::operations::decay::print_semantic_emit_large_passive_checked<
		line, char_type>(optstm, passive_bounded_values...);
}

/// @brief Coalesces a large passive/type-bounded run once when its runtime-width bound fits stream policy.
/// @details The selector establishes structural eligibility and the stream's pre-existing whole-output and stack
///          budgets; the runtime preflight proves that the summed object-dependent reserve maximum fits those budgets.
///          The stack emitter therefore performs one materialization and one `write_all`, avoiding both the generic
///          planner's type graph and one syscall per lowered component on a direct file observer.
template <bool line, typename outputstmtype, typename... passive_bounded_types>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		::fast_io::operations::decay::print_semantic_large_passive_bounded_stack_run<
			line, typename outputstmtype::output_char_type, outputstmtype,
			passive_bounded_types...>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, passive_bounded_types...>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, passive_bounded_types... passive_bounded_values)
{
	using char_type = typename outputstmtype::output_char_type;
	if (::fast_io::operations::decay::print_semantic_try_large_passive_bounded_stack<
			line, char_type>(optstm, passive_bounded_values...)) [[likely]]
	{
		return;
	}
	::fast_io::operations::decay::print_semantic_emit_large_passive_checked<
		line, char_type>(optstm, passive_bounded_values...);
}

/// @brief Gives only an extended bounded run one destination put-area attempt.
/// @details This overload is disjoint from the established compact overload above by component count. A failed cheap
///          capacity proof has not formatted any source or changed the cursor, so the unchanged dispatcher receives the
///          same named objects. Keeping both the attempt and fallback out of the generic selector prevents runs beyond
///          the extended cap from instantiating any of this machinery.
template <bool line, typename outputstmtype, typename... bounded_source_types>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		::fast_io::operations::decay::print_semantic_extended_bounded_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			bounded_source_types...>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, bounded_source_types...>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, bounded_source_types... bounded_sources)
{
	using char_type = typename outputstmtype::output_char_type;
	if (::fast_io::operations::decay::print_semantic_try_extended_bounded_put_area<
			line, char_type>(optstm, bounded_sources...)) [[likely]]
	{
		return;
	}
	if constexpr (requires { print_single_pass_bounded_put_area_refresh(optstm); })
	{
		char_type *const begin{obuffer_begin(optstm)};
		char_type *const current{obuffer_curr(optstm)};
		char_type *const end{obuffer_end(optstm)};
		if (begin != nullptr && current != begin && end != nullptr && begin <= end)
		{
			::std::size_t const full_capacity{static_cast<::std::size_t>(end - begin)};
			::std::size_t total{line ? 1u : 0u};
			(::fast_io::operations::decay::print_semantic_extended_bounded_add_component<
				 char_type>(total, full_capacity, bounded_sources),
			 ...);
			if (total != SIZE_MAX)
			{
				// A steady buffer periodically lacks tail space. Flush only after proving the complete run fits the
				// fresh window, then retry the same read-only bound and one-pass materialization.
				print_single_pass_bounded_put_area_refresh(optstm);
				if (::fast_io::operations::decay::print_semantic_try_extended_bounded_put_area<
						line, char_type>(optstm, bounded_sources...)) [[likely]]
				{
					return;
				}
			}
		}
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, bounded_sources...);
}

/// @brief Gives an unbuffered destination-neutral bounded run one compact stack attempt.
/// @details This overload is disjoint from the put-area entry above. A stream must
///          explicitly advertise a whole-output coalescing threshold before core
///          may materialize the audited run once and issue one contiguous write.
///          Keeping the stack-only shape separate also prevents its continuation
///          graph from perturbing GCC's successful buffered fast path.
template <bool line, typename outputstmtype, typename... bounded_source_types>
	requires(
		requires { typename outputstmtype::output_char_type; } &&
		!::fast_io::operations::decay::print_semantic_single_pass_bounded_put_area_run<
			typename outputstmtype::output_char_type, outputstmtype,
			bounded_source_types...>() &&
		::fast_io::operations::decay::print_semantic_single_pass_bounded_stack_run<
			typename outputstmtype::output_char_type, outputstmtype,
			bounded_source_types...>() &&
		// Preserve the same formal priority as the put-area overload above.  This
		// exclusion also covers destinations whose coalescing threshold and direct
		// fixed-view contract are both visible to overload resolution.
		!::fast_io::details::decay::print_fixed_static_reserve_run_available<
			outputstmtype, typename outputstmtype::output_char_type,
			bounded_source_types...>() &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, bounded_source_types...>)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, bounded_source_types... bounded_sources)
{
	using char_type = typename outputstmtype::output_char_type;
	if (::fast_io::operations::decay::print_semantic_try_single_pass_bounded_stack<
			line, char_type>(optstm, bounded_sources...)) [[likely]]
	{
		return;
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, bounded_sources...);
}

/// @brief Materializes a static prefix and one audited bounded leaf as one logical buffered record.
/// @details A leading immutable scatter followed by one audited bounded leaf is one logical buffered record. The leaf's
/// non-fatal bound and direct-put-area markers prove that its `noexcept` reserve definition can be attempted without
/// observing an arbitrary customization twice. If the conservative bound fits the current put area, both components
/// are emitted there with one final cursor publication. Otherwise a small byte-budgeted frame materializes the record
/// exactly once; its actual extent is either copied into the remaining put area or passed to one `write_all` call.
/// Values whose bound exceeds that frame retain the complete historical dispatcher. This policy is expressed only in
/// the IO layer and applies equally to raw IO manipulators and format-lowered leaves; no format grammar participates.
///
/// This overload deliberately remains ordinary `inline`. Forcing the whole continuation reduced a short-overflow
/// microbenchmark by only about 2--5 percent, but grew the GCC 15 default-linked integration image by about 19 KiB;
/// ordinary placement was also faster for the ample-put-area case on both audited compilers. The rare local-frame arm
/// must therefore remain available for outlining instead of imposing its formatter graph on every buffered caller.
template <::std::integral char_type, typename formatted_scalar_type>
inline constexpr bool print_fixed_prefix_scalar_define_nofail_v =
	requires(formatted_scalar_type value, char_type *iter) {
#if defined(__HERBCEPTIONS__)
		// Formal admission is `!throws(E) && noexcept(E)` for the exact named by-value parameter E executed by both
		// storage arms. The extension's current `noexcept(E)` answer alone is not a deterministic-error proof.
		requires(!throws((print_reserve_define(
			::fast_io::io_reserve_type<char_type, formatted_scalar_type>, iter, value))));
#endif
		{
			print_reserve_define(
				::fast_io::io_reserve_type<char_type, formatted_scalar_type>, iter, value)
		} noexcept -> ::std::same_as<char_type *>;
	};

template <bool line, typename outputstmtype, ::std::integral char_type, ::std::size_t extent,
		  typename formatted_scalar_type>
	requires(
		::std::same_as<typename outputstmtype::output_char_type, char_type> &&
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype,
			::fast_io::manipulators::static_scatter_t<char_type, extent>,
			formatted_scalar_type> &&
		::fast_io::dynamic_reserve_printable<char_type, formatted_scalar_type> &&
		::fast_io::single_pass_bounded_materialization_source<
			char_type, formatted_scalar_type> &&
#if defined(__HERBCEPTIONS__)
		::fast_io::operations::decay::print_fixed_prefix_scalar_define_nofail_v<
			char_type, formatted_scalar_type> &&
		requires {
			{
				print_single_pass_bounded_direct_put_area_safe(
					::fast_io::io_reserve_type<char_type, formatted_scalar_type>)
			} -> ::std::same_as<::std::true_type>;
		}
#else
		requires(formatted_scalar_type value, char_type *iter) {
			{
				print_single_pass_bounded_direct_put_area_safe(
					::fast_io::io_reserve_type<char_type, formatted_scalar_type>)
			} -> ::std::same_as<::std::true_type>;
			{
				print_reserve_define(
					::fast_io::io_reserve_type<char_type, formatted_scalar_type>,
					iter, value)
			} noexcept -> ::std::same_as<char_type *>;
		}
#endif
)
inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm,
	::fast_io::manipulators::static_scatter_t<char_type, extent> static_prefix,
	formatted_scalar_type formatted_scalar)
{
	using static_prefix_type = ::fast_io::manipulators::static_scatter_t<char_type, extent>;
	constexpr ::std::size_t fixed_size{
		::fast_io::details::decay::print_contiguous_char_extent_add_or_unavailable<char_type>(
			extent, static_cast<::std::size_t>(line))};
	constexpr ::std::size_t policy_buffer_size{
		::fast_io::details::decay::print_stack_buffer_max_size<char_type>()};
	constexpr ::std::size_t preferred_buffer_bytes{384u};
	constexpr ::std::size_t preferred_buffer_size{
		preferred_buffer_bytes / sizeof(char_type)};
	constexpr ::std::size_t buffer_size{
		policy_buffer_size < preferred_buffer_size ? policy_buffer_size : preferred_buffer_size};
	if constexpr (fixed_size != SIZE_MAX && fixed_size < buffer_size)
	{
		char_type *const curr{obuffer_curr(optstm)};
		char_type *const end{obuffer_end(optstm)};
		::std::ptrdiff_t const available_difference{
			curr == nullptr || end == nullptr ? -1 : end - curr};
		if (0 <= available_difference)
		{
			::std::size_t const available{
				static_cast<::std::size_t>(available_difference)};
			if (fixed_size <= available)
			{
				auto const scalar_bound{
					::fast_io::single_pass_bounded_materialization_size_invoke<
						char_type>(formatted_scalar, available - fixed_size)};
				if (scalar_bound != SIZE_MAX) [[likely]]
				{
					char_type *iter{print_reserve_define(
						::fast_io::io_reserve_type<char_type, static_prefix_type>,
						curr, static_prefix)};
					iter = print_reserve_define(
						::fast_io::io_reserve_type<
							char_type, formatted_scalar_type>,
						iter, formatted_scalar);
					if constexpr (line)
					{
						*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
					}
					obuffer_set_curr(optstm, iter);
					return;
				}
			}
		}
		constexpr ::std::size_t scalar_capacity{buffer_size - fixed_size};
		::std::size_t const scalar_bound{
			::fast_io::single_pass_bounded_materialization_size_invoke<char_type>(
				formatted_scalar, scalar_capacity)};
		if (scalar_bound != SIZE_MAX) [[likely]]
		{
			char_type record_buffer[buffer_size];
			char_type *iter{print_reserve_define(
				::fast_io::io_reserve_type<char_type, static_prefix_type>,
				record_buffer, static_prefix)};
			iter = print_reserve_define(
				::fast_io::io_reserve_type<char_type, formatted_scalar_type>,
				iter, formatted_scalar);
			if constexpr (line)
			{
				*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
			}
			if (record_buffer + buffer_size < iter) [[unlikely]]
			{
				::fast_io::fast_terminate();
			}
			::std::size_t const required{
				static_cast<::std::size_t>(iter - record_buffer)};
			if (0 <= available_difference &&
				static_cast<::std::size_t>(available_difference) >= required) [[likely]]
			{
				char_type *const next{::fast_io::details::non_overlapped_copy_n(
					record_buffer, required, curr)};
				obuffer_set_curr(optstm, next);
				return;
			}
			::fast_io::operations::decay::write_all_decay_dispatch(
				optstm, record_buffer, iter);
			return;
		}
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, static_prefix, formatted_scalar);
}

/// Fixed external views can bypass the by-value generic borrowed boundary entirely.  The stronger destination marker
/// and static reserve proof make this overload unambiguous for the common scalar/literal record while every owner,
/// mutex, and dynamic source continues through the established overload set below.
template <bool line, typename outputstmtype, typename... Args>
	requires(
		(sizeof...(Args) != 0u) &&
		::fast_io::details::decay::print_fixed_static_reserve_run_available<
			outputstmtype, typename outputstmtype::output_char_type, Args...>() &&
		!(::fast_io::details::decay::print_semantic_execution_node_v<Args> || ...) &&
		!::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_status_print_define<line, outputstmtype, Args...>)
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, Args... args)
{
	using char_type = typename outputstmtype::output_char_type;
	::fast_io::details::decay::print_fixed_static_reserve_run<line, outputstmtype, char_type>(optstm, args...);
}

// Declare the generic borrowed fallback after all destination-specialized overloads so the mutex adapter below can
// re-run exactly this complete overload set for its unlocked observer. The definition remains after the adapter,
// making the constrained mutex overload more specialized at the original call site.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr decltype(auto) print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, Args... args);

/// @brief Selects borrowed-output policy from the final destination while holding its outer lock.
/// @details A mutex wrapper contributes synchronization but does not replace the unlocked observer's capacity or
///          coalescing capabilities. This constrained overload is intentionally declared after every destination
///          specialization and before the generic fallback: recursive overload resolution can therefore select the
///          same put-area or direct-write strategy the unlocked observer would receive without a wrapper. The guard
///          spans that complete synchronous call, including status dispatch and a possible buffered refresh.
template <bool line, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
			outputstmtype>)
inline constexpr decltype(auto)
print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, Args... args)
{
	if constexpr (sizeof...(Args) == 0u)
	{
		// This mutex-specialized overload is selected before the generic
		// borrowed boundary. Route the complete zero-source record to the
		// line-aware dispatcher before this adapter acquires a lock itself.
		return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(
			optstm);
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<
			outputstmtype>)
	{
		constexpr bool structural_graph_can_select_empty{
			(false || ... ||
			 (::fast_io::details::decay::print_semantic_pack_argument_v<Args> ||
			  ::fast_io::details::decay::print_semantic_top_level_condition_v<Args>))};
		if constexpr (
			!line && structural_graph_can_select_empty &&
			!::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					line, outputstmtype, Args...>())
		{
			// This ownership adapter precedes the stable-reference dispatcher, so it must apply the same print-level
			// active-record proof before taking its own lock. Only fast_io structural values are inspected; a source-graph
			// status owner or any inconclusive leaf preserves the ordinary synchronized recursive path below.
			if (::fast_io::details::decay::print_semantic_run_provably_empty(args...))
			{
				return ::fast_io::operations::decay::print_freestanding_empty_run(optstm);
			}
		}
		// The arguments are sole owners at this by-value boundary. Moving them transports the normalized graph without
		// another copy; the recursive call completes before the lock guard releases either an lvalue or owned prvalue
		// unlocked observer.
		::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
			::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
		decltype(auto) unlocked_output =
			::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
		return ::fast_io::operations::decay::print_freestanding_decay_borrowed_output<line>(
			unlocked_output, ::std::move(args)...);
	}
	else
	{
		static_assert(
			::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<
				outputstmtype>,
			"fast_io: an output mutex CPO requires a storable lock/unlock proxy and a type-progressing, "
			"character-preserving unlocked output reference");
	}
}

/// @brief Borrows an output after applying the value-level normalization gate.
/// @details GCC 13--16 and Clang 21--23 already inline this adapter where profitable at `-O3`.
///          Forcing it is unnecessary and increases GCC 13 code size and call count.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr decltype(auto)
print_freestanding_decay_borrowed_output(
	outputstmtype &optstm, Args... args)
{
	return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, args...);
}

/// @brief Borrows a normalized output and every argument from an enclosing synchronous strategy frame.
/// @details Some adapters transform a heterogeneous argument run immediately before ordinary dispatch. A transformed
///          leaf may be a newly created pointer bridge, while an unchanged leaf may be an exact reference to a
///          noncopyable owner. A second by-value argument boundary cannot represent both cases: it either copies the
///          unchanged owner or forces the bridge to escape its full expression. Forwarding-reference parameters bind
///          both categories without constructing either one, and the immediate call below presents their named
///          lvalues to the established stable-reference dispatcher. The complete call is synchronous, so a temporary
///          bridge remains alive until emission returns and no borrowed argument can escape. This is an internal
///          strategy entry, not a public normalization boundary; callers must already have performed alias/status
///          forwarding and established every referent's lifetime. Its ordinary inline boundary is deliberate: this
///          forwarding shim has no value-level compiler-constant query that could justify recursively flattening the
///          downstream formatting algorithms into every caller.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr decltype(auto) print_freestanding_decay_borrowed_output_and_arguments(
	outputstmtype &optstm, Args &&...args)
{
	return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, args...);
}

/// @brief    Cold wrapper around the freestanding print dispatcher.
/// @details  This keeps unlikely front-end call sites out of the hot dispatcher body on compilers that support cold
///           placement or branch hints.
/// @tparam   line          true when a trailing newline is appended
/// @tparam   outputstmtype the decayed output stream reference type
/// @tparam   Args          the argument types in the print run
/// @param    optstm        the output stream reference
/// @param    args          the arguments to emit
/// @return   decltype(auto) the return value of the selected output customization, when any
template <bool line, typename outputstmtype, typename... Args>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
inline constexpr decltype(auto) print_freestanding_decay_cold(outputstmtype optstm, Args... args)
{
#if !__has_cpp_attribute(__gnu__::__cold__) && __has_cpp_attribute(unlikely)
	if (true) [[unlikely]]
#endif
		return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm, args...);
}

/// @brief Retains cold placement while borrowing an observer normalized by the public operation.
/// @details The ordinary cold entry above is an explicit low-level ownership boundary. A public stream-reference CPO
///          may instead return a stable, non-trivial lvalue whose identity is the device cursor itself. Copying that
///          object merely to reach cold code is neither a lifetime extension nor a transport optimization, and can be
///          ill-formed for a move-only observer. This sibling owns the already-forwarded argument proxies exactly as
///          before but preserves the lifetime and identity of the observer established by the enclosing operation.
template <bool line, typename outputstmtype, typename... Args>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
inline constexpr decltype(auto) print_freestanding_decay_cold_borrowed_output(
	outputstmtype &optstm, Args... args)
{
#if !__has_cpp_attribute(__gnu__::__cold__) && __has_cpp_attribute(unlikely)
	if (true) [[unlikely]]
#endif
		return ::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm, args...);
}

/// @brief Proves that the narrow passive-run entry may publish only its final put-area cursor.
/// @details The deferred-commit CPO is the extensibility boundary: ordinary put-area operations and a terminating
///          overflow policy do not prove that folding intermediate cursor publications preserves observable behavior.
///          Keeping this proof local avoids changing the strategy selected by ordinary dispatch.
template <::std::integral char_type, typename outputstmtype>
inline constexpr bool print_passive_mixed_put_area_destination_safe =
	::fast_io::deferred_obuffer_commit_safe<char_type, outputstmtype>;

/// @brief Tests whether a complete normalized run can use the compact passive mixed put-area entry.
/// @details Two leaves are the smallest possible mixed shape: one reserve/static component and one scatter component.
///          The capability proof below, rather than leaf count alone, still requires the whole run to be passive,
///          bounded, non-locked, and safe for one deferred cursor publication. Clang 23 measurements across 2/3/4/7/8
///          leaves reduced the two-leaf path from 1.22 ns to 0.90 ns and the seven-leaf path from 6.07 ns to 2.76 ns.
///          A 128-format object also shrank from 18,910 to 12,682 text bytes; compile RSS decreased from 421 to 414 MiB,
///          while compile time rose from 3.00 s to 3.35 s. Thus admitting the minimum proved shape does not trade the
///          short-path win for text/I-cache expansion. A one-leaf run cannot be mixed and remains on ordinary dispatch.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool print_passive_mixed_put_area_fast_entry_available() noexcept
{
	// Dispatcher recursion passes a named output expression. Preserve const while removing only reference transport so
	// the fast-entry proof cannot borrow mutable put-area operations from an observer runtime cannot mutate.
	using output_type = ::std::remove_reference_t<outputstmtype>;
	if constexpr (sizeof...(Args) < 2u ||
				  !requires { typename output_type::output_char_type; })
	{
		return false;
	}
	else
	{
		using char_type = typename output_type::output_char_type;
		constexpr auto result{
			::fast_io::details::decay::find_continuous_scatters_n<
				char_type, ::std::remove_cvref_t<Args>...>()};
		return result.position == sizeof...(Args) && result.hasscatters && result.hasreserve &&
			   !result.hasdynamicreserve &&
			   ::fast_io::operations::decay::defines::has_obuffer_basic_operations<output_type> &&
			   ::fast_io::operations::decay::print_passive_mixed_put_area_destination_safe<
				   char_type, output_type> &&
			   ::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				   output_type, char_type> &&
			   !::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<
				   output_type> &&
			   !::fast_io::operations::decay::defines::has_status_print_define<
				   line, output_type, ::std::remove_cvref_t<Args>...> &&
			   ::fast_io::details::decay::print_buffered_mixed_put_area_run_available<
				   sizeof...(Args), char_type, ::std::remove_cvref_t<Args>...>();
	}
}

/// @brief Keeps the capacity-miss continuation outside an admitted passive mixed caller.
template <bool line, typename outputstmtype, typename... Args>
#if __has_cpp_attribute(__gnu__::__noinline__)
[[__gnu__::__noinline__]]
#elif __has_cpp_attribute(msvc::noinline)
[[msvc::noinline]]
#endif
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
inline constexpr void print_passive_mixed_put_area_fallback(
	outputstmtype &optstm, Args &...args)
{
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm, args...);
}

/// @brief Emits one complete passive mixed run after a single capacity check and commits its cursor once.
/// @details The compact copy shape prevents a target-specific short-copy ladder from being cloned for every dynamic
///          scatter.  A capacity miss synchronously borrows the still-live normalized values in the historical
///          dispatcher; no descriptor copy or second alias customization is performed. Clang 23 outlines this entry
///          at `-O3` when its existing force-inline marker is removed: the measured two-leaf path regresses from about
///          0.90 ns to 2.01 ns and the seven-leaf path from 2.76 ns to 7.88 ns. This is the single proved inlining
///          boundary; the cold fallback remains explicitly out of line.
template <bool line, typename outputstmtype, typename... Args>
// Only Clang 23 was observed to outline this small successful put-area entry:
// forcing it changes the measured two-leaf path from 2.01 ns to 0.90 ns. Keep
// the positive endpoint future-open without imposing the policy on unproved
// GCC, older Clang, or MSVC releases.
#if defined(__clang__) && __clang_major__ >= 23
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void print_passive_mixed_put_area_fast_entry(
		outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(
		::fast_io::operations::decay::print_passive_mixed_put_area_fast_entry_available<
			line, outputstmtype, Args...>());
	bool emitted{};
	if constexpr (sizeof...(Args) > 96u)
	{
		// The direct-fold implementation is selected only after Clang's measured
		// recursive-inlining limit; the discarded branch is absent from smaller code.
		emitted = ::fast_io::details::decay::print_buffered_mixed_put_area_try_large<
			true, line, sizeof...(Args), char_type>(optstm, args...);
	}
	else
	{
		emitted = ::fast_io::details::decay::print_buffered_mixed_put_area_try_impl<
			true, line, sizeof...(Args), char_type>(optstm, args...);
	}
	if (emitted) [[likely]]
	{
		return;
	}
	if constexpr (
		::fast_io::operations::decay::defines::has_obuffer_overflow_never_define<
			::std::remove_reference_t<outputstmtype>>)
	{
		// Test the exact named output expression: a const observer cannot borrow a mutable-only terminal assertion. A
		// proven no-overflow put area has no continuation. Terminating here also avoids materializing the normalized pack
		// solely for an unreachable continuation at call sites whose capacity is visible to the optimizer.
		::fast_io::fast_terminate();
	}
	else
	{
		::fast_io::operations::decay::print_passive_mixed_put_area_fallback<line>(
			optstm, args...);
	}
}

/// @brief Emits source arguments through an output reference that has already been normalized.
/// @details Recursive formatters such as range views are entered from `print_define` with the exact observer chosen by
///          the outer public operation. Re-entering `print_freestanding` would incorrectly require that observer to
///          provide another `output_stream_ref` customization. This bridge deliberately normalizes only the source
///          side: semantic inputs retain their original value category, while ordinary inputs are aliased from the
///          named parameter just as they are at the public entry. The resulting compact values or exact-reference
///          `parameter` wrappers cross the argument ownership boundary by value. The enclosing print operation has
///          already established a lifetime-valid output observer, whether caller-owned or materialized, so this bridge
///          keeps one stable borrow. That is the lifetime premise for a move-only observer and the reason nested range
///          iteration cannot accumulate observer copies.
template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_freestanding_decay_unforwarded(outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr ((false || ... ||
				   ::fast_io::details::decay::print_semantic_input_argument_v<char_type, Args &&>))
	{
		::fast_io::operations::decay::print_freestanding_decay_borrowed_output<line>(
			optstm,
			::fast_io::details::decay::print_semantic_input_forward<char_type>(
				::std::forward<Args>(args))...);
	}
	else
	{
		::fast_io::operations::decay::print_freestanding_decay_borrowed_output<line>(
			optstm, ::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(args))...);
	}
}

/// @brief Cold counterpart of the source-normalization bridge used by diagnostic output front doors.
/// @details Source expressions are normalized with the same category policy as the ordinary bridge, but the completed
///          normalized run enters the established cold borrowed-output continuation. Keeping this as the false arm of
///          the compiler-constant gate preserves diagnostic call-site placement without moving the optional true-arm
///          materialization into every cold fallback.
template <bool line, typename outputstmtype, typename... Args>
#if __has_cpp_attribute(__gnu__::__cold__)
[[__gnu__::__cold__]]
#endif
inline constexpr void print_freestanding_decay_cold_unforwarded(
	outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr ((false || ... ||
				   ::fast_io::details::decay::print_semantic_input_argument_v<
					   char_type, Args &&>))
	{
		::fast_io::operations::decay::print_freestanding_decay_cold_borrowed_output<line>(
			optstm,
			::fast_io::details::decay::print_semantic_input_forward<char_type>(
				::std::forward<Args>(args))...);
	}
	else
	{
		::fast_io::operations::decay::print_freestanding_decay_cold_borrowed_output<line>(
			optstm,
			::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(args))...);
	}
}

/// @brief Identifies a source leaf whose own CPO authorizes replacement before alias/status normalization.
/// @details A volatile source is never a candidate even when its cv-stripped value type owns the protocol. Reading it
///          is observable, so neither `__builtin_constant_p` nor an early materializer may inspect it. This fail-closed
///          source check also preserves the original volatile-qualified object for ordinary alias/status normalization.
template <::std::integral char_type, typename T>
inline consteval bool
print_compiler_constant_pre_normalization_candidate() noexcept
{
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<T>>)
	{
		return false;
	}
	else if constexpr (
		!::fast_io::compiler_constant_materialization_graph_proven_source_shape<
			char_type, ::std::remove_cvref_t<T>>)
	{
		// Semantic replaceability alone cannot prove that a successful optimizer query erases this provider's complete
		// formatter graph. An unclassified extension therefore remains on the historical source path before its
		// replacement type or value query can be formed.
		return false;
	}
#if !FAST_IO_HAS_BUILTIN(__builtin_constant_p)
	else if constexpr (
		::fast_io::manipulators::is_static_arg_v<::std::remove_cvref_t<T>>)
	{
		// An explicit static argument is a type-owned constant provider, not an
		// optimizer-discovered value. Its eligibility CPO is unconditionally true
		// only when the native consteval materializer exists, so native MSVC may
		// preserve the independent rodata path without pretending that the frontend
		// implements __builtin_constant_p. Keeping this exception inside the
		// no-builtin partition leaves every measured GNU/Clang source-shape policy
		// unchanged. The complete source contract still rejects an unsupported NTTP
		// spelling, including native-MSVC floating providers.
		return ::fast_io::compiler_constant_pre_normalization_safe<
			char_type, ::std::remove_cvref_t<T>>;
	}
	else
	{
		// A frontend with no optimizer-constant query cannot enter this strategy.
		// Rejecting the source here is stronger than calling an eligibility CPO
		// which returns false: replacement types, materializers, and the discarded
		// true arm are never instantiated. The type-level static-provider case was
		// discharged above and therefore never depends on this frontend facility.
		return false;
	}
#else
	else if constexpr (
		::fast_io::compiler_constant_borrowed_text_source_shape<
			char_type, ::std::remove_cvref_t<T>>)
	{
		// A standard string/view already normalizes to its exact borrowed
		// pointer/extent pair. Its compiler-constant proxy carries the same pair,
		// so a successful query cannot remove a formatter and only adds a bounded
		// character scan or an automatic copy. Classify that leaf as passive at
		// this shared print/concat source boundary. Other leaves in the same record
		// remain independent candidates, while the text query and materializer are
		// structurally absent from both the constant and unknown continuations.
		return false;
	}
#if defined(__clang__)
	// Recursive IR/assembly audits show that Clang 13--20 retains proxy code even for raw integers and floating values.
	// Clang 21--23 fully erase only the flat scalar source category; precision, timestamp, width, and format-wrapper
	// sources retain both replacement and native formatter graphs. Keep those categories query-free until an independent
	// consumer/version deletion proof exists. Test the marker-only shape before the complete source protocol so an
	// unsupported category cannot instantiate a replacement customization as a side effect of classification.
	else if constexpr (__clang_major__ < 21)
	{
		// Old Clang releases are rejected before even looking up the optional source-shape marker.
		return false;
	}
	else if constexpr (
		!::fast_io::compiler_constant_simple_scalar_source_shape<
			char_type, ::std::remove_cvref_t<T>>)
	{
		return false;
	}
#endif
	else
	{
		return ::fast_io::compiler_constant_pre_normalization_safe<
			char_type, ::std::remove_cvref_t<T>>;
	}
#endif
}

template <::std::integral char_type, typename T>
inline constexpr bool print_compiler_constant_pre_normalization_candidate_v{
	::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_candidate<char_type, T>()};

/// @brief Reports whether a source record contains any leaf authorized for compiler-constant replacement.
/// @details This is a type-only fold. It neither forms a replacement type nor evaluates a value query, so entry-family
///          and destination proofs may reject the complete record before the optional strategy graph exists.
template <::std::integral char_type, typename... Args>
inline constexpr bool print_compiler_constant_pre_normalization_has_candidate_v{
	(false || ... ||
	 ::fast_io::operations::decay::
		 print_compiler_constant_pre_normalization_candidate_v<char_type, Args>)};

template <::std::integral char_type, typename T,
		  bool = ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<char_type, T>>
struct print_compiler_constant_pre_normalization_replacement
{
	using type = T &&;
	inline static constexpr ::std::size_t proxy_bytes{};
	inline static constexpr ::std::size_t reserve_size{};
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_replacement<char_type, T, true>
{
	using source_type = ::std::remove_cvref_t<T>;
	using proxy_type =
		::fast_io::details::compiler_constant_materialized_t<char_type, source_type>;
	using type = proxy_type &&;
	inline static constexpr ::std::size_t proxy_bytes{sizeof(proxy_type)};
	inline static constexpr ::std::size_t reserve_size{print_reserve_size(
		::fast_io::io_reserve_type<char_type, proxy_type>)};
};

template <::std::integral char_type, typename T>
using print_compiler_constant_pre_normalization_replacement_t =
	typename ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_replacement<char_type, T>::type;

/// @brief Models the exact top-level normalized type seen by the historical source bridge.
template <::std::integral char_type, bool semantic_run, typename T>
struct print_compiler_constant_pre_normalization_normalized;

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_normalized<char_type, true, T>
{
	using type = ::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
		::std::declval<T>()))>;
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_normalized<char_type, false, T>
{
	using named_source = ::std::remove_reference_t<T> &;
	using type = ::std::remove_cvref_t<decltype(::fast_io::io_print_forward<char_type>(
		::fast_io::io_print_alias(::std::declval<named_source>())))>;
};

template <::std::integral char_type, bool semantic_run, typename T>
using print_compiler_constant_pre_normalization_normalized_t =
	typename ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_normalized<
			char_type, semantic_run, T>::type;

/// @brief Models the normalized type produced by the compiler-constant true arm for one original source.
/// @details A non-candidate follows the historical normalization category: a semantic run forwards its original
///          expression, while a plain run aliases the helper's named lvalue. A candidate is different because the
///          materializer creates a new owned proxy and both true-arm forwarders normalize that proxy as an xvalue.
///          This distinction is observable through ref-qualified alias CPOs and therefore belongs in the status proof.
template <::std::integral char_type, bool semantic_run, typename T,
		  bool = ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<char_type, T>>
struct print_compiler_constant_pre_normalization_replacement_normalized
{
	using type = ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_normalized_t<
			char_type, semantic_run, T>;
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_replacement_normalized<
	char_type, false, T, true>
{
	using proxy_type =
		::fast_io::details::compiler_constant_materialized_t<
			char_type, ::std::remove_cvref_t<T>>;
	using type = ::std::remove_cvref_t<decltype(::fast_io::io_print_forward<char_type>(
		::fast_io::io_print_alias(::std::declval<proxy_type &&>())))>;
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_replacement_normalized<
	char_type, true, T, true>
{
	using proxy_type =
		::fast_io::details::compiler_constant_materialized_t<
			char_type, ::std::remove_cvref_t<T>>;
	using type = ::std::remove_cvref_t<decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
		::std::declval<proxy_type &&>()))>;
};

template <::std::integral char_type, bool semantic_run, typename T>
using print_compiler_constant_pre_normalization_replacement_normalized_t =
	typename ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_replacement_normalized<
			char_type, semantic_run, T>::type;

/// @brief Models one true-arm argument when immutable fragments are preserved as descriptors.
/// @details Only an opted-in candidate whose materialized proxy owns the static-fragment protocol changes shape: the
///          true arm wraps that owned proxy so its descriptor storage remains alive through synchronous scatter output.
///          Every other source retains the exact replacement-normalization category proved above.
template <::std::integral char_type, bool semantic_run, typename T,
		  bool = ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<char_type, T>>
struct print_compiler_constant_pre_normalization_preserved_fragment_normalized
	: ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_replacement_normalized<
			  char_type, semantic_run, T>
{};

template <::std::integral char_type, bool semantic_run, typename T>
struct print_compiler_constant_pre_normalization_preserved_fragment_normalized<
	char_type, semantic_run, T, true>
{
	using proxy_type =
		::fast_io::details::compiler_constant_materialized_t<
			char_type, ::std::remove_cvref_t<T>>;
	using type = ::std::conditional_t<
		::fast_io::compiler_constant_static_fragment_printable<
			char_type, proxy_type>,
		::fast_io::details::decay::
			print_compiler_constant_static_fragment_proxy<char_type, proxy_type>,
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_replacement_normalized_t<
				char_type, semantic_run, T>>;
};

template <::std::integral char_type, bool semantic_run, typename T>
using print_compiler_constant_pre_normalization_preserved_fragment_normalized_t =
	typename ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_preserved_fragment_normalized<
			char_type, semantic_run, T>::type;

template <typename... Args>
struct print_compiler_constant_pre_normalization_type_list
{};

template <bool allow_staged_obuffer, bool line, typename outputstmtype,
		  typename source_list, typename replacement_list,
		  typename preserved_fragment_list = replacement_list>
struct print_compiler_constant_pre_normalization_output_safe;

template <bool allow_staged_obuffer, bool line, typename outputstmtype,
		  typename... SourceArgs, typename... ReplacementArgs,
		  typename... PreservedFragmentArgs>
struct print_compiler_constant_pre_normalization_output_safe<
	allow_staged_obuffer, line, outputstmtype,
	print_compiler_constant_pre_normalization_type_list<SourceArgs...>,
	print_compiler_constant_pre_normalization_type_list<ReplacementArgs...>,
	print_compiler_constant_pre_normalization_type_list<
		PreservedFragmentArgs...>>
{
	inline static constexpr bool value{[]() consteval {
		if constexpr (
			::fast_io::operations::decay::defines::
				has_output_or_io_stream_mutex_ref_define<outputstmtype>)
		{
			if constexpr (
				::fast_io::operations::decay::defines::
					has_complete_output_stream_mutex_protocol<outputstmtype>)
			{
				using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
					::std::declval<outputstmtype &>()))>;
				return ::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_output_safe<
						allow_staged_obuffer, line, unlocked_output,
						print_compiler_constant_pre_normalization_type_list<
							SourceArgs...>,
						print_compiler_constant_pre_normalization_type_list<
							ReplacementArgs...>,
						print_compiler_constant_pre_normalization_type_list<
							PreservedFragmentArgs...>>::value;
			}
			else
			{
				return false;
			}
		}
		else
		{
			// Direct synchronous scatter output is the only destination class for
			// which the true arm exposes fragment-owner wrappers to the complete
			// dispatcher. Its exact wrapper pack must decline status independently
			// of both the historical source pack and the ordinary proxy pack.
			return (!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
						outputstmtype> ||
					allow_staged_obuffer ||
					(::fast_io::compiler_constant_obuffer_materialization_safe<
						 typename outputstmtype::output_char_type, outputstmtype> &&
					 ::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
						 outputstmtype,
						 typename outputstmtype::output_char_type>)) &&
				   !::fast_io::operations::decay::defines::has_status_print_define<
					   line, outputstmtype, SourceArgs...> &&
				   !::fast_io::operations::decay::defines::has_status_print_define<
					   line, outputstmtype, ReplacementArgs...> &&
				   (!::fast_io::details::decay::
						print_output_retains_static_scatter<outputstmtype> ||
					!::fast_io::operations::decay::defines::has_status_print_define<
						line, outputstmtype, PreservedFragmentArgs...>);
		}
	}()};
};

/// @brief Implements the common source/status/proxy proof for compiler-constant pre-normalization.
/// @details `require_reserve_budget` is true for contiguous materialization, where every replacement's maximum reserve
///          spelling can become automatic character storage.  A direct immutable-fragment strategy sets it false: in
///          that path only the compact replacement object and a separately bounded descriptor array exist, so a large
///          all-values reserve capacity (notably fixed floating output) is irrelevant and must not reject the source.
template <bool require_reserve_budget, bool line, bool allow_staged_obuffer,
		  typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_common_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool has_candidate{(false || ... ||
								  ::fast_io::operations::decay::
									  print_compiler_constant_pre_normalization_candidate_v<char_type, Args>)};
	if constexpr (!has_candidate)
	{
		return false;
	}
	else
	{
		constexpr bool source_semantic_run{(false || ... ||
											::fast_io::details::decay::print_semantic_input_argument_v<
												char_type, Args>)};
		constexpr bool replacement_semantic_run{(false || ... ||
												 ::fast_io::details::decay::print_semantic_input_argument_v<
													 char_type,
													 ::fast_io::operations::decay::
														 print_compiler_constant_pre_normalization_replacement_t<
															 char_type, Args>>)};
		if constexpr (source_semantic_run || replacement_semantic_run)
		{
			// A top-level semantic node is not the complete operation record. Pack
			// expansion and condition selection can change both active types and
			// arity, so a status probe on the node itself proves nothing about the
			// dispatcher reached after expansion. No current source/proxy protocol
			// supplies a value-independent active-record proof; fail closed here.
			return false;
		}
		else
		{
			using source_types =
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_type_list<
						::fast_io::operations::decay::
							print_compiler_constant_pre_normalization_normalized_t<
								char_type, false, Args>...>;
			using replacement_types =
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_type_list<
						::fast_io::operations::decay::
							print_compiler_constant_pre_normalization_replacement_normalized_t<
								char_type, false, Args>...>;
			using preserved_fragment_types =
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_type_list<
						::fast_io::operations::decay::
							print_compiler_constant_pre_normalization_preserved_fragment_normalized_t<
								char_type, false, Args>...>;
			if constexpr (!::fast_io::operations::decay::
							  print_compiler_constant_pre_normalization_output_safe<
								  allow_staged_obuffer, line, outputstmtype,
								  source_types, replacement_types,
								  preserved_fragment_types>::value)
			{
				return false;
			}
			else
			{
				constexpr ::std::size_t maximum{
					::fast_io::details::compiler_constant_materialization_max_bytes};
				::std::size_t proxy_bytes{};
				::std::size_t reserve_size{};
				((proxy_bytes = proxy_bytes == SIZE_MAX ||
										::fast_io::operations::decay::
												print_compiler_constant_pre_normalization_replacement<
													char_type, Args>::proxy_bytes > maximum - proxy_bytes
									? SIZE_MAX
									: proxy_bytes +
										  ::fast_io::operations::decay::
											  print_compiler_constant_pre_normalization_replacement<
												  char_type, Args>::proxy_bytes),
				 ...);
				((reserve_size = reserve_size == SIZE_MAX ||
										 ::fast_io::operations::decay::
												 print_compiler_constant_pre_normalization_replacement<
													 char_type, Args>::reserve_size > maximum / sizeof(char_type) - reserve_size
									 ? SIZE_MAX
									 : reserve_size +
										   ::fast_io::operations::decay::
											   print_compiler_constant_pre_normalization_replacement<
												   char_type, Args>::reserve_size),
				 ...);
				if constexpr (require_reserve_budget)
				{
					return proxy_bytes != SIZE_MAX && proxy_bytes <= maximum &&
						   reserve_size != SIZE_MAX &&
						   reserve_size <= maximum / sizeof(char_type);
				}
				else
				{
					return proxy_bytes != SIZE_MAX && proxy_bytes <= maximum;
				}
			}
		}
	}
}

/// @brief Proves that a public source run has a small useful contiguous pre-normalization replacement.
/// @details Only explicitly opted source leaves contribute to the proxy/reserve budget. Every other argument remains
///          the exact expression supplied to the historical bridge. Mutex and whole-run status owners are excluded:
///          both protocols must observe the original normalized type graph before any strategy replacement.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_available() noexcept
{
	return ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_common_available<
			true, line, false, outputstmtype, Args...>();
}

template <::std::integral char_type, typename T>
// This query must execute in the source caller.  A strict constant/runtime
// assembly pair showed that ordinary placement on GCC 11/12 both rejected a
// literal precision float and retained the constant proxy graph for an unknown
// precision float.  GCC 13--16 and Clang 21--23 also need the marker for the
// previously measured caller-size boundary.  The function performs only the
// side-effect-free query; neither materialization nor formatting is in its
// false arm.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr bool
	print_compiler_constant_pre_normalization_eligible_one(T const &value) noexcept
{
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, T>)
	{
		return print_compiler_constant_materialization_eligible(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>, value);
	}
	else
	{
		return true;
	}
}

template <::std::integral char_type, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr bool
	print_compiler_constant_pre_normalization_gate(Args const &...args) noexcept
{
	return (true && ... &&
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_eligible_one<char_type>(args));
}

template <::std::integral char_type, typename T>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr decltype(auto)
	print_compiler_constant_pre_normalization_materialize_one(T &&value) noexcept
{
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, T>)
	{
		return print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>,
			value);
	}
	else
	{
		return ::std::forward<T>(value);
	}
}

template <::std::integral char_type, typename T,
		  bool = ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<char_type, T>>
struct print_compiler_constant_partial_fragment_candidate
	: ::std::false_type
{};

template <::std::integral char_type, typename T>
struct print_compiler_constant_partial_fragment_candidate<char_type, T, true>
	: ::std::bool_constant<
		  ::fast_io::compiler_constant_static_fragment_printable<
			  char_type,
			  ::std::remove_cvref_t<
				  ::fast_io::operations::decay::
					  print_compiler_constant_pre_normalization_replacement_t<
						  char_type, T>>>>
{};

/// @brief Admits per-leaf compiler-constant selection only on synchronous unbuffered native-scatter outputs.
/// @details Whole-run status, mutex, and replacement-budget checks remain owned by the existing all-or-nothing proof.
///          Semantic nodes are left to their established graph traversal; this narrow path handles flat scalar runs
///          and cannot cause an unknown-only call to enter descriptor materialization.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_partial_fragment_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	return ::fast_io::operations::decay::
			   print_compiler_constant_pre_normalization_common_available<
				   false, line, false, outputstmtype, Args...>() &&
		   ::fast_io::details::decay::
			   print_output_retains_static_scatter<outputstmtype> &&
		   !::fast_io::operations::decay::defines::
			   has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		   !::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			   outputstmtype> &&
		   !(false || ... ||
			 ::fast_io::details::decay::print_semantic_input_argument_v<
				 char_type, Args>) &&
		   (false || ... ||
			::fast_io::operations::decay::
				print_compiler_constant_partial_fragment_candidate<
					char_type, Args>::value);
}

template <::std::integral char_type, typename T>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr decltype(auto)
	print_compiler_constant_partial_fragment_normalize(T &value)
{
	if constexpr (
		::fast_io::details::decay::
			print_compiler_constant_static_fragment_proxy_traits<
				::std::remove_cvref_t<T>>::value)
	{
		return (value);
	}
	else
	{
		return ::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(value));
	}
}

template <bool line, typename outputstmtype, typename source_tuple,
		  ::std::size_t... index>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 13) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_partial_fragment_original(
		outputstmtype &optstm, source_tuple &sources,
		::std::index_sequence<index...>)
{
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, ::fast_io::containers::get<index>(sources)...);
}

/// @brief Recursively substitutes only optimizer-proven leaves with immutable-fragment proxy owners.
/// @details Every false query simply forwards the original named lvalue to the next inline instantiation.  If no query
///          succeeds, the terminal false specialization calls the historical source bridge exactly once.  A mixed
///          status customization is checked after the concrete substitution set is known and restores that same
///          original bridge instead of exposing an internal strategy type to user overload resolution.
template <bool line, ::std::integral char_type, ::std::size_t source_index,
		  bool has_fragment, typename outputstmtype, typename source_tuple,
		  typename... Built>
// GCC 12 otherwise outlines the all-false recursion in a runtime raw-integer
// record, retaining a 24-byte frame and a compiler-constant helper call before
// the ordinary integer writer. Mandatory inlining restores the direct zero-frame
// jeaiii path; an aligned ABBA microbenchmark is neutral (+0.08%), while the
// complete facade TU loses the residual helper. GCC 11 likewise drops its last
// unused partial-transform body. GCC 13 and later already required this edge.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_partial_fragment_transform(
		outputstmtype &optstm, source_tuple &sources, Built &...built)
{
	constexpr ::std::size_t source_count{
		::std::tuple_size<::std::remove_cvref_t<source_tuple>>::value};
	if constexpr (source_index == source_count)
	{
		if constexpr (has_fragment)
		{
			[&]<typename... Normalized>(Normalized &&...normalized) constexpr {
				if constexpr (
					::fast_io::operations::decay::defines::
						has_status_print_define<
							line, outputstmtype,
							::std::remove_cvref_t<Normalized>...>)
				{
					::fast_io::operations::decay::
						print_compiler_constant_partial_fragment_original<line>(
							optstm, sources,
							::std::make_index_sequence<source_count>{});
				}
				else
				{
					::fast_io::operations::decay::
						print_freestanding_decay_borrowed_output_and_arguments<line>(
							optstm,
							::std::forward<Normalized>(normalized)...);
				}
			}(
				::fast_io::operations::decay::
					print_compiler_constant_partial_fragment_normalize<
						char_type>(built)...);
		}
		else
		{
			::fast_io::operations::decay::
				print_compiler_constant_partial_fragment_original<line>(
					optstm, sources,
					::std::make_index_sequence<source_count>{});
		}
	}
	else
	{
		auto &current{
			::fast_io::containers::get<source_index>(sources)};
		using current_type = ::std::remove_cvref_t<decltype(current)>;
		if constexpr (
			::fast_io::operations::decay::
				print_compiler_constant_partial_fragment_candidate<
					char_type, current_type>::value)
		{
			if (::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_eligible_one<
						char_type>(current))
			{
				auto materialized{
					::fast_io::operations::decay::
						print_compiler_constant_pre_normalization_materialize_one<
							char_type>(current)};
				using proxy_type = ::std::remove_cvref_t<decltype(materialized)>;
				::fast_io::details::decay::
					print_compiler_constant_static_fragment_proxy<
						char_type, proxy_type>
						fragment{
							::std::move(materialized)};
				::fast_io::operations::decay::
					print_compiler_constant_partial_fragment_transform<
						line, char_type, source_index + 1u, true>(
						optstm, sources, built..., fragment);
				return;
			}
		}
		::fast_io::operations::decay::
			print_compiler_constant_partial_fragment_transform<
				line, char_type, source_index + 1u, has_fragment>(
				optstm, sources, built..., current);
	}
}

template <bool preserve_static_fragments, ::std::integral char_type, typename T>
// The proved source must remain in the same optimizer frame as its proxy.
// GCC 11/12 otherwise outline this edge after a successful builtin query and
// reconstruct both the compact proxy and native precision fallback in the
// literal caller.  Unknown sources cannot reach this true-only continuation.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr auto
	print_compiler_constant_pre_normalization_plain_true_forward(T &&value)
{
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, T>)
	{
		// The replacement is a new owned prvalue. Normalize that value category directly; presenting it as this helper's
		// named lvalue would wrap a larger fields proxy in `parameter` and recreate the ABI boundary the early gate avoids.
		auto materialized{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_materialize_one<char_type>(
					::std::forward<T>(value))};
		using proxy_type = ::std::remove_cvref_t<decltype(materialized)>;
		if constexpr (
			preserve_static_fragments &&
			::fast_io::compiler_constant_static_fragment_printable<
				char_type, proxy_type>)
		{
			// A full-run true arm may later fall back to the mature mixed dispatcher
			// (for example, printf beside a run-time precision float). Preserve the
			// same descriptor-only representation used by per-leaf substitution
			// instead of exposing the proxy's large reserve spelling to that fallback.
			return ::fast_io::details::decay::
				print_compiler_constant_static_fragment_proxy<
					char_type, proxy_type>{::std::move(materialized)};
		}
		else
		{
			return ::fast_io::io_print_forward<char_type>(
				::fast_io::io_print_alias(::std::move(materialized)));
		}
	}
	else
	{
		// The historical non-semantic bridge aliases its named parameter as an lvalue, regardless of the public source
		// category. Preserve that exact expression for every argument the strategy does not replace.
		return ::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(value));
	}
}

template <bool preserve_static_fragments, ::std::integral char_type, typename T>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr auto
	print_compiler_constant_pre_normalization_semantic_true_forward(T &&value)
{
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, T>)
	{
		auto materialized{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_materialize_one<char_type>(
					::std::forward<T>(value))};
		using proxy_type = ::std::remove_cvref_t<decltype(materialized)>;
		if constexpr (
			preserve_static_fragments &&
			::fast_io::compiler_constant_static_fragment_printable<
				char_type, proxy_type>)
		{
			return ::fast_io::details::decay::
				print_compiler_constant_static_fragment_proxy<
					char_type, proxy_type>{::std::move(materialized)};
		}
		else
		{
			return ::fast_io::details::decay::
				print_semantic_input_forward<char_type>(
					::std::move(materialized));
		}
	}
	else
	{
		// A semantic run is the one historical source path which retains every original value category through aliasing.
		return ::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::forward<T>(value));
	}
}

/// @brief Classifies one source leaf for the unbuffered immutable-fragment true arm.
/// @details A compiler-constant candidate contributes the static-fragment protocol of its replacement proxy. Character
///          arrays, `static_scatter_t`, and syntax-owned provider nodes borrow storage already owned outside this
///          strategy frame; the latter therefore retains its existing rodata slice beside a proved constant scalar.
///          Ordinary borrowed scatters deliberately remain in the historical dispatcher so its measured copy/write
///          threshold still governs short run-time `%s` pointers and dynamic fields. This classification is consumed
///          only after the outer value gate succeeds, so admitting a syntax provider cannot move an unknown scalar
///          into constant materialization. Ordinary function parameters still cannot become a new structural NTTP
///          object, and `mnp::static_arg` remains the explicit facility for a merged compile-time object.
template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_fragment_source
{
	using source_type = ::std::remove_cvref_t<T>;
	inline static constexpr bool candidate{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<
				char_type, source_type>};
	inline static constexpr bool nested_candidate{};
	inline static constexpr bool static_scatter{
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, source_type>::available};
	inline static constexpr bool direct_scatter{
		::std::same_as<source_type,
					   ::fast_io::basic_io_scatter_t<char_type>>};
	inline static constexpr bool stable_scatter{[]() consteval {
		if constexpr (
			!candidate &&
			::fast_io::scatter_printable_for<char_type, source_type &> &&
			::fast_io::borrowed_scatter_source<char_type, source_type> &&
			::fast_io::scatter_output_state_independent<char_type, source_type> &&
			::fast_io::scatter_direct_print_equivalent<char_type, source_type> &&
			::fast_io::copy_stable_borrowed_print_source<char_type, source_type>)
		{
#if defined(__HERBCEPTIONS__)
			return requires(source_type &value) {
				// The fragment plan retains the descriptor returned for this named source lvalue. Its producer must
				// satisfy the deterministic and traditional no-failure contracts independently.
				requires(!throws((print_scatter_define(
					::fast_io::io_reserve_type<char_type, source_type>, value))));
				{
					print_scatter_define(
						::fast_io::io_reserve_type<char_type, source_type>, value)
				} noexcept -> ::std::same_as<::fast_io::basic_io_scatter_t<char_type>>;
			};
#else
			return noexcept(print_scatter_define(
				::fast_io::io_reserve_type<char_type, source_type>,
				::std::declval<source_type &>()));
#endif
		}
		else
		{
			return false;
		}
	}()};
	inline static constexpr bool character_array{[]() consteval {
		using unreferenced_type = ::std::remove_reference_t<T>;
		if constexpr (::std::is_bounded_array_v<unreferenced_type>)
		{
			using element_type = ::std::remove_extent_t<unreferenced_type>;
			return ::std::is_const_v<element_type> &&
				   !::std::is_volatile_v<element_type> &&
				   ::std::same_as<::std::remove_cv_t<element_type>, char_type>;
		}
		else
		{
			return false;
		}
	}()};
	// Reuse the lazy replacement trait from the source gate. A conditional_t
	// would still form compiler_constant_materialized_t for a non-candidate on
	// some frontends, making an ordinary character array ill-formed.
	using replacement_type = ::std::remove_cvref_t<
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_replacement_t<
				char_type, source_type>>;
	inline static constexpr bool available{
		(candidate &&
		 ::fast_io::compiler_constant_static_fragment_printable<
			 char_type, replacement_type>) ||
		static_scatter || character_array};
	inline static constexpr bool compact_available{
		(candidate &&
		 ::fast_io::compiler_constant_static_fragment_printable<
			 char_type, replacement_type> &&
		 ::fast_io::precise_reserve_printable<char_type, replacement_type>) ||
		static_scatter || character_array};
	/// Exact payload extent known from the source type alone.
	///
	/// A function parameter carrying a character array still retains its bound, so
	/// adjacent arrays and fixed scatters can use the stream's measured
	/// writev-to-write policy without a run-time size gate. Compiler-constant
	/// candidates deliberately report `SIZE_MAX`: their spelling length is a
	/// value property and remains owned by the separately bounded 64-byte path.
	inline static constexpr ::std::size_t compact_static_size{[]() consteval {
		if constexpr (candidate)
		{
			return SIZE_MAX;
		}
		else if constexpr (static_scatter)
		{
			return ::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::size;
		}
		else if constexpr (character_array)
		{
			constexpr ::std::size_t extent{
				::std::extent_v<::std::remove_reference_t<T>>};
			return extent == 0u ? SIZE_MAX
								: static_cast<::std::size_t>(extent - 1u);
		}
		else
		{
			return SIZE_MAX;
		}
	}()};
	inline static constexpr ::std::size_t count{[]() consteval {
		if constexpr (
			candidate &&
			::fast_io::compiler_constant_static_fragment_printable<
				char_type, replacement_type>)
		{
			return print_compiler_constant_static_fragments_size(
				::fast_io::io_reserve_type<char_type, replacement_type>);
		}
		else if constexpr (static_scatter || stable_scatter)
		{
			return 1u;
		}
		else if constexpr (character_array)
		{
			constexpr ::std::size_t extent{
				::std::extent_v<::std::remove_reference_t<T>>};
			return static_cast<::std::size_t>(extent != 0u && extent != 1u);
		}
		else
		{
			return 0u;
		}
	}()};
};

template <::std::integral char_type, typename... Args>
struct print_compiler_constant_pre_normalization_fragment_source<
	char_type, ::fast_io::manipulators::pack_t<Args...>>
{
	inline static constexpr bool candidate{
		(false || ... ||
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args>::candidate)};
	// The public eligibility gate is intentionally phrased on source arguments.
	// Until packs expose an equivalent recursive source/status proof, a candidate
	// nested inside one must retain the historical semantic dispatcher instead of
	// being materialized by an unguarded fragment traversal.
	inline static constexpr bool nested_candidate{candidate};
	inline static constexpr bool available{
		(::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args>::available &&
		 ...)};
	inline static constexpr bool compact_available{
		(::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args>::compact_available &&
		 ...)};
	inline static constexpr ::std::size_t compact_static_size{[]() consteval {
		if constexpr (!((::fast_io::operations::decay::
							 print_compiler_constant_pre_normalization_fragment_source<
								 char_type, Args>::compact_static_size != SIZE_MAX) &&
						...))
		{
			return SIZE_MAX;
		}
		else
		{
			::std::size_t total{};
			((total = ::fast_io::details::decay::
				  print_contiguous_char_extent_add_or_unavailable<char_type>(
					  total,
					  ::fast_io::operations::decay::
						  print_compiler_constant_pre_normalization_fragment_source<
							  char_type, Args>::compact_static_size)),
			 ...);
			return total;
		}
	}()};
	inline static constexpr ::std::size_t count{[]() consteval {
		if constexpr (!available)
		{
			return SIZE_MAX;
		}
		else
		{
			::std::size_t total{};
			((total = total == SIZE_MAX ||
							  ::fast_io::operations::decay::
									  print_compiler_constant_pre_normalization_fragment_source<
										  char_type, Args>::count > SIZE_MAX - total
						  ? SIZE_MAX
						  : total +
								::fast_io::operations::decay::
									print_compiler_constant_pre_normalization_fragment_source<
										char_type, Args>::count),
			 ...);
			return total;
		}
	}()};
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_fragment_source<char_type, T &>
	: print_compiler_constant_pre_normalization_fragment_source<
		  char_type, T>
{};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_fragment_source<char_type, T &&>
	: print_compiler_constant_pre_normalization_fragment_source<
		  char_type, T>
{};

template <::std::integral char_type, typename T>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_pre_normalization_fragment_emit_one(
	::fast_io::basic_io_scatter_t<char_type> *iter, T &&value);

template <::std::integral char_type, typename T, ::std::size_t... index>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_pre_normalization_fragment_emit_pack(
	::fast_io::basic_io_scatter_t<char_type> *iter, T &&value,
	::std::index_sequence<index...>)
{
	auto &&pack_ref{::fast_io::details::decay::print_semantic_node_ref(
		::std::forward<T>(value))};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_fragment_emit_one<char_type>(
			  iter, ::fast_io::containers::get<index>(pack_ref.storage))),
	 ...);
	return iter;
}

template <::std::integral char_type, typename T>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_pre_normalization_fragment_emit_one(
	::fast_io::basic_io_scatter_t<char_type> *iter, T &&value)
{
	using source_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::details::print_pack<source_type>)
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_emit_pack<char_type>(
				iter, ::std::forward<T>(value),
				::std::make_index_sequence<source_type::size>{});
	}
	else if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<
				char_type, source_type>)
	{
		auto materialized{print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, source_type>, value)};
		using replacement_type = ::std::remove_cvref_t<decltype(materialized)>;
		static_assert(
			::fast_io::compiler_constant_static_fragment_printable<
				char_type, replacement_type>);
		return print_compiler_constant_static_fragments_define(
			::fast_io::io_reserve_type<char_type, replacement_type>, iter,
			materialized);
	}
	else if constexpr (::std::is_bounded_array_v<::std::remove_reference_t<T>>)
	{
		constexpr ::std::size_t extent{
			::std::extent_v<::std::remove_reference_t<T>>};
		if constexpr (extent > 1u)
		{
			*iter++ = {__builtin_addressof(value[0u]), extent - 1u};
		}
		return iter;
	}
	else if constexpr (
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, source_type>::available)
	{
		constexpr ::std::size_t extent{
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::size};
		if constexpr (extent != 0u)
		{
			*iter++ = ::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::define(value);
		}
		return iter;
	}
	else if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_source<
				char_type, T>::stable_scatter)
	{
		auto const scatter{print_scatter_define(
			::fast_io::io_reserve_type<char_type, source_type>, value)};
		if (scatter.len != 0u)
		{
			*iter++ = scatter;
		}
		return iter;
	}
	else
	{
		static_assert(sizeof(source_type) == 0u,
					  "unclassified compiler-constant static fragment source");
	}
}

/// Classifies sources which can be prepared exactly before allocating a worst-case fragment array.
///
/// Only explicitly marked compiler-constant proxies enter this path.  Every passive sibling must have a type-known
/// immutable spelling (a character array or `static_scatter_t`); arbitrary borrowed scatters retain the established
/// dispatcher because measuring/copying them here would add work to a value-independent source.
template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_precise_fragment_source
{
	using source_type = ::std::remove_cvref_t<T>;
	using fragment_source =
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_source<
				char_type, T>;
	using replacement_type = typename fragment_source::replacement_type;
	inline static constexpr bool prepared_candidate{
		fragment_source::candidate &&
		::fast_io::compiler_constant_precise_compact_preferred<
			char_type, replacement_type>};
	inline static constexpr bool available{
		prepared_candidate ||
		(!fragment_source::candidate &&
		 (fragment_source::static_scatter || fragment_source::character_array))};
	inline static constexpr bool contains_candidate{prepared_candidate};
};

template <::std::integral char_type, typename... Args>
struct print_compiler_constant_pre_normalization_precise_fragment_source<
	char_type, ::fast_io::manipulators::pack_t<Args...>>
{
	inline static constexpr bool available{
		(::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_precise_fragment_source<
				 char_type, Args>::available &&
		 ...)};
	inline static constexpr bool contains_candidate{
		(false || ... ||
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_precise_fragment_source<
				 char_type, Args>::contains_candidate)};
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_precise_fragment_source<
	char_type, T &>
	: print_compiler_constant_pre_normalization_precise_fragment_source<
		  char_type, T>
{};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_precise_fragment_source<
	char_type, T &&>
	: print_compiler_constant_pre_normalization_precise_fragment_source<
		  char_type, T>
{};

/// @brief Proves that this compiler can erase an exact put-area replacement before the output call returns.
/// @details GCC 11--14 retain a value-dependent decimal digit loop for a scalar floating replacement even though its
///          builtin query succeeds (83/83/83/60 instructions for the `3.125` root). Expanding the provider's 32 slots
///          is a measured reversal: it grows the same roots to 642/730/662/339 instructions and 1,080/1,080/1,080/528
///          bytes of stack. Those source categories therefore fail closed before their replacement type or value query
///          is formed. GCC 15 and later reduce the precise writer to immediate stores; other source categories retain
///          their independent deletion proofs.
template <::std::integral char_type, typename... Args>
inline consteval bool
print_compiler_constant_exact_obuffer_codegen_supported() noexcept
{
#if defined(__GNUC__) && !defined(__clang__) && 11 <= __GNUC__ && __GNUC__ <= 14
	return !(false || ... ||
			 ::fast_io::compiler_constant_expanded_fragment_preferred_source<
				 char_type, Args>);
#else
	return true;
#endif
}

/// @brief Admits the exact compiler-constant path for a put-area destination.
/// @details This is deliberately independent of the replacement's ordinary reserve bound.  A scalar floating proxy
///          retains the mature 5006-character all-values reserve protocol, but its compiler-constant spelling is often
///          only one to a few characters.  The exact path therefore proves only the compact proxy budget here and uses
///          `print_reserve_precise_size/define` after materialization.  A complete mutex wrapper is classified through
///          its unlocked output so the caller can keep the value query outside the lock while forming the proxy only
///          after the guard has been acquired.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_exact_obuffer_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		!::fast_io::operations::decay::
			print_compiler_constant_exact_obuffer_codegen_supported<
				char_type, Args...>())
	{
		// This consumer-specific rejection must precede the common proof because that proof forms replacement types.
		return false;
	}
	else if constexpr (
		!::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_common_available<
				false, line, true, outputstmtype, Args...>())
	{
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_available<
				line, unlocked_output, Args...>();
	}
	else
	{
		return ::fast_io::operations::decay::defines::
				   has_obuffer_basic_operations<outputstmtype> &&
			   (::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_precise_fragment_source<
						char_type, Args>::available &&
				...) &&
			   (false || ... ||
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_precise_fragment_source<
						char_type, Args>::contains_candidate);
	}
}

template <::std::integral char_type, typename Arg>
// The exact contiguous path needs a mandatory-inline chain on GCC 11--16 and
// Clang 21--23. Removing it adds calls on GCC 16 and 36 KiB of text on Clang
// 23; Clang 17--20 instead benefits from ordinary inlining.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_exact_obuffer_measure_one(
		::std::size_t *component_sizes, ::std::size_t &total,
		::std::size_t &component_index, Arg &arg)
{
	using arg_type = ::std::remove_cvref_t<Arg>;
	::std::size_t const component_size{print_reserve_precise_size(
		::fast_io::io_reserve_type<char_type, arg_type>, arg)};
	component_sizes[component_index++] = component_size;
	if (total == SIZE_MAX || component_size > SIZE_MAX - total)
	{
		total = SIZE_MAX;
	}
	else
	{
		total += component_size;
	}
}

template <::std::integral char_type, typename Arg>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_exact_obuffer_emit_one(
		::std::size_t const *component_sizes, ::std::size_t &component_index,
		char_type *&current, Arg &arg)
{
	using arg_type = ::std::remove_cvref_t<Arg>;
	::std::size_t const component_size{component_sizes[component_index++]};
	char_type *const expected_end{current + component_size};
	using define_result = decltype(print_reserve_precise_define(
		::fast_io::io_reserve_type<char_type, arg_type>, current,
		component_size, arg));
	if constexpr (::std::same_as<define_result, char_type *>)
	{
		char_type *const actual_end{print_reserve_precise_define(
			::fast_io::io_reserve_type<char_type, arg_type>, current,
			component_size, arg)};
		if (actual_end != expected_end) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
	}
	else
	{
		print_reserve_precise_define(
			::fast_io::io_reserve_type<char_type, arg_type>, current,
			component_size, arg);
	}
	current = expected_end;
}

/// @brief Materializes one measured compiler-constant record before a put-area capacity miss is published.
/// @details The source gate has already proved every candidate value and the caller has already measured the complete
///          record.  Keeping the record in one bounded tier means `write_all_decay` observes a single range: a short
///          buffered sink can flush its prior contents and overflow the complete record instead of first overflowing a
///          static prefix and then retaining the floating suffix in its put area.  Unknown values never instantiate
///          this execution arm because they fail the source-level compiler-constant gate.
template <::std::size_t capacity, bool line, ::std::integral char_type,
		  typename outputstmtype, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_exact_obuffer_overflow_emit(
		outputstmtype &optstm, ::std::size_t const *component_sizes,
		Args &...args)
{
	static_assert(capacity != 0u);
	::fast_io::freestanding::array<char_type, capacity> buffer;
	char_type *current{buffer.data()};
#if defined(__GNUC__) && !defined(__clang__)
	FAST_IO_IF_NOT_CONSTEVAL
	{
		__asm__ __volatile__("" : "+r"(current));
	}
#endif
	::std::size_t component_index{};
	(::fast_io::operations::decay::
		 print_compiler_constant_exact_obuffer_emit_one<char_type>(
			 component_sizes, component_index, current, args),
	 ...);
	if constexpr (line)
	{
		*current++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	::fast_io::operations::decay::write_all_decay_dispatch(
		optstm, buffer.data(), current);
}

/// @brief Emits one exact compiler-constant record without publishing a partial put-area prefix.
/// @details Every exact size is evaluated once and cached before choosing the destination.  A destination which
///          explicitly permits nothrow compiler-constant put-area materialization receives a fitting record in place
///          and publishes its cursor once.  A capacity miss, or an unmarked put area, instead receives the same complete
///          record through one bounded local range and the ordinary `write_all` protocol.  The function returns false
///          only when the run is structurally ineligible, its size overflows, or it exceeds the bounded materialization
///          budget; no destination byte has been touched in those cases.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr bool
	print_compiler_constant_exact_obuffer_flat_try(
		outputstmtype &optstm, Args &...args)
{
	if constexpr (
		sizeof...(Args) == 0u || sizeof...(Args) > 16u ||
		!(::fast_io::precise_reserve_printable<
			  char_type, ::std::remove_cvref_t<Args>> &&
		  ...))
	{
		return false;
	}
	else
	{
		::std::size_t component_sizes[sizeof...(Args)]{};
		::std::size_t total{static_cast<::std::size_t>(line)};
		::std::size_t component_index{};
		(::fast_io::operations::decay::
			 print_compiler_constant_exact_obuffer_measure_one<char_type>(
				 component_sizes, total, component_index, args),
		 ...);
		if (total == SIZE_MAX)
		{
			return false;
		}
		if (total == 0u)
		{
			return true;
		}
		if constexpr (
			::fast_io::compiler_constant_obuffer_materialization_safe<
				char_type, outputstmtype> &&
			::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
				outputstmtype, char_type>)
		{
			char_type *const initial{obuffer_curr(optstm)};
			char_type *const put_end{obuffer_end(optstm)};
			if (initial != nullptr && put_end != nullptr)
			{
				::std::ptrdiff_t const difference{put_end - initial};
				if (difference >= 0 &&
					total <= static_cast<::std::size_t>(difference))
				{
					component_index = 0u;
					char_type *current{initial};
					(::fast_io::operations::decay::
						 print_compiler_constant_exact_obuffer_emit_one<char_type>(
							 component_sizes, component_index, current, args),
					 ...);
					if constexpr (line)
					{
						*current++ =
							::fast_io::char_literal_v<u8'\n', char_type>;
					}
					obuffer_set_curr(optstm, current);
					return true;
				}
			}
		}
		constexpr ::std::size_t maximum_chars{
			::fast_io::details::compiler_constant_materialization_max_bytes /
			sizeof(char_type)};
		if (maximum_chars < total)
		{
			return false;
		}
		constexpr ::std::size_t capacity8{
			8u <= sizeof(char_type) ? 1u : 8u / sizeof(char_type)};
		constexpr ::std::size_t capacity16{
			16u <= sizeof(char_type) ? 1u : 16u / sizeof(char_type)};
		constexpr ::std::size_t capacity32{
			32u <= sizeof(char_type) ? 1u : 32u / sizeof(char_type)};
		constexpr ::std::size_t capacity64{
			64u <= sizeof(char_type) ? 1u : 64u / sizeof(char_type)};
		constexpr ::std::size_t capacity128{
			128u <= sizeof(char_type) ? 1u : 128u / sizeof(char_type)};
		if (total <= capacity8)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					capacity8, line, char_type>(
					optstm, component_sizes, args...);
		}
		else if (total <= capacity16)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					capacity16, line, char_type>(
					optstm, component_sizes, args...);
		}
		else if (total <= capacity32)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					capacity32, line, char_type>(
					optstm, component_sizes, args...);
		}
		else if (total <= capacity64)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					capacity64, line, char_type>(
					optstm, component_sizes, args...);
		}
		else if (total <= capacity128)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					capacity128, line, char_type>(
					optstm, component_sizes, args...);
		}
		else
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_overflow_emit<
					maximum_chars, line, char_type>(
					optstm, component_sizes, args...);
		}
		return true;
	}
}

template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_compiler_constant_exact_obuffer_flat_continuation
{
	outputstmtype &optstm;

	template <typename... Args>
// This forwarding boundary is part of the measured exact chain: removing it
// grows GCC 16 text by 70.8 KiB and Clang 23 text by 39.7 KiB. The complete
// GCC 11--16 / Clang 17--23 matrix establishes the same 11/21 boundaries as
// the downstream exact helpers.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr bool operator()(Args &&...args) const
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_exact_obuffer_flat_try<
				line, char_type>(optstm, args...);
	}
};

/// @brief Materializes a proven source run and emits its exact put-area or staged spelling.
/// @details A representable record completes here even when the current put area is too short: the flat continuation
///          stages the whole value and gives it to `write_all`.  If structural or bounded-size admission fails, this
///          function returns false without output; its caller still owns every original source expression and can enter
///          the byte-for-byte historical continuation without exposing the proxy's conservative reserve capacity.
template <bool line, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_available<
				line, outputstmtype, Args && ...>())
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr bool
	print_compiler_constant_pre_normalization_exact_obuffer_try(
		outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool semantic_run{(false || ... ||
								 ::fast_io::details::decay::print_semantic_input_argument_v<
									 char_type,
									 ::fast_io::operations::decay::
										 print_compiler_constant_pre_normalization_replacement_t<
											 char_type, Args &&>>)};
	if constexpr (semantic_run)
	{
		return ::fast_io::details::decay::
			print_semantic_pack_expand<true, char_type>(
				::fast_io::operations::decay::
					print_compiler_constant_exact_obuffer_flat_continuation<
						line, char_type, outputstmtype>{optstm},
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_semantic_true_forward<
						false, char_type>(::std::forward<Args>(args))...);
	}
	else
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_exact_obuffer_flat_continuation<
				line, char_type, outputstmtype>{optstm}(
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_plain_true_forward<
						false, char_type>(::std::forward<Args>(args))...);
	}
}

/// @brief Acquires a complete stream mutex before constructing any exact compiler-constant proxy.
template <bool line, typename outputstmtype, typename... Args>
	requires(
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_available<
				line, outputstmtype, Args && ...>())
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr bool
	print_compiler_constant_pre_normalization_exact_obuffer_try_after_lock(
		outputstmtype &optstm, Args &&...args)
{
	if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
			::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
		decltype(auto) unlocked =
			::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_try<line>(
				unlocked, ::std::forward<Args>(args)...);
	}
	else
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_try<line>(
				optstm, ::std::forward<Args>(args)...);
	}
}

/// @brief Emits one already-measured exact record into a bounded automatic buffer and performs one direct write.
/// @details `capacity` is one selected geometric tier, not the replacement types' conservative all-values reserve sum.
///          The caller proves that the measured record fits before entering this leaf. The GCC barrier only postpones
///          an early object-size diagnostic across the tiered branch and emits no instruction.
template <::std::size_t capacity, bool line, ::std::integral char_type,
		  typename outputstmtype, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_exact_direct_flat_emit(
		outputstmtype &optstm, ::std::size_t const *component_sizes,
		Args &...args)
{
	static_assert(capacity != 0u);
	::fast_io::freestanding::array<char_type, capacity> buffer;
	char_type *current{buffer.data()};
#if defined(__GNUC__) && !defined(__clang__)
	FAST_IO_IF_NOT_CONSTEVAL
	{
		__asm__ __volatile__("" : "+r"(current));
	}
#endif
	::std::size_t component_index{};
	(::fast_io::operations::decay::
		 print_compiler_constant_exact_obuffer_emit_one<char_type>(
			 component_sizes, component_index, current, args),
	 ...);
	if constexpr (line)
	{
		*current++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	::fast_io::details::decay::print_write_all_direct(
		optstm, buffer.data(), current);
}

/// @brief Measures a flattened replacement run once and selects its smallest practical direct-write scratch tier.
/// @return true when the record was emitted; false when it exceeds the deliberately bounded 256-byte constant path.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr bool
	print_compiler_constant_exact_direct_flat_try(
		outputstmtype &optstm, Args &...args)
{
	if constexpr (
		sizeof...(Args) == 0u || sizeof...(Args) > 16u ||
		!(::fast_io::precise_reserve_printable<
			  char_type, ::std::remove_cvref_t<Args>> &&
		  ...))
	{
		return false;
	}
	else
	{
		::std::size_t component_sizes[sizeof...(Args)]{};
		::std::size_t total{static_cast<::std::size_t>(line)};
		::std::size_t component_index{};
		(::fast_io::operations::decay::
			 print_compiler_constant_exact_obuffer_measure_one<char_type>(
				 component_sizes, total, component_index, args),
		 ...);
		constexpr ::std::size_t maximum_chars{
			::fast_io::details::compiler_constant_materialization_max_bytes /
			sizeof(char_type)};
		if (total == 0u)
		{
			return true;
		}
		if (total == SIZE_MAX || maximum_chars < total)
		{
			return false;
		}
		constexpr ::std::size_t capacity8{
			8u <= sizeof(char_type) ? 1u : 8u / sizeof(char_type)};
		constexpr ::std::size_t capacity16{
			16u <= sizeof(char_type) ? 1u : 16u / sizeof(char_type)};
		constexpr ::std::size_t capacity32{
			32u <= sizeof(char_type) ? 1u : 32u / sizeof(char_type)};
		constexpr ::std::size_t capacity64{
			64u <= sizeof(char_type) ? 1u : 64u / sizeof(char_type)};
		constexpr ::std::size_t capacity128{
			128u <= sizeof(char_type) ? 1u : 128u / sizeof(char_type)};
		if (total <= capacity8)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					capacity8, line, char_type>(optstm, component_sizes, args...);
		}
		else if (total <= capacity16)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					capacity16, line, char_type>(optstm, component_sizes, args...);
		}
		else if (total <= capacity32)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					capacity32, line, char_type>(optstm, component_sizes, args...);
		}
		else if (total <= capacity64)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					capacity64, line, char_type>(optstm, component_sizes, args...);
		}
		else if (total <= capacity128)
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					capacity128, line, char_type>(optstm, component_sizes, args...);
		}
		else
		{
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_flat_emit<
					maximum_chars, line, char_type>(
					optstm, component_sizes, args...);
		}
		return true;
	}
}

#if defined(__clang__)
/// @brief Computes the conservative record capacity for Clang's post-gate direct-write leaf.
/// @details Clang 23 does not propagate the exact precision-float proxy size far enough to fold the geometric scratch
///          selection used by GCC. The proxy still has a bounded reserve contract, so a record that exceeds the common
///          256-byte materialization budget by at most one 64-byte literal allowance can be emitted unconditionally
///          from that conservative capacity. This helper is type-only and is never instantiated for the unknown-value
///          run-time arm.
template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
inline consteval ::std::size_t
print_compiler_constant_clang_direct_capacity() noexcept
{
	if constexpr (
		!::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype> ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::
			has_obuffer_basic_operations<outputstmtype> ||
		!(::fast_io::reserve_printable<
			  char_type, ::std::remove_cvref_t<Args>> &&
		  ...) ||
		!(false || ... ||
		  ::fast_io::compiler_constant_precise_compact_preferred<
			  char_type, ::std::remove_cvref_t<Args>>))
	{
		return SIZE_MAX;
	}
	else
	{
		constexpr ::std::size_t capacity{[]() consteval {
			::std::size_t total{static_cast<::std::size_t>(line)};
			((total = ::fast_io::details::decay::
				  print_contiguous_char_extent_add_or_unavailable<char_type>(
					  total,
					  print_reserve_size(::fast_io::io_reserve_type<
										 char_type, ::std::remove_cvref_t<Args>>))),
			 ...);
			return total;
		}()};
		constexpr ::std::size_t maximum_chars{
			(::fast_io::details::compiler_constant_materialization_max_bytes +
			 64u) /
			sizeof(char_type)};
		return capacity != SIZE_MAX && capacity <= maximum_chars &&
					   ::fast_io::details::decay::
						   print_stack_buffer_size_within_limit<capacity, char_type>
				   ? capacity
				   : SIZE_MAX;
	}
}

/// @brief Emits one Clang post-gate record from its conservative type-level capacity with one direct write.
template <::std::size_t capacity, bool line, ::std::integral char_type,
		  typename outputstmtype, typename... Args>
inline constexpr void print_compiler_constant_clang_direct_emit(
	outputstmtype &optstm, Args &...args)
{
	static_assert(capacity != 0u && capacity != SIZE_MAX);
	::fast_io::freestanding::array<char_type, capacity> buffer;
	char_type *const end{
		::fast_io::operations::decay::
			print_compiler_constant_materialization_define_chain<line, char_type>(
				buffer.data(), args...)};
	::fast_io::details::decay::print_write_all_direct(
		optstm, buffer.data(), end);
}
#endif

template <::std::integral char_type, typename proxy_type>
struct print_compiler_constant_prepared_precise_fragment
{
	// These aliases form an exact opt-in category boundary. A future prepared
	// record cannot enter the precision-only path merely by exposing similarly
	// named value/size data members.
	using compiler_constant_prepared_precise_tag = void;
	using compiler_constant_prepared_precise_proxy_type = proxy_type;
	proxy_type const *value;
	::std::size_t exact_size;
};

template <::std::integral char_type, ::std::size_t extent>
struct print_compiler_constant_prepared_static_fragment
{
	::fast_io::basic_io_scatter_t<char_type> scatter;
};

template <::std::integral char_type, typename T, typename continuation,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_one(
	T &value, continuation &cont, Prepared &...prepared);

template <::std::size_t index, ::std::integral char_type, typename pack_type,
		  typename continuation, typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_pack(
	pack_type &value, continuation &cont, Prepared &...prepared);

template <::std::size_t index, ::std::integral char_type, typename pack_type,
		  typename continuation>
struct print_compiler_constant_prepare_precise_fragment_pack_continuation
{
	pack_type &value;
	continuation &cont;

	template <typename... Extended>
	inline constexpr void
	operator()(Extended &...extended) const
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_pack<
				index + 1u, char_type>(value, cont, extended...);
	}
};

template <::std::size_t index, ::std::integral char_type, typename pack_type,
		  typename continuation, typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_pack(
	pack_type &value, continuation &cont, Prepared &...prepared)
{
	if constexpr (index == ::std::remove_cvref_t<pack_type>::size)
	{
		cont(prepared...);
	}
	else
	{
		auto &&pack_ref{::fast_io::details::decay::print_semantic_node_ref(value)};
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_pack_continuation<
				index, char_type, pack_type, continuation>
				next{value, cont};
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_one<char_type>(
				::fast_io::containers::get<index>(pack_ref.storage), next,
				prepared...);
	}
}

/// Prepares one leaf and keeps every proxy in its owning recursion frame until the terminal continuation returns.
///
/// This shape intentionally avoids a heterogeneous value tuple: a materializer may return a move-only proxy, and both
/// its exact-size query and its fragment producer must observe that same object exactly once on their selected path.
template <::std::integral char_type, typename T, typename continuation,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_one(
	T &value, continuation &cont, Prepared &...prepared)
{
	using source_type = ::std::remove_cvref_t<T>;
	using source = ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_precise_fragment_source<
			char_type, T>;
	static_assert(source::available);
	if constexpr (::fast_io::details::print_pack<source_type>)
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_pack<0u, char_type>(
				value, cont, prepared...);
	}
	else if constexpr (source::prepared_candidate)
	{
		auto materialized{print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, source_type>, value)};
		using proxy_type = ::std::remove_cvref_t<decltype(materialized)>;
		static_assert(
			::fast_io::compiler_constant_precise_compact_preferred<
				char_type, proxy_type>);
		auto const exact_size{print_reserve_precise_size(
			::fast_io::io_reserve_type<char_type, proxy_type>, materialized)};
		::fast_io::operations::decay::
			print_compiler_constant_prepared_precise_fragment<
				char_type, proxy_type>
				current{
					__builtin_addressof(materialized), exact_size};
		cont(prepared..., current);
	}
	else
	{
		using fragment_source = ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_source<
				char_type, T>;
		::fast_io::basic_io_scatter_t<char_type> scatter{};
		if constexpr (fragment_source::character_array)
		{
			constexpr ::std::size_t extent{
				::std::extent_v<::std::remove_reference_t<T>>};
			if constexpr (extent > 1u)
			{
				scatter = {__builtin_addressof(value[0u]), extent - 1u};
			}
		}
		else
		{
			static_assert(fragment_source::static_scatter);
			scatter = ::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::define(value);
		}
		::fast_io::operations::decay::
			print_compiler_constant_prepared_static_fragment<
				char_type, fragment_source::compact_static_size>
				current{scatter};
		cont(prepared..., current);
	}
}

template <::std::size_t index, ::std::integral char_type,
		  typename source_tuple, typename continuation,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_sources(
	source_tuple &sources, continuation &cont, Prepared &...prepared);

template <::std::size_t index, ::std::integral char_type,
		  typename source_tuple, typename continuation>
struct print_compiler_constant_prepare_precise_fragment_sources_continuation
{
	source_tuple &sources;
	continuation &cont;

	template <typename... Extended>
	inline constexpr void
	operator()(Extended &...extended) const
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_sources<
				index + 1u, char_type>(sources, cont, extended...);
	}
};

template <::std::size_t index, ::std::integral char_type,
		  typename source_tuple, typename continuation,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepare_precise_fragment_sources(
	source_tuple &sources, continuation &cont, Prepared &...prepared)
{
	constexpr ::std::size_t source_count{
		::std::tuple_size<::std::remove_cvref_t<source_tuple>>::value};
	if constexpr (index == source_count)
	{
		cont(prepared...);
	}
	else
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_sources_continuation<
				index, char_type, source_tuple, continuation>
				next{sources, cont};
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_one<char_type>(
				::fast_io::containers::get<index>(sources), next, prepared...);
	}
}

template <::std::integral char_type, typename proxy_type>
[[nodiscard]] inline constexpr ::std::size_t
print_compiler_constant_prepared_fragment_size(
	::fast_io::operations::decay::
		print_compiler_constant_prepared_precise_fragment<
			char_type, proxy_type> const &value) noexcept
{
	return value.exact_size;
}

template <::std::integral char_type, ::std::size_t extent>
[[nodiscard]] inline constexpr ::std::size_t
print_compiler_constant_prepared_fragment_size(
	::fast_io::operations::decay::
		print_compiler_constant_prepared_static_fragment<
			char_type, extent> const &value) noexcept
{
	if (value.scatter.len != extent) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	return extent;
}

/// Type-level reserve bound retained by a prepared exact fragment.
///
/// This is deliberately the formatter's public reserve contract, not an
/// estimate invented by print.  It lets the terminal dispatcher discard its
/// large fallback during template instantiation when every possible spelling
/// fits the compact record, while a value whose format can genuinely be long
/// continues to use the measured run-time decision.
template <::std::integral char_type, typename proxy_type>
[[nodiscard]] inline consteval ::std::size_t
	print_compiler_constant_prepared_fragment_capacity(
		::std::type_identity<
			::fast_io::operations::decay::
				print_compiler_constant_prepared_precise_fragment<
					char_type, proxy_type>>) noexcept
{
	return print_reserve_size(
		::fast_io::io_reserve_type<char_type, proxy_type>);
}

/// A prepared literal/fixed scatter retains its source extent in its type.
///
/// Erasing this value into basic_io_scatter_t made GCC instantiate a 9 KiB
/// floating-fragment producer for records such as `"i=3.2"`, even though the
/// final linked caller had already folded to one five-byte write.  The extent
/// changes no run-time representation and gives the terminal dispatcher the
/// missing compile-time proof.
template <::std::integral char_type, ::std::size_t extent>
[[nodiscard]] inline consteval ::std::size_t
	print_compiler_constant_prepared_fragment_capacity(
		::std::type_identity<
			::fast_io::operations::decay::
				print_compiler_constant_prepared_static_fragment<
					char_type, extent>>) noexcept
{
	return extent;
}

/// Appends one prepared compiler-constant leaf when its complete spelling is one immutable provider slice.
///
/// The descriptor itself is caller-owned scratch; only its character payload must retain provider storage.  An empty
/// precise leaf contributes no descriptor.  A nonempty leaf without the narrow single-slice CPO rejects the complete
/// record so the caller can retain its measured compact/scatter continuation.
template <::std::integral char_type, typename proxy_type>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_prepared_single_static_fragment_append(
	::fast_io::operations::decay::
		print_compiler_constant_prepared_precise_fragment<
			char_type, proxy_type> const &value,
	::fast_io::basic_io_scatter_t<char_type> *iter,
	bool &available) noexcept
{
	if (value.exact_size == 0u)
	{
		return iter;
	}
	if constexpr (
		::fast_io::compiler_constant_single_static_fragment_printable<
			char_type, proxy_type>)
	{
		auto const fragment{print_compiler_constant_single_static_fragment(
			::fast_io::io_reserve_type<char_type, proxy_type>, *value.value)};
		if (fragment.len != 0u)
		{
			if (fragment.len != value.exact_size) [[unlikely]]
			{
				::fast_io::fast_terminate();
			}
			*iter++ = fragment;
			return iter;
		}
	}
	available = false;
	return iter;
}

/// Appends one already-static format/literal leaf without copying its character payload.
template <::std::integral char_type, ::std::size_t extent>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_prepared_single_static_fragment_append(
	::fast_io::operations::decay::
		print_compiler_constant_prepared_static_fragment<
			char_type, extent> const &value,
	::fast_io::basic_io_scatter_t<char_type> *iter,
	bool &) noexcept
{
	if (value.scatter.len != 0u)
	{
		*iter++ = value.scatter;
	}
	return iter;
}

/// Emits a complete prepared record whose every nonempty leaf is one immutable provider slice.
///
/// Character bytes are never compacted into automatic storage here.  A singleton keeps the direct scalar write path;
/// multiple leaves use the destination's synchronous scatter continuation with only descriptors on the stack.  This
/// is the representable C++20 boundary for an optimizer-proven scalar beside a structural format literal: the scalar
/// parameter can select an existing provider slice, but it cannot become a new NTTP which owns a merged static object.
template <bool line, typename outputstmtype, typename... Prepared>
// The selected static-emission terminal carries the proven Clang policy. Keep
// this availability/descriptor wrapper ordinary inline to avoid inferring a
// force-inline requirement from a different function's A/B result.
[[nodiscard]] inline constexpr bool
print_compiler_constant_prepared_single_static_fragments_try(
	outputstmtype &optstm, Prepared &...prepared)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t maximum_count{
		sizeof...(Prepared) + static_cast<::std::size_t>(line)};
	static_assert(maximum_count != 0u);
	::fast_io::basic_io_scatter_t<char_type> scatters[maximum_count];
	auto iter{scatters};
	bool available{true};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_prepared_single_static_fragment_append(
			  prepared, iter, available)),
	 ...);
	if (!available)
	{
		return false;
	}
	if constexpr (line)
	{
		*iter++ = ::fast_io::details::decay::line_scatter_common<char_type>;
	}
	auto const emitted{static_cast<::std::size_t>(iter - scatters)};
	if (emitted == 0u)
	{
		return true;
	}
	if (emitted == 1u &&
		::fast_io::details::decay::
			print_output_accepts_static_provider_scalar<outputstmtype>)
	{
		auto const [base, len]{scatters[0u]};
		::fast_io::details::decay::print_write_all_direct(
			optstm, base, base + len);
		return true;
	}
	if constexpr (
		::fast_io::details::decay::
			print_uses_byte_scatter_representation<outputstmtype>)
	{
		::fast_io::io_scatter_t byte_scatters[maximum_count];
		for (::std::size_t index{}; index != emitted; ++index)
		{
			byte_scatters[index] = {
				scatters[index].base,
				::fast_io::details::intrinsics::mul_or_overflow_die(
					scatters[index].len, sizeof(char_type))};
		}
		if constexpr (
			::fast_io::details::decay::
				print_static_scatter_hot_first_compiler_policy &&
			::fast_io::details::decay::
				print_static_scatter_hot_first_bytes_available<outputstmtype>)
		{
			::fast_io::details::decay::
				print_static_scatter_write_all_bytes_direct(
					optstm, byte_scatters, emitted);
		}
		else
		{
			::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(
				optstm, byte_scatters, emitted);
		}
	}
	else
	{
		if constexpr (
			::fast_io::details::decay::
				print_static_scatter_hot_first_compiler_policy &&
			::fast_io::details::decay::
				print_static_scatter_hot_first_available<outputstmtype>)
		{
			::fast_io::details::decay::print_static_scatter_write_all_direct(
				optstm, scatters, emitted);
		}
		else
		{
			::fast_io::operations::decay::scatter_write_all_decay_dispatch(
				optstm, scatters, emitted);
		}
	}
	return true;
}

template <::std::integral char_type, typename proxy_type>
inline constexpr char_type *
print_compiler_constant_prepared_fragment_compact_emit(
	char_type *iter,
	::fast_io::operations::decay::
		print_compiler_constant_prepared_precise_fragment<
			char_type, proxy_type> const &value)
{
	auto const end{print_reserve_precise_define(
		::fast_io::io_reserve_type<char_type, proxy_type>, iter,
		value.exact_size, *value.value)};
	if (end != iter + value.exact_size) [[unlikely]]
	{
		::fast_io::fast_terminate();
	}
	return end;
}

template <::std::integral char_type, ::std::size_t extent>
inline constexpr char_type *
print_compiler_constant_prepared_fragment_compact_emit(
	char_type *iter,
	::fast_io::operations::decay::
		print_compiler_constant_prepared_static_fragment<
			char_type, extent> const &value) noexcept
{
	// Keep the short-copy decision inside this compiler-constant-only emitter.
	// GCC 13 otherwise clones small_scatter_copy_n after the prepared descriptor
	// has erased the literal's type-level extent, leaving a call in an otherwise
	// immediate `"i=3.2"` record.  Duplicating the shared sixteen-element policy
	// here exposes a propagated literal length to this always-inlined caller
	// without forcing the general range/scatter helper into every hot call site.
	auto const count{value.scatter.len};
	if (count <= 16u)
	{
		for (::std::size_t index{}; index != count; ++index)
		{
			iter[index] = value.scatter.base[index];
		}
		return iter + count;
	}
	return ::fast_io::details::non_overlapped_copy_n(
		value.scatter.base, count, iter);
}

template <::std::integral char_type, typename proxy_type>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_prepared_fragment_scatter_emit(
	::fast_io::basic_io_scatter_t<char_type> *iter,
	::fast_io::operations::decay::
		print_compiler_constant_prepared_precise_fragment<
			char_type, proxy_type> const &value) noexcept
{
	return print_compiler_constant_static_fragments_define(
		::fast_io::io_reserve_type<char_type, proxy_type>, iter,
		*value.value);
}

template <::std::integral char_type, ::std::size_t extent>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_compiler_constant_prepared_fragment_scatter_emit(
	::fast_io::basic_io_scatter_t<char_type> *iter,
	::fast_io::operations::decay::
		print_compiler_constant_prepared_static_fragment<
			char_type, extent> const &value) noexcept
{
	if (value.scatter.len != 0u)
	{
		*iter++ = value.scatter;
	}
	return iter;
}

template <::std::integral char_type, typename T>
inline constexpr char_type *
print_compiler_constant_pre_normalization_fragment_compact_emit_one(
	char_type *iter, T &&value);

template <::std::integral char_type, typename T, ::std::size_t... index>
inline constexpr char_type *
print_compiler_constant_pre_normalization_fragment_compact_emit_pack(
	char_type *iter, T &&value, ::std::index_sequence<index...>)
{
	auto &&pack_ref{::fast_io::details::decay::print_semantic_node_ref(
		::std::forward<T>(value))};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_fragment_compact_emit_one<
			  char_type>(iter, ::fast_io::containers::get<index>(
								   pack_ref.storage))),
	 ...);
	return iter;
}

template <::std::integral char_type, typename T>
inline constexpr char_type *
print_compiler_constant_pre_normalization_fragment_compact_emit_one(
	char_type *iter, T &&value)
{
	using source_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::details::print_pack<source_type>)
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_compact_emit_pack<
				char_type>(iter, ::std::forward<T>(value),
						   ::std::make_index_sequence<source_type::size>{});
	}
	else if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<
				char_type, source_type>)
	{
		auto materialized{print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, source_type>, value)};
		using replacement_type = ::std::remove_cvref_t<decltype(materialized)>;
		static_assert(
			::fast_io::compiler_constant_precise_compact_preferred<
				char_type, replacement_type>);
		auto const exact_size{print_reserve_precise_size(
			::fast_io::io_reserve_type<char_type, replacement_type>,
			materialized)};
		auto const end{print_reserve_precise_define(
			::fast_io::io_reserve_type<char_type, replacement_type>, iter,
			exact_size, materialized)};
		if (end != iter + exact_size) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		return end;
	}
	else if constexpr (
		::std::is_bounded_array_v<::std::remove_reference_t<T>>)
	{
		constexpr ::std::size_t extent{
			::std::extent_v<::std::remove_reference_t<T>>};
		if constexpr (extent > 1u)
		{
			return ::fast_io::details::decay::static_scatter_copy_n<
				extent - 1u>(__builtin_addressof(value[0u]), iter);
		}
		else
		{
			return iter;
		}
	}
	else
	{
		static_assert(
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::available);
		constexpr ::std::size_t extent{
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::size};
		auto const scatter{
			::fast_io::details::decay::print_static_scatter_traits<
				char_type, source_type>::define(value)};
		return ::fast_io::details::decay::static_scatter_copy_n<extent>(
			scatter.base, iter);
	}
}

/// @brief Materializes one already-sized compact compiler-constant record and completes its direct scalar write.
/// @details `capacity` is selected by the caller from a small geometric ladder.  Keeping the array in a separately
///          instantiated always-inline helper lets an optimizer-proven five-byte record own eight bytes of scratch
///          instead of reserving the complete 64-byte admission window, without duplicating the formatting algorithm.
template <::std::size_t capacity, bool line, typename outputstmtype,
		  typename... Args>
// GCC 11--16 and Clang 17--23 naturally inline the selected compact terminal;
// forcing it showed no caller, section-size, or throughput improvement.
inline constexpr void
print_compiler_constant_pre_normalization_fragment_compact_emit_and_write(
	outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(capacity != 0u);
	::fast_io::freestanding::array<char_type, capacity> buffer{};
	char_type *current{buffer.data()};
#if defined(__GNUC__) && !defined(__clang__)
	FAST_IO_IF_NOT_CONSTEVAL
	{
		// GCC diagnoses every capacity-tier instantiation before eliminating the
		// impossible size arms.  Hide only the scratch object's static extent from
		// that early object-size pass; this empty barrier emits no instruction and
		// leaves the selected tier, values, and destination address optimizable.
		__asm__ __volatile__("" : "+r"(current));
	}
#endif
	((current = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_fragment_compact_emit_one<
			  char_type>(current, ::std::forward<Args>(args))),
	 ...);
	if constexpr (line)
	{
		*current++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	::fast_io::details::decay::print_write_all_direct(
		optstm, buffer.data(), current);
}

/// Copies an already-built exact descriptor spelling into one bounded buffer.
///
/// The static-fragment producer is invoked before this helper and never again;
/// both actual-count selection and compact output therefore observe a custom
/// materializer/CPO exactly once.
template <::std::size_t capacity, typename outputstmtype,
		  ::std::integral char_type>
inline constexpr void
print_compiler_constant_fragment_scatters_compact_and_write(
	outputstmtype &optstm,
	::fast_io::basic_io_scatter_t<char_type> const *scatters,
	::std::size_t count)
{
	static_assert(capacity != 0u);
	::fast_io::freestanding::array<char_type, capacity> buffer{};
	char_type *current{buffer.data()};
#if defined(__GNUC__) && !defined(__clang__)
	FAST_IO_IF_NOT_CONSTEVAL
	{
		// Match the source-materializer tier helper: GCC forms every capacity
		// specialization before it eliminates the value-proved arms.
		__asm__ __volatile__("" : "+r"(current));
	}
#endif
	for (auto iter{scatters}, last{scatters + count}; iter != last; ++iter)
	{
		current = ::fast_io::details::decay::small_scatter_copy_n(
			iter->base, iter->len, current);
	}
	::fast_io::details::decay::print_write_all_direct(
		optstm, buffer.data(), current);
}

/// @brief Converts one byte-sized compact scratch tier to a nonzero character capacity.
template <::std::integral char_type, ::std::size_t bytes>
inline constexpr ::std::size_t
	print_compiler_constant_fragment_compact_capacity_chars =
		bytes <= sizeof(char_type) ? 1u : bytes / sizeof(char_type);

template <::std::size_t capacity, bool line, typename outputstmtype,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepared_fragments_compact_and_write(
	outputstmtype &optstm, Prepared &...prepared)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(capacity != 0u);
	::fast_io::freestanding::array<char_type, capacity> buffer{};
	char_type *current{buffer.data()};
#if defined(__GNUC__) && !defined(__clang__)
	FAST_IO_IF_NOT_CONSTEVAL
	{
		// GCC forms every geometric tier before eliminating the impossible size
		// arms.  Hide only the selected scratch address from that early object-size
		// pass; the barrier emits no instruction and does not obscure any payload.
		__asm__ __volatile__("" : "+r"(current));
	}
#endif
	((current = ::fast_io::operations::decay::
		  print_compiler_constant_prepared_fragment_compact_emit<
			  char_type>(current, prepared)),
	 ...);
	if constexpr (line)
	{
		*current++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	::fast_io::details::decay::print_write_all_direct(
		optstm, buffer.data(), current);
}

/// Selects the smallest compact scratch tier for one already-measured record.
///
/// Ordinary placement is intentional even though forcing this helper removes
/// 31--39 KiB from a 16-constant-callsite Clang 17--23 probe. In an isolated
/// run-time-only translation unit the attribute is inert, but in a mixed unit
/// it consumes enough of Clang's global inlining budget to outline the unknown
/// floating `io::print` path. CPU-26 `/dev/null` A/B measurements on Clang 21
/// and 23 show a repeatable roughly 0.9% run-time regression. Preserving the
/// unknown hot path takes precedence here; the constant-path code-size issue
/// requires structural isolation rather than broader forced inlining.
template <bool line, typename outputstmtype, typename... Prepared>
inline constexpr void
print_compiler_constant_prepared_fragments_compact_select_and_write(
	outputstmtype &optstm, ::std::size_t total, Prepared &...prepared)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t compact_bytes{64u};
	constexpr ::std::size_t compact_chars{
		sizeof(char_type) < compact_bytes
			? compact_bytes / sizeof(char_type)
			: 1u};
	constexpr ::std::size_t capacity8{
		::fast_io::operations::decay::
			print_compiler_constant_fragment_compact_capacity_chars<
				char_type, 8u>};
	constexpr ::std::size_t capacity16{
		::fast_io::operations::decay::
			print_compiler_constant_fragment_compact_capacity_chars<
				char_type, 16u>};
	constexpr ::std::size_t capacity32{
		::fast_io::operations::decay::
			print_compiler_constant_fragment_compact_capacity_chars<
				char_type, 32u>};
	constexpr ::std::size_t capacity64{
		::fast_io::operations::decay::
			print_compiler_constant_fragment_compact_capacity_chars<
				char_type, 64u>};
	static_assert(capacity64 == compact_chars);
	if (total <= capacity8)
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_compact_and_write<
				capacity8, line>(optstm, prepared...);
	}
	else if (total <= capacity16)
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_compact_and_write<
				capacity16, line>(optstm, prepared...);
	}
	else if (total <= capacity32)
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_compact_and_write<
				capacity32, line>(optstm, prepared...);
	}
	else
	{
		// The caller admits this selector only after proving `total <= compact_chars`.
		// Spell the final byte tier by name without repeating that already-established
		// test in the hot path; `capacity64 == compact_chars` is asserted above.
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_compact_and_write<
				capacity64, line>(optstm, prepared...);
	}
}

/// Emits a prepared record through immutable fragments without re-materializing or re-measuring any candidate.
///
/// Ordinary placement is intentional. Forcing this complete scatter emitter
/// helps GCC 13--14 eliminate a compact record's unreachable fallback and can
/// improve a 16-callsite `/dev/null` probe, but it also duplicates the genuine
/// large-record plan. With 128 distinct randomly selected large records, GCC 13
/// regresses by 1.34%, grows `.text` by 23.7 KiB, and takes 11% longer to
/// compile; GCC 16 gains only within measurement noise while also increasing
/// code size and compile time. A future optimization must structurally isolate
/// the removable fallback from the real scatter path rather than forcing this
/// shared helper into every caller.
template <::std::size_t maximum_count, bool line, typename outputstmtype,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepared_fragments_scatter_write(
	outputstmtype &optstm, Prepared &...prepared)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(maximum_count != 0u && maximum_count <= 256u);
	::fast_io::basic_io_scatter_t<char_type> scatters[maximum_count];
	auto iter{scatters};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_prepared_fragment_scatter_emit<
			  char_type>(iter, prepared)),
	 ...);
	if constexpr (line)
	{
		*iter++ = ::fast_io::details::decay::line_scatter_common<char_type>;
	}
	auto const emitted{static_cast<::std::size_t>(iter - scatters)};
	if (emitted == 0u)
	{
		return;
	}
	if (emitted == 1u)
	{
		auto const [base, len]{scatters[0u]};
		if (len != 0u)
		{
			::fast_io::details::decay::print_write_all_direct(
				optstm, base, base + len);
		}
		return;
	}
	if constexpr (
		::fast_io::details::decay::
			print_uses_byte_scatter_representation<outputstmtype>)
	{
		::fast_io::io_scatter_t byte_scatters[maximum_count];
		for (::std::size_t index{}; index != emitted; ++index)
		{
			byte_scatters[index] = {
				scatters[index].base,
				::fast_io::details::intrinsics::mul_or_overflow_die(
					scatters[index].len, sizeof(char_type))};
		}
		::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(
			optstm, byte_scatters, emitted);
	}
	else
	{
		::fast_io::operations::decay::scatter_write_all_decay_dispatch(
			optstm, scatters, emitted);
	}
}

template <::std::size_t maximum_count, bool line, typename outputstmtype,
		  typename... Prepared>
inline constexpr void
print_compiler_constant_prepared_fragments_emit(
	outputstmtype &optstm, Prepared &...prepared)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t maximum_direct_fragment_count{
		sizeof...(Prepared) + static_cast<::std::size_t>(line)};
	if constexpr (maximum_direct_fragment_count == 1u)
	{
		// A structurally singleton provider is already the complete record. Keep
		// its immutable pointer (notably mnp::static_arg's provider-owned core
		// freestanding storage)
		// instead of copying it through an automatic compact buffer.
		if (::fast_io::operations::decay::
				print_compiler_constant_prepared_single_static_fragments_try<line>(
					optstm, prepared...))
		{
			return;
		}
	}
	constexpr ::std::size_t compact_bytes{64u};
	constexpr ::std::size_t compact_chars{
		sizeof(char_type) < compact_bytes
			? compact_bytes / sizeof(char_type)
			: 1u};
	constexpr ::std::size_t maximum_total{[]() consteval {
		::std::size_t result{static_cast<::std::size_t>(line)};
		((result = ::fast_io::details::decay::
			  print_contiguous_char_extent_add_or_unavailable<char_type>(
				  result,
				  ::fast_io::operations::decay::
					  print_compiler_constant_prepared_fragment_capacity(
						  ::std::type_identity<
							  ::std::remove_cvref_t<Prepared>>{}))),
		 ...);
		return result;
	}()};
	::std::size_t total{static_cast<::std::size_t>(line)};
	((total = ::fast_io::details::decay::
		  print_contiguous_char_extent_add_or_unavailable<char_type>(
			  total,
			  ::fast_io::operations::decay::
				  print_compiler_constant_prepared_fragment_size(prepared))),
	 ...);
#if defined(__clang__) && 21 <= __clang_major__
	constexpr ::std::size_t clang_direct_capacity_limit{
		(::fast_io::details::compiler_constant_materialization_max_bytes +
		 64u) /
		sizeof(char_type)};
	constexpr bool clang_bounded_precise_direct{
		(false || ... || []<typename prepared_type>() consteval {
			if constexpr (requires {
							  typename prepared_type::
								  compiler_constant_prepared_precise_tag;
							  typename prepared_type::
								  compiler_constant_prepared_precise_proxy_type;
						  })
			{
				using proxy_type = typename prepared_type::
					compiler_constant_prepared_precise_proxy_type;
				return ::fast_io::compiler_constant_precise_compact_preferred<
					char_type, proxy_type>;
			}
			else
			{
				return false;
			}
		}.template operator()<::std::remove_cvref_t<Prepared>>()) &&
		maximum_total != 0u && maximum_total != SIZE_MAX && maximum_total <= clang_direct_capacity_limit && ::fast_io::details::decay::print_stack_buffer_size_within_limit<maximum_total, char_type>};
	if constexpr (clang_bounded_precise_direct)
	{
		// Clang 21--23 retain the conservative precision reserve and its scatter
		// fallback unless the already-measured prepared record is isolated here.
		// Clang 17--20 measured a reversal, while the positive Clang 23 endpoint
		// makes the proven 21 boundary future-open. Runtime floating sources cannot
		// construct this prepared compiler-constant proxy.
		if (total == 0u)
		{
			return;
		}
		if (total == SIZE_MAX || maximum_total < total) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		FAST_IO_IF_CONSTEVAL
		{
			::fast_io::operations::decay::
				print_compiler_constant_prepared_fragments_compact_and_write<
					maximum_total, line>(optstm, prepared...);
		}
		else
		{
			static_assert(
				maximum_total * sizeof(char_type) <=
				::fast_io::details::compiler_constant_materialization_max_bytes +
					64u);
			// The assertion proves at most 320 bytes, so this multiplication cannot
			// overflow and Clang can contract the allocation to the exact spelling.
			auto *buffer{static_cast<char_type *>(__builtin_alloca_with_align(
				total * sizeof(char_type), alignof(char_type) * 8u))};
			char_type *current{buffer};
			((current = ::fast_io::operations::decay::
				  print_compiler_constant_prepared_fragment_compact_emit<char_type>(
					  current, prepared)),
			 ...);
			if constexpr (line)
			{
				*current++ = ::fast_io::char_literal_v<u8'\n', char_type>;
			}
			::fast_io::details::decay::print_write_all_direct(
				optstm, buffer, current);
		}
		return;
	}
#endif
	if constexpr (maximum_total != SIZE_MAX && maximum_total <= compact_chars)
	{
		// The type-level reserve contracts prove that no value represented by this
		// prepared record can reach the immutable-fragment fallback. Keep that
		// fallback in the discarded arm so a frontend cannot emit its otherwise
		// unreferenced template graph into a default-linked executable.
		if (total == 0u)
		{
			return;
		}
		if (total == SIZE_MAX || maximum_total < total) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_compact_select_and_write<
				line>(optstm, total, prepared...);
	}
	else
	{
		if (total != 0u && total != SIZE_MAX && total <= compact_chars)
		{
			::fast_io::operations::decay::
				print_compiler_constant_prepared_fragments_compact_select_and_write<
					line>(optstm, total, prepared...);
			return;
		}
		if constexpr (maximum_direct_fragment_count != 1u)
		{
			// A record larger than the compact threshold remains zero-copy when every
			// leaf owns immutable provider storage. Ordinary caller constants cannot
			// form a new value-dependent static object in C++20, so this is the
			// correct large-record continuation after the small single-write policy.
			if (::fast_io::operations::decay::
					print_compiler_constant_prepared_single_static_fragments_try<line>(
						optstm, prepared...))
			{
				return;
			}
		}
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_scatter_write<
				maximum_count, line>(optstm, prepared...);
	}
}

template <::std::size_t maximum_count, bool line, typename outputstmtype>
struct print_compiler_constant_prepared_fragments_continuation
{
	outputstmtype &optstm;

	template <typename... Prepared>
	inline constexpr void
	operator()(Prepared &...prepared) const
	{
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_emit<
				maximum_count, line>(optstm, prepared...);
	}
};

/// Computes the exact payload extent of an array/fixed-scatter-only source run.
///
/// Unlike the compiler-constant value gate, this is a pure type calculation:
/// the result therefore admits or rejects coalescing without adding a branch to
/// an unknown run-time path.
template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_compiler_constant_pre_normalization_fragment_static_run_size() noexcept
{
	if constexpr (!((::fast_io::operations::decay::
						 print_compiler_constant_pre_normalization_fragment_source<
							 char_type, Args>::compact_static_size != SIZE_MAX) &&
					...))
	{
		return SIZE_MAX;
	}
	else
	{
		::std::size_t total{static_cast<::std::size_t>(line)};
		((total = ::fast_io::details::decay::
			  print_contiguous_char_extent_add_or_unavailable<char_type>(
				  total,
				  ::fast_io::operations::decay::
					  print_compiler_constant_pre_normalization_fragment_source<
						  char_type, Args>::compact_static_size)),
		 ...);
		return total;
	}
}

template <bool line, typename outputstmtype, typename... Args>
inline consteval ::std::size_t
print_compiler_constant_pre_normalization_fragment_run_count() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	::std::size_t count{static_cast<::std::size_t>(line)};
	((count = count == SIZE_MAX ||
					  ::fast_io::operations::decay::
							  print_compiler_constant_pre_normalization_fragment_source<
								  char_type, Args>::count > SIZE_MAX - count
				  ? SIZE_MAX
				  : count +
						::fast_io::operations::decay::
							print_compiler_constant_pre_normalization_fragment_source<
								char_type, Args>::count),
	 ...);
	return count;
}

template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_fragment_run_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool semantic_run{
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_input_argument_v<
			 char_type, Args>)};
	if constexpr (semantic_run)
	{
		// A semantic source is not its normalized record: pack expansion and
		// condition selection may change both the active types and their count,
		// including reducing a non-empty source pack to zero leaves. Therefore a
		// pre-normalization fragment proof cannot establish equivalence with the
		// exact whole-record status protocol. Let semantic flattening select a
		// strategy from the active normalized record instead.
		return false;
	}
	else if constexpr (
		(!::fast_io::details::decay::
			 print_output_retains_static_scatter<outputstmtype> &&
		 !::fast_io::details::decay::
			 print_output_accepts_static_provider_scalar<outputstmtype>) ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> ||
		!(::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_fragment_source<
				  char_type, Args>::available &&
		  ...) ||
		(false || ... ||
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args>::nested_candidate))
	{
		return false;
	}
	else
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_status_print_define<
				line, outputstmtype,
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_normalized_t<
						char_type, semantic_run, Args>...>)
		{
			return false;
		}
		constexpr ::std::size_t count{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_run_count<
					line, outputstmtype, Args...>()};
		return count != 0u && count != SIZE_MAX && count <= 256u &&
			   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
				   count, ::fast_io::basic_io_scatter_t<char_type>>;
	}
}

/// Selects the narrow exact-first strategy for a complete, already-proved immutable-fragment run.
///
/// Destination admission is inherited from `fragment_run_available`: a put area, mutex, whole-run status owner, or
/// output lacking the synchronous-direct marker cannot reach this branch.  The additional direct-write proof keeps
/// the compact arm on the same scalar CPO precedence as the ordinary dispatcher.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_precise_fragment_run_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	return ::fast_io::operations::decay::
			   print_compiler_constant_pre_normalization_fragment_run_available<
				   line, outputstmtype, Args...>() &&
		   ::fast_io::details::decay::
			   print_output_accepts_static_provider_scalar<outputstmtype> &&
		   (::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_precise_fragment_source<
					char_type, Args>::available &&
			...) &&
		   (false || ... ||
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_precise_fragment_source<
					char_type, Args>::contains_candidate);
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_compiler_constant_pre_normalization_fragment_emit(
	outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t count{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_run_count<
				line, outputstmtype, Args &&...>()};
	constexpr bool has_candidate{
		(false || ... ||
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args &&>::candidate)};
	constexpr bool compact_available{
		(::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args &&>::compact_available &&
		 ...)};
	constexpr ::std::size_t static_run_size{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_static_run_size<
				line, char_type, Args &&...>()};
	constexpr ::std::size_t direct_coalesce_threshold{
		::fast_io::details::decay::
			print_scatter_direct_full_output_coalesce_threshold<
				char_type, outputstmtype>()};
	constexpr ::std::size_t direct_coalesce_limit{
		direct_coalesce_threshold != 0u
			? direct_coalesce_threshold
		: (!::fast_io::details::decay::
			   print_output_retains_static_scatter<outputstmtype> &&
		   ::fast_io::details::decay::
			   print_has_preferred_direct_write_operations<outputstmtype>)
			? ::fast_io::details::compiler_constant_materialization_max_bytes /
				  sizeof(char_type)
			: 0u};
	if constexpr (
		!has_candidate && compact_available && count > 1u &&
		static_run_size != SIZE_MAX && static_run_size != 0u &&
		direct_coalesce_limit != 0u &&
		static_run_size <= direct_coalesce_limit &&
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype> &&
		::fast_io::details::decay::print_stack_buffer_size_within_limit<
			static_run_size, char_type>)
	{
		// The stream's native-scatter policy already measures the copy+write
		// crossover. For a type-sized immutable run, materialize exactly that many
		// code units and issue one direct write; no descriptor array or run-time
		// threshold test enters the generated path. This remains automatic storage:
		// ordinary function parameters cannot manufacture a new structural NTTP
		// object containing their caller's literal bytes.
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_compact_emit_and_write<
				static_run_size, line>(
				optstm, ::std::forward<Args>(args)...);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_precise_fragment_run_available<
				line, outputstmtype, Args &&...>())
	{
		// Prepare each marked proxy in its own recursion frame.  The terminal
		// continuation sees only references to those live objects plus their one
		// retained exact-size result; it either emits exact reserve output directly
		// or builds fragments from the same proxy, never both.
		auto sources{::fast_io::containers::forward_as_tuple(args...)};
		::fast_io::operations::decay::
			print_compiler_constant_prepared_fragments_continuation<
				count, line, outputstmtype>
				terminal{optstm};
		::fast_io::operations::decay::
			print_compiler_constant_prepare_precise_fragment_sources<
				0u, char_type>(sources, terminal);
		return;
	}
	static_assert(count != 0u && count <= 256u);
	::fast_io::basic_io_scatter_t<char_type> scatters[count];
	auto iter{scatters};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_fragment_emit_one<char_type>(
			  iter, ::std::forward<Args>(args))),
	 ...);
	if constexpr (line)
	{
		*iter++ = ::fast_io::details::decay::line_scatter_common<char_type>;
	}
	::std::size_t const emitted{static_cast<::std::size_t>(iter - scatters)};
	if (emitted == 1u)
	{
		// One immutable fragment is already contiguous.  Prefer the ordinary
		// direct-write primitive where available; this avoids constructing a
		// byte-descriptor mirror and, on POSIX, retains the inline write syscall
		// instead of entering the multi-descriptor cold writev continuation.
		if constexpr (
			::fast_io::details::decay::
				print_output_accepts_static_provider_scalar<outputstmtype> ||
			(::fast_io::details::decay::
				 print_output_retains_static_scatter<outputstmtype> &&
			 ::fast_io::details::decay::
				 print_has_preferred_direct_write_operations<outputstmtype>))
		{
			auto const [base, len]{scatters[0u]};
			if (len != 0u)
			{
				::fast_io::details::decay::print_write_all_direct(
					optstm, base, base + len);
			}
			return;
		}
	}
	if constexpr (
		has_candidate && count > 1u &&
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype>)
	{
		// Test the actual fragment count before copying. A one-digit integer or
		// boolalpha proxy may advertise a wider worst-case descriptor budget while
		// selecting exactly one provider-owned table slice for this value; that
		// slice must stay a direct rodata write. Only genuinely multi-fragment
		// records enter the bounded contiguous materializer below.
		constexpr ::std::size_t compact_bytes{64u};
		constexpr ::std::size_t compact_chars{
			sizeof(char_type) < compact_bytes
				? compact_bytes / sizeof(char_type)
				: 1u};
		::std::size_t total{};
		for (auto descriptor{scatters}; descriptor != iter; ++descriptor)
		{
			total = ::fast_io::details::decay::
				print_contiguous_char_extent_add_or_unavailable<char_type>(
					total, descriptor->len);
		}
		if (total != 0u && total <= compact_chars)
		{
			constexpr ::std::size_t capacity8{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_compact_capacity_chars<
						char_type, 8u>};
			constexpr ::std::size_t capacity16{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_compact_capacity_chars<
						char_type, 16u>};
			constexpr ::std::size_t capacity32{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_compact_capacity_chars<
						char_type, 32u>};
			constexpr ::std::size_t capacity64{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_compact_capacity_chars<
						char_type, 64u>};
			static_assert(capacity64 == compact_chars);
			if (total <= capacity8)
			{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_scatters_compact_and_write<
						capacity8>(optstm, scatters, emitted);
			}
			else if (total <= capacity16)
			{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_scatters_compact_and_write<
						capacity16>(optstm, scatters, emitted);
			}
			else if (total <= capacity32)
			{
				::fast_io::operations::decay::
					print_compiler_constant_fragment_scatters_compact_and_write<
						capacity32>(optstm, scatters, emitted);
			}
			else
			{
				// The enclosing `total <= compact_chars` guard is the array-bound proof.
				// Naming the 64-byte tier here documents the byte/code-unit conversion
				// while avoiding a redundant compare or termination edge.
				::fast_io::operations::decay::
					print_compiler_constant_fragment_scatters_compact_and_write<
						capacity64>(optstm, scatters, emitted);
			}
			return;
		}
	}
	if constexpr (
		::fast_io::details::decay::
			print_uses_byte_scatter_representation<outputstmtype>)
	{
		::fast_io::io_scatter_t byte_scatters[count];
		for (::std::size_t index{}; index != emitted; ++index)
		{
			byte_scatters[index] = {
				scatters[index].base,
				::fast_io::details::intrinsics::mul_or_overflow_die(
					scatters[index].len, sizeof(char_type))};
		}
		::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(
			optstm, byte_scatters, emitted);
	}
	else
	{
		::fast_io::operations::decay::scatter_write_all_decay_dispatch(
			optstm, scatters, emitted);
	}
}

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_static_source
{
	using value_type = ::std::remove_cvref_t<T>;
	inline static constexpr bool candidate{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, value_type>};
	inline static constexpr bool passive{[]() consteval {
		if constexpr (
			::fast_io::details::decay::print_buffered_passive_reserve_leaf<
				value_type>::value &&
			::fast_io::reserve_printable<char_type, value_type>)
		{
#if defined(__HERBCEPTIONS__)
			return requires(char_type *iter, value_type &value) {
				// Static pre-normalization publishes only after this named passive leaf has emitted. Preserve the
				// strategy invariant by proving both deterministic and unwind channels empty for that exact call.
				requires(!throws((print_reserve_define(
					::fast_io::io_reserve_type<char_type, value_type>, iter, value))));
				{
					print_reserve_define(
						::fast_io::io_reserve_type<char_type, value_type>, iter, value)
				} noexcept -> ::std::same_as<char_type *>;
			};
#else
			return noexcept(print_reserve_define(
				::fast_io::io_reserve_type<char_type, value_type>,
				::std::declval<char_type *>(), ::std::declval<value_type &>()));
#endif
		}
		else
		{
			return false;
		}
	}()};
	inline static constexpr bool available{candidate || passive};
	inline static constexpr ::std::size_t size{[]() consteval {
		if constexpr (candidate)
		{
			using proxy_type =
				::fast_io::details::compiler_constant_materialized_t<
					char_type, value_type>;
			return print_reserve_size(
				::fast_io::io_reserve_type<char_type, proxy_type>);
		}
		else if constexpr (passive)
		{
			return print_reserve_size(
				::fast_io::io_reserve_type<char_type, value_type>);
		}
		else
		{
			return SIZE_MAX;
		}
	}()};
};

template <::std::integral char_type, typename... Args>
struct print_compiler_constant_pre_normalization_static_source<
	char_type, ::fast_io::manipulators::pack_t<Args...>>
{
	inline static constexpr bool available{
		(::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_static_source<
				 char_type, Args>::available &&
		 ...)};
	inline static constexpr ::std::size_t size{[]() consteval {
		if constexpr (!available)
		{
			return SIZE_MAX;
		}
		else
		{
			::std::size_t total{};
			((total = ::fast_io::details::decay::
				  print_contiguous_char_extent_add_or_unavailable<char_type>(
					  total,
					  ::fast_io::operations::decay::
						  print_compiler_constant_pre_normalization_static_source<
							  char_type, Args>::size)),
			 ...);
			return total;
		}
	}()};
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_static_source<char_type, T &>
	: print_compiler_constant_pre_normalization_static_source<
		  char_type, ::std::remove_cv_t<T>>
{};

template <::std::integral char_type, typename T>
struct print_compiler_constant_pre_normalization_static_source<char_type, T &&>
	: print_compiler_constant_pre_normalization_static_source<
		  char_type, ::std::remove_cv_t<T>>
{};

template <::std::integral char_type, typename T>
// GCC 11, 12, 14, and 15 through the newest tested trunk otherwise retain a
// 1,257--1,406-instruction floating conversion body after a successful value
// query. Forcing only this constant-only leaf reduces the failing direct roots
// to their final output bytes. GCC 13 alone erases every tested source shape
// without assistance; GCC 15 still outlines the raw and bare-format shapes.
// Clang 21--23 also need the leaf to avoid 4.9 KiB of text and one extra call.
#if (defined(__GNUC__) && !defined(__clang__) &&            \
	 (__GNUC__ == 11 || __GNUC__ == 12 || __GNUC__ == 14 || \
	  __GNUC__ >= 15)) ||                                   \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr char_type *
	print_compiler_constant_pre_normalization_static_emit_one(
		char_type *iter, T &&value);

template <::std::integral char_type, typename T, ::std::size_t... index>
#if defined(__clang__) && __clang_major__ >= 21
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr char_type *
	print_compiler_constant_pre_normalization_static_emit_pack(
		char_type *iter, T &&value, ::std::index_sequence<index...>)
{
	auto &&pack_ref{::fast_io::details::decay::print_semantic_node_ref(
		::std::forward<T>(value))};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_static_emit_one<char_type>(
			  iter, ::fast_io::containers::get<index>(pack_ref.storage))),
	 ...);
	return iter;
}

template <::std::integral char_type, typename T>
#if (defined(__GNUC__) && !defined(__clang__) &&            \
	 (__GNUC__ == 11 || __GNUC__ == 12 || __GNUC__ == 14 || \
	  __GNUC__ >= 15)) ||                                   \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr char_type *
	print_compiler_constant_pre_normalization_static_emit_one(
		char_type *iter, T &&value)
{
	using value_type = ::std::remove_cvref_t<T>;
	if constexpr (::fast_io::details::print_pack<value_type>)
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_emit_pack<char_type>(
				iter, ::std::forward<T>(value),
				::std::make_index_sequence<value_type::size>{});
	}
	else if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, value_type>)
	{
		auto materialized{print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, value_type>, value)};
		using proxy_type = ::std::remove_cvref_t<decltype(materialized)>;
		return print_reserve_define(
			::fast_io::io_reserve_type<char_type, proxy_type>, iter, materialized);
	}
	else
	{
		static_assert(
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_static_source<
					char_type, value_type>::passive);
		return print_reserve_define(
			::fast_io::io_reserve_type<char_type, value_type>, iter, value);
	}
}

template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_compiler_constant_pre_normalization_static_run_size() noexcept
{
	if constexpr (!(::fast_io::operations::decay::
						print_compiler_constant_pre_normalization_static_source<
							char_type, Args>::available &&
					...))
	{
		return SIZE_MAX;
	}
	else
	{
		::std::size_t total{static_cast<::std::size_t>(line)};
		((total = ::fast_io::details::decay::
			  print_contiguous_char_extent_add_or_unavailable<char_type>(
				  total,
				  ::fast_io::operations::decay::
					  print_compiler_constant_pre_normalization_static_source<
						  char_type, Args>::size)),
		 ...);
		return total;
	}
}

template <bool line, typename outputstmtype, typename... Args>
// GCC 11/12 outline this buffer/output wrapper after the conversion leaf;
// GCC 14 through the current trunk also retain it for a constant ISO8601
// source. Forcing only this true-arm wrapper makes the affected releases emit
// one direct 25-code-unit record. GCC 13 already erases ISO naturally, while
// forcing this wrapper there makes an ordinary constant double outline its
// conversion leaf, so that exact release is deliberately excluded. The paired
// unknown-value roots keep zero compiler-constant residue and their historical
// frames/call graphs. Clang 21--23 retain the previously proved true-arm
// placement. No formatter is placed in the query's false arm.
#if (defined(__GNUC__) && !defined(__clang__) &&              \
	 (__GNUC__ == 11 || __GNUC__ == 12 || __GNUC__ >= 14)) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_pre_normalization_static_emit(
		outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t reserve_size{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_run_size<
				line, char_type, Args...>()};
	static_assert(reserve_size != SIZE_MAX);
	static_assert(
		reserve_size <=
		::fast_io::details::compiler_constant_materialization_max_bytes /
			sizeof(char_type));
	// Every exact reserve definition initializes its complete contribution before
	// the output CPO observes [buffer.data(), iter). The conservative tail is never
	// read, so value-initializing it only adds zero stores to short constant records.
	::fast_io::freestanding::array<
		char_type, reserve_size == 0u ? 1u : reserve_size>
		buffer;
	char_type *iter{buffer.data()};
	((iter = ::fast_io::operations::decay::
		  print_compiler_constant_pre_normalization_static_emit_one<char_type>(
			  iter, ::std::forward<Args>(args))),
	 ...);
	if constexpr (line)
	{
		*iter++ = ::fast_io::char_literal_v<u8'\n', char_type>;
	}
	if constexpr (
		::fast_io::details::decay::
			print_has_preferred_direct_write_operations<outputstmtype>)
	{
		::fast_io::details::decay::print_write_all_direct(
			optstm, buffer.data(), iter);
	}
	else
	{
		::fast_io::operations::decay::write_all_decay_dispatch(
			optstm, buffer.data(), iter);
	}
}

/// Extracts one immutable spelling carried entirely by a core provider type.
///
/// The primary template is deliberately unavailable. Raw `mnp::static_arg`
/// nodes use their native materializer; syntax front ends may instead pass a
/// `static_provider_node` whose recipe describes one typed semantic fragment.
/// In both cases this IO trait, rather than the producer, decides whether a
/// complete run becomes one provider-owned record.
template <::std::integral char_type, typename T>
struct print_static_provider_merged_component
{
	inline static constexpr bool available{};
};

template <::std::integral char_type>
struct print_static_provider_merged_component<char_type, ::fast_io::io_null_t>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t size{};

	template <typename storage_type>
	inline static constexpr void append(
		storage_type &, ::std::size_t &) noexcept
	{}
};

template <bool materializable, ::std::integral char_type,
		  ::fast_io::manipulators::static_argument_constant value_literal>
struct print_static_argument_merged_component
{
	inline static constexpr bool available{};
	inline static constexpr ::std::size_t size{SIZE_MAX};
};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_argument_constant value_literal>
struct print_static_argument_merged_component<true, char_type, value_literal>
{
	inline static constexpr bool available{true};
	using proxy_type = ::fast_io::manipulators::
		static_argument_materialized_t<char_type, value_literal>;
	inline static constexpr ::std::size_t size{proxy_type::size};

	// Keep the component operation constexpr rather than immediate.  The only
	// caller is the consteval merged-storage initializer below, so this does not
	// make provider materialization a runtime protocol.  It does, however, avoid
	// a Clang 17 C++20 immediate-function limitation which rejects mutation of
	// the initializer's local array through a consteval helper reference.
	template <typename storage_type>
	inline static constexpr void append(
		storage_type &destination, ::std::size_t &position)
	{
		auto const &source{
			::fast_io::manipulators::static_provider_storage_t<
				typename proxy_type::provider>::storage};
		for (::std::size_t index{}; index != proxy_type::size; ++index)
		{
			destination[position++] =
				source[proxy_type::provider_offset + index];
		}
	}

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type> define() noexcept
	{
		return {
			::fast_io::manipulators::static_provider_storage_t<
				typename proxy_type::provider>::storage.data() +
				proxy_type::provider_offset,
			proxy_type::size};
	}
};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_argument_constant value_literal>
struct print_static_provider_merged_component<
	char_type, ::fast_io::manipulators::static_arg_t<value_literal>>
	: print_static_argument_merged_component<
		  ::fast_io::manipulators::static_argument_details::
			  native_materializable<char_type, value_literal>,
		  char_type, value_literal>
{};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_argument_constant name_literal,
		  ::fast_io::manipulators::static_argument_constant value_literal>
struct print_static_provider_merged_component<
	char_type,
	::fast_io::manipulators::static_named_arg_t<name_literal, value_literal>>
	: print_static_provider_merged_component<
		  char_type, ::fast_io::manipulators::static_arg_t<value_literal>>
{};

template <::std::integral char_type,
		  ::fast_io::manipulators::static_provider_recipe provider_type,
		  ::std::size_t offset, ::std::size_t extent>
	requires ::std::same_as<char_type, typename provider_type::char_type>
struct print_static_provider_merged_component<
	char_type,
	::fast_io::manipulators::static_provider_node<
		provider_type, offset, extent>>
{
	inline static constexpr bool available{true};
	inline static constexpr ::std::size_t size{extent};

	template <typename storage_type>
	inline static constexpr void append(
		storage_type &destination, ::std::size_t &position)
	{
		auto const &source{
			::fast_io::manipulators::static_provider_storage_t<
				provider_type>::storage};
		for (::std::size_t index{}; index != extent; ++index)
		{
			destination[position++] = source[offset + index];
		}
	}

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type> define() noexcept
	{
		return {
			::fast_io::manipulators::static_provider_storage_t<
				provider_type>::storage.data() +
				offset,
			extent};
	}
};

/// Syntax-owned literal nodes have the same immutable provider lifetime as
/// explicit static-argument nodes, but remain a distinct type so mixed dynamic
/// records do not enter the eager compiler-constant fragment graph. Reusing
/// the provider component protocol here preserves the one-pointer path for a
/// literal-only record and permits adjacent provider-only runs to merge.
template <::std::integral char_type,
		  ::fast_io::manipulators::static_provider_recipe provider_type,
		  ::std::size_t offset, ::std::size_t extent>
	requires ::std::same_as<char_type, typename provider_type::char_type>
struct print_static_provider_merged_component<
	char_type,
	::fast_io::manipulators::syntax_transport_details::static_provider_scatter_node<
		provider_type, offset, extent>>
	: print_static_provider_merged_component<
		  char_type,
		  ::fast_io::manipulators::static_provider_node<
			  provider_type, offset, extent>>
{};

template <bool line, ::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_static_provider_merged_run_size() noexcept
{
	if constexpr (
		sizeof...(Args) == 0u ||
		!(::fast_io::operations::decay::
			  print_static_provider_merged_component<
				  char_type, ::std::remove_cvref_t<Args>>::available &&
		  ...))
	{
		return SIZE_MAX;
	}
	else
	{
		::std::size_t total{static_cast<::std::size_t>(line)};
		((total = ::fast_io::details::decay::
			  print_contiguous_char_extent_add_or_unavailable<char_type>(
				  total,
				  ::fast_io::operations::decay::
					  print_static_provider_merged_component<
						  char_type, ::std::remove_cvref_t<Args>>::size)),
		 ...);
		return total;
	}
}

template <::std::integral char_type, typename... Args>
inline consteval ::std::size_t
print_static_provider_nonempty_component_count() noexcept
{
	if constexpr (
		!(::fast_io::operations::decay::
			  print_static_provider_merged_component<
				  char_type, ::std::remove_cvref_t<Args>>::available &&
		  ...))
	{
		return 0u;
	}
	else
	{
		return (0u + ... + static_cast<::std::size_t>(::fast_io::operations::decay::print_static_provider_merged_component<char_type, ::std::remove_cvref_t<Args>>::size != 0u));
	}
}

template <::std::integral char_type, typename... Args>
struct print_static_provider_single_component_selector;

template <::std::integral char_type, typename T, typename... Args>
struct print_static_provider_single_component_selector<
	char_type, T, Args...>
{
	using component = ::fast_io::operations::decay::
		print_static_provider_merged_component<
			char_type, ::std::remove_cvref_t<T>>;

	[[nodiscard]] inline static constexpr ::fast_io::basic_io_scatter_t<char_type> define() noexcept
	{
		if constexpr (component::available && component::size != 0u)
		{
			return component::define();
		}
		else
		{
			return ::fast_io::operations::decay::
				print_static_provider_single_component_selector<
					char_type, Args...>::define();
		}
	}
};

/// Owns the complete all-static raw-IO record as one provider-owned core
/// freestanding array in read-only storage.
template <bool line, ::std::integral char_type, typename... Args>
	requires(
		::fast_io::operations::decay::
				print_static_provider_merged_run_size<line, char_type, Args...>() !=
			SIZE_MAX &&
		::fast_io::operations::decay::
				print_static_provider_merged_run_size<line, char_type, Args...>() <=
			::fast_io::manipulators::
				static_provider_materialization_code_unit_limit)
struct print_static_provider_merged_run_provider
{
	inline static constexpr ::std::size_t size{
		::fast_io::operations::decay::
			print_static_provider_merged_run_size<line, char_type, Args...>()};
	// This is the final IO-owned COMDAT, not a formatting helper. Hidden ELF
	// visibility permits a direct PC-relative provider address in PIC code; it
	// neither forces a function inline nor duplicates executable instructions.
	static inline constexpr auto storage
#if (defined(__GNUC__) || defined(__clang__)) && !defined(_WIN32)
		__attribute__((visibility("hidden")))
#endif
		{[]() consteval {
			::fast_io::freestanding::array<
				char_type, size == 0u ? 1u : size>
				result{};
			::std::size_t position{};
			(::fast_io::operations::decay::
				 print_static_provider_merged_component<
					 char_type, ::std::remove_cvref_t<Args>>::append(result, position),
			 ...);
			if constexpr (line)
			{
				result[position++] =
					::fast_io::char_literal_v<u8'\n', char_type>;
			}
			if (position != size)
			{
				::fast_io::fast_terminate();
			}
			return result;
		}()};
};

/// Selects the one-pointer raw path only for an unbuffered synchronous native endpoint.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_static_provider_merged_run_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t merged_size{
		::fast_io::operations::decay::
			print_static_provider_merged_run_size<
				line, char_type, Args...>()};
	constexpr ::std::size_t nonempty_component_count{
		::fast_io::operations::decay::
			print_static_provider_nonempty_component_count<
				char_type, Args...>()};
	// A single provider already is the final immutable record. Let the ordinary
	// retained-fragment path emit its original slice, avoiding a duplicate
	// COMDAT. Merging is reserved for actual concatenation or an appended LF.
	return (line || nonempty_component_count > 1u) &&
		   ::fast_io::details::decay::
			   print_output_accepts_static_provider_scalar<outputstmtype> &&
		   merged_size != SIZE_MAX &&
		   merged_size <= ::fast_io::manipulators::
							  static_provider_materialization_code_unit_limit;
}

template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_static_provider_single_run_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	return !line &&
		   ::fast_io::details::decay::
			   print_output_accepts_static_provider_scalar<outputstmtype> &&
		   ::fast_io::operations::decay::
				   print_static_provider_merged_run_size<
					   line, char_type, Args...>() != SIZE_MAX &&
		   ::fast_io::operations::decay::
				   print_static_provider_nonempty_component_count<
					   char_type, Args...>() == 1u;
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_static_provider_single_run_emit(outputstmtype &optstm)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr auto scatter{
		::fast_io::operations::decay::
			print_static_provider_single_component_selector<
				char_type, ::std::remove_cvref_t<Args>...>::define()};
	::fast_io::details::decay::print_write_all_direct(
		optstm, scatter.base, scatter.base + scatter.len);
}

template <typename... Args>
inline constexpr bool print_all_static_provider_components_v{
	sizeof...(Args) != 0u &&
	((::fast_io::manipulators::is_static_arg_v<Args> ||
	  ::fast_io::manipulators::is_static_provider_node_v<Args> ||
	  ::fast_io::manipulators::syntax_transport_details::is_static_provider_scatter_node_v<Args> ||
	  ::std::same_as<::std::remove_cvref_t<Args>, ::fast_io::io_null_t>) &&
	 ...)};

/// Selects a destination shortcut only after every source proves a type-owned
/// provider. Raw `static_arg` and named-static tokens satisfy the same merged-
/// component proof below as already-lowered provider nodes; admitting them at
/// this source boundary is what prevents a compiler fail-closed value-query
/// path from copying immutable payload into automatic scratch. Partial programs
/// deliberately fail this predicate and retain ordinary status and write policy.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_static_provider_component_run_available() noexcept
{
	return ::fast_io::operations::decay::
			   print_all_static_provider_components_v<Args...> &&
		   !::fast_io::operations::decay::defines::has_status_print_define<
			   line, outputstmtype, ::std::remove_cvref_t<Args>...> &&
		   (::fast_io::operations::decay::
				print_static_provider_single_run_available<
					line, outputstmtype, Args...>() ||
			::fast_io::operations::decay::
				print_static_provider_merged_run_available<
					line, outputstmtype, Args...>());
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_static_provider_merged_run_emit(outputstmtype &optstm)
{
	using char_type = typename outputstmtype::output_char_type;
	using provider = ::fast_io::operations::decay::
		print_static_provider_merged_run_provider<
			line, char_type, ::std::remove_cvref_t<Args>...>;
	if constexpr (provider::size != 0u)
	{
		::fast_io::details::decay::print_write_all_direct(
			optstm, provider::storage.data(),
			provider::storage.data() + provider::size);
	}
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void
print_static_provider_component_run_emit(outputstmtype &optstm)
{
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_single_run_available<
				line, outputstmtype, Args...>())
	{
		::fast_io::operations::decay::
			print_static_provider_single_run_emit<
				line, outputstmtype, Args...>(optstm);
	}
	else
	{
		::fast_io::operations::decay::
			print_static_provider_merged_run_emit<
				line, outputstmtype, Args...>(optstm);
	}
}

template <typename... component_types>
struct print_static_provider_type_pack
{};

/// @brief Bounds the number of provider types inspected by partial-prefix canonicalization.
/// @details Sixteen covers the measured five-node format prefix without recreating the pathological large-pack
///          recursion this optimization is intended to avoid. The scanner returns at most one-past this limit, so a
///          longer run is rejected without traversing the rest of a hundreds-of-fields program.
inline constexpr ::std::size_t
	print_static_provider_prefix_normalization_max_components{16u};

template <::std::integral char_type, ::std::size_t remaining,
		  typename T, typename... Args>
inline consteval ::std::size_t print_static_provider_prefix_count() noexcept
{
	static_assert(remaining != 0u);
	if constexpr (
		::fast_io::operations::decay::print_static_provider_merged_component<
			char_type, ::std::remove_cvref_t<T>>::available)
	{
		if constexpr (remaining == 1u || sizeof...(Args) == 0u)
		{
			return 1u;
		}
		else
		{
			return 1u + ::fast_io::operations::decay::
							print_static_provider_prefix_count<
								char_type, remaining - 1u, Args...>();
		}
	}
	else
	{
		return 0u;
	}
}

template <::std::integral char_type, typename tuple_type,
		  typename index_sequence>
struct print_static_provider_prefix_provider;

template <::std::integral char_type, typename tuple_type,
		  ::std::size_t... index>
struct print_static_provider_prefix_provider<
	char_type, tuple_type, ::std::index_sequence<index...>>
{
	using type = ::fast_io::operations::decay::
		print_static_provider_merged_run_provider<
			false, char_type,
			::std::tuple_element_t<index, tuple_type>...>;
};

template <bool line, typename outputstmtype, ::std::integral char_type,
		  bool semantic_run, ::std::size_t prefix_count,
		  typename prefix_type, typename source_tuple,
		  typename suffix_sequence>
struct print_static_provider_prefix_normalized_status_safe;

template <bool line, typename outputstmtype, ::std::integral char_type,
		  bool semantic_run, ::std::size_t prefix_count,
		  typename prefix_type, typename source_tuple,
		  ::std::size_t... suffix_index>
struct print_static_provider_prefix_normalized_status_safe<
	line, outputstmtype, char_type, semantic_run, prefix_count,
	prefix_type, source_tuple, ::std::index_sequence<suffix_index...>>
	: ::std::bool_constant<
		  !::fast_io::operations::decay::defines::has_status_print_define<
			  line, outputstmtype,
			  ::fast_io::operations::decay::
				  print_compiler_constant_pre_normalization_normalized_t<
					  char_type, semantic_run, prefix_type const &>,
			  ::fast_io::operations::decay::
				  print_compiler_constant_pre_normalization_normalized_t<
					  char_type, semantic_run,
					  ::std::tuple_element_t<
						  prefix_count + suffix_index,
						  source_tuple>>...>>
{};

template <::std::size_t prefix_count, bool line, typename outputstmtype,
		  typename source_tuple, ::std::size_t... suffix_index>
inline constexpr void print_static_provider_prefix_normalized_emit_impl(
	outputstmtype &optstm, source_tuple &sources,
	::std::index_sequence<suffix_index...>)
{
	using char_type = typename outputstmtype::output_char_type;
	using source_tuple_type = ::std::remove_cvref_t<source_tuple>;
	using provider = typename ::fast_io::operations::decay::
		print_static_provider_prefix_provider<
			char_type, source_tuple_type,
			::std::make_index_sequence<prefix_count>>::type;
	auto const prefix{
		::fast_io::manipulators::static_scatter_t<char_type, provider::size>{
			provider::storage.data()}};
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, prefix,
		::fast_io::containers::get<prefix_count + suffix_index>(sources)...);
}

/// @brief Canonicalizes one short immutable provider prefix before ordinary buffered dispatch.
/// @details Format and raw IO both lower static arguments to provider nodes, but five adjacent nodes followed by a
///          run-time precision float entered the generic dispatcher as six independent types. On an eight-byte put
///          area Clang 23 and GCC 13 consequently overflowed the 27-byte prefix and buffered the four-byte scalar as a
///          second operation. Replacing only that immutable prefix with the equivalent core `static_scatter_t` lets the
///          mature two-component dispatcher stage or buffer the complete record; no value query or formatting policy is
///          added here. CPU-23 Google Benchmark medians for the 31-byte record changed from 34.22/29.68 ns to
///          19.90/20.16 ns on Clang 23 (constant/unknown) and 203.34/201.71 ns to 183.77/204.13 ns on GCC 13. The GCC
///          unknown delta is within the measured 2.9--3.5 ns standard deviation; Clang improves both paths. The probe
///          TU's text changed by +93 bytes on Clang and -5,016 bytes on GCC, while compile RSS was neutral or lower.
///
///          Admission is deliberately narrow: a writable put area, two to sixteen leading provider nodes, at least one
///          compiler-constant-capable source type after them, and at most 64 bytes of merged provider storage. Complete
///          mutex ownership stays outside, and status is checked on the original, historically normalized, and reduced
///          type graphs so an internal transport type can never acquire or bypass a user customization.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_static_provider_prefix_normalization_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool semantic_run{
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_input_argument_v<
			 char_type, Args>)};
	if constexpr (
		sizeof...(Args) < 3u ||
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, ::std::remove_cvref_t<Args>...> ||
		::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype,
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_normalized_t<
					char_type, semantic_run, Args>...> ||
		!(false || ... ||
		  ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<
				  char_type, Args>))
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t count{
			::fast_io::operations::decay::
				print_static_provider_prefix_count<
					char_type,
					print_static_provider_prefix_normalization_max_components + 1u,
					Args...>()};
		if constexpr (
			count < 2u ||
			print_static_provider_prefix_normalization_max_components < count ||
			count == sizeof...(Args))
		{
			return false;
		}
		else
		{
			using source_tuple = ::std::tuple<Args...>;
			using provider = typename ::fast_io::operations::decay::
				print_static_provider_prefix_provider<
					char_type, source_tuple,
					::std::make_index_sequence<count>>::type;
			using prefix_type = ::fast_io::manipulators::
				static_scatter_t<char_type, provider::size>;
			constexpr ::std::size_t compact_prefix_limit{
				64u / sizeof(char_type)};
			return provider::size != 0u && compact_prefix_limit != 0u &&
				   provider::size <= compact_prefix_limit &&
				   ::fast_io::operations::decay::
					   print_static_provider_prefix_normalized_status_safe<
						   line, outputstmtype, char_type, semantic_run, count,
						   prefix_type, source_tuple,
						   ::std::make_index_sequence<
							   sizeof...(Args) - count>>::value;
		}
	}
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void print_static_provider_prefix_normalized_emit(
	outputstmtype &optstm, Args &...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t count{
		::fast_io::operations::decay::
			print_static_provider_prefix_count<
				char_type,
				print_static_provider_prefix_normalization_max_components + 1u,
				Args...>()};
	auto sources{::fast_io::containers::forward_as_tuple(args...)};
	::fast_io::operations::decay::
		print_static_provider_prefix_normalized_emit_impl<count, line>(
			optstm, sources,
			::std::make_index_sequence<sizeof...(Args) - count>{});
}

template <::std::integral char_type, typename T>
inline constexpr bool print_static_provider_mixed_static_component_v{
	::fast_io::operations::decay::print_static_provider_merged_component<
		char_type, ::std::remove_cvref_t<T>>::available &&
	// A syntax literal may use its provider in an all-literal record, but it
	// does not carry the language-level static identity required to pull a
	// mixed dynamic record into this eager descriptor plan. The ordinary core
	// scatter/compact dispatcher handles that case with substantially less
	// fixed work; explicit static_arg nodes remain eligible here.
	!::fast_io::manipulators::syntax_transport_details::
		is_static_provider_scatter_node_v<T>};

/// @brief Proves that historical source normalization yields one retained character descriptor.
/// @details This proof is intentionally local to the provider/scatter endpoint plan. Adding a direct scatter CPO to an
///          alias-only source would change ordinary put-area and reserve dispatch throughout the library. Instead this
///          predicate models the established `io_print_alias` then `io_print_forward` sequence on the same named lvalue
///          later observed by the emitter. The independent source markers prove descriptor lifetime, destination-state
///          independence, complete print equivalence, and safety after the small source view is copied by normalization.
///          A potentially throwing projection is rejected so descriptor construction cannot split one logical record.
///          The dependent query lives in a named immediate function rather than an immediate lambda inside the variable
///          template. Clang 17 rejects the latter as a non-constant initializer; the named form produces byte-identical
///          GCC 16 and Clang 23 executables while retaining the same type-only proof.
template <::std::integral char_type, typename T>
[[nodiscard]] consteval bool
print_static_provider_mixed_alias_scatter_component_query()
{
	using value_type = ::std::remove_cvref_t<T>;
	using source_reference = ::std::remove_reference_t<T> &;
	if constexpr (
		requires(source_reference value) {
			{
				::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(value))
			} -> ::std::same_as<::fast_io::basic_io_scatter_t<char_type>>;
		} &&
		::fast_io::borrowed_scatter_source<char_type, value_type> &&
		::fast_io::scatter_output_state_independent<char_type, value_type> &&
		::fast_io::scatter_direct_print_equivalent<char_type, value_type> &&
		::fast_io::copy_stable_borrowed_print_source<char_type, value_type>)
	{
#if defined(__HERBCEPTIONS__)
		return requires(source_reference value) {
			// The endpoint invokes the complete alias-and-forward expression on this named source lvalue; both
			// failure systems must classify that same expression as empty before its descriptor is retained.
			requires(!throws((::fast_io::io_print_forward<char_type>(
				::fast_io::io_print_alias(value)))));
			{
				::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(value))
			} noexcept -> ::std::same_as<::fast_io::basic_io_scatter_t<char_type>>;
		};
#else
		return noexcept(::fast_io::io_print_forward<char_type>(
			::fast_io::io_print_alias(::std::declval<source_reference>())));
#endif
	}
	else
	{
		return false;
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_static_provider_mixed_alias_scatter_component_v =
	print_static_provider_mixed_alias_scatter_component_query<char_type, T>();

/// @brief Classifies the dynamic half of a mixed immutable-provider endpoint plan.
/// @details Keeping this dependent proof in a named immediate function shares the Clang 17 compatibility rationale of
///          the alias-scatter query above; it does not alter any source capability or destination strategy.
template <::std::integral char_type, typename T>
[[nodiscard]] consteval bool
print_static_provider_mixed_dynamic_component_query()
{
	using value_type = ::std::remove_cvref_t<T>;
	using source_reference = ::std::remove_reference_t<T> &;
	if constexpr (
		::std::same_as<value_type, ::fast_io::basic_io_scatter_t<char_type>> ||
		::fast_io::details::decay::print_static_scatter_traits<
			char_type, value_type>::available)
	{
		return true;
	}
	else if constexpr (
		::fast_io::scatter_printable_for<char_type, source_reference> &&
		::fast_io::borrowed_scatter_source<char_type, value_type> &&
		::fast_io::scatter_output_state_independent<char_type, value_type> &&
		::fast_io::scatter_direct_print_equivalent<char_type, value_type> &&
		::fast_io::copy_stable_borrowed_print_source<char_type, value_type>)
	{
#if defined(__HERBCEPTIONS__)
		return requires(source_reference value) {
			// This is the exact named-lvalue scatter projection selected in the emitter's direct branch.
			// Descriptor retention requires `!throws(E) && noexcept(E)`, not either predicate alone.
			requires(!throws((print_scatter_define(
				::fast_io::io_reserve_type<char_type, value_type>, value))));
			{
				print_scatter_define(
					::fast_io::io_reserve_type<char_type, value_type>, value)
			} noexcept -> ::std::same_as<::fast_io::basic_io_scatter_t<char_type>>;
		};
#else
		return noexcept(print_scatter_define(
			::fast_io::io_reserve_type<char_type, value_type>,
			::std::declval<source_reference>()));
#endif
	}
	else if constexpr (
		::fast_io::operations::decay::
			print_static_provider_mixed_alias_scatter_component_v<
				char_type, T>)
	{
		return true;
	}
	else
	{
		return false;
	}
}

template <::std::integral char_type, typename T>
inline constexpr bool print_static_provider_mixed_dynamic_component_v =
	!::fast_io::operations::decay::
		print_static_provider_mixed_static_component_v<char_type, T> &&
	print_static_provider_mixed_dynamic_component_query<char_type, T>();

template <::std::integral char_type, typename T>
[[nodiscard]] inline constexpr ::fast_io::basic_io_scatter_t<char_type>
print_static_provider_mixed_dynamic_scatter(T &value) noexcept
{
	using value_type = ::std::remove_cvref_t<T>;
	using source_reference = T &;
	if constexpr (::std::same_as<
					  value_type, ::fast_io::basic_io_scatter_t<char_type>>)
	{
		return value;
	}
	else
	{
		if constexpr (::fast_io::details::decay::print_static_scatter_traits<
						  char_type, value_type>::available)
		{
			return ::fast_io::details::decay::print_static_scatter_traits<
				char_type, value_type>::define(value);
		}
		else
		{
			static_assert(::fast_io::operations::decay::
							  print_static_provider_mixed_dynamic_component_v<
								  char_type, source_reference>);
			if constexpr (::fast_io::scatter_printable_for<
							  char_type, source_reference>)
			{
				return print_scatter_define(
					::fast_io::io_reserve_type<char_type, value_type>, value);
			}
			else
			{
				static_assert(::fast_io::operations::decay::
								  print_static_provider_mixed_alias_scatter_component_v<
									  char_type, source_reference>);
				// Invoke both historical normalization CPOs exactly once. Their result is
				// retained only until this synchronous mixed-plan write completes.
				return ::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(value));
			}
		}
	}
}

/// Appends one maximal nonempty provider run to the physical descriptor plan.
///
/// A singleton retains its original provider pointer. Multiple adjacent nodes
/// use the same IO-owned merged provider as an all-static record. The caller
/// splits before the shared provider-object cap, so this helper can never form
/// an oversized COMDAT merely because a dynamic scatter follows the run.
template <::std::integral char_type, typename... component_types>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_static_provider_mixed_flush(
	::fast_io::basic_io_scatter_t<char_type> *iter,
	::fast_io::operations::decay::print_static_provider_type_pack<
		component_types...>) noexcept
{
	if constexpr (sizeof...(component_types) == 0u)
	{
		return iter;
	}
	else
	{
		constexpr ::std::size_t run_size{
			::fast_io::operations::decay::
				print_static_provider_merged_run_size<
					false, char_type, component_types...>()};
		constexpr ::std::size_t nonempty_count{
			::fast_io::operations::decay::
				print_static_provider_nonempty_component_count<
					char_type, component_types...>()};
		static_assert(run_size != SIZE_MAX);
		static_assert(
			run_size <= ::fast_io::manipulators::
							static_provider_materialization_code_unit_limit);
		if constexpr (run_size == 0u)
		{
			return iter;
		}
		else if constexpr (nonempty_count == 1u)
		{
			*iter++ = ::fast_io::operations::decay::
				print_static_provider_single_component_selector<
					char_type, component_types...>::define();
		}
		else
		{
			using provider = ::fast_io::operations::decay::
				print_static_provider_merged_run_provider<
					false, char_type, component_types...>;
			*iter++ = {provider::storage.data(), provider::size};
		}
		return iter;
	}
}

template <::std::size_t index, ::std::integral char_type,
		  typename source_tuple, typename... component_types>
inline constexpr ::fast_io::basic_io_scatter_t<char_type> *
print_static_provider_mixed_build(
	source_tuple &sources,
	::fast_io::basic_io_scatter_t<char_type> *iter,
	::fast_io::operations::decay::print_static_provider_type_pack<
		component_types...>
		run)
{
	constexpr ::std::size_t source_count{
		::std::tuple_size_v<::std::remove_cvref_t<source_tuple>>};
	if constexpr (index == source_count)
	{
		return ::fast_io::operations::decay::
			print_static_provider_mixed_flush<char_type>(iter, run);
	}
	else
	{
		auto &current{::fast_io::containers::get<index>(sources)};
		using current_type = ::std::remove_cvref_t<decltype(current)>;
		if constexpr (::fast_io::operations::decay::
						  print_static_provider_mixed_static_component_v<
							  char_type, current_type>)
		{
			// The complete-run helper uses SIZE_MAX for an empty argument pack to
			// reject an empty public record. Here an empty pack is the valid initial
			// state of the greedy builder and therefore has accumulated size zero.
			constexpr ::std::size_t run_size{[]() consteval {
				if constexpr (sizeof...(component_types) == 0u)
				{
					return 0u;
				}
				else
				{
					return ::fast_io::operations::decay::
						print_static_provider_merged_run_size<
							false, char_type, component_types...>();
				}
			}()};
			constexpr ::std::size_t current_size{
				::fast_io::operations::decay::
					print_static_provider_merged_component<
						char_type, current_type>::size};
			constexpr ::std::size_t limit{
				::fast_io::manipulators::
					static_provider_materialization_code_unit_limit};
			static_assert(run_size != SIZE_MAX && current_size <= limit);
			if constexpr (run_size <= limit - current_size)
			{
				return ::fast_io::operations::decay::
					print_static_provider_mixed_build<index + 1u, char_type>(
						sources, iter,
						::fast_io::operations::decay::
							print_static_provider_type_pack<
								component_types..., current_type>{});
			}
			else
			{
				iter = ::fast_io::operations::decay::
					print_static_provider_mixed_flush<char_type>(iter, run);
				return ::fast_io::operations::decay::
					print_static_provider_mixed_build<index + 1u, char_type>(
						sources, iter,
						::fast_io::operations::decay::
							print_static_provider_type_pack<current_type>{});
			}
		}
		else
		{
			static_assert(::fast_io::operations::decay::
							  print_static_provider_mixed_dynamic_component_v<
								  char_type, current_type>);
			iter = ::fast_io::operations::decay::
				print_static_provider_mixed_flush<char_type>(iter, run);
			auto const scatter{
				::fast_io::operations::decay::
					print_static_provider_mixed_dynamic_scatter<
						char_type>(current)};
			if (scatter.len != 0u)
			{
				*iter++ = scatter;
			}
			return ::fast_io::operations::decay::
				print_static_provider_mixed_build<index + 1u, char_type>(
					sources, iter,
					::fast_io::operations::decay::
						print_static_provider_type_pack<>{});
		}
	}
}

/// Selects physical provider-run coalescing only after semantic status dispatch
/// has declined ownership of the original typed operation pack.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_static_provider_mixed_run_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool semantic_run{
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_input_argument_v<
			 char_type, Args>)};
	if constexpr (semantic_run)
	{
		// A semantic node can change the active operation pack before status
		// dispatch. The mixed physical plan has no proof for that transformation
		// and must remain outside this pre-normalization shortcut.
		return false;
	}
	else
	{
		// Historical source normalization applies alias and character-aware
		// forwarding before exact status dispatch. Both the public source pack and
		// that normalized pack must decline ownership before provider/scatter
		// substitution is equivalent to the ordinary dispatcher.
		return sizeof...(Args) != 0u &&
			   !::fast_io::operations::decay::defines::
				   has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
			   !::fast_io::operations::decay::defines::has_obuffer_basic_operations<
				   outputstmtype> &&
			   (::fast_io::details::decay::
					print_output_retains_static_scatter<outputstmtype> ||
				::fast_io::details::decay::
					print_output_accepts_static_provider_scalar<outputstmtype>) &&
			   ((::fast_io::operations::decay::
					 print_static_provider_mixed_static_component_v<
						 char_type, Args> ||
				 ::fast_io::operations::decay::
					 print_static_provider_mixed_dynamic_component_v<
						 char_type, Args>) &&
				...) &&
			   (false || ... ||
				::fast_io::operations::decay::
					print_static_provider_mixed_static_component_v<char_type, Args>) &&
			   (false || ... ||
				::fast_io::operations::decay::
					print_static_provider_mixed_dynamic_component_v<char_type, Args>) &&
			   !::fast_io::operations::decay::defines::has_status_print_define<
				   line, outputstmtype, ::std::remove_cvref_t<Args>...> &&
			   !::fast_io::operations::decay::defines::has_status_print_define<
				   line, outputstmtype,
				   ::fast_io::operations::decay::
					   print_compiler_constant_pre_normalization_normalized_t<
						   char_type, false, Args>...>;
	}
}

template <bool line, typename outputstmtype, typename... Args>
inline constexpr void print_static_provider_mixed_run_emit(
	outputstmtype &optstm, Args &...args)
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr ::std::size_t capacity{
		sizeof...(Args) + static_cast<::std::size_t>(line)};
	::fast_io::basic_io_scatter_t<char_type> scatters[capacity];
	auto sources{::fast_io::containers::forward_as_tuple(args...)};
	auto iter{::fast_io::operations::decay::
				  print_static_provider_mixed_build<0u, char_type>(
					  sources, scatters,
					  ::fast_io::operations::decay::
						  print_static_provider_type_pack<>{})};
	if constexpr (line)
	{
		*iter++ = ::fast_io::details::decay::line_scatter_common<char_type>;
	}
	auto const emitted{static_cast<::std::size_t>(iter - scatters)};
	if (emitted == 0u)
	{
		return;
	}
	if constexpr (::fast_io::details::decay::
					  print_output_accepts_static_provider_scalar<outputstmtype>)
	{
		// A scatter-only endpoint has no scalar write protocol. Keep that invalid
		// operation outside its instantiation graph rather than asking an ordinary
		// run-time condition to discard the already-formed CPO body.
		if (emitted == 1u)
		{
			auto const [base, len]{scatters[0u]};
			::fast_io::details::decay::print_write_all_direct(
				optstm, base, base + len);
			return;
		}
	}
	if constexpr (::fast_io::details::decay::
					  print_uses_byte_scatter_representation<outputstmtype>)
	{
		::fast_io::io_scatter_t byte_scatters[capacity];
		for (::std::size_t index{}; index != emitted; ++index)
		{
			byte_scatters[index] = {
				scatters[index].base,
				::fast_io::details::intrinsics::mul_or_overflow_die(
					scatters[index].len, sizeof(char_type))};
		}
		::fast_io::operations::decay::scatter_write_all_bytes_decay_dispatch(
			optstm, byte_scatters, emitted);
	}
	else
	{
		::fast_io::operations::decay::scatter_write_all_decay_dispatch(
			optstm, scatters, emitted);
	}
}

template <bool line, ::std::integral char_type, typename outputstmtype,
		  typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_flat_materialized_available() noexcept
{
	// Cursor access alone does not authorize speculative direct materialization.
	// Keep the exact obuffer safety/nothrow proof in this availability predicate,
	// before the materialized helper (whose static assertions document that
	// contract) can be instantiated.  This is especially important after width
	// and precision values become compiler-constant proxies: their reserve
	// protocol must not accidentally opt an otherwise unmarked buffer in.
	if constexpr (
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> &&
		(!::fast_io::compiler_constant_obuffer_materialization_safe<
			 char_type, outputstmtype> ||
		 !::fast_io::details::decay::print_buffered_mixed_nothrow_put_area<
			 outputstmtype, char_type>))
	{
		return false;
	}
	else if constexpr (!(::fast_io::reserve_printable<
							 char_type, ::std::remove_cvref_t<Args>> &&
						 ...))
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t total{[]() consteval {
			::std::size_t value{static_cast<::std::size_t>(line)};
			((value = ::fast_io::details::decay::
				  print_contiguous_char_extent_add_or_unavailable<char_type>(
					  value,
					  print_reserve_size(::fast_io::io_reserve_type<
										 char_type, ::std::remove_cvref_t<Args>>))),
			 ...);
			return value;
		}()};
		return total != SIZE_MAX &&
			   total <= ::fast_io::details::compiler_constant_materialization_max_bytes /
							sizeof(char_type) &&
			   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
				   total, char_type>;
	}
}

/// @brief Selects exact compact output after the source gate has already produced a compiler-constant proxy.
/// @details This is a type-only continuation predicate: ordinary run-time width/floating leaves never instantiate the
///          path. It covers a precise replacement whose conservative reserve maximum prevents the older fixed-reserve
///          coalescer from fitting beside a format literal, while retaining direct-write, mutex, and put-area policy.
template <typename outputstmtype, ::std::integral char_type, typename... Args>
inline consteval bool
print_compiler_constant_exact_direct_materialized_available() noexcept
{
#if defined(__GNUC__) && !defined(__clang__)
	return sizeof...(Args) != 0u && sizeof...(Args) <= 16u &&
		   !::fast_io::operations::decay::defines::
			   has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		   !::fast_io::operations::decay::defines::
			   has_obuffer_basic_operations<outputstmtype> &&
		   ::fast_io::details::decay::
			   print_has_preferred_direct_write_operations<outputstmtype> &&
		   (::fast_io::precise_reserve_printable<
				char_type, ::std::remove_cvref_t<Args>> &&
			...) &&
		   (false || ... ||
			::fast_io::compiler_constant_precise_compact_preferred<
				char_type, ::std::remove_cvref_t<Args>>);
#else
	return false;
#endif
}

/// @brief Final true-arm continuation after core static fragments have been flattened.
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_compiler_constant_pre_normalization_flat_continuation
{
	outputstmtype &optstm;

	template <typename... Args>
		requires(sizeof...(Args) == 0u)
	inline constexpr void operator()(Args &&...) const
	{
		// Semantic replacement can reduce a non-empty source graph to an empty
		// active record. No flat materialization proof exists for that record;
		// re-enter the normalized dispatcher so exact status, mutex, and line
		// ownership are resolved before any physical output strategy.
		::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm);
	}

	template <typename... Args>
		requires(sizeof...(Args) != 0u)
// GCC 11--16 reconstructs the pre-normalization graph when this closure is
// left ordinary inline (GCC 16 grows by 21.5 KiB in isolation). Clang 17--23
// gains nothing here, so only the future-open measured GCC range is forced.
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr void operator()(Args &&...args) const
	{
		if constexpr (
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_flat_materialized_available<
					line, char_type, outputstmtype, Args...>())
		{
			if (::fast_io::operations::decay::
					print_freestanding_decay_compiler_constant_materialized<line>(
						optstm, ::std::forward<Args>(args)...))
			{
				return;
			}
		}
#if defined(__clang__)
		constexpr ::std::size_t clang_direct_capacity{
			::fast_io::operations::decay::
				print_compiler_constant_clang_direct_capacity<
					line, char_type, outputstmtype, Args...>()};
		if constexpr (clang_direct_capacity != SIZE_MAX)
		{
			::fast_io::operations::decay::
				print_compiler_constant_clang_direct_emit<
					clang_direct_capacity, line, char_type>(optstm, args...);
			return;
		}
#endif
		if constexpr (
			::fast_io::operations::decay::
				print_compiler_constant_exact_direct_materialized_available<
					outputstmtype, char_type, Args...>())
		{
			if (::fast_io::operations::decay::
					print_compiler_constant_exact_direct_flat_try<
						line, char_type>(optstm, args...))
			{
				return;
			}
		}
		// This fallback is reachable only for a buffered capacity miss or a semantic graph which is not a flat reserve
		// run. The replacement CPO promises equivalent spelling, and every parameter remains alive in the synchronous
		// expansion frame, so the established dispatcher is still the complete continuation.
		::fast_io::operations::decay::print_freestanding_decay_impl<line>(
			optstm, args...);
	}
};

/// @brief Completes a source-free record before entering the compiler-constant true-arm strategy graph.
/// @details A zero-source record has no value whose constancy can authorize materialization. Keeping this as a
///          structurally separate overload makes every static-provider, fragment, and stack-materialization branch
///          uninstantiable for that record. The normalized dispatcher then applies the exact zero-argument status CPO,
///          mutex ordering, and line policy shared by every other print-level entrance.
template <bool line, typename outputstmtype, typename... Args>
	requires(sizeof...(Args) == 0u)
inline constexpr void
print_compiler_constant_pre_normalization_true_emit(
	outputstmtype &optstm, Args &&...)
{
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(sizeof...(Args) != 0u)
// This true-arm boundary needs forcing on every measured GCC (11--16) and on
// Clang 21--23; Clang 17--20 is neutral without it. The lower bounds are the
// observed reversals and the positive endpoints remain future-open.
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_pre_normalization_true_emit(
		outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_single_run_available<
				line, outputstmtype, Args &&...>())
	{
		// An explicit single static argument already owns the final rodata range.
		// Emit that provider slice before the small compiler-constant stack path.
		::fast_io::operations::decay::
			print_static_provider_single_run_emit<
				line, outputstmtype, Args &&...>(optstm);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_merged_run_available<
				line, outputstmtype, Args &&...>())
	{
		// Every byte is already owned by one type-level provider.  Bypass the
		// generic multi-fragment policy so two explicit static values remain one
		// scalar write rather than a descriptor array or an automatic copy.
		::fast_io::operations::decay::
			print_static_provider_merged_run_emit<
				line, outputstmtype, Args &&...>(optstm);
		return;
	}
	constexpr bool preserve_static_fragments{
		::fast_io::details::decay::
			print_output_retains_static_scatter<outputstmtype> &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>};
	constexpr bool has_candidate{(false || ... ||
								  ::fast_io::operations::decay::
									  print_compiler_constant_pre_normalization_candidate_v<
										  char_type, Args &&>)};
	constexpr ::std::size_t eager_static_run_size{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_run_size<
				line, char_type, Args &&...>()};
	if constexpr (
		has_candidate && eager_static_run_size != SIZE_MAX &&
		eager_static_run_size <= 64u / sizeof(char_type) &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>)
	{
		// The whole record is compiler-proven constant and already bounded by the
		// shared 64-byte policy. Materialize its compact representation in one
		// automatic array and issue one contiguous write; only the earlier explicit
		// static-argument provider owns read-only storage. Entering this emitter
		// before fragment selection is required for GCC 13: at -O3 the later proxy
		// loses constant propagation and expands the full floating DA graph.
		// Dynamic values fail the outer builtin-constant gate, while buffered,
		// locked, partial-static, and larger scatter records retain their established
		// dispatcher and writev policy.
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_emit<line>(
				optstm, ::std::forward<Args>(args)...);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_run_available<
				line, outputstmtype, Args &&...>())
	{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_emit<line>(
				optstm, ::std::forward<Args>(args)...);
		return;
	}
	constexpr ::std::size_t static_run_size{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_run_size<
				line, char_type, Args &&...>()};
	if constexpr (
		static_run_size != SIZE_MAX &&
		static_run_size <=
			::fast_io::details::compiler_constant_materialization_max_bytes /
				sizeof(char_type) &&
		!::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> &&
		!::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>)
	{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_static_emit<line>(
				optstm, ::std::forward<Args>(args)...);
		return;
	}
	constexpr bool semantic_run{(false || ... ||
								 ::fast_io::details::decay::print_semantic_input_argument_v<
									 char_type,
									 ::fast_io::operations::decay::
										 print_compiler_constant_pre_normalization_replacement_t<
											 char_type, Args &&>>)};
	if constexpr (semantic_run)
	{
		::fast_io::details::decay::print_semantic_pack_expand<true, char_type>(
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_flat_continuation<
					line, char_type, outputstmtype>{optstm},
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_semantic_true_forward<
					preserve_static_fragments, char_type>(
					::std::forward<Args>(args))...);
	}
	else
	{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_flat_continuation<
				line, char_type, outputstmtype>{optstm}(
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_plain_true_forward<
						preserve_static_fragments, char_type>(
						::std::forward<Args>(args))...);
	}
}

/// @brief Models one leaf after compiler-constant substitution at the active-record boundary.
/// @details Unlike the public pre-normalization model, a non-candidate is already an IO-normalized leaf and therefore
///          keeps its exact type and reference.  Reapplying `io_print_alias` here would be a second observable CPO call.
template <::std::integral char_type, typename T,
		  bool = ::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_candidate_v<char_type, T>>
struct print_compiler_constant_active_record_replacement
{
	using type = ::std::remove_reference_t<T>;
	inline static constexpr ::std::size_t proxy_bytes{};
};

template <::std::integral char_type, typename T>
struct print_compiler_constant_active_record_replacement<char_type, T, true>
{
	using type = ::fast_io::details::compiler_constant_materialized_t<
		char_type, ::std::remove_cvref_t<T>>;
	inline static constexpr ::std::size_t proxy_bytes{sizeof(type)};
};

template <::std::integral char_type, typename T>
using print_compiler_constant_active_record_replacement_t =
	typename ::fast_io::operations::decay::
		print_compiler_constant_active_record_replacement<char_type, T>::type;

/// @brief Proves that replacing candidates in one selected active record cannot change operation ownership.
/// @details Condition selection has already removed inactive leaves, so source status is tested on the exact current
///          types rather than on the earlier semantic nodes.  Replacement status is tested independently.  Mutex
///          outputs are rejected because this continuation does not own lock acquisition.  The aggregate proxy budget
///          bounds only newly created state; passive active leaves remain borrowed by the unchanged dispatcher.
template <::std::integral char_type, typename... Args>
inline consteval bool
print_compiler_constant_active_record_codegen_supported() noexcept
{
#if (defined(__GNUC__) && !defined(__clang__) && 11 <= __GNUC__) || \
	(defined(__clang__) && 13 <= __clang_major__)
	// Recursive assembly audits found retained proxy formatters in GCC 15--17 active floating records and in Clang
	// active records through 23. The newest tested versions still fail, so the open compiler intervals remain query-free.
	// The parameters intentionally remain part of the signature: a future positive proof may reopen only a precisely
	// classified record without changing the consumer boundary.
	(void)sizeof(char_type);
	(void)sizeof...(Args);
	return false;
#else
	(void)sizeof(char_type);
	(void)sizeof...(Args);
	return true;
#endif
}

template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_active_record_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	constexpr bool has_candidate{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_has_candidate_v<
				char_type, Args...>};
	if constexpr (
		!has_candidate ||
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_execution_node_v<Args>) ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype, ::std::remove_reference_t<Args>...>)
	{
		return false;
	}
	else if constexpr (
		!::fast_io::operations::decay::
			print_compiler_constant_active_record_codegen_supported<
				char_type, Args...>())
	{
		// This rejection precedes every active-record replacement type and optimizer query.
		return false;
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> &&
		!::fast_io::operations::decay::
			print_compiler_constant_exact_obuffer_codegen_supported<
				char_type, Args...>())
	{
		// Reject the complete selected record before forming any replacement type or executing its optimizer query.
		return false;
	}
	else if constexpr (
		(false || ... ||
		 ::fast_io::details::decay::print_semantic_execution_node_v<
			 ::fast_io::operations::decay::
				 print_compiler_constant_active_record_replacement_t<
					 char_type, Args>>) ||
		::fast_io::operations::decay::defines::has_status_print_define<
			line, outputstmtype,
			::fast_io::operations::decay::
				print_compiler_constant_active_record_replacement_t<
					char_type, Args>...>)
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t maximum{
			::fast_io::details::compiler_constant_materialization_max_bytes};
		::std::size_t proxy_bytes{};
		((proxy_bytes =
			  proxy_bytes == SIZE_MAX ||
					  ::fast_io::operations::decay::
							  print_compiler_constant_active_record_replacement<
								  char_type, Args>::proxy_bytes > maximum - proxy_bytes
				  ? SIZE_MAX
				  : proxy_bytes +
						::fast_io::operations::decay::
							print_compiler_constant_active_record_replacement<
								char_type, Args>::proxy_bytes),
		 ...);
		return proxy_bytes != SIZE_MAX && proxy_bytes <= maximum;
	}
}

/// @brief Proves the descriptor-only true arm for an exact active record.
/// @details The public fragment predicate models another alias pass and is consequently too strong here.  This version
///          retains its destination, source-lifetime, descriptor-count, and stack-budget obligations while relying on
///          the exact source/replacement status proof above.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_active_record_fragment_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		(!::fast_io::details::decay::
			 print_output_retains_static_scatter<outputstmtype> &&
		 !::fast_io::details::decay::
			 print_output_accepts_static_provider_scalar<outputstmtype>) ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype> ||
		!(::fast_io::operations::decay::
			  print_compiler_constant_pre_normalization_fragment_source<
				  char_type, Args>::available &&
		  ...) ||
		(false || ... ||
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_fragment_source<
				 char_type, Args>::nested_candidate))
	{
		return false;
	}
	else
	{
		constexpr ::std::size_t count{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_run_count<
					line, outputstmtype, Args...>()};
		return count != 0u && count != SIZE_MAX && count <= 256u &&
			   ::fast_io::details::decay::print_stack_buffer_size_within_limit<
				   count, ::fast_io::basic_io_scatter_t<char_type>>;
	}
}

/// @brief Materializes only a proven candidate and otherwise preserves the selected lvalue.
template <::std::integral char_type, typename T>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr decltype(auto)
	print_compiler_constant_active_record_materialize_one(T &value) noexcept
{
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_candidate_v<char_type, T>)
	{
		return print_compiler_constant_materialize_gate_proven(
			::fast_io::io_reserve_type<char_type, ::std::remove_cvref_t<T>>,
			value);
	}
	else
	{
		return (value);
	}
}

/// @brief Proves that every selected active leaf has a bounded exact spelling after a successful value query.
/// @details The eligibility-implies-compact marker is a value-level contract: once the source query returns true, the
///          replacement's precise size is at most the common compiler-constant budget.  Requiring the precise compact
///          protocol independently proves that the replacement can measure and write exactly that many code units.
///          This conjunction is what makes a put-area capacity miss an output-transport decision rather than permission
///          to re-enter the native formatter.  Passive siblings are deliberately excluded until they provide an
///          equivalent aggregate bound; the current single-condition entrance selects exactly one candidate leaf.
template <::std::integral char_type, typename T>
inline consteval bool
print_compiler_constant_active_record_exact_leaf_available() noexcept
{
	using source_type = ::std::remove_cvref_t<T>;
	using replacement_type =
		::fast_io::operations::decay::
			print_compiler_constant_active_record_replacement_t<char_type, T>;
	return ::fast_io::operations::decay::
			   print_compiler_constant_pre_normalization_candidate_v<char_type, T> &&
		   ::fast_io::compiler_constant_precise_compact_preferred<
			   char_type, replacement_type> &&
		   requires {
			   {
				   print_compiler_constant_eligible_implies_compact_size(
					   ::fast_io::io_reserve_type<char_type, source_type>)
			   } -> ::std::same_as<::std::true_type>;
		   };
}

/// @brief Admits the definitive exact emitter for a selected active put-area record.
/// @details Active-record status equivalence is proved by the caller.  This predicate adds the physical obligations:
///          a real put area, at most sixteen exact components, and a successful-query compact bound for every leaf.
///          Mutex outputs remain excluded by the active-record proof, so staging a capacity miss through `write_all`
///          preserves the same lock and publication boundary as the established buffered dispatcher.
template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_active_record_exact_obuffer_available() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	return sizeof...(Args) != 0u && sizeof...(Args) <= 16u &&
		   ::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			   outputstmtype> &&
		   ::fast_io::operations::decay::
			   print_compiler_constant_active_record_available<
				   line, outputstmtype, Args...>() &&
		   (::fast_io::operations::decay::
				print_compiler_constant_active_record_exact_leaf_available<
					char_type, Args>() &&
			...);
}

/// @brief Materializes and emits one gate-proven exact active record without a formatter fallback.
/// @details Every argument arrives here only after its optimizer query succeeded.  The availability proof guarantees
///          that exact sizing cannot exceed the bounded tier, while `print_compiler_constant_exact_obuffer_flat_try`
///          handles both a fitting put area and a capacity miss using the same already-materialized spelling.  A false
///          result would violate one of those type/value contracts and is therefore terminated instead of silently
///          invoking the native formatter after the compiler-constant branch has been selected.
template <bool line, ::std::integral char_type, typename outputstmtype>
struct print_compiler_constant_active_record_exact_obuffer_continuation
{
	outputstmtype &optstm;

	template <typename... MaterializedArgs>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
	FAST_IO_GNU_ALWAYS_INLINE
#endif
		inline constexpr void operator()(MaterializedArgs &&...materialized_args) const
	{
		if (!::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_flat_try<line, char_type>(
					optstm, materialized_args...)) [[unlikely]]
		{
			::fast_io::fast_terminate();
		}
	}
};

/// @brief Tries compiler-constant materialization on an already-selected active record.
/// @details A true query emits from materialized proxies and therefore cannot retain the native formatter.  A false
///          query returns without constructing a proxy, after which the caller invokes the byte-for-byte historical
///          semantic run-time continuation.  Static and fragment strategies are IO policies; format lowering is not
///          named here and contributes no policy decision.
template <bool line, typename outputstmtype, typename... Args>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	[[nodiscard]] inline constexpr bool
	print_semantic_try_compiler_constant_active_record(
		outputstmtype &optstm, Args &...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_active_record_available<
				line, outputstmtype, Args...>())
	{
		if (::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_gate<char_type>(
					args...))
		{
			if constexpr (
				::fast_io::operations::decay::
					print_compiler_constant_active_record_exact_obuffer_available<
						line, outputstmtype, Args...>())
			{
				::fast_io::operations::decay::
					print_compiler_constant_active_record_exact_obuffer_continuation<
						line, char_type, outputstmtype>{optstm}(
						::fast_io::operations::decay::
							print_compiler_constant_active_record_materialize_one<
								char_type>(args)...);
				return true;
			}
			constexpr ::std::size_t static_run_size{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_static_run_size<
						line, char_type, Args...>()};
			if constexpr (
				static_run_size != SIZE_MAX &&
				static_run_size <= 64u / sizeof(char_type) &&
				!::fast_io::operations::decay::defines::
					has_obuffer_basic_operations<outputstmtype>)
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_static_emit<line>(
						optstm, args...);
				return true;
			}
			if constexpr (
				::fast_io::operations::decay::
					print_compiler_constant_active_record_fragment_available<
						line, outputstmtype, Args...>())
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_fragment_emit<line>(
						optstm, args...);
				return true;
			}
			if constexpr (
				static_run_size != SIZE_MAX &&
				static_run_size <=
					::fast_io::details::compiler_constant_materialization_max_bytes /
						sizeof(char_type) &&
				!::fast_io::operations::decay::defines::
					has_obuffer_basic_operations<outputstmtype>)
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_static_emit<line>(
						optstm, args...);
				return true;
			}
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_flat_continuation<
					line, char_type, outputstmtype>{optstm}(
					::fast_io::operations::decay::
						print_compiler_constant_active_record_materialize_one<
							char_type>(args)...);
			return true;
		}
	}
	return false;
}

/// @brief Proves the narrow source shape whose active condition arm can be selected before the general semantic graph.
/// @details The complete source record contains exactly one condition, the output owns neither mutex nor source-record
///          status, and both arms normalize to one ordinary active leaf.  These obligations make early selection
///          observationally identical to the established condition continuation.  Nested conditions, packs, nulls,
///          volatile storage, and ownership customizations fail closed and retain the general semantic dispatcher.
template <bool line, typename outputstmtype, typename T>
inline consteval bool
print_compiler_constant_single_condition_active_record_available_one() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::std::is_volatile_v<::std::remove_reference_t<T>> ||
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>)
	{
		return false;
	}
	else
	{
		using normalized_result = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::declval<T>()));
		using normalized_expression = ::std::add_lvalue_reference_t<
			::std::remove_reference_t<normalized_result>>;
		if constexpr (
			!::fast_io::details::decay::
				print_semantic_top_level_condition_v<normalized_expression> ||
			::fast_io::details::decay::
				print_freestanding_effective_status_owner<
					line, outputstmtype,
					::std::remove_reference_t<normalized_expression>>())
		{
			return false;
		}
		else
		{
			using node_reference = decltype(::fast_io::details::decay::print_semantic_node_ref(
				::std::declval<normalized_expression>()));
			using named_node_reference = ::std::add_lvalue_reference_t<
				::std::remove_reference_t<node_reference>>;
			using first_arm_reference = decltype((::std::declval<named_node_reference>().t1));
			using second_arm_reference = decltype((::std::declval<named_node_reference>().t2));
			using first_active =
				::fast_io::details::decay::print_semantic_stable_input_forward_t<
					char_type, first_arm_reference>;
			using second_active =
				::fast_io::details::decay::print_semantic_stable_input_forward_t<
					char_type, second_arm_reference>;
			return !::fast_io::details::decay::
					   print_semantic_execution_node_v<first_active> &&
				   !::fast_io::details::decay::
					   print_semantic_execution_node_v<second_active> &&
				   !::std::same_as<::std::remove_cvref_t<first_active>,
								   ::fast_io::io_null_t> &&
				   !::std::same_as<::std::remove_cvref_t<second_active>,
								   ::fast_io::io_null_t>;
		}
	}
}

template <bool line, typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_single_condition_active_record_available() noexcept
{
	if constexpr (sizeof...(Args) != 1u)
	{
		return false;
	}
	else
	{
		return (false || ... ||
				::fast_io::operations::decay::
					print_compiler_constant_single_condition_active_record_available_one<
						line, outputstmtype, Args>());
	}
}

/// @brief Applies the compiler code-generation partition to one source-level active-condition leaf.
/// @details Clang 13--17 compile the dynamic-star floating protocol but cannot erase its mutually exclusive proxy and
///          native graphs: the recursive audit finds up to eleven proxy functions and twenty-nine native formatter
///          functions, including proxy residue in the run-time-precision root. Clang 18--20 are already rejected by the
///          general source-candidate proof, while the independent active-record proof currently rejects all Clang
///          releases through 23. This earlier source-shape partition remains necessary for a structurally query-free
///          condition continuation and for future narrowly reopened active records. It runs before `materialized_t` or
///          any value query, so an unsupported complete condition record cannot construct a replacement.
template <::std::integral char_type, typename T>
inline consteval bool
print_compiler_constant_active_condition_leaf_codegen_supported() noexcept
{
	if constexpr (!::fast_io::operations::decay::
					  print_compiler_constant_active_record_codegen_supported<
						  char_type, T>())
	{
		// A compiler which rejects the selected active record must choose the source-level runtime branch before that
		// record's availability proof, replacement type, or optimizer query can be instantiated.
		return false;
	}
#if defined(__clang__) && __clang_major__ < 18
	else
	{
		return !::fast_io::compiler_constant_dynamic_precision_floating_source_shape<
			char_type, T>;
	}
#else
	else
	{
		return true;
	}
#endif
}

/// @brief Emits one already-selected ordinary condition arm through the active-record constant gate.
/// @details Semantic input forwarding is executed exactly once.  A rejected value query constructs no replacement and
///          continues with the same named active leaf in the historical stable-reference dispatcher, so neither alias
///          nor status CPOs are replayed.
template <bool line, typename outputstmtype, typename Branch>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_single_condition_active_branch(
		outputstmtype &optstm, Branch &&branch)
{
	using char_type = typename outputstmtype::output_char_type;
	decltype(auto) active{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::forward<Branch>(branch))};
	if (::fast_io::operations::decay::
			print_semantic_try_compiler_constant_active_record<line>(
				optstm, active))
	{
		return;
	}
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, active);
}

/// @brief Emits one selected condition arm without entering the compiler-constant strategy graph.
/// @details The source-level consumer selects this continuation only when its compiler proof rejects the complete
///          dynamic-precision condition. Forwarding and active-arm selection remain identical to the optimized branch,
///          but no query, proxy type, or materializer is named here.
template <bool line, typename outputstmtype, typename Branch>
inline constexpr void
print_compiler_constant_single_condition_runtime_branch(
	outputstmtype &optstm, Branch &&branch)
{
	using char_type = typename outputstmtype::output_char_type;
	decltype(auto) active{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::forward<Branch>(branch))};
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(
		optstm, active);
}

/// @brief Normalizes one source condition, selects its active arm, and enters the IO-level value gate.
/// @details This is intentionally not a format strategy: brace and printf lowering merely produce the same semantic
///          condition node accepted by raw IO.  Output policy, status exclusion, materialization, and the run-time
///          continuation all remain in core IO. The type-only code-generation proof covers both alternatives before
///          selection: rejecting only the explicit-precision arm would let a negative star precision lose its source
///          identity and re-enter the scalar constant gate through the omitted-precision arm.
template <bool line, typename outputstmtype, typename T>
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_single_condition_active_record(
		outputstmtype &optstm, T &&source)
{
	using char_type = typename outputstmtype::output_char_type;
	static_assert(
		::fast_io::operations::decay::
			print_compiler_constant_single_condition_active_record_available_one<
				line, outputstmtype, T>());
	decltype(auto) normalized_condition{
		::fast_io::details::decay::print_semantic_input_forward<char_type>(
			::std::forward<T>(source))};
	auto &&node_ref{
		::fast_io::details::decay::print_semantic_node_ref(normalized_condition)};
	using node_reference = decltype(node_ref);
	using named_node_reference = ::std::add_lvalue_reference_t<
		::std::remove_reference_t<node_reference>>;
	using first_arm_reference = decltype((::std::declval<named_node_reference>().t1));
	using second_arm_reference = decltype((::std::declval<named_node_reference>().t2));
	using first_active =
		::fast_io::details::decay::print_semantic_stable_input_forward_t<
			char_type, first_arm_reference>;
	using second_active =
		::fast_io::details::decay::print_semantic_stable_input_forward_t<
			char_type, second_arm_reference>;
	constexpr bool active_condition_codegen_supported{
		::fast_io::operations::decay::
			print_compiler_constant_active_condition_leaf_codegen_supported<
				char_type, first_active>() &&
		::fast_io::operations::decay::
			print_compiler_constant_active_condition_leaf_codegen_supported<
				char_type, second_active>()};
	if constexpr (active_condition_codegen_supported)
	{
		if (node_ref.pred)
		{
			::fast_io::operations::decay::
				print_compiler_constant_single_condition_active_branch<line>(
					optstm, node_ref.t1);
		}
		else
		{
			::fast_io::operations::decay::
				print_compiler_constant_single_condition_active_branch<line>(
					optstm, node_ref.t2);
		}
	}
	else
	{
		if (node_ref.pred)
		{
			::fast_io::operations::decay::
				print_compiler_constant_single_condition_runtime_branch<line>(
					optstm, node_ref.t1);
		}
		else
		{
			::fast_io::operations::decay::
				print_compiler_constant_single_condition_runtime_branch<line>(
					optstm, node_ref.t2);
		}
	}
}

/// @brief Identifies a source graph whose active print record is not known before semantic normalization.
/// @details Providers may use this predicate only as a fail-closed strategy partition. A true value proves that source
///          types are insufficient for whole-record status selection because pack expansion or condition selection can
///          change the operation arity and types. It does not authorize emitting or inspecting any source value.
template <typename outputstmtype, typename... Args>
inline constexpr bool print_pre_normalization_semantic_source_run_v{
	(false || ... ||
	 ::fast_io::details::decay::print_semantic_input_argument_v<
		 typename outputstmtype::output_char_type, Args &&>)};

/// @brief Identifies a compiler-constant replacement run which can change the active record before output dispatch.
/// @details This predicate proves only active-record stability for the non-semantic true-arm overload. It deliberately
///          does not claim that any physical compiler-constant strategy is available; the caller owns that independent
///          proof. Both ordinary semantic nodes and recursively parameter-wrapped packs are rejected through the shared
///          execution predicate used by `print_semantic_input_argument_v`.
template <typename outputstmtype, typename... Args>
inline constexpr bool print_pre_normalization_semantic_replacement_run_v{
	(false || ... ||
	 ::fast_io::details::decay::print_semantic_input_argument_v<
		 typename outputstmtype::output_char_type,
		 ::fast_io::operations::decay::
			 print_compiler_constant_pre_normalization_replacement_t<
				 typename outputstmtype::output_char_type, Args &&>>)};

/// @brief Enters a proven compiler-constant true arm only after acquiring any complete output mutex.
/// @details The optimizer query is deliberately evaluated by the caller before this continuation.  Source
///          materialization remains inside it: for a mutex wrapper the original source objects cross the lock boundary,
///          the unlocked observer is obtained under the guard, and only then are replacement proxies formed and
///          emitted. This avoids constructing a constant byte proxy on the caller's stack merely to reload it into a
///          buffered destination after locking. Only marker-free outputs retain the direct true arm; a partial mutex
///          protocol is rejected instead of being silently treated as an unlocked terminal.
template <bool line, typename outputstmtype, typename... Args>
	requires(sizeof...(Args) == 0u)
inline constexpr void
print_compiler_constant_pre_normalization_true_emit_after_lock(
	outputstmtype &optstm, Args &&...)
{
	// A zero-source call has no compiler-constant candidate. Keeping it as a
	// separate overload makes the non-empty materialization graph uninstantiable.
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...>)
inline constexpr void
print_compiler_constant_pre_normalization_true_emit_after_lock(
	outputstmtype &optstm, Args &&...args)
{
	// A source-level semantic graph does not identify its active record. Pack
	// expansion and condition selection may remove or replace operation types,
	// so no pre-normalization shortcut can prove exact status equivalence. This
	// structurally separate overload prevents those shortcuts from being
	// instantiated and transfers ownership directly to semantic normalization.
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_replacement_run_v<
				outputstmtype, Args...>)
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_compiler_constant_pre_normalization_true_emit_after_lock(
		outputstmtype &optstm, Args &&...args)
{
	if constexpr (
		::fast_io::operations::decay::defines::
			has_output_or_io_stream_mutex_ref_define<outputstmtype>)
	{
		if constexpr (
			::fast_io::operations::decay::defines::
				has_complete_output_stream_mutex_protocol<outputstmtype>)
		{
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_true_emit<line>(
					unlocked, ::std::forward<Args>(args)...);
		}
		else
		{
			// A mutex marker is an ordering requirement, never permission to use the output as an unlocked terminal.
			static_assert(
				::fast_io::operations::decay::defines::
					has_complete_output_stream_mutex_protocol<outputstmtype>,
				"fast_io: compiler-constant print requires a complete output mutex protocol");
		}
	}
	else
	{
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_true_emit<line>(
				optstm, ::std::forward<Args>(args)...);
	}
}

/// @brief Applies the destination-specific code-generation proof at the shared print-level source boundary.
/// @details This predicate intentionally precedes every value query in both hot and diagnostic entry families. A
///          complete mutex wrapper is classified through its unlocked destination because that is where physical output
///          occurs. On a rejected put-area/compiler pair, the overload set selects a structurally separate historical
///          continuation; the rejected function body therefore cannot instantiate a replacement or builtin query.
/// @brief Admits the narrow Clang mixed-record materialization escape hatch.
///
/// Clang deliberately keeps the general multi-source compiler-constant gate
/// query-free: replacing a run-time scalar beside a literal used to retain a
/// large formatter graph on its fallback edge.  A byte sink which exposes only
/// synchronous scatter-some output is a distinct strategy, however.  For a
/// tiny bounded record we can materialize the complete spelling into the same
/// caller-owned scratch range used by the established constant arm and issue
/// one scalar write; no provider pointer is passed to the sink and no scatter
/// CPO is needed.  Keep the admission tied to this exact capability/size shape
/// so ordinary POSIX, buffered, status, and larger mixed records retain the
/// conservative Clang gate.
template <typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_clang_small_byte_fragment_run_available() noexcept
{
#if defined(__clang__) && 21 <= __clang_major__
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		sizeof...(Args) <= 1u ||
		!::fast_io::details::decay::print_output_retains_static_scatter<outputstmtype> ||
		!::fast_io::details::decay::print_has_preferred_direct_write_operations<outputstmtype> ||
		::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<outputstmtype> ||
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<outputstmtype>)
	{
		return false;
	}
	else
	{
		constexpr bool byte_scatter_shape{
			::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype> &&
			!::fast_io::operations::decay::defines::has_scatter_write_all_bytes_overflow_define<outputstmtype> &&
			!::fast_io::operations::decay::defines::has_write_all_bytes_overflow_define<outputstmtype> &&
			::fast_io::operations::decay::defines::has_scatter_write_some_bytes_overflow_define<outputstmtype>};
		constexpr bool typed_scatter_shape{
			!::fast_io::details::decay::print_uses_byte_scatter_representation<outputstmtype> &&
			!::fast_io::operations::decay::defines::has_scatter_write_all_overflow_define<outputstmtype> &&
			!::fast_io::operations::decay::defines::has_write_all_overflow_define<outputstmtype> &&
			::fast_io::operations::decay::defines::has_scatter_write_some_overflow_define<outputstmtype>};
		if constexpr (!(byte_scatter_shape || typed_scatter_shape))
		{
			return false;
		}
		constexpr ::std::size_t bound{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_static_run_size<
					false, char_type, Args...>()};
		return bound != SIZE_MAX && bound <= 16u;
	}
#else
	(void)sizeof...(Args);
	return false;
#endif
}

template <typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_codegen_supported() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
#if defined(__clang__) && 21 <= __clang_major__
	if constexpr (
		sizeof...(Args) != 1u &&
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_has_candidate_v<
				char_type, Args...> &&
		!::fast_io::operations::decay::
			print_compiler_constant_clang_small_byte_fragment_run_available<
				outputstmtype, Args...>())
	{
		// Clang 21--23 reduce a single raw scalar to immediate stores, but a mixed literal/scalar record retains the
		// replacement formatter on an ordinary put-area miss edge. A fixed external view has no refill/growth edge and
		// additionally needs exact physical writes: the generic reserve formatter may legally touch its complete upper
		// bound beyond the returned logical cursor. Admit only that stronger destination proof so the exact compiler-
		// constant writer preserves the untouched suffix; buffered owners and direct devices retain the established
		// query-free policy.
		if constexpr (
			::fast_io::operations::decay::defines::has_obuffer_basic_operations<
				outputstmtype> &&
			::fast_io::direct_obuffer_copy_safe<char_type, outputstmtype>)
		{
			return ::fast_io::operations::decay::
				print_compiler_constant_exact_obuffer_codegen_supported<
					char_type, Args...>();
		}
		else
		{
			return false;
		}
	}
	else
#endif
		if constexpr (
			::fast_io::operations::decay::defines::
				has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		return ::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_codegen_supported<
				unlocked_output, Args...>();
	}
	else if constexpr (
		::fast_io::operations::decay::defines::has_obuffer_basic_operations<
			outputstmtype>)
	{
		return ::fast_io::operations::decay::
			print_compiler_constant_exact_obuffer_codegen_supported<
				char_type, Args...>();
	}
	else
	{
#if defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11
		if constexpr (
			(false || ... ||
			 ::fast_io::compiler_constant_dynamic_precision_floating_source_shape<
				 char_type, Args>))
		{
			// Recursive assembly audits on GCC 11 through the current trunk find
			// 6.2--7.9k reachable instructions from the precise-fragment planner
			// after a successful direct-output query. Forcing its preparation links
			// does not erase that graph, so the shared raw/format print level rejects
			// the source shape before replacement formation or __builtin_constant_p.
			// Constant and unknown values consequently share the mature native
			// precision formatter, with no half-selected proxy path.
			return false;
		}
#endif
		return true;
	}
}

/// @brief Early compiler-constant gate shared by public raw print and format's already-lowered component callback.
/// @details The false arm is exactly the historical source-normalization entry. Replacement proxies and their
///          materializers occur only in the proven true arm; an unknown source therefore retains the old frame, ABI,
///          alias calls, and dispatcher. Non-candidate arguments are forwarded through either arm without reconstruction.
template <bool line, typename outputstmtype, typename... Args>
	requires(sizeof...(Args) == 0u)
inline constexpr void
print_freestanding_compiler_constant_pre_normalization(
	outputstmtype &optstm, Args &&...)
{
	// Output-reference normalization has already completed. The normalized
	// print level now owns status, mutex, and optional newline semantics.
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		::fast_io::operations::decay::
			print_compiler_constant_single_condition_active_record_available<
				line, outputstmtype, Args...>())
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 13)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_freestanding_compiler_constant_pre_normalization(
		outputstmtype &optstm, Args &&...args)
{
	// The availability proof establishes exactly one source.  The fold avoids a
	// tuple or another by-value ownership boundary between that expression and
	// the builtin query.
	(::fast_io::operations::decay::
		 print_compiler_constant_single_condition_active_record<line>(
			 optstm, ::std::forward<Args>(args)),
	 ...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		!::fast_io::operations::decay::
			print_compiler_constant_single_condition_active_record_available<
				line, outputstmtype, Args...>())
inline constexpr void
print_freestanding_compiler_constant_pre_normalization(
	outputstmtype &optstm, Args &&...args)
{
	// Keep semantic sources outside the compiler-constant strategy graph. The
	// normalized dispatcher will select exact status or physical output from the
	// active record after pack expansion and condition selection have completed.
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		!::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_codegen_supported<
				outputstmtype, Args && ...>())
inline constexpr void
print_freestanding_compiler_constant_pre_normalization(
	outputstmtype &optstm, Args &&...args)
{
	// This overload is the query-free arm selected by the compiler/destination proof. Keeping a separate body makes
	// replacement formation structurally impossible instead of relying on dead-code elimination after a failed query.
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_codegen_supported<
				outputstmtype, Args && ...>())
#if (defined(__GNUC__) && !defined(__clang__) && __GNUC__ >= 11) || \
	(defined(__clang__) && __clang_major__ >= 21)
FAST_IO_GNU_ALWAYS_INLINE
#endif
	inline constexpr void
	print_freestanding_compiler_constant_pre_normalization(
		outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_static_provider_component_run_available<
					line, unlocked_output, Args &&...>())
		{
			// Mutex unwrapping precedes status discovery in the public dispatcher;
			// an outer-wrapper status overload is therefore not callable here. Lock
			// first, then select the immutable provider on the unlocked sink.
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_static_provider_component_run_emit<
					line, unlocked_output, Args &&...>(unlocked);
			return;
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_component_run_available<
				line, outputstmtype, Args &&...>())
	{
		// A complete provider run is one IO record regardless of its fragment
		// count. Core owns the merged array and emits one scalar direct write.
		::fast_io::operations::decay::
			print_static_provider_component_run_emit<
				line, outputstmtype, Args &&...>(optstm);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_static_provider_mixed_run_available<
					line, unlocked_output, Args &&...>())
		{
			// Match normal mutex precedence: outer status is unreachable, while an
			// unlocked status owner was already rejected by the availability gate.
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_static_provider_mixed_run_emit<line>(
					unlocked, args...);
			return;
		}
	}
	if constexpr (::fast_io::operations::decay::
					  print_static_provider_mixed_run_available<
						  line, outputstmtype, Args &&...>())
	{
		::fast_io::operations::decay::
			print_static_provider_mixed_run_emit<line>(optstm, args...);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_prefix_normalization_available<
				line, outputstmtype, Args &&...>())
	{
		::fast_io::operations::decay::
			print_static_provider_prefix_normalized_emit<line>(
				optstm, args...);
		return;
	}
	constexpr bool has_candidate{(false || ... ||
								  ::fast_io::operations::decay::
									  print_compiler_constant_pre_normalization_candidate_v<
										  char_type, Args &&>)};
	if constexpr (
		!has_candidate &&
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_run_available<
					line, unlocked_output, Args &&...>())
		{
			// Mutex precedence remains unchanged: acquire the wrapper lock before
			// testing any unlocked output shortcut.  A literal/static-only run then
			// keeps its provider pointer through the same synchronous direct branch
			// available on the unlocked observer.
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_emit<line>(
					unlocked, ::std::forward<Args>(args)...);
			return;
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_run_available<
				line, outputstmtype, Args &&...>())
	{
		// A literal/static-scatter-only run has no value query. A candidate run
		// first proves only compact proxy storage: its immutable-fragment spelling
		// has an independently bounded descriptor array and must not inherit the
		// much larger contiguous reserve capacity used by buffered destinations.
		if constexpr (
			!has_candidate ||
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_common_available<
					false, line, false, outputstmtype, Args &&...>())
		{
			if constexpr (!has_candidate)
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_fragment_emit<line>(
						optstm, ::std::forward<Args>(args)...);
				return;
			}
			else if (::fast_io::operations::decay::
						 print_compiler_constant_pre_normalization_gate<char_type>(
							 args...))
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_true_emit_after_lock<line>(
						optstm, ::std::forward<Args>(args)...);
				return;
			}
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_exact_obuffer_available<
				line, outputstmtype, Args &&...>())
	{
		if (::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_gate<char_type>(args...))
		{
			if (::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_exact_obuffer_try_after_lock<
						line>(optstm, ::std::forward<Args>(args)...))
			{
				return;
			}
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_available<
				line, outputstmtype, Args &&...>())
	{
		if (::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_gate<char_type>(args...))
		{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_true_emit_after_lock<line>(
					optstm,
					::std::forward<Args>(args)...);
			return;
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_partial_fragment_available<
				line, outputstmtype, Args &&...>())
	{
		auto sources{
			::fast_io::containers::forward_as_tuple(args...)};
		::fast_io::operations::decay::
			print_compiler_constant_partial_fragment_transform<
				line, char_type, 0u, false>(optstm, sources);
		return;
	}
	if constexpr (
#if defined(__clang__)
		// Clang outlines the general semantic dispatcher for a passive mixture of
		// immutable scatters and bounded reserve leaves. Keep this policy at the
		// IO source-normalization boundary: the type-only proof cannot evaluate a
		// source CPO, while the selected call below aliases every named source once
		// and retains each result through the complete synchronous put-area attempt.
		::fast_io::operations::decay::
			print_passive_mixed_put_area_fast_entry_available<
				line, outputstmtype,
				decltype(::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(
						::std::declval<::std::remove_reference_t<Args> &>())))...>()
#else
		false
#endif
	)
	{
		::fast_io::operations::decay::
			print_passive_mixed_put_area_fast_entry<line>(
				optstm,
				::fast_io::io_print_forward<char_type>(
					::fast_io::io_print_alias(args))...);
		return;
	}
	::fast_io::operations::decay::print_freestanding_decay_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

/// @brief Proves whether a diagnostic entry may instantiate a compiler-constant replacement graph.
/// @details Static-provider-only records retain their type-level cold optimizations. A record containing a value-query
///          candidate is rejected on the tested GNU frontends: GCC 15--17 retain a 120--152-byte floating proxy frame
///          and its materialization chain in public `perr`/`perrln`, and no newer tested GCC or Clang proves deletion.
///          The rejection occurs in overload constraints, before replacement formation or `__builtin_constant_p`.
template <typename outputstmtype, typename... Args>
inline consteval bool
print_compiler_constant_pre_normalization_cold_codegen_supported() noexcept
{
	using char_type = typename outputstmtype::output_char_type;
#if (defined(__GNUC__) && !defined(__clang__) && 11 <= __GNUC__) || \
	(defined(__clang__) && 13 <= __clang_major__)
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_has_candidate_v<
				char_type, Args...>)
	{
		return false;
	}
#endif
	return ::fast_io::operations::decay::
		print_compiler_constant_pre_normalization_codegen_supported<
			outputstmtype, Args...>();
}

/// @brief Diagnostic-output variant whose unknown arm retains the historical cold continuation.
/// @details The cold overload set shares the same source-normalization level as `perr`, `perrln`, panic, and their
///          internal diagnostic callers. Unsupported value-query records select a structurally query-free body; passive
///          static records may still use the established provider paths without acquiring a hot dispatcher clone.
template <bool line, typename outputstmtype, typename... Args>
	requires(sizeof...(Args) == 0u)
inline constexpr void
print_freestanding_compiler_constant_pre_normalization_cold(
	outputstmtype &optstm, Args &&...)
{
	// Cold placement is irrelevant to a source-free type graph. Reuse the same
	// normalized record dispatcher as every hot and borrowed ownership boundary.
	::fast_io::operations::decay::print_freestanding_decay_impl<line>(optstm);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...>)
inline constexpr void
print_freestanding_compiler_constant_pre_normalization_cold(
	outputstmtype &optstm, Args &&...args)
{
	// The cold front door has the same exact active-record obligation as the
	// hot path. Its separate overload makes the pre-normalization strategy graph
	// ill-formed by construction for semantic sources, then preserves diagnostic
	// placement through the established cold semantic continuation.
	::fast_io::operations::decay::print_freestanding_decay_cold_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		!::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_cold_codegen_supported<
				outputstmtype, Args && ...>())
inline constexpr void
print_freestanding_compiler_constant_pre_normalization_cold(
	outputstmtype &optstm, Args &&...args)
{
	// Diagnostic facades share the same query-free compiler/destination rejection as the hot print level.
	::fast_io::operations::decay::print_freestanding_decay_cold_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

template <bool line, typename outputstmtype, typename... Args>
	requires(
		sizeof...(Args) != 0u &&
		!::fast_io::operations::decay::
			print_pre_normalization_semantic_source_run_v<
				outputstmtype, Args...> &&
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_cold_codegen_supported<
				outputstmtype, Args && ...>())
inline constexpr void
print_freestanding_compiler_constant_pre_normalization_cold(
	outputstmtype &optstm, Args &&...args)
{
	using char_type = typename outputstmtype::output_char_type;
	if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_static_provider_component_run_available<
					line, unlocked_output, Args &&...>())
		{
			// Cold diagnostic APIs retain their placement and acquire the wrapper
			// lock before selecting the same IO-owned immutable record as print.
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_static_provider_component_run_emit<
					line, unlocked_output, Args &&...>(unlocked);
			return;
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_static_provider_component_run_available<
				line, outputstmtype, Args &&...>())
	{
		::fast_io::operations::decay::
			print_static_provider_component_run_emit<
				line, outputstmtype, Args &&...>(optstm);
		return;
	}
	if constexpr (
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_static_provider_mixed_run_available<
					line, unlocked_output, Args &&...>())
		{
			// Diagnostic entry points remain cold; only the selected physical
			// provider plan is shared, and lock/status ordering is unchanged.
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_static_provider_mixed_run_emit<line>(
					unlocked, args...);
			return;
		}
	}
	if constexpr (::fast_io::operations::decay::
					  print_static_provider_mixed_run_available<
						  line, outputstmtype, Args &&...>())
	{
		::fast_io::operations::decay::
			print_static_provider_mixed_run_emit<line>(optstm, args...);
		return;
	}
	constexpr bool has_candidate{(false || ... ||
								  ::fast_io::operations::decay::
									  print_compiler_constant_pre_normalization_candidate_v<
										  char_type, Args &&>)};
	if constexpr (
		!has_candidate &&
		::fast_io::operations::decay::defines::
			has_complete_output_stream_mutex_protocol<outputstmtype>)
	{
		using unlocked_output = ::std::remove_reference_t<decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
			::std::declval<outputstmtype &>()))>;
		if constexpr (
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_run_available<
					line, unlocked_output, Args &&...>())
		{
			::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
				::fast_io::operations::decay::output_stream_mutex_ref_decay(optstm)};
			decltype(auto) unlocked =
				::fast_io::operations::decay::output_stream_unlocked_ref_decay(optstm);
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_fragment_emit<line>(
					unlocked, ::std::forward<Args>(args)...);
			return;
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_fragment_run_available<
				line, outputstmtype, Args &&...>())
	{
		if constexpr (
			!has_candidate ||
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_common_available<
					false, line, false, outputstmtype, Args &&...>())
		{
			if constexpr (!has_candidate)
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_fragment_emit<line>(
						optstm, ::std::forward<Args>(args)...);
				return;
			}
			else if (::fast_io::operations::decay::
						 print_compiler_constant_pre_normalization_gate<char_type>(
							 args...))
			{
				::fast_io::operations::decay::
					print_compiler_constant_pre_normalization_true_emit_after_lock<line>(
						optstm, ::std::forward<Args>(args)...);
				return;
			}
		}
	}
	if constexpr (
		::fast_io::operations::decay::
			print_compiler_constant_pre_normalization_available<
				line, outputstmtype, Args &&...>())
	{
		if (::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_gate<char_type>(args...))
		{
			::fast_io::operations::decay::
				print_compiler_constant_pre_normalization_true_emit_after_lock<line>(
					optstm, ::std::forward<Args>(args)...);
			return;
		}
	}
	::fast_io::operations::decay::print_freestanding_decay_cold_unforwarded<line>(
		optstm, ::std::forward<Args>(args)...);
}

} // namespace decay

namespace decay::defines
{

/// @brief    Validates already-forwarded freestanding print parameters for one character type.
/// @tparam   char_type the output character type
/// @tparam   Args      the already-forwarded argument types
template <typename char_type, typename... Args>
concept print_freestanding_params_okay =
	::std::integral<char_type> &&
	(::fast_io::details::decay::print_freestanding_decay_param_okay_single<char_type, Args>::value && ...);

/// @brief    Validates already-decayed freestanding print arguments for one concrete output stream reference.
/// @details  Destination-independent formatting protocols keep their ordinary structural checks. Direct
///           `print_define` leaves, including leaves nested in semantic nodes, are instead checked against the stream
///           reached after mutex unwrapping. This closes the dummy-stream false-positive without changing the public
///           character-only `printable` compatibility concept.
/// @tparam   output the decayed output stream reference type
/// @tparam   Args   the already-forwarded argument types
template <typename output, typename... Args>
concept print_freestanding_okay =
	::std::integral<typename ::std::remove_cvref_t<output>::output_char_type> &&
	(::fast_io::details::decay::print_freestanding_output_param_okay_single<
		 ::std::remove_cvref_t<output>,
		 typename ::std::remove_cvref_t<output>::output_char_type, Args>::value &&
	 ...);

/// @brief Validates one already-forwarded operation, including line-specific whole-run status handling.
/// @details Unlike the compatibility concept above, this operation-level concept follows mutex unwrapping first and
///          then tests the exact status CPO for the complete argument run. It is the admission predicate used by
///          public print entry points because `print` and `println` can expose different status capabilities.
template <bool line, typename output, typename... Args>
concept print_freestanding_okay_for_line =
	::fast_io::details::decay::print_freestanding_output_run_okay<line, output, Args...>();

} // namespace decay::defines

namespace defines
{

template <typename output>
using print_freestanding_named_output_t = ::std::remove_reference_t<decltype(::fast_io::operations::output_stream_ref(
	::std::declval<::std::remove_reference_t<output> &>()))>;

/// @brief Lazily selects one source-normalization expression for a complete public print run.
/// @details Runtime makes one run-level decision before it normalizes any source. If any named source becomes a
///          semantic node, every source is passed through `print_semantic_input_forward`; otherwise every source uses
///          ordinary alias/status forwarding. Separate specializations are required because forming both expressions
///          would instantiate a CPO from a phase which execution never enters.
template <::std::integral char_type, bool semantic_run, typename Arg>
struct print_freestanding_named_source_arg;

template <::std::integral char_type, typename Arg>
struct print_freestanding_named_source_arg<char_type, false, Arg>
{
	using source_expression =
		::std::add_lvalue_reference_t<::std::remove_reference_t<Arg>>;
	using type = decltype(::fast_io::io_print_forward<char_type>(
		::fast_io::io_print_alias(::std::declval<source_expression>())));
};

template <::std::integral char_type, typename Arg>
struct print_freestanding_named_source_arg<char_type, true, Arg>
{
	using source_expression =
		::std::add_lvalue_reference_t<::std::remove_reference_t<Arg>>;
	using type = decltype(::fast_io::details::decay::print_semantic_input_forward<char_type>(
		::std::declval<source_expression>()));
};

/// @brief Carries the single normalized type sequence proved for all public print compatibility predicates.
/// @details The three projections below intentionally consume the same list. This makes it impossible for the
///          character-only, destination-aware, and line-aware concepts to disagree about the source-normalization
///          phase while still allowing each decayed predicate to prove its own operation contract.
template <typename... NormalizedArgs>
struct print_freestanding_named_normalized_run
{
	template <::std::integral char_type>
	inline static constexpr bool params_okay{
		::fast_io::operations::decay::defines::print_freestanding_params_okay<
			char_type, NormalizedArgs...>};

	template <typename output>
	inline static constexpr bool output_okay{
		::fast_io::operations::decay::defines::print_freestanding_okay<
			output, NormalizedArgs...>};

	template <bool line, typename output>
	inline static constexpr bool output_okay_for_line{
		::fast_io::operations::decay::defines::print_freestanding_okay_for_line<
			line, output, NormalizedArgs...>};
};

template <::std::integral char_type, typename... Args>
inline constexpr bool print_freestanding_named_semantic_run_v{
	(false || ... ||
	 ::fast_io::details::decay::print_semantic_input_argument_v<
		 char_type,
		 ::std::add_lvalue_reference_t<::std::remove_reference_t<Args>>>)};

template <::std::integral char_type, typename... Args>
using print_freestanding_named_normalized_run_t =
	print_freestanding_named_normalized_run<
		typename print_freestanding_named_source_arg<
			char_type,
			print_freestanding_named_semantic_run_v<char_type, Args...>,
			Args>::type...>;

/// @brief    Validates public freestanding print parameters after the exact run-level source normalization.
/// @details  Every public/default-output wrapper owns forwarding-reference parameters, but its body passes the named
///           parameters as lvalues. The all-semantic-or-all-ordinary phase choice is identical to
///           `print_freestanding_decay_unforwarded`; a per-argument choice is not an equivalent proof.
/// @tparam   char_type the output character type
/// @tparam   Args      the original public argument types
template <typename char_type, typename... Args>
concept print_freestanding_params_okay =
	::std::integral<char_type> &&
	::fast_io::operations::defines::
		print_freestanding_named_normalized_run_t<char_type, Args...>::
			template params_okay<char_type>;

/// @brief    Validates public freestanding print arguments for an output stream object.
/// @details  This compatibility projection deliberately shares the source phase and named-lvalue model used by the
///           exact line-aware public admission predicate.
/// @tparam   output the public output stream object type
/// @tparam   Args   the original public argument types
template <typename output, typename... Args>
concept print_freestanding_okay =
	::std::integral<typename ::fast_io::operations::defines::
						print_freestanding_named_output_t<output>::output_char_type> &&
	::fast_io::operations::defines::print_freestanding_named_normalized_run_t<
		typename ::fast_io::operations::defines::
			print_freestanding_named_output_t<output>::output_char_type,
		Args...>::template output_okay<::fast_io::operations::defines::print_freestanding_named_output_t<output>>;

/// @brief Validates the exact public print or println operation after its named-lvalue normalization.
/// @details Function parameters are named lvalues at the actual call site even when the caller supplied an rvalue.
///          The run first selects one normalization phase for every source, then the resulting argument types are
///          checked as one line-specific active record. Runtime binds the output CPO result with `decltype(auto)`, so
///          its cv-qualification is likewise part of the executable expression.
template <bool line, typename output, typename... Args>
concept print_freestanding_okay_for_line =
	::std::integral<typename ::fast_io::operations::defines::
						print_freestanding_named_output_t<output>::output_char_type> &&
	::fast_io::operations::defines::print_freestanding_named_normalized_run_t<
		typename ::fast_io::operations::defines::
			print_freestanding_named_output_t<output>::output_char_type,
		Args...>::template output_okay_for_line<line,
												::fast_io::operations::defines::print_freestanding_named_output_t<output>>;

/// @brief Selects the measured public fixed-view integral-manipulator entry.
/// @details This is a code-generation policy layered on the target-independent semantic proof.  On Apple AArch64,
///          Clang 23 otherwise outlines the full pre-normalization continuation for a run which is already a closed
///          library scalar record.  The cost proof is deliberately limited to `char`, an owned at-most-64-bit value,
///          the exact plain non-decimal `base<B>` policy, and packs of at most 32 records; presentation variants remain
///          on the general dispatcher.  Paired M4 medians reduced base-36 pack1 from 3.09 ns to 1.81 ns, pack9 from
///          12.65 ns to 8.62 ns, and base-2 pack32 from 51.91 ns to 46.70 ns while also shrinking text.  Clang 23 is the
///          first verified endpoint, so later Clang versions inherit the narrow policy; other targets retain the
///          ordinary dispatcher until independently measured.
template <bool line, typename output, typename... Args>
inline consteval bool
print_freestanding_fixed_integral_manip_entry_available() noexcept
{
#if defined(__APPLE__) && (defined(__aarch64__) || defined(__arm64__)) && \
	defined(__clang__) && 23 <= __clang_major__
	using normalized_output =
		::fast_io::operations::defines::print_freestanding_named_output_t<output>;
	using char_type = typename normalized_output::output_char_type;
	return ::std::same_as<char_type, char> &&
		   ::fast_io::details::decay::
			   print_fixed_public_integral_manip_run_available<
				   line, normalized_output, char_type, Args...>();
#else
	return false;
#endif
}

} // namespace defines

/// @brief Emits a proved integral-manipulator run through the compact fixed-view public entry.
/// @details Output normalization and the success-only operation guard remain identical to the general entry.  Source
///          alias/forward normalization occurs exactly once inside `print_fixed_public_run`; the static reserve emitter
///          retains ordered writes, physical-overstore staging, line-feed placement, capacity failure, and one final
///          cursor publication.  The forced boundary applies only after the semantic and target cost proofs above.
template <bool line, typename output, typename... Args>
	requires(
		::fast_io::operations::defines::
			print_freestanding_fixed_integral_manip_entry_available<
				line, output, Args...>())
FAST_IO_GNU_ALWAYS_INLINE inline constexpr void
print_freestanding(output &&outstm, Args &&...args)
{
	::fast_io::operations::basic_output_operation_guard<output &&> guard{outstm};
	decltype(auto) outref = guard.ref();
	using normalized_output = ::std::remove_cvref_t<decltype(outref)>;
	using char_type = typename normalized_output::output_char_type;
	::fast_io::details::decay::print_fixed_public_run<
		line, normalized_output, char_type>(outref, args...);
	guard.commit();
}

/// @brief    Public freestanding print entry point.
/// @details  The wrapper obtains the stream reference, forwards semantic input arguments when present, and then
///           delegates to the decayed freestanding dispatcher. This broad policy boundary deliberately remains
///           ordinary inline. Forcing it does expose mixed integer/floating literals to GCC 11--16, but a 16-call-site
///           volatile probe makes GCC 15 optimize the complete dispatcher at every caller, increasing compile time by
///           more than one half and growing text. Constant discovery therefore needs a smaller caller-side gate with an
///           independently shared unknown-value continuation before this boundary can be forced safely.
/// @tparam   line   true when a trailing newline is appended
/// @tparam   output the output stream object type
/// @tparam   Args   the argument types in the print run
/// @param    outstm the output stream object
/// @param    args   the arguments to emit
template <bool line, typename output, typename... Args>
	requires(!::fast_io::operations::defines::
				 print_freestanding_fixed_integral_manip_entry_available<
					 line, output, Args...>())
inline constexpr void print_freestanding(output &&outstm, Args &&...args)
{
	// Keep the normalized observer alive for the complete print and finish an
	// explicitly opted-in temporary only after every argument has been emitted.
	// Lvalue adapters remain open so multiple print calls share one engine state.
	::fast_io::operations::basic_output_operation_guard<output &&> guard{outstm};
	decltype(auto) outref = guard.ref();
	// The source-normalization bridge is shared with nested formatters. Public entry owns output normalization; the
	// bridge owns exactly one source normalization, so recursive printing cannot accidentally normalize either twice.
	::fast_io::operations::decay::
		print_freestanding_compiler_constant_pre_normalization<line>(
			outref, ::std::forward<Args>(args)...);
	guard.commit();
}

} // namespace operations

} // namespace fast_io

namespace fast_io
{

namespace details::decay
{

/// @brief    Owns one string literal as structural compile-time storage.
/// @tparam   char_type the literal character type
/// @tparam   n         the literal array extent including its terminator
template <typename char_type, ::std::size_t n>
struct basic_compiled_scatter_literal_storage
{
	char_type elements[n];

	/// @brief Copies a string literal into the structural non-type template argument representation.
	/// @param literal the source literal including its terminator
	consteval basic_compiled_scatter_literal_storage(char_type const (&literal)[n]) noexcept
	{
		// Copy every code unit, including the terminator, so the object remains a structural value.
		for (::std::size_t i{}; i != n; ++i)
		{
			elements[i] = literal[i];
		}
	}
};

/// @brief Marks types that are valid components of a compiled scatter plan.
/// @tparam T the component type being classified
template <typename T>
struct compiled_scatter_component : ::std::false_type
{};

/// @brief Reports whether a type is a valid compiled scatter-plan component.
/// @tparam T the component type being classified
template <typename T>
inline constexpr bool compiled_scatter_component_v{
	::fast_io::details::decay::compiled_scatter_component<::std::remove_cvref_t<T>>::value};

} // namespace details::decay

namespace manipulators
{

/// @brief Represents one compile-time literal slot in a compiled scatter plan.
/// @details The literal is type-owned, excludes its terminating null from `size`, and needs no run-time patching.
/// @tparam literal the structural string-literal value
template <::fast_io::details::decay::basic_compiled_scatter_literal_storage literal>
struct compiled_scatter_literal_t
{
	using component_tag = void;
	using char_type = ::std::remove_cvref_t<decltype(literal.elements[0])>;
	static inline constexpr bool is_static{true};
	static inline constexpr ::std::size_t size{sizeof(literal.elements) / sizeof(char_type) - 1u};
	static inline constexpr auto storage
#if (defined(__GNUC__) || defined(__clang__)) && !defined(_WIN32)
		__attribute__((visibility("hidden")))
#endif
		{literal};
};

/// @brief Represents one runtime-patched slot in a compiled scatter plan.
/// @details `index_value` selects the corresponding dynamic argument descriptor; the component owns no payload.
/// @tparam index_value the zero-based runtime argument index used to fill the slot
template <::std::size_t index_value>
struct compiled_scatter_dynamic_t
{
	using component_tag = void;
	static inline constexpr bool is_static{false};
	static inline constexpr ::std::size_t index{index_value};
};

/// @brief Creates a compile-time literal component for make_scatter_plan.
/// @details The variable template is stateless and stores the structural literal in its component type.
/// @tparam literal the string literal to store in the plan blueprint
template <::fast_io::details::decay::basic_compiled_scatter_literal_storage literal>
inline constexpr compiled_scatter_literal_t<literal> scatter_literal{};

/// @brief Creates a runtime component for make_scatter_plan.
/// @details The variable template records only the zero-based dynamic argument index.
/// @tparam index the zero-based runtime argument index used to patch this slot
template <::std::size_t index>
inline constexpr compiled_scatter_dynamic_t<index> scatter_dynamic{};

} // namespace manipulators

namespace details::decay
{

template <::fast_io::details::decay::basic_compiled_scatter_literal_storage literal>
struct compiled_scatter_component<::fast_io::manipulators::compiled_scatter_literal_t<literal>> : ::std::true_type
{};

template <::std::size_t index>
struct compiled_scatter_component<::fast_io::manipulators::compiled_scatter_dynamic_t<index>> : ::std::true_type
{};

/// @brief Proves that a component belongs to a plan with the requested character domain.
/// @details Dynamic slots acquire their domain from the runtime source. A literal already owns typed storage, so its
///          code-unit type must agree here, at the public substitution boundary. Deferring this test to the descriptor
///          builder would make `requires` report an invalid mixed-domain plan as constructible and diagnose only after
///          the selected function body instantiated.
template <::std::integral char_type, typename component_type>
inline constexpr bool compiled_scatter_component_for_v = []() constexpr {
	using component = ::std::remove_cvref_t<component_type>;
	if constexpr (!::fast_io::details::decay::compiled_scatter_component_v<component>)
	{
		return false;
	}
	else if constexpr (component::is_static)
	{
		return ::std::same_as<char_type, typename component::char_type>;
	}
	else
	{
		return true;
	}
}();

/// @brief Reports whether one dynamic component observes a particular public argument index.
/// @details The `if constexpr` is material: literal components deliberately have no `index` member. Keeping the member
///          access in the selected branch makes the mask substitution-safe rather than relying on Boolean
///          short-circuiting to hide an ill-formed expression.
template <::std::size_t index, typename component_type>
inline constexpr bool compiled_scatter_component_uses_argument_v = []() constexpr {
	using component = ::std::remove_cvref_t<component_type>;
	if constexpr (component::is_static)
	{
		return false;
	}
	else
	{
		return component::index == index;
	}
}();

/// @brief Occupies an unreferenced positional slot without retaining or inspecting the caller's object.
/// @details Public arity remains `max(dynamic-index)+1`, but an index hole is a syntactic placeholder rather than a
///          printable source. Replacing it with an empty internal value is what makes the documented "unobserved"
///          guarantee true for binding as well as for direct emission.
struct compiled_scatter_plan_unused_argument
{};

/// @brief Materializes forwarded runtime values into an owning fast_io tuple.
/// @tparam Args the forwarded runtime value types
/// @param  args the values to store
/// @return a tuple that owns the decayed runtime values
template <typename... Args>
[[nodiscard]] inline constexpr ::fast_io::containers::tuple<::std::remove_cvref_t<Args>...>
compiled_scatter_plan_tuple(Args &&...args)
{
#if defined(__clang__)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wmissing-braces"
#endif
	return ::fast_io::containers::tuple<::std::remove_cvref_t<Args>...>{
		::std::forward<Args>(args)...};
#if defined(__clang__)
#pragma clang diagnostic pop
#endif
}

/// @brief Binds runtime values to a compiled plan for use as a normal printable object.
/// @tparam plan_type     the compiled scatter plan type
/// @tparam dynamic_types the retained pre-alias source transport types
template <typename plan_type, typename... dynamic_types>
struct compiled_scatter_plan_bound
{
	::fast_io::containers::tuple<dynamic_types...> dynamic_values;
};

/// @brief Models the character-aware normalization applied to one stable named compiled-plan source.
/// @details Public direct parameters become lvalues once named. A bound plan first owns or references its pre-alias
///          source and later exposes that stable source as either const& or the original lvalue reference. Both paths
///          perform aliasing and status forwarding through precisely this expression, so admission must not probe the
///          caller's earlier rvalue category instead.
template <::std::integral char_type, typename source_expression>
using compiled_scatter_plan_forwarded_expression_t = decltype(::fast_io::io_print_forward<char_type>(
	::fast_io::io_print_alias(::std::declval<source_expression>())));

template <typename char_type, typename source_expression>
concept compiled_scatter_plan_normalizable = ::std::integral<char_type> && requires {
	typename ::std::type_identity<
		::fast_io::details::decay::compiled_scatter_plan_forwarded_expression_t<
			char_type, source_expression>>::type;
};

/// @brief Proves that direct compiled-plan emission may retain one dynamic descriptor until the final scatter write.
/// @details A raw argument is preferred when its exact named-lvalue CPO is available and borrowed. Otherwise the same
///          alias/status forwarding used by the body may produce a safe transport object. In both cases the independent
///          marker is mandatory: scatter shape alone permits shared scratch that a later dynamic slot may overwrite.
template <::std::integral char_type, typename source_expression>
inline constexpr bool compiled_scatter_plan_direct_source_v = []() constexpr {
	if constexpr (::fast_io::details::decay::retained_scatter_printable_v<
					  char_type, source_expression>)
	{
		return true;
	}
	else if constexpr (
		::fast_io::details::decay::compiled_scatter_plan_normalizable<char_type, source_expression>)
	{
		using forwarded_expression =
			::fast_io::details::decay::compiled_scatter_plan_forwarded_expression_t<
				char_type, source_expression>;
		using named_forwarded_expression = ::std::remove_reference_t<forwarded_expression> &;
		return ::fast_io::details::decay::retained_scatter_printable_v<
			char_type, named_forwarded_expression>;
	}
	else
	{
		return false;
	}
}();

template <typename T>
struct compiled_scatter_plan_parameter_storage : ::std::false_type
{};

template <typename value_type>
struct compiled_scatter_plan_parameter_storage<::fast_io::parameter<value_type>> : ::std::true_type
{};

/// @brief Recovers the source expression retained by the pre-alias bound transport.
/// @details `parameter<T&>` preserves the original lvalue category even when the bound tuple is observed through
///          `const&`. An owned source is intentionally exposed as `const&`, because a composable bound plan may be
///          printed more than once and must not consume its backing object merely to recreate an alias.
template <typename storage_type>
[[nodiscard]] inline constexpr decltype(auto)
compiled_scatter_plan_bound_storage_source(storage_type const &storage) noexcept
{
	if constexpr (::fast_io::details::decay::compiled_scatter_plan_parameter_storage<
					  ::std::remove_cvref_t<storage_type>>::value)
	{
		return (storage.reference);
	}
	else
	{
		return (storage);
	}
}

/// @brief Proves that an already-owned bound slot can be normalized and retained for one synchronous emission.
template <::std::integral char_type, typename storage_expression>
inline constexpr bool compiled_scatter_plan_bound_storage_v = []() constexpr {
	using storage_type = ::std::remove_cvref_t<storage_expression>;
	if constexpr (!::std::is_object_v<storage_type> || !requires { sizeof(storage_type); })
	{
		return false;
	}
	else if constexpr (!requires {
						   ::fast_io::details::decay::compiled_scatter_plan_bound_storage_source(
							   ::std::declval<storage_expression>());
					   })
	{
		return false;
	}
	else
	{
		using source_expression = decltype(::fast_io::details::decay::compiled_scatter_plan_bound_storage_source(
			::std::declval<storage_expression>()));
		if constexpr (!::fast_io::details::decay::compiled_scatter_plan_normalizable<
						  char_type, source_expression>)
		{
			return false;
		}
		else
		{
			using forwarded_expression =
				::fast_io::details::decay::compiled_scatter_plan_forwarded_expression_t<
					char_type, source_expression>;
			using named_forwarded_expression = ::std::remove_reference_t<forwarded_expression> &;
			return ::fast_io::details::decay::retained_scatter_printable_v<
				char_type, named_forwarded_expression>;
		}
	}
}();

/// @brief Proves that binding owns the pre-alias source and can safely normalize it at each later emission.
/// @details Owning only an alias result is insufficient: an rvalue string-like source may return a bare scatter into
///          its own small-string buffer, after which the source temporary dies and the bound descriptor dangles. The
///          plan therefore applies only ABI-aware value/reference transport while binding, before alias discovery.
///          Rvalues become owned source objects; lvalues use the same compact value-or-reference policy as ordinary
///          print entry. Alias and status forwarding run later against that stable storage, and their result lives
///          through the complete synchronous scatter write.
template <::std::integral char_type, typename source_expression>
inline constexpr bool compiled_scatter_plan_bound_source_v = []() constexpr {
	if constexpr (!requires {
					  ::fast_io::details::io_print_forward_transport_for<char_type>(
						  ::std::declval<source_expression>());
				  })
	{
		return false;
	}
	else
	{
		using storage_expression = decltype(::fast_io::details::io_print_forward_transport_for<char_type>(
			::std::declval<source_expression>()));
		using storage_type = ::std::remove_cvref_t<storage_expression>;
		if constexpr (!::std::is_object_v<storage_type> || !requires { sizeof(storage_type); })
		{
			return false;
		}
		else
		{
			return ::std::constructible_from<storage_type, storage_expression> &&
				   ::fast_io::details::decay::compiled_scatter_plan_bound_storage_v<
					   char_type, storage_type const &>;
		}
	}
}();

/// @brief Proves that one component index lies within a supplied runtime argument extent.
/// @details Public arity is the greatest dynamic index plus one, and every dynamic component must address within that
///          extent. Components need not reference every earlier index: holes are legal and the corresponding supplied
///          arguments are unobserved. Exact public arity rejects fewer or more arguments relative to the greatest
///          index; it does not prove that every positional argument is used.
template <::std::size_t runtime_argument_count, typename component_type>
inline constexpr bool compiled_scatter_plan_component_index_in_range_v = []() constexpr {
	if constexpr (component_type::is_static)
	{
		return true;
	}
	else
	{
		return component_type::index < runtime_argument_count;
	}
}();

/// @brief Recognizes a tuple-like runtime-value container without instantiating `tuple_size_v` for unrelated types.
template <typename T>
concept compiled_scatter_plan_tuple_like = requires {
	::std::tuple_size<::std::remove_cvref_t<T>>::value;
};

template <typename T>
struct compiled_scatter_plan_fast_tuple : ::std::false_type
{};

template <typename... element_types>
struct compiled_scatter_plan_fast_tuple<::fast_io::containers::tuple<element_types...>> : ::std::true_type
{};

/// @brief Reads a fast_io tuple base by index while deducing its element type from the unique indexed base.
/// @details The C++20 implementation of the general tuple `get<I>` must recursively recover the Ith pack type. A large
///          compiled plan would repeat that search for every component and can reach the compiler's template-depth
///          limit. The tuple already has one `tuple_element_impl_<I, T>` base per element, so normal derived-to-base
///          deduction finds T directly without modifying the general-purpose container or its public ABI.
template <::std::size_t index, typename element_type>
[[nodiscard]] inline constexpr decltype(auto) compiled_scatter_plan_fast_tuple_value(
	::fast_io::containers::details::tuple_element_impl_<index, element_type> const &element) noexcept
{
	return (element.val_);
}

/// @brief Reads one runtime value with the exact cv/ref category exposed by the final container expression.
/// @details Admission and descriptor materialization deliberately share this accessor. A const tuple which contains a
///          reference may still expose a mutable lvalue; spelling either side independently risks accepting one CPO
///          category and invoking another after the function body has already been selected.
template <::std::size_t index, typename dynamic_tuple>
[[nodiscard]] inline constexpr decltype(auto)
compiled_scatter_plan_runtime_value_exact(dynamic_tuple const &values)
{
	if constexpr (::std::is_bounded_array_v<::std::remove_cvref_t<dynamic_tuple>>)
	{
		return (values[index]);
	}
	else if constexpr (::fast_io::details::decay::compiled_scatter_plan_fast_tuple<
						   ::std::remove_cvref_t<dynamic_tuple>>::value)
	{
		return ::fast_io::details::decay::compiled_scatter_plan_fast_tuple_value<index>(values);
	}
	else
	{
		return ::fast_io::containers::get<index>(values);
	}
}

/// @brief Proves that the exact runtime accessor has a valid in-range expression without a delayed body diagnostic.
template <::std::size_t index, typename dynamic_tuple>
inline constexpr bool compiled_scatter_plan_runtime_value_available_v = []() constexpr {
	// The accessor protocol intentionally has no volatile overload. Silently erasing that qualifier here would admit
	// the declaration and fail only when the selected function body tries to read the container.
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<dynamic_tuple>>)
	{
		return false;
	}
	using values_type = ::std::remove_cvref_t<dynamic_tuple>;
	if constexpr (::std::is_bounded_array_v<values_type>)
	{
		return index < ::std::extent_v<values_type>;
	}
	else if constexpr (!::fast_io::details::decay::compiled_scatter_plan_tuple_like<values_type>)
	{
		return false;
	}
	else if constexpr (index >= ::std::tuple_size<values_type>::value)
	{
		return false;
	}
	else if constexpr (
		::fast_io::details::decay::compiled_scatter_plan_fast_tuple<values_type>::value)
	{
		return true;
	}
	else
	{
		return requires(values_type const &values) {
			::fast_io::containers::get<index>(values);
		};
	}
}();

template <::std::size_t index, typename dynamic_tuple>
using compiled_scatter_plan_runtime_value_expression_t = decltype(::fast_io::details::decay::compiled_scatter_plan_runtime_value_exact<index>(
	::std::declval<::std::remove_reference_t<dynamic_tuple> const &>()));

/// @brief Proves the retained descriptor property for one runtime component in its final container expression.
template <::std::integral char_type, typename dynamic_tuple, typename component_type>
inline constexpr bool compiled_scatter_plan_runtime_component_v = []() constexpr {
	if constexpr (component_type::is_static)
	{
		return true;
	}
	else
	{
		if constexpr (!::fast_io::details::decay::compiled_scatter_plan_runtime_value_available_v<
						  component_type::index, dynamic_tuple>)
		{
			return false;
		}
		else
		{
			return ::fast_io::details::decay::retained_scatter_printable_v<
				char_type,
				::fast_io::details::decay::compiled_scatter_plan_runtime_value_expression_t<
					component_type::index, dynamic_tuple>>;
		}
	}
}();

/// @brief Proves that a runtime container exactly covers a plan's dynamic argument domain and retention requirements.
template <::std::integral char_type, ::std::size_t runtime_argument_count, typename dynamic_tuple,
		  typename... component_types>
inline constexpr bool compiled_scatter_plan_runtime_values_v = []() constexpr {
	// A volatile tuple/array is outside the runtime-value accessor protocol. Reject it at the proof boundary instead of
	// probing a fabricated non-volatile const view and producing a delayed body diagnostic in `emit`.
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<dynamic_tuple>>)
	{
		return false;
	}
	using values_type = ::std::remove_cvref_t<dynamic_tuple>;
	if constexpr (::std::is_bounded_array_v<values_type>)
	{
		return ::std::extent_v<values_type> == runtime_argument_count &&
			   (compiled_scatter_plan_runtime_component_v<char_type, values_type, component_types> && ...);
	}
	else if constexpr (::fast_io::details::decay::compiled_scatter_plan_tuple_like<values_type>)
	{
		return ::std::tuple_size<values_type>::value == runtime_argument_count &&
			   (compiled_scatter_plan_runtime_component_v<char_type, values_type, component_types> && ...);
	}
	else
	{
		return false;
	}
}();

/// @brief Proves one bound slot's deferred normalization through the exact const tuple expression used by emission.
template <::std::integral char_type, typename dynamic_tuple, typename component_type>
inline constexpr bool compiled_scatter_plan_bound_runtime_component_v = []() constexpr {
	if constexpr (component_type::is_static)
	{
		return true;
	}
	else
	{
		using values_type = ::std::remove_cvref_t<dynamic_tuple>;
		if constexpr (!::fast_io::details::decay::compiled_scatter_plan_tuple_like<values_type>)
		{
			return false;
		}
		else if constexpr (component_type::index >= ::std::tuple_size<values_type>::value)
		{
			return false;
		}
		else if constexpr (!::fast_io::details::decay::compiled_scatter_plan_runtime_value_available_v<
							   component_type::index, dynamic_tuple>)
		{
			return false;
		}
		else
		{
			using storage_expression =
				::fast_io::details::decay::compiled_scatter_plan_runtime_value_expression_t<
					component_type::index, dynamic_tuple>;
			return ::fast_io::details::decay::compiled_scatter_plan_bound_storage_v<
				char_type, storage_expression>;
		}
	}
}();

/// @brief Proves the exact owned-source tuple stored by a publicly constructible bound plan.
template <::std::integral char_type, ::std::size_t runtime_argument_count, typename dynamic_tuple,
		  typename... component_types>
inline constexpr bool compiled_scatter_plan_bound_runtime_values_v = []() constexpr {
	// Deferred bound emission uses the same const, non-volatile accessor contract as direct emission.
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<dynamic_tuple>>)
	{
		return false;
	}
	using values_type = ::std::remove_cvref_t<dynamic_tuple>;
	if constexpr (!::fast_io::details::decay::compiled_scatter_plan_tuple_like<values_type>)
	{
		return false;
	}
	else
	{
		return ::std::tuple_size<values_type>::value == runtime_argument_count &&
			   (compiled_scatter_plan_bound_runtime_component_v<
					char_type, values_type, component_types> &&
				...);
	}
}();

/// @brief Proves the final primitive output route used after compiled-plan descriptor materialization.
template <::std::integral char_type, typename output>
inline constexpr bool compiled_scatter_plan_final_output_v = []() constexpr {
	// The compiled-plan borrowed-output boundary deliberately does not model volatile observers. Preserve that fact in
	// admission rather than erasing the qualifier and allowing `emit_borrowed` to fail after overload selection.
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<output>>)
	{
		return false;
	}
	using output_type = ::std::remove_reference_t<output>;
	if constexpr (!requires { typename output_type::output_char_type; })
	{
		return false;
	}
	else
	{
		return ::std::same_as<char_type, typename output_type::output_char_type> &&
			   ::fast_io::operations::decay::defines::writable<output_type>;
	}
}();

/// @brief Proves the output route selected by `print`, including complete mutex unwrapping.
template <::std::integral char_type, typename output>
inline consteval bool compiled_scatter_plan_print_output() noexcept
{
	if constexpr (::std::is_volatile_v<::std::remove_reference_t<output>>)
	{
		return false;
	}
	// Mutex recursion retains the cv-qualification of each named unlocked result; strategy admission must do likewise.
	using output_type = ::std::remove_reference_t<output>;
	if constexpr (!requires { typename output_type::output_char_type; })
	{
		return false;
	}
	else if constexpr (::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<output_type>)
	{
		if constexpr (::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<output_type>)
		{
			// Preserve the unlocked CPO's exact cv/ref result. A volatile reference must be rejected by the recursive proof,
			// not rewritten into a different observer expression that the dispatcher never receives.
			using unlocked_output = decltype(::fast_io::operations::decay::output_stream_unlocked_ref_decay(
				::std::declval<output_type &>()));
			return ::fast_io::details::decay::compiled_scatter_plan_print_output<char_type, unlocked_output>();
		}
		else
		{
			return false;
		}
	}
	else
	{
		return ::fast_io::details::decay::compiled_scatter_plan_final_output_v<char_type, output>;
	}
}

/// @brief Proves that `print` can normalize its named output lvalue and reach one valid final scatter-write route.
template <::std::integral char_type, typename output>
inline constexpr bool compiled_scatter_plan_public_output_v = []() constexpr {
	using named_output = ::std::remove_reference_t<output> &;
	if constexpr (requires {
					  ::fast_io::operations::output_stream_ref(::std::declval<named_output>());
				  })
	{
		using output_ref = decltype(::fast_io::operations::output_stream_ref(::std::declval<named_output>()));
		return ::fast_io::details::decay::compiled_scatter_plan_print_output<char_type, output_ref>();
	}
	else
	{
		return false;
	}
}();

/// @brief Builds the compile-time descriptor contributed by one plan component.
/// @tparam char_type      the output character type
/// @tparam scatter_type   void for byte lengths, otherwise the typed scatter character type
/// @tparam component_type the static or dynamic plan component
/// @return the literal descriptor, or a zero descriptor reserved for a dynamic slot
template <::std::integral char_type, typename scatter_type, typename component_type>
[[nodiscard]] inline consteval ::fast_io::basic_io_scatter_t<scatter_type>
compiled_scatter_plan_static_descriptor() noexcept
{
	if constexpr (component_type::is_static)
	{
		// Literal components contribute their final address and length during constant evaluation.
		static_assert(::std::same_as<char_type, typename component_type::char_type>);
		if constexpr (::std::same_as<scatter_type, void>)
		{
			// Byte-scatter descriptors express the literal length in bytes.
			return {component_type::storage.elements, component_type::size * sizeof(char_type)};
		}
		else
		{
			// Typed scatter descriptors express the literal length in character units.
			return {component_type::storage.elements, component_type::size};
		}
	}
	else
	{
		// Dynamic components leave a zero descriptor for the runtime patch pass.
		return {};
	}
}

/// @brief Stores compile-time component reuse and sorted argument-index data without hosted `std::array`.
template <::std::size_t count>
struct compiled_scatter_plan_position_table
{
	::std::size_t elements[count];
	::std::size_t sorted_argument_indices[count];
	::std::size_t dynamic_count;
};

/// @brief Builds the first-earlier-component table for every repeated dynamic argument.
/// @details Repeated components are emitted in source order. Copying the first materialized descriptor into every
///          later occurrence avoids both a second source CPO invocation and a separate argument-indexed runtime cache.
///          That cache cannot be sized by public arity: a single sparse index would otherwise create an unrelated large
///          stack object. A recursive pack search is also unsuitable because N components produce O(N^2) template
///          instantiations and O(N) template depth. This consteval LSD radix pass instead instantiates each component
///          once and performs a fixed number of stable counting passes derived from `size_t` value bits. Its work and
///          temporary constant-evaluation storage are O(N), independent of the greatest public argument index. The final
///          table is compile-time data only and contributes no runtime stack storage.
template <typename... component_types>
[[nodiscard]] inline consteval auto compiled_scatter_plan_make_previous_dynamic_components() noexcept
{
	constexpr ::std::size_t count{sizeof...(component_types)};
	static_assert(count != 0u);
	struct position_record
	{
		::std::size_t argument_index;
		::std::size_t component_position;
	};

	compiled_scatter_plan_position_table<count> result{};
	position_record records[count]{};
	position_record scratch[count]{};
	for (::std::size_t position{}; position != count; ++position)
	{
		result.elements[position] = SIZE_MAX;
	}
	::std::size_t used{};
	::std::size_t component_position{};
	([&]<typename component_type>() constexpr {
		if constexpr (!component_type::is_static)
		{
			records[used++] = {component_type::index, component_position};
		}
		++component_position;
	}.template operator()<component_types>(),
	 ...);

	// A stable nibble-wise radix sort has deterministic linear work for every possible size_t representation, unlike an
	// open-addressed hash table whose collision pattern could consume a compiler's constant-evaluation step budget. Four
	// bits per pass avoids assuming that a target byte has eight bits.
	constexpr ::std::size_t radix_bits{4u};
	constexpr ::std::size_t radix_size{1u << radix_bits};
	constexpr ::std::size_t radix_mask{radix_size - 1u};
	constexpr ::std::size_t pass_count{
		(::std::numeric_limits<::std::size_t>::digits + radix_bits - 1u) / radix_bits};
	for (::std::size_t pass{}; pass != pass_count; ++pass)
	{
		::std::size_t bucket_sizes[radix_size]{};
		::std::size_t const shift{pass * radix_bits};
		for (::std::size_t position{}; position != used; ++position)
		{
			++bucket_sizes[(records[position].argument_index >> shift) & radix_mask];
		}
		::std::size_t bucket_offsets[radix_size]{};
		for (::std::size_t bucket{1u}; bucket != radix_size; ++bucket)
		{
			bucket_offsets[bucket] = bucket_offsets[bucket - 1u] + bucket_sizes[bucket - 1u];
		}
		for (::std::size_t position{}; position != used; ++position)
		{
			auto const bucket{(records[position].argument_index >> shift) & radix_mask};
			scratch[bucket_offsets[bucket]++] = records[position];
		}
		for (::std::size_t position{}; position != used; ++position)
		{
			records[position] = scratch[position];
		}
	}

	bool have_dynamic_group{};
	::std::size_t group_index{};
	::std::size_t group_first_position{};
	result.dynamic_count = used;
	for (::std::size_t position{}; position != used; ++position)
	{
		auto const record{records[position]};
		result.sorted_argument_indices[position] = record.argument_index;
		if (!have_dynamic_group || group_index != record.argument_index)
		{
			have_dynamic_group = true;
			group_index = record.argument_index;
			group_first_position = record.component_position;
		}
		else
		{
			// Stability preserves component order within an equal-index group, so this is the first earlier slot.
			result.elements[record.component_position] = group_first_position;
		}
	}
	return result;
}

/// @brief Owns all compile-time lookup policy for one component type pack.
/// @details The component pack is attached to the class, while component positions and public argument indices are
///          nested non-type templates. This shape is not cosmetic: GCC rejects the equivalent mixed
///          `<position, dependent_type_pack...>` variable-template reference inside the lock-step generic-lambda fold.
///          Separating those two dependent lists is both parser-stable and keeps the single radix table shared.
template <typename... component_types>
struct compiled_scatter_plan_component_metadata
{
	static inline constexpr auto table{
		::fast_io::details::decay::compiled_scatter_plan_make_previous_dynamic_components<
			component_types...>()};

	template <::std::size_t component_position>
	static inline constexpr ::std::size_t previous_component{
		table.elements[component_position]};

	/// @brief Reports whether a public argument index occurs without re-expanding the component pack.
	/// @details Direct and bound admission visit every public argument type. Repeating an N-component fold for each one
	///          would recreate O(N^2) constraint work for a dense plan. The radix-generated keys are already sorted, so
	///          this binary search performs O(log N) ordinary constant-evaluation steps and instantiates no component
	///          recursion. Storage remains component-sized even when the greatest argument index is sparse.
	template <::std::size_t argument_index>
	static inline constexpr bool argument_used = []() constexpr {
		::std::size_t first{};
		::std::size_t last{table.dynamic_count};
		while (first != last)
		{
			auto const middle{first + (last - first) / 2u};
			if (table.sorted_argument_indices[middle] < argument_index)
			{
				first = middle + 1u;
			}
			else
			{
				last = middle;
			}
		}
		return first != table.dynamic_count &&
			   table.sorted_argument_indices[first] == argument_index;
	}();
};

template <typename... component_types>
inline constexpr auto const &compiled_scatter_plan_previous_dynamic_components{
	::fast_io::details::decay::compiled_scatter_plan_component_metadata<
		component_types...>::table};

/// @brief Builds one final descriptor from a static component or its normalized runtime source.
/// @tparam char_type      the output character type
/// @tparam scatter_type   void for byte lengths, otherwise the typed scatter character type
/// @tparam component_type the plan component being materialized
/// @tparam dynamic_tuple  the array or tuple containing normalized runtime values
/// @param  values         the runtime values indexed by dynamic components
/// @return the fully materialized descriptor for this component
template <::std::integral char_type, typename scatter_type, typename component_type, typename dynamic_tuple>
[[nodiscard]] inline constexpr ::fast_io::basic_io_scatter_t<scatter_type>
compiled_scatter_plan_runtime_descriptor(dynamic_tuple const &values)
{
	if constexpr (component_type::is_static)
	{
		// Static components reuse the descriptor produced entirely during constant evaluation.
		return ::fast_io::details::decay::compiled_scatter_plan_static_descriptor<char_type, scatter_type,
																				  component_type>();
	}
	else
	{
		decltype(auto) value{
			::fast_io::details::decay::compiled_scatter_plan_runtime_value_exact<
				component_type::index>(values)};
		using value_type = ::std::remove_cvref_t<decltype(value)>;
		// Preserve the container's exact cv/ref expression. In particular, a tuple of mutable references must not be
		// silently changed to const merely because the tuple object itself is observed through const&.
		::fast_io::basic_io_scatter_t<char_type> const scatter{print_scatter_define(
			::fast_io::io_reserve_type<char_type, value_type>,
			::std::forward<decltype(value)>(value))};
		if constexpr (::std::same_as<scatter_type, void>)
		{
			// Byte-scatter output converts the normalized character count to bytes.
			return {scatter.base,
					::fast_io::details::intrinsics::mul_or_overflow_die(
						scatter.len, sizeof(char_type))};
		}
		else
		{
			// Typed scatter output preserves the normalized character descriptor.
			return scatter;
		}
	}
}

/// @brief Materializes one descriptor for the direct-build strategy.
/// @details Keeping the component pack inside `metadata_type` is required for GCC 15 portability. GCC rejects a
///          nested member-template lookup through `component_types...` in the generic-lambda form of the lock-step
///          fold even though Clang accepts the valid C++20 program. This ordinary helper receives only one metadata
///          type plus the already-zipped position/component pair, so no recursive pack indexing or parser-sensitive
///          dependent lookup is needed.
template <typename metadata_type, ::std::size_t component_position, typename component_type,
		  ::std::integral char_type, typename scatter_type, typename dynamic_tuple>
inline constexpr void compiled_scatter_plan_build_direct_component(
	::fast_io::basic_io_scatter_t<scatter_type> *scatters,
	dynamic_tuple const &values)
{
	constexpr ::std::size_t previous{
		metadata_type::template previous_component<component_position>};
	if constexpr (previous == SIZE_MAX)
	{
		scatters[component_position] = ::fast_io::details::decay::compiled_scatter_plan_runtime_descriptor<
			char_type, scatter_type, component_type>(values);
	}
	else
	{
		// The first occurrence already queried and converted this source in the same final representation.
		scatters[component_position] = scatters[previous];
	}
}

/// @brief Patches one dynamic descriptor in an already-copied static blueprint.
/// @details Static slots deliberately instantiate no runtime work. For a repeated dynamic source the left-to-right
///          fold guarantees that its first descriptor has already been materialized before this helper copies it.
template <typename metadata_type, ::std::size_t component_position, typename component_type,
		  ::std::integral char_type, typename scatter_type, typename dynamic_tuple>
inline constexpr void compiled_scatter_plan_patch_dynamic_component(
	::fast_io::basic_io_scatter_t<scatter_type> *scatters,
	dynamic_tuple const &values)
{
	if constexpr (!component_type::is_static)
	{
		constexpr ::std::size_t previous{
			metadata_type::template previous_component<component_position>};
		if constexpr (previous == SIZE_MAX)
		{
			// Only the first occurrence invokes the normalized source CPO.
			scatters[component_position] = ::fast_io::details::decay::compiled_scatter_plan_runtime_descriptor<
				char_type, scatter_type, component_type>(values);
		}
		else
		{
			scatters[component_position] = scatters[previous];
		}
	}
}

/// @brief Builds every descriptor directly from its corresponding plan component.
/// @details This direct-construction branch is selected when the current count/density heuristic does not select static
///          blueprint copying. The selection is an empirical policy, not a universal compiler-profitability proof.
/// @tparam char_type       the output character type
/// @tparam scatter_type    the descriptor value type
/// @tparam dynamic_tuple   the normalized runtime value container
/// @tparam component_types the ordered plan component types
/// @tparam index           the expanded descriptor positions
/// @param  scatters         the destination descriptor array
/// @param  values           the normalized values used by first dynamic occurrences
template <::std::integral char_type, typename scatter_type, typename dynamic_tuple,
		  typename... component_types,
		  ::std::size_t... index>
inline constexpr void compiled_scatter_plan_build_direct(
	::fast_io::basic_io_scatter_t<scatter_type> *scatters,
	dynamic_tuple const &values,
	::std::index_sequence<index...>)
{
	using metadata_type = ::fast_io::details::decay::compiled_scatter_plan_component_metadata<component_types...>;
	(::fast_io::details::decay::compiled_scatter_plan_build_direct_component<
		 metadata_type, index, component_types, char_type>(scatters, values),
	 ...);
}

/// @brief Patches only dynamic positions after copying the compile-time blueprint.
/// @tparam char_type       the output character type
/// @tparam scatter_type    the descriptor value type
/// @tparam dynamic_tuple   the normalized runtime value container
/// @tparam component_types the ordered plan component types
/// @tparam index           the expanded descriptor positions
/// @param  scatters         the copied blueprint to patch
/// @param  values           the normalized values used by first dynamic occurrences
template <::std::integral char_type, typename scatter_type, typename dynamic_tuple,
		  typename... component_types,
		  ::std::size_t... index>
inline constexpr void compiled_scatter_plan_patch_dynamic(
	::fast_io::basic_io_scatter_t<scatter_type> *scatters,
	dynamic_tuple const &values,
	::std::index_sequence<index...>)
{
	using metadata_type = ::fast_io::details::decay::compiled_scatter_plan_component_metadata<component_types...>;
	(::fast_io::details::decay::compiled_scatter_plan_patch_dynamic_component<
		 metadata_type, index, component_types, char_type>(scatters, values),
	 ...);
}

/// Owns the one immutable character object used by an all-static compiled plan.
///
/// The one-element fallback keeps the type valid for a pack of empty literals;
/// `size` remains zero and no pointer is passed to an output operation in that
/// case.
template <::std::integral char_type, ::std::size_t size>
struct compiled_scatter_plan_merged_static_storage
{
	char_type elements[size == 0u ? 1u : size]{};
};

/// Computes the number of code units contributed by type-level literal slots.
template <typename... component_types>
[[nodiscard]] inline consteval ::std::size_t
compiled_scatter_plan_static_character_count() noexcept
{
	::std::size_t total{};
	([&]<typename component_type>() consteval {
		if constexpr (component_type::is_static)
		{
			total = ::fast_io::details::decay::
				print_contiguous_char_extent_add_or_unavailable<
					typename component_type::char_type>(
					total, component_type::size);
		}
	}.template operator()<component_types>(),
	 ...);
	return total;
}

/// Concatenates every type-level literal component into one static object.
///
/// This construction is intentionally limited to structural NTTP providers.
/// A character-array function parameter retains only its extent in the
/// specialization type; the caller's bytes cannot become a new template
/// argument inside the callee. Keeping this boundary explicit prevents an
/// incorrect first-call static cache for same-extent, different-content calls.
template <::std::integral char_type, typename... component_types>
[[nodiscard]] inline consteval auto
compiled_scatter_plan_make_merged_static_storage() noexcept
{
	constexpr ::std::size_t size{
		::fast_io::details::decay::
			compiled_scatter_plan_static_character_count<component_types...>()};
	static_assert(size != SIZE_MAX,
				  "fast_io: merged static scatter-plan storage exceeds one contiguous object");
	::fast_io::details::decay::compiled_scatter_plan_merged_static_storage<
		char_type, size>
		result{};
	::std::size_t position{};
	([&]<typename component_type>() consteval {
		if constexpr (component_type::is_static)
		{
			static_assert(::std::same_as<
						  char_type, typename component_type::char_type>);
			for (::std::size_t index{}; index != component_type::size; ++index)
			{
				result.elements[position++] =
					component_type::storage.elements[index];
			}
		}
	}.template operator()<component_types>(),
	 ...);
	return result;
}

} // namespace details::decay

/// @brief Compiles a fixed scatter layout into static literal descriptors and runtime patch positions.
/// @details Plans admitted by the platform-independent count/density heuristic copy a constant descriptor blueprint
///          and patch only dynamic slots. All other plans build descriptors directly. These are the two mechanisms;
///          the heuristic records the current tuning policy rather than proving either mechanism profitable
///          universally.
/// @tparam char_type       the output character type
/// @tparam component_types the ordered literal and dynamic component types
template <::std::integral char_type, typename... component_types>
	requires(sizeof...(component_types) != 0 &&
			 (::fast_io::details::decay::compiled_scatter_component_for_v<char_type, component_types> && ...))
struct basic_compiled_scatter_plan
{
	using output_char_type = char_type;
	static inline constexpr ::std::size_t scatter_count{sizeof...(component_types)};
	static inline constexpr ::std::size_t dynamic_count{
		(static_cast<::std::size_t>(!component_types::is_static) + ...)};
	static inline constexpr ::std::size_t merged_static_size{
		::fast_io::details::decay::
			compiled_scatter_plan_static_character_count<component_types...>()};
	/// Provider-owned merged bytes for the zero-dynamic-slot direct path.
	///
	/// Dynamic plans may still instantiate the value as harmless metadata, but
	/// only an all-static plan may expose its address as the complete record.
	static inline constexpr auto merged_static_storage
#if (defined(__GNUC__) || defined(__clang__)) && !defined(_WIN32)
		__attribute__((visibility("hidden")))
#endif
		{::fast_io::details::decay::
			 compiled_scatter_plan_make_merged_static_storage<
				 char_type, component_types...>()};
	// Runtime arity is one greater than the largest referenced dynamic index. Public calls therefore supply every
	// positional argument in [0, N], but plan components may leave holes and those supplied values remain unobserved.
	// This predicate rejects the unrepresentable SIZE_MAX index; exact public arity is checked separately.
	static inline constexpr bool dynamic_argument_domain_valid{
		(::fast_io::details::decay::compiled_scatter_plan_component_index_in_range_v<SIZE_MAX,
																					 component_types> &&
		 ...)};
	static inline constexpr ::std::size_t runtime_argument_count{[]() constexpr {
		::std::size_t count{};
		([]<typename component_type>(::std::size_t &result) constexpr {
			if constexpr (!component_type::is_static)
			{
				if constexpr (component_type::index != SIZE_MAX)
				{
					constexpr ::std::size_t candidate{component_type::index + 1u};
					if (result < candidate)
					{
						result = candidate;
					}
				}
			}
		}.template operator()<component_types>(count),
		 ...);
		return count;
	}()};

	/// @brief Reports whether the plan observes one position in its exact public runtime domain.
	/// @details The mask is shared by constraints, normalization, and descriptor materialization. Using one definition
	///          prevents a plan from documenting an index hole as unobserved while still aliasing, forwarding, storing,
	///          or constraining the corresponding caller object.
	template <::std::size_t index>
	static inline constexpr bool dynamic_argument_used{
		::fast_io::details::decay::compiled_scatter_plan_component_metadata<
			component_types...>::template argument_used<index>};

	template <::std::size_t argument_count>
	static inline constexpr bool dynamic_indices_in_range{
		dynamic_argument_domain_valid &&
		(::fast_io::details::decay::compiled_scatter_plan_component_index_in_range_v<argument_count,
																					 component_types> &&
		 ...)};

	template <typename dynamic_tuple>
	static inline constexpr bool runtime_values_acceptable{
		dynamic_indices_in_range<runtime_argument_count> &&
		::fast_io::details::decay::compiled_scatter_plan_runtime_values_v<
			char_type, runtime_argument_count, dynamic_tuple, component_types...>};

	template <typename dynamic_tuple>
	static inline constexpr bool bound_runtime_values_acceptable{
		dynamic_indices_in_range<runtime_argument_count> &&
		::fast_io::details::decay::compiled_scatter_plan_bound_runtime_values_v<
			char_type, runtime_argument_count, dynamic_tuple, component_types...>};

	/// @brief Applies the direct-source proof only to an argument position which the plan actually references.
	template <::std::size_t index, typename source_type>
	static consteval bool direct_source_acceptable() noexcept
	{
		if constexpr (!dynamic_argument_used<index>)
		{
			return true;
		}
		else
		{
			return ::fast_io::details::decay::compiled_scatter_plan_direct_source_v<
				char_type, ::std::remove_reference_t<source_type> &>;
		}
	}

	template <typename... Args, ::std::size_t... index>
	static consteval bool direct_sources_acceptable_impl(::std::index_sequence<index...>) noexcept
	{
		// Expanding both packs in the same pattern pairs each source with its public position directly. Repeated
		// pre-C++26 pack indexing would otherwise create a recursive Ith-type search for every argument.
		return ([]<::std::size_t source_index, typename source_type>() consteval {
			return direct_source_acceptable<source_index, source_type>();
		}.template operator()<index, Args>() &&
				...);
	}

	template <typename... Args>
	static inline constexpr bool direct_sources_acceptable{
		direct_sources_acceptable_impl<Args...>(::std::make_index_sequence<sizeof...(Args)>{})};

	/// @brief Applies the owning-source proof only to a referenced argument position.
	/// @details The selected branch, rather than `!used || trait<T>`, is necessary for substitution safety: a hole may
	///          intentionally expose deleted alias/status CPOs, and no part of the plan may instantiate that protocol.
	template <::std::size_t index, typename source_type>
	static consteval bool bound_source_acceptable() noexcept
	{
		if constexpr (!dynamic_argument_used<index>)
		{
			return true;
		}
		else
		{
			return ::fast_io::details::decay::compiled_scatter_plan_bound_source_v<
				char_type, source_type &&>;
		}
	}

	template <typename... Args, ::std::size_t... index>
	static consteval bool bound_sources_acceptable_impl(::std::index_sequence<index...>) noexcept
	{
		return ([]<::std::size_t source_index, typename source_type>() consteval {
			return bound_source_acceptable<source_index, source_type>();
		}.template operator()<index, Args>() &&
				...);
	}

	template <typename... Args>
	static inline constexpr bool bound_sources_acceptable{
		bound_sources_acceptable_impl<Args...>(::std::make_index_sequence<sizeof...(Args)>{})};
	// Apply the same fixed count/density heuristic on every compiler and architecture. The thresholds encode the current
	// platform-independent tuning policy, not a universal profitability proof; any target-specific replacement should
	// carry explicit measurement evidence rather than being implied by platform wording.
	static inline constexpr bool use_blueprint_copy{
		scatter_count >= 64u && dynamic_count <= scatter_count / 16u};

	template <typename scatter_type>
	static inline constexpr ::fast_io::basic_io_scatter_t<scatter_type> blueprint[scatter_count]
#if (defined(__GNUC__) || defined(__clang__)) && !defined(_WIN32)
		__attribute__((visibility("hidden")))
#endif
		{
			::fast_io::details::decay::compiled_scatter_plan_static_descriptor<char_type, scatter_type,
																			   component_types>()...};

	/// @brief Binds runtime values to this plan as a composable printable object.
	/// @tparam Args the runtime argument types
	/// @param  args the values referenced by scatter_dynamic components
	/// @return a printable object that retains ABI-decayed pre-alias source transports
	template <typename... Args>
		requires(dynamic_indices_in_range<sizeof...(Args)> &&
				 sizeof...(Args) == runtime_argument_count &&
				 ((sizeof...(Args) == 0u && dynamic_count == 0u) ||
				  (sizeof...(Args) != 0u && bound_sources_acceptable<Args...>)))
	[[nodiscard]] inline constexpr auto operator()(Args &&...args) const
	{
		return bind_arguments(::std::make_index_sequence<sizeof...(Args)>{},
							  ::std::forward<Args>(args)...);
	}

	/// @brief Emits the compiled plan under one checked output-operation lifetime.
	/// @tparam output the output stream type
	/// @tparam Args   the runtime argument types
	/// @param  outstm the destination stream
	/// @param  args   the values referenced by scatter_dynamic components
	template <typename output, typename... Args>
		requires(::fast_io::details::decay::compiled_scatter_plan_public_output_v<char_type, output> &&
				 dynamic_indices_in_range<sizeof...(Args)> &&
				 sizeof...(Args) == runtime_argument_count &&
				 ((sizeof...(Args) == 0u && dynamic_count == 0u) ||
				  (sizeof...(Args) != 0u && direct_sources_acceptable<Args...>)))
	inline constexpr void print(output &&outstm, Args &&...args) const
	{
		// Compiled scatter plans obey the same owner-lifetime rule as the ordinary
		// print front door; plan selection must not bypass temporary finish.
		::fast_io::operations::basic_output_operation_guard<output &&> guard{outstm};
		decltype(auto) outref = guard.ref();
		if constexpr (sizeof...(Args) == 0u)
		{
			// Fully static plans need no runtime source normalization.
			::fast_io::containers::tuple<> values;
			print_output(outref, values);
		}
		else
		{
			// Dynamic plan positions normalize and bind the supplied runtime sources.
			print_arguments(outref, ::std::make_index_sequence<sizeof...(Args)>{},
							::std::forward<Args>(args)...);
		}
		guard.commit();
	}

	/// @brief Materializes descriptors and performs one high-level scatter-output dispatch through a stable output borrow.
	/// @details Descriptor storage obeys the global print stack policy and switches to one dynamic allocation when the
	///          fixed descriptor array would exceed the allowed stack size. The caller must keep `outstm` live for this
	///          synchronous call; it may be caller-owned or materialized by an outer boundary. That precondition is
	///          established by either `print`, `print_define`, or the public by-value `emit` compatibility boundary below.
	///          Copying the observer again cannot extend any descriptor lifetime and prevents otherwise valid move-only
	///          normalized observers. The backend may split or retry the scatter dispatch.
	/// @tparam output        the decayed output stream reference type
	/// @tparam dynamic_tuple the normalized runtime value container
	/// @param  outstm        the destination stream reference
	/// @param  values        the values used to fill dynamic positions
	template <typename output, typename dynamic_tuple>
		requires(::fast_io::details::decay::compiled_scatter_plan_final_output_v<char_type, output> &&
				 runtime_values_acceptable<dynamic_tuple>)
	inline static constexpr void emit_borrowed(
		output &outstm, dynamic_tuple const &values)
	{
		if constexpr (
			dynamic_count == 0u &&
			::fast_io::details::decay::
				print_output_retains_static_scatter<output> &&
			::fast_io::details::decay::
				print_has_preferred_direct_write_operations<output>)
		{
			// An all-static structural plan owns one genuinely immutable object.
			// Direct synchronous endpoints can therefore issue a single write from
			// that object without materializing either payload or descriptors.
			if constexpr (merged_static_size != 0u)
			{
				auto const first{merged_static_storage.elements};
				::fast_io::details::decay::print_write_all_direct(
					outstm, first, first + merged_static_size);
			}
			return;
		}
		using scatter_type = ::std::conditional_t<
			::fast_io::details::decay::print_uses_byte_scatter_representation<output>,
			::fast_io::io_scatter_t, ::fast_io::basic_io_scatter_t<char_type>>;
		if constexpr (::fast_io::details::decay::print_stack_buffer_size_within_limit<scatter_count, scatter_type>)
		{
			// The final component array is the only descriptor-array/cache storage. Repeated dynamic slots copy their first
			// earlier component in place, so sparse public indices and repeated components require no second cache array.
			scatter_type scatters[scatter_count];
			build(scatters, values);
			::fast_io::details::decay::print_scatter_write_all_dispatch(outstm, scatters, scatter_count);
		}
		else
		{
			// Plans above the central byte budget use one exact-size dynamic descriptor allocation.
			::fast_io::details::local_operator_new_array_ptr<scatter_type> scatters(scatter_count);
			build(scatters.ptr, values);
			::fast_io::details::decay::print_scatter_write_all_dispatch(outstm, scatters.ptr, scatter_count);
		}
	}

	/// @brief Emits a bound plan after recreating aliases from its stable pre-alias source storage.
	/// @details This path is distinct from low-level `emit_borrowed`, whose caller already supplies normalized values.
	///          Deferred normalization is what keeps a scatter returned by an rvalue string alias tied to the string now
	///          owned by the bound tuple. Every used public index is normalized once; holes are never inspected.
	template <typename output, typename dynamic_tuple>
		requires(::fast_io::details::decay::compiled_scatter_plan_final_output_v<char_type, output> &&
				 bound_runtime_values_acceptable<dynamic_tuple>)
	inline static constexpr void emit_bound_borrowed(
		output &outstm, dynamic_tuple const &values)
	{
		if constexpr (runtime_argument_count == 0u)
		{
			::fast_io::containers::tuple<> normalized_values;
			emit_borrowed(outstm, normalized_values);
		}
		else
		{
			emit_bound_arguments(
				outstm, values, ::std::make_index_sequence<runtime_argument_count>{});
		}
	}

	/// @brief Preserves the historical owning `emit` boundary for direct low-level callers.
	/// @details Passing a normalized observer expression directly to this function remains a deliberate by-value
	///          compatibility boundary: an lvalue follows ordinary parameter-copy semantics, and a prvalue move-only
	///          observer is materialized exactly once. Internal paths that have already established a lifetime-valid
	///          observer call `emit_borrowed` instead, so this wrapper is never reopened recursively.
	template <typename output, typename dynamic_tuple>
		requires(::fast_io::details::decay::compiled_scatter_plan_final_output_v<char_type, output> &&
				 runtime_values_acceptable<dynamic_tuple>)
	inline static constexpr void emit(
		output outstm, dynamic_tuple const &values)
	{
		emit_borrowed(outstm, values);
	}

private:
	/// @brief Selectively retains one pre-alias bound source, replacing an index hole with an empty internal value.
	/// @details The used branch applies only ABI-aware source transport. It deliberately does not call alias or status
	///          forwarding: an rvalue must first become stable owned storage so a later alias may safely point into it.
	///          The hole branch forms no protocol expression involving `source_type`.
	template <::std::size_t index, typename source_type>
	[[nodiscard]] inline static constexpr decltype(auto) bind_argument(source_type &&source)
	{
		if constexpr (dynamic_argument_used<index>)
		{
			return ::fast_io::details::io_print_forward_transport_for<char_type>(
				::std::forward<source_type>(source));
		}
		else
		{
			return ::fast_io::details::decay::compiled_scatter_plan_unused_argument{};
		}
	}

	template <typename... Args, ::std::size_t... index>
	[[nodiscard]] inline static constexpr auto bind_arguments(
		::std::index_sequence<index...>, Args &&...args)
	{
		return [&]<typename... forwarded_types>(forwarded_types &&...forwarded) constexpr {
			using plan_type = basic_compiled_scatter_plan;
			return ::fast_io::details::decay::compiled_scatter_plan_bound<
				plan_type, ::std::remove_cvref_t<forwarded_types>...>{
				::fast_io::details::decay::compiled_scatter_plan_tuple(
					::std::forward<forwarded_types>(forwarded)...)};
		}(bind_argument<index>(::std::forward<Args>(args))...);
	}

	/// @brief Selects the exact retained representation for one direct argument.
	/// @details A raw retained source is borrowed without requiring an alias protocol. Otherwise the ordinary one-decay
	///          normalization is evaluated once. Unused positions produce only the internal placeholder. This per-index
	///          selection also handles a mixed pack containing both raw-only and normalization-only sources, which a
	///          single all-or-nothing pack branch cannot represent safely.
	template <::std::size_t index, typename source_type>
	[[nodiscard]] inline static constexpr decltype(auto) direct_argument(source_type &source)
	{
		if constexpr (!dynamic_argument_used<index>)
		{
			return ::fast_io::details::decay::compiled_scatter_plan_unused_argument{};
		}
		else if constexpr (
			::fast_io::details::decay::retained_scatter_printable_v<char_type, source_type &>)
		{
			return (source);
		}
		else
		{
			return ::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(source));
		}
	}

	template <typename output, typename... Args, ::std::size_t... index>
	inline static constexpr void print_arguments(
		output &outstm, ::std::index_sequence<index...>, Args &&...args)
	{
		// The generic lambda extends every normalized prvalue through the synchronous output call. Its tuple contains
		// references only; it neither copies a raw retained source nor moves a normalized source a second time.
		[&]<typename... forwarded_types>(forwarded_types &&...forwarded) constexpr {
			auto values{::fast_io::containers::forward_as_tuple(forwarded...)};
			print_output(outstm, values);
		}(direct_argument<index>(args)...);
	}

	/// @brief Recreates one normalized emission value from stable bound source storage.
	template <::std::size_t index, typename dynamic_tuple>
	[[nodiscard]] inline static constexpr decltype(auto)
	bound_emit_argument(dynamic_tuple const &values)
	{
		if constexpr (!dynamic_argument_used<index>)
		{
			return ::fast_io::details::decay::compiled_scatter_plan_unused_argument{};
		}
		else
		{
			decltype(auto) storage{
				::fast_io::details::decay::compiled_scatter_plan_runtime_value_exact<index>(values)};
			decltype(auto) source{
				::fast_io::details::decay::compiled_scatter_plan_bound_storage_source(storage)};
			return ::fast_io::io_print_forward<char_type>(::fast_io::io_print_alias(source));
		}
	}

	/// @brief Normalizes every referenced bound source exactly once and emits while those results remain alive.
	template <typename output, typename dynamic_tuple, ::std::size_t... index>
	inline static constexpr void emit_bound_arguments(
		output &outstm, dynamic_tuple const &values, ::std::index_sequence<index...>)
	{
		[&]<typename... forwarded_types>(forwarded_types &&...forwarded) constexpr {
			auto normalized_values{::fast_io::containers::forward_as_tuple(forwarded...)};
			emit_borrowed(outstm, normalized_values);
		}(bound_emit_argument<index>(values)...);
	}

	/// @brief Applies output locking before building and writing final component descriptors.
	template <typename output, typename dynamic_tuple>
	inline static constexpr void print_output(
		output &outstm, dynamic_tuple const &values)
	{
		if constexpr (
			::fast_io::operations::decay::defines::has_output_or_io_stream_mutex_ref_define<output>)
		{
			if constexpr (
				::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<output>)
			{
				// Mutex-bearing streams hold their lock across descriptor construction and the scatter write. The unlocked
				// CPO may return either a stable reference or a move-only prvalue. `decltype(auto)` preserves the former and
				// owns the latter once; recursion then borrows that exact object instead of copying at every wrapper layer.
				::fast_io::operations::decay::stream_ref_decay_lock_guard guard{
					::fast_io::operations::decay::output_stream_mutex_ref_decay(outstm)};
				decltype(auto) unlocked_output =
					::fast_io::operations::decay::output_stream_unlocked_ref_decay(outstm);
				print_output(unlocked_output, values);
			}
			else
			{
				static_assert(
					::fast_io::operations::decay::defines::has_complete_output_stream_mutex_protocol<output>,
					"fast_io: compiled scatter output requires a complete, type-progressing mutex protocol");
			}
		}
		else
		{
			// Unlocked stream references can emit immediately without reopening the public ownership boundary.
			emit_borrowed(outstm, values);
		}
	}

	/// @brief Builds the descriptor array using the selected compile-time strategy.
	/// @tparam scatter_type  the destination descriptor type
	/// @tparam dynamic_tuple the normalized runtime value container
	/// @param  scatters      the destination descriptor array
	/// @param  values        normalized values indexed by dynamic components
	template <typename scatter_type, typename dynamic_tuple>
	inline static constexpr void build(scatter_type *scatters, dynamic_tuple const &values)
	{
		using scatter_value_type = typename scatter_type::value_type;
		if constexpr (use_blueprint_copy)
		{
			// Plans selected by the count/density heuristic copy constant descriptors and patch dynamic positions.
			::fast_io::freestanding::my_memcpy(
				scatters, blueprint<scatter_value_type>, sizeof(scatter_type) * scatter_count);
			::fast_io::details::decay::compiled_scatter_plan_patch_dynamic<
				char_type, scatter_value_type, dynamic_tuple, component_types...>(
				scatters, values, ::std::make_index_sequence<scatter_count>{});
		}
		else
		{
			// Plans not selected by that heuristic build each descriptor directly in final order.
			::fast_io::details::decay::compiled_scatter_plan_build_direct<
				char_type, scatter_value_type, dynamic_tuple, component_types...>(
				scatters, values, ::std::make_index_sequence<scatter_count>{});
		}
	}
};

/// @brief Creates a compiled scatter plan from literal and dynamic components.
/// @tparam char_type       the output character type
/// @tparam component_types the deduced component types
/// @return an empty stateless plan whose layout and blueprint are compile-time data
template <::std::integral char_type, typename... component_types>
	requires(sizeof...(component_types) != 0 &&
			 (::fast_io::details::decay::compiled_scatter_component_for_v<char_type, component_types> && ...))
[[nodiscard]] inline consteval auto make_scatter_plan(component_types...) noexcept
{
	return ::fast_io::basic_compiled_scatter_plan<char_type, ::std::remove_cvref_t<component_types>...>{};
}

/// @brief Emits a bound compiled scatter plan through the ordinary print customization protocol.
/// @tparam char_type     the output character type
/// @tparam output        the decayed output stream reference type
/// @tparam plan_type     the compiled scatter plan type
/// @tparam dynamic_types the retained pre-alias source transport types
/// @param  outstm        the destination stream reference
/// @param  bound         the plan and its stable pre-alias source transports
template <::std::integral char_type, typename output, typename plan_type, typename... dynamic_types>
	requires(::fast_io::details::decay::compiled_scatter_plan_final_output_v<char_type, output> &&
			 ::std::same_as<char_type, typename plan_type::output_char_type> &&
			 plan_type::template dynamic_indices_in_range<sizeof...(dynamic_types)> &&
			 sizeof...(dynamic_types) == plan_type::runtime_argument_count &&
			 plan_type::template bound_runtime_values_acceptable<
				 ::fast_io::containers::tuple<dynamic_types...>>)
inline constexpr void print_define(
	::fast_io::io_reserve_type_t<char_type,
								 ::fast_io::details::decay::compiled_scatter_plan_bound<plan_type, dynamic_types...>>,
	output &outstm,
	::fast_io::details::decay::compiled_scatter_plan_bound<plan_type, dynamic_types...> const &bound)
{
	// The bound object is publicly constructible, so repeat the retention proof at the protocol boundary instead of
	// relying solely on the plan factory. This closes the direct-aggregate bypass for an unmarked shared-scratch type.
	// `bound_runtime_values_acceptable` applies deferred-normalization proof only at referenced indices, so internal hole
	// placeholders remain inert. The tuple owns pre-alias rvalue sources and retains lvalue transports; aliasing them only
	// now prevents a descriptor from outliving its backing source. Observing the bound wrapper by const reference also
	// avoids copying a move-only source merely to enter the output protocol.
	plan_type::emit_bound_borrowed(outstm, bound.dynamic_values);
}

} // namespace fast_io
